# Final Conservation And Consumer Evidence

Status: `PASS`

Evidence class: **Ran + Static** at frozen commit
`de520f1ff867ca5c65b1f82dfe32a19c213ae18c`.

## Result

Fresh post-correction output reads close every requested water, groundwater,
sediment, snow/frost, publication, and W11B consumer claim. In particular,
the H2637 manifest now publishes the four timing-qualified groundwater
operands that were absent from the earlier campaign: initial storage `S0`,
terminal pre-export storage `SN`, terminal-day baseflow `QbN`, and terminal-day
deep seepage `QsN`. Both the recurrence-to-`SN` and the complete post-export
run ledger close within `4.26e-11 m3`. No blocker was found.

This artifact does not reuse the pre-correction groundwater conclusion. The
H2637 output was regenerated by the final campaign at the frozen commit; snow,
frost, W11B CLI, same-grid, and baseflow-once consumers were rerun separately
for this reconstruction. P61, p102, H2637, snow/frost, and W11B serialized
surfaces were independently decoded after those runs.

## Operand Lineage And Rejected Aliases

| Claim | Authoritative operands; units and basis | Real consumer / output | Rejected alias |
| --- | --- | --- | --- |
| H2637 surface | source, routed outlet, end-window storage, clamp; `m3` over the 731-day hillslope run | active routing summary in the generated run manifest; HBP/pass are production outputs | precipitation magnitude, pass `runvol`, shadow or compatibility counters |
| H2637 groundwater | `S0`, `SN`, `QbN`, `QsN`, cumulative recharge/baseflow/deep seepage; `m3` on the manifest's explicit pre-/post-export timing | runner manifest; HBP groundwater fields are parsed into watershed contributions | latest runoff-EVENT HBP baseflow as terminal-day `QbN`; `latqcc`; `cbase`; inferred storage |
| P61 | pass `runvol` in `m3`, five `sedcon` columns in `kg/m3`, HBP detachment/deposition/hourly mass in `kg`, class fractions dimensionless | strict HBP intake plus pass-Parquet consumer | unit width, adjacent concentration, peak discharge, producer aggregation helper |
| P102 hillslope | HBP chain detachment/deposition/hourly class mass in `kg`; event-day pass `runvol` and HBP hourly volumes in `m3` | strict HBP intake and pass inventory | outlet-OFE detachment as chain detachment; a different day with similar totals; zero fill |
| P102 publication | public `tdet`, `tdep`, `sed_del`, and EBE yield in `kg`; public/EBE runoff in `m3` | `totalwatsed3.parquet` and `ebe_pw0.parquet` | raw `tdet-tdep` as routed `sed_del` |
| Snow | `P`, `RM`, `Snow-Water` in `mm` over one OFE-day | production WAT and next-day carry | physical `Snow-Depth`; snowbench output |
| Frost | liquid `Total-Soil`, frozen `frozwt`, and external fluxes in `mm` over one OFE-day | production WAT on dry, unchanged-snow rows | frost depth `frdp`; freeze/thaw transfer as external water |
| W11B | HBP hourly water in `m3`, hourly/class sediment in `kg`, terminal EBE outflow/yield, channel storage | watershed CLI, typed interval states, EBE and channel-water-balance Parquet | scalar peak/duration replay; upstream flow or baseflow re-added as downstream local source |

## H2637 Surface And Complete Groundwater Recurrence

**Ran:** the final H2637 active-owner test passed in `495.225 s`. The fresh
manifest reports 731 days seen and 610 routed. Independent surface arithmetic
is:

```text
374420.2511558311 source
- 371254.38460113516 routed outlet
-   3165.866554692616 end-window storage
-      4.9829504321846435e-14 clamp
=      3.322334316072185e-9 m3
```

The relative residual is `8.873276233900748e-15`.

The newly observable groundwater operands are:

```text
S0   =    0.0 m3
R    = 3668.610172576748 m3
Qb   = 3547.636225849919 m3
Qs   =    0.0 m3
SN   =  126.01452784040274 m3
QbN  =    5.04058111361611 m3
QsN  =    0.0 m3
```

On the contract's terminal pre-export timing,
`S0 + R - (Qb-QbN) - (Qs-QsN) = 126.01452784044523 m3`; the
`SN - reconstructed` residual is `-4.249045559845399e-11 m3`. After the
terminal export, `SN-QbN-QsN = 120.97394672678662 m3`, while the independent
full-run ledger `S0+R-Qb-Qs = 120.97394672682913 m3`; residual
`-4.250466645316919e-11 m3`.

The latest runoff-EVENT HBP contains `5.032033091000001 m3` baseflow, not
`QbN`; this explicitly rejects the old/latest-event alias because the latest
runoff event is not necessarily the terminal simulation day. The real
watershed HBP parser reads groundwater fields at
`watershed_supervisor.rs:506-559`, and the CLI maps them into
`HillslopeContribution` at `openwepp-cli-watershed.rs:514-557`.

Hashes: manifest
`fdb03a4ff05f03da0a5aeacee24cbd9b836ab922b10581b527c556932e5f7907`,
HBP `378a8c1d80a22c9452fb256cf9a95eab09035f3a6cd387c6d626ab26c426c453`,
pass Parquet
`915f3b99c2ff20e3e0632b4e90a6ceb1cb8e7fee58f0d3e29b41de10c540f550`.

## P61 And P102 Closure

**Ran:** the fresh p61 outputs close all three independent views:

```text
HBP detachment - deposition - sum(hourly sediment) = 4.533831088338047e-10 kg
pass sum(sedcon_1..5) * runvol - tdet       = -9.094947017729282e-13 kg
pass runvol - HBP sum(hourly runoff)        =  1.136868377216160e-13 m3
sum(class fractions)                        =  1.0
minimum class fraction                      =  0.026647828384138175
```

P61 HBP and pass hashes are
`edcba9b5463a994d0ec77a02e0b0c091d6a625beaf595618754f152be3eb4524`
and `b03896969524d8d0df78f80cba252e6f7ee07eb7e52cc9651211e7671f6f6c40`.

**Ran:** p102 chain export is `584.2332653870001 - 282.14618621700004 =
302.08707917000004 kg`; the HBP hourly export is
`302.08707916989255 kg`, residual `1.074909050657880e-10 kg`. Its event-day
pass `runvol` and HBP hourly volumes are both `393.8589837616709 m3`, residual
exactly `0 m3`. Five class fractions sum to `1.0`, the minimum is
`0.03380000000000001`, and reconstructed class mass sums exactly to
`302.08707916989255 kg`.

The public routed `sed_del` and EBE yield are both
`302.13161177539132 kg`, exact residual `0 kg`. Their difference from raw
hillslope export is `0.04453260539128223 kg`, an output-level rejection of the
raw-export alias. Public and EBE runoff are both `395.60278125725654 m3`,
exact residual `0 m3`. P102 HBP, pass, total-water/sediment, and EBE hashes are
respectively
`42b4fc68ec749150e8360579295bebf77f373fd65c882e939c4792638a5181d4`,
`16b2d16d20243755a196f1ca199a4b14b838f05315d15556d38680195a6694e1`,
`2ebbc77e9a7b7f46289881d8e4b1d497e4e3fe8ce96b9f046c1c5a54ececc9f7`,
and `5fd2a11057fb6f137bbfe9e65e55469fb4976d730aac469c76950c1e753c4bf7`.

## Snow And Frost Ledgers

**Ran:** the focused production snow consumer passed. Its WAT hash is
`f8569f2bacc747e6954b8291da109e71d3448afebdc42f3c9979457e97bc61c6`.
Independent storage arithmetic gives `8.881784197001252e-16 mm` residual on
the accumulation day and exactly `0 mm` on the release day. Physical depths
`40.630422948035005 mm` then `0 mm` remain distinct from SWE and are not used
as the ledger operand.

**Ran:** the focused p313 production frost consumer passed. The WAT hash is
`6e8791348a1daf6bddcf8e6f0facdf8341fbc9efcd73ceb57bcec09e5a13ea30`.
On a dry freeze-growth row, combined liquid+frozen storage closes to
`8.526512829121202e-14 mm`. On year 4 Julian 290, frozen storage falls from
`2.2465604492615676` to `0.025598531884922018 mm`, liquid storage rises from
`311.84477731657813` to `312.91336100094156 mm`, and external sinks are
`1.1523782330132049 mm`; residual `-2.632616347142402e-14 mm`. Snow is
unchanged and `RM=0` on both selected rows, so the reconstruction does not
silently absorb snow exchange.

## P102 Serial/Parallel Semantic Identity

**Ran:** all 14 serial and four-worker Parquet products have equal row counts,
ordered values, null posture, field types/nullability, field metadata, and
schema metadata. HBP and pass bytes are also identical. Parquet file hashes
differ because serialization metadata ordering is not byte-canonical; this is
recorded rather than promoted to a false byte-identity requirement. Per-file
hashes and equality booleans are in `logs/final-reconstruction-arithmetic.log`.

## W11B Same-Grid, Timing, And Baseflow-Once

**Ran:** the fresh two-channel CLI consumer and both direct consumer tests
passed. Independent serialized-output arithmetic is:

| Shape | Input (`m3`) | Outlet (`m3`) | Storage (`m3`) | Water residual (`m3`) | Input/yield (`kg`) | Peak (`m3/s`) |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| spike | 7200 | 7089.739831820012 | 110.26016817998794 | `-5.684341886080802e-14` | 240 / 240 | 1.9999938168811557 |
| spread | 7200 | 7161.017078834353 | 38.982921165647085 | `-9.947598300641403e-14` | 240 / 240 | 0.5 |

Equal totals and different peaks reject scalar-total replay. Static binding
shows the interval kernel reads contributor hourly runoff and dependency
`q1` on the same grid (`hourly.rs:219-279`), and the active interval branch
returns before scalar routing (`direct.rs:174-180`). The direct class test
asserts downstream inlet classes equal upstream same-index egress. The
baseflow-once test injects `864 m3` only at the upstream hillslope, asserts
downstream local lateral/baseflow is zero, and closes external water plus
initial storage against terminal outflow plus both channels' storage
(`hourly_tests.rs:1535-1602`).

Spike H1/H2/EBE/channel-balance hashes are
`cbeeac392b3dca5b54d04249bd2be822e69760f1865d988ef87bb3c34a21911c`,
`63940f8f85a0378c9576b67df91cfd0fafb70695654eff1655466e9144f56038`,
`2041e970e407e8f49822f3fb83182d164a36b5ec29ee2e3e1445d71d309e1234`,
and `475fb765f2994c8e31a852a49dc966fd216bc180a388ac96b9246ed9f399312e`.
Spread counterparts are
`7c6c4a084c3e88514d78a7d54c95d62edacff722b9fa1173d9db05e905c62cdc`,
the same zero H2 hash,
`6fd92f455e54b0ba54f25933fd695fa4e7143cd7d3fc3c696274878e29d62e1d`,
and `65022766894da2ebe746928e3703fa6f375f7add1e383a391780bcad507eb761`.

## Ran Evidence

- `logs/final-02-h2637-native-active-owner-routes-and-closes.log`: final
  frozen-source H2637 generation and recurrence assertion.
- `logs/final-reconstruction-arithmetic.log`: independent HBP/Parquet reads,
  exact operands, residuals, semantic identity, and hashes.
- `logs/final-reconstruction-snow-production-test.log` and
  `logs/final-reconstruction-frost-production-test.log`: focused production
  reruns, both PASS.
- `logs/final-reconstruction-w11b-cli-consumer.log` and
  `logs/final-reconstruction-w11b-direct-consumers.log`: real CLI and typed
  same-grid/baseflow-once consumers, all three tests PASS.

No conservation, consumer-path, publication, or evidence-availability blocker
remains in this bounded reconstruction.
