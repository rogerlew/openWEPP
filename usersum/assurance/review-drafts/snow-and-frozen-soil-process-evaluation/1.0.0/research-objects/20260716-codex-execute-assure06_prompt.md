# Execute ASSURE-06

Scope: local openWEPP scientific-report engineering; flat-file reads and edits,
local deterministic evidence reconstruction, and disposable staging only; no
external-system writes or publication.

Execution mode: package-end-to-end. Execute all phases in `package.md` through
truthful terminal disposition unless a proven external authority boundary is
reached.

Required reading — Core: `AGENTS.md`, `docs/codex_exec_plans.md`,
`docs/work-packages/AGENTS.md`, `docs/ROADMAP.md`, this package,
`artifacts/required-reading-map.md`, ADR-0038, the V2 architecture/lifecycle/
source-build contracts, the scientific report standard, and
`assurance/v2/README.md`. Conditional: science-contract instructions and
`SC-SNOWFREEZE-001` before scientific synthesis; tests/crates instructions only
if those paths enter an amended write set. On-demand: the model narrative,
observation manifests, literature, and named retained evidence in the package
inventory. Required-reading budget: recorded in
`artifacts/required-reading-map.md`.

Files: only the package's declared write set.

Task: freeze the integrated-vs-split study decision, reconstruct a compact
result from identified retained evidence, author the snow/frost flagship
manuscript and supplement, bind the V2 source, exercise the disposable consumer,
review, verify, and disposition.

Constraints: scientific findings lead. Keep precipitation-phase, snowpack,
frozen-soil, conservation, and production-path evidence claim-specific. State
development/selection use of observations. Legacy and PySnobal are diagnostic
flags, not truth targets. Do not change physics, tune parameters, edit fixtures,
invent human authority, publish, export, release, or vendor.

Subagent requirement: REQUIRED. This prompt explicitly authorizes subagent
spawning/delegation to an internal domain-science reviewer, an independent
reproduction/publication reviewer, and two terminal verifiers for read-only
review. Outputs: compact findings and gate/identity dispositions returned to
the parent for package artifacts; write access: read-only. If a reviewed
amendment requires heavy closure runs, a heavy-gate-runner is authorized with
writes limited to package logs and scratch.

Autonomy: execute all package phases without additional user direction unless
hard-blocked. ASSURE-05 remains `HELD`/`DRAFT`; ASSURE-06 must not treat it as
approved.

Outputs: complete package artifacts, V2 source, deterministic staging evidence,
dual review/disposition, dual terminal verification, and a truthful
`HOLD-HUMAN-APPROVAL` handoff unless real human approval records exist.
