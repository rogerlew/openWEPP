# Execute ASSURE-05

Scope: local openWEPP scientific-report engineering; flat-file reads/edits,
local evidence runs, disposable staging, and read-only primary-source citation
checks only; no external-system writes.

Execution mode: package-end-to-end. Execute every phase in `package.md` through
truthful terminal disposition unless the declared human-approval boundary is
reached.

Required reading — Core: `AGENTS.md`, `docs/codex_exec_plans.md`,
`docs/work-packages/AGENTS.md`, `docs/work-packages/README.md`, ADR-0038, the v2
architecture/lifecycle/source-build contracts, the scientific report standard,
ASSURE-05 roadmap, this package, and `artifacts/required-reading-map.md`.
Core also includes `tests/AGENTS.md` after the package's bounded reproduction-
test amendment. Conditional: `crates/AGENTS.md` and builder source only if a
blocking defect requires a preauthorized amendment. On-demand:
`SC-GWBASEFLOW-001`, the model narrative, current report source, prior
groundwater evidence, and H2637 consumer paths. Required-reading budget: 453497
local bytes, `WARN`; map: `artifacts/required-reading-map.md`. The large package
catalog is retained as Core because it is the canonical execution/process map;
mechanism-specific evidence remains On-demand.

Files: only the declared write set. Kernels, schemas, tests, release tooling,
other reports, export/release/vendor trees, and WEPPcloud are read-only unless
the package is explicitly amended before an indispensable correction.

Task: regenerate the exact groundwater recurrence and production-transfer
evidence, independently reconstruct every material value, revise the manuscript
and supplement into a genuine scientific report, preserve public-safe research
objects, and exercise the real deterministic staging consumer.

Constraints: scientific findings lead; no status headline or aggregate
validation grade. Separate code/integration verification from empirical
accuracy. Do not attribute Priest River performance to openWEPP. Preserve units,
timing, sign conventions, rejected aliases, uncertainty, limitations, and
application boundary. The builder and agents cannot create human identity,
competence, approval, peer-review, release authority, or application fitness.

Conservation/output acceptance: record operand lineage before runs; separate
plausible aliases; reject latest-event and diagnostic operands; independently
reconstruct from produced outputs; use two-sided residual/allowance comparison;
do not close on producer self-consistency alone.

Subagent requirement: REQUIRED. This prompt explicitly authorizes subagent
spawning/delegation to one heavy fixture/closure runner, one internal
domain-science reviewer, one independent reproduction/publication reviewer, and
two terminal verifiers. Outputs are compact findings, metrics, exits, hashes,
timings, and log paths. Heavy-runner writes are bounded to package artifacts and
scratch; all reviewers are read-only. Subagents cannot populate human approvals.

Autonomy: execute all phases and update artifacts without requesting direction
until the declared human-accountability boundary or another proven external
blocker is reached.

Outputs: fresh study protocol, realization/path freeze, operand lineage,
retained evidence and result objects, revised manuscript/supplement/descriptor,
deterministic staging proof, internal scientific/reproduction/publication
reviews, finding disposition, heavy gates, dual terminal verification, and
either a verified publication or exact `HOLD-HUMAN-APPROVAL` handoff.
