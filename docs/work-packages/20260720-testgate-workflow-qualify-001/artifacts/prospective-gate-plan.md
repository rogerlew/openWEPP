# Prospective Qualification Gate Plan

Status: scaffold expectation; exact implementation intake selects current
commands and identities.

## Intake and local qualification

1. Instruction, status, package-base, write-set, and subject-freeze validation.
2. Qualification interface/schema validation and anti-fabrication inspection.
3. One `Q01`–`Q15` black-box matrix execution with bounded probe payloads.
4. Independent matrix schema/report validation, spawn-count reconciliation,
   receipt verification, ledger/hash-chain reconstruction, and subject rehash.

## Real heavy and provider qualification

1. Verify and reuse the current exact trusted combined-run receipt when
   eligible; otherwise exactly one ordinary TESTGATE execution after queue-idle
   preflight.
2. Verify single full-process execution, functional/LCOV/CRAP common lineage,
   timing thresholds or typed non-adoption, durable upload, hosted independent
   verification, and attestation.
3. Validate concurrency behavior with the frozen hermetic workflow harness and
   retained provider records; never manufacture overlapping live runs.

## Package closure

1. Dual independent result review and finding disposition.
2. Dual independent terminal verification of exact roots and all case/provider
   evidence.
3. Package-only Markdown lint, prompt inventory, path integrity, diff hygiene,
   and `.rs` line-count `NOT_APPLICABLE` record.

Successful implementation receipts are not rerun merely because qualification
adds documentation evidence. Any subject change invalidates the entire package.
