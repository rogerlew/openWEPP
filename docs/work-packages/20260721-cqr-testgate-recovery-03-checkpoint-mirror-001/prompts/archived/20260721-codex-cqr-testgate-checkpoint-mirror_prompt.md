# Archived Prompt: CQR TESTGATE Checkpoint Mirror

Execution mode: package-end-to-end.

Execute `20260721-cqr-testgate-recovery-03-checkpoint-mirror-001` within the sole
production module `crates/openwepp-gate-planner/src/checkpoint_mirror.rs`.
Preserve exact fail-closed root/path validation, directory and copy ordering,
error codes, bytes, and canonical checkpoint publication while reducing every
eligible row to CRAP `<= 30`.

Autonomy: characterize, refactor, measure, review, verify, and complete or hold
the package. This prompt explicitly authorizes subagent spawning/delegation to
independent review, verification, and comparator/closure-runner subagents for
eligibility, metrics, selected gates, behavior-identity review, and terminal
verification. Outputs are package-local review, verification, metric, and
command evidence. Write access is read-only unless explicitly assigned a
bounded change inside the declared package write set. Do not run
campaign-global TESTGATE; the master seven-package ExecPlan owns it.

Archived after corrected dual review, dual terminal verification, and
`EXECUTED-COMPLETE` disposition.
