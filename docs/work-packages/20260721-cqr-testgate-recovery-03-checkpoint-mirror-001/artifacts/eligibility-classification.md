# Eligibility Classification

Static: both rows are `E-PRODUCTION`. They control trusted filesystem roots,
symlink/special-entry rejection, output copying, and durable checkpoint
publication. Aggregate, function-floor, and CRAP treatment remain visible; no
retained exception is proposed.

Static: two independent read-only reviewers passed this classification at exact
clean `02745e8d`. Both verified the source hash, rows, real executor/resume
consumer paths, single-module scope, and authorization for whole-block helper
extraction. Both require direct characterization before production decomposition.
