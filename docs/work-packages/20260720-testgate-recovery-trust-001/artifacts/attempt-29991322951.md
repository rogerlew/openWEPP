# TESTGATE Attempt 29991322951

Static: GitHub push run `29991322951` targeted exact head
`5a77ca2fb7b4478f361565493805628c5debe866`.

Ran: trusted comparison admission, pinned toolchain verification, dependency
bootstrap, planner build, and durable-history restore passed. The exact
comparison base was the preceding pushed head
`8e55cb910a9252afd705254140ac94d8f121ea03`.

Ran: the terminal plan selected one documentation-lint node for the two changed
package files. That node passed, receipt
`0641b86403e9a2ea0b62a79f985213b91b1c0abb32c96f3714db0a8809309792`
reported PASS, the durable attempt closed PASS in 151.229 seconds, and no retry
ran. LIGHT beyond that selected node, HEAVY, full Nextest, and coverage did not
run.

Ran: unsigned artifact `8557267631` and authenticated recovery artifact
`8557279038` retain the exact plan, checkpoint, receipt, predicate, ledger, and
recovery archive. Hosted verification then failed before envelope verification
because its inline consumer read
`intent_authorization.package_path`; the producer schema contains
`intent_authorization.intent_package_path`.

Static: RTR-056 binds that verifier field drift. RTR-057 binds the separate
comparison-loss defect: attempt `29990800842` failed before planning, but its
pushed head became this run's default base, so the reviewed planner correction
was outside the selected diff. A fail-closed exact-head comparison-base
declaration may correct this only by expanding backward to an ancestor of the
event's `before` commit.

Ran: retained-predicate replay after correcting the field name exposed
RTR-058. The inline verifier also called the canonical intent-authority helper
with its obsolete four-argument signature, omitting the planner binary, exact
head, and output path required for independent reconstruction.

Ran: the corrected consumer reconstructed the retained intent-authority object
exactly with package-chain ID
`7822ec4b24209ae6ed90fb7da2cfd6ce87c0beda09802e97afcaba8ee649266e`.
The comparison-base and intent-package resolver suites pass 17/17, both owning
integration targets pass 21/21, all-target planner Clippy passes, and the
workflow YAML and affected Markdown validate.
