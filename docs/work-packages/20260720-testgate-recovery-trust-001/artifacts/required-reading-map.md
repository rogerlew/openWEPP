# Required Reading Map

Evidence class: Static.

Core instructions are root `AGENTS.md`, `docs/work-packages/AGENTS.md`,
`crates/AGENTS.md`, this package, `docs/codex_exec_plans.md`,
`docs/defect_closure_execplans.md`, and
`docs/standards/testing-and-gate-strategy.md`.

Mechanism reading is `pre_heavy.rs`, `resume.rs`, `executor.rs`, `main.rs`,
`package_validation.rs`, `tools/local_ci/testgate.py`,
`.github/workflows/testgate-shadow.yml`, and
`tools/ci/omarchy-runner/{README.md,manage.sh,Dockerfile}`.

Review evidence is the predecessor package's implementation gate evidence and
the two independent HOLD reports retained in this session. Science-contract
authority is not triggered because no simulation or kernel behavior is in
scope.
