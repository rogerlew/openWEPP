# Automatic Attempt 29984179443

Ran: exact changed head `9864a94c2bd5c55af07a43a1a9f24f10684a8c98`
passed checkout, intent admission, toolchain verification, dependency
bootstrap, planner build, authenticated history restoration, and the
superseded-head check.

Ran: LIGHT sealed 6/6 PASS in 313.557 seconds. The pre-HEAVY audit reached
`READY`. HEAVY admission then failed in 1.002 seconds with
`GATE-RESUME-PROVENANCE-MISSING` before any HEAVY node or subprocess started.
There was no retry.

The current HEAVY `STARTED` record named
`/testgate-history/recovery/29984179443-1`; resume discovery immediately treated
that just-created current root as a prior archive and required hosted
provenance that cannot exist until after the attempt. RTR-050 opened at durable
digest `d5b38ad3...`.

Ran: the always-run finalizer retained the five-record attempt chain, six LIGHT
checkpoints, terminal plan, audit, logs, and outputs. Hosted re-ingest then
failed because the downloaded artifact omitted 20 indexed hidden paths under
`.attempts`, `.checkpoints`, and `.work`. RTR-051 opened at durable digest
`ef48a69c...`.

Static: the three defunct pre-pivot Omarchy runner records remain historical
non-blocking metadata and did not affect this forest1 run.
