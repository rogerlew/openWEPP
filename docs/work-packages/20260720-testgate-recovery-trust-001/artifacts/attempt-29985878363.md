# Automatic Attempt 29985878363

Ran: automatic push run `29985878363` tested exact head
`63c0f3ea8fcd192976ebaa0b7eeed9ed17cd88e1`. All six LIGHT nodes passed in
317.933 seconds. Pre-HEAVY audit
`4ea83e0a8e7b991288a3bebdca3562728fecc8eb74607f95acc4cfebb04f703d`
returned `BLOCKED`; its first nine checks passed and
`OPEN_TOOLING_DEFECTS` alone reported
`GATE-AUDIT-OPEN-TOOLING-DEFECT`.

Ran: no HEAVY admission, HEAVY node, subprocess, or retry occurred. The runner
ledger retained AUTO defect `AUTO-5f75d58df001e6fe` as `OPEN`, so the audit
correctly stopped before expensive work.

Ran: hosted recovery re-ingest, predicate verification, GitHub attestation
verification, and authenticated recovery publication all passed. Artifact
`testgate-recovery-verified-29985878363-1` includes the indexed hidden
`.attempts`, `.checkpoints`, and `.work` paths. This is downstream consumer
proof for RTR-051.

Static: the authenticated archive contains only current recovery root
`/testgate-history/recovery/29985878363-1`. It correctly omits failed root
`29984179443-1`, which never acquired hosted provenance. The older root remains
named by explicit failed HEAVY records in the append-only runner ledger and
would therefore block resume after the AUTO defect closes unless it receives a
narrow durable invalidation.

Static: RTR-052 records the runner-local AUTO closure divergence. RTR-053
records the permanently unattestable failed-root resume blocker. The three
defunct pre-pivot Omarchy runner provider records remain nonblocking
historical metadata and are unrelated to this forest1 audit result.
