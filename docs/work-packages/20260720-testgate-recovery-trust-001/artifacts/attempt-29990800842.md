# TESTGATE Attempt 29990800842

Static: GitHub push run `29990800842` targeted exact head
`8e55cb910a9252afd705254140ac94d8f121ea03`.

Ran: trusted comparison admission exited 2 because that exact tip commit did
not contain the required case-sensitive `TESTGATE-Intent-Package` trailer.
The canonical local reproducer reported:

```text
ERROR: push head must contain exactly one TESTGATE-Intent-Package trailer
```

Ran: checkout succeeded, but toolchain verification, dependency bootstrap,
planner construction, recovery restore, LIGHT, HEAVY, and coverage were all
skipped. This attempt executed zero TESTGATE nodes and no retry.

Ran: the finalizer published unsigned artifact `8556970330`. Hosted
verification authenticated recovery artifact `8556980209`, whose index binds
the exact repository, workflow, run, attempt, and head. The archive contains
the unchanged eight-record runner ledger ending at reviewed closure
`2852c7ed8076cc53ff8dfae7d68abf53d0820e4a794ce2fbc1860c659477d978`.

Static: RTR-055 records the workflow packaging defect. The correction is a
tip commit with exactly one trailer naming this active package, followed by a
narrow resolver reproduction and dual independent review before another push.

Ran: exact correction head `35e3d83b3cad7ceceeee3b304a0433a0878ebec7`
contains exactly one valid trailer. The canonical resolver returns this
package, package-chain reconstruction is `READY` with chain ID
`c7204f9951d0721f17f7d585049f695b0151f86017d119c7105f6974d73e93c8`,
and both independent reviewers passed. RTR-055 closed durably at
`39c4ee9ab49e9a5b32ad2305705795f6707cbf7a72806b12606acbe5f61e954a`.
