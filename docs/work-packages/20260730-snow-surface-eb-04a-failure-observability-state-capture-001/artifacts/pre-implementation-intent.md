# Pre-Implementation Intent

Static:

- Intent is diagnostic implementation and independent reconstruction.
- The correction removes lossy error translation and missing trace
  publication only.
- Process equations, numeric constants, guards, tolerances, selectors,
  defaults, forcing, fixtures, observations, and user surfaces are frozen.
- EB-04 remains the immutable first experiment; 04A executions are targeted
  diagnostic replays, not a second factorial.
