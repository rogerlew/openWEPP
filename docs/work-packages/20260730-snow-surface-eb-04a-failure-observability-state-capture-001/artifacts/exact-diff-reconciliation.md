# Exact Diff Reconciliation

Static:

The terminal diff contains typed rare-failure payloads, Stage 3 replay,
additive daily/hourly diagnostics and trace fields, focused tests, package
tools/evidence, and roadmap/catalog advancement.

No canonical contract, process equation, physical constant, coefficient,
threshold, tolerance, selector, default, forcing, fixture, observation,
parser, runfile schema, or user-facing schema changed. Removing a water-depth
epsilon from a `kg m^-2` diagnostic quotient changes no exchange, energy, or
snow state.

`diagnostic-replay.json` binds the exact binary and executable-source diff over
`crates/` and `tests/`; documentation-only completion edits do not invalidate
that binding.
