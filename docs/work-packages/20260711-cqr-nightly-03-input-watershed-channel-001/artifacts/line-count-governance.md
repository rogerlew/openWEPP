# Line-Count Governance

Static: the attempted target grew from `766` to `859` lines and remained below
the `2000` warning threshold. After mandatory rollback it is again `766` lines;
no exception is required.
