# Numeric Equivalence

Ran, attempt only: `32/32` focused tests passed at the strongest provisional
point. The nominal fixture asserted every per-channel scalar, comment, mode,
rating-curve operand, effective control value, and principal file-level fields;
boundary/error tests preserved typed fail-closed outcomes. Independent Review A
found parse, guard, warning, record-consumption, field-mapping, and strict/compat
order unchanged.

The package makes no landed equivalence claim because all implementation/test
edits are rolled back. Scaffold hashes prove final identity.
