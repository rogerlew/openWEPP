# Native-Forest Ecohydrology Authority Reframe

The canonical execution specification is [package.md](package.md). The kickoff
prompt is archived under `prompts/completed/` with the terminal executed-hold
disposition and subsequent authorized full-workspace hold lift;
`prompts/active/` contains no executable prompt.
