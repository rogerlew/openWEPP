# Worker Handoff

No production worker handoff is authorized by this package. The existing
coupled-vegetation successor remains held on complete constitutive authority,
contract-first tests, and its own implementation gates. Its corrected target is
recorded prospectively in that package and prompt.

The historical lift condition required replaying
`assurance_v2_publication_contract::draft_subject_root_is_stable_but_cannot_publish`
in an independently authorized assurance scope and then rerunning the exact
Critical full workspace. Package
`20260809-assurance-draft-publication-defect-closure-001` satisfied both: the
isolated case and 2,325/2,325 selected workspace tests pass with external
scratch. This package is complete and has no worker handoff. Any production
work begins only through the held coupled successor; do not repeat site-value
literature selection.
