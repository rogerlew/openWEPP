# Operand Lineage

Status: `queued / must freeze before result execution`.

Evidence class: `Static`.

The final table must identify every historical/current term, units, duration,
area basis, daily/window reduction, source commit/trace, support rule,
authoritative-versus-diagnostic posture, and rejected adjacent alias. It must
separate external carrier, active internal conduction, cold-content response,
and terminal energy.
