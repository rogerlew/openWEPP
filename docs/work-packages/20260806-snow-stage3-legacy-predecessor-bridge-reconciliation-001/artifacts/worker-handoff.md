# Worker Handoff

Status: `queued`.

The first actionable item is to execute the result-blind source-checkpoint
bridge search. Final handoff must name the exact next package only when evidence
requires work outside this package's declared boundary.
