# Owned File Manifest

Status: `queued`.

Evidence mode: `not run`.

Reconcile the exact base-to-head inventory to the prospective write set before
every result-bearing execution and at terminal closure.
