# Required-Reading Map

Status: `scaffolded`

Evidence mode: `Static`

Core local reading is `586599` bytes after adding the precursor dependency
(`WARN`). External source and literature files are on demand so the executor
reads exact accepted dependencies rather than bulk-loading both repositories.
Recompute after Core changes and after the precursor's terminal amendments.
