# Required-Reading Map

Status: `scaffolded`

Evidence mode: `Static`

Core local reading is `560923` bytes at scaffold time (`WARN`). External source
and literature files are on-demand so the executor reads exact dependencies
rather than bulk-loading both repositories. Recompute after Core changes.
