# Source Guard Evidence

Status: `queued`

Evidence mode: `not-run`

Record source scans and tests proving public watershed routing no longer uses
or exposes the deleted old runtime. The guard must distinguish watershed old
runtime symbols from out-of-scope hillslope/generic compatibility symbols.
