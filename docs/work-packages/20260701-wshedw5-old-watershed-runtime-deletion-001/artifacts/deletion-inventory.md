# Deletion Inventory

Status: `queued`

Evidence mode: `not-run`

Record pre-edit and post-edit scans for watershed old-runtime symbols. Each
finding must be classified as `delete`, `migrate`, `retained-out-of-scope`, or
`hold-blocked`.
