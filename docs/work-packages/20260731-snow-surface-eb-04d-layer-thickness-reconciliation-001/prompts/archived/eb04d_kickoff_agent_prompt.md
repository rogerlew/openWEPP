# SNOW-SURFACE-EB-04D Kickoff

Scope: local repository science-contract/kernel engineering work; flat-file
reads and edits only; no external connectivity.

Execution mode: package-end-to-end.

Execute every phase in `package.md` sequentially through disposition.

Required reading:

- Core: `AGENTS.md`, `docs/codex_exec_plans.md`,
  `docs/work-packages/AGENTS.md`, `docs/work-packages/README.md`, and
  `package.md`.
- Conditional: DC procedure, science-contract procedure/profile, testing
  strategy, `crates/AGENTS.md`, and `tests/AGENTS.md` when their surfaces are
  touched.
- On-demand: `SC-SNOWENERGY-001`, EB-04A/04B geometry evidence, EB-04C
  protected behavior, and the density/runtime source files.

Required-reading budget: `1158378 bytes`, `REQUIRES-JUSTIFICATION`; map and
heavy-file rationale:
`artifacts/required-reading-map.md`.

Task: close `DEFECT-EB04B-GEOMETRY-001` end-to-end inside the declared envelope.

Constraints: contract-first sequencing; canonical authority; typed guards; no
silent defaults, tolerance inflation, state deletion, or cross-unit filter.

DC closure: do not hold while source reading, contract/test work,
implementation, or validation remains possible inside the envelope. Any HOLD
requires a package-local legitimacy audit.

Real consumer proof: the production multilayer density handoff must consume the
corrected lifecycle; wrappers, shadows, and test-only paths cannot close it.

Subagent requirement: REQUIRED for Critical full validation. This prompt
explicitly authorizes subagent spawning/delegation to the comparator suite
runner, two read-only reviewers, and two read-only terminal verifiers for the
roles in `package.md`.

Autonomy: execute end-to-end without requesting user direction unless a hard
boundary is proven.
