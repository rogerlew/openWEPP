# Authority Reconciliation

Status: `complete`

Evidence class: `Static`

EB-04B proves the exact mechanism. `INV-SNOWENERGY-021` requires complete
persistent-column reconstruction. Version 5 `INV-SNOWENERGY-027` now makes the
dimensional distinction canonical: the existing density-model zero-mass
lifecycle boundary is `1e-9 kg m^-2`, equivalent to `1e-12 m` SWE, while the
independent aggregate residual tolerance remains `1e-9 m`.

The two omitted fragments are approximately `5.26e-7 kg m^-2`, more than 500
times the mass-lifecycle boundary. They are represented state, not roundoff.
