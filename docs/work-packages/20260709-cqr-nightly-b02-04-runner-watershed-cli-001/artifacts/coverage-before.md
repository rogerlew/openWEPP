# Coverage Baseline

Ran: delegated fresh batch LCOV measurement recorded in `target-selection.md`.
The target has `1212/1748` executable lines covered (69.336%). Coverage is
uneven across CLI dispatch, TOML runfile parsing, manifest publication metadata,
and hourly carry branches.

Before decomposition, characterisation must consume the real CLI or its
existing contract-test seam, binding exact error/output behavior for every
affected parse, validation, and dispatch branch.
