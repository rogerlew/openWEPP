# Disposition

Evidence class: Static + Ran.

Closure: `COMPLETE-05G-HARNESS-FIDELITY-RERUN`.

SNOWDENSITY-05G completed the harness-fidelity correction required by
SNOWDENSITY-05F. Diagnostic `coe-melt` replay now consumes configured
runtime-surface coniferous canopy and publishes the shortwave bridge proof.

The representative five-site SNOTEL rerun closed `NON-PROMOTION` for default
activation:

- `legacy_coe`: robust failures `9`, ordinal score `84`.
- `coe_shortwave_albedo_v1`: robust failures `9`, ordinal score `86`.

The opt-in path did not reduce robust failures, so the 05E
`PROMOTION-CANDIDATE` result is superseded as regime-limited. SNOWDENSITY-06
may proceed to density compaction from fixed melt boundaries, but it must not
use melt changes as density compensation.

Post-closeout external review endorsed the package. The review also clarified
that coniferous neutrality is expected because `amelt` is heavily attenuated at
`cancov ~= 0.9`; this result should not retire `coe_shortwave_albedo_v1`.
Low-canopy/mixed-forest fixtures remain the decisive melt-value test and require
real per-day seasonal canopy before a verdict.
