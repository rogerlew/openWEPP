# Independent Implementation Prompt

Status: ready for prospective package planning.

Evidence mode: Static.

This is an implementation-facing sanitized handoff, not authorization to
implement within the current package.

Prepare a new work package named
`VEGETATION-RADIATION-INTERCEPTION-CONDUCTANCE-SLICE`. Read
`SC-VEGETATION-001` and every adjacent contract named by its producer/consumer
obligations before editing. The package may establish only:

- typed, versioned native-stratum topology/configuration/initial-state/runtime
  boundaries;
- recipient-specific radiation and liquid-interception request/receipt types;
- conductance inputs and layer-resolved Stage A potential-demand types whose
  constitutive relationships have first received independent scientific
  authority; and
- default-off, non-publishing orchestration scaffolding that cannot mutate
  soil, frozen water, ground snow, residue, or public output.

The package must not invent or import physiology, coefficients, bounds,
defaults, rooting profiles, competition policy, canopy-snow physics,
compatibility reductions, calibration, or cutover behavior. RHESSys semantics
may inform architecture only through the digest-approved sanitized artifact;
direct or close source translation remains prohibited. Every implemented
equation requires independently reviewed literature/canonical authority,
explicit units/domain/guards, adversarial vectors, and independent operand
reconstruction.

Preserve current GSI, ET, litter, snow/frost, water-balance, and runtime
consumers unchanged. Any future adapter remains read-only, field-specific, and
incapable of a cutover claim until the real downstream consumer is proven to
read it and the old owner is retired atomically.
