# CQR35 Kickoff Prompt

Execute CQR35 against
`crates/openwepp-hillslope-orchestrator/src/hydrology/kernel_phases_mod/hydrology_phase_lateral_drainage.rs`.

Close by live metrics if the current target-file CRAP rows are all `<= 30`;
otherwise perform only behavior-preserving private helper extraction while
preserving WB19 lateral/drainage science-contract behavior.
