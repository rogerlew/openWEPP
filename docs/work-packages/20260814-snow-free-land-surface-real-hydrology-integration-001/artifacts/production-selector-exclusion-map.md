# Production Selector Exclusion Map

The campaign may add only an explicit default-off API. At intake no campaign
edit reaches `crates/openwepp-runner`, direct-runtime default dispatch, model
selectors, production configuration defaults or output publication. These
paths remain protected and will be recursively scanned at each child closure.
