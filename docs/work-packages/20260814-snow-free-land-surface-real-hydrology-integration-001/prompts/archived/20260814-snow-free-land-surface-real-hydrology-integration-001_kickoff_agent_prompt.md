# Execute the Snow-Free Land-Surface and Real-Hydrology Campaign

Scope: local `/home/workdir/openWEPP` flat-file engineering only. Start at
`0db1960129ad4f8fc4e292b20574dfe7229d5fe1`. Do not reset to the older remote,
pull, rebase, push, create a branch or PR, deploy, activate, publish, calibrate,
or change production defaults.

Execution mode: campaign-end-to-end. Execute the coordinator and its four
children sequentially through truthful terminal disposition. Apply
contract-first sequencing, exact owner custody, typed failures, independent
ledger reconstruction, real-consumer evidence, and atomic rollback. Do not
invent or substitute process physics.

Required reading and exact write surfaces are recorded in each package's
`package.md` and `artifacts/required-reading-map.md`.

Subagent authorization: this prompt explicitly authorizes and requires
subagent spawning/delegation to the land-surface science, hydrology/ownership,
Rust correctness, comparator-runner, and two terminal-verifier roles named by
the campaign. Read-only roles return compact findings; the comparator may
write only ignored logs and package gate artifacts.

Keep this prompt active until all four children and the coordinator pass.
