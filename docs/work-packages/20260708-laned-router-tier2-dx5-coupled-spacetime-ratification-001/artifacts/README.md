# Artifacts

Evidence for the Tier-2 `dx5` coupled space-time ratification package.

Raw rerun directories live under `artifacts/coupled-spacetime-runs/` and are
ignored package-locally. Committed evidence is restricted to summaries,
hashes, command logs, reviews, verification, gates, and disposition artifacts.
