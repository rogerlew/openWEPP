# Worker Handoff

Status: `READY`
Evidence mode: Ran.

## Next Package

Suggested name:

`20260708-laned-router-wa-sediment-reference-adequacy-attribution-001`

## Objective

Attribute the `wa_cascades_forest_h1` refined-75 fine-reference annual
pass-sediment adequacy miss before any renewed `dx5` production mesh-policy
promotion.

## Required Inputs

- This package's `artifacts/mesh-policy-ratification.md`
- This package's `artifacts/mesh-policy-ratification.json`
- This package's `artifacts/coupled-spacetime-summary.json`
- `SC-OFEROUTE-001` rev 43
- D13 routed-hydrograph erosion-shape obligations

## First Actions

1. Confirm the `tdep:4` annual pass-sediment surface from
   `mesh-policy-ratification.json` for `wa_cascades_forest_h1`
   `fine_reference_adequacy_dt75`.
2. Compare daily pass-sediment deltas for that implicated annual surface
   against
   routed-outlet, routed-shape, end-storage, tail-fold, and closure trace
   surfaces.
3. Classify the miss as erosion-consumer sensitivity, routed-water
   timing/magnitude sensitivity, or active-router numerics.
4. If mechanism-class evidence supports a contract amendment, amend
   `SC-OFEROUTE-001` first; otherwise hold with the exact mechanism blocker.

## Binding Rules

- Do not widen shape or sediment tolerances to fit the miss.
- Do not promote `dx5` until the coupled fine-reference basis closes.
- Keep runtime cost priced but non-blocking unless the operator changes the
  fidelity-first posture.
