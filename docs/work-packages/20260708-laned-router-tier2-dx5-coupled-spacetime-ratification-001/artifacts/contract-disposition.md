# Contract Disposition

Status: `NO-AMENDMENT-HOLD`
Evidence mode: Ran.

`SC-OFEROUTE-001` remains at rev 43.

No contract amendment landed because `dx5` production promotion is not
ratified. The evidence supports these contract-consistent statements:

- The trace-gated coupled space-time ladder completed for all selected real
  cohort rungs.
- `dx5` candidate-vs-`dx2p5` comparisons pass production tolerances at both
  `max_dt=300` and `max_dt=75`.
- The fine-reference basis remains unclosed, so the contract's
  candidate-vs-adequate-fine-reference promotion precondition is unmet.

Any future `SC-OFEROUTE-001` amendment must first close the two reference
adequacy blockers recorded in `artifacts/hold-legitimacy-audit.md`.
