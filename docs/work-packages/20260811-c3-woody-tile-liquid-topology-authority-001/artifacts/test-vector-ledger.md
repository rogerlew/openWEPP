# Test Vector Ledger

Status: `independent oracle passing`

Evidence mode: `Ran`

Command:

```text
.venv/bin/python artifacts/reference_calculator.py
```

Result: `"all_pass": true`, computed as the conjunction of 31 executable
checks. The committed fixture SHA-256 is
`c02e5e2a2287d84cfc584a6e3ec9c499cf7168160bc71f2577323f19dcb50bf1`.
It contains authoritative operands and expected outputs, rather than verdict
booleans alone, for the same lower stratum under distinct upper columns; the
potential-request/arbitration plus controlled exogenous-vapor column-causality
sequence; complete
four-owner rollback state and warm starts; layer/species mineral-N arbitration;
canonical state serialization and its MPa poison; digest-bound FvCB/E04
locality alternatives evaluated by the exact V1 authority oracle; and
accepted-once versus duplicate shared C/N. The controlled vapor values prove
topology causality only and explicitly bind the complete capped coupled result
to the Stage-B `STAGE_B_E11_E15_EXACT_ORACLE` gate; they are not constitutive
expected values.

The committed fixture is
`openwepp_c3_woody_v2_topology_vectors.json`; it is generated independently of
Rust and binds unequal tile fractions, distinct rain/store/wetness, two ranks,
same-tile descendant routing, stemflow bypass, condensation/second drainage,
empty tile, local/column/stand closure, occupancy water area conversions,
migration, authorization-sensitive upper-to-lower capped routing, shared C/N
aggregation, mineral-N layer/species identity, byte rollback, and nonlinear
aggregate-first poisons. Poison checks cover replicate store, wrong area/routing,
stemflow-through-foliage, omitted second drainage, missing/double `f_t`, wetness
and PAR preaggregation, wrong authorization, duplicate/missing lanes, and
carbon/energy aggregation alternatives. Production Rust conformance remains a
separate Stage-B obligation; Stage A expected values never use production Rust
as an oracle.
