# Topology-State Problem Statement

Status: `closed by selected rule`

Evidence mode: `Static`

V1 permits one stratum in multiple topology tiles but owns only one nonlinear
stand-ground canopy-liquid store. Neither aggregate-first nor tile-first E04 is
derivable: aggregate-first loses descendant routing and nonlinear state;
tile-first requires an unauthorized store distribution. The historical HOLD and
independent adjudication remain immutable evidence.

V2 resolves the missing information by making `(stratum_id,tile_id)` the liquid,
wet-energy, coupled-solve, and water-request execution lane. The shared stratum
C/N state remains singular and consumes weighted accepted local fluxes once.
