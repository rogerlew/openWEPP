# Required Reading Map

Status: `complete / intake`

Evidence mode: `Ran + Static`

Base commit: `02631ae92af6b073ed7957592fef4bad68dcf77f`

The discovered instruction chain is:

- root `AGENTS.md` for every path;
- `docs/work-packages/AGENTS.md` for this package;
- `docs/specifications/science-contracts/AGENTS.md` for canonical contracts and
  the contract index;
- `tests/AGENTS.md` for integration tests;
- `crates/AGENTS.md` for the pre-authority containment guard.

`tools/agents/find-agents --for` was run for every intended Stage-A write path.
The exact listed local reading set was `1,162,243` bytes. This exceeds compact
context and is classified `WARN`; the canonical procedures, affected contract
sections, complete V1 authority artifacts, implementation HOLD/review evidence,
and source-triggered mechanism sections were read directly. The work-package
catalog and the very large WATBAL historical body were routed by headings and
affected invariant/change-log surfaces; no unrelated historical science was
used as constitutive authority.

No new external reference bytes were required. The selected rule is an explicit
openWEPP canonical topology/ownership selection over the already admitted CLM5
E04 operator, not a new empirical interception equation.
