# Execute TESTGATE-BOUND-LEDGER-CONTRACT-01

Scope: local repository engineering work; flat-file reads/edits in the
openWEPP worktree only; no external systems or network actions are required.

Execution mode: package-end-to-end.

Phase plan: execute every phase in `package.md` sequentially through
disposition without conversational context or user intervention unless
hard-blocked.

Required reading:

- Core: `AGENTS.md`, `docs/work-packages/AGENTS.md`, `tests/AGENTS.md`,
  this package, and `artifacts/required-reading-map.md`.
- Conditional: `docs/standards/testing-and-gate-strategy.md` before intent,
  terminal, or canonical gate work; `docs/defect_closure_execplans.md` before a
  HOLD or final disposition.
- On-demand: the predecessor gate evidence, the target integration test, and
  `crates/openwepp-gate-planner/src/main.rs` only for the touched mechanism.

Required-reading budget: 55,809 core local bytes, `OK`; tier routing is in
`artifacts/required-reading-map.md`.

Execute the exact stale source-contract alignment through focused/full gates,
dual review, dual terminal verification, one comparator-owned canonical
transaction, dual receipt verification, and update of only the named
predecessor hold artifact.

Only replace the obsolete pathname resume assertion with exact bound-text API
and retained-ledger-read assertions. Do not change production behavior, CAL
data, or Harvard state.

Before the Rust edit, record and accept a pre-implementation intent plan. Before
heavy execution, authenticate and reconcile the exact terminal diff and
terminal plan under `docs/standards/testing-and-gate-strategy.md`. Close defect
`TESTGATE-BOUND-LEDGER-CONTRACT-01` end-to-end; do not stop at `HOLD` while
source reading, implementation, review, or validation remains possible inside
the declared envelope. Any HOLD requires the package's legitimacy audit.

Subagent requirement: REQUIRED: canonical heavy execution uses
`comparator_suite_runner`, never the parent model. Use an exact clean terminal
head, authority base `f8cba1c9f3e02d241a2bb7fccc3329a0a142ac57`,
this package identity, boundary
`INCREMENT`, campaign `TESTGATE-BOUND-LEDGER-CONTRACT-01`, and one fresh
external artifact root/ledger. No reuse, placeholder, retry, or fallback.
Require package chain READY, exact intent/terminal predecessor, LIGHT PASS,
ten-check READY audit, HEAVY PASS, balanced ledger, exact inventory/counts, and
two independent receipt verifiers.

This prompt explicitly authorizes subagent spawning/delegation to one bounded
worker for the exact target-test edit, two read-only scaffold/implementation
reviewers, two read-only terminal/receipt verifiers, and the
`comparator_suite_runner` for heavy and canonical execution. Expected outputs
are compact exact-subject verdicts, metrics/log paths, and the package's named
evidence artifacts. Worker writes are limited to
`tests/integration/testgate_ci_executor_contract.rs`; comparator writes are
limited to its fresh external artifact root and ledger.

Autonomy: execute the package end-to-end and update all required artifacts
without requesting further direction unless hard-blocked.

Outputs: maintain the living ExecPlan, required-reading map, intent and terminal
plans, review/finding/gate/handoff/line-count/exact-diff evidence, canonical
execution, receipt verification, and final disposition.
