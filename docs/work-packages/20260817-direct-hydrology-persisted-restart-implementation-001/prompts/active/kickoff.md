# Execute Persisted Restart Implementation

Begin implementation only after restart authority is terminal COMPLETE. Execute
all continuation, poison, review, and verification gates.

Subagent authorization: this package explicitly authorizes the read-only review
and verification delegation declared in `package.md`.

