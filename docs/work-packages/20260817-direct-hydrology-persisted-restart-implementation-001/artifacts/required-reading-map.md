# Required reading map

Read before implementation: root, crate, test, science-contract, and work-package instructions; canonical validation strategy; released authority package, final disposition, gate record, manifest, schema, metadata, field ledger, poison matrix, three reviews, two verifiers, and every reference owner-mapping module; restart, forcing, Child-4, and campaign package state; roadmap and catalog.

Instruction discovery was run over every path named by the kickoff prompt. Applicable chains are root plus `crates/AGENTS.md`, `tests/AGENTS.md`, or `docs/work-packages/AGENTS.md` according to the write path.
