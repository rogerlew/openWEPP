# Production No-Mutation

The capability is behind a nondefault Cargo feature and a separate default-off
crate. No runner selector, environment variable, default, output schema,
publication sink, or production owner path was added. Admission builds all
owners in isolation; installation is one assignment to an explicit shadow
host. Production activation and cutover remain unauthorized.

