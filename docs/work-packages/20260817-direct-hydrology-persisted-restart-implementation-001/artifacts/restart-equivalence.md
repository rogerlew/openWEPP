# Restart Equivalence

The released repository-backed evidence is executed by the production crate.
Fresh interval-24 admission constructs a new `DirectV10RealConsumerShadow`,
installs admitted staged owners and scheduler position, executes intervals
24--47 through the existing physical path, advances the day index, and matches
the uninterrupted scientific, GSI, cursor, and scheduler owners. The production
host test additionally proves exact day-beginning abort bytes and finish at 48.
