# Implementation write set

Authorized production ownership is the orchestrator-owned `persisted_restart_v1` module, sealed accessors/refactors in the named owner crates, contract-derived integration tests, and the four named package/campaign evidence trees. The released authority package is read-only and excluded from production dependencies. Selectors, defaults, production owners, outputs, publication, calibration, deployment, PRs, branches, and pushes are excluded.
