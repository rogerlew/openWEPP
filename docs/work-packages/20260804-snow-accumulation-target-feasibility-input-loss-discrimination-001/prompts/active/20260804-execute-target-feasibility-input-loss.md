# Execute SNOW-ACCUMULATION-TARGET-FEASIBILITY-AND-INPUT-LOSS-DISCRIMINATION

Scope: local repository scientific diagnostic; flat-file reads and package/
target writes only; no external connectivity or credential access.

Execution mode: package-end-to-end (default).

Phase plan: execute all phases in `package.md` sequentially through disposition.

Required reading:

- Core: `AGENTS.md`, `docs/codex_exec_plans.md`,
  `docs/work-packages/AGENTS.md`,
  `docs/standards/testing-and-gate-strategy.md`,
  `docs/standards/prompt-wording-guidance.md`, package-local `package.md`,
  `artifacts/analysis-freeze.json`, `artifacts/operand-lineage.md`, and the
  predecessor post-closure feasibility disposition.
- Conditional: files and triggers in `artifacts/required-reading-map.md`.
- On-demand: predecessor machine results/traces, forcing dispositions, EB-04W2
  evidence, and fixture provenance named in the map.

Required-reading budget: `125590` local bytes, `OK`; map:
`artifacts/required-reading-map.md`.

Files: write only the package-local tree, `docs/work-packages/README.md`,
`docs/ROADMAP.md`, `docs/planning/snow-surface-energy-balance-roadmap.md`, and
`target/snow_accumulation_target_feasibility_input_loss_discrimination/`.

Task: execute the package objective end-to-end. Preserve the prospective
operators and truth table. Verify retained source identities before accepting
results. Generate figures and source sidecars. Keep observations, climates,
fixtures, production code, contracts, and public outputs unchanged.

Constraints: canonical `SC-*` physics authority remains unchanged; no surrogate
physics, silent default, fitted correction, provider promotion, or PRCPSA
independence claim. Distinguish magnitude diagnostics from conservation proof
and point observations from hillslope state.

Subagent requirement: no heavy batch or comparator runner. This prompt
explicitly authorizes subagent spawning/delegation to two read-only scientific
reviewers and two read-only terminal verifiers; outputs are compact findings,
independent metric spot checks, and evidence paths; write access is read-only.

Autonomy: execute all phases and update required artifacts without requesting
additional user direction unless hard-blocked.

Outputs: complete machine results, figures/sidecars, synthesis, review finding
disposition, verification, gate evidence, prompt archival, catalogs, and final
package disposition.
