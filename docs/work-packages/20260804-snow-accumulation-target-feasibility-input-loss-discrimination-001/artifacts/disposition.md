# Disposition

Status: `complete / review and verification pass / no correction`

Evidence mode: `Ran` plus `Static`

The frozen four-site analysis adjudicates to `LOSS_PRIORITY_SIGNAL` as a
correction-family investigation priority only. Niwot, Paradise, and Snowbird
pass the guarded dry-loss screen; Snowbird supplies the third signal at exactly
the 10-annual floor. Only Snowbird fails the all-phase zero-loss mass ceiling.

The result is threshold-passing but coverage-fragile. Cold-event input remains
non-systemic under both frozen padded and supplemental active-day alignments,
but the latter produces site signals at Paradise and Snowbird. Snowbird is a
multifactor sentinel whose current-fixture mass ceiling cannot distinguish
forcing error from scale, elevation, redistribution, or target mismatch.

The preliminary custody-incomplete namespace is rejected. Accepted v2 binds
the predecessor's tracked manifest, retained receipt, annual result, all four
traces, and PRCPSA intake identity. Both independent reviews are complete, all
findings are accepted and resolved, and direct terminal gates pass. Both fresh
terminal verifiers independently reproduce the evidence and return `PASS` with
no findings.

No production equation, selector, default, contract, test, fixture,
observation, or public output changed. No forcing, phase, loss-physics,
calibration, validation, or promotion correction is authorized. The package is
closed.
