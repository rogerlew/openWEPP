# Worker Handoff

Status: `complete / no correction authorized`

Evidence mode: `Ran` plus `Static`

## Accepted Result

The frozen cohort truth table returns `LOSS_PRIORITY_SIGNAL`, but the claim is
threshold-passing and coverage-fragile. Niwot, Paradise, and Snowbird pass the
guarded dry-loss screen; Snowbird supplies the third site with exactly 10
annuals. Only Snowbird fails the all-phase seasonal mass ceiling. Cold-event
input is non-systemic under both the frozen padded operator and a supplemental
active-day-only alignment, though the latter produces site signals at Paradise
and Snowbird.

Snowbird must remain a multifactor sentinel. Its current fixture lacks enough
all-phase mass to reach the point-pillow peak under a no-lateral-input ceiling,
and modeled pre-peak loss is also material. Neither observation establishes a
forcing error or a correction factor.

## Recommended Next Scope

If the campaign proceeds, open a separate scale-aware warm/mixed pre-peak loss
attribution package. It should:

- retain separate forcing, snowfall, storage, and loss ledgers;
- use Niwot and Paradise as stronger loss-screen cases, Mica Creek as a
  non-passing control, and Snowbird as a multifactor sentinel;
- localize dry-period loss timing against independently reconstructed energy
  and state operands without interpreting empirical CoE components as measured
  energy fluxes;
- preserve active-day and timing-tolerant event alignments side by side;
- carry the 0.1-inch WTEQ resolution and point-versus-hillslope mismatch as
  explicit observation operators; and
- require independent, scale-matched evidence before any parameter, equation,
  forcing, or default change.

Do not fit dry-loss differences, promote PRCPSA, infer a precipitation
multiplier, or treat `LOSS_PRIORITY_SIGNAL` as proof that loss is the sole
cause of low peak SWE.
