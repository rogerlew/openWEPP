# Worker Handoff

Evidence mode: Static.

## Next Package

Scaffold a renewed Tier-2 target-`dx` mesh-policy ratification package under
`SC-OFEROUTE-001` rev 43.

Suggested name:

`20260708-laned-router-tier2-dx5-coupled-spacetime-ratification-001`

## Objective

Decide whether `dx5` can become the active production mesh default using
coupled space-time evidence, not the superseded fixed-300-only fine-reference
basis.

## Required Inputs

- This package's `artifacts/timestep-policy-adjudication.md`
- This package's `artifacts/timestep-policy-analysis-inputs.json`
- `SC-OFEROUTE-001` rev 43
- Prior Tier-2 re-adjudication package:
  `20260708-laned-router-tier2-dx-target-mesh-policy-readjudication-rev41-001`
- Fine-reference hold-lift and day-792 attribution packages in the chain

## Binding Rules

- Do not widen routed-shape tolerance.
- Do not promote `dx5` from candidate evidence alone.
- Do not change production default until contract-first promotion evidence is
  complete.
- Treat runtime cost as priced evidence, not a promotion blocker, per operator
  posture.
- Use same-`dt` spatial and same-`dx` timestep-refinement controls for any
  implicated selected-cohort surface.
- Keep `OPENWEPP_LANED_ACTIVE_MAX_DT_S` diagnostic and trace-gated unless a
  later contract package explicitly changes production policy.

## First Actions

1. Predeclare the coupled space-time ladder for selected-cohort members.
2. Reuse the compact replay pattern from this package so committed evidence
   does not require bulk raw run trees.
3. Confirm whether the `mn_corn_h4` day-792 repaired comparison is enough to
   lift the only remaining fine-reference adequacy miss, or whether other
   members/surfaces become implicated under matched timestep controls.
4. If selected-cohort fidelity passes, perform contract-first `dx5`
   production-promotion amendment before code default changes.

## Carry-Forward Evidence

This package closed the old fixed-300 day-792 hold:

- original fixed-300 shape L1: `0.02094494047849004`
- threshold: `0.016666666666666666`
- shared-75 shape L1: `0.002982804005304084`
- classification: `TIMESTEP-POLICY-ARTIFACT-CLOSED`

The next package owns the broader selected-cohort verdict and any production
default change.
