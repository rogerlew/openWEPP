# Residual/Jacobian prototype continuation

Static: initialized 2026-09-08 PDT at
`da16c7640851f4dad15583bd95aaf5324f19f3a8` with a clean primary checkout.
Status: COMPLETE — `INCONCLUSIVE_WITH_BOUND`; production and predecessor HOLDs
unchanged. Dual terminal review and dual verification PASS.

## Authorization and protected boundaries

Execute the contract-first A-versus-J prototype end-to-end. Primary checkout
holds canonical experimental authority and compact package evidence only;
experimental Rust/builds/fixtures remain in detached source kits/worktrees.
No F/R reruns, combined treatment, production activation, push, deployment,
named branch changes, new controller/solver/physics, or historical reconstruction.

## Settled inputs

- Review checkpoint and baseline A: `da16c764…`.
- Initial design: one component-temperature derivative block over every affected
  residual row; primary method selected only after bounded source inspection.
- Canonical primal `r(x;u,b0)` remains ordinary execution authority.
- R graph/oracle and predecessor harness are validation assets only.
- First-block local work target: about 10x fewer charged complete residual/leaf
  evaluations plus decisive measured local cost reduction.
- The 30% wall target applies only after prospectively declared broad coverage.

## Current work and next action

The first N=2/S=6 capture (`b8f6a923…`) is quarantined: every stem area is zero
and its V3 FixedFinal context is incomplete. A lawful existing two-rank
positive-stem transaction then produced 8 exact replay records (4 Potential,
4 FixedFinal), N=2/S=2/D=25, corpus `a05ca090…`; both stems are active in every
record and full inputs/caps/frozen/trial/base/lifecycle data round-trip exactly.

The independently implemented baseline-only nine-scale oracle and its selector
self-tests PASS as software, but frozen scientific admission is 0/16 active
columns. Every column lacks at least one required second-order FD basin; the
first own-stem row is visibly roundoff dominated. No v33 constant was tuned.
Therefore Phase A cannot exit, J was not implemented, and all candidate,
full-solve, cost, timing, memory, expansion and scale work is NOT RUN by design.
The package is closed. A successor requires new owner authority and a prospective
v34-or-later oracle-conditioning/precision amendment; see
`rearchitecture-handoff.md`. Unknown workflow-total reading exposure remains
UNOBSERVED.
