# CRAP Before

Ran: sole row above `30` is `PmetparaParseError::fmt` at line `231`, CC `12`,
coverage `0%`, CRAP `156`. `PmetWarningCode::as_str` is exactly `30`; every
parser/normalization row is below `19`.
