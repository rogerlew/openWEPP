# Final Disposition

Evidence mode: `Static + Reused Ran + Ran validation`.

Disposition: `DIAGNOSTIC_COMPLETE`.

EB-04T reconstructs the exact 16 EB-04S baseline forcing-robust failures across
B/L/S/LS without rerunning the model. All 64 relevant ordinal labels remain
`fail`. Under LS, 15 selected primary errors move exactly away from their
observation target and one is unchanged; no materiality threshold was defined.

The total-failure criterion has mixed scientific alignment. Nine failures are
density-trajectory diagnostics, and two depth-SWE failures have mixed,
ambiguous density/interception ownership. The five timing failures are all open
SNOTEL controls. They legitimately test sublimation and whether the combined LS
bundle is promotable, but they provide zero canopy-longwave-identifying cases.

Thirteen of 16 rows have nonzero factorial interactions. The Niwot peak-depth
case is especially diagnostic: S alone improves timing error by four days, but
a `+4 d` interaction cancels the gain in LS. The retained evidence therefore
does not support a unique sublimation-causality claim.

EB-04S nonpromotion and default-off remain authoritative. EB-04T authorizes no
retroactive pass, limited default promotion, calibration, or new factorial
round. It supports proceeding to EB-05 assurance while routing density/geometry
and mountain under-persistence to their owning process/forcing investigations.

All scoped validation, deterministic generation, three SVG/sidecar pairs, two
independent reviews with finding disposition, two independent terminal
verifications, protected-diff, exact-write-set, security, and documentation
gates pass.
