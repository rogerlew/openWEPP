# Execute GATE-AUTH11-NODE-01

Execute this package through the prospective AUTH11 planner correction,
focused gates, dual review, canonical admission, delegated heavy execution,
dual verification, and closeout. Do not inject gates outside the terminal plan,
run CAL population, or access Harvard.

Subagent authorization: this package explicitly authorizes subagent
spawning/delegation to one bounded implementation worker, two independent
read-only reviewers, two independent terminal verifiers, and the
`comparator_suite_runner`; writes are limited to the declared write set.
