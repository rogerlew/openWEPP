# Characterization

Evidence label: Static.

Status: `QUEUED`

Expected behavior oracle:

- existing snowbench CLI help/error strings and parse precedence;
- existing request field mapping for `export-pysnobal`, `physics-bulk`,
  `coe-melt`, `coe-bound-density`, and `jennings-phase`;
- existing integration tests that confine snowbench CLI selectors to diagnostic
  surfaces.

Focused characterization commands and outcomes will be recorded after tests are
added or confirmed.
