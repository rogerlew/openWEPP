# R7G Iterative Completion Agent Prompt

Archived status: R7G closed at
`HOLD-R7G-FROST-STATEFUL-SUBSOLVER-REQUIRED`.

Execute `docs/work-packages/20260623-r7g-iterative-completion-001/package.md`
autonomously.

You must continue the defect-closure loop until R7G completes or a legitimate
out-of-envelope `HOLD-R7G-...` boundary is proven. Do not stop after reproducing
a marker, changing a marker, producing one direct endpoint, or finding the next
blocker. Every in-envelope blocker must be reduced, implemented, tested, and
rerun before disposition.

Start with the inherited active-snow blocker. Confirm the contract authority
that sidecar presence alone is not active snow, implement the direct authority
correction, run focused validation, then rerun H2637 direct default candidate.
If actual active snow/frost authority is required, add typed surface-free state,
compute, mutation, downstream operands, shadow projection, publication operands,
fixtures, and no-compatibility proof inside this package.
