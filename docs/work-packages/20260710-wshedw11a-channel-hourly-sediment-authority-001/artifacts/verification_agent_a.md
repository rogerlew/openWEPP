# Verification Agent A

Status: EXECUTED
Evidence mode: Ran + Static — Ran: `git -C /home/workdir/openWEPP diff` of the uncommitted SC-ROUTE-001 v51 amendment (204 insertions), targeted `grep`/`sed` reads of the working-tree contract text and package artifacts, and `.venv/bin/python tools/check_sc_binding_exposure.py docs/specifications/science-contracts/contracts/SC-ROUTE-001.md` (output: `PASS ... 7 binding exposure row(s) fully consolidated`, exit 0). Static: all closure adjudication (whether the text found actually resolves each finding as reviewed). No builds, tests, or simulations were run (none apply to a contract-text verification); source PDFs were not re-opened (source fidelity was Review Agent A's lens, not this verification's).
Verifier: Claude subagent (verification A), 2026-07-10

| finding_id | closure status | evidence |
|---|---|---|
| A-1 | closed | SC-ROUTE-001.md:147 (INV-ROUTE-018): "**WEPP-adapted lineage realization**" governs — linear rate `dwdti = excess * 2 * Kch * (tau_b - taucr) / rho_soil` with the `^1.05` exponent explicitly dropped (paralleled to the detachment path dropping [I-128] `1.35`/`^1.05`), lineage-modified exponential `wstar = (1 - exp(-1.0176*t_star))/1.0176`, fitted `f(x_b)` (`shdist` lineage); CREAMS [I-133]-[I-140] re-scoped to "**structural provenance** ... not as a literal override." Widening Clock section (:684-687) opens with the same realization statement ("the CREAMS equation numbers below cite structural provenance, not literal forms"). authority-matrix.md:411+ carries the "Post-review corrections (2026-07-10, dual-review cycle)" section with the widening-law realization correction attributed to A-1. All four sub-points of the finding (linear rate, 1.0176, fitted f(x_b), excess multiplier) are now in the invariant text. |
| A-2 | closed | INV-ROUTE-018 (:147): "Within a layer-contact interval the erosion-time budget partitions per the lineage `timpot`/`timex` semantics: incision consumes `timpot = depmid * rho_soil / d_i` and only the residual budget drives widening." Same partition in the operand-table erosion-time row (:638) and Widening Clock step 1 (:692-694). Test vector 10 (:748-752) covers mid-interval layer contact with an explicit no-double-count clause. `timpot`/`timex` Variables row added (:124). |
| A-3 | closed | INV-ROUTE-019 (:148): "the quasi-steady interval solve carries no storage term, so each interval closes exactly and no sediment mass is attributable to end-of-grid routed water storage on this lane." INV-ROUTE-020(c) (:149): storage mass "**zero by construction**"; deposit-at-grid-end rescoped to the recorded unsteady fallback lane. GAP-ROUTE-013 (:770) amended: "moot by construction ... governs the recorded unsteady fallback lane only." Vector 6 (:736-738), degenerate-state row (:243), and sequencing step 2 (:676-678) all coherent with the zero-by-construction reading — reviewer's second construction, as the disposition claims. |
| A-4 | closed | INV-ROUTE-018 evidence tag now `[DIRECT][Static] + [INFERENCE][Static]` (:147, end of row); per-interval re-anchoring labeled "the interval-ization of the lineage's own per-event re-anchoring ... a **labeled refinement** with the same recorded-fallback posture as INV-ROUTE-016." authority-matrix.md post-review section bullet 2 retracts the "memoryless ... native, not an invention" overstatement. |
| A-5 | closed | Allowed-degenerate-states zero-flow row (:241) re-tagged `[DIRECT][Static] + [INFERENCE][Static]` with the annotation "extended by inference from the flowing-channel boundary condition to the dry interval" — matches the INV-ROUTE-020 row's mixed tag. |
| A-6 | closed | (i) Gate operands pinned in INV-ROUTE-018 and the operand table (:638): detachment gates on average soil shear (`effsh` lineage), widening on boundary shear `tau_b = tau * f(x_b)`. (ii) "Known-divergence note (comparator posture)" added to the Widening Clock section (:700-706): surrogate-vs-interval operand shift + hour-mean truncation, Investigation-tier per ADR-0017/ADR-0036 D5, never acceptance gates. (iii) Vector 1 (:710-718) pinned (see B-7). |
| A-7 | closed | INV-ROUTE-017 (:146) now cites GAP-ROUTE-012 for the no-re-erodible-bed-store rule and carries the mixed `[DIRECT][Static] + [INFERENCE][Static]` tag. |
| B-1 | closed | INV-ROUTE-005(a) (:135) now embeds the dependency-authority definition: an upstream channel node on the active interval lane (INV-ROUTE-015), publishing same-grid per-interval per-class egress, carries channel-hourly surface authority for (a) and (c); "it is the only non-hourly dependency form that does; impoundment dependency nodes carry no such authority." Activation section rewritten with topological-order evaluation/propagation, and the interval-active-channel-feeding-a-non-activatable-inlet topology explicitly declared the INV-ROUTE-005(c) mixed-authority fail-closed state — both halves of the proposed disposition present. |
| B-2 | closed | INV-ROUTE-015 (:144): "**lane activation is biconditional and mandatory** — the interval lane is active ... if and only if (i) `ipeak >= 3` ... and (ii) INV-ROUTE-005(a) authority ... When the predicate holds, executing the event-scalar sediment solve is invalid." Anti-partial sentence rescoped in Activation: "On an activated channel, partial execution is invalid ... (Non-activated `ipeak >= 3` channels routing interval water with the event-scalar sediment solve are the ordinary INV-ROUTE-005(e) lane, not partial execution.)" INV-ROUTE-005(e) is now explicitly conditional on lane activation, so the BEI-ROUTE-007 note's selector claim is no longer hidden authority. All three defects resolved. |
| B-3 | closed | Duplicate of A-7; GAP-ROUTE-012 pointer verified in INV-ROUTE-017. |
| B-4 | closed | Variables-and-Units rows added covering every symbol the finding listed: `V_h`/`S_h`, `q1(it)`/`ntchr`/`dtchr`, `W`/`W_i`/`W_f`/`wera`/`werb`, `omega`/`t_star`, `(dW/dt)_i`/`e_m`/`d_ch`, `rho_soil` (defined as in-place soil weight density, baseline `wtdsoi` basis, from the channel soil input — resolving the bulk-vs-particle ambiguity), `timpot`/`timex` (:118-124). Unit-bridge declaration in the addendum (:648-657): SI operands cross to English at the named `chnrt.for:166`/`chnrt.for:846` migrated conversion lineage sites; TOL-ROUTE-006/007/008 closures evaluated on the SI side; no new constants. See note 1 on the row count. |
| B-5 | closed | INV-ROUTE-019 (:148): boundary-detached mass "**defined constructively** as eroded geometry volume * `rho_soil` ... a derivation rule, not a separately checked residual" — reviewer's option (ii). Vector 10 exercises the derivation at the layer-contact seam. |
| B-6 | closed | Merged fix verified: labeled-refinement sentence + "no persistent widening state exists beyond the carried geometry" in INV-ROUTE-018 and Widening Clock step 4; the binary erosion-time operand is stated explicitly in INV-ROUTE-018 and its divergence covered by the known-divergence comparator note. |
| B-7 | closed | Vector 1 (:710-718) comparator construction pinned exactly as proposed: shared solve core with `durrof := dtchr`, `qsed_top := interval class mass / dtchr`, erosion-time budget `:= dtchr`; explicitly scoped to "sequencing/wiring identity, not operand-law differences." |
| B-8 | closed | "WS18-WS31" has zero occurrences in the working-tree file (Ran: grep); replaced by "WSHEDIMPL18-41 migrated segment-solve lanes (WS20/WS21 runtime families)" in INV-ROUTE-016, guard row 016, INV-ROUTE-018, the unit-bridge text, and sequencing step 1b (7 occurrences). |
| B-9 | closed | INV-ROUTE-020(a) (:149): "the routed-closure constant of INV-ROUTE-007 (the `qpo` floor, `1e-12 m^3 s^-1`, applied here per interval)" — matches the proposed rewording. |
| B-10 | closed | Projection formula with `f_k`, overlap/3600 s, and 00:00 grid anchor written into the addendum (:640-646); TOL-ROUTE-008 (:300) carries the zero-mass carve-out ("contributions with `Σ_h S_h = 0` require exactly zero projected mass (absolute)"); INV-ROUTE-020(d) adds "a non-covering grid handed to the sediment lane is a typed hard failure," mirrored in guard row 020. |
| B-11 | closed | Ran: `grep -c '×'` = 0 (INV-ROUTE-019 now uses `*`); `grep -n ' $'` = no trailing-whitespace lines anywhere in the file; step 1b disambiguates "§13.5.6 detachment-deposition Cases I-IV — the case12/case34 branch families ... distinct from the §13.2 runoff Cases I-IV of INV-ROUTE-003"; guard rows 015-020 all pin the `WKERNEL-WS10-CHANNEL-E-*` family (015 pins E-003; 016-020 pin E-001..003), consistent with vector 9. |
| B-12 | closed | REF-ROUTE-HECRAS-QUS anchor (:80) annotated "treated as **class corroboration** co-anchored with REF-ROUTE-ARS77-SAMEGRID and REF-ROUTE-CREAMS-CH3-QS; the formal TRM citation remains pinned for acquisition, bibliography R-107." R-107 verified to exist (`references/annotated_bibliography.md:1248`, the HEC-RAS capture entry). |
| B-13 | closed | Activation section: "Inlets with impoundment dependency nodes do not activate ... remain on the existing INV-ROUTE-005 branches"; INV-ROUTE-015 predicate: "any impoundment dependency precludes activation"; INV-ROUTE-005(a): "impoundment dependency nodes carry no such authority." Stated in all three places. |

Disposition-table completeness: verified — 21 rows, one per finding (A-1..A-8, B-1..B-13), with all required columns (finding_id, source, severity, decision, action_taken, artifact_ref, rationale). A-8 is the sole rejection, with a register-precedent rationale (GAP-ROUTE-004 precedent for "closed" on retained limitations); per my brief it was not verified for closure. Binding-exposure lint: Ran, PASS, exit 0, 7 rows consolidated — matching the disposition's claimed post-fix re-run. The v51 revision-history row accurately records the review-cycle adjudications (A-1/A-2/A-3/B-1/B-2/B-4 named).

Implementation matches the disposition claims: for all 20 accepted findings, the claimed action_taken exists verbatim-or-stronger in the current working-tree contract text (and, for A-1/A-4, in the authority-matrix post-review section), and in each case the text actually resolves the finding as the reviewer scoped it — including the six load-bearing spot-checks (A-1, A-2, A-3, B-1, B-2, B-4), each verified against the primary contract text rather than the disposition's description. Two minor discrepancies, neither reopening a finding:

1. **B-4 row count**: the disposition says "Eight Variables-and-Units rows added"; the working tree contains **seven** new rows (covering ~17 symbols — every symbol B-4 enumerated; `W_current` is represented by the `W` entry). Substance fully closed; the disposition's count is off by one.
2. **Vector 4 residual equation citation**: test vector 4 still says "widens per Eq. [I-133]-[I-136]" with no per-instance lineage qualifier. It is governed by the Widening Clock realization statement ("the CREAMS equation numbers below cite structural provenance, not literal forms") within the same addendum and by the unambiguous INV-ROUTE-018 adjudication, so A-1 remains closed — but a one-word qualifier ("per the INV-ROUTE-018 lineage realization") would remove the last literal-reading foothold. Editorial only.

## Verdict

PASS-WITH-NOTES
