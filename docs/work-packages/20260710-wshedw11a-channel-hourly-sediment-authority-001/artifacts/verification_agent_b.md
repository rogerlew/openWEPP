# Verification Agent B

Status: EXECUTED

Evidence mode: Static (contract/anchor/artifact reading and consistency reasoning) + Ran (binding-exposure lint; `git diff -U0` change-surface enumeration; scripted table pipe-count check on v50 and v51; clause-level programmatic diff of INV-ROUTE-005; vocabulary survey grep across all SC contracts)

Verifier: Claude subagent (verification B), 2026-07-10

## Regression check

**Change surface (Ran).** `git diff -U0` against committed v50 shows exactly three removed lines: `contract_version: 50`, `last_reviewed: 2026-06-14`, and the single INV-ROUTE-005 row. Every other hunk is pure addition (anchors +8, Variables-and-Units +7, invariants +6, guard rows +6, BEI +1, degenerate states +3, invalid states +4 bullets, TOL +3, W11A addendum +160, gap register +2, revision history +1). No pre-existing row was deleted or edited except INV-ROUTE-005.

**INV-ROUTE-005 clause integrity (Ran, programmatic clause diff).** Preamble and clauses (b), (c), (d) are byte-identical to v50; severity/authority/evidence columns identical. Only (a) and (e) changed, exactly as the disposition and v51 revision row claim. (a) adds the dependency-authority definition parenthetical and changes "channel-hourly surfaces" to "channel-hourly surface authority" — it defines a previously undefined term (v49/v50 never said what a channel-hourly surface for an upstream channel *was*) and narrows nothing; the hour-resolved superposition mandate, peak/volume/timing formulas are unchanged. (e) converts the unconditional scope limit into a lane conditional: the inactive-lane branch preserves the v50 requirement (active-hour time base, carried per-hour inlet array) in full, and the closing prohibition is strengthened from unqualified to "invalid on both lanes." Not a weakening in either clause.

**INV-ROUTE-015 biconditional vs 005(a) vs Activation section — composition (Static).** No blocking circularity: 015 defines activation of channel C in terms of (i) C's own `ipeak >= 3` wave routing and (ii) 005(a) authority at C's inlet, where the 005(a) dependency-authority definition refers only to *strictly upstream* channels' activation. On the acyclic watershed network this is well-founded recursion (base case: channels with only hillslope contributors), and the Activation section makes the evaluation order explicit ("evaluated in topological order and propagates down an all-hourly network"). No contradiction among the three: the impoundment exclusion is stated identically in 005(a) ("carry no such authority"), 015 ("precludes activation"), and Activation ("do not activate ... remain on the existing INV-ROUTE-005 branches"); the all-hourly-inlet-but-`ipeak<=2` case resolves consistently to the 005(e) inactive branch (predicate (i) fails; (e) inactive-lane text governs with the S_h active-hour time base); the mixed-authority downstream case resolves to 005(c) fail-closed, which the Activation section restates without altering (c). The anti-partial-execution rule is correctly scoped to activated channels with the non-activated `ipeak >= 3` carve-out, avoiding the false-positive the pre-fix text would have created.

**INV-ROUTE-018 vs REF-ROUTE-CREAMS-CH3-WIDEN (Static).** No remaining conflict. The anchor row documents the CREAMS-literal content (`omega = 1 - exp(-t_star)`, [I-133]-[I-140]); the rewritten invariant states the governing realization (`wstar = (1 - exp(-1.0176*t_star))/1.0176`, exponent-dropped linear rate, fitted `f(x_b)`) and explicitly scopes the citation: "CREAMS Eq. [I-133]-[I-140] is cited as **structural provenance** for the law's form, not as a literal override of the lineage realization." The Widening Clock section repeats the scoping ("the CREAMS equation numbers below cite structural provenance, not literal forms"). Anchor describes the source; invariant binds the realization — coherent.

**Zero-by-construction storage rule (Static).** INV-ROUTE-019 ("no sediment mass is attributable to end-of-grid routed water storage on this lane"), INV-ROUTE-020(c) (zero by construction on the quasi-steady lane; deposit-at-grid-end scoped to the recorded unsteady fallback lane; cross-midnight carry invalid on both), test vector 6 (nonzero water storage, zero storage-attributed sediment, TOL-ROUTE-007 closure), the End-of-grid degenerate-state row, sequencing step 2, and GAP-ROUTE-013 ("moot by construction" on the ratified lane; the recorded decision governs the fallback lane only) all state the same two-lane disposition. No residue of the old deposit-at-grid-end-on-quasi-steady-lane semantics anywhere (Ran: grep on "grid end/grid-end" — all five hits are fallback-scoped or the water-storage-neutral closure statement).

**Pre-existing invariants/guards/tolerances/BEI (Ran, diff enumeration).** INV-ROUTE-007/009/010 rows, guard rows 001-014, TOL-ROUTE-001..005, and BEI-ROUTE-001..006 are untouched context lines. Nothing weakened or broken.

**Cross-references (Static + Ran greps).** All resolve: GAP-ROUTE-012 is pointed to only from INV-ROUTE-017 (re-erodible bed store — matches 012's subject); GAP-ROUTE-013 only from INV-ROUTE-020(c) and the degenerate row (storage disposition — matches 013's subject); no swapped pointers. TOL-ROUTE-006/007/008 are defined in the tolerance table and every reference (INV-ROUTE-019, guard row 019, sequencing steps, unit bridge, vectors 1/2/6) targets the correct one. All eight new REF anchors exist and every authority cited by INV-ROUTE-015..020 is in the anchor table. `SC-SED-001#GAP-SED-008` exists (open, interchange-surface scope — consistent with how 019 and the operand table use it). `WSHED-W11-HOLD-001` resolves (ROADMAP WSHED-W11A row + package.md). W11A vectors number 1-10 with no gaps and coherent subjects. No orphaned "WS18-WS31" mentions (Ran: zero hits; the defined term "WSHEDIMPL18-41 migrated segment-solve lanes" is used consistently in INV-ROUTE-016/018, guard row 016, unit bridge, sequencing step 1b, Widening Clock).

**Binding-exposure lint (Ran).** `.venv/bin/python tools/check_sc_binding_exposure.py docs/specifications/science-contracts/contracts/SC-ROUTE-001.md` → `PASS ... 7 binding exposure row(s) fully consolidated`. Matches the disposition's claim.

**Markdown table integrity (Ran, scripted).** With code spans stripped, all 18 tables in v51 have every row matching its header's column count; same check passes on v50 (17 tables). The only naive-pipe-count anomaly in the file (Symbol Alias Map line 201, literal `|` inside a backtick span in `{field=Qpo|Durrof}`) is byte-identical in v50 — pre-existing, untouched, not a regression. All diff-hunk tables (anchors, variables, invariants, guard map, BEI, degenerate states, tolerances, operand assembly, gap register, revision history) are well-formed.

**Notes (non-blocking):**
1. Test vector 4 says "widens per Eq. [I-133]-[I-136]" without repeating the "structure" qualifier; the Widening Clock preamble one subsection above scopes all CREAMS equation numbers "below" as structural provenance, and the binding invariant row is unambiguous, but a hyper-literal reader of the vector in isolation could momentarily misread. Cosmetic.
2. The disposition's B-4 row says "Eight Variables-and-Units rows added"; the diff adds seven (V_h/S_h; q1/ntchr/dtchr; widening family; omega/t_star; rates; rho_soil; timpot/timex). Off-by-one in the disposition artifact only; the contract itself is consistent and B-4's substance (all reviewer-named symbols covered + unit bridge) is fully delivered.
3. BEI-ROUTE-007's note attributes lane "selection" to the INV-ROUTE-005(e) conditional while INV-ROUTE-015 declares the biconditional predicate; the two compose (015 defines the predicate, 005(e) states which authority governs each side) — loose wording, no contradiction.
4. Pre-existing, untouched: the v47 revision-history row describes the 2026-07-04 additions as clauses "(c)/(d)" where the current lettering is (d)/(e) — a v49-era relabel artifact, not introduced by this pass.

## Rejected-finding validation

**A-8 as filed (read from review_agent_a.md):** Low severity, self-labeled "Governance nit only" — GAP-ROUTE-012/013 carry Promotability "closed," which the reviewer says the register "otherwise uses for resolved defects (GAP-ROUTE-009..011)"; proposes "accepted-labeled (or equivalent)" for retained limitations.

**Disposition's precedent claim — verified accurate.** GAP-ROUTE-004's actual row text: "Chapter-13 mixed-unit and regression-derived formulation caveats remain and are explicitly retained as documented limitations with governance risk acceptance," Promotability `closed`. The disposition's quotation is verbatim-faithful. The precedent is exactly on point: a permanently retained documented limitation with governance risk acceptance, marked closed. It is additionally reinforced by GAP-ROUTE-001 (residual automation limitation "explicitly risk-accepted," closed) and revision-history v8, which records the deliberate governance act ("dispositioned GAP-ROUTE-001 and GAP-ROUTE-004 from promotable-with-risk to closed via explicit governance risk acceptance"). The reviewer's own premise — that the register uses closed only for resolved defects — is factually incomplete within the very register cited.

**Vocabulary-divergence claim — verified supported.** Ran: survey across all `docs/specifications/science-contracts/contracts/*.md` gap registers finds exactly four Promotability values in use: `closed` (45), `promotable-with-risk` (28), `non-promotable` (23), `open` (1). No "accepted-labeled" or any retained-limitation value exists anywhere; `science-contract-spec.md` requires "Gap register and promotability labels" without enumerating a vocabulary, so established cross-contract convention is the operative authority, and introducing a fifth value would indeed diverge from it. The disposition's mitigation observation is also accurate: both gap statements carry the reviewer's intent in-row (GAP-ROUTE-012 "Retained deliberately with governance risk acceptance"; GAP-ROUTE-013 "labeled decision ... Revisit only with an explicit acceptance driver").

**One imprecision (non-blocking):** the parenthetical "(registry rules: short, lifecycle-oriented rows)" loosely attributes the convention to the AGENTS.md "Registry Rules" section, which actually governs the `index.md` lifecycle registry (required fields, short notes), not gap-register promotability vocabulary. The rejection does not rest on this citation — the verbatim GAP-ROUTE-004 precedent and the corpus-wide vocabulary evidence carry it — but the attribution is inexact.

**Verdict on A-8 rejection:** authority-backed and correctly characterized. The finding's proposal is accurately restated, the governing precedent is quoted verbatim and is directly applicable, the convention claim is empirically true across the contract corpus, and rejecting a Low-severity self-described nit on reasoned precedent is a proportionate disposition.

## Verdict

PASS-WITH-NOTES

No new regressions from the post-review fix pass: the fixed text is internally consistent (activation biconditional composes with the 005(a) dependency-authority definition and the Activation section without circularity; INV-ROUTE-018's structural-provenance scoping resolves the anchor conflict; the zero-by-construction storage rule is stated consistently across 019/020(c)/vector 6/GAP-ROUTE-013), no pre-existing invariant/guard/tolerance/BEI row was weakened, all new cross-references resolve, the lint passes (7 rows), and all tables are structurally sound. The A-8 rejection rationale is supported by verified in-register precedent and corpus-wide vocabulary convention. Notes are cosmetic: vector 4's unqualified CREAMS citation, the disposition's "eight rows" miscount (seven), the loose "registry rules" attribution in the A-8 rationale, and one loose BEI-ROUTE-007 wording — none binding, none requiring a further fix pass before ratification.
