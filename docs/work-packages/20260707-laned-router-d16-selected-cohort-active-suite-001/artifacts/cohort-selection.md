# Cohort Selection

Status: EXECUTED. Evidence mode: Static + Ran.

## Selection Rule

The selected suite uses the previous D16 minimum cohort shape, but converts the
external members from inventory roots into package-local openWEPP active
hillslope runs:

- H2637 high-runoff reference from the existing native active fixture evidence.
- One representative hillslope from each owcmp inventory root.
- External representative selection uses the lowest `wepp_id` in
  `landuse/landuse.parquet` matching the target `disturbed_class`.

## Selected Members

| Member | Source | Selected `wepp_id` | Class | Reason |
|---|---|---:|---|---|
| `h2637` | `docs/work-packages/20260707-laned-router-d16-hybrid-default-promotion-001/artifacts/h2637-prechange-active-plain` | `2637` | Native H2637 route-coeff fixture | High-runoff multi-OFE Lane-D reference already used by D16. |
| `mn_corn_h4` | `/wc1/runs/al/algebraic-radium` | `4` | `agriculture crops` | Lowest row-crop member from the Minnesota corn owcmp inventory root. |
| `n_idaho_forest_h1` | `/wc1/runs/un/unpalatable-rind` | `1` | `forest` | Lowest forest member from the N Idaho single-OFE owcmp inventory root. |
| `wa_cascades_forest_h1` | `/wc1/runs/ar/arboreal-dendrite` | `1` | `forest` | Lowest forest member from the WA Cascades MOFE owcmp inventory root. |

## Authority Notes

- The three external members carry `_map = disturbed` in
  `landuse/landuse.parquet`.
- `landuse.parquet` records the `wepp_id`, `man_fn`, and `disturbed_class`
  used for native route-coefficient production.
- The route coefficients are generated by WEPPpy Disturbed from explicit class
  defaults. They are not inferred from legacy WEPP management fields.
- The selected external roots' persisted lookup tables predate the new route
  columns. That is acceptable for package-local regeneration because the
  producer code now owns the additive native coefficient table, and this package
  records generated files under artifacts rather than mutating the roots.
