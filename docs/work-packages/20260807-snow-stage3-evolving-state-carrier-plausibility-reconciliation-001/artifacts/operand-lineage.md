# Operand Lineage

Status: `queued`.

Evidence mode: `not-run`.

Before implementation, tabulate every forcing, geometry, temperature,
humidity, stability, exchange, energy, raw vapor, bounded transfer, mass,
support, and aggregation operand with units, interval/area basis, source,
authority class, applicability, and rejected aliases.
