# Wind-Forcing Custody

Status: `queued`.

Evidence mode: `not-run`.

Record provider, source file and hash, cadence, spatial support, units,
reference-height/exposure metadata, transformations, runtime consumer, and
whether `open-site`, `above-canopy`, `within-canopy`, or `sub-canopy` is proven,
rejected, or unresolved for each site.
