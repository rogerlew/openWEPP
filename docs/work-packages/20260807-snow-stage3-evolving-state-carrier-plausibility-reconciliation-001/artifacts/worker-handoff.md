# Worker Handoff

Status: `authority-shaped follow-on required`.

Evidence mode: `Ran`.

First actionable next package: a result-blind wind-forcing source-custody and
exposure-authority reconciliation.

It must trace each fixture's CLI `w-vl` back through its actual GRIDMET product,
version, grid cell/spatial support, native cadence, aggregation/transformation,
reference height, and exposure metadata. It must decide whether the value is
applicable to the modeled forest snow surface and virtual `5 m` geometry. If
the retained generation lineage cannot supply those facts, the package must
record `AUTHORITY_MISSING` and identify the external authoritative metadata
needed; it must not infer exposure from the values or fit attenuation.

Only after exposure/applicability is resolved may a separate contract package
ask whether a licensed canopy-aerodynamic operator is required. The Paradise
WY2015 support failure and comparable physical magnitude-envelope authority
remain explicit secondary blockers. No persistent shadow, physics correction,
or production cutover should begin from this handoff.
