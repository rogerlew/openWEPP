# Gate Results

Status: `queued`.

Evidence mode: `not-run`.

Declare direct validation requirements before implementation. At closure,
classify each as `PASS`, `FAIL`, `BLOCKED`, or `NOT RUN`, with exact command,
source identity, result, and artifact path. Physical `DIVERGES` is distinct
from execution failure.
