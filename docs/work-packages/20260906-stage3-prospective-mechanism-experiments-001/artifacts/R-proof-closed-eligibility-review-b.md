# R-PC1 prospective authority review B

Static: independent canonical-authority review before selector behavior,
2026-09-06. No build or runtime execution. Review A was not consulted before
this first independent judgment.

## Findings

No authority blocker in canonical `SC-LANDSURFACEENERGY-001.md:3227`,
`EXP-STAGE3-20260906-R-PC1`, whole-file SHA-256
`c993020e96aec0dac934acd5ccfa3050f8173a90bcd7f860b88b8da7405418cd`.
Inspected all 59 additive lines and `R-proof-closed-authority.md`; historical
rows/results remain unchanged.

The condition admits affected sun/shade replay only at exact binary64 beta one
before replay-capability creation. The unchanged same-probe successful current
call then supplies the exact canonical maximum result under INV163. Wet/stem
retain the graph proof of every leaf operand remaining unchanged. Other cases
select complete evaluation before replay, as INV164 already requires for
unproved component probes. Physical admission, domains, arithmetic, guards,
source order, solver, ownership and output do not change. This is proof-closed
optimization scope, not a new physical bound or recovery fallback.

The amendment explicitly retains other normative implication/boundary tests,
current-leaf source-real errors, complete graph/custody, every-field/Jacobian/
potential/fixed trajectory and output/rollback parity, independently derived
raw-beta signed classifications and nonzero completed real-workload replay.
It correctly refuses to infer a theorem from 198 successful diagnostic bases
with no maximum-only witness.

## Concrete derived-test gate

Subsequently independently reviewed the frozen R runtime test module SHA-256
`113a06e5cf74fad506b3192b604248c3c6129281181cbcdb8593d7e545839d54`
and `R-runtime-test-plan.md` SHA-256
`73e02f35698d45c03d4cf0b1a7d0017084ab7513945d35b867fbe7325a45b265`,
before selector edits. The test matrix uses raw beta one/next-down/zero/0.4,
both classes and all supplied occupancies, potential/fixed caps and both
canonical signed capabilities. Actual consumed `evaluate_for` calls compare
replay/complete starts, full-evaluation and leaf-call counts, residual/full-field
bits, beta-one probe-current/maximum equality and wet/stem unchanged captures.
Ineligible cases require zero replay starts and only complete leaf work.

All six current-error families now set affected beta one before actual base
admission, retaining source-real first-error/order, zero complete fallback and
rollback assertions. Full-field replay uses the original production capture.
The bounded historical maximum search is no longer invoked; its earlier
unrestricted result is not proof of this selector. Low maintainability debt:
the inert historical test function could later live solely in the retained
source-cut artifact rather than remain allowed dead code; not a gate blocker.

Ran: isolated-R `git diff --check`, PASS. No Rust test/build. Independent real
raw-beta stencil collection, other matrix proofs, executable parity/custody and
all scientific admission gates still require their own evidence.

QA: GO canonical authority and concrete derived-test cuts for implementation of
the declared PC1 selector, subject to the other independent review. This is
permission to author/test the specified behavior, not an executed correctness
pass, proof waiver, measured benefit or production promotion.

## Independent collector adaptation

Static: inspected `reproduction/audit_stencils.py` SHA-256
`39fd80bc3d333c1f4be437b0dc55f34536c6e2b8490b8ec7f9927342d1b72e73`:
classification reads the actual base's affected sun/shade beta bits at indices
10o+4/5 and retains wet/stem, canonical stencils/anchors and exact lifecycle
reconciliation. Graph completeness remains a separately explicit assumption/
gate, not a claim derived from labels. The old unrestricted script is retained.

Ran: `.venv/bin/python` on `reproduction/test_collectors.py`, seven tests PASS
(test-file SHA-256
`830d10b39999a2ebacbe014027dc5179862357eeb5be56e655a741eec98694c4`).
This is offline collector evidence, not a real replay trace. Low follow-up:
add opposite sun/shade beta values to the direct predicate matrix to target
class-index swaps explicitly; its present per-class cases set both beta values
equal. Real R-PC1 trace reconciliation and nonzero completion remain pending.
