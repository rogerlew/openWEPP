# R-SG1 prospective assurance review B

Static: bounded review of the proposed text and actual canonical/replay calls,
2026-09-06. No build, test, parameter search or canonical edit performed.
Reviewed `R-shared-guard-authority.md` SHA-256
`856ff186550815276e2baf205f7d1a4f127bfa07e63ed6d43a4155e53ff7131d`.
The proposal remains an artifact, not adopted canonical authority.

## Findings

No blocker in the exact proposed canonical text. It explicitly replaces only
the prospective assurance classification of actually recomputed instances of
route.prepare/wet/finalize and occ.wet/liquid, including downstream occupancy
instances. It does not claim historical C020 noncrossability was proved, assert
crossability from a symbolic concern, or introduce a physical pressure bound.

For these executed nodes, exact same signed-probe operands and branches reach
the same shared canonical calls in the same source order; result or first
existing typed error returns directly. This is a valid behavioral equivalence
obligation even when no global successful-base-implies-probe-success theorem
has been established. It is not ordinary tolerance-based numerical agreement.
The proposal honestly labels the qualification change rather than claiming the
old assurance gate passed. No equation, physical admission, guard, solver,
ownership, retry, or publication semantics change.

Every skipped/copied node still requires successful actual-base capture and
bit-identical complete-call operands/branch under the complete graph. Earlier
skipped fallible guards are included, preserving first-error precedence. Both
source-distinct wet/finalization calls retain their roles and counts. PC1
maximum, current-leaf errors and longwave/hydraulic/route-match/lower obligations
remain unchanged; the text does not hide a broader proof substitution.

## Required concrete source/test gate

Before adoption/admission, review the retained exact call/operand/source-order
proof and R-only derived tests for every named family and copied/recomputed
instance, including upper-to-lower routing. Preserve actual production capture,
full field/ledger bits, wet/dry/zero/exact-capacity successes, source-real
natural-error differential catch-all, no later work/retry after first error,
and exact rollback/custody. A success-only fixture is not evidence of an
unexecuted error surface. Do not forge a private intermediate or embark on a
new parameter hunt to manufacture a crossability label.

The canonical append must wait until the active main-source gate is finished,
then receive its own exact hash/cut identity. Historical logs and PC1 authority
remain unchanged. Scientific mechanism admission, runtime correction review,
full validation and real consumer evidence are still separate obligations.

QA: GO exact proposed SG1 text; concrete derived-test/source-proof cut remains
pending, so this is not canonical adoption, measurement admission, historical
C020 qualification or production promotion.

## Concrete source and corrected rollback test review

Static: GO concrete prospective cut, still no build/test by reviewer B.
Instruction discovery again returned root and work-package AGENTS.

- Source proof `R-shared-guard-source-proof.md`:
  `48e5dcfe7ea904e19bf434023a87e87da0efde87900879cb4718a719551c72de`.
- Formatted runtime tests:
  `638de1d0e5d04c32f4e8a5e4f10a9fe2fca2f2f964ed2515499b061ed162e2e2`.
- Runtime plan:
  `70d64ad77e98323ea16d48cd7fb3f46711f98b107f10d460765c705f1c3b0aa3`.
- Production replay:
  `0c557403ded163aa67867a28d3eecdfd9444908da3666ab3469670f4befdb7ce`;
  evaluator `c23618821c6c2e3a9bc954959585ba42ab93afe452015859d88747cee6e637a2`;
  liquid `56d1dde3832175db9a2cd60f8859ed001982e4867b4ad84c1190545396803d18`.

Verified the five exact helper bodies/call positions against the source proof.
The top-down incident-rain induction preserves preparation/wet/finalization
operands and `?` order at every reachable rank. Copied earlier route and leaf
guards retain successful actual-capture operands; the second wet/finalization
calls are still executed separately in the shared occupancy body. No pressure
margin, guaranteed changed-wet success, forged error witness or skipped first
error is claimed. Existing source-real leaf errors remain independent evidence.

The source proof correctly identified the old natural Err/Err rollback assertion
limitation. The newer test cut above resolves that assertion gap: an explicit
recursive float-bit vector covers all13 nested declared input types (including
Option floats, complete shortwave bands, each occupancy/soil/root/lower-boundary
field); typed clones separately preserve IDs, enum/Option tags, map keys and
cardinality/order. Actual base evaluation and actual capture are cloned, then
checked with the existing full-field bit oracles. Trial, caps, frozen maps,
base/validated/column/caps/boundary pointer joins, lifecycle and exposed graph
digest are retained. There is no Debug-string or padded Rust-memory surrogate.
The snapshot is asserted on natural Err/Err and after the named real-error
comparisons. Natural Err/Err still returns immediately; no later probe is
executed. Private graph masks remain protected by immutable ownership and the
independent graph corpus; they are not falsely described as serialized here.

The proof's old test hash and open assertion note remain historical; this
addendum binds the corrected cut. Current QA GO permits parent-confirmed
canonical adoption of the exact draft, followed by required execution. It does
not claim natural wet-error execution, historical noncrossability success,
measurement admission or general release conformance.

Non-blocking maintainability follow-up: explicit nested float encoders should
be reviewed whenever those DTO declarations gain a field. Typed equality alone
will not detect an omitted future signed-zero-only mutation.
