# Independent protocol review B — draft 1

Static: prospective design review, 2026-09-06; no comparative samples or heavy
commands. Independent first finding submission preceded any consultation of
Review A. Reviewed `experiment-protocol.md` SHA-256
`fff88645ff2b958f3ad8b18491f3e3f2114d18eb08606cdf135b2f02c34e60ef`
and `package.md` SHA-256
`2233064b4d4f90d154e30b5bae0fc851f9d661b650d89b79ce2e6dae14bc9ea9`.
Canonical amendment identities are retained in `review-b.md`.

## Findings

No blocking protocol-design finding.

| Requirement | Assessment |
| --- | --- |
| Independence (`experiment-protocol.md:7`) | One newly frozen A, independent F/R source deltas, complete path/build/input identity, fixed executables and new series after relevant change. Historical binary custody and combined-treatment arithmetic are excluded. |
| Authentic measurement (`:24`, `:30`, `:56`) | Real authenticated owner/runner path, whole production call including publication, paired fresh processes, balanced order, fixed CPU/stack/build/counter posture, bounded retries/timeouts and retention of valid slow samples. CPU tick resolution is explicit. |
| Anti-tautology (`:43`, `:47`, `:105`) | F joins actual invocation identities and complete forced-reference results. R's canonical amendment requires independent stencil/classification reconstruction and direct graph expansion; producer counters/labels cannot be the expectation. Output/closure reconstruction remains an additional admission requirement. |
| Memory (`:79`) | Separate six-pair series; distinguishes endpoint, sampled active maximum and wait4 lifetime peak. Explicit teardown and three processes of ten runs support bounded lifetime observations without claiming long-run bounds. Boundary reads occur outside the timed consumer interval. |
| Opportunity cost (`:69`, `:95`, `:111`) | Prospective wall/CPU priority rule and paired-bootstrap interval, one extension, one performance refinement, bounded allocation follow-up, scale checks, independent arm dispositions and mandatory architecture handoff. No favorable bucket alone establishes a complete-run win. |
| Validation (`:108`) | Full critical-cut correctness and science/consumer/guard/closure evidence remain admission requirements; expected-red/inherited results cannot become PASS through the new authority. |

## Non-blocking implementation follow-ups

| ID / severity | Path | Action before freezing the applicable series |
| --- | --- | --- |
| PB-1 / Low | `experiment-protocol.md:95` | Record which explicit arm dispositions count as "competitive" for secondary 10/19-OFE runs, and the rationale for each inclusion/exclusion. This avoids an opaque post-result selection rule. |
| PB-2 / Low | `experiment-protocol.md:83` | Name exact mapping fields and units in emitted data. Local `/proc/self/smaps_rollup` has `Pss_File`, `Pss_Anon`, `Anonymous`, `Private_Clean` and `Private_Dirty`; file PSS is not file-backed RSS, and private+anonymous are overlapping descriptions. Do not derive or label a file/private RSS partition from these unlike quantities. |

These are reporting/implementation details within the already authorized
measurement design, not requests for a new observer campaign or unrelated
acceptance gates. Preserve detailed-audit versus compact-counter separation;
trace completeness and actual lifecycle cardinality must be checked before
the oracle can admit R. The concrete harness/oracle source and executable
results remain subject to later immutable implementation review.

QA decision: GO for draft-1 protocol design. This review is not execution
evidence, measurement admission, package closure or production qualification.
