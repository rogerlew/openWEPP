# R implementation plan

Static: experimental isolated R only. Critical solver impact; full correctness,
LSE forced-complete node/residual/Jacobian/solve and error parity, real runner
output parity, graph oracle, lint and independent reviews remain required.
Parent owns serial builds/measurements and runner oracle; no worker builds.

1. Add runtime-derived differential assertions at the authentic existing
   represented-snow fixture before behavior edits. Static expected-red binds
   absent replay dispatcher/capture; prospective canonical authority tests have
   already passed in parent artifacts/raw/authority-R-test.log.
2. Capture successful liquid routing and current/maximum leaf states from one
   canonical evaluator. Share all physical arithmetic between full and replay.
3. Bind a topology graph to validated inputs and a successful immutable sweep;
   mint consumed signed capabilities at canonical construction. Replay only
   represented-snow component probes; all other selections stay canonical.
4. Preserve probe order, finite differences, dense solve, backtracking and
   result assembly. Record ComponentReplay before invoking replay.
5. Compile/test through parent's serial queue; fix correctness issues and
   deliver exact source cut for dual review and controlled measurements.

API cut for prospective review: `CoveredReplayCapture` stores per-occupancy
`CoveredLiquidPreparation`, finalized liquid ledgers and four `LeafTrialState`
values in canonical sun-current/shade-current/sun-maximum/shade-maximum order.
`evaluate_covered_column_validated` remains the public internal complete entry;
its shared evaluator accepts optional capture and replay operands. Capture is
published to the iteration object only after evaluation succeeds. Graph belongs
to `ValidatedCoveredEvaluationInputs`, created once per validated solve. Base
owns capture plus exact trial and frozen branches, borrows validated inputs.
No base/probe result crosses a sweep. Signed capabilities borrow that exact base
and canonical constructor-owned probe slice, bind coordinate/sign/h/stencil,
and are non-Clone, consumed by value. Production construction replaces neither
existing +/- arithmetic nor bound tests. Untrusted test adapter does full shape
and sole-coordinate checking; canonical constructor proves it directly.

Only graph-unreachable route and leaf nodes copy. Every remaining tail node
executes the existing shared expression even if unreachable; no new dependency
edge or claim that those extra executions were eliminated is needed. Captured
copies retain the existing routing-prefix arithmetic and both wet finalizations.
Tests compare full captured fields, residual/tolerance bits and finite-difference
columns, existing forced full solve outcomes and immutable inputs. Source-real
VPD boundary corpus uses unchanged canonical perturbation around saturation,
with zero-area/exact-beta/wet-capacity success vectors; no fault injector.
Private custody is enforced through lifetimes and consumed values, with invalid
shape/multicoordinate/noncomponent probes retaining canonical selection/error.

No calibration parameter or equation changes. Existing solver quarantine is
unchanged; no successor or recovery solver is added. Source files near 3000
lines require extraction into named replay module before closure.

Intrinsic custody proof: the real transaction map owns the solve invocation;
its validated-input object lives inside that solve. Each Newton iteration owns
one successful iteration evaluation. The Jacobian base is constructed within
that iteration's sweep and owns capture/trial/frozen values, borrowing that exact
validated object (including immutable caps and graph). Canonical pairs borrow
the base; each signed non-Clone capability owns its canonical probe and cannot
outlive the base. Evaluation consumes the capability and compares its bound base
reference with the expected base via exact pointer equality, rejecting a foreign
live base as ConstitutiveDomain("covered_component_dependency_replay_integrity").
No observation ID authorizes reuse: disabled observation leaves the identical
structural custody. The optional read-only lifecycle accessor merely associates
the actual existing event identities with the same scopes. Replay errors return
directly; no full evaluator follows a replay failure. Base/capabilities never
enter a result, warm start, transaction, retry, or publication object.

Additional exact R-only source path authorized by parent before edits:
crates/openwepp-land-surface-energy/src/solver_mechanism_audit.rs for a read-only
current-lifecycle accessor. Root/crates AGENTS apply. A remains frozen.

R-only real consumer oracle addition authorized before edits:
crates/openwepp-runner/src/hillslope/tests03/controlled_mechanism_experiments.rs,
new ignored `stage3_controlled_mechanism_forced_complete_experiment`. It wraps
the same authenticated common workload in the scoped test-support complete-probe
oracle. It has no measured-treatment selector and does not change A's harness.
Root/crates AGENTS apply; source 1–28 and 116–170 read for entry/consumer joins.

Before callback activation the parent additionally approved, on that same exact
runner path, `stage3_controlled_mechanism_node_oracle_experiment`. It is the
separate actual-N2/S6 potential/fixed full capture oracle. Its injected probe
events are diagnostic only; the normal and forced-complete tests keep callback
off and supply uncontaminated lifecycle/output parity. No audit suspension or
suppression is used. Compiler-negative custody checks use temporary copies of
the actual R source with attempted clone/reuse/escape; unrelated privacy errors
cannot close those checks.

R-PC1 prospective write-map addition: isolated R
`docs/specifications/science-contracts/contracts/SC-LANDSURFACEENERGY-001.md`,
only the reviewed EXP-STAGE3-20260906-R-PC1 additive supplement, copied verbatim
from retained canonical authority after dual GO. Root/science-contract AGENTS
discovered and read; the entire 58-line (4241-byte) supplement was read before edits.
Existing approved replay/solve paths receive the exact-beta-one admission
predicate before component capability creation; solver_tests binds the existing
two-occupancy fixture to the reviewed potential/fixed selection and error corpus.
No new numerical domain, h, stencil, arithmetic, or error retry is authorized.
