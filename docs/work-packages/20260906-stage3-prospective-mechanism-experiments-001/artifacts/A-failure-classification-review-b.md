# A full-gate failure classification B

Static: bounded independent diagnostic support, not terminal verification.
Read retained logs, exact named child stderr files, source guards and relevant
pre-A bytes at `e89befa4678eadec039b3e7f7fe0a176af8e9dc5`. No build/test,
fixture cleanup, source mutation or assurance adoption executed. Lightweight
Python parsed logs/hashes only. Instruction discovery for this artifact returned
root and work-package AGENTS; package governance was read.

## Findings first

- P2, introduced common-harness guard mismatch:
  `tests/integration/stage3_native_vegetation_laned_throughput_recovery.rs:936`
  still counts four canonical manifest reads solely in
  `stage3_runner_qualification.rs`. A extracted one into
  `controlled_mechanism_experiments.rs:253`, leaving three plus one. The pre-A
  qualification file contains four. Full03 line1997 records 3 versus 4.
  Follow both real source files, preserve total four and absence of stale
  `manifest["execution"]["laned_active"]`, and bind the original baseline
  entrypoint's call to `controlled_mechanism_run(1, 0, false)` plus its actual
  `03_tests.rs` include. Do not lower four to three or merely add a token.
- P1 evidence/disposition risk: the 181 failures are not all stack/timeouts.
  There are 37 direct stack overflows, one external kill, and 143 other failures.
  Six nested watershed failures contain actual typed custody refusals, not
  stack overflow. Eleven additional tests fail runtime/model/custody assertions.
  Their inheritance is not established merely because A is called baseline.
- P2 inherited-source and provenance debt: several static guards and frozen
  identity fixtures were already stale before A. Preserve FAIL and distinguish
  stale binding/guard evidence from proof that the protected invariant passes.

## Complete debug-full03 family accounting

Log: `raw/A-gates-01-workspace-full-03.log`, SHA-256
`af351cd3b51af5303c711f4a14a6460521bf9229f28464949f8ff97d34678ab6`.
Recorded argv: `timeout 1200s nix develop --offline -c cargo nextest run
--workspace --profile full`; source commit label `2b56e6ebc9c8`, dirty tree.
Unoptimized build, no 64 MiB stack override in recorded argv. Final result:
4202 tests run, 4021 passed, 181 failed, 75 skipped; exit124 after1200.184s.
The last test was externally SIGTERM-killed after673.369s. That incomplete
execution is not a canonical full PASS.

Parsing each failure header once yields the following disjoint totals:

| Diagnostic family | Count | Evidence/disposition |
| --- | ---: | --- |
| Assurance identity/status | 82 | 76 explicit generated SC-SNOW identity drift; six derivative SHA mismatch/not-current/Selected-versus-Current failures. Inherited stale lock, additionally affected by this package's additive authority; not pure infrastructure. |
| Direct stack overflow | 37 | Explicit `has overflowed its stack`/fatal stack overflow. Root cause beyond stack posture not established; corrected execution can expose later failures. |
| Other textual/source guards | 34 | Old versions/index prose, symbol/receiver names, fixture source literals and frozen impact row count. Concrete inherited examples below; no blanket scientific PASS. |
| Runtime/model/custody assertions | 11 | Five evaluation-budget refusals, two dependent-output-instability restart refusals, one empty-active-topology identity error, one receiver-publication-support error, one E002-versus-E008 precedence assertion and one 1-versus-2 count assertion. |
| Nested watershed child failure | 6 | Five forest-litter/open-snow custody refusals; one pending-parent-finalization source identity refusal. Actual child stderr inspected. |
| Fixture setup | 3 | Two flat-copy operations encounter a directory; one century climate runfile span expects one year but sees100. |
| Historical structural expected-red | 2 | Revision31 missing replay declarations and revision61 absent original seam names. Preserve historical scope; experimental runtime gates separately qualify new mechanisms. |
| Oracle shared-library environment | 2 | VEG-E-133: runtime mismatch is not libcrypto-only; both `libcrypto.so.3` and `libz.so.1` differ. No equivalence PASS. |
| C3 frozen source binding | 1 | V10 state hash drift, already present before A, detailed below. |
| Generated Rust fixture API mismatch | 1 | Reference fixture fails E0308/E0599 at its soil-thermal restart accessor; not timeout. |
| A extraction source guard | 1 | Confirmed introduced four-read guard mismatch above. |
| External total-run timeout | 1 | `accepted_stage3_real_runner_routes_lane_d_and_publishes_summary` killed, not a scientific result. |
| Total | 181 | No failure omitted or relabeled PASS. |

## Attribution and bounded next actions

**Confirmed inherited static conditions.** Pre-A byte checks establish:

- `tools/release/authority-policy/impact-map.json` is unchanged; it already
  contains27 WAT5 rows while the test requires22 (log28). This is not five
  newly added common-A rows. Do not revive frozen planner machinery.
- C3 binding expects V10-state SHA `fed6f222...`; pre-A and current
  `crates/openwepp-vegetation/src/v10_state.rs` are both `ebd92a14...`.
  The other three named source/ledger bindings match. This isolates the stale
  state-source binding without executing the schema validator.
- Coupled-time `transaction.rs` is unchanged from pre-A. Its source guard
  splits on a stale exact derive delimiter and subsequently sees `serde_json`
  (log2335); inspect its intended function boundary, not a physics rewrite.
- `wat5_output_transaction.rs` and `subhourly_generation.rs` are unchanged.
  The former's 1/10/19 literal guard fails because pre-A qualification already
  uses `[1_usize, 9, 10, 19]`, not because A lost an OFE case (log2928).
  Its aggregate-only source-symbol guard also predates A (log2909).
- Century fixture source is unchanged. The one-year-versus100-year parser
  failure is an existing fixture/input problem, not observation code execution.
- Existing science-contract versions/index text and original revision31/61
  expected-red declarations were not replaced by A's additive amendments.
  The corresponding old version/name failures therefore remain historical
  source-binding failures. This does not waive their underlying authority.

`assurance-impact.md` retains the independent read-only pre-A lock assessment:
lock prefix1d23afda, pre-A SC-SNOW prefix7a12fda0, A94eb7a73. Thus the identity
was already stale, and the new authority changes it further. Adopting unrelated
intervening scientific authority is not an implicit step of this experiment.

**Environment/setup failures.** `laned_shadow_h2637.rs:40` calls `fs::copy` on
every directory entry; the fixture has an ignored `output/` directory, listed
with a Sep1 timestamp. It cannot copy that directory as a file. No cleanup was
performed. Repair/fixture isolation needs declared ownership, not removal of
guard expectations. VEG-E-133 requires inspecting both actual dynamic-library
identities; do not relabel a two-library mismatch libcrypto-only.

The generated restart-reference compile at full03 lines3161–3216 passes a
`Result<&SoilThermalSnapshot, DirectV9RealConsumerError>` where a snapshot
reference is expected, and calls `.clone()` on that Result without a Clone
error type. The reference fixture is stale against the existing accessor API;
this is not failure of the production crate to compile. Its guarded correction
must handle the actual typed result rather than erase failure semantics.

**Runtime failures not yet attributable to A observation.** Preserve these
specific failures for corrected-environment reproduction/source disposition:

| Full03 line | Test/diagnostic |
| --- | --- |
| 2384 | `wb14_failure_preserves_every_resource_candidate_byte`: E002 returned, E008 expected. |
| 2549 | `ordinary_physical_reuse_is_byte_identical_to_forced_double_evaluation`: support-liquid receiver publication support. |
| 2571 | `stable_minimum_production_support_accepts_one_direct_trial`: observed1, expected2. |
| 2614,2636,2669,2691,2757 | Interior event capture/execution, mixed open-covered, resolved/snow-free and two-resolved-lane tests: canonical covered evaluation budget. |
| 2724 | Solid-precipitation reappearance: canonical covered empty active topology. |
| 3220,3244 | Restart after/before snow reappearance: canonical covered dependent-output instability. |

No F/R treatment ran in A, so these cannot be F/R-induced. Common observation
source review preserves arithmetic/guard order, which makes inheritance
plausible but does not supply pre-A executable reproduction. An unchanged test
file alone is insufficient to prove an unchanged complete execution dependency.
Do not invent a single infrastructure root cause or silently approve model
refusals. Reuse exact pre-A execution evidence if it exists; otherwise label
attribution unresolved while pursuing the authorized bounded diagnosis.

All six named nested child stderr paths from log3013–3143 still existed under
`/tmp/openwepp-roger-openWEPP-295c6e060aa9/` when read. wshedw2 relative-output,
wshedw2 serial, wshedw3 jobs1, wshedw7 relative-run and wshedw7r p102 report
`forest litter cannot enter the Stage-3 open-snow solver`. wshedw10 reports
`pending V11 parent-finalization record source`. These are further actual
custody failures; preserve the referenced child logs before scratch cleanup.

## Bounded inactive-observer attribution follow-up

Static inheritance implication, not pre-A executed reproduction: inspected the
complete e89-to-A production hook/wrapper deltas and the observer inactive
branches. Global call-site search finds session activation only in the new
controlled runner helper and observer tests. The helper is called only by its
separately ignored experimental/legacy-positive-profile entries, not these17
runtime/nested failing tests. Nextest runs each test in its own process.

- Orchestrator `stage3_mechanism_experiment_audit.rs:70` starts thread-local
  state as None. `Scope::begin` returns inactive with no record; input closures
  are not called. `complete` changes only its private success boolean; output
  closures are not called. Drop returns immediately. Thus there is no
  fingerprint/format/result access, retained owner, published counter or guard
  feedback when inactive.
- LSE `solver_mechanism_audit.rs:117` also starts None. `Scope::new` has no id;
  leaf/iteration hooks update only Some audit state; detail is false, so no
  duplicate stencil-domain evaluation runs. `finish` returns its exact generic
  Result unchanged after reading `is_ok()`. Probe classification and Drop have
  no active target. No audit count supplies a Newton or carrier budget.
- The new potential-phase MapToken is solely a lifecycle join; its PartialEq
  deliberately returns true, it has no physical serialization, and the V3
  synthetic phase uses its default. `join_map` sees None and mutates no physical
  phase or owner. Existing phase equality/guards cannot acquire an audit-id
  dependency.
- Evaluator/solve wrappers forward the same arguments to the one unchanged
  body and return the same Result. Canonical h, vectors, stencils, minus-before-
  plus calls, residuals/LU/backtracking and error propagation match pre-A.
  Batch observation delays only `?` across inert observation bookkeeping;
  the identical provider Result reaches the same original boundary guards.
  Carrier `is_ok()` observation similarly never selects or retries a result.
- New monotonic clock calls occur only in the controlled test helper. No new
  clock or elapsed observation feeds physical guard thresholds. The Cargo
  change adds the already resolved rustix dependency/feature, not new physics
  configuration or changed dependency versions.

The exact named guard consumers are unchanged from pre-A: forest-litter refusal
in `land_surface_energy_shadow/v3_multitile_adoption.rs:354` reads surface class
and projected tile variant before solver execution; pending-parent source at
`v9_real_consumer_shadow.rs:1517` reads existing vegetation/LSE/BGC transaction
identities; receiver support at `v9_real_consumer_shadow_v10_accessors.rs:910`
reads accepted-publication history. No observer writes any of those operands.
Evaluation-budget/dependent-output/empty-topology decisions in
`v11_covered/canonical_covered_solver.rs` retain their original physical map
charges, candidate comparison and topology operands. The E002/E008 ingress
test's constructor/validation path and stable-support adaptive-count source
also do not read observation counters or call the controlled helper.

Consequently the seventeen typed runtime/custody/assertion outcomes have a
source-level inheritance explanation under the same inputs and successful
execution environment: no common-A hook-to-operand/control dependency was
found. This is materially stronger than test-file equality, but remains Static.
It does not prove historical run results, eliminate compiler/resource effects,
or qualify failing physics. Extra stack frames/local storage can affect stack
capacity, so this implication does NOT classify the37 stack overflows as
historically reproduced. Experimental admission still needs its actual
mechanism consumers, every affected invariant, exact parity and no new defect;
unrelated baseline failures must remain visible with bounded disposition.

## Optimized full run: partial observation only

`raw/A-full-release-01.log` snapshot: first86815 bytes,1858 lines, prefix SHA-256
`22cc6523e51f999b166c480734491a7b94f5612fcebda48953b18c79b2884388`.
It had89 failure headers and no final Summary when inspected. Do not treat
partial counts as the final run result. Twelve CLI01/CLI03/CLI04 cases that
previously stack-overflowed now reach typed runtime failures: nine pending
parent-finalization source mismatches and three dependent-output-instability
refusals. This directly demonstrates why fixing stack posture alone cannot
classify the earlier tests as passed. Assurance drift also persists.

## Optimized full run: final exact classification

The partial snapshot above is superseded, not erased. Completed
`raw/A-full-release-01.log`: 367740 bytes, 7237 lines, SHA-256
`5f4c279602e193c305048e7613337c688728daf7dc87124abbe44083c892917e`.
Parent executed `cargo nextest run --release --workspace --profile full` with
`CARGO_PROFILE_RELEASE_LTO=false`, `RUST_MIN_STACK=67108864`, no external total
timeout. Summary at line3702: 513.757s, 4202 tests run, 4022 passed (2 slow),
180 failed, 75 skipped; exit100. Parent reports whole-wrapper1066.303s.
The repeated immediate/final failure listings were deduplicated by full test
name; there are exactly180 unique failures, all ordinary FAIL, no stack aborts
or externally killed tests in this run.

Exactly179 failing names overlap debug-full03. The formerly stack-aborted
`child2c_scheduler_commits_the_concrete_v11_lse_bgc_soil_owner_candidate` and
formerly killed `accepted_stage3_real_runner_routes_lane_d_and_publishes_summary`
are no longer failed. One name newly fails: LSE
`covered_natural_iteration_limit_matches_declared_rust_outcome`, discussed below.
The143 previous non-stack/non-timeout failures remain failed. The other36
previous stack aborts now expose these disjoint actual diagnostics:

| Diagnostic now reached | Count | Final release evidence lines |
| --- | ---: | --- |
| Pending V11 parent-finalization source mismatch | 20 | 5151–5417; 6785; 6823–6975; 7158; 7179 |
| Canonical covered dependent-output instability | 6 | 5265,5284,5379,6418,6481,6585 |
| Covered probe typed beginning joint custody | 3 | 6203,6725,6744 |
| Snow conductivity pressure must be positive, received0 | 2 | 5513,5532; both expose control-volume T≈−265.926°C |
| Canonical covered evaluation budget | 1 | 6522 |
| Native V3 solver-ready projection identity | 1 | 7217 |
| Inactive covered-native surface received liquid ingress | 1 | 6804; rejection precedes the expected duplicate-mapping poison |
| Missing native-canopy research JSONL | 1 | 7198 |
| Forest-litter/open-snow custody through CLI exit1 | 1 | 7094; actual stderr appears before the exit-code assertion |
| Total newly exposed former-stack diagnostics | 36 | Not stack/infrastructure passes |

Thus180 =143 retained non-stack failures +36 former-stack failures +1 new
release-only numerical golden failure. The additional typed failures remain
scientific/consumer evidence, not waived by the prior inactive-observer
source-level inheritance implication. No historical executable reconstruction
or model repair was attempted.

### Native-canopy missing real trace: confirmed inherited source omission

The actual publication callback in
`hillslope/05_runner_execution_and_outputs.rs:459` calls
`write_canopy_research_trace(day_frame, None)`. The writer in
`direct_publication/day_input_and_helpers/00c_day_input_builder_impl.rs:1052`
returns Ok immediately for the missing builder before reaching its JSONL
writer. These two files have no e89-to-A diff. The test config is present;
the test reaches successful report/pass-file assertions, then fails its real
writer assertion at `03_tests.rs:530`. The test body also predates A (A adds
only another include to that file). No new mechanism counter controls the
literal None or this early return. This is a missing actual trace consumer
already present in source, newly visible beyond the debug stack abort, not
an introduced observation regression. Producer counters cannot close it.
Repair would need authentic per-day/per-lane builder handoff and its consumer
proof; it is not a reason to manufacture a file or weaken the assertion.

### Release-only numerical failure diagnostic mismatch

At final release line6705, the natural-iteration-limit test reaches
`compare_exact_failure` at `covered_oracle_conformance_tests.rs:989` and fails
the exact15-element normalized-residual vector. The test and frozen fixture
have no A-authoring change. Earlier assertions already matched failure kind,
occupancy identity, active bounds, iteration count50 and backtracking count340.
The first actual residual is10845740.51192798 versus10845886.579452142 expected;
all15 values are retained verbatim in raw lines6722–6723. Assertions after that
vector (pivot, matrix norm, per-row bits, remaining diagnostics) did not execute
and must not be reported passed. This is a cross-build diagnostic-golden
disagreement, not evidence of an accepted nonclosing candidate. Its exact
compiler/libm cause is NOT established; neither inheritance of optimized
execution nor harmlessness follows from unchanged test source. Separate
authority disposition is required; no tolerance, expected vector or compiler
flag was changed by this review. Same-build treatment/control exact parity
remains mandatory independently.

Authority disposition: this is NOT merely an observational architecture check.
SC-LSE1028–1058 binds the50-update limit, first typed error, complete ordered
diagnostics and no usable failed candidate; INV-108 at1097 binds deterministic
solver order, and1105 requires natural numerical failure fixtures. The test's
separately SHA-bound Rust diagnostic fixture is explicitly retained by the
authority package's `oracle-fixture-manifest.md:55` and
`final-disposition.md:47`; neither permits a widened numerical envelope.
On the other hand, no inspected canonical rule requires identical floating
trajectories across Cargo optimization modes. `docs/numerics/README.md:16`
states within-config run-to-run determinism, not cross-build golden identity.
All15 observed values differ: max absolute normalized difference is
245.01338251307607 (zero-based row1); max relative difference is
2.2296764159043515e-5 (row4). Actual normalized infinity norm is
20550647.817955893 versus expected20550838.175593577. These magnitudes are
diagnostic differences on an intentionally rejected trajectory, not closure
tolerances to be adopted.

Qualified relative-treatment evidence can retain this explicit build-variant
golden FAIL only with current default-debug exact-golden conformance and
same-build control/treatment full diagnostic, trajectory, first-error and
rollback parity for the affected mechanism. It cannot claim this optimized
test, full run or general release qualification passed. A treatment-specific
diagnostic divergence, wrong failure/iteration/bounds, suppressed error or
usable rejected candidate remains a binding correctness blocker. This review
does not silently promote an existing golden into a cross-build physics law,
nor demote an exact regression gate to unimportant instrumentation.

### Retained child stderr custody

`raw/baseline-child-failures/manifest.json` retains original source paths,
parent log name, exact byte count and SHA-256 for all six debug-full03 and all
six release-full01 child stderr logs. All12 copies were hash/byte verified
after copying through apply_patch. Both runs have exactly the same respective
five forest-litter refusals (each337 bytes, SHA1c56b771...) and one pending-parent
refusal (240 bytes, SHA46e458f1...). Original scratch files were not altered.
Instructions for this bounded artifact write were root and work-package
AGENTS. This is raw evidence retention, not new execution.

## Proposed focused default-debug commands

NOT RUN here. Use the owning source cut, normal Cargo debug profile, recorded
64 MiB stack and external TMPDIR topology already prescribed by the package.
No external total-run timeout; retain Nextest's own configured limits. These
supplement optimized full evidence and never claim debug-full coverage.

```sh
nix develop --offline -c env RUST_MIN_STACK=67108864 cargo nextest run -p openwepp-land-surface-energy --profile full
nix develop --offline -c env RUST_MIN_STACK=67108864 cargo nextest run -p openwepp-hillslope-orchestrator --profile full -E 'test(stage3_mechanism_experiment_audit) | test(feed_forward) | test(terminal_coupling)'
nix develop --offline -c env RUST_MIN_STACK=67108864 cargo nextest run -p openwepp-runner --profile full -E 'test(controlled_mechanism_clock_is_monotonic) | test(explicit_stage3_runner_fixture_bootstraps_before_day_execution) | test(explicit_two_ofe_stage3_runner_fixture_bootstraps_exact_topology)'
nix develop --offline -c env RUST_MIN_STACK=67108864 cargo nextest run -p openwepp-vegetation --profile full -E 'test(occupancy_solver)'
nix develop --offline -c env RUST_MIN_STACK=67108864 cargo nextest run -p openwepp-kernel-contract --profile full
nix develop --offline -c env RUST_MIN_STACK=67108864 cargo nextest run --test stage3_native_vegetation_laned_throughput_recovery --profile full -E 'test(stage3_lane_d_qualification_reads_canonical_manifest_provenance)'
```

LSE covers touched evaluator/probe/custody and exact-dyadic debug assertions;
orchestrator covers new observation and F invocation/guard tests; runner covers
clock and authenticated setup; vegetation retains reachable occupancy debug
assertions; the small kernel contract covers symbol-registry debug assertions.
The integration command targets the exact extraction-guard correction.
Retain each command's selected inventory and fail on an empty selection.
Existing mandatory authority, full-correctness, ignored real consumer/oracle,
restart/closure/output and terminal verification gates remain independently
required. Add exact runtime-failure reproductions only to close a specific
unresolved diagnostic, not a broad second fixture campaign by reflex.

QA disposition: diagnostic support complete against the final release log. Full execution,
failure disposition, common correction verification and terminal dual review
remain open; no full PASS, science admission or package closure asserted.
