Static: independent prospective F API/runtime-test review A. No build, test or
real-carrier execution by reviewer. Isolated root:
`/tmp/openwepp-controlled-mechanisms-Hb6uS2/F`; baseline A
`2b56e6ebc9c8e8073fb68b89626d4d4e2b4602e3`.

## Findings

No blocking findings on the frozen cut. GO for implementation authoring within
the declared F plan. This is not runtime, measurement or production admission.

| File under `crates/openwepp-hillslope-orchestrator/src/` | SHA-256 |
| --- | --- |
| `hydrology/support_helpers_mod/runoff_reconciliation/terminal_carrier_provider.rs` | `aea2e47f8f134dfe6c9f764b2033334665c7ed0d700130a9811f57f42f285c77` |
| `hydrology/support_helpers_mod/runoff_reconciliation/stage3_solver/terminal_feed_forward_tests.rs` | `1f78f061ef5093f99edc05d96d1f5241445f0991f0bccb300d43e06a9cf6d3c6` |
| `snow_stage3_v11_terminal_feed_forward_tests.rs` | `4fbd55a350afab3ebb994fc6872cde0f68dc415236352031de52c9053cc7cef3` |
| `snow_stage3_v11_adaptive_production_tests.rs` | `6a1cc7a02caf0d6eb1a889695cac93522f8de5bbaa1d7200e9bac098a0446954` |
| `v11_covered/carrier_phase.rs` | `d7b8d6e0ea8fde3c07815281f93dcc37291f108c6c28055417d6f9a12f2ab105` |

Also inspected the cfg(test) include/export wiring and parent
F-implementation-plan.md, F-implementation-evidence.md, F-test-plan.md and
F-required-reading.md. The worker confirmed no behavior activation; required
evaluator/helper entrypoints are absent at this static expected-red cut.

## Evidence adequacy and protected behavior

The request has explicit physical fields and no ending hint or coupling
ordinal; exhaustive field destructuring guards that structural capability.
The provider enum chooses an interface before execution. Its proposed behavior
must not choose another interface after failure. Shared canonical surface/flux
arithmetic is required, with the existing generic feedback chronology retained.

Lower tests exercise independent identical requests, discovery/exact
independence, outer guard-before-provider ordering, one versus two provider
calls across Full/Retry/Half1/Half2/Root, bitwise flux/boundary comparisons,
real terminal transition and competing support/role/attempt/beginning poisons.
The constructed boundary fixture is explicitly limited; it does not claim
complete LSE/vegetation/soil carrier or publication proof.

The complete-owner test enables its oracle in the same large-stack thread as
the real workload, requires nonzero records, every authentic role and both
discovery/exact modes. At each actual F result the planned callback runs two
fresh original physical carrier calls from the same immutable beginning and
child identity. It compares the candidate with both complete references and
uses the canonical hydrology preview for the second legacy request's hint.
Beginning owner bytes must remain unchanged; preview deltas must be exactly
zero. The cfg(test) callback must remain excluded from all measured processes.

The frozen comparator lists every `CoveredCarrierPhaseResultV1` field and calls
a private test-only comparator for both ephemeral candidate objects. That
helper includes their previously unexposed soil candidate, soil continuation,
validated resident/candidate owner bytes and soil candidate seal. Canonical
owner byte comparison accompanies typed equality. Debug comparisons cover
full finite-value fields at this exact source cut and distinguish signed zero;
they are test-only evidence, never a production custody seal. Do not generalize
that shortcut to NumericalFailure: its custom Debug omits failed_solution.

## Residual risk and mandatory later execution

Connecting the callback and passing a one-day run cannot alone prove complete
INV-088/C-056 admission. Before measurement, execute full forced-two-call
physical/custody comparisons, every applicable stale/foreign/reuse/forcing/
topology/owner guard and exact error precedence, rollback/restart, independent
discovery/exact/batch/final chronology, real invocation multiset/removal joins,
output identity and independent closure reconstruction, and all selected
critical-cut validation. These are current F-arm obligations, not a later
package. A successful synthetic provider test cannot replace them.

Ran by reviewer: read-only diff/source inspection, exact hash verification,
line counts and isolated `git diff --check` (PASS). No workflow executed.
Adaptive test host 2526 and carrier_phase.rs 2935 lines are WARN; retain the
declared bounded-test rationale and decomposition intent. Recheck the 3000-line
gate after behavior implementation. No existing test assertion is removed or
weakened by this prospective cut; formatter-only changes do not alter it.
