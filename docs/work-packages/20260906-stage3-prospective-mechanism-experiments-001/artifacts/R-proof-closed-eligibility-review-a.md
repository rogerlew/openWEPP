Static: independent prospective scientific/authority assessment A. No source,
contract, test or oracle implementation changed by this reviewer. Read the
owner kickoff, SC-LANDSURFACEENERGY-001 INV-163/164/C-020/EXP-R clauses and the
actual `covered_maximum_leaf_trial_state` implementation. Parent reports the
bounded exploratory corpus executed 198 successful bases with zero maximum-only
counterexamples; this reviewer did not run or independently verify that result.

## Current unrestricted R disposition

NOT scientifically admitted under the retained C-020 maximum-validity proof
obligation. For affected sun/shade probes with beta below one, the beta-one
maximum may execute a different binary64 root/branch trajectory from the
successful current call. The remaining `v10_low_light_accepted_branch` and
trajectory qualifications are not discharged by source sharing or by zero
counterexamples among 198 bases. That finite result supports neither a general
validity theorem nor reclassification as canonically crossable. No further
unbounded parameter search is required or recommended by this assessment.

## Proposed bounded correction: GO in principle

Admit sun/shade component replay only when the affected current beta is exact
binary64 one in the immutable base. For occupancy rank o:

| Probe | Additional replay condition | Maximum validity argument |
| --- | --- | --- |
| sun, coordinate 10*o+6 | base[10*o+4].to_bits() == 1.0_f64.to_bits() | The admitted probe changes only sun temperature. Its successful current call already uses beta one; canonical maximum returns that exact current state. |
| shade, coordinate 10*o+7 | base[10*o+5].to_bits() == 1.0_f64.to_bits() | Same argument for shade. |
| wet/stem, coordinates 10*o+8/+9 | Existing represented-snow/graph/custody conditions | All current/maximum leaf-state call operands remain bit-identical to their successful captured base nodes, even when wet routing changes lower dry/wet area multipliers outside the leaf function. |

No tolerance, rounding, physiological cap or beta normalization is permitted.
Each sun/shade condition concerns its own beta, not the other leaf's beta.
If the affected current call fails, its original first error returns before
maximum evaluation. With the existing exhaustive-maximum diagnostic flag, the
fresh maximum call has the same inputs as that successful beta-one current call
and therefore the same deterministic result; production's canonical exact-reuse
branch remains unchanged.

For every other sun/shade beta, choose the original shared complete evaluator
before minting component-replay capability. This is conservative optimization
selection, not recovery from a replay error, altered physical acceptance or a
new solver fallback. It changes only lower-level work selection/counts. The
complete graph, authentic base/capability custody, all admitted-node arithmetic,
typed first errors, rollback and output/closure obligations remain binding.
The unproved maximum case is executed completely, not declared proved away.

This fits the owner's bounded mechanism-evaluation/correctness-fix scope:
kickoff sections 3 and 6 authorize prospective authority for this exact
mechanism while forbidding numerical/physical changes; section 8 distinguishes
correctness fixes from performance-driven tuning. It needs no materially
different architecture, new derivative or cross-evaluation cache. The owner
requires nonzero actual replay, not maximum possible replay or a fixed bucket
pattern. Reduced savings remain a legitimate negative or bounded outcome.

## Prospective and execution boundary

GO to prepare a separately identified prospective authority note and derived
tests/oracle classification, then obtain the required independent review of
that concrete cut before behavior edits. The note must explicitly bind this
narrower reuse eligibility and leave historical/unrestricted results and every
scientific obligation intact. It must not falsely claim the old all-component
qualification passed or remove historical scientific assertions.

Required concrete checks stay within the existing scope: exact-one versus
next-lower beta and other lawful non-one values; independent sun/shade gates;
wet/stem at non-one beta; same canonical h/stencils and complete-path first
errors; successful current-to-maximum clone/identical-call proof; and all
admitted replay full-field/Jacobian/solve/error/rollback/output comparisons.
The offline oracle must derive eligibility from raw immutable base beta bits
and topology/coordinate domains, not producer bucket labels or a hardcoded
count. Preserve rejected/unattempted signs and complete/replay/anchor lifecycle
reconciliation. Freeze a new source/oracle series identity and require nonzero
completed replay on the unchanged real primary workload before timing.

This is prospective design/scope GO, not review of an as-yet-unwritten note,
runtime PASS, admission of the unrestricted R implementation, permission to
waive other guard proofs, or measurement approval.

## Concrete canonical R-PC1 authority cut: GO

Static: reviewed the complete 59-line additive supplement at canonical
SC-LANDSURFACEENERGY-001 lines 3227–3284. Whole-file SHA-256:
`c993020e96aec0dac934acd5ccfa3050f8173a90bcd7f860b88b8da7405418cd`.
Author's `R-proof-closed-authority.md` hash:
`7fb4cfde0f96788515799526f7916d43802d75840c8b2ec7f23d629120e6999f`.
No historical text/assertion was deleted by this cut.

The concrete supplement faithfully binds exact affected beta-one admission,
current-before-maximum source order, unchanged wet/stem leaf operands,
pre-capability complete selection for unproved cases, original errors and
rollback, and all other C-020 scientific/graph/custody/parity obligations.
It explicitly rejects interpreting the bounded zero-witness search as a proof.
The raw-base-beta independent oracle and nonzero actual replay remain required.
Its subordinate EXP-R identifier is explicit and does not recast historical
revision-31 results or authorize production retention.

Decision: GO for this concrete canonical authority note. The R-only derived
runtime test/API and oracle cut is still pending independent prospective review;
this authority approval alone does not open behavior activation or measurement.

## Concrete R-PC1 derived test/API and independent oracle cut: GO

Static: no remaining prospective blocker in the following frozen cut, reviewed
before the production predicate change:

| Artifact | SHA-256 |
| --- | --- |
| R `solver_component_dependency_replay_tests.rs` | `113a06e5cf74fad506b3192b604248c3c6129281181cbcdb8593d7e545839d54` |
| `R-runtime-test-plan.md` | `73e02f35698d45c03d4cf0b1a7d0017084ab7513945d35b867fbe7325a45b265` |
| `reproduction/audit_stencils.py` | `39fd80bc3d333c1f4be437b0dc55f34536c6e2b8490b8ec7f9927342d1b72e73` |
| `reproduction/test_collectors.py` | `830d10b39999a2ebacbe014027dc5179862357eeb5be56e655a741eec98694c4` |

The runtime matrix derives expected admission from raw beta bits, not the
production predicate: exact one, next-lower binary64 one, zero and 0.4, each
leaf class and supplied occupancy, potential and fixed-cap contexts, both
canonical signs. It asserts actual replay/complete start counts, including zero
replay starts for unproved beta. Eligible leaf probes compare current and
maximum state bits; wet/stem probes compare every captured leaf state against
the immutable successful base. Complete counterparts retain full residual and
evaluation bit comparisons. The six real first-error families now explicitly
use affected beta one and retain first-error/no-complete-fallback checks.
Historical maximum search code is not invoked as current qualification.

The offline oracle independently derives sun beta from raw coordinate 10*o+4
and shade beta from 10*o+5, admits wet/stem without a beta condition, and keeps
represented-snow, coordinate domains, canonical step/sign enumeration,
identity anchors and lifecycle/prefix reconciliation unchanged. Its delta from
the retained unrestricted oracle is only this classification plus the explicit
R-PC1 qualification identifier. No bucket labels or fixed histogram decide
admission.

Ran: `.venv/bin/python -B artifacts/reproduction/test_collectors.py` using the
full package-relative path: seven tests PASS. Also ran 56 read-only in-memory
asymmetric sun/shade beta classifier cases over N=1/2/4: PASS. These checks
validate the independent Python classification, not the unimplemented Rust
predicate or an actual R trace. No Rust build/test was run by this reviewer.

Decision: GO to author the narrowly specified R-PC1 predicate against canonical
authority hash `c993020e96aec0dac934acd5ccfa3050f8173a90bcd7f860b88b8da7405418cd`.
Implementation source review, runtime execution, all retained C-020 proof and
parity obligations, independently classified real nonzero replay, exact output
identity and full validation remain mandatory before measurement/admission.
This does not admit unrestricted R or infer any beta-below-one maximum theorem.
