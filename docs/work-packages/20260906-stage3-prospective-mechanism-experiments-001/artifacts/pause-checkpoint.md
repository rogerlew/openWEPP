# Explicit pause checkpoint

Status: PAUSED BY OWNER REQUEST — not complete, not a scientific disposition.

Purpose: stop quota-consuming work after the expensive correctness/admission
stage and before comparative performance work. No heavy process was running when
this checkpoint was written; the process check found no cargo, rustc, runner,
`run_series.py`, or admission process.

## Safe stopping boundary

Completed enough to preserve a resumable checkpoint:

- A, F, and R have isolated current source kits and authority composition
  evidence. Current kits are A-source-04, F-source-05, and R-source-05; the
  current-A SG1 recipe is explicitly separated from historical A labels.
- F feed-forward and R replay implementations received independent source
  reviews. R's compiler-negative borrow gate passed, and its real consumer,
  forced-complete, node-oracle, trace, and independent stencil checks passed.
- A/F/R full optimized runs were executed and retained as FAIL evidence. R had
  4,206 tests: 4,028 passed, 178 failed, 77 skipped; the comparison found no
  new R failure names versus A/F. These are not full-workspace PASS claims.
- The common natural-limit observer was independently reviewed and executed in
  all three arms. Its complete records match exactly, including all numerical
  failure fields, validated public diagnostic DTO, actual input rollback, 50
  Newton sweep bases, and 9,163 audit events. The unchanged release golden
  remains a separate FAIL and was not relaxed.
- Broad Clippy remains FAIL because inherited orchestrator diagnostics remain;
  corrected source cuts have bounded direct-target checks in progress/retained.
  No blanket lint suppression or production numerical change was introduced.

Not started (intentionally):

- no controlled 12-pair timing series;
- no six-pair memory series;
- no three-process/ten-teardown memory campaign;
- no 10-/19-OFE scale pairs;
- no final treatment decisions based on timing or memory;
- no production promotion, deployment, branch switch, or push.

## Resumption recipe

Resume from the current immutable source kits and source/build manifests. First
finish only the cheap current-cut validation and dual terminal verification. Then
freeze the exact A/F/R release executables and run the predeclared series in
order: F comparisons first, R comparisons second. Do not rebuild or edit a
source cut between samples. Treat all unexecuted performance fields as
`NOT RUN`, never as negative measurements.

The next expensive command should be the corrected F release build/admission,
not a new exploratory audit or another historical reconstruction. Re-read this
artifact and the protocol before authorizing that spend.
