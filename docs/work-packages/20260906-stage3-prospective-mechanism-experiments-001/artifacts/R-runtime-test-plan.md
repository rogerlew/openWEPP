# R runtime contract tests

Static: prospective tests; no compilation or execution by this worker.

Exact owned write paths, declared before edits: isolated R
`crates/openwepp-land-surface-energy/src/solver_component_dependency_replay_tests.rs`
and this artifact. Applicable instruction discovery returned root AGENTS.md,
crates/AGENTS.md and docs/work-packages/AGENTS.md; no nearer crate guide.
Read root 12599 bytes, crates guide 5436 bytes, science guide 5942 bytes,
work-package guide workflow/consumer/gate sections (lines 1–150 plus remaining
sections in initial combined read), numerical architecture 6742 bytes,
kernel preparation lines 1–130, package.md 5340 bytes, R implementation plan,
SC-LANDSURFACEENERGY-001 lines 2860–3225 (INV164/C020 and EXP binding), existing
solver_tests.rs canonical covered fixture and represented-snow derivation
lines 850–1310, solver evaluator leaf/capture and solver probe/sweep source.
Impact: critical solver experiment; optional coverage metrics do not replace
exact contract tests or full correctness; parent owns serial gate execution.

Prospective test APIs coordinated with implement_r: capture helper returns
complete evaluation plus preparations/ledgers/four ordered leaf states;
canonical probe pair owns signed capabilities, consumed by dispatcher;
test-only solver trace records float bits for matrices/RHS/linear results and
trial/iteration residual trajectory at canonical sites.

Tests extend the real existing covered hydraulic fixture after its authenticated
Stage-3 lower-boundary and optical receipts are built. Differential corpus must
assert successful base admission, execute every admitted signed component probe,
compare explicit float bits including residual/tolerance/normalized vectors,
and retain input equality. Malformed and multicoordinate vectors compare actual
dispatcher selection and first complete-evaluator errors. Capability tests use
two real successful bases; they never forge private fields. Saturation-boundary
vectors move only source-real inputs before admission, then use unchanged
canonical h and probe arithmetic. Full solve trace compares canonical matrices,
linear diagnostics and trial sequences with forced-complete selection.

Graph oracle is owned by r_graph; real-runner lifecycle/consumer oracle and
immutable-cut dual review/gates remain parent obligations. Tests do not alone
claim full C020 closure. Final test coverage and source hashes follow below.

## Binding admission matrix (before behavior)

Every row below remains required before R measurement. NOT RUN is not waived.

| Obligation | Concrete test/evidence owner | Present status |
|---|---|---|
| All evaluation fields, raw/tolerance/normalized rows and captured route/leaf nodes | runtime tests `replay_evaluation_bits`, `replay_capture_bits`, `replay_probe_corpus`; explicit float `to_bits`, discrete equality | Authored; NOT RUN |
| Complete dense Jacobian, matrix/RHS/pivot/norm and iteration/backtrack trial trajectory | all-coordinate stencil loop plus `replay_solve_trace_parity`; implement_r records canonical trace bits | Authored; APIs pending; NOT RUN |
| Potential and fixed-final caps | `replay_fixed_and_boundary_corpus` derives request/tie authorization from actual potential fields; both corpus and complete solve compare | Authored; NOT RUN |
| Successful source-real current-leaf first VPD error, source order, no later execution/fallback | `replay_current_leaf_first_errors`, every present occupancy sun/shade, canonical h and humidity just below saturation | Authored; NOT RUN; other authentic crossable errors must be enumerated against named leaf guards before acceptance |
| Captured unreachable leaf calls actually eliminated | existing leaf audit around replay/complete captured evaluations | Authored; NOT RUN |
| Private foreign-base rejection; malformed/multicoordinate/NaN ordinary selection | `replay_selection_and_custody`, two real successful bases and no private mutation | Authored; NOT RUN |
| Coordinate/sign/h/stencil joins and second-use/lifetime | runtime binding API tests + compile-negative test gate; implement_r exposes consumed checked API; parent owns compiler invocation | Required before measurement; additions pending |
| Two occupancy / six soil reciprocal longwave, both wet finalizations, terminal descendants | runtime corpus must be invoked on authentic multirank fixture or real consumer equivalent; graph N2/S6 alone insufficient | Required; parent/implement_r fixture binding pending |
| Byte-exact beginning/custody rollback and accepted owner parity | current tests compare immutable input structure and exact base trial bits; borrowed-only source proof plus real consumer output/owner byte oracle | Parent/implement_r; not claimed complete by PartialEq |
| One shared canonical arithmetic, noncrossable named-guard implications | implement_r source extraction audit + two independent reviews, table below | Required; review pending |
| Complete graph record/hash/mutation oracle | r_graph | Separate current-scope gate |
| Real workload nonzero completed replay and full stencil/lifecycle reconciliation | parent real runner plus independent audit script | Separate current-scope gate |

Noncrossable proof obligations are source-specific, not inferred from one success:

- Leaf maximum: successful current leaf, identical beta-one inputs and gas branch;
  exact-beta-one and zero-PAR +/-zero boundary corpus must preserve every leaf field.
- Liquid preparation/finalization and both wet calls: immutable rain/store/area/caps,
  admitted wet-temperature bounds, and frozen branch preserve every named finite,
  capacity, nonnegative store, cap and enthalpy guard. Dry/exact-capacity vectors
  compare all liquid fields and both capture/output finalizations.
- Longwave: finite bounded temperatures and finite nonnegative immutable areas
  preserve finite reciprocal calculation and denominator guards; include zero-area
  and actual reciprocal multi-occupancy success, all component/ground bits.
- Hydraulic: immutable roots/soil/caps and successful leaf/wet predecessors preserve
  root-domain guards; existing fixture includes accessible, inaccessible and frozen
  roots; fixed cap/tie and beta-bound cases retain all six residual/tolerance bits.
- Route-match: exact same immutable inputs reach the one shared finalizer twice;
  prove no duplicated formula and compare both ledgers on upper/lower routes.
- Lower-ground: immutable represented-snow boundary and caps/frozen inputs preserve
  under-canopy resistance guards; retain ground/soil bound and identity-anchor corpus.
- Infallible tails: exact operand ordering and shared expression/source proof plus
  explicit field-bit comparisons, no invented error or fault seam.

No boundary vector or source implication is labeled passed until execution or
review supplies that evidence. Formatting attempt: plain rustfmt unavailable
(`command not found`); parent uses the configured toolchain. No build executed.

## Post-admission test additions

After dual prospective GO, the same owned test source adds consumed
`evaluate_for` wrong-coordinate/sign/h/stencil tests (each authentic newly minted
capability), and a second source-real crossable error: zero-PAR `v10_zero_par_ci`.
The latter sets the existing Rd input so the unchanged analytic Ci reaches
pressure at the midpoint between successful base and canonical plus probe;
there is no alternate production formula or fault seam. These vectors remain
NOT RUN until the serial compilation/test owner records execution.

The live N2/S6 callback `observe_runtime_base` invokes the same complete node/
capture/Jacobian bit oracle on actual potential and fixed-final inputs, once
each successfully completed posture. It stores only two completion flags.
An RAII guard restores caller force-complete and private audit states on return
or unwind. Parent enables this explicitly in its separate correctness process;
timing/memory runs leave it disabled. Plain source/graph eligibility never sets
the completion flags. Fixture success corpus now asserts full completion, so a
matched early typed error cannot silently satisfy a boundary-success claim.

## Compiler-negative custody gate

Prospective runner write/read map (source-only authoring; parent serial execution):
new `artifacts/reproduction/check_replay_borrow_guards.py` and
`artifacts/reproduction/replay_borrow_negative.rs`; read existing
`verify_source_kit.py`, R-PC1 source manifest, actual private pair/base/sign APIs,
and this reviewed snippet. Applicable instructions: root and work-package
AGENTS.md. Runner reconstructs a fresh verified kit, positively compiles the
unmodified reconstructed crate, then appends only the private test include in
that preserved scratch. Both commands/logs, verification receipt, exact injected
source, and diagnostic-to-function binding are retained. No worker builds.
The runner rechecks every manifest row after the positive command, injects with
`apply_patch`, and rejects missing/extra/unbound compiler errors. Invocation:
`.venv/bin/python artifacts/reproduction/check_replay_borrow_guards.py --repo
<repository> --kit <frozen-R-kit> --output <new-evidence-directory> -- cargo check
--tests -p openwepp-land-surface-energy --message-format=json` (script path is
relative to this package; interpreter path relative to repository). An explicit
environment wrapper may precede cargo. Static Python AST check passed; compiler
execution and E0382/E0502/E0505 evidence remain parent-owned and pending.

Parent serial gate owner should append these functions inside the private oracle
module in a disposable copy of frozen R (never retained runtime source), compile
the actual crate tests, and require the named diagnostics at these functions.
The crate must already compile positively. Unknown-symbol/private-access errors
are not custody proof. Preserve injected source and compiler log as artifacts.
This compiles actual capabilities, not a stub type or inaccessible doctest.

```rust
fn replay_negative_second_consumption(base: &ValidatedCoveredJacobianBase<'_>) {
    let h = f64::EPSILON.sqrt() * base.trial[6].abs().max(1.0);
    let pair = CoveredCanonicalProbePair::new(base, 6, h).unwrap();
    let stencil = pair.stencil;
    let cap = pair.plus.unwrap();
    let _ = cap.evaluate_for(base, 6, CoveredProbeSign::Plus, h, stencil);
    let _ = cap.evaluate_for(base, 6, CoveredProbeSign::Plus, h, stencil); // E0382
}
fn replay_negative_mutate_borrow(mut base: ValidatedCoveredJacobianBase<'_>) {
    let h = f64::EPSILON.sqrt() * base.trial[6].abs().max(1.0);
    let pair = CoveredCanonicalProbePair::new(&base, 6, h).unwrap();
    base.trial[6] += h; // E0502
    let _ = pair.plus.unwrap().evaluate_for(&base, 6, CoveredProbeSign::Plus, h, pair.stencil);
}
fn replay_negative_drop_borrow(base: ValidatedCoveredJacobianBase<'_>) {
    let h = f64::EPSILON.sqrt() * base.trial[6].abs().max(1.0);
    let pair = CoveredCanonicalProbePair::new(&base, 6, h).unwrap();
    drop(base); // E0505
    drop(pair);
}
```

Static custody implication: the capability carries the exact borrowed base;
the base owns trial/capture/frozen state and borrows validated column/caps/graph.
Moving the capability consumes it; holding its borrow prevents base mutation
or destruction. Different real bases are rejected by the runtime pointer join.
The graph test owner separately proves graph records/hash generation. This
source argument does not relabel pending compiler-negative execution as run.

## Exact leaf guard disposition

Prospective SG1 assertion-only refinement: owned test source gains
`ReplayRollbackSnapshot` plus explicit recursive column float-bit collection.
Read map: CoveredColumnInputs and nested ground/geometry/optical/biochemical/root
definitions, actual base/capture/validated owner and graph definitions, existing
natural and named leaf error assertions. Snapshot includes original actual base
evaluation/capture, trial, enum-only frozen branches, caps IDs/float bits, owner
pointer joins and exposed graph bytes. Assert after existing errors, without new
error generation or changed physics. Typed structural comparison supplements,
but never substitutes for, complete float-bit checks. Borrow proof stays separate.

Concrete snapshot coverage map (paths relative to LSE `src/`):

| Declaration | Explicit bit traversal |
| --- | --- |
| `solver_covered_evaluation.rs:217` CoveredColumnInputs | all 12 scalar floats; recurse into every nested field |
| `physics.rs:164` UnderCanopyGeometry | all four floats |
| `solver.rs:78` OpenSurfaceProblem | all 16 scalar floats; optional litter capacity; terminal bands, open geometry, optional bare soil, every soil node |
| `physics.rs:115` OpenNeutralGeometry | all four floats |
| `solver.rs:68` BareSoilParameters | all six floats, if present |
| `solver.rs:59` SoilThermalNodeOperands | all four floats, in node order; typed clone retains layer IDs |
| `solver_covered_evaluation.rs:160` CoveredOccupancyInputs | all 24 scalar floats; both biochemical leaves, constants, every root layer |
| same file:3 LeafBiochemicalInputs | all six floats for each class |
| same file:13 BiochemicalConstants | all 19 floats |
| same file:36 RootHydraulicLayer | all six floats; typed clone retains IDs/accessibility/frozen tags |
| same file:196,206 shortwave inputs | all three column band groups and all three groups for every occupancy; occupancy IDs retained |
| `physics.rs:235` BandDirectionalFluxes | direct/diffuse VIS/NIR, all four floats at every occurrence |
| `solver_covered_evaluation.rs:274` Stage3SnowCoveredLowerBoundary | all nine floats; typed clone retains every required/optional receipt digest |
| same file:299 Stage3SnowOpticalBoundaryReceiptV1 | three complete band groups and both albedos; typed clone retains OFE/tile IDs and all digests |
| same file:49,55 SourceWaterCap/CoveredWaterCaps | both floats for every keyed root cap and ground; typed clone retains keys and presence |
| same file:63 CoveredFrozenBranches | exact typed maps/enums; contains no floats |

Snapshot owns clones of actual (not reconstructed) base evaluation/capture and
uses existing exhaustive `replay_evaluation_bits`/`replay_capture_bits`; trial
uses per-value bits. It also compares base/validated/column/caps/boundary pointer
joins, lifecycle, and the exposed graph schema digest. Private graph masks are
not exposed here; graph-owned tests and structural immutable borrowing remain
separate evidence. Typed clone checks preserve all Option discriminants and
vector/map cardinalities before float vectors are compared. Snapshot assertions
run after every existing named error result comparison (also paired successful
signs), plus the natural corpus Err/Err branch. No new physical vector is added.

Static follow-up to parent R-PC1-SG1-focused-01 failure: the new N2 hookup in
`solver_tests.rs` copied N1 optical/lower absorption custody after the authentic
N2 fixture had changed terminal shortwave and rebuilt its column shortwave.
`validate_covered_shortwave_inputs` correctly rejects this mismatch as
`Stage-3 snow optical/column absorption` before any PC1 dispatch. Implementation
owner repairs only that fixture hookup using canonical ground partition and the
existing typed optical receipt constructor on actual N2 inputs, updating its
joined lower absorption/receipt hash. No replay assertion, closure tolerance,
production input guard, or physical arithmetic is relaxed. Parent rerun remains
required; this source diagnosis is not runtime evidence for the corrected cut.

Static source reading: canonical leaf implementation in
`solver_covered_evaluation.rs::leaf_trial_state` and helpers `arrhenius`,
`peaked`, `smaller_quadratic_root`, `canopy_saturation_q` (A lines580–968),
`covered_trial_is_valid` (A lines1638–1679), R
`CoveredReplayReuse::leaves` and shared occupancy evaluator. Also read
SC-VEGETATION-001 lines1166–1240 and vegetation/config.rs lines390–445:
external configuration supplies g0 and activation parameters; g0 is finite
nonnegative and activation parameters are positive, with no invented global
physiological upper/lower bounds. Canonical curvatures are fixed .7/.98/.95.
Parameter-boundary tests are ASSUMED_FOR_EXECUTION, not calibrated phenotypes.

There are two different proofs; do not conflate them:

1. A **skipped** current leaf consumes the unchanged original
   `LeafBiochemicalInputs` (including original leaf area, not wet-adjusted dry
   area), `BiochemicalConstants`, class temperature, canopy q, class beta,
   authority/pressure/ca, gb, g0 and Medlyn g1. Each value is borrowed from the
   exact successful base or its unchanged trial coordinate. A skipped maximum
   also consumes unchanged current `LeafTrialState` and beta; graph current-to-
   maximum edges force recomputation whenever current changes. The canonical
   maximum reuse/force-exhaustive posture must match. Therefore every guard
   sees exactly the same bits as its successful captured invocation. Wet-area
   changes do not change leaf-call arguments; they affect later vapor nodes.
   This proves unchanged skipped-node outcomes without any global finite-range
   assumption. Both independent reviewers explicitly accepted this scope.
2. An **affected** current/maximum leaf executes the same canonical function in
   source order. That preserves guards, exact first errors and no fallback;
   it does not itself prove global noncrossability. Concrete source-real
   current-error vectors below establish their own tested surfaces only.

| Existing guard | Current-node disposition and maximum-call implication |
|---|---|
| `arrhenius_response` | Its explicit predicate reads positive finite immutable activation and finite T>0. Admitted class T is 273.15–350 K; no admitted temperature probe can newly fail that predicate. Maximum repeats identical arguments. Arrhenius exponential overflow is not checked here; it must not be mislabeled this error. |
| `peaked_response` | Immutable-input predicate cannot change. Finite-result predicate can change with T: bounded complete-evaluator activation bracket plus canonical signed probe is authored. Maximum repeats precisely the successful current call's response inputs, so cannot newly fail after current at that same probe succeeds. |
| `liquid_saturation_polynomial` | Admitted class T is 273.15–350 K, strictly inside/at lower edge of polynomial [273.15,373.15]; inadmissible outward sign never executes. Maximum uses identical T/P. |
| `quadratic` | Canonical a coefficients .7/.98/.95 are immutable and nonzero; the sole error arm requires a==0 and b==0. A caller-altered fixed curvature is not an authentic canonical configuration. |
| `photosynthesis_discriminant` | With canonical 0<a<1, finite x,y produce (x+y)^2-4axy>=0. Source allowance 64epsilon times max product covers ordinary floating subtraction/multiplication rounding. This is not a proof about arbitrary overflowed intermediates: inf-inf becomes NaN and is handled, if exposed, as downstream carbon nonfinite. Maximum carbon uses identical constants; valid positive-branch Ci remains between gamma and ca, with monotonically bounded carbon terms. |
| `photosynthesis_nonfinite` | All prelude coefficients are unchanged between current and maximum at one probe. Carbon evaluation at each endpoint is beta-independent. Arbitrary external coefficient overflow is not globally excluded by successful base alone. Reachable calls retain this exact guard; no vector here claims this surface was executed unless an actual paired result establishes it. |
| `v10_zero_area_stomatal_conductance` | Source-real positive-subnormal g0 boundary vector authored on existing zero-area fixture, ordinary pressure 99600Pa, canonical T≈300.003013K and minus h. The inactive base's returned resistance can be infinity; the test must report actual full-evaluation admission, not assert a finite-resistance physical bound. Maximum reuses the exact inactive current state and makes no second gas call. |
| `v10_zero_par_stomatal_conductance` | Positive-finite gs precondition is unchanged at maximum; exact-zero-PAR maximum reuses current. A successful zero-PAR current also must pass finite Ci afterwards. This rules out claiming inactive gs-underflow evidence as an executed zero-PAR guard. No separate zero-PAR gs error vector claimed. |
| `v10_zero_par_ci` | Analytic Ci-pressure midpoint vector is authored for both class positions, with successful complete base and canonical plus error mandatory. Maximum reuses the successful exact-zero-PAR current state. |
| `surface_vpd` | Saturation-midpoint vector is authored for each class/occupancy, canonical minus error, exactly one reachable current-leaf call and no complete fallback. Maximum T/q/P operands are identical to successful current at that probe. |
| `surface_co2` | Bounded source-real complete-column gb bracket plus canonical signed temperature is authored. Carbon and cs at a given Ci are beta-independent. Maximum endpoint cs guards repeat already evaluated current endpoints; positive-root bracket stays between them. |
| `stomatal_conductance` | For finite physical operands g0>0, g1>=0, beta∈[0,1], positive-An maximum β=1 increases gs; nonpositive An uses exactly g0 for both calls. Thus maximum cannot introduce a nonpositive real conductance. This does not assert every external-parameter IEEE overflow/underflow globally absent. |
| `ci_bracket` | At gamma, Ag=0 and An=-Rd, so the residual uses g0 and is beta-independent. At ca, residual sign is An(ca) for positive resistance; reducing resistance at β=1 preserves its sign, or exact rounded zero takes the successful endpoint branch. The canonical positive/lowlight admission branch therefore does not newly fail solely by changing beta. |
| `v10_low_light_stomatal_conductance` | gs0 depends on g0,T,P only and is identical at current/maximum. No executed vector is claimed for this particular error; zero/nonfinite gs hypotheses must be reconciled with the earlier current residual guard and positive complete-base admission, not guessed from ordinary fixture constants. |
| `v10_low_light_ci_dark` | Bounded source-real Rd bracket and canonical signed probe authored. ci_dark uses unchanged Rd,g0,T,P,gb,ca, independent of beta; a successful current lowlight call proves this identical maximum guard. |
| `v10_low_light_dark_bracket` | In the nonpositive-An part, f_dark=K0*Ag>=0; in positive-An part, ci_dark>ca and predicted<ca, so f_dark>0. Exact shared arithmetic at Ag=0 gives identical ca+K0*Rd expressions. This implication requires canonical nonnegative finite carbon terms; it is not a blanket exclusion of unbounded parameter arithmetic pathology. |
| `v10_low_light_accepted_branch` | Canonical lowlight root is in [ca,ci_dark], has Ag>0 and An<=0, hence uses the same g0 resistance bits for all beta. Existing zero/low-light, exact-beta and full solve differential corpus observes actual accepted fields. Successful-current alone is not presented as a proof of every possible floating Brent termination at arbitrary extreme coefficients; any actual maximum-only counterexample must be paired and reclassified. |

The **remaining limitation is explicit**: the last carbon/IEEE/Brent clauses
above are conditional canonical-domain implications, not a proven global
numeric range theorem over every schema-admitted external coefficient.
Reviewers must judge that scope against C020; neither source reuse nor finite
test coverage can truthfully manufacture a global theorem. No production bound,
fallback, error guard or physiological parameter is changed to obtain closure.

Bounded new source-real corpus: 3 families × present occupancies × 2 classes,
each with 60 complete-evaluator bracket steps followed by only canonical signed
temperature probes. Families are surface_co2, lowlight_ci_dark, peaked_response.
No repeat-until-pass or unlimited parameter campaign is authorized by these
tests. Any unmatched intermediate first error fails with its actual identity.

Skipped routing implication, confirmed by reviewer B: the successful iteration
base runs both wet calls with natural branches; freeze_covered_branches takes
the occupancy wet branch from that very evaluation. Identical preparation,
wet-temperature, canopy T/q and immutable column/caps imply both natural wet
branches agree. For a graph-unreachable route the later frozen branch is thus
exactly the original natural branch, not merely the same enum type. Both
finalizations consume identical operands through the one shared finalizer;
wet probes recompute their own and every lower route by graph closure.

## Actual capture binding and one targeted maximum investigation

Reviewer B's concrete oracle finding was corrected: node/full-field replay now
consumes `base.capture.as_ref()`, the exact production sweep capture. The
independent frozen-base re-evaluation is compared against that actual capture
and actual base evaluation; it is never substituted as replay evidence.

The one remaining targeted route investigates the lowlight final-state guard
near An=0, where beta-dependent positive-An trial resistance can affect Brent's
finite stopping trajectory. It is bounded to two classes, three beta inputs,
56 actual-complete PAR bracket steps, three bracket PAR points, eleven nearby
base temperatures and the two unchanged canonical signs. No alternate leaf
math, private-node fault seam or adjusted perturbation is introduced.
Only successful actual complete bases seed the signed probes.

`R_MAXIMUM_COUNTEREXAMPLE` is emitted only for the exact existing
`v10_low_light_accepted_branch` error when the replay attempts two leaf calls
(affected current then affected maximum) and the complete evaluator attempts
three (both current classes then the affected maximum; other class is an
authentic zero-PAR maximum-reuse branch). Both paths must return the same
typed error, zero replay fallback and unchanged base/column. It records exact
class, beta/PAR/base-temperature/probe-temperature bits. An actual witness
requires explicit C020 maximum crossability reclassification; this test does
not silently rewrite authority. The final `R_MAXIMUM_BOUNDED_RESULT` reports
successful base and maximum-only first-error counts. Zero witnesses is not
reported as a global noncrossability theorem. All execution remains parent-
owned and NOT RUN in this source-author artifact until logs establish results.

Reviewer B independently verified the actual-capture correction (RIB-2).
Its diagnostic QA follow-up is also implemented: bracket errors and rejected
candidate bases retain their exact typed identity and class/beta/PAR/T bits;
the final bounded record reports both populations. A zero-witness count can
therefore never be confused with execution of every attempted candidate.

Ran (parent-owned exploratory cut, not immutable terminal evidence):
`artifacts/raw/R-focused-02.log` records 150/150 passing LSE tests. The covered
fixture emits `R_MAXIMUM_BOUNDED_RESULT successful_bases=198
maximum_only_first_errors=0`. All 198 planned base points succeeded; the
logged cut precedes the extra rejected/bracket population fields. All six
new asserted current first-error families therefore passed in that executed
fixture. The single targeted maximum route found no authentic counterexample;
no maximum reclassification is authorized by that result. Source proof review
must disposition the remaining bounded-binary64/Brent implication, and no
further parameter-search campaign is undertaken to manufacture a witness.

## R-PC1 prospective derived test cut

Before selector behavior edits, read the additive canonical
`EXP-STAGE3-20260906-R-PC1` supplement at SC-LANDSURFACEENERGY-001 lines3227–3284
(canonical source SHA c993020e96aec0dac934acd5ccfa3050f8173a90bcd7f860b88b8da7405418cd).
The owned source remains this same isolated R test module; no production path
is edited by this worker. Dual review of this concrete cut precedes behavior.

`replay_pc1_selection_matrix` prospectively covers both classes and every
occupancy supplied by the fixture, potential and fixed-final caps, beta values
exact binary64 1, its immediate predecessor, zero and 0.4. For every such base
it executes canonical signed capabilities for the affected leaf and for both
wet/stem components. Expected classification is independently derived from raw
base beta bits, never from the production eligibility function or labels.
Real mechanism-audit starts must be exactly one replay for proved coordinates
and zero replay/one complete for other beta values. Full-call and actual leaf
counts independently guard against replay followed by complete fallback.
Both signs retain residual bits, complete evaluation parity, exact probe-current
to maximum state bits for beta1, and every unchanged wet/stem leaf capture bit.

All six current-first-error families now explicitly set the affected beta to
exact one before base admission, retaining the assertions that a real replay
starts and returns the existing first error without complete fallback. This
prevents a newly ineligible complete-only vector from being relabeled replay
error evidence. The all-coordinate Jacobian/full-field corpus also derives
expected admission from raw beta and only invokes full-field replay for proved
coordinates, using the actual production base capture.

The earlier maximum search is no longer called by qualification; its inert
test-only function is labeled historical, with the existing executed result
and source cut preserved by the parent. It is not a new solver or fallback.
The PC1 implication is exact and closes the former affected-maximum proof
gap by admission: at beta1 maximum either returns successful probe-current
directly or, under the existing exhaustive test posture, repeats identical
canonical operands. Wet/stem maximum operands never changed. Unproved beta
sun/shade coordinates select complete before a replay capability exists.
No global biochemical/Brent theorem or physiological bound is asserted.

Prospective test-source SHA before formatting/review:
`113a06e5cf74fad506b3192b604248c3c6129281181cbcdb8593d7e545839d54`.
NOT RUN for R-PC1; unrestricted R results above are not narrower-selector proof.

Dual prospective reviewers A/B both gave test/API GO on the named frozen PC1
cut before predicate behavior. Subsequent test-only coverage refinement exposes
`replay_pc1_multirank_contracts` for the existing authentic multirank fixture:
both cap postures, both classes and each occupancy's beta-boundary matrix plus
current-error families. The VPD complete-call position independently accounts
for each preceding class's actual raw beta and gas branch, including lawful
maximum reuse; it no longer assumes four calls per preceding occupancy.
The worker owns the fixture hookup and compilation. Source immediately after
this refinement: `c7c87c3792598d3feec8d3a8f7a832fa08ac61c58cc61a21f9caba36d13c5aa7`.

Parent-requested mixed mapping coverage is also explicit: two exact-one versus
adjacent-below beta patterns reverse sun/shade by occupancy rank. The complete
per-target matrix runs against both patterns under both cap postures in N1 and
the authentic N2 wrapper. Other classes/occupancies retain opposite beta bits
while the target varies; this detects any-class and wrong-occupancy predicates.
Source before final formatting: `27b572716e8c17f7ef57bd3aae189529cafe5c957156e519ba300fa321ee34cf`.

## Prospective common natural-limit diagnostic observer

Exact new source path `crates/openwepp-land-surface-energy/src/covered_natural_limit_diagnostic_parity_tests.rs`;
existing `covered_oracle_conformance_tests.rs` changes only by child-module binding.
Author in main and isolated F now only after B prospective GO; isolated R remains
frozen until parent explicitly unfreezes it. Applicable read instructions: root,
crates, science-contracts and work-packages AGENTS.md; package.md and C020 error/
rollback authority. Concrete design authority/evidence: `diagnostic-parity-review-a.md`
SHA `c6bec31887d772e567bd4526e09334ce7acd959b03a60c62a88b9f5f2408537a`.
Read map: original fixture()/column() and natural test at covered conformance1150;
NumericalFailure all11 fields (numerics19); residual/stepnorm/DTO declarations and
validation (diagnostics97–270); unchanged public rejected converter(transaction1173)
and RuntimeTileIdentity44; common detailed mechanism audit/Event49/Session183;
the reviewed thirteen-type column float-bit traversal above. No production,
test-support hook, scientific golden, or numerical behavior edits. Parent serial
builds and three-arm full-record comparison are separate pending execution gates.

Concrete common observer cut (main/F only): new file SHA
`88d8a16a433df8b25d16fab76ae1364743f724c72910ef431a4bfbc45125676c`;
binding-only existing file SHA
`06f445c735db5b4c7a2c717b99bf20aa32b8c50915d27eaee4ea5401f2b3a3b3`.
Its local `column_float_bits` is the same reviewed thirteen-type traversal above;
no float/discrete fields are replaced by Debug alone. Exact ignored test name:
`covered_oracle_conformance_tests::natural_limit_diagnostic_parity::natural_iteration_limit_full_diagnostic_parity`.
Marker `STAGE3_NATURAL_LIMIT_DIAGNOSTIC` carries entire failure bits, DTO JSON and
bit view, actual before/after input values, complete audit and Newton/final bits.
Source formatting and diff hygiene passed; no compiler/observer execution by
the author. R installation and all three-arm execution remain parent-gated.

Parent-approved B robustness refinement requires declared ordered-row metadata
cardinality equal actual failure rows before zip. Final main/F new-file SHA:
`0d27a1e53cfa9c6ac719dea7cee6f713c88dd85fe97119a484386f37954f1467`.
No numeric-golden change, R edit, or compiler run. DTO parser roundtrip-bit
assertion remains explicit; any observed parser failure must be separately
adjudicated, not attributed to solver physics or hidden by a tolerance.

Compiler-directed final annotation correction after R-clippy-02: macro-statement
`#[expect]` was ignored by the compiler. All three arms now put the unchanged
release-only assertion in private `require_release_posture()`, whose body is
only that assertion; the narrowly scoped expected lint retains the explicit
reason. No general observer allowance or guard removal. Final common source SHA
`2c219dbd3626195f735a9ee4db3a74b173462a1ef13fa290afdefa6a9e1dd156`;
parent-owned compilation/three-arm execution still governs runtime admission.

## Prospective common runner lint-only correction

Parent assigns only `crates/openwepp-runner/src/hillslope/tests03/controlled_mechanism_experiments.rs`
in main/F/R (preserve R's two additional ignored wrappers). Discovered applicable
root/crates AGENTS.md; read exact common functions and parent A-runner-clippy-02
JSON diagnostics. Prospective dual-review scope: function-local expected constant
assertion lint on existing entry and run functions, run-only expected length
lint explicitly preserving indivisible authentic lifetime/clock/memory protocol,
and `usize::from(hbp.nofe)` replacing infallible-u16 fallible conversion. Preserve
both release guards, all clock placements/drop statements, inherited
`drop_non_drop`, and R wrappers. No extraction, lifetime alteration, test-support
selector or numerical change. No source edits before dual GO and parent active
R-runner Clippy barrier; no worker builds.

Applied after both GO reviews and parent barrier: main/F runner SHA
`d37a7f681223117eaa3fb34f6b2a66ce80448cb2f5b021c01a64cca86d397aeb`;
R runner SHA `53f071425fc4618e8bf8bd46a9abd29381a8edcfff258a1f1b39516c26e72828`.
R's unchanged sixteen-line wrapper prefix is the sole A/F versus R difference;
read-only normalized-source comparison passed. Three function-local expectations
and one infallible conversion only; inherited drop remains. Formatting/diff
checks passed, no worker builds and no LSE edits during its active diagnostic gate.
