# R implementation evidence

R Clippy corrective cut after parent source04 unfreeze: actual completed JSON
emitted 21 new LSE diagnostics (including repeated lib/test emissions). Dual
review approved only if-let/is_none_or graph spelling, explicit feature imports,
oracle panic documentation, lexical Restore declaration placement, identical
graph-oracle arm/iterator/hash-string rewrites, checked bounded test count
conversions and a semicolon. No graph edges, probe arithmetic, eligibility,
runtime TLS/check ordering, guards or physics changed. No blanket allowances.
Scoped rustfmt and diff checks passed; no worker build ran. Parent owns actual
Clippy and regression reruns; concrete reviewer GO requested on final cut.

- solver.rs: 3337c2a392e851264b1ec7f4e0592492774abf7ecd1a206bc4f534d65b4b756e
- graph: 4fca5d95b4fd764aa41ef1accbf1ea3a2723443fa68cea0c2d115c1d645cb135
- graph tests: 36af591efe4f77618e0d30598c11f392982b880b024b22b104367751d0ba255c
- replay: ba07446fe108bedd96a5ded2fcac6fb493c6fe739f87b6827e699afd3ff45941
- replay tests: d606fb02f874fb53538c295282ba1c3ca45af129bd42a72d3ab70c47ea8393a2

PC1/SG1 focused-01 fixture correction (Static, execution pending): the new N2
binding copied the N1 optical receipt despite the authentic N2 fixture having
different terminal radiation and a rebuilt bound shortwave column. The unchanged
Stage-3 absorption guard correctly rejected it before PC1 base construction.
Corrected only solver_tests: canonical partition_ground_shortwave on actual N2
terminal/albedos, typed receipt constructor with existing fixture identity
provenance, and matching lower-boundary absorbed shortwave/receipt hash. No
production guard, physics, probe, or replay-test change. The test fixture hash
is ee516d65a81f0c4a4c0e4184c351d3444141148bbcd2f0261f0e84ce183e1b8f.
Scoped rustfmt and diff checks passed; no worker builds ran. Parent owns rerun.

Ran by parent serial fallback: `R-focused-01.log` / `.log.json` records
`nix develop --offline -c cargo nextest run -p openwepp-land-surface-energy
--lib --no-fail-fast`, exit 0 (150 tests; 18.751 seconds process envelope).
`R-runner-check-01.log` records successful runner development-profile checking,
with one inherited orchestrator dead-code warning. These are the unrestricted
R implementation's focused execution and compiler evidence, not PC1 admission
or real consumer execution. PC1's additional proof-closed eligibility and its
derived tests require their own source-bound revalidation.

Prospective PC1 correction (not activated at this record): independently
classified exact beta-one sun/shade probes plus wet/stem probes retaining their
leaf inputs. Unproved beta-below-one sun/shade probes select complete before
component-capability creation. The bounded 198-case maximum investigation
established no counterexample and no global noncrossability theorem. Canonical
PC1 supplement and dual frozen test review precede this predicate change.

Review-A eligibility correction: canonical pair construction now mints generic
`ValidatedCoveredSignedProbe` values. Their consumed dispatcher first checks
forced-complete/regime/component/graph eligibility, and only then constructs
`ValidatedCoveredComponentProbeReplay`. Hydraulic/beta/shared/identity and
nonrepresented/unknown graph selections therefore never mint replay capability.
The generic proof retains the same minus/plus vectors, h, bounds and stencil;
no numerical operation or error path was relaxed. Runtime validation remains
pending the serial queue.

Static implementation cut: dual prospective GO recorded in R-prospective-review-a.md
and R-prospective-review-b.md before evaluator activation. R now uses the shared
canonical column/occupancy evaluator with optional route/leaf capture. Successful
iteration capture moves into the immutable sweep base; consumed canonical
minus/plus capabilities bypass repeated full-vector custody scans. Graph-selected
unreachable nodes copy; all other expressions run in their existing order.
Consuming the base after sweep completion drops capture before LU/backtracking.

Ran: `nix develop -c cargo fmt --all`, followed by explicit rustfmt for included
solver/replay/test files, and `git diff --check`, exit 0. Cargo fmt's incidental
authority-test formatting was restored, so the treatment does not alter that
unowned path. No worker build or test execution: parent owns serial comparator
queue. LSE2700-line evaluator is WARN; no 3000-line changed source. Existing
evaluation-file decomposition is retained; the new replay/graph modules own
added mechanism details, and future evaluator decomposition remains follow-on.

Three exact ignored runner tests share `controlled_mechanism_run`:
`stage3_controlled_mechanism_experiment` is the normal measurement/lifecycle path;
`stage3_controlled_mechanism_forced_complete_experiment` is complete-probe output
parity with a scoped test-support flag;
`stage3_controlled_mechanism_node_oracle_experiment` runs full-field/capture
comparison at the first authentic N2/S6 potential and fixed-final bases.
The third path's extra probes are diagnostic and must not count as production
lifecycle or timing evidence. No audit event suppression is used. All three
are presently NOT RUN, not admitted measurements.

Static: prospective runtime assertion added to the existing authenticated
represented-snow solver fixture before behavior changes. It enumerates every
lawful signed component probe, requires zero complete dispatcher calls, compares
the returned residual/error to forced canonical evaluation, and checks immutable
inputs. On baseline A, every component dispatch increments the full-probe audit;
therefore the assertion is expected red. No execution is claimed: parent's serial
queue owns builds and tests. Graph/runtime test review precedes behavior edits.

Prospective runtime test scope also retains existing full-solve forced-complete,
finite-difference stencils, bound/error/rollback tests. Additional capture-field,
full Jacobian and capability tests will bind the implementation API after its
reviewed design, without inventing physical error hooks.

## Shared-node extraction and guard map (Static, before activation)

Baseline evaluator source ranges: solver_covered_evaluation.rs 1255–1548
(occupancy arithmetic), 1944–2381 (column arithmetic); leaf source 682–970;
covered_liquid.rs 121–228; physics.rs 322–418 reciprocal longwave.

Route preparation guards finite/nonnegative immutable stores, rain, fractions
and areas. Component sun/shade/stem probes change none of those inputs; their
route trio is graph-unreachable. Wet probes recompute their own routing trio
and every lower descendant; the canonical first finalizer bounds evaporation
to preliminary store, partitions condensation/drainage, and validates finite,
nonnegative, enthalpy and mass-closure fields. The second occupancy finalizer
always executes the same function with identical prepared state/wet flux/pass.
No separate replay formula is introduced. Runtime boundary and matched-ledger
tests remain necessary; these source facts are not a numerical test result.

Hydraulic root flux, root-cap presence and authorization errors read unchanged
potentials/root layers/caps/frozen branches for component-temperature probes.
Leaf/vapor descendants affect residuals and tolerances, not those cap identities.
Represented-snow lower-boundary validation/resistance inputs also do not change.
Reciprocal longwave guards immutable atmospheric/clumping/plant operands and
nonnegative component areas; admitted component temperatures retain finite
positive bounds. Wet-dependent areas are recomputed from canonical routing.
Full longwave and ground-facing output arithmetic executes on every replay.

Leaf current remains a crossable physical error source. The tests must use a
successful complete base and authentic infinitesimal signed probes, report the
first existing error with no later fallback, and retain input/base bytes. Every
reachable leaf current/maximum executes its unchanged canonical function;
copied leaves are graph-unreachable because their temperature, beta, canopy
humidity, forcing and biochemical inputs are unchanged. Maximum-call
noncrossability requires the separate named-guard review and boundary corpus;
no blanket implication from a historical successful run is claimed here.

R-PC1 corrective static cut: after dual prospective GO, the selector now
requires the affected sun/shade beta bits to equal binary64 one before creating
component replay authority. Then canonical maximum reuses the just-successful
probe-current state under INV-163, including all guard operands; beta below
one instead takes complete evaluation before replay starts. Wet/stem copied
leaf operands remain identical. This closes the maximum implication only in
the admitted proof domain, not globally; the retained 198/0 investigation is
diagnostic and supplies no admission proof. No physical guard or retry changed.
The existing authentic N2 fixture now runs potential/fixed PC1 selection plus
every-occupancy first-error vectors. Their execution is pending parent queue.

Ran: explicit rustfmt for changed included sources and `git diff --check`
passed. No worker build/test ran. Copied canonical supplement is exact hash
c993020e96aec0dac934acd5ccfa3050f8173a90bcd7f860b88b8da7405418cd.
Production replay source 0c557403ded163aa67867a28d3eecdfd9444908da3666ab3469670f4befdb7ce;
solve source fdb5ed651eabd847e18076cc74004282af29613a4d976dc29e7c28d48e6ec200.
Mixed-class/occupancy additional test cut is owned by r_contract_tests; final
test hash and actual runtime disposition remain required before measurement.

Final PC1 compile-ready test cut after mixed-beta refinement and formatting:
replay tests 36bf6a8cbe4e8faaa9e093097a91d458d970822337ebaf8d2ee5312cbc445036;
fixture wiring 1e9dfdddc36b37fa2087c06b24ceadd0ac872b78ae3d7320ac1be7b0662bd668.
Both opposite alternating exact-one/adjacent-below patterns exercise mismatched
sun/shade and occupancy beta positions in potential/fixed matrices. Parent owns
all execution; this is a source freeze, not a passing-runtime claim.
