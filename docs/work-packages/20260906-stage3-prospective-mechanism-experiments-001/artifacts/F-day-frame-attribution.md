# F day-frame construction attribution

Static: exact source-call attribution plus read-only extraction of completed parent admission records. No code change, build, timing run or allowlist activation. This proposal requires independent dual review before timing. Exact document path passed to find-agents (root/work-package guidance).

## Observed operands

Parse the single `STAGE3_CONTROLLED_MECHANISM` JSON record in each retained log. `raw/A-admission-02.log` SHA256 `9fcf007dc167bb67cbf4627a8c4fa49a5f806e603d15e21559b1ff73d54c2c18`; `raw/F-admission-01.log` SHA256 `e1df38ce94d323d9ac43845480917bf02613c96e17c5f5acce0c1bccbb2b6c35`.

Both have `ofe_count=1`. `carrier_counts.counts` ordering is established by `stage3_mechanism_experiment_audit.rs:20-22`: Provider=2, Carrier=3, BatchProvider=4. A: Outer 72, Evaluator 200, Provider 400, Carrier 400, BatchProvider 0. F: Outer 72, Evaluator 200, Provider 200, Carrier 200, BatchProvider 0. Each started=completed, errors=0. Embedded `output_manifest.direct_runtime_counters.day_frame_constructions`: A 1205, F 805. All other direct-runtime counters compare equal, including day_frame_commits=0, direct_compute_operations=10, direct_phase_entries=26 and direct_state_mutations=10. The parent reports all remaining scientific manifest content exact after the already reviewed provenance qualification; this document does not replace that comparison.

## Exact unchanged counter and carrier chain

Paths below are relative to `crates/openwepp-hillslope-orchestrator/src/` in F; relevant counter/adapter/native-physical source files have empty diffs against A.

1. `direct_runtime/04_audit_error_helpers.rs:89-90`: `record_day_frame_construction` performs one AtomicU64 `fetch_add(1, Relaxed)`. Its sole production increment call is `direct_runtime/00_core_frames.rs:1506`, in `DirectDayFrame::seed`, after seed-index validation. This is construction telemetry, not accepted-day count or a scientific owner operand.
2. `direct_runtime/00_core_frames.rs:832-850`: `DirectRunFrame::seed_day_frame` calls `DirectDayFrame::seed` once. `vegetation_real_hydrology_shadow.rs:291-320`: `RealHydrologyShadowAdapter::try_from_day_start` loops over every frame lane and calls this seed once per lane, storing beginning-day frames. Therefore each successful adapter construction adds exactly lane_count to this counter, even for trial-only immutable adapters.
3. Actual F carrier `v11_covered/carrier_phase.rs:1284-1315` invokes the shared physical phase; that phase invokes `build_provisional_covered_iteration_evidence_v1` at :1534. The release path does not force a full provisional envelope (`carrier_engine.rs:543`), and its physical-only builder calls native `evaluate_covered_frozen_litter_v4_physical_v1` at `carrier_engine.rs:490` for this native represented-snow fixture. F retains this physical path; its removed same-invocation second call no longer enters it.
4. `v9_real_consumer_shadow/frozen_litter_v4_adoption.rs:951`: native physical evaluation first calls `prepare_covered_frozen_litter_v3_fixed_final_with_projection`. That delegates to `prepare_frozen_litter_v3_fixed_final_common` (`frozen_litter_v3_adoption.rs:894-912`), which constructs a hydrology adapter at :1004. This is the first seed per lane.
5. Returning to `frozen_litter_v4_adoption.rs:976`, the same native physical evaluation independently constructs a second hydrology adapter from the immutable beginning frame for its physical candidate. This is the second seed per lane. These are existing distinct adapter constructions inside each carrier execution, not new F instrumentation or a renamed counter. Promotion of the resulting physical evidence into the complete owner envelope remains in `carrier_phase.rs:1966-1987`; the treatment does not change it.
6. Runner `crates/openwepp-runner/src/hillslope/05_runner_execution_and_outputs.rs:2056-2058` takes the audit snapshot delta using saturating subtraction; :2028 copies that delta to manifest provenance. Neither runner counter mapping nor the atomic/seed/adapter/native builder chain changed in F.

## Derived bounded acceptance relation

For this single-lane successful native fixture, the removed 200 duplicate carrier executions each avoid the two existing adapter seeds:

`D_A - D_F = 1205 - 805 = 400 = 2 × (P_A - P_F) = 2 × (400 - 200)`.

Independent residual check: `D_A - 2P_A = 1205 - 800 = 405`; `D_F - 2P_F = 805 - 400 = 405`. The unchanged residual is not asserted to be 405 accepted days or assigned to guessed source categories. It is the measured construction total outside the attributed provider-local two-seed contribution. Successful provider/carrier counts agree, so carrier counts give the same equation.

Proposed comparison qualification is ONLY the exact `/direct_runtime_counters/day_frame_constructions` manifest leaf, and only with independently checked A=1205, F=805, one lane, balanced/error-free Provider=Carrier counts A400/F200, exact 400 reduction, and exact equal residual405. Retain the raw values and relation evidence. Do not delete the whole counters object, classify this as arbitrary volatile provenance, mutate either raw manifest, or permit any arbitrary delta. A different fixture, lane count, physical branch, error count or construction total needs new attribution rather than reusing these constants.

The detailed A trace exists at `raw/A-trace-01/carrier-0.json`; detailed F trace and exact independent outer/evaluator/role/support/custody multiset preservation remain the parent's separate pending gate at this writing. Count algebra cannot discharge that gate, full-field physical oracle, closure, or batch/canonical independence. This attribution supports review of a mechanism-dependent telemetry exception; it does not broaden F or excuse any scientific mismatch.

## Prospective authenticated 10/19-OFE qualification

Static extension requested by parent before scale admission/timing, same document only. No scale run or generalized allowlist activation is claimed. The one-OFE constants above remain historical measured operands; they are not extrapolated into invented 10/19-OFE counts.

The factor is the actual hydrology owner frame lane count, not merely a printed workload label or the number of covered destinations. Source binds that count to the controlled fixture's N as follows:

- Runner `hillslope/tests03/controlled_mechanism_experiments.rs:126-151` constructs exactly N area entries, prepares the corresponding scale fixture and calls `assert_authored_stage3_owner_cardinality` on its authored seed. `stage3_runner_qualification.rs:1691-1760` rebuilds the real static setup, checks area cardinality and LSE OFE cardinality/order, including decimal OFE identities at N>=10. At :1811-1840 it bootstraps that seed and checks `frame.identity.lane_count == frame.lanes.len() == N`, attachment lane IDs=N, and surface topology/bindings=N. Thus the source does not infer N from a single downstream represented-snow tile.
- `hillslope/snow_stage3_v11_production_seed.rs:1001` clones the entire live frame for the authenticated owner, and :1421-1434 passes that frame into `DirectV10RealConsumerShadow::try_new`. Bootstrap restores the scientific hydrology owner and rejects it unless it equals the entire freshly built live frame (:361-369). There is no single-lane owner substituted for a selected downstream carrier lane in this binding.
- Orchestrator `v9_real_consumer_shadow/direct_v9_real_consumer_shadow_impl.rs:214` invokes `validate_complete_owner_set`; :1300-1335 requires surface OFE-binding cardinality and layer-map cardinality equal hydrology frame lane cardinality, with matching production lane index, lane ID and ordered soil layers. These are typed owner joins, not telemetry assumptions.
- Each of the two native adapter calls already traced above receives that owner's entire hydrology frame (or its full clone with only the effective surface state replaced). `vegetation_real_hydrology_shadow.rs:311-320` rejects lane-vector/identity/layer-map cardinality mismatches and then seeds every frame lane exactly once. Consequently one successful native carrier execution adds `2*N` construction increments even when the adaptive trial identifies only one covered lane.
- The completed controlled workflow independently asserts committed snapshot lane count=N and published HBP OFE count=N (`controlled_mechanism_experiments.rs:237,266`). These executed assertions, together with matching authenticated input/owner provenance, must be present in each scale admission; the `ofe_count` JSON field alone is insufficient.

Prospective equation for matched successful native A/F scale admissions is therefore `D_A - D_F = 2*N*(P_A - P_F)`, with residual equality `D_A - 2*N*P_A = D_F - 2*N*P_F`. Use checked nonnegative integer arithmetic and actual completed provider counts, additionally require Provider=Carrier and started=completed/errors=0 on both sides. N must match on A and F and be an authenticated allowed fixture cardinality (1,10,19). Residual is scale-specific: do NOT pin it to405 outside the observed one-OFE case, and do not presume provider counts400/200 outside their observed workload. The one-OFE case still must reproduce its pinned raw values and405 residual.

This generic source relation is valid only on the same successful native physical-only carrier branch, with unchanged required outer/evaluator/group/mode/support/custody populations, independent resolved/batch/canonical obligations and full scientific outputs. Batch counters may not be silently folded into P: any changed batch population, alternate physical branch, failed provider, incomplete counters or failed cardinality binding rejects this qualification and needs separate attribution. Independently matched scale A/F admission records must populate raw D/P/N/residual values and pass the equation before scale comparison is accepted. No blanket volatile-counter exemption is authorized. All other manifest leaves remain exact under their separately reviewed existing qualifications.
