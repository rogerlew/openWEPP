# Isolated F implementation plan

Focused-04 prospective correction (dual review A/B approved): exact owned `crates/openwepp-hillslope-orchestrator/src/snow_stage3_v11_adaptive_production_tests.rs`, test lines 41-64; find-agents reconfirmed root/crates guidance. Report counts for all five roles, retain required Full/Retry/Half1/Half2 presence and Discovery/Exact presence, but remove unsupported existential Root requirement. The production wrapper selects Discovery/Exact; DirectStepTrial's only actual wrapper constructor is in a cfg(test)-included diagnostic. Root dispatch remains intact and all-five lower physical-transition/poison tests remain required. This corrects the historical prospective all-five actual-fixture expectation below, not a science/assertion waiver. Every actual invocation retains complete-field two-reference comparisons and closure. Primary A/F invocation-multiset parity is a separate gate, not inferred from this focused fixture. No new fixture campaign or A harness edits.

Postcompile exact owned correction scope: `terminal_carrier_provider.rs`, `stage3_solver/support.rs`, `stage3_solver/terminal_carrier_evaluation.rs`, and `runoff_reconciliation.rs` gate unused generic feedback support/interface/loop/helpers to cfg(test), preserving all existing feedback tests. Scan found no surviving production caller of the old support API; only cfg(test) convenience API calls it. Observation-only legacy request schema remains needed by shared audit traits/fingerprints; explicit Debug may preserve exact derived field order/value formatting without broad warning suppression. Dedicated actual oracle fixture in `snow_stage3_v11_adaptive_production_tests.rs` adopts the existing accepted NativeMixedPhase .0006/60s-boundary fixture used by native inactive-prefix validation; closure and every-role/mode assertions remain mandatory.

FIB1 prospective corrective test write map: `crates/openwepp-hillslope-orchestrator/src/v11_covered/open_snow_tail_tests.rs` (`identity_poisons_precede_acceptance_and_physics_rerun_is_absent`) and existing `crates/openwepp-hillslope-orchestrator/src/v11_covered/carrier_phase.rs` (`phase_receiver_is_immutable_and_trial_identity_is_exact`). Both exact paths passed to find-agents before edits; root/crates instruction chain applies. Extend existing source guards to the actual typed feed-forward entrypoint while retaining checks on the legacy test-only wrapper. No behavior or guard relaxation.

Prospective review B correction: the owning `CoveredCarrierEphemeralCandidatesV1` test comparator exhaustively names all private fields (no rest pattern), compares private soil candidate/continuation and validated resident/candidate/seal as well as public custody. Full-result equality uses typed equality/canonical owner bytes plus supplementary derived Debug round-trip representations for finite float bit distinctions; lower evaluator tests compare scalar `.to_bits()`. Actual reference test requires every Full/Retry/Half1/Half2/Root role and both discovery/exact; missing fixture coverage must be resolved before admission, never silently weakened. Invocation reuse evidence means fresh actual execution for repeated equal payloads plus existing identity-poison rejection; equal opaque bits alone cannot reveal a malicious provider's origin without changing custody semantics, and no global registry/new receipt is authorized.

Static: before behavior edits. Baseline A `2b56e6ebc9c8e8073fb68b89626d4d4e2b4602e3`; write root `/tmp/openwepp-controlled-mechanisms-Hb6uS2/F` only. No retained production treatment selector.

Authority: SC-SNOWENERGY-001 INV-088/C-056 and EXP-STAGE3-20260906-F; parent reports independent authority/protocol approvals and prospective authority tests PASS. Preserve the source-real guard ordering at carrier and evaluator seams.

Implement a structurally feed-forward request without hint/iteration, selected-provider enum chosen before execution, and separate typed feed-forward support entry. Preserve the generic feedback API for existing feedback tests. Share boundary validation and canonical surface/flux arithmetic. Actual isolated attachment uses one typed provider invocation; feedback still runs its original convergence loop. Resolved, adaptive Full/Retry/Half1/Half2/Root, discovery/exact, batch and canonical final evaluations remain independent. No cache, stopping-rule, physical formula, guard, owner or publication change.

Prospective exact write paths, relative to `crates/openwepp-hillslope-orchestrator/src/`:

- `hydrology/support_helpers_mod/runoff_reconciliation.rs`
- new `hydrology/support_helpers_mod/runoff_reconciliation/terminal_carrier_provider.rs`
- `hydrology/support_helpers_mod/runoff_reconciliation/stage3_solver.rs`
- `hydrology/support_helpers_mod/runoff_reconciliation/stage3_solver/support.rs`
- `hydrology/support_helpers_mod/runoff_reconciliation/stage3_solver/evaluation.rs`
- new `hydrology/support_helpers_mod/runoff_reconciliation/stage3_solver/terminal_carrier_evaluation.rs`
- new `hydrology/support_helpers_mod/runoff_reconciliation/stage3_solver/terminal_feed_forward_tests.rs`
- `hydrology/support_helpers_mod/mod.rs`
- `hydrology/03_kernel_support_00_support_helpers.rs`
- `snow_stage3_v11_terminal_execution.rs`
- `v11_covered/carrier_phase.rs`
- `v11_covered/carrier_phase/snow_boundary.rs`
- new `snow_stage3_v11_terminal_feed_forward_tests.rs` (test-only actual carrier oracle/control)
- `snow_stage3_v11_adaptive_production_tests.rs` (one authentic full-result reference test)

Additional exact paths passed to find-agents before edits: root/crates guidance applies. The authentic test enables a thread-local test-only oracle in the complete-owner test thread. At each actual candidate result, two fresh original carrier executions use identical immutable beginning/child/request physical inputs; the second legacy request receives the canonical hydrology preview hint. Compare candidate to both complete results including all phase-result fields and typed/canonical beginning/ending owners, then drop oracle results before ordinary retention. Record role/support/attempt/mode for every actual oracle execution. The source-real oracle is excluded from non-test builds and never changes measured compact runner posture. Generic evaluator tests independently require exactly two feedback calls and exact-zero coupling deltas; no fabricated convergence record on the typed path.

Derived tests are authored before behavior and expected-red source absence recorded. Parent coordinates serial execution; no worker builds or heavy tests. Gate commands: focused feed-forward tests, complete orchestrator tests, real consumer forced-reference/full-output parity, source structural negative capability, warnings-denied scoped lint, formatting, and critical-cut full-workspace checks per parent terminal gate map. NOT RUN until corresponding logs exist.

Lifetime delta: the sole carrier result is retained through the existing `Rc<CoveredCarrierPhaseResultV1>` maps; the duplicate same-invocation map replacement and its transient result no longer exist. No new result cache or changed owner lifetime is intended. Detailed audit is correctness-only and must report no fictitious second coupling iteration.
