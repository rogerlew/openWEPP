# Natural-limit diagnostic parity — review A

Static: independent read-only source assessment; no Rust build or runtime test by this reviewer. Ran: read-only parsing of retained A/F release logs, confirming their identical printed 15-entry actual residual vector. No scientific golden, source, or fixture edits. Applicable write guidance: root AGENTS.md and docs/work-packages/AGENTS.md, discovered for this artifact.

## Findings

1. **Medium — existing release evidence does not establish complete natural-limit diagnostic/rollback parity.** `crates/openwepp-land-surface-energy/src/covered_oracle_conformance_tests.rs:989` fails before pivot/matrix values, ordered residual identities/units and four float-bit fields are checked (`:999–1029`). Later finiteness/shape checks and final iteration assertion (`:1060–1085`, `:1184`) also do not execute. Kind, occupancy, active bounds, iterations and backtracking count were checked before the panic. The rollback assertion at `:1085` compares two fixture JSON hashes, not actual beginning inputs. This is a missing evidence finding, not an observed F/R numerical regression.
2. **Medium — Debug alone would silently omit a required failure field.** `numerics.rs:19–48` has eleven `NumericalFailure` fields, but its custom Debug deliberately omits `failed_solution`. `PartialEq` also does not establish signed-zero bit identity. A new observer must exhaustively destructure the actual failure and explicitly encode every float/optional float; a debug dump alone cannot close this gap.

Retained A/F logs both fail the unchanged golden with the same printed actual vector. Their raw SHA256 values are respectively `5f4c279602e193c305048e7613337c688728daf7dc87124abbe44083c892917e` and `13c1f3d6ebed0a36e38debc33cc4da2dfbb41376fcd985dc7aa7bf596bf6fb6e`. Two repeated presentations in each log are the same result, not independent runs. Default-debug PASS reported by the parent is different-build evidence, not a release golden PASS or a complete same-build cross-arm comparison.

## Existing evidence and its exact limits

- The natural vector at `covered_oracle_conformance_tests.rs:1150` uses the existing `column()` factory (`:443`), `HistoricalV8` (`:470`), and absent Stage 3 boundary/optical inputs (`:495`). R component replay is therefore ineligible. Preserving this existing failure route does not require constructing a new represented-snow failure fixture.
- R's `solver_component_dependency_replay_tests.rs:677` compares real solve/forced-complete outcomes, every rejected-failure float including failed solution and optional step norms, and exact matrix/RHS/linear/trial trace bits. It is not called with this natural vector and ends with a strict replay-savings assertion (`:767`), inappropriate for the ineligible historical fixture. Its successful represented-snow corpus complements but does not replace natural-limit evidence.
- `transaction_tests.rs:599` checks typed error mapping and diagnostic rollback lineage with a manufactured failure. It does not observe this actual natural solve. `transaction.rs:1173` is the canonical public `rejected_numerical_diagnostics` converter; its complete serialized `NumericalDiagnostics` can be observed without changing production APIs. Rollback hashes generated from a declared identity are diagnostic lineage, not evidence that external owners were executed and rolled back.
- Existing common `solver_mechanism_audit.rs:49` events contain ordered Newton sweep-base bits, stencil/probe values, evaluation scope/order and iteration ordinals. `solver_covered_solve.rs:576` returns the last actual Newton vector as `failed_solution` before that iteration's sweep. Thus all sweep bases plus final failed-solution bits cover the actual Newton-state trajectory. This audit does **not** expose every line-search trial value, Jacobian matrix, or LU delta. Do not relabel its exact event comparison as those richer observations.

## Minimal prospective common evidence — GO to author

Exact shared write set in A/F/R after each source unfreeze:

- `crates/openwepp-land-surface-energy/src/covered_oracle_conformance_tests.rs`: add only the child-module binding; preserve the existing golden test and all assertions.
- `crates/openwepp-land-surface-energy/src/covered_natural_limit_diagnostic_parity_tests.rs`: a self-contained ignored test and observation helpers. It is already under the parent's cfg(test) module; no production, test-support runner, public API, numerical hook or science-contract change is needed.

The child reuses private `fixture()` and `column()` and exactly reproduces the original four hydraulic assignments and 15-entry starting vector. The bounded repetition is test-vector setup, not a second physics implementation; explicitly bind/review it against the unchanged original test. Do not alter authority, occupancy count, forcing, caps, initial trial, or acceptance criteria to make replay occur.

Required observation/assertion content:

1. Call the actual `solve_covered_column` once under the existing detailed mechanism session. Require the actual `Rejected(IterationLimit)` outcome, declared iteration count, no candidate, and complete/nonoverflowed/nontruncated audit. Mathematical rejection is an `Ok(Rejected(...))`; do not falsely require the audit's Result-based Solve scope to be a typed-error failure.
2. Exhaustively destructure `NumericalFailure` with no `..`: kind, iterations, normalized residuals, ordered residuals, failed solution, occupancy ID, active bounds, backtracking count, step norms, pivot and matrix norm. Serialize discrete identity/order/Option fields and encode all floats via `to_bits`: both vectors, all four float fields in every ordered residual, all five optional step norms, optional pivot/matrix. Assert the still-applicable structural/finiteness/kind/shape/unit obligations independently of the failing frozen numeric equality.
3. Snapshot the actual input before/after: owned typed clone, complete derived Debug for structural fields, and an explicit nested float-bit vector covering the actual column, ground, geometry, soil, occupancy, leaf, biochemical, roots and shortwave operands. Include initial-trial bits and assert the actual absent cap/Stage 3 options. Reuse the reviewed R field traversal where practical; an equivalent local traversal must retain its field-coverage map. Do not use raw Rust memory/padding or replace actual input assertions with fixture hashes. No external owner transaction is executed by this test, and none should be claimed.
4. Use the unchanged public diagnostic converter on this actual failure and a fixed explicitly test-owned identity; validate and serialize the entire DTO, plus exact float bits. Retain every identity/code/pass/solve, residual/unit, active-bound/cap, bracket, optional value, and owner-lineage field. The identity is conversion context, not evidence of a newly executed five-owner workflow. Do not manufacture a replacement failure to feed the converter.
5. Emit the complete unfiltered common audit, including every Newton sweep-base and final failed-solution vector. Require its actual nonempty iteration/sweep/probe population, no loss/overflow, and zero component replay/identity-anchor starts on this HistoricalV8 route. Retain event and count equality across A/F/R; no arm-specific numeric normalization. Full per-line-search/LU observation remains separately scoped to the existing R trace tests, not falsely asserted here.
6. Run the exact same new test under the frozen release toolchain/profile/flags in all three arms, retain raw outputs and source/test/binary identities, and compare every diagnostic/input/trajectory field exactly. Retain the unchanged golden FAIL independently. This is an additional same-build differential oracle, not a golden rewrite, relaxed tolerance, full-workspace PASS, or scientific waiver.

## Residual risk and disposition

No existing test/raw record currently establishes the requested full same-build natural-limit comparison. The concrete additional test and A/F/R execution remain pending; failure/golden divergence root cause is not inferred merely from equal A/F printed vectors. The bounded common observer above is sufficient prospective scope for the preserved HistoricalV8 natural-limit route, alongside already-required R arithmetic/full-trace and custody evidence. No additional physical fixture campaign or numerical behavior change is warranted by this finding.

**GO to author the two-path common test-only observation cut. HOLD any claim of complete natural-limit differential PASS until its complete retained records compare exactly.**

## Concrete common observer review

Static: reviewed the entire new common observer and its parent binding. Ran: read-only source comparisons and SHA256 checks; no Rust compilation/execution. Final reviewed new-file hash is `0d27a1e53cfa9c6ac719dea7cee6f713c88dd85fe97119a484386f37954f1467`, identical in main/A and isolated F. The first submitted `88d8a16a...` cut was superseded solely by the additional declared-ordered-residual length assertion before zip (`:276`); it is included in this GO. R remains frozen and has not yet received the common file.

No new correctness blockers. `covered_natural_limit_diagnostic_parity_tests.rs:53` exhaustively encodes all eleven actual failure fields; residual and step-norm destructures are exhaustive too. Its whole DTO serialization (`:85`, `:402`) retains discrete identities and adds explicit bits for every currently declared float field; the actual canonical converter and validator supply code/shape/finite-domain checks. Parse-roundtrip bit equality is a real assertion, not presumed to pass. A future runtime failure here must be dispositioned truthfully, not hidden in the output collector.

The new file's setup (`:207`) exactly matches all four original assignments and the complete 15-number source vector, verified by read-only comparison. Both parent conformance files differ from the prior checked-in source only by the child-module binding; the numerical golden and its assertions are unchanged. Actual solve-input typed/Debug/float-bit before-after assertions (`:360–377`) and actual `Rejected` matching are independent of fixture rollback hashes. DTO owner hashes are explicitly limited to converter lineage.

Detailed observation (`:305–348`, `:409–433`) requires a real solve/sweep/probe population, no dropped events or overflow, closed/balanced scopes, no represented-snow replay or anchors, and a first Newton base equal to the original start. Every raw event is retained alongside all Newton bases and final failed solution. The audit's successful Result-based solve scope is correctly distinguished from mathematical rejection. The claimed trajectory remains Newton-state/event parity, not unobserved LU matrices or every rejected line-search trial.

**Medium, intentionally retained test duplication:** `column_float_bits` (`:135`) repeats the R-only rollback observer's nested field traversal. Read-only extraction comparison proves their function bodies identical at this frozen cut, so the earlier thirteen-type field audit applies. Retention is justified here to keep a byte-identical self-contained common A/F/R observer independent of the R-only replay test module while R is frozen; it copies no physics, guards or solver algorithm. The original natural fixture's short numeric setup is similarly repeated solely to leave the frozen failing golden test untouched. These are bounded experiment-only observation copies, not permission to maintain parallel production logic; any later reusable test framework should centralize them and preserve complete field coverage.

**GO for this concrete common test-only source cut and parent-owned same-build execution.** No runtime PASS, cross-arm equality, complete-workspace PASS or golden relaxation is inferred. Final formatted/source-kit identities and exact A/F/R raw record comparison remain required before the corresponding parity claim.

### Prospective two-lint cleanup

Static: independently read the A-clippy-json-02 raw diagnostics for this observer: `clippy::doc_markdown` and `clippy::assertions_on_constants`. GO for doc backticks around `HistoricalV8` and only a statement-local `#[expect(clippy::assertions_on_constants, reason="ignored comparison must reject debug execution")]` on the unchanged `assert!(!cfg!(debug_assertions), ...)`. This deliberately retains the fail-closed release-posture guard; no broad lint allowance, numerical change or assertion removal is approved. Apply identically to all three arms after the parent's source-read barrier; final source confirmation and actual lint rerun remain pending.

Concrete confirmation: final `b995075a11c0ec6343a7b34708c1550fc4c5269b9dcd606ea2e4a8630acaa6a4` is identical in main/A, F and R. A read-only inverse-edit/hash check reconstructs the exact previously reviewed `0d27a1e...` source by removing only those two approved edits. Statement-local expectation preserves the original release guard and all runtime assertions. Source GO; actual current-cut execution/record comparison remains pending.

Compiler-driven placement correction: the parent reports the statement-macro expectation was ignored and produced `unused_attribute` plus the original assertion lint. The prior static placement GO did not establish compiler acceptance and that cut did not pass its lint gate. Reviewed replacement `2c219dbd3626195f735a9ee4db3a74b173462a1ef13fa290afdefa6a9e1dd156` is identical A/F/R: only move the same expectation and unchanged assertion into private `require_release_posture()`, called as the ignored test's first statement. The helper contains no other logic or assertions, so the expectation remains narrowly scoped and rejection precedes fixture setup. Read-only inverse transformation reconstructs exact prior `b995075a...` bytes. GO for this corrective source cut; actual compiler/lint execution remains required and has not been run by reviewer A.
