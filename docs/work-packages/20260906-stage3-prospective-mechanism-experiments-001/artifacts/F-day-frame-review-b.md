# F day-frame telemetry qualification review B

Static: independent source review plus read-only extraction of the two
parent-executed admission records. No source/collector edit, build or simulation
by B. Instruction discovery returned root and work-package AGENTS.

## Findings and decision

No blocker in the narrowly scoped mechanism-dependent telemetry attribution.
GO to implement a separately identified conditional comparator qualification
for exactly `/direct_runtime_counters/day_frame_constructions`, not a volatile
provenance exemption. Detailed invocation/custody parity and executable
collector negative controls remain required before timing admission.

Independently read the source chain and confirmed no F-versus-A changes to
the adapter/native builder/counter/runner mapping files. Native physical
evaluation calls the first hydrology adapter at
`v9_real_consumer_shadow/frozen_litter_v3_adoption.rs:1004`, through V4's
fixed-final preparation, and constructs the second at
`frozen_litter_v4_adoption.rs:976`. Each successful adapter seeds exactly one
day frame per actual lane (`vegetation_real_hydrology_shadow.rs:317`). The
sole production counter increment is `DirectDayFrame::seed` at
`direct_runtime/00_core_frames.rs:1506`, one atomic increment after index
validation. It counts frame constructions, not heap allocation bytes, live heap,
accepted publications, physical budgets or owner mutations. The runner serializes the unchanged
snapshot delta; no telemetry value controls the solver.

The actual represented-snow physical-only branch retains these two distinct
constructions per successful carrier call. Removing F's redundant second
carrier invocation removes both adapter constructions for that invocation;
it does not remove an independent publication/commit or change those builders.

## Actual operands and strict relation

Exactly one controlled record was read from each log:

- A `raw/A-admission-02.log`:
  `9fcf007dc167bb67cbf4627a8c4fa49a5f806e603d15e21559b1ff73d54c2c18`.
- F `raw/F-admission-01.log`:
  `e1df38ce94d323d9ac43845480917bf02613c96e17c5f5acce0c1bccbb2b6c35`.

Both one-OFE records have72 outer and200 evaluator starts/completions, zero
errors/dropped records, zero batch providers. Provider=carrier starts=completions
are400 for A and200 for F. Day-frame constructions are1205 and805. All other
direct-runtime counter fields are exactly equal, including commits0, compute10,
phase entries26 and state mutations10.

For this exact frozen one-lane native fixture:
`1205−805 = 400 = 2×(400−200)`, and
`1205−2×400 = 805−2×200 = 405`.
This joins independent existing construction telemetry with actual physical
carrier/provider lifecycle counts and the two-call source proof. It is not
merely a rewritten producer formula. The residual405 is not assigned a guessed
physical meaning or equated with accepted days.

The comparator must preserve raw values; require exact fixture/native posture,
one lane, exact balanced/error-free counts above, exact1205/805 totals and
equal405 residual; then qualify only the one named leaf. Every other manifest
counter/scientific operand remains exact. Wrong total/count/role, dropped/error
record, different topology/fixture/branch or a second changed leaf must reject.
Do not generalize by subtracting any arbitrary observed provider count, deleting
the counters object, or calling this counter volatile provenance.

The separate detailed trace must still prove preservation of every authentic
outer/evaluator/role/support/custody invocation group; counts alone cannot prove
it. Full physical candidate/flux/owner/closure oracle, output and error/rollback
gates remain unchanged. This source attribution is neither F timing admission
nor evidence that an unmatched scientific output may be normalized.

Non-blocking follow-up: future topology/branch expansion needs its own scoped
attribution; do not silently promote the single-lane constants into a universal
two-seeds-per-provider policy.

QA: GO exact proposed bounded diagnostic relation, executable comparator and
remaining mechanism admission gates pending.

## Prospective authenticated scale extension

Static: reviewed `F-day-frame-attribution.md` extended cut SHA-256
`7536ede62b8b16ca7a9723a0c4deddef73a4465ac7a88595af936ee0c29cdb33`.
No scale execution or collector activation occurred in this review. GO to
freeze one bounded relation before timing for N in1/10/19, with the following
scope; it does not erase the single-lane source restriction above by assumption.

Source closes the additional premise: production-seed authoring clones the
entire live frame at `snow_stage3_v11_production_seed.rs:1001` and installs it
into the actual complete owner at1424. Bootstrap rejects unless the restored
hydrology owner equals the whole newly built live frame at365. Complete-owner
validation independently binds frame lanes, surface OFE mappings and layer
maps at `direct_v9_real_consumer_shadow_impl.rs:1300`. The controlled fixture
bootstrap asserts lane identity/vector/attachment cardinalities equal N
(`stage3_runner_qualification.rs:1813`), and downstream snapshot/HBP assertions
retain N. It is not enough to trust the printed `ofe_count` alone.

Both native adapter calls receive that full frame or its full effective-surface
clone, then validate lane/vector/map cardinality before seeding every lane.
Therefore their successful per-carrier construction contribution is2*N even
when only one downstream covered lane is the trial target. This is source
ownership evidence, not extrapolation from the1205/805 observations.

The prospective qualified leaf may retain an explicit
`qualification: F-two-full-lane-seeds` tag and
`noncarrier_constructions: D−2*N*P`. Require checked nonnegative integer
operands/arithmetic; identical authenticated allowed N and frozen input/native
physical-only posture; complete balanced zero-error/no-drop Provider=Carrier
counts; unchanged independent batch population (never folded into P); and all
other manifest/scientific fields exact. Retain raw D/N/P and qualification
evidence. For N=1 retain the actual1205/400 and805/200 pins and405 residual.
For N=10/19, independently observed paired admission records must satisfy
`D_A−D_F = 2*N*(P_A−P_F)` with their own exact equal residual. Do not import405
or predict future counts. A failed scale premise rejects qualification.

Required detailed actual invocation/group/mode/support/custody parity, full
physical/owner/closure and output/error tests remain separate admission gates
for each applicable scale. The expression is not an alternative to those
proofs. This prospective extension avoids a later unexplained exception but
does not approve any as-yet-unobserved scale record or timing sample.
