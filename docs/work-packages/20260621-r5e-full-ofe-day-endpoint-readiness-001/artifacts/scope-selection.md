# Scope Selection

Status: complete.
Evidence mode: Static.

R5E is selected as the final R5 package from
`docs/work-packages/r5-burndown-execplan.md`.

Selected scope:

- canonical 14-phase direct endpoint accounting;
- R4/R5 direct subspan folding under canonical phase entries;
- no-compatibility source/runtime proof;
- default-disabled H2637 guard;
- opt-in direct-skeleton endpoint/RSS evidence;
- protected output identity/equivalence.

Not selected:

- direct publication cutover;
- default activation;
- output schema changes;
- R7 compatibility-runtime deletion.

Scope verdict: complete as selected. R6 owns the next publication authority
change.
