# Worker Handoff

Status: `EXECUTED`

Completed item:

`WSHED-W4-HOLD-001` was closed for the production public watershed CLI path by
replacing compatibility projection routing with `WatershedNetworkFrame`-native
dispatch and typed publication.

Remaining boundary for future work:

- W5 owns old compatibility-code deletion or retirement.
- The committed carnivorous-adobo fixture remains parser/input substrate only
  until a TOML/HBP-bound end-to-end fixture is authored.
