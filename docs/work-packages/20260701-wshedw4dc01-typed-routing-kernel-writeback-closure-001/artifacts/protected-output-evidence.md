# Protected Output Evidence

Status: `QUEUED`

W4DC01 execution must prove protected output identity or contract-governed
deltas after typed routing no longer projects through the old writeback surface.

Required:

- committed fixture paths used;
- command surface and `--jobs` values;
- decoded Parquet row comparison by content and order;
- explicit contract-governed delta disposition if identity is not preserved;
- comparator/log artifact paths.
