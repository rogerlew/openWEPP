# Source Guard Evidence

Status: `QUEUED`

W4DC01 execution must add or update source guards for:

- public watershed CLI production routed-stage handoff;
- watershed orchestrator production dispatch;
- WS10/WS11/WS12/WS18/WS20 production routing helpers;
- typed publication frame creation;
- any allowed compatibility/replay/diagnostic old-surface paths.

Allowances must be path-scoped and justified. Broad allowlists cannot close the
gate.
