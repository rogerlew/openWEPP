# Conservation Reconstruction

Status: `QUEUED`

Record independent reconstruction and conservation/magnitude audit for typed
publication operands.

Required:

- operand-lineage updates if W4 lineage changes;
- rejected aliases and wrong formulas;
- independent reconstruction from produced outputs or typed frame artifacts;
- closure or magnitude audit on committed fixtures;
- metadata/schema alignment.

One-sided bounds and exact self-consistency are supporting sanity evidence only.
