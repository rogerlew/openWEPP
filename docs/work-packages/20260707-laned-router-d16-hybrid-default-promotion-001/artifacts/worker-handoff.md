# Worker Handoff

Status: EXECUTED-HOLD-FIDELITY-TOLERANCE. Evidence mode: Static + Ran.

## First Actionable Follow-On

Scaffold a hybrid default-promotion fidelity-tolerance hold-lift package.

Start from:

- `SC-OFEROUTE-002` rev 4.
- D16 H2637 pre-change outputs under
  `docs/work-packages/20260707-laned-router-d16-hybrid-default-promotion-001/artifacts/`.
- GAP-OFEHYB-001 Case-4 ladder evidence.
- GAP-OFEHYB-002 solve-cost evidence.

Required first action: define production-facing acceptance tolerances before
any selector flip, then run a fixture cohort broad enough to justify or reject
those tolerances. H2637 alone is not enough to reverse-fit acceptable movement
for HBP/pass sediment publication surfaces.

## Current Posture

- Hybrid remains explicit opt-in via `OPENWEPP_LANED_ACTIVE_IMPLICIT=1`.
- Active Lane-D routing remains explicit opt-in via `OPENWEPP_LANED_ACTIVE=1`.
- Tier-2 mesh-resolution adjudication remains queued and separate.
