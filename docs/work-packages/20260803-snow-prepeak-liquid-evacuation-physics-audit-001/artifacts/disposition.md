# Disposition

Status: `HOLD-EVIDENCE`

Evidence mode: `Static + Ran + dual review + dual verification`

The read-only characterization is executed, reviewed, and verified. Production
Rust, contracts, tests, fixtures, observations, selectors, defaults, and
parameters remain unchanged. The accepted scientific surface is the
prospectively frozen same-binary v3 execution only; v1 and v2 remain rejected
custody evidence.

## Scientific Disposition

Snowbird's modeled pre-peak pack loss is generated upstream of Stage 3. Median
primary-window loss is `0.5296 m` against `0.5379 m` gross-positive hourly CoE
melt. The modern positive-parts/capacity trajectory exports this liquid, while
current Stage-3 cold content, refreeze, and explicit longwave are snow-neutral.
Legacy routing produces an order-one rollback bound, but does not prove that
the legacy density gate is correct or isolate capacity as the sole owner.

Warm/aerodynamic `B/C` terms are the largest Snowbird signed raw-term scale;
forcing bias versus empirical-formula inadequacy remains unresolved. Mixed
signed hours, downstream-neutral thermal state, and export policy interact at
material magnitude. Rain heat, pack exhaustion, downstream mass accounting,
and current Stage-3 radiation are excluded as dominant direct causes. Broader
phase or precipitation-forcing error, stateful cloud/shortwave response, and
the physically correct signed-hour/export treatment remain unresolved.

Wet compaction receives duplicated state-loss data through two aliases. The
data-flow duplication is exact and independently reconstructed, but active
multilayer authority does not yet prescribe the correct complete operand. The
separate CoE boundary excludes this alias as the direct source of authoritative
SWE loss.

## Hold Basis

The package required independent Stage-3 liquid closure. The runtime diagnostic
owns incoming, routed, retained, refrozen, and residual liquid operands, but the
real JSONL consumer omits incoming, routed, retained, and residual values.
Internal guards are not independent downstream evidence. Instrumentation was
outside this read-only write set, so the unmet current-scope closure gate forces
`HOLD-EVIDENCE`; it does not invalidate the completed mass reconstruction,
energy boundary, event attribution, or causal bounds.

## Next Admissible Action

Prepare a behavior-neutral, contract-first instrumentation package that
publishes the missing Stage-3 liquid operands and the hourly forcing/state
operands needed to adjudicate mixed signed hours. Establish active multilayer
wet-compaction operand authority before correcting the duplicate alias. Then
run a prospectively frozen, no-tuning event cohort that separates forcing,
empirical `B/C`, signed-hour thermal handling, and export-boundary hypotheses.
