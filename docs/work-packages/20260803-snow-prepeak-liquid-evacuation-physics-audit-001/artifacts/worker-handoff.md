# Worker Handoff

Status: `ready / package held on evidence`

Evidence mode: `Static + Ran + dual review + dual verification`

The smallest justified successor is behavior-neutral instrumentation, not a
physics correction or calibration:

1. publish Stage-3 incoming, routed, retained, refrozen, and closure-residual
   liquid through the real JSONL consumer;
2. publish the hourly forcing, branch, cold-content, retained-liquid, and
   pre/post pack-state operands needed to adjudicate mixed signed hours;
3. establish the authoritative active-multilayer wet-compaction operand before
   altering its duplicated data flow; and
4. freeze a no-tuning event cohort before testing forcing, empirical `B/C`,
   signed-hour thermal handling, or export-boundary candidates.

Do not treat the legacy-routing rollback bound as proof of correct legacy
physics. Do not add negative empirical melt directly to SWE, fit a phase
threshold, or promote a cloud/shortwave correction from the retained diagnostic
comparisons. Preserve v3 identities and the rejected v1/v2 custody record.
