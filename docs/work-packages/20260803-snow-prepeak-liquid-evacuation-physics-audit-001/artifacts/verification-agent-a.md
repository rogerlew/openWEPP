# Verification Agent A

Status: `PASS after remediation`

Evidence mode: `Static + Ran`

The verifier independently rehashed the accepted v3 freeze, tool, release
binary, dynamic result, execution receipt, all replay outputs, fixtures,
runfiles, observations, predecessor traces, helper tools, upstream diagnostic
inputs, and legacy blobs. All 16 real-consumer cells returned zero and every
recorded identity reproduced.

Independent reconstruction over 61,364 daily and 1,472,736 hourly rows passed
the frozen mass, CoE-term, applied-melt, routed-alias, wet-input-alias, and
Stage-3 energy tolerances. The 154-window primary cohort, 158-window censored
sensitivity, event coverage, forensic predicates, and same-binary operator
deltas also reproduced.

The initial verification precheck found an undeclared invalid-run recovery
namespace, stale review wording, four stale event-share values, and undisclosed
post-freeze HEAD drift. Each was accepted and remediated without changing the
accepted v3 evidence. A fresh exact-current recheck returned `PASS`.

The verifier confirmed that the Stage-3 runtime owns four liquid operands that
the real JSONL consumer omits. Independent liquid closure is therefore
impossible, and `HOLD-EVIDENCE` is the correct disposition.
