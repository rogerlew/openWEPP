# Independent Verification A

Verifier: `/root/cal01_verify_a`

Mode: independent read-only terminal integrity and completeness verification.

Verdict: `PASS_WITH_PUBLICATION_HOLD`

The verifier independently confirmed:

- all 19 source paths, sizes, and SHA-256 identities, both subtree checksum
  manifests, and commissioned-file identity against WEPPpy;
- 140 unique 18-column ledger rows, 60 return-period rows, and 12 correctly
  typed WEPPcloud hill-streamflow/lateral-flow rows;
- deterministic `READY_BOUNDED` fixture, files, transformations, four arms,
  `0.92`/`0.95` branches, seasonal-deciduous baseline, and constant-cover
  prohibition;
- ten readable Git-LFS-routed PDFs;
- 23-file Markdown lint and validation with zero findings;
- clean diff hygiene and a clear separation between the 47 CAL-01 paths and
  prior/unrelated dirty-tree paths; and
- the publication-rights hold as the only substantive hold.

At the snapshot, only the two verifier-receipt files were necessarily absent.
This file records Verifier A's result.

Post-verification note: the operator subsequently confirmed redistribution
permission for the retained third-party set and directed commit/push,
superseding the sole hold.
