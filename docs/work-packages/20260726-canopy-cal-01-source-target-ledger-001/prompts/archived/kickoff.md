# Execute CANOPY-CAL-01 Source And Target Ledger

Scope: local repository scientific-evidence engineering; flat-file reads and
edits in the openWEPP worktree plus read-only inspection of the sibling
WEPPcloud source directory and operator-supplied sources. Official
publisher/institutional network acquisition is authorized only for cited
literature absent from the supplied set.

Execution mode: package-end-to-end.

Phase plan: execute every phase in `package.md` sequentially through final
disposition.

Required reading:

- Core: `AGENTS.md`, `docs/work-packages/AGENTS.md`, this package,
  `docs/planning/canopy-phenology-assurance-roadmap.md`, the WEPPcloud Elliot
  reference README, commissioned PDF, both delivered management files, and the
  WEPPcloud parameterization-research artifact, operator-supplied literature,
  and the retained Hubbard Brook synthesis-book sources.
- Conditional: `docs/standards/testing-and-gate-strategy.md` for intent and
  terminal gate planning; `docs/standards/prompt-wording-guidance.md` if this
  prompt changes; `tests/fixtures/AGENTS.md` only if scope is prospectively
  amended before fixture edits.
- On-demand: the completed GSI/native integration packages and canonical plant
  and residue contracts when classifying a disputed operand.

Required-reading budget: `REQUIRES-JUSTIFICATION`; the commissioned PDF and
target-bearing literature are load-bearing authorities and cannot move
on-demand. Map:
`artifacts/required-reading-map.md`.

Files: edit only the declared write set in `package.md`. Treat the sibling
WEPPcloud repository and every production/contract/test path as read-only.

Task: preserve and verify the commissioned sources, build the typed target and
comparison-scale ledgers, preserve the Hubbard 92/95 branches, inventory missing
run artifacts, issue the deterministic CAL-02 admission verdict, and complete
review, verification, and disposition.

Constraints: evidence-first classification; no model changes; no silent
defaults; no unsourced value may carry calibration or validation; no hillslope,
watershed, or channel aliasing; hash mismatch fails closed.

Subagent requirement: no heavy gate is selected. This prompt explicitly
authorizes subagent spawning/delegation to two independent read-only reviewers
and two independent read-only terminal verifiers for source/target
classification, scale review, admission reconstruction, and final path/hash
validation; outputs are compact package artifacts with exact findings; write
access is read-only.

Autonomy: execute all package phases without requesting further direction
unless a declared hash, source-identity, or authority boundary hard-blocks safe
progress.

Outputs: update every required package artifact and record a truthful final
disposition; scaffold placeholders do not count as run evidence.
