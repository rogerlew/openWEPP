# Poison And Rollback Evidence

Evidence class: `Ran`.

Executable tests cover caller changes to matric potential, current
conductivity, root path, signed gravity, accessibility and frozen posture;
the complete day ignores those non-authoritative template values and produces
the same complete V10 candidate as canonical input. Ground VIS/NIR albedo and
upward longwave poisons reproject to exact tile-qualified LSE-owner bits. A nonempty unowned
routed-runon parcel rejects with typed unsupported before any shadow mutation.

Persisted restart tests retain the complete checkpoint poison matrix, exact
live bytes on every rejection, interval-24 fresh-object continuation, exact
abort bytes and day-boundary cumulative scheduler position. All 30 production
restart tests and all 28 released-reference tests pass.
