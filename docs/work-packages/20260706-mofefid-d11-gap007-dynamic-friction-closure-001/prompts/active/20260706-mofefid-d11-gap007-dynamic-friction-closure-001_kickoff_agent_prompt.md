# Kickoff Prompt - MOFEFID-D11 Gap-007 Dynamic Friction Closure

Scope: local repository science-contract/kernel migration task; flat-file
reads/edits only; no external connectivity required.

Execution mode: package-end-to-end (default).

Phase plan: execute all phases in `package.md` sequentially through
disposition.

Required reading (read before edits):

Core:

- `AGENTS.md`
- `docs/codex_exec_plans.md`
- `docs/work-packages/AGENTS.md`
- `docs/work-packages/README.md`
- `docs/specifications/science-contracts/AGENTS.md`
- `docs/specifications/science-contract-authoring-procedure.md`
- `docs/specifications/science-contracts/kernel-process-contract-profile.md`
- `docs/work-packages/20260706-mofefid-d11-gap007-dynamic-friction-closure-001/package.md`
- `docs/work-packages/20260706-mofefid-d11-gap007-dynamic-friction-closure-001/artifacts/required-reading-map.md`

Conditional:

- `docs/standards/AGENTS.md` and
  `docs/standards/prompt-wording-guidance.md` when editing prompts.
- Science-contract profile/index docs when editing
  `SC-OFEROUTE-001` or registry entries.
- Rust local `AGENTS.md` files for any touched crate/test paths.

On-demand:

- `docs/specifications/science-contracts/contracts/SC-OFEROUTE-001.md`
- D11 package artifacts under
  `docs/work-packages/20260705-mofefid-d11-friction-operand-authority-001/`
- Current Lane D shadow and direct runtime files named in `package.md`.

Required-reading budget: OK; map:
`artifacts/required-reading-map.md`.

Files: use the intended write set in `package.md`.

Task: execute the package objective end to end. Close the remaining
`SC-OFEROUTE-001#GAP-OFEROUTE-007` hold for dynamic Lane D friction operands
(`I`, `LAI`, `h_c`) through contract-first correction and real consumer proof,
or record a legitimate source-authority hold.

Constraints: contract-first sequencing; canonical `SC-*` authority; typed
guards; no silent defaults; no canonicalize-and-proceed for domain violations;
no surrogate/provisional/proxy/heuristic process physics.

No surrogate physics: production or activation-candidate code must consume
actual contract-backed runtime surfaces. Missing authority is a hold boundary;
known in-scope authority is an implementation obligation.

Real consumer proof: prove the opt-in Lane D shadow reads the corrected dynamic
operand path. Shadow diagnostics cannot claim production/default activation,
D10 Case-4 shock acceptance, D12 melt-limb closure, D13 erosion hourly-shape
closure, D14 profiling readiness, or D15 default promotion.

Subagent requirement: REQUIRED for independent review/verification and heavy
closure gates when session-level tool policy permits spawning. This prompt
explicitly authorizes subagent spawning/delegation to `rust_code_reviewer`,
`rust_qa_reviewer`, `explorer`, and `comparator_suite_runner` for the package
scopes named in `package.md`; outputs: compact findings, metrics, log paths,
and artifact text; write access: read-only unless explicitly bounded by the
operator. If session authorization is absent or spawning is unavailable, record
that block before substituting local gates where package governance allows it.

Autonomy: execute package phases end-to-end and update required artifacts
without requesting additional user direction unless hard-blocked.

Outputs: updated package artifacts, updated `SC-OFEROUTE-001` if evidence
requires it, tests/code changes needed for the consumer proof, gates with
`Static:`/`Ran:` labels, finding disposition, worker handoff, and final
disposition.
