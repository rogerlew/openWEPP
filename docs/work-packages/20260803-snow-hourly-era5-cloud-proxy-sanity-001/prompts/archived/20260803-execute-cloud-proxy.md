# Execute Cloud-Proxy Sanity Check

Scope: local repository external-data diagnostic; flat-file reads/edits only;
no external connectivity.

Execution mode: package-end-to-end. Execute `package.md` through disposition.
Read `artifacts/required-reading-map.md`, predecessor radiation artifacts, and
the frozen cloud comparison manifest before edits. Preserve production, tests,
fixtures, observations, precipitation, multipliers, and snow parameters.

Subagent requirement: none for execution. This prompt explicitly authorizes
subagent spawning/delegation to two independent read-only science/data reviewers
and two read-only terminal verifiers; outputs are compact package-local evidence.
