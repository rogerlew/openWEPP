# TESTGATE Affected CRAP Planner Escalation Prompt

Execute `20260722-testgate-affected-crap-planner-escalation-001` end-to-end.
Make measurement-only Cargo selection escalate to global quality before node
construction, test narrowly, obtain dual review, close RTR-035 only at the exact
reviewed correction commit, then delegate exactly one changed-head qualification
and dual terminal verification.

Subagent authorization: this prompt explicitly authorizes subagent
spawning/delegation to the review, comparator, and verifier roles named in
`package.md`; expected outputs and write access are exactly those declared there.
Do not push, deploy, switch branches, manually dispatch TESTGATE, run HEAVY on
the parent, or repeat an unchanged expensive gate.
