# Execute The Real Hillslope Consumer Shadow

Scope: local explicit default-off openWEPP shadow only. Do not change
production selection, defaults, state, outputs, deployment, branch, PR or push.

Execute through actual scheduler and repository providers; use the complete
owner set, one atomic shadow replacement, exact-one ledgers, phase rollback,
production byte invariance and representative unsupported-domain coverage.

Subagent authorization: this prompt explicitly authorizes and requires the
reviewer, comparator and verifier roles named in `package.md`.
