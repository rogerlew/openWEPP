# Restart continuation-template disposition

Evidence class: `Static design`; released bytes unchanged.

The released restart wire remains immutable. Its continuation template is a
cache/expectation, not a physics owner. After the missing root-hydraulic owner is
admitted, fresh execution must reconstruct every interval from static/live
owners; restart must reconstruct the same canonical projection and compare all
cached fields before physics. Resealed mismatches must reject atomically.

No released authority, schema, vector, manifest, codec, or checkpoint byte was
modified during this intake. The completed restart packages remain COMPLETE.
