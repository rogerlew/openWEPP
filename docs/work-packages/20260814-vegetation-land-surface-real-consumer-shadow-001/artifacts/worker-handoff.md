# Worker handoff

Status: `HOLD / contract-first root-hydraulic owner admission required`.

First actionable item: amend `SC-VEGETATION-001` (and the owning soil-hydraulic
contract if selected) to admit the exact per-OFE/per-layer source and projection
for `matric_potential_mm`, `root_path_length_mm`, and `gravity_root_mm`.

The amendment must define units, sign, equations or explicit configuration
fields, OFE/layer/root mapping, digest/lineage, ordering, domains, and independent
test vectors. It must state why WB14 wetting-front suction is not the vegetation
root-zone potential. Once admitted, resume the live-owner API redesign and all
remaining phases from the user execution prompt. Do not alter the released
restart wire.
