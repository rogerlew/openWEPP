# Review Finding Disposition

Status: `first review findings accepted / remediation awaiting re-review`

The first independent land-surface, hydrology/ownership, and Rust reviews all
returned HOLD. Every material finding was accepted:

- moved the explicit attachment to a complete pre-native repository day
  projection and exact-joined the later lane-interleaved production inputs;
- made laned-active routing typed unsupported;
- validated the incoming V8 identity before V9 rebinding and added three
  identity poisons;
- generalized inactive canopy-component temperature anchoring to wet, sun,
  shade, and stem zero-area nodes;
- replaced duplicated nitrogen inventory projection with the BGC-owned helper;
- made shared-soil candidate aggregation OFE/layer complete and tile-order
  canonical, with omission, duplicate, wrong-owner, extra-tile, distinct-credit
  and reorder evidence;
- strengthened complete-owner transaction and lane/layer-map joins;
- replaced clone-only restart evidence with JSON round trips for all four
  serializable scientific owners before reconstruction and continuation;
- removed the inaccurate claim that arbitrary external row-sink side effects
  can be rolled back; owned production and shadow candidates remain atomic.

Multi-OFE forcing heterogeneity remains explicitly typed unsupported because
the admitted `SnowFreeForcing` physiological soil vector is global. A complete
multi-OFE repository day record may execute only when those global operands
are bit-identical; no averaging or first-OFE selection is permitted.

Fresh exact-byte re-reviews remain pending.
