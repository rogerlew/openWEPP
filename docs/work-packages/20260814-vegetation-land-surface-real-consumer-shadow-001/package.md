# Execute Vegetation And Land Surface Through A Real Shadow Consumer

Status: `COMPLETE / terminal A+B PASS`

Date: `2026-08-14`

Package ID: `20260814-vegetation-land-surface-real-consumer-shadow-001`

Plan class: `Critical real-consumer default-off integration`

## Objective

Execute the admitted vegetation/LSE/real-hydrology owner set through the actual
hillslope scheduler and repository providers on isolated shadow state, then
prove exact-one custody, atomic replacement, rollback, production invariance,
domain coverage and performance without activation.

## Progress

- [x] Start after Children 1--3 close.
- [x] Freeze real input providers, scheduler point and complete owner set.
- [x] Implement explicit shadow API and atomic whole-state transaction.
- [x] Run repository-backed scenarios, coverage audit and legacy comparison.
- [x] Admit and implement exact root-zone matric-potential and root
  path/gravity ownership; remove caller-controlled ground optics and unowned
  runon from the V10 seam.
- [x] Complete comparator and independent reviews on one exact implementation
  commit.
- [x] Complete exact-head full-workspace correctness.
- [x] Complete terminal verification.

## Implementation Intent

Intent: `critical default-off real-consumer integration`; no calibration,
empirical-validation, activation, publication or cutover claim is authorized.

The existing direct scheduler gains an explicit opt-in shadow attachment. When
absent, its control flow and production state remain unchanged. When present,
the scheduler executes exactly one complete-OFE shadow day after repository day
input projection and before native hydrology, advances 48 retained
`1800 s` transactions on isolated owner state, then atomically replaces only
the shadow attachment. Any failure preserves both production and shadow
beginning bytes.

V9 is adapted to the unchanged V8 physical runtime only through the reviewed
identity-only V9/V8 payload boundary. V9 changes no equation, parameter,
owner, branch or supported domain.

## Intended Write Set

- this package and campaign-coordinator lifecycle/evidence trees;
- `crates/openwepp-hillslope-orchestrator/**` for the explicit attachment,
  provider projection, V9 adapter, scheduler hook and atomic replacement;
- `crates/openwepp-vegetation/**` only if an identity-only V9 runtime adapter
  requires an owning helper;
- affected integration tests and Cargo target declarations;
- canonical contracts only if exact implementation evidence exposes a missing
  integration rule that cannot be derived without an amendment.

No runner selector, environment variable, production default, production
owner commit, published output or deployment surface is in the write set.

## Execution Phases

1. Reconcile governance and freeze exact providers, scheduler seam, owner set,
   V9 identity boundary, cadence, rollback bytes and exclusion surfaces.
2. Add contract-derived consumer and poison tests before production edits.
3. Implement the explicit default-off attachment and repository-provider
   projection, including complete-OFE soil-thermal candidate aggregation into
   one independently validated shared ending snapshot.
4. Execute repository-backed open, covered, mixed, multi-OFE, dry/wet,
   day/night, partial-authorization, condensation/runon and unsupported-domain
   scenarios; prove 48-step restart equivalence, exact-one custody, rollback
   and production byte invariance.
5. Run affected and campaign-wide validation, all seven Child-3 benchmark
   surfaces, comparator classification, independent reviews, finding
   disposition and dual terminal verification.
6. Close this package, then the campaign coordinator, and archive their active
   prompts only after every required result is direct exact-byte PASS evidence.

## Exit Criteria

- the actual direct scheduler consumes repository-projected forcing and owner
  inputs through the strict V9 shadow attachment at the frozen seam;
- absent attachment is byte-for-byte and behaviorally identical to the intake
  production path;
- present attachment mutates no production frame/output and atomically replaces
  the complete shadow owner set only after 48 accepted intervals;
- owner identity, exact-one water/energy/material custody, shared soil-thermal
  aggregation, restart, phase rollback and unsupported domains have direct
  positive and poison evidence;
- representative repository scenarios and all seven corrected-litter benchmark
  surfaces have nonzero passing evidence with performance disposition;
- affected formatting, warnings-denied Clippy, authority/anti-evasion checks,
  docs/link checks, cargo-deny, doctests and clean full-workspace correctness
  pass on exact terminal bytes;
- independent land-surface, hydrology/ownership and Rust reviews have every
  finding dispositioned, followed by two independent terminal PASS verdicts;
- every changed `.rs` file is counted; 2000+ is WARN with split rationale and
  no nonexempt file reaches 3000 lines;
- no selector, default, production mutation, publication, snow handoff,
  calibration, empirical-validation, deployment, PR, branch or push claim is
  introduced.

## Delegation

Subagent authorization: this package explicitly authorizes and requires
land-surface, hydrology/ownership and Rust reviewers, one comparator runner and
two terminal verifiers. Reviewers/verifiers are read-only; comparator writes
are limited to ignored logs and package gate artifacts.

## Outcome

Exact implementation commit `3ea08d81d966ccbf163ee64377aa741308e2665a`
passed three independent reviews, the 20/20 comparator, exact-head workspace
correctness and both terminal verifiers through evidence commit
`b327f9eabdc3bc061aab6d5496aaf7496762eade`. No production activation,
selector, default, publication or output change is included.
