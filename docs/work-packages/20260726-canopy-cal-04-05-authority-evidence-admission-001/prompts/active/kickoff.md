# Kickoff: CANOPY-CAL-04/05 Authority Evidence Admission

Scope: local repository scientific-evidence admission plus authorized external
source discovery.

Execution mode: package-end-to-end.

Required reading: root `AGENTS.md`, `docs/work-packages/AGENTS.md`,
`docs/codex_exec_plans.md`,
`docs/planning/canopy-phenology-assurance-roadmap.md`, this package,
CANOPY-CAL-01 through CANOPY-CAL-03 package outcomes, and CAL-03's
`pre-calibration-protocol.md`, `calibration-holdout-ledger.csv`,
`observation-inventory.csv`, and corpus metadata.

Task: execute the package end-to-end. Search for, assess, retain, and
prospectively admit evidence that can lift the CAL-04 quantitative phenology
timing and prior-design blockers and the CAL-05 site-matched litter-source
composition blocker. Do not fit parameters or change physics.

Operator assistance: if a promising resource is inaccessible because it
requires login, institutional access, purchase, a local archive path,
permission clarification, author/custodian contact, or another action the agent
cannot perform, record the exact resource and access failure, then request the
minimum necessary assistance from the operator. Continue other independent
search work while waiting. Never treat an inaccessible candidate as absent or
admitted.

Subagent authorization: this prompt explicitly authorizes subagent
spawning/delegation to two scientific authority reviewers and two terminal
verifiers for independent source, role, partition, and closure assessment;
expected outputs are two review artifacts and two verification artifacts;
write access is bounded to assigned package-local artifacts.

Autonomy: execute all phases and update artifacts without requesting direction
unless operator action is required for a concrete resource or another declared
hard boundary is reached.

Outputs: exact retained research objects where terms permit, immutable source
and role ledgers, operator-assistance log, CAL-04 partition/prior disposition,
CAL-05 source/stock matching disposition, reviews, verifications, and truthful
`COMPLETE` or `HOLD` final disposition.
