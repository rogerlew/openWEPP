# Operator Assistance Log

Status: `QUEUED / NO REQUESTS`

For each request, record the timestamp, candidate ID, exact resource and
persistent identifier, expected authority value, access block, checks already
performed, minimum requested operator action, operator response, supplied
object provenance, and final disposition.

Do not record credentials, private access links, or tokens.
