# Final Disposition

Status: `IN PROGRESS / historical custody HOLD lifted / independent review remediation active`

The historical custody HOLD below remains immutable evidence of the dependency
that blocked the first execution attempt. Commit `a7d692da4` lifted that
dependency, and the existing Child-3 package resumed. This artifact is now an
in-progress terminal placeholder while the findings from the exact-byte review
of `dfc7cf971` are remediated.

The exact hold and rejected aliases are documented in
`real-hydrology-surface-liquid-hold-audit.md` and independently confirmed in
`review_real_hydrology_surface_liquid_hold.md`. A bare-mineral-soil-only
release is prohibited by the campaign objective, so no runtime completion,
benchmark, terminal verification or real-consumer claim is made.

The historical first lift action was completed by the persistent,
restart-serialized hydrology-owned per-OFE/tile liquid state. No terminal
Child-3 disposition is claimed until strict input projection, final E04 ingress,
complete multi-tile execution, independent full-column energy reconstruction,
actual-byte rollback, oracle binding, fresh reviews, benchmarks, heavy gates
and two terminal verifiers pass.

Production selectors, defaults, execution, state and outputs remain unchanged.
No activation, cutover, publication, calibration or empirical-validation claim
is included.
