# Child-3 Resume Intake

Evidence class: `Static`

Status: `dependency lifted / implementation resumed`

The historical Child-3 HOLD at
`af9a989063aa8751dfadb14c442e1b360653658c` remains immutable evidence. Its
named blocker is terminally lifted by dependency package
`20260814-persistent-snow-free-surface-liquid-hydrology-custody-001` at commit
`a7d692da4`, after dual terminal PASS and byte-preserving prompt archival.

The resumed implementation remains inside this existing package. Canonical
authority is `SC-LANDSURFACEENERGY-001@3`, `SC-SURFACELIQUID-001@6`, the V8
model definition and the already frozen Child-1 oracle vectors. No new science
authority or model identity is required.

The first resumed increment will:

1. expose a typed covered-forest orchestrator boundary over the existing exact
   covered potential and capped solvers;
2. combine root-layer and litter/surface-liquid requests in one immutable real
   hydrology snapshot and one authorization;
3. finalize the covered solve from beginning state under fixed caps;
4. bind signed constitutive condensation credit and post-solve ingress to the
   persistent custody owner;
5. retain exact water, energy, soil-thermal, rollback and production-frame
   identities; and
6. prove the path with the frozen nondegenerate multirank forest-litter fixture.

This is implementation intent. It does not yet close the forest endpoint,
Child 3, Child 4, the coordinator, any benchmark, or any production consumer.
Production selectors, defaults, runner dispatch, outputs, calibration,
activation and cutover remain excluded.
