# Implementation And Test Evidence

Status: `bounded checkpoint PASS / forest real-owner endpoint HOLD`

Evidence class: `Ran + Static`

The new dependency-light `openwepp-land-surface-energy` crate implements strict
configuration, state, forcing and identity surfaces; exact open and covered
surface physics; the arbitrary-rank shared canopy-air/reciprocal-longwave
solver; potential and fixed-cap reconstruction from immutable beginnings;
typed root/ground water protocol; LSE and soil-thermal candidate operands;
energy, latent, ground-heat and advection reconstruction; diagnostics; state
digests; and five-owner rollback hashes.

The default-off orchestrator bridge batches V8-root and bare-ground top-layer
requests against one cloned production `DirectRunFrame`, calls the actual
production layer-withdrawal authorization once, rebuilds the final surface from
beginning state and debits finalized use only in the cloned candidate. The
original production frame remains byte/value identical. The active-cap branch
uses the exact typed authorization as final owner use; the physical local rate
and energy operands remain solver-derived.

Ran on the final bounded bytes:

- LSE runtime: 26/26 PASS;
- LSE-real-hydrology integration: 5/5 PASS;
- preserved Child-2 real-owner integration: 3/3 PASS;
- orchestrator quick suite: 507/507 PASS in delegated evidence;
- affected checks and strict Clippy: PASS;
- LSE authority: 7/7 PASS;
- formatting, diff hygiene and package Markdown: PASS.

Typed negative coverage rejects wrong/duplicate identity, frozen sources,
stale state, unsupported litter/surface sources, unsupported condensation and
selector reachability. These results do not turn the soil-layer path into the
required forest-litter endpoint.
