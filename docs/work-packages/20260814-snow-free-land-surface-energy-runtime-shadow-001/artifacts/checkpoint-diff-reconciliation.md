# Checkpoint Diff Reconciliation

## Historical Resumed Checkpoint — 2026-08-14

Status: `resumed increment reconciled / Child 3 remains executing`

Evidence class: `Static`

The checkpoint diff contains:

- workspace registration and lockfile entries for
  `openwepp-land-surface-energy`;
- the dependency-light LSE runtime crate;
- a default-off orchestrator bridge and narrow read-only Child-2 accessors;
- one focused integration target and its root manifest registration; and
- Child-3 and campaign lifecycle/evidence updates.

The diff contains no production runner, selector, scheduler call site, default,
output publication, V7 model definition or completed V7 package mutation.
Child 4 was not scaffolded because its real-consumer work is blocked on the
complete Child-3 forest endpoint.

The historical checkpoint matched its bounded claim. The resumed delta adds a
typed covered forest wrapper, distinct vegetation-root owner identity and one
frozen two-rank real-owner integration fixture. Persistent surface/litter liquid
is no longer unsupported at this boundary: litter withdrawal, post-solve
ingress and the signed-credit-capable owner protocol are connected to the
released custody owner. The new covered fixture is evaporative, so a positive
covered-solver condensation vector remains pending.

No caller fabricates companion physics inside the wrapper: other configured
tile requests, finalized uses and receiving candidates are explicit caller
operands and are validated by the unified envelope. Persistent V8 vegetation
state, final carbon operands, BGC receipts and the heterogeneous five-owner
atomic shadow remain later Child-3 work. The active kickoff prompt stays active;
no terminal benchmark, verifier or completion claim is made.

## Current Review-Remediation Reconciliation — 2026-08-15

Status: `executing / active diff reconciled / line-count closure blocked`

Evidence class: `Static`

Snapshot: `2026-08-15T19:31:28-07:00`, committed HEAD
`dfc7cf971284d772246f147382f4bb8a2292ee4c`, plus the live worktree. The
dependency-lift baseline for the resumed Child-3 delta is `a7d692da4`. The
historical checkpoint statements above remain evidence for their dated bytes;
the following section supersedes them only as a description of the active
remediation diff.

The active delta now includes the complete V8 uncommitted vegetation owner,
not a later placeholder. It contains strict V8 state/configuration projection,
persistent C/N processing, candidate/receipt construction, covered E04 output,
whole-column LSE projection, actual owner/envelope binding, and the default-off
covered transaction. Review remediation adds derived-ingress,
physical-input-projection, and rollback modules and extends the covered solver,
transaction outputs, vegetation persistent/candidate surfaces, and focused
covered-forest tests. The package artifacts preserve the independent review
findings and their accepted correction routes.

The complete active ownership groups are:

- workspace/crate dependency bindings;
- `crates/openwepp-land-surface-energy/**`;
- Child-3-owned
  `crates/openwepp-hillslope-orchestrator/src/land_surface_energy_shadow/**`
  adapter modules, with closed surface-custody modules in that directory
  remaining dependency-owned;
- the narrow orchestrator vegetation/hydrology adapter and export;
- the V8-only vegetation model registry, configuration, state, canonical
  digest, C/N phase, persistence, candidate, and receipt/projection additions;
- the Child-3 integration target and all its named submodules; and
- this package plus its campaign/catalog lifecycle rows.

No active path changes a production selector, default, runner dispatch,
scheduler call site, published output, state commit API, deployment, or real
consumer cutover. The endpoint remains an uncommitted, default-off library
shadow while review remediation is active.

Exact current line-count governance for every Rust/test file touched by either
the retained checkpoint or resumed Child-3 delta and at or above 2,000 lines
is:

| File | Lines | Result |
|---|---:|---|
| `crates/openwepp-land-surface-energy/src/solver.rs` | 3,204 | `BLOCKED`: mandatory decomposition before closure |
| `crates/openwepp-hillslope-orchestrator/src/land_surface_energy_shadow/mod.rs` | 2,964 | `WARN` |
| `tests/integration/land_surface_energy_real_hydrology_shadow_contract.rs` | 2,757 | `WARN` |
| `crates/openwepp-land-surface-energy/src/transaction.rs` | 2,677 | `WARN` |
| `crates/openwepp-hillslope-orchestrator/src/vegetation_real_hydrology_shadow.rs` | 2,157 | `WARN`; only the narrow Child-3 adapter/accessor delta is owned here |
| `crates/openwepp-vegetation/src/transaction.rs` | 2,082 | `WARN` |

The new active modules are independently visible in `owned-file-manifest.md`;
none is folded into an unreported monolith. Because `solver.rs` is currently
above the 3,000-line hard stop, this reconciliation cannot support terminal
closure, benchmark, heavy-gate, or verifier claims. After the accepted review
findings are corrected, the solver is decomposed, and the worktree reaches
stable bytes, replace this executing snapshot with a fresh exact terminal diff
and line-count reconciliation.
