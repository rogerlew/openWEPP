# Worker Handoff

Resume Child 3 after the hydrology owner exposes the persistent forest-floor
liquid and condensation-credit surfaces named in the HOLD audit.

Retain without reinterpretation:

- the exact LSE constitutive and transaction crate;
- mixed soil-layer real-owner arbitration;
- immutable-beginning potential/final ordering;
- exact active-cap `F=A` branch identity;
- typed unsupported litter/surface and condensation boundaries; and
- all failed and passing gate history.

The first resumed test must replace the typed litter/condensation rejection with
an actual owner snapshot/candidate path, while preserving the bare-ground/root
competition tests and production byte invariance. Do not synthesize a sidecar
inventory, alias residue input, or debit a depression diagnostic.
