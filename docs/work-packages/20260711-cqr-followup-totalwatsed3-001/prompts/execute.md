# FQ-04 execution prompt

Execution mode: package-end-to-end

Autonomy: execute the package through its terminal commit without pausing for
ordinary implementation choices. Preserve exact accepted behavior, error
priority, floating grouping, and output identity.

Execute
`docs/work-packages/20260711-cqr-followup-totalwatsed3-001/package.md`
end-to-end. Close the science-tier characterization safety net before any
production decomposition. Independently reconstruct conservation-sensitive
water and sediment output from source rows whose wrong columns, areas, keys,
or OFE scopes produce different answers. Decompose only after coverage passes,
then require every eligible CRAP row at most 30, full gates, dual independent
review with finding disposition, dual verification, and a terminal commit.

This prompt explicitly authorizes subagent spawning/delegation for bounded
coverage implementation, independent lineage/review/verification, and heavy
gate execution. Each delegated writer is limited to its assigned portion of
the package write set.
