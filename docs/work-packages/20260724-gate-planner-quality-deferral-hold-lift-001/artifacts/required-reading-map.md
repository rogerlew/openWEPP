# Required Reading Map

Evidence class: Static / Ran.

Core:

- `AGENTS.md` — repository governance.
- `docs/work-packages/AGENTS.md` — package and heavy-gate lifecycle.
- package-local `package.md` — correction authority envelope.
- retained Order-3 attempt-3 blocker — exact seven-test evidence.
- `docs/work-packages/20260724-testgate-quality-deferral-001/package.md` —
  predecessor objective and protected quality-deferral behavior.
- `docs/decisions/0041-separate-testgate-from-observational-quality-ci.md` —
  canonical quality separation.
- `docs/standards/testing-and-gate-strategy.md` §7.4, §8.5, and §12 — intent,
  pre-heavy, retry, and closure authority.

Conditional, triggered:

- `crates/AGENTS.md` — Rust crate edits.

On-demand:

- `gate-policy/v1/schemas/*.json`
- `crates/openwepp-gate-planner/src/{executor,executor_coverage_tests,planner,pre_heavy,verifier}.rs`
- adjacent fixture helpers reached by the exact seven tests.

The applicable instruction chain was enumerated with
`tools/agents/find-agents --for` before scaffold edits.

Core bytes: `157585`. Conditional bytes: `5165`. Total required bytes:
`162750`. Disposition: `OK`.
