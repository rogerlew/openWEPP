# Required Reading Map

Static: before edits, the worker read the applicable instruction chain reported by `tools/agents/find-agents`: root `AGENTS.md`, `crates/AGENTS.md`, and `docs/work-packages/AGENTS.md`.

Static: the worker also read this package, the B03 master ExecPlan, `testing-and-gate-strategy.md`, the mechanical-refactor and code-quality-refactor authoring guides, ADR-0021, `main.rs`, and the adjacent package-validation authority implementation needed to construct characterization fixtures.
