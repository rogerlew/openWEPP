# Assurance Editorial Fast Path Terminal Verification A

Status: **PASS — independent terminal engineering verification**

Date: 2026-07-16 UTC

Evidence class: **Static + Ran (proportional current-tree and retained-evidence
integrity checks)**

## Scope And Boundary

This verifier inspected the terminal implementation identity, both independent
re-review histories, finding dispositions, focused and heavy gate records,
fresh adjudicated-CRAP bundle, line-count governance, protected report source,
roadmap and work-package catalog state, and required pre-terminal artifacts.

The verifier did not rerun the ten-minute full workspace suite or the
forty-six-minute coverage acquisition. Conclusions about those expensive gates
come from their retained status files, logs, checksum-bound structured
evidence, and source manifests. Proportional current-tree execution covered
formatting, documentation, the real normalization check, selected validation,
protected-byte identity, and diff hygiene.

This is an engineering verification. It does not grant scientific review,
publication approval, release-owner approval, or release authority.

## Terminal Candidate And Independent Review

Ran: current
`crates/openwepp-assurance/src/v2/normalization.rs` hashes to
`eb4f51a0f2258ca32c819960db98f07f1adf2523e224b9a170e9372a2ecbd57b`,
exactly matching the final heavy runner and both CRAP-remediation re-reviews.

Static: Review A and Review B independently recommend **PASS** on that current
hash. Both artifacts truthfully state that the prior untracked file bytes were
unavailable and therefore do not claim a literal pre/post decomposition diff;
each instead records a full-current static review plus focused current-hash
execution. Their accepted findings, including the converter communication and
non-UTF-8 fixture races, are closed rather than waived.

## Heavy Closure And CRAP Integrity

The retained final heavy status files all record exit code zero for format,
workspace/all-target Clippy, full Nextest, dependency policy, fresh CRAP,
normalization check, selected validation, and diff hygiene. The terminal
Nextest log identifies run `d2fa2208-86ad-4a98-91e7-fb2f0ed0aa9f` and records
2,063 tests run, 2,063 passed, three skipped, and zero failures.

Ran: all 16 entries in the remediation CRAP checksum manifest verified. The
before, after, and final production-source manifests compare byte-for-byte and
share SHA-256
`72d2fa3d449fc492a05818daa680b548d2aa6bb14b6c0428ab8af6b0e16873ae`.
`run-status.json` records fresh acquisition, exit zero, and `PASS`.

Ran: direct queries of the checksum-bound JSON confirm closure eligibility,
debt status `PASS`, two raw rows, two current adjudications, zero actionable
rows, zero invalid adjudications, and seven touched production files. Direct
queries of the raw measurement data confirm:

| Scope | Maximum CRAP | Function |
| --- | ---: | --- |
| All touched production files | `30.0` | `publish_selected` |
| `v2/normalization.rs` | `15.101256515775034` | `prepare_normalization` |

The preserved first acquisition remains a real failure at four raw, two
adjudicated, and two actionable rows. The package decomposed both actionable
normalization functions and performed a new fresh acquisition; it did not
waive or overwrite the failure.

## Findings, Governance, And Non-Deferral

`finding-disposition.md` lists every Review A, Review B, heavy-CRAP, and
re-review finding as accepted and resolved. It rejects none and assigns none to
deferred or follow-up work. The disposition values agree with the final review
artifacts and fresh metric evidence.

The required reading map, focused gate results, both independent reviews,
finding disposition, heavy-gate record, and line-count governance artifact are
present and substantive. This file supplies terminal verification A.
`terminal-verification-b.md` is the contemporaneous parallel decision and was
not an input to verifier A. `final-disposition.md` must consume both terminal
decisions. That sequenced closeout is an administrative obligation, not
deferred technical gate evidence supporting this PASS.

No current-scope technical gate is failed, blocked, or represented as a later-
package obligation. The only 2,000-line warning, `v2.rs` at 2,841 lines, has an
owner, coherent split intent, and blocking 3,000-line sunset; no touched Rust
file reaches the closure block.

## Protected Report Identity

Ran: a combined path-and-content hash over the complete protected report tree,
`assurance/v2/catalog.yaml`, and `assurance/v2/principals.yaml` was
`3a8e1d3bde52072da617a931825d29c19b518564232dba9e4aa94c2d2164a3c8`
before and after the real normalization check. Git reports no protected report,
catalog, principals, or `usersum` change.

The real check returned `changed=false`, no changed paths, and identical old
and new selected source roots
`08e2b5e3b6444067db7204f790a6670af2d6f16bf1b733879cbc3e95d235dfa6`.
Selected validation passed with one version `1.0.0` report at lifecycle
`DRAFT`, `fixture_only=false`, and the same selected source root. The package
therefore did not mutate protected report bytes while introducing the
normalization subsystem.

## Roadmap, Catalog, And Documentation

Static + Ran: the package ID and editorial-fast-path queue wording are absent
from `docs/ROADMAP.md`, and that file has no final diff. The work-package
catalog contains one discoverable `EXECUTED-COMPLETE` entry that records the
transactional DRAFT normalizer, 65-test focused profile, corrected review and
CRAP history, 2,063/2,063 terminal tests, and final CRAP maxima.

The American-English preview of that new catalog entry is empty. A preview of
the complete historical `docs/work-packages/README.md` still proposes unrelated
changes to older unit spellings, compound-word forms, and legacy `CoE`
identifiers. Those preexisting historical and identifier changes were not
applied; they do not affect the new entry's American English.

Ran:

- `cargo fmt --check` passed.
- `git diff --check` passed.
- `markdown-doc lint` and `markdown-doc validate` passed with zero errors or
  warnings for each changed implementation/governance document, the work-
  package catalog, and all 15 Markdown files in the final package tree.

## Verdict

**PASS.** Terminal verifier A found no implementation, evidence-integrity,
finding-disposition, documentation, protected-byte, or non-deferral blocker on
the final candidate. Package completion still requires the independent
terminal-B result and a truthful final disposition that consumes both terminal
decisions.
