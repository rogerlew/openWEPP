# Execute SNOW-SURFACE-EB-04

Work only in `/home/workdir/openWEPP`. Execute the package end to end from
`package.md`. Preserve the frozen protocol and the contract-first authority
already implemented by EB-02/03/03A. Do not change production physics,
coefficients, forcing, fixtures, observations, defaults, or public schemas.

Run the one fixed `B/L/S/LS` matrix, reconstruct physical ledgers before
performance scoring, generate plot-only SVG figures with complete Markdown
sidecars, apply the stop-loss, and complete direct validation, dual independent
review/finding disposition, dual verification, roadmap/catalog updates, and
truthful final disposition.

Subagent authorization: this prompt explicitly authorizes subagent
spawning/delegation to two read-only science/code reviewers and two terminal
verification agents with package-artifact-only write access for their assigned
outputs.
