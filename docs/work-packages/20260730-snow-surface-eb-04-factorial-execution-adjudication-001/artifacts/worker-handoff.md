# Worker Handoff

Status: `complete`

Evidence mode: `Static + Ran`

The next campaign action is bounded EB-05 negative-result assurance closeout.
Do not promote, activate, tune, rescale forcing, alter observations, or rerun
the EB-04 factorial.

If maintainers want the exact conductivity-path cause, scaffold a separate
defect package whose first action is to preserve the underlying
`MeteorologyError` variant and its actual rejected operand at the hydrology
boundary. That package may reproduce a minimal failing state, but it must not
be represented as another EB-04 calibration round or permission to weaken the
typed guard.
