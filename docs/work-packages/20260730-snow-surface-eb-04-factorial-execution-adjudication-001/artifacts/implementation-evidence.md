# Implementation Evidence

Status: `implemented / evidence hold`

Evidence mode: `Static + Ran`

- Added nine already-computed Stage 3 diagnostic fields to the opt-in JSONL
  trace; no public WAT schema or process calculation changed.
- Authored a deterministic package-local runner that freezes 12 lanes, four
  cells, non-target selectors, thresholds, operators, and stop-loss.
- The runner hashes fixture trees, observations, the frozen protocol, and the
  executed binary; runs isolated output directories; independently
  reconstructs the mass ledger; audits producer-carried energy and latent/mass
  residuals; scores only completed independent-validation cells; and refuses
  incomplete factorial contrasts.
- The original dirty-tree source-file hash was not retained. The attempt ledger
  records that limitation rather than claiming exact source identity from the
  clean base commit.
- Retained output lacks shortwave and the signed operands needed for
  independent latent/mass reconstruction, so the anti-tautological evidence
  obligations remain blocked and the package remains on HOLD.
- Generated seven SVG plots and one Markdown sidecar per figure. Failure-aware
  plots explicitly distinguish partial from completed trajectories.
