# HPHYS0219 Worker Handoff

Status: completed
Evidence mode: Static + Ran

## Completed in HPHYS0219
- WB19 threshold coefficient-family authority corrected from `cpm_####` to
  `coca_####` in canonical contracts and production runtime/kernel surfaces.
- Contract-derived WB19 `coca` threshold tests added and passing.
- Workspace gates pass (`fmt`, `clippy`, `test`, `deny`).
- Fresh `unpalatable-rind` 39-hillslope rerun + semantic reports completed.

## Residual posture
- `Dp`: fail-saturated (`39/39`), mean improved vs HPHYS0218.
- `latqcc`: fail-saturated (`39/39`), mean regressed vs HPHYS0218.
- `Total-Soil` / `SoilWaterTotal`: fail-saturated (`39/39`), mean regressed
  vs HPHYS0218.
- `ProfileFCStore`: unchanged control.
- Integrated disposition remains `HOLD`.

## Immediate next package recommendation
- **HPHYS0220** (follow-on):
  1. Add WB19 lineage diagnostics for per-day/per-layer threshold branching and
     realized withdrawal components (`drfc`, `D`, `q`, `Qdd`, `Qd`,
     `wb11_drainable_storage`) on the same 39-hillslope lane.
  2. Contract-authorize and implement minimal corrective changes that preserve
     `Dp` gains while recovering `latqcc`/total-soil residual direction.
  3. Re-run the 39-hillslope semantic lane and compare against HPHYS0217/0218/0219
     to classify monotonic or tradeoff behavior.
  4. Keep integrated disposition `HOLD` unless fail saturation materially
     reduces or a documented hold-lift acceptance rule is satisfied.
