# Execute SNOW-SURFACE-EB-04U

Scope: local repository retained-evidence science-design task; flat-file
reads/edits only; no external systems or network actions are required.

Execution mode: package-end-to-end.

Autonomy: execute every phase in `package.md` sequentially through disposition
without requesting user intervention unless a declared hard boundary is proven.

## Required Reading

Core: read every Core entry in `artifacts/required-reading-map.md` before edits.

Conditional: read the exact canonical contract and contract-authoring guidance
only when the mechanism/operator claim triggers it, as mapped in the artifact.

On-demand: read prior retained traces, manifests, and source/schema locations
only when needed to prove operand availability or lineage.

Required-reading budget: `234817` local bytes, disposition `OK`; map:
`artifacts/required-reading-map.md`.

## Task

Execute EB-04U end-to-end within its declared write set. Reconstruct the exact
16-row population; freeze the `9/2/5` cohort partition; define seasonal phases,
mechanism-discriminating operators, operand lineage and availability, evidence
roles, materiality/noninferiority/interaction semantics, and EB-04V/04W/04X
admission gates. Produce accessible figures with Markdown sidecars.

Use canonical `SC-*` authority for process claims and ADR-0042 for science,
data, readiness, and identifiability status. Do not substitute surrogate,
provisional, proxy, or heuristic production physics. EB-04U cannot edit
production, tests, contracts, fixtures, observations, prior packages, retained
outputs, selectors, or defaults; run the model; fit parameters; rescale forcing;
inspect new candidate results; or issue a promotion decision.

Select and run validation directly under
`docs/standards/testing-and-gate-strategy.md`. Record pre-execution intent and
reconcile the exact terminal diff. Every current-scope gate needs direct
evidence; otherwise hold with the named blocker.

Subagent requirement: REQUIRED for two independent science/governance reviews
and two terminal verifications. This prompt explicitly authorizes subagent
spawning/delegation to those roles for package-local bounded review and
verification artifacts; write access is limited to their assigned artifact.
No comparator or heavy-suite subagent is selected.
