# Kickoff: RTR-029 READY-Audit Fixture Closure

Scope: local repository engineering work; flat-file reads/edits only; no
external systems or network actions.

Execution mode: package-end-to-end.

Read `package.md` and its required-reading map. Close RTR-029 by replacing the
ambient-current-diff verifier fixture with an isolated, self-owned package
authority fixture. Preserve every production byte and READY-audit/receipt
semantic. Run focused validation, obtain dual review and verification, close
the durable defect only after the exact correction commit, and resume rank 7.

Subagent requirement: no heavy gate is selected. This prompt explicitly
authorizes subagent spawning/delegation to two read-only implementation
reviewers and two read-only terminal verifiers; outputs are compact package
artifacts.

Autonomy: execute all phases through disposition unless a declared hard
boundary is proven.
