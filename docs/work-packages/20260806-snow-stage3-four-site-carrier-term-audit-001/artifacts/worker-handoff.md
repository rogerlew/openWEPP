# Worker Handoff

Status: `persistent shadow blocked; turbulent reconciliation next`

Next package:
`SNOW-STAGE3-TURBULENT-CARRIER-LINEAGE-AND-OPERATOR-RECONCILIATION`.

Required intake:

- exact v2 result, receipt, tables, and retained manifest under
  `target/snow_stage3_four_site_carrier_term_audit_v2/`;
- the predecessor sequential Snowbird carrier result and its exact source-state
  semantics;
- v128 same-state and sequential operator authority;
- the bound `z_T`, `z_q`, `z_u`, and `z_0` contract definitions; and
- turbulent flux and latent/mass lineage in the extracted Stage 3 solver.

Required result-blind design:

1. select byte-identical daily snapshots and hourly forcing for both operators;
2. export and reconcile every external carrier term, especially longwave,
   sensible, and latent, plus surface temperature, air/dewpoint/vapor state,
   wind, stability, exchange coefficients, geometry, vapor mass, and sign
   convention;
3. separate operator state evolution from term-generation math;
4. prospectively freeze plausibility and identity checks before results; and
5. stop without persistence, fitting, correction, promotion, or cutover.

CoE remains authoritative. Terminal meltout and land-surface energy remain
separate programs and cannot advance snow cutover past this upstream blocker.
