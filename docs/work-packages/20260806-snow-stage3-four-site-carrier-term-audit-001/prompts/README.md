# Prompt Lifecycle

The kickoff prompt starts in `active/` and moves byte-identically to
`archived/` when execution begins. This package authorizes the bounded delegates
named in the prompt and package only.
