# Prompt Lifecycle

The kickoff prompt moved byte-identically from `active/` to `archived/` after
result execution. This package authorized only the bounded delegates named in
the prompt and package.
