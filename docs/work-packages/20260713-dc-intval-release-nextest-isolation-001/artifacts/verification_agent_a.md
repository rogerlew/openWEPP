# Independent Verification A

Status: `PASS-HOLD-VERIFICATION`

Evidence class: **Ran** for lightweight hash, Git-object, row-count, Markdown,
and diff-integrity checks; **Static** for evidence classification, archived-log
reconciliation, protected-boundary, successor, roadmap, catalog, and restart
inspection. No heavy gate was rerun.

## Terminal verdict

Verdict: `PASS`

`HOLD-INTVAL-REL-001` is the correct terminal package result. The archived
exact release run proves the nextest isolation correction across the selected
full workspace, then fails on a real protected required-authority provenance
defect. No false release or integrated-validation PASS is present, blocked
gates are classified correctly, and the bounded successor/restart chain is
precise.

Independent verification A certifies that the accepted focused-evidence and
successor-precision findings are fully fixed.

## Accepted-finding verification

### Focused evidence: verified

`preimplementation-guard.md` correctly classifies pre-fix evidence as
`Static`, identifies the unarchived interactive run as reported only, and
binds terminal post-fix proof to the archived exact release log.
`focused-results.md` correctly labels the focused result `SUPPORTING-PASS`,
states that interactive raw output was not archived, and makes the archived
full-workspace release log terminal. `implementation-evidence.md`, `intake.md`,
and the package Progress and Outcomes sections now make the same distinction:
static pre/post discrimination, unarchived interactive focused results as
supporting only, and `logs/01-release-candidate.log` as terminal run authority.
The accepted focused-evidence finding is fully disposed.

### Successor precision: verified

The successor removes the contradictory legacy top-level source fields and
requires `schema_version: 1`, one exact per-item Git source commit, and equality
among fixture bytes, lock, provenance hashes, and Git-object bytes. The
accepted successor-precision finding is fully disposed.

## Archived terminal-evidence audit

The archived exact command is the required no-skip release entry point with
both pinned stability paths and both expected-suite counts. Its log/time pair
records exit 1 after 9:43.01 with this exact progression:

| Lane | Verified result |
| --- | --- |
| Workspace check and all-target Clippy | Completed before full tests |
| Full nextest profile | 1,945 selected tests passed, four slow, three skipped |
| Dependency policy | Advisories, bans, licenses, and sources passed |
| Required authority fixture integrity | Failed closed on missing provenance keys and `schema_version` |
| Remaining authority suites | `BLOCKED` by fail-fast integrity failure |
| Binary build/staging/release lint | `BLOCKED` |
| Required stability cohorts | `BLOCKED` |

The full profile selects `all()`, the source guard and three named H2637 tests
are not ignored, and the former environment collision does not recur. The log
does not establish the unarchived focused 1/1 or 3/3 command claims, and the
corrected dedicated evidence records no longer use it that way.

The separately required final formatting, Clippy, full-nextest, deny,
Markdown, and diff loop is correctly `BLOCKED`, not satisfied by partial lanes
inside the failed release invocation.

## HOLD, boundary, and successor verification

- The authority validator identifies suite
  `cas_l4_subhyd_watyld_fcwp_consistency_001`, its locked fixture, missing
  per-item provenance, and missing top-level schema version. Static inspection
  confirms the missing per-item fields are `source_repo` and `source_commit`.
- The provenance file, fixture, lock, suite/registry posture, validator,
  thresholds, production behavior, and skip behavior are protected by this
  package. Retrying, bypassing, or editing them here would violate acceptance.
- `INTVAL-AUTH-PROV-001` is a finite metadata-only successor. It requires
  `schema_version: 1`, removes contradictory legacy top-level source claims,
  requires the exact per-item 40-hex Git commit, and requires equality among
  fixture bytes, lock, provenance hashes, and Git-object bytes.
- Commit `9aa4c3d61549ab30da665a4dc109bab811522fe9` exists, is an ancestor of the
  intake commit, and contains fixture bytes hashing to
  `a1c50a82cd1e497875cb034481c4b2ef710c319907480b0f584fde30f48fae5e`,
  exactly matching the current fixture and lock.
- `/workdir/wepp-forest` remains at the pinned commit. The cohort and watchlist
  remain 1,167 and 20 lines respectively, matching headers plus the expected
  1,166 and 19 records.
- The successor requires the exact release command to restart from its
  beginning and pass all release/stability and final gates before integrated
  validation restarts from Phase 0. No partial result is reusable.

## Disposition consistency

Package status, gate results, HOLD audit, disposition, worker handoff, roadmap,
and work-package catalog consistently identify `INTVAL-AUTH-PROV-001` as the
active blocker. They claim neither terminal release PASS nor integrated-
validation PASS. The catalog records `INTVAL-REL-001` as executed-HOLD and the
successor as queued; the roadmap retains the successor-then-release-then-full-
restart sequence.

Scoped Markdown and diff-integrity checks pass. No verification defect remains.
