# Independent Verification B

Evidence class: **Ran** for lightweight hash, Git-object, repository-identity,
Markdown, and diff checks; **Static** for finding disposition, successor,
protected-boundary, HOLD, and restart inspection. No heavy gate was rerun.

Status: `PASS-HOLD-VERIFICATION`

Verdict: `PASS`

The package's `HOLD-INTVAL-REL-001` result remains correct and no false release
or integrated-validation PASS is present. INTVAL-REL-B-01 and
INTVAL-REL-B-02 are fully disposed.

## INTVAL-REL-B-01 verification: verified

The new `preimplementation-guard.md` correctly labels the pre-fix red gate
`Static`, records that the interactive output was not archived, and makes the
archived ID 01 full-workspace run the terminal post-correction evidence. The
new `focused-results.md` likewise calls the focused 1/1 and 3/3 reports
supporting only and binds terminal correction evidence to the 1,945/1,945 ID 01
run. `implementation-evidence.md` makes the same distinction. `intake.md` now
explicitly separates archived pinned/release output from unarchived supporting
focused reports, and the package Progress and Outcomes identify static
discrimination plus ID 01's 1,945/1,945 full-workspace result as terminal run
authority. No record presents the unarchived focused counts as terminal `Ran`
evidence.

## INTVAL-REL-B-02 verification: verified

The queued `INTVAL-AUTH-PROV-001` package now requires all of the requested
corrections and proofs:

- add exactly `schema_version: 1`;
- remove the obsolete top-level `source_repo` and
  `source_commit: working-tree-hphys0256` when authority moves to the item;
- bind the item to `/workdir/openWEPP` and exact 40-hex commit
  `9aa4c3d61549ab30da665a4dc109bab811522fe9`;
- require equality among fixture bytes, lock SHA-256, per-item fixture/source
  SHA-256, and Git-object bytes; and
- protect fixture bytes, lock, suite/registry posture, validator, thresholds,
  production code, and skip behavior.

Independent lightweight reconstruction gives the same SHA-256 for every
current operand:

```text
a1c50a82cd1e497875cb034481c4b2ef710c319907480b0f584fde30f48fae5e
```

That value matches the fixture file, lock entry, existing per-item `sha256`,
existing per-item `source_sha256`, and the fixture path read from the pinned Git
object. Git recognizes the pinned 40-character identifier as a commit. The
successor's source guard and exact no-skip release/final-gate acceptance are
therefore coherent and test the substantive provenance correction rather than
mere field presence. This verification does not claim the queued correction is
already implemented.

## HOLD and restart verification

ID 01 remains a real exit-1 required release command. Required authority work
after fixture integrity, binary/staging/sidecars, release lint, stability, and
separate final closure gates remain blocked. The 1,945/1,945 workspace result
proves the isolation correction but cannot supply terminal release acceptance.
The authority provenance file is protected in this package, making the bounded
successor a legitimate HOLD boundary.

The restart rule remains exact: close `INTVAL-AUTH-PROV-001`, rerun the exact
pinned-input release command from the beginning, and only after release
acceptance restart integrated validation from Phase 0 at the correction commit.
No result from this attempt may carry terminal closure.
