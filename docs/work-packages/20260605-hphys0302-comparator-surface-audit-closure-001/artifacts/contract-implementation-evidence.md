# Contract Implementation Evidence

Status: complete

Evidence mode: Static

Static:

- `SC-SNOWFREEZE-001` bumped to version `36` and added
  `INV-SNOWFREEZE-033`.
- `SC-WATBAL-001` bumped to version `125` and added `INV-WATBAL-077`.
- `docs/specifications/science-contracts/index.md` added the HPHYS0302
  registry note binding the comparator-surface gate to both invariants.
- Contract authority requires same physical quantity in same units before a
  producer-defect conclusion.
- `RM`/`Snow-Water` are daily output/publication surfaces; raw `hrmlt` and
  post-raw `wmelt` are aggregate cut-point surfaces only.
- Term-level melt producer edits require paired baseline/openWEPP term-state
  surfaces for `amelt`, `bmelt`, `cmelt`, `dmelt`, `hrrain`, `hrtemp`, `tdpt`,
  `hrad`, `cloudC`, `vwind`, `snodpt`, and `densgt`.

Ran:

- Not applicable; this artifact records static contract authoring evidence.
