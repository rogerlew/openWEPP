# Worker Handoff

Evidence mode: Static + Ran.

SNOWFROST-FIDELITY-A is complete as a characterization package. All five pilot
sites now run through the direct observed harness and emit metric-bearing
reports, but no site is eligible for frost-model defect attribution because the
modeled snow-depth diagnostic required by `TOL-SNOWFREEZE-009` is absent.

Next actionable package:

1. Add a modeled snow-depth diagnostic/publication surface sufficient for
   `TOL-SNOWFREEZE-009`.
2. Rerun SNOWFROST-FIDELITY-A classification.
3. Only after snow-control is evaluable, proceed with no-`Qwet` heat-flow
   benchmarks and mechanism discrimination.

Do not use the current residuals to tune heat flow, frozen conductivity, SFCC,
impedance, or migration heat. In particular, do not enable `Qwet` from the dead
legacy block based on these reports.
