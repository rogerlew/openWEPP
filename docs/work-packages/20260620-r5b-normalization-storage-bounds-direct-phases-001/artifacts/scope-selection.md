# Scope Selection

Static:

Selected scope: direct `Normalization` and `StorageBounds` phase ownership.

Deferred:

- decomposition transition;
- residue partition transition;
- annual growth transition;
- perennial growth transition;
- public output cutover.
