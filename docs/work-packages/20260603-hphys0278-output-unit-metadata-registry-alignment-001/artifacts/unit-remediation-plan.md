# Unit Remediation Plan

Status: completed
Evidence mode: static

Static: HPHYS0278 implemented the planned output metadata registry alignment.

Next remediation focus:

1. Continue HPHYS0279 `SC-*` unit compliance linting so contract unit sections
   and alias maps become machine-checkable.
2. Keep publication-only output rows explicit; promote a row to
   boundary-backed only when a canonical runtime boundary symbol exists and the
   output unit matches.
3. Resolve the unrelated SIMIMPL18/PL14S `HKERNEL-WB11-ET-E-003` workspace
   HOLD before claiming full workspace green status.

Ran: not applicable for this artifact.
