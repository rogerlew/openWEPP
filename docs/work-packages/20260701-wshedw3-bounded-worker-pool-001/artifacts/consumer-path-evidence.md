# Consumer-Path Evidence

Status: `QUEUED`

W3 execution must prove the real public runner uses bounded worker-pool
execution for the W3 claim.

Required proof fields:

- Producer source:
- Worker-pool state or frame object:
- Runner handoff:
- Pass inventory handoff:
- Downstream routing/publication consumer:
- Output/API surface:
- Positive evidence:
- Negative proof that old shell-loop/shared-output orchestration is not used:
- What still reads the old path:

Producer-only, test-only, timing-only, or counter-only evidence cannot close the
W3 consumer-path gate.
