# CAL-07E Kickoff

Scope: local repository literature-authority work with flat-file package edits
and bounded public literature retrieval; no production systems are changed.

Execution mode: package-end-to-end.

Read `package.md` and `artifacts/required-reading-map.md`, execute every phase
through disposition, and retain exact source/claim evidence.

Files: this package, the canopy roadmap, and the work-package catalog.

Constraints: distinguish direct-site authority from regional and mechanism
analogues; require full text for claim-bearing sources; preserve ADR-0042
authority boundaries; do not introduce parameters, forcing corrections,
observation equivalences, or process equations.

Subagent requirement: terminal dual review and verification only. This prompt
explicitly authorizes subagent spawning/delegation to two science reviewers
for independent source classification, claim calibration, citation checks,
and closure verification. Outputs are the four named terminal artifacts;
write access is bounded to those artifacts.

Autonomy: execute end-to-end and request user help only for a high-value source
that cannot be obtained through accessible authoritative channels.
