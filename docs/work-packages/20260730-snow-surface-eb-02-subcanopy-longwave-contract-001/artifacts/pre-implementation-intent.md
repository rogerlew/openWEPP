# Pre-Implementation Intent

Status: `frozen before canonical edits`.

Evidence class: Static.

The increment implements canonical equation/interface authority and analytical
test evidence only. It will not edit Rust, tests, selectors, runtime
projection, schemas, fixtures, or outputs. Runtime implementation is a
prospectively declared campaign hold owned by EB-03's shared
snow-temperature/cold-content provider decision.

Expected terminal risk: authority-documentation / integrated-domain.

Selected increment requirements:

- canonical contract/profile/schema completeness;
- equation, units, sign, branch, guard, alias, and test-vector consistency;
- registry, links, Markdown, CSV, SVG, spelling, and diff hygiene;
- dual independent contract review and terminal verification.

Rust, comparator, empirical, external-authority, conservation-runtime, and
real-consumer gates are not selected because the intended write set cannot
alter executable behavior.
