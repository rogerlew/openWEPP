# Execute 21L

Scope: local repository scientific-analysis task; flat-file reads/edits only;
no external connectivity or external-system action.

Execution mode: package-end-to-end (default).

Phase plan: execute all phases in `package.md` sequentially through
disposition.

Required reading:

- Core: `AGENTS.md`, `docs/work-packages/AGENTS.md`, this package,
  `docs/standards/testing-and-gate-strategy.md`,
  `docs/standards/prompt-wording-guidance.md`, the 21K worker handoff,
  `artifacts/analysis-freeze.json`, `artifacts/operand-lineage.md`, and
  `artifacts/required-reading-map.md`.
- Conditional: science-contract and kernel-preparation governance only if an
  out-of-scope authority or production change is proposed; amend scope before
  such work.
- On-demand: exact 21J result tables, 21K receipts/traces, and the two named
  `SC-SNOWFREEZE-001` invariants.

Required-reading budget: `94884` local bytes, `OK`; map:
`artifacts/required-reading-map.md`.

Files: only the package tree, three roadmap/catalog files, and untracked
`target/snow_warm_mixed_prepeak_loss_energy_attribution_v2/`. Retain the first
namespace without alteration as rejected prepublication evidence.

Task: execute corrected-state warm/mixed pre-peak loss and energy attribution
end-to-end under the frozen analysis contract.

Constraints: preserve canonical/development evidence roles; do not mutate
production, contracts, tests, fixtures, or observations; do not interpret
empirical CoE terms as measured energy shares or downstream Stage-3 energy as
upstream causation; do not tune or recommend a correction from association.

Subagent requirement: dual scientific review and dual terminal verification
are required. This prompt explicitly authorizes subagent spawning/delegation to
two read-only scientific reviewers and two read-only terminal verifiers for
compact findings, independent metric reconstruction, evidence citations, and
verdicts. No heavy batch is selected; `comparator_suite_runner` is not required.

Autonomy: execute all package phases and update required artifacts without
requesting user direction unless a declared hard blocker is proven.

Outputs: accepted machine-readable results, source tables, figures and
sidecars, synthesis, reviews, verifications, gate evidence, exact-diff
reconciliation, disposition, archived prompt, and catalog/roadmap closure.
