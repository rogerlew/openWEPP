Static correction review of the revised authority cut.

- **High — receipt frames still omit authoritative payload fields.** The current `run_identity` omits `cycle`, `forcing_projection`, and `source_joins`; both cycles therefore derive the same run identity, and mutations to joins/segments need not change any receipt. Root’s proposed replacement is sound: compute raw `payload_content_sha256 = SHA256(full canonical provider payload)` as a content digest, with no run/receipt field inside the payload, then include that raw32 digest in the framed `run_identity`. Because the payload contains cycle, sources, implementation, segments, records, and joins, this makes run identity cycle-specific and binds those surfaces noncircularly. `calendar_receipt` and parent-specific `forcing_receipt` then inherit that binding transitively. Root may apply this small coordinating correction; it resolves an accepted finding without choosing scientific/source values.

- **High — `rust_symbol` is not an authentic runtime-source mechanism.** “Extracts the single named initialized value” has no defined Rust semantic boundary and cannot represent constructor-computed GSI receipts, topology/configuration objects, controller policy identities, or values assembled from multiple inputs. Source text location plus file hash proves code custody, not the runtime value selected for a support. The author must identify each actual canonical data source or constructor input/output identity and its existing encoding. If unavailable, name the missing field and consumer and retain HOLD.

- **High — current GSI is internally inconsistent.** Lines 3384–3388 call it an accepted endpoint receipt consumed by `advance_m1_caller_support`, while `SourceJoinV1` can only point to JSON or a Rust initializer and stores only scalar `1.0`. The payload must distinguish the frozen expected scalar from the support-specific authenticated GSI receipt and bind that receipt’s support/current identity. A static `1.0` or Rust literal cannot satisfy finding D.

- **High — structured joins lack canonical value schemas.** “Canonical structured value” is undefined for exposure, topology, and controller policy. Each must reference an existing canonical serialization/digest contract or define its exact closed fields, types, ordering, and constructor correspondence. Destination likewise needs the actual configured runtime value, not merely a source literal.

- **Medium — fixture extraction remains underdefined.** Restore the exact projection ID constant and require `cycle.ordinal/id/initial_phase_records` and `segments` to equal the selected pinned `/cycles/<ordinal>` data. Explicitly bind both cycles’ breakpoints or state exact fixture-path equality. Bound parent ordinal to `0..143`; each parent must select its corresponding 30 records and sequence.

- **Medium — invariant/error map remains inconsistent.** `INV-VEGETATION-144` still states only restart custody, while provider admission now uses `VEG-E-144`. Expand the invariant and taxonomy/guard text to cover private-provider parse, canonicalization, source-join, receipt, and support failures, or add a dedicated provider invariant. Updating only the BEI and obligation vectors is insufficient.

**Verdict: HOLD pending correction.** Root can safely apply the whole-payload content-digest frame fix. The concrete runtime joins and their canonical encodings require source evidence from the authority author/implementer and should not be filled by coordinating prose.
