# COLD-CANOPY-M1 - resolve the non-target face-pivot obstruction

## Prospective scope and accounting

**DRAFT FOR ROGER'S ADOPTION.** This proposes a narrower continuation and a two-hour charged supplement. It does not authorize itself, waive a failed positive control, change the numerical method, or release either target arm. Incorporate adopted direction into the existing package.md; this is not a second maintained narrative.

**Checkpoint:** COLD-CANOPY-M1, `docs/work-packages/20260920-cold-canopy-m1-001/package.md`, fixed experiment `COLD-CANOPY-M1-TR-SVD-BVLS-01`. Attribute the actual non-target ordinary-update `FacePivotLimit` through its admission decision and active-face sequence. Correct a demonstrated implementation deviation if one exists under current authority. Otherwise return a source-bound account of the method limitation and the smallest justified prospective decision. Do not start another general adapter or solver-design campaign before resolving this named boundary.

**Pinned record:** `rogerlew/openWEPP` at `21f0d48db390aeae58f6da378f1a0567c558229e`. Inspect the current physical-adapter terminal disposition; `physical-terminal-reconciliation.json`; actual `physical-body12-custody.stderr`; relevant `physical-adapter-reviews.json` findings; and current numerical-method, root/nearest/package/science/testing/bounded instructions. Reuse unaffected accepted authority and reviews. The prior handoff remains the source of unchanged exclusions and broader requirements, not permission to use its stopped reserve.

**Exact detached start:** `/home/roger/openwepp-experiments/cold-canopy-m1-20260920`, 764 entries, SHA-256 `9ec7bf7698d055d267e41cac381eaafb56ca57fa1fce5a62359da1d3714a9a3d`. Recovery is `artifacts/physical-terminal-recovery.json`, from observer `85b8314efcd53ccb8111aa97af1f752ed49a99f46b805e754db5a1b2c7059963` plus `physical-body12-custody-from-observer-cut02.patch`, SHA-256 `6d44b35b3b8171feb0c1d872d901df5c1e383907a0120350afd3bec88313a5a6`. Reconstruction: `/home/roger/openwepp-experiments/cold-canopy-m1-physical-terminal-reconstruction-20260925`. Compare actual bytes before editing; reconstruct only when needed. Do not apply the full patch to an already patched tree. Main Rust is not the experimental source.

**Fixed authority:** draft04 numerical-method SHA-256 `3931d006af19db61f321abd23e0990d088770b297eb23cc56a004c704df5bb63`; vegetation cross-reference `a54f0fc436dd63273c50f252069098d786f0e80e1b4228cb2cf2df565ae61599`. Adoption prospectively reactivates only the diagnostic/correction scope below in the same stopped study. Preserve historical expiry and failures. No new anti-cycling policy, KKT tolerance, precision policy, rank repair, regularization, radius/pivot limit, solver fallback, physics, domain, initial condition, timestep or target seed is authorized.

**Budget:** carry at least `334933.959294` charged seconds. Add `7200` charged seconds, giving fixed cumulative ceiling `342133.959294`. Do not stack the old `1080` protected seconds, refund conservative closing charges, or reset prior consumption. Read the published `artifacts/physical-final-ledger.json` and any actual later local receipt once; greater verified consumption reduces the new balance. Retain at least `1800` seconds for independent disposition, preservation and publication. Anchor the verified balance at first resumed work, including reading, to one fixed UTC deadline; no mid-session re-anchoring or new wait deductions.

**Stops:** preserve owner direction, full declared command bounds plus reserve, uncontained integrity failure, indispensable capacity/source/independence unavailable after bounded recovery, completed bounded disposition, and any necessary out-of-envelope method change. Two unsuccessful corrections or 60 charged minutes trigger evidence-based internal reassessment, not renewed time or a routine per-error owner handoff. Stop after a complete method-limitation attribution rather than continue changing unrelated code to exhaust the allowance.

## 1. Preserve what has actually passed

The current 81-control selection has 80 passes and one required positive failure. Retain the accepted real-root materialization/closure path, negative-domain and capacity-custody checks, five actual owner caps, success/error/nested scope restoration, and the accepted Stage 1/analytic behavior. Preserve the 39 local C023 passes under their source-specific scope. They do not qualify a complete physical controller or M1.

The root-path control first obtains a canonical original60 endpoint, then exercises the trust controller at that local endpoint. Its 42 core / 1 raw-J / 6 hydraulic / 1 materialization counts exclude canonical setup and do not demonstrate a target seed-to-root solve. The failing ordinary control starts from that local fixture with its already-fixed coordinate-20 `+1e-4 K` perturbation. It reaches an intermediate accepted update/fresh assembly, then refuses: 83 total core / 2 raw-J / 4 hydraulic / 0 materialization entries, no budget denial, and total factorizations greater than the failed local subproblem's 43. Do not conflate those paths or local and total counters.

The ordinary positive remains FAIL. Its companion negative/custody checks do not discharge that positive obligation. Preserve the `FacePivotLimit` classification as a currently source-conforming non-target refusal, with no demonstrated numerical implementation defect. A new finding must identify actual evidence that supersedes that limited assessment.

## 2. Obtain the decisive record from the unchanged local trajectory

First inspect retained source and observations; reuse sufficient primary evidence instead of rerunning it. If the active-face/admission record is missing, add a bounded opt-in private observation and perform one planned instrumented replay of:

`m1_coupled_tests::m1_trust_region_physical_adapter_ordinary_update_reassembles_before_root_materialization`

Use the same original60 setup and fixed local perturbation. Do not reduce it, choose another coordinate, switch to an easier positive fixture, splice in a guessed near-root state, or change target inputs. This is a NON-TARGET physical control, not the canonical measured workflow prefix or either support-1 arm. Retain its real failing test result. A recording-correction rerun requires a named missing discriminator or independently demonstrated implementation deviation; no unexplained repeat.

Capture values already computed, without extra nonlinear evaluations, extra subproblem solves, reset accounting, or altered algorithmic branches:

- Actual local beginning, first proposed/accepted physical step, installed iterate and following base; ordered raw residuals, units, dynamic normalizers, normalized predicates and governed step operands/thresholds. Record exactly why post-update admission does not materialize before the next assembly, and the current full/replacement witness eligibility. Do not infer eligibility from a scalar raw maximum across different physical units.
- Exact failing base `x`, raw Jacobian, row weights, coordinate scales, weighted matrix/residual, coordinate boxes, radius and natural/predictor/selected phase/capacity context. Separate canonical setup output from trust-controller output. Do not use the setup's Newton/drainage trace as the failing trust subproblem.
- Every actually reached face operation up to the existing cap: pre/post active-lower/active-upper/free masks, active coordinate values, full scaled step, candidate direction, crossing factor/index, release index and tie decision, lambda, remaining radius, gradients `g` and `h`, factorization/sweep/pivot counters and exact refusal stage. Where an operation did not occur, report absence rather than invented zero data. Store the base matrix once and bounded per-face records; do not build a general recorder framework.
- Whole-solve and failed-subproblem counters separately; actual protected input/current-state preservation and exact prior accounting-scope restoration. Preserve original typed error if observation itself fails, and label incomplete observation as such.

Use existing non-target observation controls and source/manifest review for the affected hook. Establish that observation preserves the original failure, physical operands, trajectory and counters under their stated semantics. The existing 180-second physical-command maximum and complete-bound-plus-reserve checks apply. No target execution, extra off-trajectory physical probe, or release benchmark is selected in this diagnostic.

## 3. Independently reconstruct the decision, not just the cap count

From the recorded arrays and prescribed operation order, independently reconstruct the decisive face/crossing/release sequence. Verify the unchanged rules:

`g = A^T(f + A*p)` and `h = g + lambda*p`.

A lower-active coordinate is released for `h_i < 0`; an upper-active coordinate for `h_i > 0`, with the stated largest-violation and tie rules. Examine feasibility, finite-bound representation, crossing theta, active values, free-face solve and stationarity together. A repeated mask alone is not proof of cycling when the step, active values or radius differ. Distinguish:

1. actual deviation from frozen assembly/arithmetic/selection/admission authority;
2. a prescribed finite-precision sequence that revisits a face or makes no useful progress;
3. distinct progressing faces that exhaust the prescribed bound;
4. incomplete or insufficient observation.

The raw residual maximum `4.547473508864641e-12` is not root acceptance by itself. Verify all original normalized and governed predicates and why another step or witness was required. Do not bypass that requirement by accepting a small residual, zeroing a tiny direction or calling a failed subproblem a valid witness.

Offline binary64 reconstruction and higher-precision analysis of the SAME retained linear system are permitted as labelled diagnostic calculations. They are not a changed runtime precision policy, another nonlinear solve, or evidence that a modified physical method converges. Do not add precision/dependency changes to Rust. If cancellation, roundoff-sensitive release, or anti-cycling handling appears necessary, describe its exact evidence and proposed authority change; do not implement it under this continuation.

## 4. Correct only supported implementation defects; reconcile remaining interfaces

If an independently verified implementation deviation exists, author its discriminating control, obtain corresponding review, correct the existing implementation and perform affected verification. Preserve all former failures and the fixed numerical policy. Existing physical callbacks, nested scope, materialization custody and accepted analytic checks should not be redesigned by default.

If the algorithm conforms and the positive still refuses, close this limited attribution with the exact decision needed. Do not raise 42 pivots, insert a gradient epsilon, force an active bound, clip drainage, alter the SVD/ball tolerances, relax governed admission, or tune the fixture. Do not relabel the required ordinary-positive test as expected success or delete it. A valid non-target negative does not constitute the target experiment's negative outcome or consume a target slot.

The full controller-stage inventory's 18 errors are separate unfinished test-interface/coverage obligations. Read `physical-body12-full-feature-inventory.stderr`; preserve the underlying domain, witness, phase/tie, cap and refusal requirements. Within remaining time, reconcile each named seam to actual existing producer operations and identify the smallest missing connection. Mechanical private rebinding to reviewed real code is in scope with affected review; canned factories such as a name that simply emits `FacePivotLimit`, public test exports, deleted assertions or a new feature hiding failures are not. Do not turn this diagnosis into another broad harness implementation project. Full-feature acceptance remains HOLD wherever unimplemented or unexecuted.

## 5. Qualification, target reservation and return

All original target slots remain UNUSED and are NOT RELEASED by this narrower continuation: measured canonical prefix, baseline, treatment and conditional corrected target repeat. Parent/cycle and production progression remain excluded. A target comparison cannot bypass the still-failed mandatory positive control or missing full-feature qualification.

Run affected compile/format, actual non-target control and observation tests, source/reachability checks, and same-command lint attribution under current testing policy. Reuse the exact 238 inherited-diagnostic evidence only for verified unchanged surfaces; introduced or ambiguous diagnostics require correction/attribution. No lint suppression, fingerprint update, unrelated cleanup or new validation engine. The old full09/A1/strict-lint, material/BGC, parent, positive-late-rejection, cycle/restart and performance HOLDs remain. No broad failure census is selected merely to repeat those failures. Any independently applicable requirement remains unmet until actually satisfied.

Astra owns sequencing with one functioning writer and distinct independent correctness/QA reviewers. Reuse accepted findings and reviewer-owned fix verification; use attributable replacements only when necessary. Use exposed lifecycle operations, at most two concurrent children, no nested/runtime-limit workaround or reviewer-to-author conversion. Preserve incomplete code, current raw evidence and unrelated dirty work. Scoped publication of reviewed status/evidence, required recorder changes and exact detached recovery on the already-authorized main branch remains permitted; no main Rust adoption, branch switch, destructive cleanup, new acquisition, WAT5 work, Grid40 reuse, original E008 repair or old reader run.

Return the admission decision and exact face-pivot mechanism first, distinguishing observed facts from numerical hypotheses; the valid retained positive FAIL or a demonstrated conforming correction; actual counter/state preservation; remaining full-feature interfaces; independent scopes; exact recoverable source/build/input identities and conservative final ledger. This limited increment can complete as an attribution without completing the fixed comparison or M1. Do not substitute another control count for the cause of this failure.

## Adviser evidence boundary

At the pinned revision the adviser inspected the current package and incorporated reviewer classifications, exact final recovery/reconciliation and published charged ledger, actual final non-target test failure, full-feature compiler output, frozen active-face/admission authority and the supplied preceding authorization. Previously read governing repository guidance remains applicable; execution must verify current path-scoped instructions. The adviser did not inspect the complete terminal numerical implementation or reconstruct the active-face sequence, execution-host binary or complete physical corpus. No independent acceptance review is supplied. Only retrieval, handoff creation and arithmetic checks ran; no Rust, nonlinear evaluator, target arm or new subproblem computation was executed here.
