Static review of the frozen `SC-VEGETATION-001.md` diff, SC-COUPLEDTIME framing clauses, and the two pinned input artifacts. No Rust or runtime review.

- **High — lines 3321–3327 violate existing receipt framing.** Raw `SHA-256(payload)` and raw `SHA-256(forcing_projection)` conflict with SC-COUPLEDTIME-001’s rule that every identity and receipt uses `OPENWEPP_CANONICAL_FRAMED_SHA256_V1`. Define provider-owned framed receipt domain(s) and closed ordered fields, or define raw hashes only as content digests and frame the receipt identities over them. Passing the resulting 32-byte receipts into the unchanged `parent-interval` fields does not alter coupled-time’s closed domains.

- **High — lines 3325 and 3353–3370 do not make `forcing_receipt` a sealed forcing receipt.** Hashing only `forcing_projection` omits the two source hashes and projection implementation hash. A changed source or implementation can retain the same forcing receipt. The framed forcing receipt must bind provider/schema version, cycle identity, authenticated source content digests, implementation content digest, and the ordered projection digest.

- **High — lines 3368–3371 cannot represent the required joins.** `{field,source,json_pointer,binary64_bits}` covers only floating JSON leaves. Destination, topology, controller/model/configuration identities, receipt IDs, strings, booleans, arrays/objects, and Rust-source constants need a closed typed value union plus a locator union such as JSON pointer versus Rust symbol. Every referenced source must have path and content hash in `sources`. The two pinned JSON artifacts do not contain current GSI, controller policy, or complete configured destination/topology identities; the authority must identify their authentic sources or name the exact missing field and consumer and remain HOLD.

- **High — the payload shapes are not uniquely constructible.** `sources` does not state object/array shape or exact member names; `segments.units`, permitted `boundary_overrides`, `source_joins` ordering, and typed values are unspecified. “Canonical JSON” also leaves duplicate-key rejection, key ordering basis, string escaping/normalization, integer spelling, pointer grammar, and numeric spelling ambiguous. Freeze these rules now. The implementation path/hash and final receipt values may be sealed after implementation, but admission must occur only after those concrete values are present and authenticated.

- **High — run/cycle/parent relations are incomplete.** Each cycle requires its own authenticated `run_identity`, clock/counter reset, beginning seven-owner set, and cycle-specific calendar/forcing receipts; old repeated-byte run or transaction identities cannot survive. Require `forcing_projection.cycle_id == cycle.id`, exact selection of `cycle.ordinal/id/initial_phase_records` from the pinned fixture, and a join from those phase records to the constructed beginning vegetation owner. Define parent `n` as exactly records `30n..30n+29`, support `[1800n,1800(n+1))`, with sequence/counter advancement only through complete-parent commit.

- **Medium — lines 3371–3375 conflate segments and projected records.** `[60,86400)` is a source segment, not one projected record. Cycle 0 must yield 4,320 contiguous 60-second records, with the first parent containing `[0,60)` through `[1740,1800)`. “Without repetition” is misleading because piecewise-constant values repeat across distinct supports; prohibit support duplication or stretching instead. State equivalent exact segment breakpoints/cardinality for cycle 1.

- **Medium — lines 3383–3392 overload restart error authority.** `INV-VEGETATION-144` and `VEG-E-144` currently describe restart/state custody. Either explicitly extend that invariant, guard map, taxonomy, and precedence to provider admission or add a dedicated provider identity invariant/error mapping. Update the Binding Exposure Index and change log, and enumerate the required mutation vectors rather than referring generally to additional `OBL-VEGETATION-C-008` vectors.

**Verdict: HOLD.** The physical fixture hashes and intended 60-second schedule are correctly identified, but the current cut cannot yet produce an unambiguous, canonically framed, source-authentic receipt or complete cycle-specific parent identity.
