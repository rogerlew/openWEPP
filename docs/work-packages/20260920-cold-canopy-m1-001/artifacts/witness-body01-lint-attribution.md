# BODY01 lint attribution — QA

**High — HOLD.** The frozen BODY01 receipts contain 243 diagnostic occurrences, 239 unique identities. Mechanical identity matching against BODY07 classifies **92 inherited exact/unchanged**, **142 exact transfers from BODY07's independently reviewed candidate**, and **five changed or unmatched**. The latter category deliberately does not assert BODY01 authorship; it means the historical result cannot be reused. The complete mapping is in `witness-body01-lint-attribution.json`.

The named Stage2 diagnostics remain at `m1_trust_region_stage2_controls.rs:721`, `:760`, and `:1677`. BODY01 also has `m1_trust_region_controller.rs:379` (`transition`, 118/100 lines). All four are changed/unmatched, so none is waived by the BODY07 chain. The fifth unmatched strict diagnostic is `openwepp-vegetation/src/v11.rs:2149`; it is outside the BODY01 LSE diff and remains a separate strict-gate HOLD.

**Non-blocking debt/follow-up.** The two recorded commands have the same Nix wrapper, target directory, jobs, package, all-targets selection, analytic feature, and `-D warnings` as BODY07; focused also matches `--no-deps`. Strict output has the same stderr SHA as BODY07, but that does not replace per-diagnostic identity matching. Rerun the recorded focused command after fixes; strict Clippy remains required.

Advisory harness classification: this is compatible with `SC-LANDSURFACEENERGY-001` numerical-methods.md:586–590 only when an immutable clone of the actual prepared `M1CoupledColumnInput` is taken at the real prefix-to-solver boundary before either policy. A bit-encoded private envelope may transport that clone; it must not reconstruct from JSON traces, iteration-15 state, or guessed replay. `CoveredColumnInputs` being serde and fields being public are insufficient until every required field and float bit correspondence is verified. With private `cfg(test)` guards and no public/re-exported interface, dependency/lockfile, production path, schema, or selector change, it remains noncritical. A public cross-crate feature remains Critical and retains `cargo nextest run --workspace --profile full` under the existing 900-second Critical allowance.

Advisory timing assessment: `/proc/self/stat` utime+stime ticks plus pre-interval `getconf CLK_TCK` is compatible with the required monotonic wall and process-CPU clocks if CPU samples bracket the same solver-entry-to-return interval. Record method, resolution, endpoints, conversion and boundaries; fail closed on unavailable/malformed/nonpositive/overflow/nonmonotonic data. It is not a release measurement by itself.

The earlier BODY01 QA verdict remains: “No blocking findings. Non-blocking: full/A1/release qualification remains outside BODY01. QA PASS: BODY01 is test-only, private, source-custodied, and passes its 16 focused controls plus formatting.” That verdict predates and does not waive this lint HOLD.
