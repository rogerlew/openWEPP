# COLD-CANOPY-M1 complete-parent controls — correctness review 02

**Verdict: HOLD for the complete-parent control cut.** The exact 30-record provider/forcing bridge and the test-side reservoir mass/enthalpy reconstruction are accepted. The remaining controls do not yet prove typed resource/material custody, installation in the actual caller, a required positive receiver event, or late rejection at that caller's owning seam. Do not author the complete-parent body against this cut.

**Evidence class: Static.** Independent review of frozen canonical 760-entry source aggregate `e89c3053500428381c70c825a45de024c2899d71cf276a0463f25349baf49eed`. No source/configuration was edited and no test or physical command was run.

## Findings

1. **High — resource and material checks remain vacuous and erase the canonical typed protocols.** `m1_provider_parent_controls.rs:71-77` accepts empty collections, then chains resource and material records through one invented `amount/source_owner_id/destination_owner_id` interface. That cannot authenticate the actual structures: `V11ResourceDebit` carries request, authorization and final use plus owner/OFE/tile/occupancy/layer/source/basis (`openwepp-vegetation/src/v11.rs:483-500`); admitted fluxes and `V11SharedResourceOwnerTransition` carry exact debit/flux ID sets and beginning/ending inventories (`:513-572`); `MaterialTransfer` separately carries transaction/owner/proposal, donor/receiver and carbon/nitrogen/dry-matter amounts (`carbon_nitrogen.rs:114-123`). Expose these existing typed records in each raw support outcome. Assert exact expected cardinality and identity membership; independently fold debits and fluxes into each transition's beginning-to-ending inventory and compare its receipt-ID sets and owner-candidate digest; independently fold material amounts into their actual owner deltas. If a class is legitimately empty, derive zero demand/transfer from the raw candidate operands and assert exact emptiness. A loop that also passes on empty vectors is not evidence.

2. **High — completion and accepted-slab ownership are still reported by the future driver instead of observed on the caller.** `:82-105` trusts result-provided revision/counter/cleared-state/owner-byte values; its only direct assertion is that an opaque caller snapshot changed. The stratum map equality at `:60` can also pass for empty maps. The test must own the loop: call a narrow support-advance seam on the same `RealM1ProviderCaller`, collect raw outcomes itself, and inspect private read-only caller accessors after every support and after one `complete_parent` call. Assert the exact expected occupancy/stratum keys, zero per-support parent publication/revision, the actual accepted cursor and staging state, seven actual ending owner bytes, one parent revision/publication/consumption, and cleared actual clock/staging. For every slab, construct the actual seven `OwnerState` endings and call the existing `AcceptedSlabReceiptV1::authenticates_complete_ending_owners` (`openwepp-coupled-time/src/transaction.rs:256-270`), while matching its real ID, slab ID, parent ID, segment ID, ordinal and support accessors (`:165-193`). Assert a second completion attempt returns the exact typed already-consumed refusal. Result booleans or copied post-state do not establish these facts.

3. **High — the required positive receiver discriminator is optional and capability custody is self-reported.** `:106-116` passes when no support is positive, while `:112` accepts booleans saying a capability was minted and consumed. The success control must either require the independently selected first positive raw native candidate in this frozen parent, or fail with the separate typed no-positive discriminator. For the required positive path, inspect the concrete candidate and native receipt: positive mass, finite enthalpy, exact source/destination/support/transaction/slab identity, actual receiver-owner delta, and actual consumption (the consuming API succeeds once and replay is typed-refused, or the non-clone capability is moved through the real seam). Keep an all-zero census as a separate discriminator; it cannot satisfy a test claiming positive work.

4. **High — late rejection still permits the wrong seam and treats absence of the required event as success.** `:123-147` delegates preparation and injection to one opaque helper, accepts `NoPositiveNativeReceiverInParent`, and compares driver-reported before/after fields. Final equality to the pre-parent snapshot cannot show that an accepted prefix ever existed on the real caller; work on a disposable clone would pass. Advance the authentic accepted prefix support-by-support on the same caller, select the concrete first positive candidate, and capture actual caller state immediately before injection. Invoke the real owning validation/commit boundary once and require only the exact `LateOwningReceiver` typed error. Direct caller accessors must prove the prefix was staged before injection and, after rejection, that actual owners, clock, staging, accepted chronology, publication/outbox, and provider position have the authority-required rollback state, with the rejected capability consumed/no replay possible. `NoPositiveNativeReceiverInParent` belongs only in the separate discriminator control.

## Accepted corrections and residual validation

Review-01 finding 2 is resolved: `:42-56` binds all 30 ordinals to exact admitted intervals, override bits, GSI support receipts, parent forcing receipt and slab support. The independent reservoir arithmetic at `:61-69` resolves that portion of review-01 finding 1. These two subscopes are released and need no further redesign.

The file remains intentionally expected-red because the body is absent. After the four controls above are corrected, compilation and the focused nonphysical parent-control run are still required; no runtime evidence was produced by this review. Settled provider parsing/admission, canonical owner bytes and primitive solver physics remain outside this re-review.

## Reviewed identities

- parent controls: `m1_provider_parent_controls.rs` SHA-256 `43d9e232f56254f4a09ceee31b03cdb9fbc080bb92305d3e9d3f095969c36769`
- module wiring: `land_surface_energy_shadow/mod.rs` SHA-256 `83cf785961155d5d0a504399ae03fe1b5c7acd6d4dc99d54fe8a72da64f36568`
- source recorder: `provider-parent-parent-controls-review-02-source.json` SHA-256 `5e6330364fdcab59719a4cd4a30ce6388277d236731301de39aeb963aca47579`
- observer-relative patch: `provider-parent-parent-controls-review-02-from-observer-cut02.patch` SHA-256 `4b34fd3439a8e5ce5d15045cbff0db84f6a3dd772819bcb394a1529cacb2e2c7`
