**Static QA review — capacity confirmed.** This is a functioning, independent QA/evidence review conversation. It establishes only preimplementation capacity; it does not approve source changes, a run, or parent admission.

**High — authority adoption / package record:** [/tmp/openwepp_cold_canopy_m1_parent_metadata_authorization.md](/tmp/openwepp_cold_canopy_m1_parent_metadata_authorization.md:3) is explicitly a “DRAFT FOR ROGER’S ADOPTION.” The maintained [package.md](/workdir/openWEPP/docs/work-packages/20260920-cold-canopy-m1-001/package.md:4673) still records the prior terminal HOLD. Before implementation, the owner must adopt the narrow prospective metadata direction into that single record, with its bounded source surfaces and acceptance; otherwise this is advice, not active authority.

**High — new provider receipt must be semantically complete:** The former `digest(2)` calendar value is insufficient by the retained correctness review. The new private payload must bind: calendar system, offset/DST/leap-second policy, run origin, checked-nanosecond mapping, day mapping, actual ordered forcing records and their supports (including the 60-second breakpoint), units, cycle identity, original/continuous fixture bytes, required GSI/exposure/destination/CO2/reference-height/topology/controller facts, and deterministic projection/expansion implementation identity. [SC-COUPLEDTIME-001](/workdir/openWEPP/docs/specifications/science-contracts/contracts/SC-COUPLEDTIME-001.md:107) requires receipt-based calendar authority and sealed forcing/support; closed canonical domain tags prohibit inventing a coupled-time tag or substituting an arbitrary `[2; 32]`.

**High — parent admission evidence must be constructed, never inherited:** The parent must use new identities derived from the complete authentic inputs: exact `[0, 1800s)` parent support, parent sequence, regime/participants, schema/model/configuration identities, sealed provider receipt, and beginning owner set. It needs all seven canonically ordered aggregate owners, including unchanged inactive snow bytes; six-owner clock acceptance is invalid. See [SC-VEGETATIONTRANSACTION-001](/workdir/openWEPP/docs/specifications/science-contracts/contracts/SC-VEGETATIONTRANSACTION-001.md:390). Required negative controls should cover altered calendar/origin/day/support, omitted/reordered forcing, foreign/missing snow, wrong sequence, incomplete/reordered manifest, early commit, and rollback/no publication.

**High — final execution manifest remains incomplete:** Existing preflight findings in [parent-correction-review-responses-03.json](/workdir/openWEPP/docs/work-packages/20260920-cold-canopy-m1-001/artifacts/parent-correction-review-responses-03.json:22) remain binding. The final selected-run manifest must source-bind coupled-time `clock.rs`, `restart.rs`, `transaction.rs`, `lib.rs`, orchestrator bridge, actual M1 owner/receiver/validation surfaces, workspace `Cargo.toml`/`Cargo.lock`, affected crate manifests, selected support/link contents, exact binary/hash, argv, cwd, environment, timeout/physical flag, authority/input pins, and deadline/reserve receipt. A separate manifest is required for any full-workspace gate.

**High — prior terminal evidence cannot be promoted:** The terminal package records full-workspace correctness blocked by two missing imports and Clippy failures (53 library / 124 test-build diagnostics), with authority completion 56 pass / 39 fail. Metadata/provider/source changes invalidate affected evidence under the testing strategy; retain those failures and rerun applicable focused and required gates against the frozen final source. `cargo fmt`, Clippy with warnings denied, tests/nextest as selected by the current workflow, and `cargo deny check` when manifests/lock change require current attributable results.

Non-blocking follow-up: preserve the existing fixture hashes only if byte-identical; the proposed calendar labels must not trigger weather/solar/GSI regeneration or alter frozen physical scalar expectations. The later concrete specification should name the private receipt encoding and its test vectors before Rust implementation, then receive distinct correctness and QA review of that stable cut.
