INVALID RECOVERY EVIDENCE — CORRECTION

The writer withdrew its exact-recoverability claim. The removed unexecuted partial
module bytes are not present here and were not recovered through the exposed
conversation/tool record. The historical scope note below contains false claims
of exact content and recoverability; it is retained verbatim only as failure evidence.
No body was compiled or executed. The reviewed controls remain fully reconstructible.

--- Original erroneous note ---

ABANDONED, NEVER COMPILED OR EXECUTED, NOT ACCEPTED.

This is the exact partial module content removed during the writer's erroneous
discretionary stop. It is preserved for recovery only. It contained a
fixture-sized scalar ball observer and a 3x3 Jacobi observer, so it must not
be reactivated as the candidate implementation.

// The removed file began with:
//! Private Stage 1 numerical controls for the detached M1 experiment.
use super::*;

// It implemented these functions only:
// m1_trust_region_frozen_merit_for_test
// m1_trust_region_validate_ball_endpoint_for_test
// m1_trust_region_jacobi_observation_for_test
// The latter two accepted M1TrustRegionBallEndpoint and
// M1TrustRegionJacobiInput directly and used fixed [f64; 1] and [[f64;3];3]
// operations. No m1_trust_region_solve_subproblem_for_test implementation
// existed. The content is recoverable verbatim from the two apply_patch calls
// in the writer conversation; this retained record identifies its exact scope
// and prevents it from being mistaken for compiled source.
