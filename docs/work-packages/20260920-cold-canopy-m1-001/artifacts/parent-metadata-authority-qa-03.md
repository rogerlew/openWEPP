**Static QA — HOLD.** The revised cut fixes canonical ASCII/float encoding, source hash pinning, typed join categories, mutation-vector scope, BEI coverage, and the change log.

**High — receipt coverage bypass:** [SC-VEGETATION-001.md](/workdir/openWEPP/docs/specifications/science-contracts/contracts/SC-VEGETATION-001.md:3395) frames `forcing_receipt` over records but omits both `source_joins` and source `segments`. A join path/hash/value or segment provenance can change while `run_identity`, `calendar_receipt`, `forcing_receipt`, and the coupled-time parent identity remain unchanged. Bind the canonical complete `ProjectionV1` to `forcing_receipt`, or explicitly include canonical `segments`, `records`, and ordered `source_joins`.

**High — run identity does not distinguish the two executions:** [SC-VEGETATION-001.md](/workdir/openWEPP/docs/specifications/science-contracts/contracts/SC-VEGETATION-001.md:3387) omits cycle ordinal, cycle ID, and initial phase records from `run_identity`, despite each cycle being a separate execution with a distinct initial state. Include those fields in the run frame. The calendar receipt may then bind the same values as a cycle-specific receipt consistency check.

**High — Rust locator is not deterministically evaluable:** [SC-VEGETATION-001.md](/workdir/openWEPP/docs/specifications/science-contracts/contracts/SC-VEGETATION-001.md:3366) says `rust_symbol` extracts a “single named initialized value,” but Rust symbols may be shadowed, constructed, computed, generic, associated, or macro-generated. The grammar identifies text, not a uniquely evaluable value. Restrict Rust joins to a dedicated canonical static descriptor with an exact literal grammar, or define a source extraction adapter and bind its source/hash plus exact extraction result. Require `source_path` to be a canonical relative path that matches an admitted `SourceV1` or the selected implementation source.

**Medium — remaining schema closure gaps:** [SC-VEGETATION-001.md](/workdir/openWEPP/docs/specifications/science-contracts/contracts/SC-VEGETATION-001.md:3349) leaves `provider_id`, `projection_id`, IDs, and several ordinal/tick field types unconstrained; fix the provider/projection literals and exact scalar types. Require every cycle’s segments/records to start at zero, end at `259200s`, have ordinals `0..4319`, and permit parent ordinal only `0..143`.

The concrete implementation path/hash and known payload receipts may remain pending until the provider exists. Before any parent admission or physical launch, seal the instantiated payload against both fixture cycles, compute known run/calendar/forcing receipts, and review the frozen implementation and manifest.
