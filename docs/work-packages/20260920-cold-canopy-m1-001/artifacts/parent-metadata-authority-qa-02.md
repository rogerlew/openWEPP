**Static QA — HOLD for contract-derived controls; no implementation or launch approval.**

**High — closed join schema is unimplementable as written:** [SC-VEGETATION-001.md](/workdir/openWEPP/docs/specifications/science-contracts/contracts/SC-VEGETATION-001.md:3368) requires every `source_joins` member to carry `binary64_bits`, while also requiring joins for destination, topology, and controller policy. Those are identity/structured values, not binary64 scalars. Define a closed typed union with exact value-kind, extraction, and comparison rules: binary64-bit joins for scalar operands; canonical structured-value bytes/digest joins for configuration/identity values. Reject a kind/path/value mismatch before receipt minting.

**High — receipt encoding remains underspecified:** [SC-VEGETATION-001.md](/workdir/openWEPP/docs/specifications/science-contracts/contracts/SC-VEGETATION-001.md:3322) combines “compact canonical JSON” with binary64 values represented by exact source JSON tokens, but does not define the permitted numeric-token grammar, JSON-Pointer decoding/escaping, duplicate-key handling, or the extraction result encoded by each join. Two conforming implementations could mint different receipt bytes. Use the source-file byte hashes to preserve original lexical provenance and encode all projected scalar values as fixed 16-hex binary64 strings; define source-pointer extraction and canonical serialization fully.

**High — required provider vectors are not concretely enumerated:** [SC-VEGETATION-001.md](/workdir/openWEPP/docs/specifications/science-contracts/contracts/SC-VEGETATION-001.md:3380) assigns failures to the broad existing `OBL-VEGETATION-C-008`, whose current vector list does not name provider encoding/admission. Add explicit pre-Rust vectors: valid canonical payload and receipt known-answer; source hash/path/pointer/value-kind/value mutations; calendar/origin/day/cycle/initial-state mutations; segment and record omission/reordering; 60-second breakpoint/support mutation; noncanonical/unknown/duplicate fields; projection-vs-sealed-record mismatch; and physical-scalar byte equality across metadata-only mutations. State expected `VEG-E-144` precedence and no owner mutation.

The specification may intentionally defer the instantiated payload, selected implementation path/hash, and final receipt known answers until implementation exists. That is acceptable for this authority phase if it fixes deterministic extraction and encoding now. Before any parent admission or physical launch, the final implementation must mint and seal the concrete payload with both actual fixture-cycle joins and known receipts; final source/manifest QA must review that stable cut.

**Medium — contract maintenance:** the 80-line authority addition has no corresponding change-log entry, and the M1 Binding Exposure Index does not mention provider-receipt admission. Update the canonical change record and binding description when correcting the above.

No non-blocking debt. `git diff --check` passes; no commands, source edits, or physical work were performed.
