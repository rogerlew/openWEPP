# Contract Implementation Evidence

Evidence mode: `Static + Ran`

Status: `implemented; terminal gates pending`

- `SC-PLANT-001` rev 22 maps to the typed realization and state in
  `openwepp-plant-phenology`, explicit native YAML projection, and the direct
  growth `TypedStateOverride` consumer handoff.
- `SC-RESIDUE-001` rev 14 maps to the early native-litter branch that rejects
  pending compatibility mass and returns before aggregate-decline/`jdharv`
  logic.
- `SC-INFILE-MANAGEMENT-YAML-001` v0.2.0 maps to the required schema block,
  parser type, strict validation, and exact slotted plus primary PL symbols.
- A real one-year native YAML forest run completed through the
  `direct-production-executor`.
