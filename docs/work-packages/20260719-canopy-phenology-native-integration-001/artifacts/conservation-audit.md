# Conservation And No-Drift Audit

Evidence mode: `Ran`

Status: `focused audit pass; terminal regression pending`

Independent test reconstruction checks
`B_before + allocation - litter == B_after` rather than reusing the producer
expression. A two-year repeated forcing test proves identical endpoint foliar
mass and identical annual allocation/litter totals. Runner tests prove native
litter enters the decomposition input immediately and fails if a compatibility
pending bucket is present.
