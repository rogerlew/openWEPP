# Real Consumer Path Proof

Evidence mode: `Static + Ran`

Status: `proved; terminal regression pending`

The day builder advances per-lane GSI before residue, snow, and interception.
It installs the realized state as the active growth input with
`TypedStateOverride`; the executor validates and republishes that state to ET
and erosion. Native litter returns directly before compatibility aggregate
loss and fixed-date logic. The source-order negative guard and a real native
YAML direct-production run both pass.
