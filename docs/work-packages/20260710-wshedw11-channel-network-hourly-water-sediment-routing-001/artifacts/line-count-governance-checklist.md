# Line-Count Governance Checklist

Status: `NOT-APPLICABLE-NO-RUST-EDITS`

Evidence mode: `Static`.

`kernel/direct.rs` starts in the 2000-line warning band. The hourly solver must
use a cohesive module unless review proves a smaller bounded integration. Record
all touched `.rs` line counts and disposition before closure.

No `.rs` file was edited. Future resumed W11 still requires `hourly.rs`; the
existing `direct.rs` warning-band constraint remains binding.
