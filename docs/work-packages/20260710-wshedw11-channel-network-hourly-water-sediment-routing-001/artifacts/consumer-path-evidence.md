# Consumer-Path Evidence

Status: `BLOCKED-PRE-IMPLEMENTATION`

Evidence mode: `Static`.

Name producer, HBP bytes/parser, frame object, leaf channel intake, routed
channel hourly output, downstream dependency consumer, and public aggregate.
Include negative proof that scalar summaries and compatibility paths do not
carry W11 acceptance.

Current proven path stops at leaf-channel M-T3 scalar reduction:

`HBP V_h/S_h -> HBP parser -> HillslopeContribution ->`
`leaf inlet peak/volume/span + total mass/active span -> scalar channel state`.

No routed channel hourly output exists for the downstream consumer. The existing
guard rejects hourly contributors when dependency nodes lack channel-hourly
state. W11 made no production edit, so no wrapper, scalar reconstruction,
shadow, or compatibility path now carries the channel-network claim.

The real consumer gate remains `BLOCKED` pending W11A authority and resumed W11
implementation.
