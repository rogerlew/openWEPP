# Required Reading Map

Status: `IMPLEMENTATION INTAKE`

Evidence class: `Ran + Static`

Applicable instruction chains were resolved with `tools/agents/find-agents`:

- root `AGENTS.md` for all paths;
- `crates/AGENTS.md` for `crates/openwepp-gate-planner/**`;
- `docs/standards/AGENTS.md` for testing-strategy edits; and
- `docs/work-packages/AGENTS.md` for package and catalog paths.

Required implementation authority:

- this package, active prompt, transaction contract, acceptance matrix, defect
  inventory, both scaffold reviews, and finding disposition;
- `docs/standards/testing-and-gate-strategy.md` §8.5 and §8.5.1;
- `crates/openwepp-gate-planner/src/pre_heavy.rs`, `executor.rs`,
  `verifier.rs`, `canonical.rs`, `artifact_contract.rs`, and relevant schemas;
- CAL-04B package, frozen executor plan/observed contract, custody tools, and
  coordinator tools.

No Harvard content is required or authorized during implementation.

