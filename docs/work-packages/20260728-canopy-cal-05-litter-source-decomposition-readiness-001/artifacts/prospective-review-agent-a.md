# Prospective Scientific Review A — Amended Scaffold

Status: `PASS`

Evidence class: `Static: independent prospective re-review; Ran: read-only
arithmetic inspection of the retained Harvard join; no result-bearing
execution`

## Re-review result

- `PSRA-001 CORRECTED`: the scaffold now separates runner rate projection,
  authenticated CAL-03 native-source/consumer evidence, direct-runtime
  decomposition, downstream publication, and real residue/depth consumption.
  The synthetic producer now uses
  `DirectDayFrame::run_r5c_decomposition_phase` and is explicitly not called a
  new end-to-end runner (`package.md:31-35,59-64,109-117,226-233`).
- `PSRA-002 CORRECTED`: the axis is now a synthetic
  `surface_litter_input_kg_m2` operand rather than an observed total source.
  `K000` is explicitly separated from the native seasonal-forest fallback, and
  the fallback receives its own projection proof
  (`package.md:123-129,153-156`;
  `artifacts/deterministic-design.csv`).
- `PSRA-003 CORRECTED`: the complete recurrence now freezes one-based day
  indexing, initial state, factor equations, source-before-decay chronology,
  daily/annual carry-forward, terminal sampling, and first-divergence reporting
  (`artifacts/execution-control-contract.md`).
- `PSRA-004 CORRECTED`: package completion is now separate from
  stage status, empirical calibration remains prohibited, and
  `CALIBRATION_READY_DATA_LIMITED` is conditional rather than preassigned
  (`package.md:226-251,277-288`). The recurring-needle and fine-woody rule and
  authority ledger now use the exact orthogonal ADR-0042 triple:
  `science_implementation_status=AUTHORITY_MISSING`,
  `calibration_evidence_status=NOT_CALIBRATION_READY`, and
  `identifiability_status=NOT_ASSESSED`.

## Verdict

`PASS`. PSRA-001 through PSRA-004 are corrected. The material/stock boundary,
assumption labeling, synthetic recovery design, terminal-ridge design, and
anti-fitting controls are prospectively acceptable. This verdict authorizes
result-bearing execution only after the package's required second independent
prospective review also passes; it is not result evidence.

## Incident 001 Amendment Review

Amendment verdict: `PASS`

The aborted diagnostic failed closed before publication, so it produced no
scientific result. Independent arithmetic over all 28 retained Harvard rows
confirmed:

- 24 rows exceed the original `1e-9 g C m^-2 yr^-1` arithmetic guard;
- the maximum absolute serialized mean-closure residual is
  `1.6166666227945825e-8 g C m^-2 yr^-1`;
- no row exceeds the amended `1e-7 g C m^-2 yr^-1` guard; and
- the maximum residual relative to its serialized total is approximately
  `8.56e-11`.

The amended guard is therefore a narrow serialization-tolerance correction,
about 6.19 times the observed maximum residual and scientifically negligible
relative to the retained flux totals. It changes no source value, material
class, unit, plot, period, observation role, conversion, objective, fit
permission, or stock/pool comparability rule. The existing descriptive-only,
noncontemporaneous, pooled-nonfoliar, carbon-basis claim boundary remains
unchanged.

Incident 001 and the prospective package amendment at `package.md:183-188` and
`package.md:295-301` are sufficient. The Harvard diagnostic may be rerun with
the amended guard; this approval does not validate any rerun result.
