# Scope Selection

Evidence mode: Static.

R6C is an implementation hold-lift for
`HOLD-R6-DIRECT-PUBLICATION-PARITY-AND-MANIFEST-CUTOVER`, specifically the
R6B hold `HOLD-R6B-DIRECT-PUBLICATION-TYPED-OPERAND-BRIDGE-ABSENT`.

Selected scope:

- direct publication operand bridge;
- direct publication output-family cutover gates;
- anti-alias and independent-reconstruction fixtures;
- manifest direct projection proof;
- no-compatibility proof and validation evidence.

Rejected scope:

- default direct publication activation;
- compatibility writer deletion;
- process-physics changes;
- output schema changes;
- compatibility wrappers presented as direct publication authority.
