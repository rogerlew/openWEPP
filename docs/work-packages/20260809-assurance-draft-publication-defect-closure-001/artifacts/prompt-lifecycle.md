# Prompt Lifecycle

Evidence class: `Ran`

Source path before archival:

```text
prompts/active/20260809-assurance-draft-publication-defect-closure-001_kickoff_agent_prompt.md
```

Terminal path:

```text
prompts/completed/20260809-assurance-draft-publication-defect-closure-001_kickoff_agent_prompt.md
```

SHA-256 before and after:

```text
a68817d4fc21bad2ad2f55fd9532109172646374da6de123831e19bf328c9a5b
```

`markdown-doc mv --no-backup` was attempted first and timed out after 60
seconds of CPU-bound repository reference scanning without moving the file. A
direct same-filesystem rename then preserved the bytes. The only remaining
active-path text is the historical pre-run inventory in
`full-workspace-gate-pre.txt`; it truthfully records the path at gate start and
is not an executable prompt reference.
