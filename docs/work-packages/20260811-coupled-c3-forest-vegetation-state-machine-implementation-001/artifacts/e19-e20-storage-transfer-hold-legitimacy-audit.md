# E19-to-E20 Storage/Transfer Authority Hold Audit

Status: `HISTORICAL HOLD LIFTED / V7 authority admitted at 83db91251`

Evidence class: `Static` canonical-authority audit plus `Ran` unaffected E16/E17 operand gates.

## Exact Blocked Surface

`SC-VEGETATION-001` v10, `GAP-VEGETATION-027`, states that E19 credits
noncurrent growth to leaf `storage`, while E20 onset debits leaf `transfer`, and
that no admitted equation moves the C/N operands between those owners. The V4
amendment requires execution needing that bridge to fail closed under
`VEG-E-060` until successor authority supplies ordering, amounts, and C/N
conservation.

The missing operation cannot be replaced by copying, draining, averaging, or
renaming either pool. Those alternatives change persistent carbon/nitrogen
ownership and would be new constitutive science.

## Unaffected Work Completed

The accepted capped E11--E15 solve now retains exact sun/shade gross
assimilation, class leaf area, and class-resolved Rd. `carbon_phase` aggregates
only the sealed final capped operands once per shared stratum with exactly one
phase-bound interval and tile-area conversion. It rejects potential-pass or absent
production operands, duplicate/wrong occupancy identity, inconsistent shared
T10, and invalid area/interval values. No
nonlinear solve is rerun and no persistent state or owner is mutated.

Ran:

- vegetation quick suite: 186/186 passed;
- production public-water test consumes final capped carbon operands and
  rejects potential-pass operands;
- gross-versus-net and missing-tile-weight poisons pass.

## Containment

- `execute_candidate()` remains fail closed at E16--E22/multi-owner assembly;
- no persistent shared state, N request, material proposal, or owner candidate
  is emitted by this increment;
- V1--V6 model-definition bytes and the V6 solver are unchanged;
- no V6 diagnostic comparison tolerance is used by carbon aggregation.

## First Concrete Lift Action

Admit a successor immutable model identity specifying the complete E19
noncurrent-growth storage-to-E20 transfer preparation: exact source and
destination subpools, timing, carbon and nitrogen amounts, partial-transition
behavior, conservation equations, failure posture, migration, and independent
seasonal trajectory vectors. Then resume this same implementation package and
connect the already retained capped carbon operands to the shared C/N/N-owner
transaction.
