# V5 Failure-Payload Portability HOLD Legitimacy Audit

Status: `HOLD / STAGE_B_E11_E15_EXACT_ORACLE not passed / public path fail-closed`

Evidence mode: `Static + Ran`

## Exact blocked contract and evidence

`SC-VEGETATION-001` v9 requires ordinary Rust tests to consume the immutable,
digest-bound V5 failure payloads generated by the independent Python oracle.
The frozen backtracking-exhaustion payload reports:

```text
step_norm = 3925.8532969524972
```

The stable Rust implementation reports:

```text
step_norm = 3925.8544224384018
absolute delta = 0.0011254859045948251
relative delta = 2.8668568e-7
```

All categorical evidence agrees: typed hydraulic-system failure, iteration 7,
backtracking count 94, configured active-cap order, complete authorization and
layer operands, no candidate or last iterate, and byte-identical rollback.
The first numerical divergence occurs before the outer hydraulic solve in the
nested canopy humidity/energy evaluation. The Python oracle evaluates the
saturation polynomial with CPython power operations and `math.fsum`; production
Rust uses the admitted equation with Horner evaluation. Last-bit nested-state
differences are amplified by centered beta-column differencing and the failed
Newton trajectory.

V5 defines no portable cross-runtime comparison tolerance, canonical rounding
rule, or required floating-point reduction order for this failed-iterate
diagnostic. Selecting the locally observed `3e-6` relative comparison would add
an unauthorized numerical-evidence rule. The test retains that comparison only
as a clearly provisional observation; it is not Stage-B acceptance evidence.

## In-scope correction routes attempted

- Restored and verified the exact V3 uncapped arithmetic and diagnostics.
- Compared outer residual, Jacobian, pivot, backtracking, and step selection.
- Tested Rust compensated summation for the configured layer aggregate.
- Transcribed the Python saturation-polynomial power/summation form locally.
- Tested linear-solve and aggregate ordering hypotheses.
- Traced beta perturbations into nested canopy humidity, temperature, `ci`, and
  transpiration to identify the first divergence.

The experimental arithmetic changes either did not remove the mismatch or
moved the trajectory farther from the frozen payload; all were reverted. The
stable bounded implementation passes 173 vegetation tests and preserves the
frozen V3 behavior.

## Why no implementation-only route closes the gate

Forcing Rust to the CPython scalar would require an unadmitted runtime-specific
arithmetic emulation, a magic correction, or a new comparison tolerance. Each
would change canonical numerical or evidence semantics. The implementation
package cannot select any of them without constitutive/numerical authority.

## Lift action

The authority must define either a portable failure-payload comparison rule or
a canonical cross-language arithmetic/reduction rule, then freeze any required
successor model identity and regenerated independent vectors. After that lift,
rerun exact Rust fixture tests and both independent implementation reviews.

Until then:

- `STAGE_B_E11_E15_EXACT_ORACLE=NOT_PASSED`;
- Milestones 2 and 3 remain incomplete;
- `execute_candidate()` remains fail-closed;
- no request, finalized use, owner candidate, or commit is published;
- the bounded V5 identity/capped-core implementation may be checkpointed, but
  it is not public-path or science-implementation completion.
