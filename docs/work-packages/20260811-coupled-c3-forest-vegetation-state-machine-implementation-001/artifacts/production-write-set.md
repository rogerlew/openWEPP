# Production Write Set

Status: `FROZEN`

The authorized production set is root `Cargo.toml`/`Cargo.lock`; `crates/openwepp-kernel-contract/**`; new `crates/openwepp-vegetation/**` and `crates/openwepp-biogeochemistry/**`; bounded `crates/openwepp-hillslope-orchestrator/**`; the stable registry at `crates/openwepp-vegetation/model-registry/openwepp_c3_woody_v1_definition.json`; package-owned integration tests, fixtures, benchmarks, and this package tree; and bounded catalog lifecycle text. Runtime selectors, CLI defaults, canonical contracts, deployment, publication, and unrelated crates are excluded.
