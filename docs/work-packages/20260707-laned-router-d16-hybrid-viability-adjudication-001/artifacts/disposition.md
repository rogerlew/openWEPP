# Disposition

Status: EXECUTED-HOLD-HYBRID-VIABILITY. Evidence mode: Static + Ran.

Decision:

- Do not promote the current hybrid selector to default.
- Keep hybrid explicit opt-in / evidence-gathering.

Why:

- H2637 shows the method can win when exact bare-skin implicit evaluation
  applies.
- The selected cohort shows current hybrid is not no-harm: aggregate selected
  timing is slower and WA Cascades is a material regression.
- Fidelity/tolerance authority remains insufficient for active-path default
  production deltas.

Next:

- Implement an adaptive no-harm selector and/or reduce non-bare implicit
  solve cost before reconsidering promotion.
- Claude review findings are accepted as follow-on constraints: the next
  package must disambiguate selector-first staging from non-bare solve-cost
  closure, constrain predictor inputs to deterministic run-input classes, and
  predeclare promotion tolerances before tuning evidence.
