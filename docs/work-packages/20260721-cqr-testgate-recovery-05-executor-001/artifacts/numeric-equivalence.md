# Numeric Equivalence

Static: no process physics; preserve exact bytes, identities, ordering, errors,
and execution/receipt behavior.

Ran/Static: focused tests bind exact receipt identities, attempt prefix/suffix
ordering, inventory union, audit embedding, artifact paths and bytes, typed
errors, and fallback envelopes. The refactor moves complete control blocks and
passes the same references; it changes no arithmetic, schema, threshold,
subprocess argument, public API, or serialized field.
