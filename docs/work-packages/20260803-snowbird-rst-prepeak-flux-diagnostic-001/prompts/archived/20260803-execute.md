# Execute SNOWBIRD-RST-PREPEAK-FLUX-DIAGNOSTIC

Execute the frozen `rst = 0..4 deg C` sensitivity across all four retained
SNOTEL fixtures. Treat values above `1 deg C` only as best-case diagnostic
stress values. Reconstruct pre-observed-peak mass and process fluxes, preserve
additive-versus-overlapping semantics, run direct gates, and complete the
authorized independent review and verification sequence.
