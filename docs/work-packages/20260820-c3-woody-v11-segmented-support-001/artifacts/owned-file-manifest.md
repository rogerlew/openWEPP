# Owned File Manifest

Status: Phase 0 frozen

Canonical authority may edit the two vegetation contracts and index. Production
may add `v11_*` modules and bounded exports/config access, the actual default-off
orchestrator consumer, additive V11 restart types, and named integration tests.
Cargo and lifecycle files are conditional. V10 source behavior, all released
vectors, DirectV10 V1, coupled-time V2, snow science, and selectors/defaults are
protected. Exact terminal diff reconciliation is mandatory.

## Prospective implementation amendment — 2026-08-20

Additive read-only accessors in
`crates/openwepp-coupled-time/src/transaction.rs` are admitted so the first
physical adopter can bind the already-authenticated parent transaction,
segment, slab, ordinal, and duration bits. This changes no identity, wire,
acceptance, restart, or chronology semantics and is required to avoid caller
reconstruction. Focused coupled-time regression gates are mandatory.
