# Tiny-Support LSE Authority Handoff

Status: `HOLD dependency / Child 2C not yet authorized`.

Starting authority is `SC-LANDSURFACEENERGY-001@3`; do not change V10 or weaken
its nominal-support behavior. The next package must decide a deterministic,
restartable tiny-support numerical/state representation capable of closing the
same coupled canopy/ground/soil/hydraulic physics at one nanosecond. Evidence
shows predictor-only continuation and linear preconditioning are insufficient.

Required release result:

- no time/state/receipt advancement from provisional predictor attempts;
- unchanged nominal/full-support V10 outputs;
- admitted conservation treatment for sub-ULP storage increments;
- one-nanosecond actual-stack acceptance and rollback poisons;
- restart-equivalent controller/compensation state if introduced.

After that authority is released, return here to finish Restart V3 consumer
integration, implementation reviews, heavy gates, and terminal verification.
