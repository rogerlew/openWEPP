# Behavior Equivalence

Static: this non-numeric entry point preserves exact statement order and data dependencies. The refactor moves whole evidence, disposition, and serialization stages without changing Git arguments, write-set parsing, wildcard evaluation, reason ordering, JSON field values, SHA-256 inputs, or derived-ID construction. No floating-point operations exist in the seam.

Ran: characterization verifies the complete audit fields and recomputes the derived audit identity for READY, BLOCKED, and all disposition-level INVALID outcomes before and after extraction.
