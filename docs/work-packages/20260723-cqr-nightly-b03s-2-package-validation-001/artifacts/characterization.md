# Characterization

Static: characterization was added before decomposition. It binds READY, BLOCKED, and INVALID audit shapes; the ordered reason codes `SCAFFOLD_COMMIT_REQUIRED`, `BASE_WRITE_SET_SCHEMA_INVALID`, `CURRENT_WRITE_SET_SCHEMA_INVALID`, `RETROACTIVE_WRITE_SET_WIDENING`, and `UNDECLARED_CHANGED_PATH`; changed and unauthorized path ordering; current/base digests; derived audit identity; and typed invalid-path, missing-file, UTF-8, Git, and schema-read failures.

Ran: the characterization passed 1/1 before helper extraction.
