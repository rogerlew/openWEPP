# Execute Stage 3 native-vegetation laned watershed throughput recovery

Status: `QUEUED — DO NOT EXECUTE UNTIL EXPLICITLY STARTED`

Scope: local openWEPP kernel/runtime architecture and performance recovery;
flat-file reads/edits and local commands only. Do not edit wepppy or use network
services.

Execution mode: `package-end-to-end after explicit kickoff`.

Phase plan: execute every phase in `package.md` through truthful disposition.
Phase 0 and Phase 1 are mandatory pre-production gates. Do not modify a
production solver or canonical contract until their exact write set and
numeric budget/tolerance evidence satisfy the package gates.

## Required reading

Core:

- `/workdir/openWEPP/AGENTS.md`;
- `/workdir/openWEPP/docs/codex_exec_plans.md`;
- `/workdir/openWEPP/docs/defect_closure_execplans.md`;
- `/workdir/openWEPP/docs/work-packages/AGENTS.md`;
- this `package.md`;
- `artifacts/required-reading-map.md`;
- predecessor `package.md` and `v57-bounded-liquid-eligibility/r151-disposition.md`;
- `docs/standards/testing-and-gate-strategy.md`;
- `docs/standards/kernel-work-package-preparation.md`;
- `docs/standards/numerical-solver-architecture.md`;
- `docs/decisions/0044-prohibit-accretive-production-solver-dispatch.md`.

Conditional:

- `crates/AGENTS.md`, `tests/AGENTS.md`, and every nearest nested `AGENTS.md`
  before Rust/test edits;
- `docs/specifications/science-contracts/AGENTS.md`, contract authoring
  procedure, kernel process profile, and every affected `SC-*` contract before
  contract/kernel edits;
- `docs/standards/local-ci-gate-selection.md` before benchmark and validation
  execution.

On demand:

- Lane D decisions/contracts, frozen-litter package and cited PDF, exact
  v33--v57 implementation/evidence files, baseline Fortran provenance, and
  module READMEs selected by the Phase-0 inventory.

Required-reading budget at scaffold: `261999` local bytes, `OK` under the
canonical `<=400000` threshold; map: `artifacts/required-reading-map.md`.
Recompute against the frozen execution-intake files before edits. A stale
scaffold count is not authority to skip required files.

## Task

Recover Stage 3/native-vegetation/Lane D throughput for the declared watershed
scale by replacing the accretive solver architecture. Preserve the r151
worktree until Phase 0 classifies every change. Do not resume the suspended
workspace-gate package and do not create V58.

## Binding constraints

- No accretive solver dispatch. One canonical solver per physical regime;
  delete superseded production paths in the replacement increment.
- Regime selection is physical and pre-iteration. Nonconvergence produces the
  canonical adaptive response or typed error, never an older solver fallback.
- Retain Lane D per-OFE MOFE execution, native `SC-VEGETATION-001`
  RHESSys-derived vegetation/ET, non-CoE Stage 3 snow, and frozen-litter
  authority.
- Establish numeric performance budgets and physically justified tolerance
  authority before production solver edits.
- Do not require bit-exact equality for continuous numerical fixed points.
  Preserve exact discrete identity, ownership, ordering, and custody.
- Contract-first sequencing is mandatory: canonical authority, derived tests,
  recorded expected red, then production.
- No surrogate/proxy/heuristic physics, silent default, clamp, test weakening,
  compatibility wrapper, shadow-only proof, or comparator-as-target.
- Real consumer proof must trace runner input through native vegetation, Stage
  3, Lane D, accepted owner state, and final ledgers, and prove old solver/CoE/
  legacy vegetation paths are not carrying the claim.
- Conservation/output acceptance requires prospective operand lineage,
  anti-tautology fixtures, rejected wrong formulas, independent reconstruction,
  real closure/magnitude evidence, and aligned metadata.
- Execute applicable validation directly under the testing strategy; TESTGATE
  and the gate planner are historical only.

## Subagent requirement

REQUIRED for heavy batch/closure work: spawn `comparator_suite_runner` for
long-run workloads, full-workspace correctness, comparison suites, and terminal
heavy gates. This prompt explicitly authorizes subagent spawning/delegation to
`comparator_suite_runner`, `rust_code_reviewer`, `rust_qa_reviewer`, and bounded
workers for the scopes and write limits in `package.md`. Outputs are compact
metrics/log paths or package review artifacts. Do not run heavy gates on the
parent model unless the required role is unavailable and the package records
command-level evidence of that block.

## Autonomy and outputs

After explicit kickoff, execute the package end-to-end without stopping at
intake, characterization, contract-first red, a partial speedup, or an
intermediate HOLD while safe in-envelope work remains. Keep every required
artifact current and finish with exact-diff reconciliation, dual review, dual
verification, and truthful disposition. Do not create a branch, commit, push,
deploy, or edit another repository without separate authorization.
