# Stage 3 native-vegetation laned watershed throughput recovery

Status: `EXECUTED — TERMINAL HOLD; BOUNDED CORRECTNESS RETAINED; RELEASE/PERFORMANCE QUALIFICATION FAILED`

Execution mode: `package-end-to-end after explicit kickoff`

Implementation intent: `kernel/runtime architecture correction + performance qualification`

This is a living ExecPlan maintained under `docs/codex_exec_plans.md`. The
package is authorized by owner direction on 2026-09-01 as the replacement for
the suspended workspace-gate numerical work. Scaffolding does not authorize
production edits or resumption of the predecessor package.

## Progress

- [x] 2026-09-01: scaffold replacement package and roadmap entry.
- [x] 2026-09-01: freeze current source identity and dirty state; distinguish
  the clean checkpoint from the predecessor's historical dirty r151 tree.
- [x] 2026-09-01: inventory the production-reachable v33--v57 chain and the
  real runner/vegetation/Stage 3/publication path. The inventory also proved
  that accepted Stage 3 publication currently bypasses production Lane D;
  this is an in-scope consumer-path defect, not qualifying evidence.
- [x] 2026-09-01: reproduce the frozen failure on an optimized integration-test
  executable and freeze prospective engineering budgets, tolerance classes,
  and measurement protocol. Independent design review rejected the first
  draft; the package was amended before production or contract edits.
- [x] 2026-09-01: independent correctness and QA re-review accepted the amended
  package for contract-only Phase 2; release CLI characterization recorded two
  typed pre-day-zero owner-seed failures without making a throughput claim.
- [x] 2026-09-01: amend canonical cross-contract authority and record contract-
  derived expected reds for the selected replacement. Independent correctness
  and QA reviewers accepted exact tested-source digest
  `667f21801dab4acc24effaed3018a39b4540d3201acbc6a4a75cf7e3cf05cfd7`
  after the negative matrix and anti-evasion parsers were strengthened.
- [x] 2026-09-01: adjudicate the unpublished native-V2 soil/V3 boundary and
  amend authority before implementation. Candidate evaluation may consume only
  the authenticated non-owner physical beginning; publishable owner/restart
  custody is constructed once by complete final replay. Support rebinding,
  synthetic owner bytes, intermediate/dual acceptance, and proxy physics remain
  forbidden.
- [x] 2026-09-01: pass the three amended contract-authority suites, strict
  binding-exposure checks, formatting, and diff hygiene. The focused production
  seam remains expected-red solely because the typed candidate-only V3 branch
  and its required behavior proofs are not yet implemented.
- [x] 2026-09-01: adjudicate the valid snow-free V3 litter ending above liquid
  capacity. Contract v21 requires one phase-receipt-owned mass/enthalpy spill
  into current-ingress/WB14; it forbids clipping, condensation aliasing,
  duplicate supply, same-support re-solve, and guard weakening. Contract gates
  pass and the focused production seam is expected-red only on the absent typed
  spill and required behavior proofs.
- [x] 2026-09-02: adjudicate heterogeneous V3 water-protocol finalization.
  Contract v22 requires native litter-vapor rows to remain consumed only by
  their exact phase receipts and applies the complete unmatched ordinary set
  once, in canonical key order, to the accepted phase-adjusted V2 owner before
  one ingress. Native phase/spill/ice/enthalpy custody is retained; wholesale
  resource replacement, replay, retiming, and a second energy debit are
  forbidden. The exact production and focused-test paths were already present
  in the authorized write set below.
- [x] 2026-09-02: adjudicate WAT5 accepted additional-supply timing. Output
  contract v3 admits only exact receipt-bound snow-terminal, routed-runon, and
  litter-overflow segments, keeps precipitation exclusively in the rain
  hyetograph, and replays the combined piecewise supply through WB14 once.
  Aggregate-only, relabeled, retimed, duplicated, omitted, or foreign supply
  remains `WAT5-E-001`; WAT/PASS/HBP/peak/erosion/manifest and runtime state
  remain protected. The write set below was extended before production work.
- [x] 2026-09-02: adjudicate a source-complete WAT5 replay with exact zero raw
  generation but a subnormal positive accepted WB14 hour. WAT5 v4 retains the
  accepted hour using one bounded closing-ledger operand on the bin containing
  the latest canonical positive-source piece; raw rain/additional/infiltration/
  depression/generation operands and exact segment support remain unchanged.
  `TOL-WATBAL-009` validates but never mutates the hourly owner. Uniform,
  first/source-free, split, duplicated, above-bound, or source-debit repairs
  remain forbidden. Existing WAT5 production/test paths cover the amendment.
- [x] 2026-09-02: bind the WAT5 v4 output consumer. The existing v2.0 row
  schema is sufficient because the streaming writer already validates complete
  hours: it can enforce the exact typed source code, combined rain+additional
  raw closure, one bounded closing operand on the latest positive-source bin,
  and hourly closure without adding a field or weakening finite/nonnegative
  guards. Row-local acceptance without hour-final proof remains forbidden. The
  output implementation and focused tests were added to the write set below
  before production work.
- [x] 2026-09-02: bind private validated in-memory handoffs for the three
  measured custody hot paths. Surface-liquid v24 `INV-031/C-021`, LSE v24
  `INV-159/C-014`, and vegetation v31 `INV-134/C-006` permit validation-once
  reuse only for one unchanged immutable trusted in-process revision. Mutation
  consumes the proof. Restart/checkpoint, external bytes, durable publication,
  and untrusted executor returns retain fresh full validation and canonical
  reconstruction. Exact prospective production/test paths were added below;
  authority gates pass and both focused production/runtime seams are expected-
  red solely on the absent implementation and proofs.
- [x] 2026-09-02: adjudicate the first post-topology-order 10-OFE runner
  boundary. The V8 bijection guard is correct; the exact test-seed expander
  duplicated one configured vegetation occupancy across every OFE after
  rebinding only OFE/LSE tile identity. Existing vegetation authority requires
  one common BGC-bearing OFE, so the fixture correction retains the configured
  mapping only on the downstream bearing OFE and gives every other OFE a
  unique nonconfigured/open-ingress mapping. Production validators remain
  unchanged. The already-authorized seed and qualification files must prove
  valid and rollback/identity poison cases at 1/9/10/19 OFEs.
- [x] 2026-09-02: replace the accretive v33--v57 production chain with one
  canonical covered solver, delete the superseded production modules, and
  retain historical evidence only in the named nonproduction quarantine.
- [x] 2026-09-02: clarify the canonical multisecant cold-branch phase
  reconstruction after persisted-restart reappearance evidence exposed an
  impossible interpolated `W<I` trial. Positive proposed cold content uses the
  already-authoritative V22 cold branch `(I,L)=(W,0)`; this adds no clipping,
  physical evaluation, tolerance, solver regime, or fallback.
- [x] 2026-09-02: amend coupled-time authority for the measured duplicate
  acceptance-validation hot path. Contract v11 binds a private live-clock-
  revision proof, exact-clone semantics, restart invalidation, unchanged wire
  and error precedence, and move-only acceptance without reconstruction,
  owner/ledger rehashing, serialization, or payload cloning.
- [x] 2026-09-03: amend coupled-time authority for the remaining duplicate
  accepted-publication-support validation. Contract v17 binds one private,
  move-only support capability to the exact process-local publication-history
  incarnation and complete live-tail revision. The trusted append retains only
  exact cached chronology/owner-tail joins plus existing WB14 compaction and
  Arc/COW installation; wire, archive, restart, and untrusted reconstruction
  still validate every support and reconstruct the complete chronology. The
  contract-derived expected-red setup is recorded statically; no Rust edit or
  Cargo run is claimed by this contract-first increment.
- [x] 2026-09-02: remove the measured snow-free provisional/final duplicate
  physical execution. The contract-first increment permits one private,
  move-only, non-wire proof from the fully validated provisional physical
  execution to reseal only final accepted-slab identities. It retains exact
  ending-owner equality, one final publication, fresh execution after restart,
  and fail-closed rejection for stale, foreign, mutated, or reused proofs.
- [x] 2026-09-02: repair persisted adaptive-boundary WB14 posture validation.
  Represented-snow execution performs no litter/WB14 work and preserves either
  absent or pre-existing inactive parent custody byte-for-byte; a positive
  snow-free partial parent still requires one and an exactly finalized
  snow-free parent requires none. The exact cross-midnight and adaptive-
  boundary restart fixtures are the regressions.
- [x] 2026-09-02: stop initial/history covered outcomes after their fully
  validated physical endpoint. Each post-initial charge yields a private
  pending adjudication map consumed exactly once into history, typed adaptive
  rejection, or same-map `FinalAccepted` complete V8/vegetation/BGC/owner
  construction. The typed split must preserve native represented-snow identity,
  all physical operands, the maximum-eight budget, exact rollback, and one
  final publication. The exact write set includes the existing private native-
  resident and WB14 cadence test fixtures so the ordinary/native regime and
  poison matrix executes real constructors and validators; fabricated outcomes,
  counters, and fixture-only substitutes remain prohibited.
- [x] 2026-09-04: authorize exact same-evaluation reuse of the private current
  leaf state for its immediately adjacent beta-one maximum-demand call only
  when beta is bit-exact one or the successful `Inactive`/`ExactZeroPar`
  branch does not read beta. LSE contract v29 `INV-163/C-018` requires an
  exhaustive-call differential oracle, exact call counts, boundary probes,
  unchanged error precedence, and authentic release retention evidence. No
  other gas branch, cross-evaluation cache, approximate result, or fallback is
  authorized.
- [x] 2026-09-04: evaluate, then reject, exact same-solve hydraulic-probe
  recomputation. The experiment preserved bit-exact residual/Jacobian, error,
  solver, release-science, and workload-count parity, but exact-head release
  repetitions failed the target-plus-total retention rule. Its probe shortcut
  and prospective v30 authority were removed; all four hydraulic coordinates
  retain complete evaluation.
- [x] 2026-09-04: attribute the apparent 400-map physical-evidence workload.
  Direct release evidence proves 400 common-carrier maps; static call-graph and
  separate focused real-capture evidence support, but do not directly prove on
  that exact release workload, the inference of 200 provider invocations each
  executing twice. Select a contract-first invocation-local typed feed-forward
  carrier path that preserves every independent Full/Retry/Half1/Half2/Root,
  discovery/exact, batch, and canonical final evaluation while removing only
  the structurally dead second replay. Exact 200-invocation multiset evidence
  remains a mandatory postimplementation release gate.
- [x] 2026-09-04: prospectively bind revision-61 retention before production
  edits. The pre-change source/binary record is
  `carrier_static_attribution_one_ofe_release.log` with
  `provider_carrier=2_049_833 us`, `run_wall_us=4_984_488 us`, and
  `rss_kib=62_560`. Retain only when three exact CPU-0 runs of one unchanged
  postimplementation binary have medians at or below `1_299_833 us` and
  `4_234_488 us`, every run has RSS at or below `65_536 KiB`, exact science and
  `48/56/20/32/4` identity, and the exact 200-invocation multiset. Otherwise
  fully revert the revision-61 production increment.
- [x] 2026-09-04: implement, measure, reject, and fully revert the revision-61
  production increment. Three exact CPU-0 runs of source manifest `650f6713…`
  and binary `e6b57efa…` preserved exact science, `48/56/20/32/4` counts, and
  one identical 200-invocation multiset. Median provider/total times passed at
  `1_032_416/3_913_131 us`, but runs one and three used `69_768/70_484 KiB`,
  above the binding `65_536 KiB` ceiling. Timing success cannot override the
  conjunctive RSS gate; the feed-forward code and release telemetry were
  removed and the revision-61 structural test is expected red again.
- [x] 2026-09-04: derive, independently review, implement, and independently
  rereview revision-31 component-temperature dependency replay. The exact
  candidate passed focused `14/14`, full LSE `154/154`, forced-complete
  differential/full-solve parity, authentic runner/file parity, checks,
  feature diagnostics, formatting, and both implementation reviews at ordered
  16-path manifest `edc3f0b94c393e537b0115403548b779754f33308e53a26b1041932e9915be71`.
  Exact release run 1 then failed before JSON because the real aggregate had no
  authentic completed `N=2,S=6` sweep with `58/14/16/28` accounting. The
  conjunctive protocol required runs 2/3 not to run and the complete candidate
  to be reverted. Post-revert, the seven-symbol seam is expected red, LSE is
  `140/140`, all three affected crates check, and no v31 replay mechanism
  remains in production.
- [x] Close the native-V2 soil/V3, litter spill, heterogeneous join, WAT5,
  Lane-D local-only, opaque-topology WB14, and exact-enthalpy custody seams.
  The correction passed the authentic nested/outer runner, canonical covered,
  native inactive-prefix, V2 authority, and package suites; independent code
  and QA rereview approved it. Terminal exact-head regression remains part of
  Phase 5 rather than an unresolved correctness seam.
- [x] 2026-09-02: characterize one complete positive release OFE-day and record
  exact closure/counter evidence. Runtime fell from 36.377997 s to
  12.436785 s. Because the fixture is a full mixed-regime one-OFE day, that
  result is diagnostic and is not adjudicated against the pure snow-free
  250 us/OFE-day budget. This is interim characterization, not package
  qualification.
- [x] Continue in-envelope architecture work beyond the first slow day through
  the complete retained/rejected optimization campaign and measured 1/10/19
  matrix. The slow day and partial speedups were not used as a stopping point.
  The applicable prior-source 10-OFE complete-day and 19-OFE per-OFE matrix
  budgets still fail by orders of magnitude. Terminal verification rejected
  the earlier owner-choice HOLD because this package still authorized broader
  canonical architecture work. Execution therefore continued through
  revision 61 and the independently reviewed component-dependency replay.
  Both were rejected by their prospectively frozen exact release gates and
  fully reverted. The last credible bounded in-envelope route is now
  exhausted; changing the budget/workload or selecting a materially different
  solver/evaluation architecture requires new owner authority.

## Objective

Recover production throughput for the coupled non-CoE Stage 3 snow,
native-vegetation/ET, soil, and Lane D per-OFE runtime so the engine has a
credible path to typical watershed workloads of approximately 5,000
hillslopes, at least 10 OFEs per hillslope, and 100 years of climate.

The package must replace the v33--v57 accretive solver architecture with one
canonical solver per explicitly defined physical regime. It must separate
continuous numerical convergence from exact discrete identity and custody,
derive tolerances from physical/numerical error scales, bound physical-map
cost, and prove the real runner consumes the replacement path.

## Predecessor disposition and intake truth

The predecessor
`20260830-workspace-gate-hold-lift-001` is suspended, not complete. Canonical
r151 failed at `1800..1860 s` inside the frozen temperature-primary
safeguarded solve after `5:09.55` wall with peak RSS `442368 KiB`. It did not
complete one simulated day, so accepted/rejected counts, completed step-width
distribution, qualified runtime, and final water/energy closure are
unavailable. V56 focused 10/10 and V57 focused 6/6 plus contract 2/2 evidence
do not establish canonical acceptance.

The inherited worktree was reported at suspension as 90 modified and 98
untracked entries. Phase 0 must freeze the actual kickoff identity and diff;
the reported counts are historical intake evidence, not a terminal manifest.
No V58, numerical successor, replacement implementation, commit, or push was
performed by the predecessor.

## Binding architecture rule: no accretive solvers

This package applies accepted ADR-0044 and
`docs/standards/numerical-solver-architecture.md`.

Production kernels must not use accretive solver dispatch. A successor solver
may not retain earlier solver versions as sequential, eligibility-based,
convergence-based, or failure-recovery fallbacks.

- One canonical solver is allowed for each explicitly contracted physical
  regime.
- Regime selection occurs before iteration from physical state and authoritative
  domain predicates, never from solver history or failure chronology.
- A successor replaces the superseded implementation in its regime. Superseded
  production branches and selectors are deleted, not disabled or left behind.
- Nonconvergence returns a typed error or invokes the one canonical adaptive
  time-support response. It may not invoke an older solver.
- Temporary comparison implementations may exist only in test/diagnostic code,
  must be unreachable from production, and require a named deletion gate.
- Runtime version labels may describe persisted schema migrations; they may not
  encode a production cascade of historical numerical algorithms.
- Static anti-accretion tests must fail if a superseded solver becomes reachable
  from the production runner.

This rule is an owner requirement. It cannot be relaxed merely to preserve the
current worktree, pass a fixture, or avoid deleting historical implementation.

## Protected scientific and runtime requirements

The following are hard requirements throughout characterization,
implementation, and qualification:

1. **Lane D MOFE:** retain native upstream-to-downstream per-OFE Lane D state,
   transfer custody, and routing. Hillslope aggregation, daily aggregate
   substitution, or single-OFE collapse cannot carry a multi-OFE claim.
2. **Native vegetation and ET:** retain the native process implementation
   governed by `SC-VEGETATION-001`, including its admitted ecosystem-physics
   derivation and ET coupling. Legacy vegetation, PMET-only substitution,
   parameter-default shortcuts, or a surrogate canopy process cannot carry the
   production claim.
3. **Non-CoE Stage 3 snow:** Stage 3 remains the snow energy/phase process under
   qualification. CoE may be a diagnostic comparator only and cannot generate
   production melt, act as a fallback, or form a dual-owner path.
4. **Frozen litter:** retain the admitted frozen forest-litter process and
   source authority, including
   `references/vendorable/gmd-10-1621-2017-isba-meb-litter.pdf`. Performance
   work may change representation or scheduling, not substitute litter physics.
5. **Conservation and custody:** retain typed conservation, phase, topology,
   receipt, restart, rollback, event, and owner-custody invariants. Exact
   discrete identities remain exact; continuous-state convergence does not
   become bit-exact merely because receipts are authenticated.

## Numerical tolerance policy

No production solver edit may begin until `artifacts/tolerance-authority.md`
is accepted in package review and the applicable canonical contracts are
amended first.

The tolerance design must:

- distinguish nonlinear iteration stopping error, temporal truncation error,
  physical ledger closure, constitutive-domain guards, and discrete/custody
  equality;
- use dimensional absolute/relative scales tied to forcing precision,
  represented storage/flux scales, support duration, constitutive sensitivity,
  and independently reconstructed conservation limits;
- document why each tolerance is materially below the smallest process signal
  the model is expected to resolve without demanding meaningless binary64
  equality;
- prohibit exact-bit fixed points for continuous temperatures, fluxes,
  enthalpies, vapor, or CN heat unless a separately approved authority proves
  exactness is physically and numerically necessary;
- retain exact equality for identifiers, ordering, branch/event identity,
  exact-one ownership, duplicate-transfer detection, and other genuinely
  discrete invariants;
- define a small, measured physical-evaluation budget and a typed outcome when
  it is exhausted; and
- demonstrate sensitivity of accepted outputs and closure to the selected
  tolerances across frozen, mixed-phase, snow-free, wet-canopy, and Lane D
  multi-OFE regimes.

Tolerance loosening without this evidence is prohibited. Conversely, retaining
an inherited tolerance solely because it is stricter is not acceptable
authority.

## Throughput contract

Before production edits, `artifacts/performance-budget.md` must define numeric
budgets, hardware/source identity, measurement method, and pass/fail thresholds
for:

- physical-map evaluations and solver iterations per accepted OFE support;
- CPU and wall time per OFE-day for snow-free, frozen, mixed-phase, and
  thaw/refreeze workloads;
- native-vegetation/ET and Stage 3 cost shares;
- Lane D 1-, 10-, and greater-than-10-OFE scaling;
- memory per active hillslope/OFE and bounded temporary allocation;
- one complete day, one complete snow season, one complete representative
  10+-OFE year, and one complete representative 10+-OFE 100-year hillslope;
- scaling projection to at least `5,000 * 10 * 365.25 * 100 = 1,826,250,000`
  OFE-days, with explicit concurrency and I/O assumptions.

The owner-reported current posture is approximately 30,000 times too slow.
That statement is a severity bound, not a benchmark result. Phase 1 must
reproduce and normalize the gap on stable release binaries before selecting
the terminal budget. A sub-day extrapolation alone cannot qualify throughput.
The package's numeric limits are engineering qualification SLOs selected to
make the declared watershed workload tractable; they are not process-science
constants and do not alter physical legality, adaptive cadence, or
conservation. Baseline failure characterization is the Phase-1 gate. Passing
production-seam measurements is necessarily a post-implementation Phase-3/4
gate.
The 100-year single-hillslope workload must execute; the 5,000-hillslope result
may be a measured linear scaling projection because watershed scheduling is a
wepppy concern, but its assumptions and confidence bounds must be explicit.

## Defect and correction authority envelope

| ID | Observed condition | Required disposition |
| --- | --- | --- |
| `THROUGHPUT-001` | R151 consumed 309.55 seconds and failed after only 1,860 simulated seconds; the target workload is billions of OFE-days. | Establish normalized release-mode costs, select hard budgets, and make every qualified workload meet them. |
| `SOLVER-ACCRETION-001` | V33--V57 accumulated guarded numerical successors and retained prior paths. | Replace the chain with one canonical solver per physical regime and prove superseded production paths are unreachable or deleted. |
| `NUMERICS-TOLERANCE-001` | Continuous solves pursue exact receipt/state witnesses beyond useful physical resolution. | Contract physically justified convergence/closure scales and remove bit-exact continuous-state admission. |
| `R151-FROZEN-SOLVE-001` | V57 reaches the frozen temperature-primary solve, which fails before canonical completion. | Close the valid frozen regime through the replacement architecture, not V58 or another specialization. |

Allowed correction classes are profiling/benchmark instrumentation,
architecture and science-contract amendments, contract-derived tests,
replacement solver/runtime implementation, deletion of superseded solver
branches, performance-preserving data-layout/batching work, typed error and
adaptive-controller correction, restart migration if an actually published
schema requires it, and package evidence.

No implementation may use proxy physics, reduced-process stand-ins, hidden
fallbacks, threshold-only test weakening, or a compatibility wrapper as the
production closure path.

## Included scope

- exact source/diff and runtime-path inventory of the preserved worktree;
- release-mode profiling with result-blind counters;
- workload fixtures spanning snow/vegetation/Lane D regimes;
- solver architecture, numerical tolerance, evaluation-budget, and adaptive
  support policy for the coupled Stage 3/native-vegetation/LSE/soil path;
- removal of v33--v57 production dispatch and obsolete solver-only restart or
  carry surfaces after publication/migration audit;
- data-layout, allocation, batching, and repeated-evaluation elimination inside
  openWEPP;
- real runner and downstream Lane D/ledger consumer proof;
- focused, frost-profile, representative long-run, and critical terminal gates;
- dual independent review, dual verification, and exact-diff disposition.

## Excluded scope

- wepppy GIS, watershed delineation, climate acquisition, run-state, and job
  scheduling;
- replacing Lane D with hillslope aggregation or legacy MOFE routing;
- replacing native vegetation/ET with legacy vegetation, PMET-only ET, or a
  surrogate process;
- reverting snow melt/phase ownership to CoE or enabling dual CoE/Stage 3
  generation;
- changing frozen-litter constitutive physics without separate source/contract
  authority;
- empirical vegetation calibration or claims of site-specific fitness;
- weakening conservation, custody, event, topology, restart, rollback, or
  fail-closed guards to obtain speed;
- adding V58 or any new historical-version solver fallback;
- claiming the suspended workspace-gate package complete.

## Deliverables

- `artifacts/required-reading-map.md`;
- `artifacts/inherited-worktree-and-runtime-inventory.md`;
- `artifacts/solver-chain-deletion-map.md`;
- `artifacts/performance-budget.md`;
- `artifacts/tolerance-authority.md`;
- `artifacts/workload-and-benchmark-matrix.md`;
- `artifacts/operand-lineage.md`;
- `artifacts/pre-implementation-gates.md`;
- `artifacts/implementation-and-focused-validation.md`;
- `artifacts/long-run-throughput-and-closure.md`;
- `artifacts/real-consumer-proof.md`;
- `artifacts/line-count-governance.md`;
- `artifacts/gate-results.md`;
- independent `review-a.md`, `review-b.md`, `verification-a.md`, and
  `verification-b.md`;
- `artifacts/finding-disposition.md` and `artifacts/final-disposition.md`.

## Dependencies and authority

Core predecessor and governance:

- `AGENTS.md`;
- `docs/codex_exec_plans.md`;
- `docs/defect_closure_execplans.md`;
- `docs/work-packages/AGENTS.md`;
- `docs/standards/testing-and-gate-strategy.md`;
- `docs/standards/kernel-work-package-preparation.md`;
- `docs/standards/numerical-solver-architecture.md`;
- `docs/decisions/0044-prohibit-accretive-production-solver-dispatch.md`;
- `docs/work-packages/20260830-workspace-gate-hold-lift-001/package.md`;
- predecessor r151 disposition and gate results.

Process/runtime authority:

- `SC-SNOWENERGY-001`, `SC-VEGETATION-001`,
  `SC-LANDSURFACEENERGY-001`, `SC-COUPLEDTIME-001`,
  `SC-OFEROUTE-001`, `SC-RUNOFFPART-001`, `SC-WATBAL-001`,
  `SC-SURFACELIQUID-001`, `SC-SYSTEM-001`, and the science-contract index as
  selected by the exact implementation diff;
- `docs/decisions/0033-ofe-by-ofe-overland-flow-routing.md`;
- `docs/decisions/0037-abandon-hybrid-implicit-stepping.md`;
- `docs/decisions/0025-array-native-hillslope-day-frame.md`;
- `/workdir/wepp-forest_260430_baseline` at
  `dac3c950d8b16cc73774bf5ce2e7e11f80baac70` where baseline process provenance
  applies;
- `references/vendorable/gmd-10-1621-2017-isba-meb-litter.pdf` and the frozen
  forest-litter authority package.

## Exact authorized write set

Scaffold/intake writes:

- `docs/ROADMAP.md`;
- `docs/work-packages/README.md`;
- this package tree.

Phase 0 wrote only this package tree. The following exact files are authorized
for the contract-first replacement and real-consumer correction. A file is not
required to change merely because it is listed. Any additional path must be
added here before its first edit.

Canonical authority and package evidence:

- `docs/specifications/science-contracts/contracts/SC-SNOWENERGY-001.md`;
- `docs/specifications/science-contracts/contracts/SC-LANDSURFACEENERGY-001.md`;
- `docs/specifications/science-contracts/contracts/SC-COUPLEDTIME-001.md`;
- `docs/specifications/science-contracts/contracts/SC-OFEROUTE-001.md`;
- `docs/specifications/science-contracts/contracts/SC-WATBAL-001.md`;
- `docs/specifications/science-contracts/contracts/SC-SURFACELIQUID-001.md`;
- `docs/specifications/science-contracts/contracts/SC-VEGETATION-001.md`;
- `docs/specifications/science-contracts/contracts/SC-VEGETATIONTRANSACTION-001.md`;
- `docs/specifications/science-contracts/contracts/SC-RUNOFFPART-001.md`;
- `docs/specifications/science-contracts/contracts/SC-OUTPUT-WAT5-001.md`;
- `docs/specifications/science-contracts/index.md`;
- this package tree;
- `docs/ROADMAP.md` and `docs/work-packages/README.md` for terminal status only.

Contract-derived and integration tests:

- `Cargo.toml` (register the package-specific integration test only);
- `crates/openwepp-land-surface-energy/Cargo.toml`
  (declare a non-default `test-support` feature for private parity projection
  accessors only);
- `crates/openwepp-hillslope-orchestrator/Cargo.toml`
  (enable the LSE `test-support` feature only in the orchestrator
  dev-dependency/test build; production dependencies remain unchanged);
- `tests/integration/snow_terminal_enthalpy_event_numerics_contract.rs`;
- `tests/integration/solver_architecture_authority_contract.rs`;
- `tests/integration/land_surface_energy_balance_authority_contract.rs`;
- `tests/integration/surface_liquid_hydrology_custody_authority_contract.rs`;
- `tests/integration/vegetation_boundary_authority_contract.rs`;
- `tests/integration/subhourly_generation_contract.rs`;
- `tests/integration/subhourly_generation_properties.rs`;
- `tests/integration/subhourly_water_output_roundtrip.rs`;
- `tests/integration/snow_stage3_adaptive_compositional_contract.rs`;
- `tests/integration/snow_surface_eb04w_accumulation_melt_diagnostics_contract.rs`;
- `tests/integration/snow_stage3_shared_carrier_authority_contract.rs`;
- `tests/integration/snow_surface_eb03_contract.rs`;
- `tests/integration/snow_stage3_wind_source_custody_contract.rs`;
- `tests/integration/snow_stage3_turbulent_operator_reconciliation_contract.rs`;
- `tests/integration/snow_stage3_terminal_receiver_authority_contract.rs`;
- `tests/integration/dff_ws2_ksatadj_direct_runtime.rs`;
- `tests/integration/laned_shadow_h2637.rs`;
- `tests/integration/stage3_native_vegetation_laned_throughput_recovery.rs`;
- exact files under `tests/fixtures/erosion_multi_ofe_p102/` only when required
  to construct the checked-in 10+-OFE Stage 3/Lane D qualification fixture.

Canonical solver replacement and deletion:

- `crates/openwepp-hillslope-orchestrator/src/v11_covered/mod.rs`;
- `crates/openwepp-hillslope-orchestrator/src/v11_covered/receipt_sets.rs`
  (test-only query for whether exhaustive provisional-physical audit capture
  is actually armed; the audit guard, payload, and assertions are unchanged);
- `crates/openwepp-hillslope-orchestrator/src/v11_covered/execution.rs`;
- `crates/openwepp-hillslope-orchestrator/src/v11_covered/fixed_point.rs`;
- `crates/openwepp-hillslope-orchestrator/src/v11_covered/open_snow.rs`;
- `crates/openwepp-hillslope-orchestrator/src/v11_covered/open_snow_physical_support.rs`;
- `crates/openwepp-hillslope-orchestrator/src/v11_covered/open_snow_receipt_reseal_helpers.rs`
  (shared final-map receipt resealing used by the one canonical covered solver);
- `crates/openwepp-hillslope-orchestrator/src/v11_covered/regime.rs`;
- `crates/openwepp-hillslope-orchestrator/src/v11_covered/open_snow_convergence_metrics.rs`;
- `crates/openwepp-hillslope-orchestrator/src/v11_covered/canonical_covered_solver.rs`;
- `crates/openwepp-hillslope-orchestrator/src/v11_covered/canonical_covered_solver_test_audit.rs`
  (behavior-preserving extraction of covered-map test audit and poison support
  from the touched over-3,000-line canonical solver only);
- `crates/openwepp-hillslope-orchestrator/src/v11_covered/carrier_phase.rs`
  (diagnostic-only split of complete covered-carrier execution into its
  physical-prefix and complete-owner finalization costs under the existing
  opt-in parent telemetry; the private move-only physical result also carries
  the already validated exact V9-to-V8 vegetation projection from the physical
  prefix into its immediate complete-envelope continuation, avoiding a second
  projection of the unchanged beginning; no cross-call/restart reuse and no
  state, publication, or solver effect; correct native-V2 stale-carry poison
  vectors to mutate the private typed trial after its seal is minted, because
  the canonical joint intentionally retains the complete resident soil owner
  and therefore cannot distinguish unpublished trial alternatives by bytes;
  mint a private non-Clone/non-wire borrowed proof after exact parent-lineage
  normalization verifies the V10 identity, lineage fields, and recomputed
  canonical digest, consume it before any mutation to construct endpoint owner
  bytes once, and move the candidate with those exact bytes; add
  diagnostic-only joint-seal/custody/candidate-byte subphase attribution under
  the existing opt-in guard; replace separable physical soil
  candidate/continuation fields with one private move-only, non-wire token
  minted only after producer authentication plus exact transaction,
  predecessor, support, original-owner, and continuation-posture checks, then
  consume that token at the complete endpoint without replaying the same soil
  custody validation; mint a private borrowed soil-read capability only after
  the sealed carrier candidate matches its immutable V1 value or V2 unpublished-
  trial digest, bind it by pointer identity to that exact candidate clone, and
  pass it through the same physical call so live vegetation projection does not
  revalidate/recanonicalize the unchanged ending state; ordinary/restart/
  external soil validation is unchanged; extract the unchanged snow-boundary
  projection implementation into the private sibling below to restore the
  touched-file 3,000-line hard ceiling without changing visibility or module
  ownership);
- `crates/openwepp-hillslope-orchestrator/src/v11_covered/carrier_phase/snow_boundary.rs`
  (behavior-preserving private extraction of `CoveredSnowBoundaryStateV1` and
  its existing boundary/trial projection methods from `carrier_phase.rs`; no
  equation, error, visibility, or call-site change);
- `crates/openwepp-hillslope-orchestrator/src/v11_covered/carrier_physical_phase.rs`
  (single shared covered physical-prefix implementation extracted to eliminate
  duplicate live physical engines and keep the touched carrier below the hard
  line ceiling);
- `crates/openwepp-hillslope-orchestrator/src/v11_covered/carrier_engine.rs`
  (construct the expensive test-only canonical physical projection,
  arbitration-sensitivity replay, and cloned audit payload only while the
  corresponding thread-local audit guard is armed; production execution and
  guarded audit evidence remain unchanged; thread the private borrowed exact
  V9-to-V8 projection proof from carrier receipt construction into the
  immediately adjacent native physical preparation; thread the candidate-bound
  borrowed soil-read capability through that same call without exposing it or
  permitting mutation/restart reuse; diagnostic-only attribution of the first
  physical-evidence construction and surface/WB14 replay copy/hash stages under
  the existing opt-in release guard, with no production behavior change);
- `crates/openwepp-hillslope-orchestrator/src/v11_covered/physical_outcome_ledger.rs`
  (diagnostic-only attribution of terminal snow/soil receipt construction and
  its immediate validation/reseal under the opt-in release guard; no receipt,
  error, serialization, or production-control change);
- `crates/openwepp-hillslope-orchestrator/src/v11_covered/owner_finalization.rs`;
- `crates/openwepp-hillslope-orchestrator/src/v11_covered/owner_finalization_nitrogen_protocol_tests.rs`;
- `crates/openwepp-hillslope-orchestrator/src/v11_covered/owner_finalization_v50_transition.rs`;
- `crates/openwepp-hillslope-orchestrator/src/v11_covered/phase_consistent_coupled_solve.rs`;
- `crates/openwepp-hillslope-orchestrator/src/v11_covered/phase_consistent_temperature_primary.rs`;
- `crates/openwepp-hillslope-orchestrator/src/v11_covered/phase_consistent_private_q_lattice.rs`;
- `crates/openwepp-hillslope-orchestrator/src/v11_covered/stable_monotone.rs`;
- `crates/openwepp-hillslope-orchestrator/src/v11_covered/open_snow_convergence_tests.rs`;
- `crates/openwepp-hillslope-orchestrator/src/v11_covered/open_snow_convergence_v44_tests.rs`;
- `crates/openwepp-hillslope-orchestrator/src/v11_covered/open_snow_convergence_v45_tests.rs`;
- `crates/openwepp-hillslope-orchestrator/src/v11_covered/open_snow_convergence_v46_tests.rs`;
- `crates/openwepp-hillslope-orchestrator/src/v11_covered/open_snow_convergence_v51_tests.rs`;
- `crates/openwepp-hillslope-orchestrator/src/v11_covered/open_snow_convergence_v52_tests.rs`;
- `crates/openwepp-hillslope-orchestrator/src/v11_covered/open_snow_convergence_v53_tests.rs`;
- `crates/openwepp-hillslope-orchestrator/src/v11_covered/open_snow_convergence_v54_tests.rs`;
- `crates/openwepp-hillslope-orchestrator/src/v11_covered/open_snow_convergence_v55_tests.rs`;
- `crates/openwepp-hillslope-orchestrator/src/v11_covered/open_snow_convergence_v56_tests.rs`;
- the preceding version-specific tests are authorized for deletion or
  replacement by equation/regime/guard tests;
- `crates/openwepp-hillslope-orchestrator/src/v11_covered/open_snow_superseded_historical_solver_tests.rs`;
- `crates/openwepp-hillslope-orchestrator/src/v11_covered/open_snow_tail_tests.rs`;
- `crates/openwepp-hillslope-orchestrator/src/snow_stage3_v11_adaptive_execution_tests.rs`;
- `crates/openwepp-hillslope-orchestrator/src/snow_stage3_v11_adaptive_execution.rs`;
- `crates/openwepp-hillslope-orchestrator/src/snow_stage3_v11_adaptive_execution_stack_helpers.rs`;
- `crates/openwepp-hillslope-orchestrator/src/snow_stage3_v11_restart.rs`;
- `crates/openwepp-hillslope-orchestrator/src/snow_stage3_v11_restart_tests.rs`
  (behavior-preserving extraction of the adaptive trial-grid unit tests from
  the touched restart host to keep it below the 3,000-line hard ceiling);
- `crates/openwepp-hillslope-orchestrator/src/snow_stage3_v11_terminal_execution.rs`
  (diagnostic-only terminal candidate setup, provider custody/projection,
  physical carrier, retention, and result-finalization attribution under the
  existing opt-in parent telemetry; no state or controller effect; compare
  the exact same-call validated typed WB14 parent fields for nonfinal child
  retention instead of serializing and reparsing both complete owner sets;
  restart, external, and mutable boundaries remain unchanged);
- `crates/openwepp-hillslope-orchestrator/src/snow_stage3_v11_terminal_precomputed_package.rs`
  (test-only timing attribution for authentic precomputed-terminal-package
  validation, payload assembly/cloning, and authority sealing before any
  shared-payload design is selected; no production behavior or custody change);
- `crates/openwepp-hillslope-orchestrator/src/snow_stage3_v11_terminal_execution_preterminal_replay.rs`
  (typed authenticated native-inactive-marker versus ordinary WB14 replay
  dispatch at the real preterminal replay boundary; no synthesized replay or
  represented-snow WB14 physics);
- `crates/openwepp-hillslope-orchestrator/src/snow_stage3_v11_terminal_execution_batch.rs`
  (typed native-inactive-marker versus ordinary WB14 replay dispatch in the
  simultaneous-terminal batch path; no synthesized replay or physics);
- `crates/openwepp-hillslope-orchestrator/src/snow_stage3_v11_terminal_execution_tests.rs`;
- `crates/openwepp-hillslope-orchestrator/src/snow_stage3_v11_terminal_event_application.rs`;
- `crates/openwepp-persisted-restart-v1/src/snow_stage3_v11.rs`;
- `crates/openwepp-persisted-restart-v1/src/snow_stage3_v11_tests.rs`;
- one new canonical solver module in `v11_covered/`, with its exact filename
  recorded before creation if decomposition is required: fulfilled by
  `canonical_covered_solver.rs` above.
- `crates/openwepp-hillslope-orchestrator/src/v9_real_consumer_shadow_v11_error.rs`;
- `crates/openwepp-hillslope-orchestrator/src/v9_real_consumer_shadow.rs`
  (diagnostic-only attribution of the enclosing snow-free imported-stack
  validation, physical candidate, accepted-candidate, owner/publication seal,
  and install stages under the existing opt-in parent telemetry guard; these
  measurements never enter identity, restart, publication, or control flow;
  consume the exact private validated V11 parent-finalization handoff at the
  immediately adjacent trusted install instead of repeating full V11/V10/V9
  semantic validation; transaction, configuration, BGC, and owner joins remain
  freshly checked, and ordinary/untrusted/restart/external paths are unchanged);
- `crates/openwepp-hillslope-orchestrator/src/v9_real_consumer_shadow/live_vegetation_forcing.rs`
  (behavior-preserving same-module extraction of the existing live vegetation-
  forcing projection from the touched `v9_real_consumer_shadow.rs` hard-ceiling
  host; no signature, visibility, equation, or error change);
- `crates/openwepp-hillslope-orchestrator/src/v9_real_consumer_shadow_v10_accessors.rs`
  (crate-private borrowed access to the exact typed WB14 parent field for the
  immediately adjacent validated terminal-child retention comparison; no
  public, wire, restart, clone, or mutation capability);
- `crates/openwepp-hillslope-orchestrator/src/v9_real_consumer_shadow/accepted_publication_support_capability.rs`
  (new private move-only, non-Clone, non-wire validated-support capability,
  exact publication-history incarnation/live-revision binding, numeric-
  precedence joins, and trusted consuming append seam; no public or serialized
  compatibility surface);
- `crates/openwepp-hillslope-orchestrator/src/v9_real_consumer_shadow/accepted_publication_support_capability_tests.rs`
  (new expected-red-first validation-once counters, exhaustive live-tail poison,
  stale/foreign/replay/restart/rotation, untrusted-restore, non-wire, exact-byte
  rollback, and fresh-success vectors);
- `crates/openwepp-hillslope-orchestrator/src/v9_real_consumer_shadow/frozen_litter_v3_adoption.rs`
  (mint a private borrowed, non-wire exact V9-to-V8 projection proof and admit
  it only against pointer-identical immutable V9 configuration/state during
  the same native physical call; ordinary, independently supplied, restart,
  and external projection paths retain full validation; consume the private
  candidate-bound soil-read capability only after the existing unpublished-
  continuation custody checks and only for its exact pointer-identical V2
  candidate, while all ordinary/independent calls retain full read validation;
  diagnostic-only attribution of effective-surface hydrology-frame cloning and
  snapshot construction under the opt-in release guard);
- `crates/openwepp-hillslope-orchestrator/src/vegetation_real_hydrology_shadow.rs`
  (diagnostic-only attribution of the adapter's same-call complete-frame clone
  and validation under the opt-in release guard; no hydrology state, authority,
  error, or production-control change);
- `crates/openwepp-hillslope-orchestrator/src/v9_real_consumer_shadow/frozen_litter_v3_publication_retention.rs`
  (the private typed constructor validates its derived support directly
  against the already validated complete-owner projection and typed litter
  receipts, avoiding immediate serialize/reparse validation; canonical-byte,
  restore, external, restart, and independently supplied support paths retain
  full parse, replay, canonicalization, and semantic validation);
- `crates/openwepp-hillslope-orchestrator/src/v9_real_consumer_shadow/frozen_litter_v4_adoption.rs`
  (diagnostic-only snow-free V4 execution setup, core runtime call, and
  resident-acceptance attribution under the existing opt-in parent telemetry;
  no state, validation, control-flow, or physics effect; consume the exact
  same-call V9-to-V8 projection proof during represented-snow physical-only
  preparation instead of projecting the unchanged shadow twice; carry the
  same-call candidate-bound soil-read capability into that preparation without
  creating a public, wire, restart, cloned, or mutable proof surface);
- `crates/openwepp-hillslope-orchestrator/src/v9_real_consumer_shadow/v10_soil_thermal_v2.rs`;
- `crates/openwepp-hillslope-orchestrator/src/v9_real_consumer_shadow/v10_soil_thermal_v2_unpublished_continuation.rs`;
- `crates/openwepp-hillslope-orchestrator/src/v9_real_consumer_shadow/v10_soil_thermal_v2_tests.rs`;
- `crates/openwepp-hillslope-orchestrator/src/v9_real_consumer_shadow/v10_soil_thermal_v2_v49_tests.rs`;
- `crates/openwepp-hillslope-orchestrator/src/v9_real_consumer_shadow/v10_soil_thermal_v2_resident_install_tests.rs`;
- `crates/openwepp-hillslope-orchestrator/src/v9_real_consumer_shadow/v3_publication_retention_tests.rs`;
- `crates/openwepp-hillslope-orchestrator/src/v9_real_consumer_shadow_wb14_tests.rs`;
- `crates/openwepp-hillslope-orchestrator/src/v9_real_consumer_shadow_wb14_covered_physical_tests.rs`
  (behavior-preserving extraction of the covered physical parity/failure
  fixture family from the touched over-3,000-line WB14 test module only);
- `crates/openwepp-hillslope-orchestrator/src/v9_real_consumer_shadow_wb14_routing_tests.rs`
  (real ordinary accepted-support routing proof that one constructor validation
  and one capability mint feed one exact-revision/tail joined append with zero
  append-time validation, serialization, full-prefix scan, or payload clone);
- `crates/openwepp-hillslope-orchestrator/src/land_surface_energy_shadow/v3_execution.rs`
  (consume the crate-private atomic V4 construction/canonical-byte result at
  same-call native publication sites, and trust the full validation performed
  by the private-field V3 projection constructors instead of serializing,
  reparsing, comparing, and discarding that exact unchanged value; these
  same-call corrections cross no mutation, restart, external, or untrusted
  boundary, and both public canonical parsers remain unchanged);
- `crates/openwepp-hillslope-orchestrator/src/canonical_owner_bytes.rs`
  (diagnostic-only nested attribution of vegetation validation, hydrology
  projection, surface canonicalization, and vegetation/surface/soil encoding
  exclusively inside covered-carrier endpoint owner-byte construction under
  the existing opt-in parent telemetry guard; consume a private borrowed
  same-reference proof for the normalized V10 vegetation owner at that exact
  endpoint without repeating reverse V9/V8 validation, while ordinary,
  restart, external, and independently supplied owner-byte callers retain full
  V10 validation and remain unprofiled; no byte, state, or control-flow effect);
- `crates/openwepp-hillslope-orchestrator/src/land_surface_energy_shadow/v8_projection.rs`
  (mint and consume the private validated V8 configuration/beginning handoff
  only within one projection call; no cache, wire surface, or cross-mutation
  reuse);
- `crates/openwepp-hillslope-orchestrator/src/land_surface_energy_shadow/v8_input_projection.rs`
  (compute the fully validated immutable V2 soil-beginning snapshot digest
  once per projection call and thread that exact value through cross-owner
  admission and the returned private projection; no restart, external, or
  untrusted boundary reuse);
- `crates/openwepp-hillslope-orchestrator/src/land_surface_energy_shadow/covered_parent_structural_admission.rs`
  (prospective private parent-generation structural admission, per-map forcing
  proof, and borrowed native proof sourced only from the exact
  `FrozenLitterV3Resident` validated revision at the existing native-validation
  position; V8 does not attest the distinct resident V3/V2 objects, immutable
  configuration/topology may be admitted once per parent, and dynamic owner
  state remains freshly validated without cross-map caching; no
  wire/restart/public API or physical-result cache);
- `crates/openwepp-hillslope-orchestrator/src/land_surface_energy_shadow/v3_input_projection.rs`;
- `crates/openwepp-hillslope-orchestrator/src/land_surface_energy_shadow/covered_v8_owner.rs`;
- `crates/openwepp-hillslope-orchestrator/src/land_surface_energy_shadow/multi_tile_runtime.rs`
  (test-only exhaustive canonical projection of the private multi-tile
  physical result for covered-map parity; no production behavior or public
  serialization surface);
- `crates/openwepp-hillslope-orchestrator/src/land_surface_energy_shadow/v3_multitile_adoption.rs`
  (mint a private borrowed nonserializable proof after the native V3 solver
  projection fully validates the exact immutable SurfaceLiquid V2
  configuration/owner pair, and consume that same-reference proof in the
  immediately following potential authorization without a second owner
  serialization; mutation and trust boundaries remain unchanged);
- `crates/openwepp-hillslope-orchestrator/src/land_surface_energy_shadow/strict_v8_endpoint.rs`
  (diagnostic-only native V3 projection/topology, potential, authorization,
  and fixed-final wall-time attribution under the existing opt-in parent
  telemetry guard; telemetry never enters state, receipts, publication, or
  controller decisions);
- `crates/openwepp-hillslope-orchestrator/src/land_surface_energy_shadow/real_hydrology_execution.rs`
  (diagnostic-only represented-snow unified-hydrology entry/arbitration,
  finalization, inactive-surface, receiver, and candidate-validation timing
  under the existing opt-in parent guard; no state or decision effect; consume
  the exact private validated inactive-resource capability through the
  immediately adjacent ingress construction before recovering its owned
  candidate, without crossing a mutation, restart, or external boundary);
- `crates/openwepp-hillslope-orchestrator/src/land_surface_energy_shadow/v3_tests.rs`;
- `crates/openwepp-land-surface-energy/src/litter_phase.rs`;
- `crates/openwepp-land-surface-energy/src/litter_phase_output.rs`;
- `crates/openwepp-land-surface-energy/src/litter_phase_closure.rs`;
- `crates/openwepp-land-surface-energy/src/litter_phase_tests.rs`;
- `crates/openwepp-land-surface-energy/src/transaction.rs`
  (retain the `cfg(test)`-only owning-crate canonical projection accessors for
  otherwise private potential-phase beginning, accepted, root-identity, and
  gas-branch state required by exhaustive covered-map parity; also make the
  public potential-request constructor perform its unchanged content checks
  against the just-constructed immutable value without immediately recomputing
  its canonical digest, while public validation and every mutation, restart,
  or external boundary retain a fresh full digest check; no public
  serialization surface);
- `crates/openwepp-land-surface-energy/src/transaction_tests.rs`
  (constructor-domain and post-construction mutation/digest regression proof
  for the validation-once request-batch construction path);
- `crates/openwepp-land-surface-energy/src/solver_covered_evaluation.rs`
  (factor immutable covered-column/cap/shortwave/Stage-3-boundary admission
  into a private borrowed validation proof and let the one canonical Newton
  solve reuse it across its finite-difference and backtracking evaluations;
  trial-shape/domain checks remain fresh per evaluation, public evaluation
  retains full admission, and equations, order, convergence, and errors remain
  unchanged; expose only the private validated Stage-3 identity-anchor operands
  needed by the same canonical solve and a test-only full-evaluation counter;
  within one occupancy evaluation only, reuse a successful private leaf state
  for its adjacent beta-one maximum-demand result when the beta operand is
  bit-identical or the returned branch is proven beta-independent, retaining
  the complete beta-one call in every other case and test-only call auditing;
  retain the unchanged factored hydraulic block only inside the complete
  evaluator after rejecting its prospective probe consumer; no public,
  restart, serialized, alternate-evaluator, or hydraulic shortcut authority);
- `crates/openwepp-land-surface-energy/src/solver_covered_solve.rs`
  (mint the same-call immutable evaluation-input proof after the existing
  initial-trial guard and consume it only within the current canonical solver;
  for represented-snow ground and soil identity-anchor Jacobian columns only,
  construct and domain-check the unchanged canonical minus-then-plus probes,
  assemble their complete normalized residual vectors from the bit-identical
  base residuals plus the one exact anchor residual, and pass those values
  through the unchanged finite-difference stencil; every other column and all
  base/backtracking evaluations retain the full evaluator, with no cache,
  mutation, restart, external, alternate-solver, analytic derivative, or
  fallback path);
- `crates/openwepp-land-surface-energy/src/solver_component_dependency_replay_test_support.rs`
  (non-default `test-support`/test-only result-blind projection and scoped
  forced-complete audit for authentic potential/final solve comparison; no
  default-build, wire, restart, or production-control surface);
- `crates/openwepp-land-surface-energy/src/solver_tests.rs`
  (contract-derived validation-count, exact-result, invalid-input, and public-
  evaluation regression proof for the private same-solve handoff; add exact
  full-evaluator differential/Jacobian parity and full-evaluation-count proof
  for the represented-snow ground/soil identity-anchor columns, including
  closed-bound stencils and dependency-invalidating poisons; add exact
  exhaustive-versus-reuse leaf-state/evaluation/solve parity, call-count,
  beta-boundary, branch, and error-precedence vectors; preserve complete-call
  hydraulic differential/error vectors as a guard against reviving the
  rejected shortcut);
- `crates/openwepp-land-surface-energy/src/solver_tests/component_dependency_replay.rs`
  (same-module extraction of the revision-31 graph, exact projection, privacy,
  capability, and parity evidence from the near-ceiling solver test host);
- `crates/openwepp-land-surface-energy/src/solver_tests/component_dependency_replay_execution.rs`
  (independently selectable graph-execution, rainy multirank, all-sign,
  potential/final, dense-trajectory, and audit evidence);
- `crates/openwepp-land-surface-energy/src/solver_tests/component_dependency_replay_integrity.rs`
  (independently selectable source-real error/rollback, noncrossable boundary,
  pre-admission, move-only capability, lifetime, and custody evidence);
- `crates/openwepp-land-surface-energy/src/transaction_v3.rs`;
- `crates/openwepp-land-surface-energy/src/transaction_v3_tests.rs`;
- `crates/openwepp-land-surface-energy/src/exact_dyadic_enthalpy_v56_tests.rs`;
- `crates/openwepp-land-surface-energy/src/soil_thermal_v2_cutover_tests.rs`;
- `crates/openwepp-land-surface-energy/src/numerics.rs`;
- `crates/openwepp-land-surface-energy/src/owner_envelope.rs`;
- `crates/openwepp-land-surface-energy/src/transaction_v3_bridge.rs`
  (warnings-denied affected-LSE lint reconciliation only: format long test
  literals, use a checked bounded test-index conversion, and explicitly retain
  the existing cohesive 103--111-line numerical/owner construction functions;
  no runtime equation, branch, error, tolerance, or test-value change);
- `crates/openwepp-land-surface-energy/src/v2_state.rs`
  (consume the contract-authorized private same-call validation proof after
  exact V1/V2 configuration-payload admission; construct the deterministic V2
  state once without recursively revalidating the unchanged source payload;
  public/untrusted V2 validation and restart paths remain unchanged);
- `crates/openwepp-hillslope-orchestrator/src/land_surface_energy_shadow/v4_restart_evidence.rs`;
- `crates/openwepp-hillslope-orchestrator/src/direct_runtime/surface_liquid_owner/v3_exact_enthalpy.rs`;
- `crates/openwepp-hillslope-orchestrator/src/direct_runtime/surface_liquid_owner/v3_projection.rs`;
- `crates/openwepp-hillslope-orchestrator/src/direct_runtime/surface_liquid_owner/v4_projection.rs`
  (add a crate-private atomic constructor that serializes the exact V4 value
  immediately after `new` completes its full validation; typed construction
  performs the same nested identity, mirror, operand, and receipt checks
  directly on the supplied typed values instead of serializing and immediately
  reparsing them; canonical-byte validation, mutation rejection, restart, and
  external parse surfaces retain their complete replay checks);
- `crates/openwepp-hillslope-orchestrator/src/direct_runtime/surface_liquid_owner/v3_projection_tests.rs`;
- `crates/openwepp-hillslope-orchestrator/src/direct_runtime/surface_liquid_owner/identity_validation.rs`
  (shared exact owner-identity validation used by the authenticated native
  prefix/successor join);
- `crates/openwepp-hillslope-orchestrator/src/direct_runtime/surface_liquid_owner.rs`
  (return the specialized represented-snow zero-work resource as a private,
  owning, nonserializable validated capability after its existing full
  validation; public resource validation and ordinary construction remain
  unchanged; extract unchanged scalar/restart helper functions into the private
  same-module sibling below solely to restore the hard line ceiling);
- `crates/openwepp-hillslope-orchestrator/src/direct_runtime/surface_liquid_owner/validation_helpers.rs`
  (behavior-preserving same-module extraction of existing validation helpers;
  no signature, visibility, taxonomy, arithmetic, or call-site change);
- `crates/openwepp-hillslope-orchestrator/src/direct_runtime/surface_liquid_owner/resource_validation.rs`
  (test-only exhaustive surface-resource projection required by opaque
  covered-map parity; no production behavior or public wire surface);
- `crates/openwepp-hillslope-orchestrator/src/direct_runtime/surface_liquid_owner/v2_ingress_adapter.rs`;
- `crates/openwepp-hillslope-orchestrator/src/direct_runtime/surface_liquid_owner/v2_ingress_tests.rs`;
- `crates/openwepp-hillslope-orchestrator/src/direct_runtime/surface_liquid_closure.rs`;
- `crates/openwepp-hillslope-orchestrator/src/direct_runtime/surface_liquid_closure_terminal_validation.rs`;
- `crates/openwepp-hillslope-orchestrator/src/direct_runtime/surface_liquid_attachment.rs`;
- `crates/openwepp-hillslope-orchestrator/src/direct_runtime/surface_liquid_ingress_tests.rs`;
- `crates/openwepp-hillslope-orchestrator/src/snow_stage3_v11_adaptive_frontend.rs`;
- `crates/openwepp-persisted-restart-v1/src/frozen_litter_v3_projection.rs`;
- `crates/openwepp-persisted-restart-v1/src/gsi_forcing.rs`;
- `crates/openwepp-persisted-restart-v1/src/surface_liquid.rs`;
- `crates/openwepp-vegetation/src/transaction/state_canonical.rs`
  (byte-identical allocation reduction in the released canonical state
  encoder: reuse one recursive path buffer and write scalar encodings directly;
  released canonical preimage, digest, and mutation vectors remain unchanged);
- `crates/openwepp-vegetation/src/v10_state.rs`
  (after full exact V10-configuration and reconstructed V9-source validation,
  admit the same-call constructed V10 state by its derived identity, canonical
  digest, and explicit initial-state receipt invariant without immediately
  reversing and revalidating the unchanged payload; ordinary public V10
  validation, migration, external, and restart paths remain unchanged; expose
  the existing unchecked V10-to-V9 identity projection only crate-internally so
  the fully validated V11 parent-finalization handoff can retain its already
  proven derived V9 revision without a second semantic replay);
- `crates/openwepp-vegetation/src/v8_persistent.rs`
  (private immutable validation-once construction of the potential/capped/final
  V8 receipt-pass family for one exact configuration/beginning revision;
  ordinary public constructors and every external/restart/mutated boundary
  retain full validation);
- `crates/openwepp-vegetation/src/v8_candidate.rs`
  (consume the same exact validated configuration/beginning revision while
  admitting the final V8 tile pass; final-receipt lineage and payload checks
  remain fresh and unchanged);
- `crates/openwepp-vegetation/src/v11.rs`
  (carry the already fully validated pre-lineage and lineage-mutated parent
  revisions in one private move-only, non-wire finalization handoff and consume
  it only against the exact configuration, beginning V10 state, and finalized
  V11 state; retain the public independently validated projection unchanged);
- `crates/openwepp-vegetation/src/v11_lse_receipt_impl.rs` (temporary exact
  beginning-owner diagnostic and any proven same-scope receipt correction);
- `crates/openwepp-vegetation/src/v11_tests.rs` (behavior-preserving extraction
  of the touched over-3,000-line `v11.rs` test module only);
- `crates/openwepp-hillslope-orchestrator/src/snow_stage3_v11_parent_chronology_tests.rs`;
- `crates/openwepp-hillslope-orchestrator/src/snow_stage3_v11_real_parent_execution.rs`;
- `crates/openwepp-hillslope-orchestrator/src/snow_stage3_v11_attachment_helpers.rs`
  (trusted in-process parent-finalization install variant that consumes the
  exact vegetation handoff; the ordinary helper retains full validation);
- `crates/openwepp-hillslope-orchestrator/src/snow_stage3_v11_adaptive_production_tests.rs`
  (real adaptive accepted-parent support publication proof for validation-once
  capability custody, exact counters, typed atomic refusal, and unchanged
  selected-parent-only publication chronology; bind the real two-active-lane
  terminal-plus-survivor fixture to a nearby fixed snow amount that actually
  converges at minimum support, after bounded diagnostic evidence showed the
  former amount legitimately violates dependent-carrier tolerances; no solver
  tolerance or adaptive response changes);
- `crates/openwepp-hillslope-orchestrator/src/snow_stage3_v11_attachment.rs`
  (result-blind covered-map role/envelope counters and reuse of the existing
  wall-time profile recorder for enclosing imported-stack attribution only if
  required by the final-complete telemetry proof);
- `crates/openwepp-hillslope-orchestrator/src/snow_stage3_v11_attachment_telemetry.rs`
  (behavior-preserving extraction of the existing detailed telemetry plus a
  bounded aggregate qualification mode; no solver selection or physics
  effect);
- `crates/openwepp-hillslope-orchestrator/src/snow_stage3_v11_profile.rs`
  (opt-in, nonserialized qualification state for accepted physical-map counts,
  ordered Lane-D calls, balanced exclusive subsystem timing buckets, and the
  enclosing imported-stack phase buckets, including diagnostic-only snow-free
  physical setup, frozen-litter fixed-input preparation, execution/acceptance,
  envelope construction, final validation, and complete-owner serialization
  subphases; no production control-flow selection or physics effect);
- `crates/openwepp-hillslope-orchestrator/src/snow_stage3_v11_attachment_receipts.rs`
  (authenticated represented-snow inactive child-custody marker validation;
  the native physical-only path must not fabricate a parseable WB14 replay);
- `crates/openwepp-hillslope-orchestrator/src/snow_stage3_v11_attachment_runtime.rs`
  (result-blind observation at the actual non-fallible accepted-parent adoption
  boundary and rejection-safe pending-state ordering);
- `crates/openwepp-hillslope-orchestrator/src/snow_stage3_v11_attachment_tests.rs`
  (behavior-preserving relocation of the attachment host's existing
  `cfg(test)` fixed-point tests when required to keep that touched production
  host below the 3,000-line hard ceiling);
- `crates/openwepp-hillslope-orchestrator/src/v9_real_consumer_shadow_publication_retention.rs`
  (support constructor full validation/sealing, private live-revision issuance,
  consuming trusted append, exact cached tail join, mutation revision advance,
  full untrusted/restart reconstruction, and result-blind test audit counters;
  existing WB14 compaction, Arc/COW install, wire bytes, and publication order
  remain unchanged);
- `crates/openwepp-hillslope-orchestrator/src/v9_real_consumer_shadow_publication_rotation_tests.rs`
  (rotation/replacement and event/support revision invalidation, stale
  capability refusal, full untrusted reconstruction, fresh-incarnation success,
  archive error precedence, and byte-identical rollback vectors);
- `crates/openwepp-hillslope-orchestrator/src/land_surface_energy_shadow/mod.rs`;
- `crates/openwepp-hillslope-orchestrator/src/lib.rs`
  (private module registration required by the canonical covered-solver
  decomposition; no public compatibility surface);
- `crates/openwepp-hillslope-orchestrator/src/v9_real_consumer_shadow/direct_v9_real_consumer_shadow_impl.rs`
  (typed physical-only carrier routing only if the existing module boundary
  cannot expose the authenticated endpoint without it);
- `crates/openwepp-hillslope-orchestrator/src/v9_real_consumer_shadow/snow_free_physical_reuse.rs`
  (snow-free identity-only reseal must complete exactly one support validation
  and operand/receipt seal before minting the fresh publication-support
  capability; diagnostic-only validation/reseal/install attribution uses the
  existing opt-in parent telemetry; physical-reuse custody and
  no-physical-replay authority remain unchanged);
- `crates/openwepp-hillslope-orchestrator/src/v11_vegetation_consumer.rs`
  (result-blind snow-free reuse publication observation at the sole real
  `accept_direct_v11_segment` parent-acceptance seam; candidate validation is
  not publication evidence);
- `crates/openwepp-coupled-time/src/clock.rs`;
- `crates/openwepp-coupled-time/src/transaction.rs`;
- `crates/openwepp-coupled-time/src/restart.rs`;
- `crates/openwepp-coupled-time/tests/authority.rs`;

Accepted Stage 3 to Lane D real-consumer path:

- `crates/openwepp-hillslope-orchestrator/src/direct_runtime.rs`;
- `crates/openwepp-hillslope-orchestrator/src/direct_runtime/00_core_frames.rs`
  (`cfg(test)`-only exhaustive canonical `DirectRunFrame` projection required
  by opaque covered-map parity; no production behavior or public wire surface);
- `crates/openwepp-hillslope-orchestrator/src/direct_runtime/00_core_frames_test_projection.rs`
  (test-only projection include extracted prospectively because the core-frame
  host begins at 2,978 lines and must remain below the 3,000-line ceiling);
- `crates/openwepp-hillslope-orchestrator/src/direct_runtime/stage3_committed_publication.rs`;
- `crates/openwepp-hillslope-orchestrator/src/direct_runtime/stage3_committed_publication_tests.rs`;
- `crates/openwepp-hillslope-orchestrator/src/direct_runtime/stage3_committed_publication_wat5.rs`;
- `crates/openwepp-hillslope-orchestrator/src/direct_runtime/stage3_committed_publication_tests_tail.rs`;
- `crates/openwepp-hillslope-orchestrator/src/direct_runtime/runoff.rs`;
- `crates/openwepp-hillslope-orchestrator/src/direct_runtime/runoff_result_types.rs`
  (behavior-preserving extraction of the touched over-3,000-line result and
  downstream-publication type tail only);
- `crates/openwepp-hillslope-orchestrator/src/direct_runtime/subhourly_generation.rs`;
- `crates/openwepp-hillslope-orchestrator/src/tests/tests_mod/direct_runtime_dc01.rs`;
- `crates/openwepp-hillslope-output/src/hillslope_wat.rs`
  (extend the existing deterministic Arrow schema encoder to the Utf8 field
  type used by WAT5; ordinary WAT bytes and schema semantics remain unchanged);
- `crates/openwepp-hillslope-output/src/hillslope_wat_subhourly.rs`
  (publish the Arrow schema through the existing sorted stable metadata
  encoder so identical WAT5 rows produce byte-identical Parquet and manifest
  hashes across isolated qualification batches);
- `crates/openwepp-hillslope-orchestrator/src/direct_runtime/03_executor.rs`;
- `crates/openwepp-hillslope-orchestrator/src/direct_runtime/03_executor_tests.rs`
  (behavior-preserving extraction of the touched over-3,000-line executor test
  module only);
- `crates/openwepp-hillslope-orchestrator/src/direct_runtime/laned_active.rs`;
- `crates/openwepp-hillslope-orchestrator/src/direct_runtime/snow_stage3_v11_scheduler.rs`;
- `crates/openwepp-hillslope-orchestrator/src/direct_runtime/surface_liquid_ingress.rs`;
- `crates/openwepp-hillslope-orchestrator/src/direct_runtime/surface_liquid_wb14.rs`
  (contract-first authenticated inactive-prefix chronology at the first exact
  snow-free successor; no represented-snow WB14 physics or receipt);
- `crates/openwepp-hillslope-orchestrator/src/direct_runtime/surface_liquid_wb14_native_prefix_tests.rs`
  (behavior-preserving extraction of native inactive-prefix `cfg(test)`
  regressions from the touched WB14 host to keep it below the 3,000-line hard
  ceiling);
- `crates/openwepp-hillslope-orchestrator/src/direct_runtime/surface_liquid_wb14_linear_reseal_tests.rs`
  (direct 0/1/many-receipt and multi-OFE parity/poison evidence for the private
  consuming linear accepted-slab reseal builder, extracted prospectively to
  keep the production WB14 host below the 3,000-line hard ceiling);
- `crates/openwepp-hillslope-orchestrator/src/direct_runtime/surface_liquid_ingress_mass_allocation.rs`;
- `crates/openwepp-hillslope-orchestrator/src/direct_runtime/surface_liquid_ingress_coordinator.rs`
  (consume the same immutable validated inactive-resource capability during
  the immediately adjacent specialized ingress construction, preserving
  identity, structural, and zero-work checks while avoiding repeated full
  resource validation; no public, wire, restart, or mutable proof reuse);
- `crates/openwepp-hillslope-orchestrator/src/direct_runtime/surface_liquid_ingress_terminal_tests.rs`;
- `crates/openwepp-hillslope-orchestrator/src/hydrology/support_helpers_mod/runoff_reconciliation.rs`
  (terminal typed-taxonomy reconciliation for the authorized surface-liquid
  replay variant);
- `crates/openwepp-hillslope-orchestrator/src/hydrology/support_helpers_mod/runoff_reconciliation/stage3_solver.rs`
  (exact settling-chronology preservation at the thermal fragment split/rejoin
  origin; no tolerance relaxation or post-hoc normalization);
- `crates/openwepp-hillslope-orchestrator/src/hydrology/support_helpers_mod/runoff_reconciliation/stage3_solver/evaluation.rs`
  (typed feed-forward terminal-carrier evaluation with exactly one real
  provider call per logical terminal trial; generic feedback-capable evaluation
  remains only for providers that structurally consume feedback, with no
  fallback between the two modes);
- `crates/openwepp-hillslope-orchestrator/src/hydrology/support_helpers_mod/runoff_reconciliation/cqr_row5_tests.rs`
  (role/support/call-count, forced two-call reference parity, poison, and
  rollback evidence for feed-forward terminal-carrier evaluation);
- `crates/openwepp-hillslope-orchestrator/src/hydrology/support_helpers_mod/runoff_reconciliation/stage3_solver/liquid_routing.rs`
  (enforce exact density and temperature constitutive caps before the generic
  conservation-range helper's residual slack; expose the validator only to
  package-owned in-crate origin tests, with no tolerance relaxation);
- `crates/openwepp-hillslope-orchestrator/src/hydrology/support_helpers_mod/runoff_reconciliation/stage3_solver/support.rs`
  (typed Stage-3 support/receipt plumbing required by the same canonical
  settling and terminal-custody correction);
- `crates/openwepp-hillslope-orchestrator/src/hydrology/support_helpers_mod/runoff_reconciliation/stage3_evaluation_validation_tests.rs`
  (origin-level equal-bit preservation and adjacent-ULP non-coalescence
  regressions for the same settling correction);
- `crates/openwepp-runner/src/hillslope/mod.rs`;
- `crates/openwepp-runner/Cargo.toml`
  (enable the existing non-default LSE `test-support` feature only through the
  runner dev-dependency so the package-owned release test can execute and
  report the real forced-complete parity oracle; production dependencies and
  default features remain unchanged);
- `crates/openwepp-runner/src/hillslope/03_tests.rs` (registration of the
  package-owned long-run qualification module only);
- `crates/openwepp-runner/src/hillslope/laned_active.rs`;
- `crates/openwepp-runner/src/hillslope/05_runner_execution_and_outputs.rs`;
- `crates/openwepp-runner/src/hillslope/snow_stage3_v11_production_seed.rs`;
- `crates/openwepp-runner/src/hillslope/snow_stage3_v11_qualification_audit.rs`
  (result-blind real-runner accepted-parent append/adoption and map telemetry);
- `crates/openwepp-runner/src/hillslope/direct_publication/day_input_and_helpers/00_builders_and_authority.rs`;
- `crates/openwepp-runner/src/hillslope/tests03/direct_publication_source_guards.rs`;
- `crates/openwepp-runner/src/hillslope/tests03/cqr_laned_active_outputs.rs`;
- `crates/openwepp-runner/src/hillslope/tests03/stage3_evaluation_publication_parity.rs`;
- `crates/openwepp-runner/src/hillslope/tests03/stage3_runner_qualification.rs`
  (emit the diagnostic-only enclosing imported-stack phase totals in the
  ignored release qualification record, including the nested snow-free
  physical-candidate and complete-owner construction attribution; parameterize
  the shared routed-output proof by the qualification surface's exact OFE
  count instead of retaining its original two-OFE fixture constant; extract the
  unchanged reappearance complete-season test into the private same-module
  include below to restore the touched test host's hard line ceiling);
- `crates/openwepp-runner/src/hillslope/tests03/stage3_runner_qualification/complete_season_reappearance.rs`
  (behavior-preserving same-module extraction of the existing reappearance
  lifecycle test; no fixture, assertion, or runtime change);
- `crates/openwepp-runner/src/hillslope/tests03/stage3_runner_qualification/component_replay_audit.rs`
  (same-module component-replay lifecycle aggregation and real forced-complete
  parity reporting for the package-owned release qualification only);
- `crates/openwepp-runner/src/hillslope/tests03/stage3_runner_qualification/component_replay_internal_parity.rs`
  (authentic test-support-only replay/forced potential and fixed-final internal
  solve/evaluation/trajectory/accepted-owner projection comparison);
- `crates/openwepp-runner/src/hillslope/tests03/stage3_runner_qualification/component_replay_file_parity.rs`
  (exhaustive HBP and WAT/PASS schema, null, row, and exact-value parity for the
  authentic replay/forced release fixture pair);
- `crates/openwepp-runner/src/hillslope/tests03/stage3_long_run_qualification.rs`
  (the canonical physical-map distribution is conditional on authentic
  ordinary-covered supports: its cardinality is the canonical-covered width
  histogram total, not the broader adaptive-support receipt count; a
  terminal-only surface records the distribution as not applicable while
  retaining terminal direct/split trial counts and every time/memory gate);
- `crates/openwepp-runner/src/hillslope/tests03/stage3_long_run_recipe.rs`
  (typed, deny-unknown-fields binding of the checked-in representative recipe
  and exact matrix area-extension policy);
- `crates/openwepp-runner/src/hillslope/tests03/stage3_long_run_terminal_protocol.rs`
  (fresh-child warm-up plus five-sample release qualification, bounded
  telemetry reconciliation, and exact CPU/RSS/source identity evidence);
- `crates/openwepp-runner/src/hillslope/tests03/stage3_long_run_output_evidence.rs`
  (streaming scientific hashes, semantic manifest identity, independent output
  closure, and retained-output custody);
- `crates/openwepp-runner/src/hillslope/tests03/stage3_complete_season_qualification.rs`
  (the two distinct native and legacy-management complete-season lifecycle
  gates, extracted before extending the near-ceiling runner qualification
  host);
- `crates/openwepp-runner/src/hillslope/tests03/wat5_output_transaction.rs`;
- `tests/integration/common/mod.rs`
  (shared package-integration fixture support only).

Terminal HBP evidence support, prospectively authorized before implementation:

- `crates/openwepp-input-contract/src/parsers/hbp/mod.rs`;
- `crates/openwepp-input-contract/src/parsers/hbp/payload_validator.rs`;
- `crates/openwepp-input-contract/src/parsers/hbp/types.rs`
  (bounded visitor/fold support over every validated HBP payload; no duplicate
  binary decoder and no change to the existing latest-payload APIs);
- `tests/integration/infile_hbp_parser_contract.rs`
  (all-payload ordering, closure-fold, corruption, and bounded-retention
  evidence for that API).

Retrospective write-set reconciliation (2026-09-03): eight paths added
above were discovered by independent current-source QA after they had already
been edited during the covered-solver/native-prefix correction. This is a
governance defect: they were not added before first edit as required. Their
changes are within the package objective and are now explicitly bounded here,
but this amendment does not relabel the earlier edits as prospectively
authorized. Terminal review and disposition must retain this process finding.
The eight paths are `identity_validation.rs`, `resource_validation.rs`,
`stage3_solver/support.rs`, the orchestrator `lib.rs`,
`snow_stage3_v11_attachment_runtime.rs`,
`open_snow_receipt_reseal_helpers.rs`, the runner
`snow_stage3_v11_qualification_audit.rs`, and `tests/integration/common/mod.rs`.

A second retrospective write-set defect was identified by independent QA on
2026-09-03: `v11_vegetation_consumer.rs` had already received a reuse-audit hook
before its exact file was added above. The hook is within the package objective,
and the only acceptable correction is to bind it to the real successful parent
acceptance seam, but this amendment does not relabel the earlier edit as
prospectively authorized. Terminal review and disposition must retain this
additional process finding.

A third retrospective write-set defect was identified by the revision-31
implementation rereview on 2026-09-04: the two cohesive evidence extractions
`solver_tests/component_dependency_replay.rs` and
`stage3_runner_qualification/component_replay_audit.rs` had been authorized in
the live execution handoff but were edited before both exact paths were entered
in this package write set. They are now explicitly bounded above, but this
reconciliation does not relabel those first edits as prospectively authorized;
terminal review and disposition must retain the process finding.

A fourth process defect was identified during terminal reconciliation: the
vapor/emax production correction was applied before the intended expected-red
capture. The later authority and positive/negative executable evidence remain
valid, but this package records the contract-first sequencing miss and does not
claim that the red preceded implementation.

The initial instruction-chain command and result are recorded in
`artifacts/required-reading-map.md`. Broad directory ownership remains
forbidden. The unrelated untracked `tmp/pdfs/r156-review/` files are preserved.

## Phase plan and gates

### Phase 0 — frozen intake and architecture inventory

Freeze exact source identity and dirty diff, map every reachable solver branch,
classify v33--v57 symbols as `delete`, `migrate`, `test-only`, or
`schema-compatibility`, trace the real runner through Stage 3, native vegetation,
soil/LSE, and Lane D, and record what still reads every old path. No production
code changes.

Gate: the deletion map has no unclassified production branch, names ordinary
Picard and every historical selector/call site, defines exhaustive
pre-iteration physical regimes, and the package write set is amended to exact
files.

### Phase 1 — baseline characterization and prospective authority

Build exact release binaries where the inherited path permits it, run every
existing workload arm that can reach a real consumer without production
changes, reproduce failure mechanisms, define result-blind measurement and
sampling protocols, derive numeric engineering budgets, and author dimensional
tolerance/custody authority. A blocked inherited production path is recorded as
a failed baseline, not extrapolated into a completed-day rate.

Gate: `performance-budget.md` and `tolerance-authority.md` contain numeric
pass/fail values, reproducible commands, hardware/source identity, dimensional
derivations, exact discrete/continuous receipt semantics, an executable
sensitivity matrix, and independent design acceptance. Existing production
measurements must be recorded honestly; the missing replacement seam and real
Lane-D path are expected-red obligations, not reasons to weaken or fabricate
baseline evidence. No production solver change precedes this gate.

### Phase 2 — contract-first replacement design

Apply ADR-0044, amend all affected canonical science contracts, add
contract-derived/static anti-accretion and production-seam sensitivity tests,
and record expected pre-implementation failures. The design must name physical
regimes, one solver per regime, convergence/adaptive behavior, evaluation
budget, conservation/custody boundaries, and restart migration.

Gate: applicable kernel profiles pass, contract tests fail only on absent
replacement production seams (including real Lane D and tolerance-sensitivity
hooks), and independent design reviews accept the replacement without an
old-solver fallback.

### Phase 3 — replacement and deletion

Implement the smallest canonical solver/runtime architecture, delete
superseded production paths in the same increment, preserve required process
models, and run focused regime, closure, restart, and real-consumer tests.

Gate: anti-accretion scans prove no production reachability of superseded
solvers; each physical regime has exactly one solver; the full production-seam
sensitivity matrix passes; focused correctness, closure, and performance
budgets pass.

### Phase 4 — representative qualification

Run complete day, snow-season, 10+-OFE year, and 10+-OFE 100-year workloads in
release mode. Reconstruct conservation independently, record native vegetation
ET and non-CoE Stage 3 activation, prove Lane D per-OFE execution, measure
scaling, and project the 5,000-hillslope workload with confidence bounds.

Gate: every declared runtime/memory/evaluation budget passes; no run is aborted,
silently narrowed, or qualified solely from an incomplete fixture.

### Phase 5 — terminal gates and disposition

Reconcile exact diff intent, execute the critical terminal validation selected
under the testing strategy, complete dual Rust/science reviews and independent
verification, disposition every finding, and report truthful completion or
HOLD.

## Exit criteria

- the inherited v33--v57 production cascade is removed; no V58 or replacement
  cascade exists;
- one canonical solver per contracted physical regime is statically and
  dynamically proven from the real runner;
- nonconvergence invokes only the typed failure/canonical adaptive response;
- Lane D operates per OFE on representative 10+-OFE workloads;
- native `SC-VEGETATION-001` vegetation and ET are active and no legacy/PMET-only
  substitute carries the claim;
- Stage 3 is the active non-CoE snow process and no CoE generation/fallback is
  reachable for the claim;
- frozen-litter authority and physics remain intact;
- physically justified tolerances and evaluation limits pass all sensitivity,
  domain, and anti-evasion vectors;
- complete day, season, year, and 100-year hillslope workloads finish within
  the Phase-1 numeric budgets;
- the 5,000-hillslope/10+-OFE/100-year projection is reported with measured
  scaling and explicit concurrency/I/O assumptions;
- mass, energy, phase, liquid, receipt, restart, owner, topology, event, and
  rollback closure pass independent reconstruction;
- warnings-denied affected Clippy and every exact-diff-selected correctness gate
  pass; critical/full-workspace gates run on the exact terminal source;
- all 2,000+/3,000+ line-count findings are dispositioned under repository
  policy;
- both independent reviews and both verifications accept the terminal evidence;
- the package status, roadmap, catalog, prompt, and final disposition agree.

## Review, verification, and subagent requirement

This package explicitly authorizes subagent spawning/delegation to:

- `comparator_suite_runner` for heavy full-workspace, long-run workload,
  comparison, and terminal gate execution, returning compact metrics and log
  paths with read-only source access;
- `rust_code_reviewer` for independent correctness/architecture review;
- `rust_qa_reviewer` for independent test, maintainability, and performance-gate
  review; and
- bounded worker agents only after exact file ownership is recorded, with no
  permission to revert or overwrite other worktree changes.

Heavy batch/closure runs must use `comparator_suite_runner` when available; the
parent must record unavailability before running an equivalent heavy gate
locally. Reviews are independent and findings require explicit disposition and
verification.

## Security and external-state gate

Expected security impact is `NONE`. If implementation changes subprocess,
untrusted input, protected assurance, or external data surfaces, amend the
package before edits and execute the applicable security/anti-evasion gates.

No network action, branch creation, commit, push, wepppy edit, or deployment is
authorized by this scaffold.

## HOLD boundaries

Effort, slow tests, implementation size, dirty-worktree complexity, or a
partially improved benchmark are not legitimate HOLD boundaries. A HOLD is
permitted only for missing/contradictory science authority, a required owner
choice that materially changes the runtime budget, unavailable representative
input evidence, an out-of-envelope dependency, or an external-state/security
boundary. Any HOLD requires `artifacts/hold-legitimacy-audit.md`.
