# Real-consumer proof

Status: `PASS — ACTIVATION/CUSTODY/PUBLICATION; PERFORMANCE HOLD SEPARATE`

Evidence mode: `Static + Ran`

The terminal production chain is:

1. `execute_hillslope_run_with_runtime_policy` selects
   `DirectProductionExecutor` and loads an authored production seed.
2. Pre-bootstrap Lane-D eligibility projects the Stage-3 SurfaceLiquid owner to
   all-local custody, preventing a DC01 pre-route plus Lane-D double feed.
3. Native V10/V11 vegetation, the single canonical covered solver, native
   LSE/soil, frozen litter, and one WB14 transaction construct the accepted
   Stage-3 day. Candidate-only native-V2 soil cannot publish owner/restart.
4. WB14 replay, exact-surface operands, and retained-credit ordinals follow the
   authenticated topology, including opaque `ofe-9`/`ofe-10` identities.
5. WAT5 projects sealed receipt dispositions and exact additional-supply
   segments without a second Green-Ampt solve.
6. `route_laned_active_day` consumes per-OFE local books and installs routed
   hourly/outlet ledgers on committed frames.
7. WAT/WAT5, outlet-only PASS, HBP, erosion operands, and
   `execution_provenance.laned_active` consume those frames.

Static anti-accretion guards prove v33--v57 is absent from production. The
former coupled-solve, temperature-primary, private-Q lattice, stable-monotone,
and convergence-version modules are deleted; historical tokens remain only in
the named test quarantine. No CoE, legacy vegetation, PMET-only, shadow-only,
or compatibility path carries the claim.

Ran terminal evidence:

- package suite `18/18 PASS`;
- SurfaceLiquid authority `22/22 PASS`; solver architecture `5/5 PASS`;
- release real runner `1/1 PASS` in `320.13 s`, covering baseline and
  factor-1000 sensitivity at 1/9/10/19 OFEs plus a positive two-OFE output case;
- closure: source `0.8488061229561478 m3` = outlet
  `0.8471105124736579 m3` + storage `0.0016956104824910018 m3`, clamp `0`.

This closes activation, ownership, topology, and output-consumer claims. It
does not close throughput: the one-OFE day is 12.436785 s and long-run budgets
remain a terminal HOLD.
