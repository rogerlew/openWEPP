# Independent verification A — final post-revert audit

Status: `APPROVE TERMINAL HOLD — BOUNDED CORRECTNESS RETAINED; RELEASE/PERFORMANCE QUALIFICATION FAILED`

Evidence mode: `Static + Ran + Forensic reconstruction`

## Findings

No blocker remains to the truthful terminal `HOLD` disposition. Release and
performance approval remain blocked and are not granted.

1. **CRITICAL — revision-31 release conjunct failed and candidate retention is
   forbidden.** The exact candidate run 1 exited `101` before benchmark JSON
   because the real release aggregate contained no authentic completed
   `N=2,S=6` sweep with `58/14/16/28` accounting. The frozen protocol is
   conjunctive, so runs 2/3 were correctly not run and no timing/RSS median can
   be inferred. The candidate was fully rejected and reverted.
2. **MEDIUM — four governance/sequencing defects remain accepted history.**
   Eight covered-solver/native-prefix paths were edited before retrospective
   write-set listing; `v11_vegetation_consumer.rs` was edited before its exact
   entry; the two component replay extracted paths were edited before exact
   listing; and the vapor/emax production correction preceded capture of its
   intended expected red. All are accurately recorded without backdating.
   Candidate-only extracted paths were removed; the retained changes are now
   bounded. These findings prevent a claim of flawless sequencing but do not
   block a truthful failed/reverted `HOLD` disposition.
3. **Non-blocking promotion debt.** Broad orchestrator warnings-denied Clippy
   and exact-workspace correctness remain failed or unestablished. They are not
   relabeled as throughput evidence and cannot turn the failed release and
   performance gates into a pass.

## Terminal evidence

The previous verification-A rejection of an early HOLD remains valid history:
safe package-authorized architecture work still existed then. Execution did
continue through the revision-61 feed-forward candidate and revision-31
component-temperature replay candidate. Both were prospectively gated,
measured, rejected on binding conjuncts, and fully reverted. The reason for
terminal HOLD has therefore materially changed from the earlier rejected one.

The final component candidate's captured ordered 16-path manifest contains
exactly 16 entries and independently hashes to
`edc3f0b94c393e537b0115403548b779754f33308e53a26b1041932e9915be71`.
Both controlling final implementation reviews approve those exact bytes for
measurement. Their recorded gates include focused replay `14/14`, full LSE
`154/154`, feature-enabled compiler-negative diagnostics `2/2`, authentic
runner replay/forced parity `1/1`, affected checks, LSE warnings-denied Clippy,
formatting, and diff hygiene. Approval of the candidate's correctness cannot
override its later failed release conjunct.

The retained raw release log contains exactly one run-1 begin and one exit-101
record, no run-2/run-3 begin, an explicit not-run marker, and no
`STAGE3_LANED_RELEASE_PROBE` JSON. Candidate source identity is unchanged before
and after run 1 at
`039a312502a5e6ef442b1e81ac78b988141199f6283fedcc86518ba78ba61abc`.
The cached executed binary independently hashes to
`f9386eec584664f9639da281c15796730240239cd43ad2f158f4fa6d27fbeeaf`,
matching the log.

Post-revert static inspection finds all seven replay graph/base/probe/audit/
function declarations absent, all seven candidate-only extracted/test-support
files absent, and no replay symbol or runner hook in the restored production
hosts. The retained non-default LSE `test-support` feature, orchestrator
dev-dependency, and transaction projection accessors are not v31 residue: they
were prospectively authorized earlier in this package for private parity
projection and still have a real orchestrator test consumer.

The current sorted changed/untracked Rust-source manifest independently hashes
to `2813f6e8faabb9408bac5e59b9271626ff5bcdc7fe49ab6dda810d3a1c3eee0d`
over 179 files. Its difference from frozen pre-v31
`78d756be1fa11ed85ee92b7d19e6c04427b01b122efaf7804d1b55d60536bbbe`
is forensically explained by the authorized post-baseline authority-test
patches: replacing only the current authority-test entry
`9250d0dd...a2451` with independently reconstructed pre-v31 hash
`912bb3de...2096` reproduces the frozen aggregate exactly. This is an explained
authority-test delta, not surviving v31 production behavior.

## Bounded checks run

- `cargo nextest run -p openwepp-land-surface-energy --no-fail-fast` through
  the Nix shell: `PASS`, 140/140.
- Exact v31 structural seam: `EXPECTED_RED`, exit `101`, listing exactly the
  seven absent declarations.
- LSE, hillslope-orchestrator, and runner `cargo check --all-targets`: `PASS`.
- `cargo fmt --all -- --check`: `PASS`.
- Whole-worktree `git diff --check`: `PASS`.
- Current Rust-manifest reconstruction, candidate-manifest record, release-log
  structure, binary hash, candidate-file absence, replay-residue searches, and
  in-memory frozen-manifest reconstruction: `PASS`.

The known inherited two-field terminal-request dead-code warning remains during
orchestrator/runner checks. No heavy release command was rerun.

## HOLD legitimacy and safe remaining work

The retained architecture has one canonical solver per admitted regime and the
v33--v57 cascade remains deleted. The completed 1/10/19 matrix preserves science
but misses wall/RSS budgets by orders of magnitude. The campaign has already
retained exact-custody, allocation, projection, validation-once, identity-anchor,
and leaf-reuse gains; it rejected and reverted shared-custody, hydraulic,
terminal-package, evidence-assembly, revision-61, and revision-31 candidates
when their prospective gates failed. Remaining source-audited local envelopes
are either below materiality or dominated by mandatory physics.

No safe, credible performance route remains inside the existing workload,
budget, solver/evaluation architecture, write set, and science authority.
Weakening the authentic `58/14/16/28` release audit, changing cadence or
tolerances, introducing approximation/fallback, or relabeling diagnostics is
forbidden. The only safe remaining actions are evidence housekeeping and
owner-directed work after new authority: either revise the runtime premise and
qualification protocol, or authorize a materially different canonical
solver/evaluation architecture with a new contract/work package as needed.

Final verdict: `APPROVE TERMINAL HOLD`. This is not package completion, release
approval, exact-workspace correctness, or performance qualification.
