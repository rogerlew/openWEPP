# Independent QA review B

Static:

Reviewer: `rust_qa_reviewer`

Verdict: `HOLD`

| Finding | Severity | Summary | Required disposition |
|---|---|---|---|
| `CRB-CNFP-001` | high | Expected red was token-only and omitted the behavioral matrix and `P-005`. | Add executable crate tests, retain the source test only as supplemental anti-evasion evidence. |
| `CRB-CNFP-002` | high | Exact role-to-ordinal authority was not reproducible. | Bind `Initial@0`, predictor `@1`, contiguous trials, and `FinalAccepted@(M-1)` with no gaps/repeats. |
| `CRB-CNFP-003` | high | SurfaceLiquid `INV-034` cited provisional physical reuse although canonical final physics must be independently charged. | Remove `INV-033` from the new authority and explicitly exclude that reuse path. |
| `CRB-CNFP-004` | high | Typed failure mapping was incomplete and referenced undefined `SURFACELIQUID-E-012`. | Use defined semantic error classes and publish deterministic precedence. |
| `CRB-CNFP-005` | medium | Publication was ambiguous between private envelope and external publication. | Separate counters and enforce parent-only publication. |
| `CRB-CNFP-006` | medium | Static lint evidence did not prove the complete kernel profile. | Add a cycle readiness/profile matrix with evidence and obligation-to-test mapping. |
| `CRB-CNFP-007` | medium | Snow date/version, registry date, and a trailing vegetation change log were inconsistent. | Reconcile lifecycle metadata and distinguish monotonic contract revision from the forbidden process-solver V58. |

Nonblocking debt: later consolidate duplicated late amendment sections and
split structural, compile-time privacy, behavioral, and poison tests for
diagnostic clarity.

The full reviewer output was returned in the collaboration record for task
`/root/covered_physical_contract_review_b`.
