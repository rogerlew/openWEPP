# Solver-chain deletion map

Status: `QUEUED — NOT RUN`

Evidence mode: `Not run`

Enumerate every production-reachable v33--v57 branch and classify it as
`delete`, `migrate`, `test-only`, or `schema-compatibility`. Record callers,
consumers, replacement ownership, and the proof that no numerical fallback
remains. No production edit may precede complete classification.
