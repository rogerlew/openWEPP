# Implementation and focused validation

Status: `TERMINAL EVIDENCE — RETAINED INCREMENTS VALIDATED; FINAL CANDIDATE REVERTED`

Evidence mode: `Static + Ran`

Record the replacement implementation, deleted superseded paths, exact diff,
focused commands, regime coverage, evaluation/runtime budgets, typed failures,
restart disposition, anti-accretion proof, and conservation results.

## Recorded isolated ownership

- Canonical-solver worker owns only the package-authorized
  `v11_covered/{mod.rs,receipt_sets.rs,fixed_point.rs,open_snow.rs,regime.rs,
  open_snow_convergence_metrics.rs,canonical_covered_solver.rs,
  phase_consistent_*.rs,stable_monotone.rs,open_snow_*tests.rs}` files plus
  `v9_real_consumer_shadow_v11_error.rs`,
  `snow_stage3_v11_adaptive_frontend.rs`,
  `snow_stage3_v11_adaptive_execution_tests.rs`, and
  `snow_stage3_v11_terminal_execution.rs`. Historical versioned solver files
  may be deleted; no runner/direct-runtime file is in this ownership.
- Real-consumer worker owns only the package-authorized
  `direct_runtime/{stage3_committed_publication.rs,
  stage3_committed_publication_tests.rs,stage3_committed_publication_wat5.rs,
  03_executor.rs,laned_active.rs,snow_stage3_v11_scheduler.rs}` and
  runner `hillslope/{mod.rs,laned_active.rs,05_runner_execution_and_outputs.rs,
  direct_publication/day_input_and_helpers/00_builders_and_authority.rs,
  tests03/direct_publication_source_guards.rs,
  tests03/cqr_laned_active_outputs.rs,
  tests03/stage3_evaluation_publication_parity.rs,
  tests03/stage3_runner_qualification.rs}` files. No canonical-solver file is
  in this ownership.

Both workers were told that the worktree is shared, unrelated/user changes
must be preserved, and neither may revert or overwrite another owner's work.

After the combined real-runner path crossed the corrected deferred-soil join,
it exposed `SURFACELIQUID-E-008` at the exact coupled-child/WB14 scalar-support
join. Before first edit, solver-worker ownership was narrowly extended to
`direct_runtime/surface_liquid_ingress.rs` and
`direct_runtime/surface_liquid_ingress_terminal_tests.rs`. The correction must
preserve, not remove, the exact child-start/child-end/scalar-cursor authority;
instrumented operands and a focused regression are required.

Instrumented execution then proved the scalar cursor itself was correct but
the native frozen-litter V3/V4 resident read a separate `wb14_parent` owner
that `stage_unpublished_v2_carrier_owners` had not advanced. Before first edit,
solver-worker ownership was narrowly extended to
`v11_covered/carrier_phase.rs` and
`v9_real_consumer_shadow/frozen_litter_v3_adoption.rs`. The carrier must stage
the same authenticated parent into both canonical custody views with an exact
equality guard; no default owner or chronology relaxation is authorized.

The succeeding real-child E012 refusal proved that the carrier envelope still
advanced only legacy inner LSE/surface owners while the native frozen-litter
V3 physical and V4 exact residents remained stale. Before first edit,
solver-worker ownership was narrowly extended to
`v11_covered/carrier_engine.rs` and
`v9_real_consumer_shadow/frozen_litter_v4_adoption.rs`. Each charged authentic
carrier map must produce and retain the native V3/V4 candidate pair; hash
rebinding, mirror canonicalization, or a second physical replay is forbidden.

The authentic V4 candidate then exposed an authority gap in exact-surface
partial-child chronology. Canonical contracts were amended first to
SC-LANDSURFACEENERGY-001 v18 `INV-153/C-008` and SC-SURFACELIQUID-001 v18
`INV-025/C-015`; both authority suites passed, while the new package-specific
implementation test was expected red on the missing typed receipt posture.
Before first implementation edit, solver-worker ownership was extended only
to `land_surface_energy_shadow/v3_execution.rs` and
`direct_runtime/surface_liquid_owner/v3_exact_enthalpy.rs`. The pre-production
V1 receipt wire must seal derived partial/final posture and parent bounds;
partial children retain the persistent predecessor while exact carry/state/
receipt chain advance, and final children stamp the transaction exactly once.

After that typed chronology repair, the real runner crossed the exact-surface,
WB14, and carrier gates and stopped at the V2 authenticated complete-owner
source transition. Static inspection localized the mismatch to the V50
transition authority: it reconstructed the legacy non-soil ending from the
covered envelope but did not bind the native V3/V4 pair now retained by the
same charged carrier map. Before first edit, solver-worker ownership was
extended only to
`v11_covered/owner_finalization_v50_transition.rs` and its exact one-line
callsite in `v11_covered/owner_finalization.rs`. The correction must add the
already-authenticated native pair to the independently reconstructed expected
ending; it may not weaken the downstream full non-soil equality, source
transaction, or envelope validation.

The next exact real-runner boundary was the explicit snow-free native-V4 plus
deferred-native-V2 placeholder refusal. Independent authority inspection found
that the complete typed join already exists immediately below the refusal:
`DeferredNativeV2SoilCustodyV1::advance_snow_free_child` authenticates exact
same-support or contiguous-successor chronology and reconstructed operands,
then the finalizer preserves native-V2 soil while accepting non-soil owners,
composes and seals the continuation, authenticates split source/soil-target
authority, and installs atomically. Before first edit, solver-worker ownership
was extended only to `v9_real_consumer_shadow.rs`. The permitted correction is
to remove the V4-only placeholder refusal and project accepted inner
vegetation/LSE into the V10 outer owners before the unchanged continuation
authentication; the analogous V3 refusal and all chronology, operand, seal,
authority, install, pair, and rollback checks remain unchanged. Authority:
SC-LANDSURFACEENERGY-001 INV-150/151/153; SC-SURFACELIQUID-001
INV-009/014/022/023/025; SC-SNOWENERGY-001 INV-063/071--074 and
OBL-C-031/039--042.

With the V4 placeholder removed, the same real run exposed the earlier physical
input side of that join: `execute_and_accept_frozen_litter_v4` sealed its V3
runtime input from the installed (intentionally older) native-V2 soil resident,
so the V4 transaction check refused before the later continuation install.
Both exact paths were already authorized before this correction:
`v9_real_consumer_shadow.rs` and
`v9_real_consumer_shadow/frozen_litter_v4_adoption.rs`. The V4 evaluator may
receive the deferred continuation's authenticated `original_prepared()` soil
beginning, require exact equality with that custody, and use its existing
receipt-free restart seal. The ordinary no-deferred path remains unchanged;
the later advance/compose/seal/authenticate/install chain remains the sole soil
acceptance path.

The next 120--1800-second snow-free child had no deferred custody because the
preceding ordinary `RetainFinal` child had already installed native-V2 soil;
transient deferred custody is terminal-only. Independent authority review
rejected an initial source-rebasing hypothesis: transaction 41 remains the
lawful physical/surface source retained by the partial exact records, while
the authenticated next soil target is transaction 42. SC-SNOWENERGY-001
INV-063/073 and OBL-C-031/041 require those two identities to remain distinct.
Before edit, the already-authorized
`land_surface_energy_shadow/v3_execution.rs`,
`v9_real_consumer_shadow/frozen_litter_v4_adoption.rs`, and
`v9_real_consumer_shadow.rs` seams were explicitly re-authorized to carry and
validate `PhysicalSoilEnergyTransactionAuthorityV2`: source 41 must join the
physical transaction/ingress, target 42 must join the independently prepared
soil owner/restart, and swapped, stale, copied, or rebased identities must
fail. No adaptive/terminal custody producer or transaction-adjacency inference
is authorized. Because the runtime input is shared, exact field additions to
the existing `land_surface_energy_shadow/v3_tests.rs` and
`land_surface_energy_shadow/v4_restart_evidence.rs` literals were added to the
write set before edit; same-ID V3 and split-ID V4 plus swapped/stale/rebased
poisons are required.

The typed source/soil-target split then reached the V3 complete-owner
projection, whose pre-production identity still stored one transaction and
therefore conflated the physical surface/LSE source with the independently
authenticated soil target. Before first edit, ownership was extended only to
`direct_runtime/surface_liquid_owner/v3_projection.rs`. Its schema/digest must
bind the typed split: surface/LSE bytes validate against the source, soil-V2
bytes validate against the target, and same-ID ordinary V3 remains valid;
copying, swapping, rebasing, or omitting either identity fails closed.
The exact existing publication-retention test literal at
`v9_real_consumer_shadow/v3_publication_retention_tests.rs` was added to the
write set before its required soil-target field update.

The real runner next reached the intentionally same-ID-only generic soil
installer. The V3 complete-owner acceptance already holds the exact
authoritative beginning, prepared beginning, accepted candidate, and seals, so
the already-authorized `frozen_litter_v3_adoption.rs` callsite must use the
existing explicit
`authenticate_soil_thermal_prepared_beginning_install_authority_v2` plus
`install_soil_thermal_accepted_v2_from_authenticated_beginning` path. The
generic installer and its split refusal remain unchanged.

After the first genuine canonical map completed, the ordinary no-replay reuse
path failed because `execute_ordinary_physical_reuse` discarded the retained
accepted `self.ending` and invoked owner finalization with no precomputed
physical ending. Before first edit, ownership was extended only to
`v11_covered/open_snow_physical_support.rs`. Its existing finalization call may
transfer `self.ending.as_ref()` into the precomputed-ending argument; it may not
replay physics, synthesize an owner, or bypass the accepted-ending posture
check.

That transfer proved the ordinary reuse seed itself lacked the charged map's
authenticated native-V2 soil trial/continuation, unlike the existing terminal
reuse seed. Before first edit, ownership was extended only to
`v11_covered/execution.rs` plus the already-authorized
`canonical_covered_solver.rs` and `open_snow_physical_support.rs` seam lines.
`CoveredOrdinaryPhysicalReuseSeedV1` may retain and transfer the exact charged
`DirectSoilThermalCandidate` and optional continuation into existing
finalization guards; no reconstruction, replay, or independent acceptance is
authorized.

Retaining the fixed-final image then proved V14 frozen-litter V3 is correctly
snow-free-only: its kernel guard rejects Stage3 lower boundaries, and the V3
wrapper would otherwise apply snow-surface coordinates to litter vapor/phase.
Production implementation was paused for a required contract-first amendment.
The authoritative covered regime is one standard Stage3 covered-column solve
under native vegetation/native LSE identities inside the same heterogeneous
charged batch; represented snow is the atmospheric ground surface, while
under-snow V3/V4 litter vapor, phase, storage, current ingress, and WB14 are
inactive and their exact owners remain unchanged. The same call must retain
the typed Stage3 lower boundary and optical receipt; no second inner/legacy
envelope is permitted. Before further production edit, the write set was
extended to `land_surface_energy_shadow/v3_multitile_adoption.rs` and
`land_surface_energy_shadow/strict_v8_endpoint.rs`; existing SC-LSE, SC-SNOW,
SC-SURFACELIQUID, authority-test, package-test, carrier-engine, covered-owner,
and frozen-litter paths were already authorized. Contract authority and
expected-red guards precede implementation.

Contract-first gate result: PASS. SC-LANDSURFACEENERGY-001 v19 adds
`INV-154/C-009`; SC-SURFACELIQUID-001 v19 adds `INV-026/C-016`; the
non-versioned ADR-0044 section of SC-SNOWENERGY-001 adds `INV-083/C-051`
without creating v58. Authority suites passed LSE 16/16, SurfaceLiquid 17/17,
and Snow 37/37 with 22 superseded v32--v57 production-symbol checks explicitly
ignored as historical. Strict binding-exposure lint passed all three contracts
and `cargo fmt --check` passed. The exact package production-seam test is
expected red only on missing `Stage3CoveredNative` and its seven behavior
proofs: one standard covered solve, zero under-snow litter V3/V4/WB14, exact
optical/lower receipts, no second envelope, unchanged V3/V4 bytes, snow-free
transition, and rollback.

After typed `Stage3CoveredNative` classification and the standard potential/
final solve compiled, the single-envelope constructor required a hydrology
join that does not manufacture a frozen-litter phase result. Before first edit,
ownership was extended only to
`land_surface_energy_shadow/real_hydrology_execution.rs` for a sibling
`construct_stage3_covered_native_unified_hydrology_candidate`. It must consume
the standard fixed finals and unchanged native V2 surface owner, execute no
litter phase/current ingress/WB14 for marked native represented-snow
destinations, preserve ordinary non-native rows and exact soil candidates, and
return one complete unified candidate. Fake/empty V3 receipts and second
physical construction are forbidden.

The remaining reuse mismatch distinguished the charged unpublished physical
ending from `self.ending`, which is already post-finalization/publication
posture. The same authorized ordinary reuse seed may additionally retain the
exact charged `ending_candidates.shadow()` captured before finalization and
pass that value as the precomputed physical ending. It may not substitute the
post-finalization owner or rederive the unpublished shadow.

Exact route tracing then localized the still-failing posture to the first
canonical authentic production finalization, before ordinary reuse. The
already-authorized `owner_finalization.rs` ownership is temporarily widened
from its prior one-line callsite to result-blind diagnostic reporting of the
existing `native_v2_selected_trial_matches_accepted_ending_v1` predicates.
Instrumentation must not alter a predicate or return value, must identify the
producer/candidate seam, and must be removed before disposition.

Predicate evidence showed exact transaction/support/beginning/predecessor and
receipt-chain custody passed; only the accepted soil ending and credits
differed because finalization recomputed CN credits instead of using the
authentic carrier map's retained/applied batch credits. Production acceptance
now transfers those retained credits. The same already-authorized ordinary
reuse seed/execution/physical-support seams may retain and transfer the exact
`Vec<SoilThermalTopBoundaryCreditV1>` used by the charged carrier map,
mirroring terminal reuse. Recomputing credits from end-state receipts or
changing trial-vs-accepted predicates remains forbidden.

A later WB14 parent exposed the deeper producer conflict: the charged native
V3/V4 evaluation returned a complete native envelope, but `carrier_engine.rs`
discarded it and ran a second independent legacy covered physical/envelope
evaluation. Their beginning liquid and receipt chains differ physically, so no
rebase or bridge can lawfully make them equal. Under existing SC-SNOWENERGY-001
INV-082/C-050, SC-LANDSURFACEENERGY-001 INV-152/C-007,
SC-SURFACELIQUID-001 INV-014/018/019/023/025, and ADR-0044, the returned native
candidate and native envelope must be the sole charged carrier image whenever
native V4 is active. The already-authorized `carrier_engine.rs` must skip the
legacy constructor in that branch. Legacy construction is admitted only when
both native V3 and V4 residents are absent; either half-native posture fails
typed. Downstream carrier reconstruction, precipitation/soil operands,
finalization, and V50 transition already consume an envelope generically and
must receive that exact native envelope. No contract amendment or receipt
weakening is authorized. Stale dual-envelope source tests must be replaced by
one-native-envelope, no-discard, no-legacy-call, half-native-refusal, and real
production-seam guards in the already-authorized tests.

Using the native envelope exposed a typed support-map retention gap: its
`CoveredV8PhysicalOwner::FrozenLitterV3` variant retained only unified
hydrology even though the constructor already received the exact charged V3
fixed-final candidate. Before first edit, ownership was extended only to
`land_surface_energy_shadow/covered_v8_owner.rs`. The native physical owner may
retain that exact fixed-final candidate alongside hydrology and derive fixed-
cap release, iteration state, longwave, shortwave, and optical support maps
directly from its charged legacy-tile and native complete-physical fields.
Every map must validate exact transaction/destination/receipt joins; no replay,
legacy constructor, zero/default field, or inferred optical receipt is
authorized.

The consumer ownership was amended before first edit to include
`direct_runtime/snow_stage3_v11_scheduler.rs`. Static inspection confirmed it
is the narrow post-bootstrap atomic seam where the authenticated outer frame's
Lane-D configuration and summary must be copied into the completed accepted
publication candidate before `Stage3AcceptedPublicationDayV1::try_complete`;
pre-bootstrap installation correctly violates fresh-frame authentication.

The consumer ownership was further amended before first edit to include only
`direct_runtime/stage3_committed_publication_wat5.rs`. Static inspection proved
that accepted WB14 producer-input generation returned early when
`wat5_subhourly_requested` was false. Physical Lane-D sourcing must not depend
on a reporting-selection flag, so this seam may generate the authenticated
24-bin source for Lane D while retaining the independent WAT5 publication flag.

Before edit, consumer ownership was extended to the exact existing selector
test `runner/src/hillslope/tests03/cqr_laned_active_outputs.rs`. Canonical
SC-OFEROUTE-001 retains explicit active opt-in and the active-disable rollback
selector; accepted Phase-2 no-selector qualification did not retire them.
Tests must require disable only on complete native authority, reject
active+disable, and keep shadow/profile selectors rejected.

Before edit, consumer ownership was extended to the exact stale integration
test `tests/integration/laned_shadow_h2637.rs`. Its active/disable cases must
test canonical explicit-active preconditions and active+disable conflict; only
truly retired shadow/trace/implicit selectors remain on the retired-selector
assertion path.

The next authentic-map refusal exposed a distinct V2 soil custody boundary.
`DirectSoilThermalUnpublishedContinuationV2` already authenticates the installed
resident, original prepared owner, exact predecessor unpublished trial, and
contiguous next-child support. Its
`SoilThermalUnpublishedPhysicalBeginningV2`, however, is deliberately a
non-owner and nonrestartable constitutive read surface, while frozen-litter V3
runtime/projection accepted only an exact-child publishable owner plus restart.
Authority review rejected construction or support rebinding of owner-shaped
bytes from the private beginning: that would promote an unpublished trial,
create a synthetic restart, and permit intermediate or dual acceptance.

Contract-first authority instead admits one typed candidate-only V3 soil-
beginning branch. SC-LANDSURFACEENERGY-001 v20 `INV-155/C-010` owns the exact
continuation constructor and single final promotion;
SC-SURFACELIQUID-001 v20 `INV-027/C-017` requires a non-owner projection whose
owner/restart fields are absent, not empty; the non-versioned ADR-0044 Snow
companion adds `INV-084/C-052` without process version 58. The private branch
may retain only exact original-owner, predecessor-trial, beginning-state,
transaction/predecessor/receipt-chain, and child-support custody. Authentic
final acceptance must replay the original prepared owner, complete canonical
accumulated operands, exact selected physical ending, and layer-credit chain
through the existing unpublished-composition path, then seal/install exactly
one owner/receipt/restart/checkpoint bundle. SC-SOIL-001 requires no amendment:
its soil-process equations are unchanged and exact soil-thermal representation
remains LSE authority.

Before any production edit, the write set was extended only to the existing
producer `v9_real_consumer_shadow/v10_soil_thermal_v2.rs`, its focused tests,
the already-authorized `land_surface_energy_shadow/v3_execution.rs`,
`v9_real_consumer_shadow/frozen_litter_v3_adoption.rs`, and
`direct_runtime/surface_liquid_owner/v3_projection.rs` plus its focused tests.
Required proofs are exact publishable/candidate branch discrimination,
contiguous support, predecessor-trial and receipt substitution refusal, absence
of owner/restart bytes and intermediate installation, complete final replay,
one atomic install, and byte-exact rollback. No proxy physics, support rebind,
synthetic owner bytes, private-trial publication, or dual acceptance is
authorized.

## Candidate-only V2 soil-beginning contract-first gate

Ran 2026-09-01 before production implementation:

- `.venv/bin/python tools/check_sc_binding_exposure.py --strict` for
  `SC-LANDSURFACEENERGY-001`, `SC-SURFACELIQUID-001`, and
  `SC-SNOWENERGY-001`: PASS (`5`, `12`, and `47` consolidated rows).
- `nix develop --command cargo nextest run --test
  land_surface_energy_balance_authority_contract`: PASS, `16/16`.
- `nix develop --command cargo nextest run --test
  surface_liquid_hydrology_custody_authority_contract`: PASS, `17/17`.
- `nix develop --command cargo nextest run --test
  snow_terminal_enthalpy_event_numerics_contract --no-fail-fast`: PASS,
  `38/38`, with `22` intentionally skipped retired historical-version checks.
- `nix develop --command cargo fmt --all -- --check`: PASS.
- `git diff --check` over the owned authority/package/test files: PASS.
- `nix develop --command cargo nextest run --test
  stage3_native_vegetation_laned_throughput_recovery
  unpublished_soil_continuation_enters_v3_only_as_typed_candidate_beginning`:
  expected RED, exit `100`. The only reported defects are the absent typed
  `FrozenLitterV3SoilBeginningV1` / `CandidateOnlyUnpublishedSoil` production
  discriminator and the six required exact-custody/final-replay/rollback
  behavior proofs. Existing authenticated unpublished-beginning construction
  and canonical final-composition symbols were found.

## Exact V3 litter phase-capacity spill adjudication

The observed `SURFACELIQUID-E-003` is not a configuration mismatch: forest
liquid capacity and LSE phase capacity are both exactly `6 kg m^-2`, while the
accepted bounded melt can lawfully make the raw post-phase liquid exceed that
capacity. Existing LSE v14 authority already orders `litter overflow` after
bounded phase and before current ingress, SurfaceLiquid `INV-018/020` owns the
post-phase liquid-only handoff, and WATBAL `INV-103` owns timed routed melt/
liquid supply exactly once. The legitimate correction is therefore an exact
physical spill, not a relaxed owner-domain guard.

Contract-first LSE v21 `INV-156/C-011` and SurfaceLiquid v21 `INV-028/C-018`
bind the precise construction. The immutable phase receipt remains the raw
bounded-phase image. A typed `LitterPhaseCapacitySpillV1` companion computes
positive excess by checked subtraction, removes the liquid sensible enthalpy
at the raw phase-ending temperature, seals the retained within-capacity owner,
adds one named negative exact-surface operand, and produces one internally
owned `LitterPhaseOverflow` parcel on the same transaction and full accepted
child support. Checked tile/OFE mass and enthalpy conversions, original phase-
receipt/source identity, ordinary WB14 infiltration/excess/retention/runoff,
and complete rollback remain mandatory. Capacity `min`/clamp, loss, fusion or
latent alias, condensation labeling, caller synthesis, retiming, duplicate
WB14 supply, same-support phase/LSE re-solve, and tolerance repair are
forbidden. WATBAL needs no new process version because existing `INV-103`
already owns this exact timed liquid-supply chronology.

Before production work, the package write set was extended to the exact LSE
phase output/closure/transaction files and tests; V3 input projection,
execution, and focused tests; SurfaceLiquid V2 resource adapter, ingress,
closure, attachment, exact-surface/projection seams, exhaustive parcel-kind
consumer, and focused tests. The package integration guard requires typed
spill/source/energy-operand symbols plus capacity-boundary, melt-spill,
one-WB14, alias/substitution, and rollback proofs.

Ran 2026-09-01 before production implementation:

- strict Binding Exposure Index checks: PASS for LSE (`6` consolidated rows)
  and SurfaceLiquid (`13` consolidated rows);
- `cargo nextest run --test land_surface_energy_balance_authority_contract`:
  PASS, `17/17`;
- `cargo nextest run --test
  surface_liquid_hydrology_custody_authority_contract`: PASS, `18/18`;
- `cargo nextest run --test
  stage3_native_vegetation_laned_throughput_recovery
  litter_phase_capacity_spill_is_typed_and_routes_once_through_wb14`:
  expected RED, exit `100`, solely on the three absent typed production symbols
  and six named focused behavior proofs.

The typed candidate-only projection then exposed one exact downstream compile
boundary in
`crates/openwepp-persisted-restart-v1/src/frozen_litter_v3_projection.rs`:
the restart adapter treated owner and restart accessors as infallibly present.
Before edit, the package write set was extended to that exact adapter only.
Its authorized correction is fail-closed rejection when either accessor is
absent, proving candidate-only custody cannot enter restart persistence;
ordinary publishable and post-replay accepted projections must retain their
existing canonical bytes unchanged.

## Heterogeneous V3 finalized-use/resource join adjudication

The runner next rejected `V3 finalized uses include a nonzero ordinary surface
row`. Static provenance showed that the typed litter phase-capacity spill is
not a `WaterProtocol.finalized_uses` row: it remains the separate V2 internal
`LitterPhaseOverflow` parcel. Accepted native litter-vapor rows are already
consumed by their exact phase receipts. The unmatched nonzero row is an
ordinary legacy open/covered surface withdrawal in the same V3 batch. The
existing V3 join then copied `accepted.surface_resource` wholesale, which
discarded that authenticated ordinary withdrawal.

SurfaceLiquid `INV-004` already binds exact D/A/F identity, canonical key-order
aggregation, and one finalized-use debit. `INV-018` orders finalized vapor and
phase before current ingress, while `INV-028` retains spill as a separate
internal parcel. Contract-first LSE v22 `INV-157/C-012` and SurfaceLiquid v22
`INV-029/C-019` therefore authorize only a typed heterogeneous resource join:

- match native litter-vapor rows exactly to accepted phase receipts and never
  replay them as resource debits;
- treat the complete unmatched set as ordinary only after exact request,
  authorization, transaction, child-support, key/source/basis, cardinality,
  and amount validation;
- apply its canonical `F/f_t` liquid debit once to the accepted phase-adjusted
  V2 owner, then use that one resource candidate for the existing one ingress;
- retain native phase closure, spill companion/internal parcel, litter ice,
  thermodynamic high mirrors, exact-energy receipt/carry, and owner lineage;
- add no ingress parcel and no second sensible, latent, fusion, or exact-
  surface-energy operand for the ordinary debit; and
- roll back all physical/exact owners, WB14 parent/cursor, receipts, spill, and
  runner state on any partition, join, closure, ingress, or publication error.

The authorized constructor semantics are
`SurfaceLiquidV2HeterogeneousResourceJoinV1` and
`apply_ordinary_finalized_uses_to_phase_adjusted_v2`. Wholesale legacy/native
resource replacement, phase-row replay, spill reclassification, enthalpy
recomputation, support rebinding, capacity normalization, duplicate ingress,
and guard relaxation remain forbidden. The necessary production path
`land_surface_energy_shadow/real_hydrology_execution.rs`, V2 resource adapter,
owner implementation, and focused `v3_tests.rs` / `v2_ingress_tests.rs` paths
were already named in the package write set before implementation authority.

Required focused proofs are:

- `heterogeneous_v3_resource_join_debits_ordinary_finalized_use_once`;
- `heterogeneous_v3_resource_join_retains_native_phase_and_spill_custody`;
- `heterogeneous_v3_resource_join_rejects_native_vapor_replay_as_ordinary_use`;
- `heterogeneous_v3_resource_join_rejects_foreign_or_duplicate_finalized_use`;
- `heterogeneous_v3_resource_join_executes_one_ingress`; and
- `heterogeneous_v3_resource_join_rolls_back_all_owners`.

Contract-first validation and expected-red results are recorded below after
direct execution.

Ran 2026-09-02 before production implementation:

- strict Binding Exposure Index checks: PASS for LSE (`7` consolidated rows)
  and SurfaceLiquid (`14` consolidated rows);
- `cargo nextest run --test land_surface_energy_balance_authority_contract`:
  PASS, `18/18`;
- `cargo nextest run --test
  surface_liquid_hydrology_custody_authority_contract`: PASS, `19/19`;
- `cargo fmt --all -- --check`: PASS after formatting the owned authority
  tests;
- `git diff --check` over the owned contracts/index/tests/package/artifact:
  PASS; and
- `cargo nextest run --test
  stage3_native_vegetation_laned_throughput_recovery
  heterogeneous_v3_surface_resource_join_applies_ordinary_finalized_uses_once`:
  expected RED, exit `100`, solely on the absent typed join/constructor, six
  required focused proofs, and the still-present wholesale-resource assignment.

## WAT5 exact accepted additional-segment adjudication

The accepted runner crossed WB16 and then rejected `accepted WAT5 precipitation
source kind`. The accepted LSE forcing contains a typed
`SnowTerminalReceiver`, while WAT5 treated every forcing parcel as raw rain.
The package requires WAT5, WAT, HBP, PASS, and manifest publication for all
required 1/10/19-OFE cases, so omitting WAT5 or dropping the non-rain source is
not an admissible disposition.

The old `SC-OUTPUT-WAT5-001` v2 `GAP-WAT5-001` correctly rejected positive
hourly additional supply because `[f64;24]` had no source or subhourly timing.
The accepted Stage3/Lane-D path now owns distinct, finer exact evidence: raw
rain `Precipitation` forcing parcels; `SnowTerminalReceiver` forcing parcels;
receipt/support-bound routed runon; and phase-receipt/full-child-bound
`LitterPhaseOverflow`. This closes the gap only for those exact accepted
sources, not for arbitrary hourly/daily aggregates or partial frost retention.

Contract-first WAT5 v3 `INV-WAT5-010` / `OBL-WAT5-P-005` binds
`Wat5AdditionalSupplySegmentV1` with a closed
`Wat5AdditionalSupplySourceKindV1::{SnowTerminalReceiver,RoutedRunon,
LitterPhaseOverflow}` enum. Each segment retains source receipt SHA,
transaction, destination OFE, exact half-open day support, OFE-ground depth,
and canonical order. Precipitation remains exclusively the rain hyetograph;
it cannot be relabeled as additional supply. The canonical segment set must
reconstruct accepted per-source and hourly additional-supply ownership exactly
once. Aggregate-only, unknown, foreign, duplicated, omitted, retimed,
relabelled, or receipt-substituted input remains `WAT5-E-001`.

Replay forms the ordered union of rain, additional-segment, and 300-second
boundaries. Each elementary interval advances unchanged WB14 exactly once on
the checked combined rain-plus-additional rate. WAT5 integrates rain only to
`R5`, non-rain supply only to `A5`, and partitions their combined supply into
`F5/D5/G5raw`; independent closure is
`sum(R5+A5)=sum(F5+D5+G5raw)`. Existing closing-to-authoritative-hour behavior
cannot move source support. WAT5 remains diagnostic-only and must preserve WAT,
PASS, HBP, peak, erosion, routing, accepted owners/receipts, and complete
manifest-last output-set rollback.

Before production edit, the write set was extended to the WAT5 contract and
authority test, `direct_runtime/runoff.rs`, `subhourly_generation.rs`, accepted
Stage3 WAT5 projector and focused tests, DC01 replay tests, and runner WAT5
transaction tests. Required proof names are:

- `wat5_exact_segments_keep_rain_and_snow_terminal_distinct`;
- `wat5_exact_segments_preserve_routed_runon_receipt_support`;
- `wat5_exact_segments_preserve_litter_overflow_phase_receipt_support`;
- `wat5_overlapping_sources_advance_piecewise_wb14_once`;
- `wat5_exact_segments_reconstruct_hourly_additional_supply_without_double_count`;
- `wat5_exact_segments_reject_unknown_retimed_or_substituted_source`;
- `wat5_exact_segments_preserve_wat_pass_hbp_manifest_for_1_10_19`; and
- `wat5_exact_segments_roll_back_complete_output_set`.

The v2 runner test
`wat5_day_two_source_failure_publishes_no_partial_output_set` is superseded by
the v3 exact typed-segment authority. Its complete-owner fixture can no longer
be required to fail as "untimed melt," and an unrelated upstream WB14
continuation failure is not WAT5 evidence. The replacement source guard
`wat5_aggregate_only_positive_supply_remains_wat5_e_001` proves that the
aggregate-only entry still reaches exact-segment reconstruction and retains
typed `WAT5-E-001`; the real
`wat5_exact_segments_roll_back_complete_output_set` remains the complete-output
transaction rollback proof.

Post-implementation clean-run evidence reached a separate exact-arithmetic
boundary after crossing the earlier WAT5 source-kind guard. The clean real
runner failed after `626.48 s` at `WAT5-E-002`: positive authoritative WB14
hour with zero raw replay runoff. A source-unmodified `gdb` breakpoint inside
that typed branch established the exact operands:

- lane `0`, day `0`, hour `2`;
- accepted `wb14_hourly_excess_m[2] = 2.9989032090949053e-19 m`;
- replayed hour-2 post-depression excess is exact zero;
- the accepted hourly vector is `[0, 0.008488061229561455,
  2.9989032090949053e-19, 3.084338340286763e-18, 0, ...]`;
- hour 2 has no rain but retains exact receipt-bound `LitterPhaseOverflow`
  supply in every 300-second bin: `[7200,9000)` totals
  `1.629809487737042e-6 m` and `[9000,10800)` totals
  `2.803293675964245e-6 m`; and
- unchanged continuous WB14 replay infiltrates all of those exact segments,
  while the accepted support-level runoff receipt ledger retains the positive
  `2.9989032090949053e-19 m` roundoff disposition in the same hour.

Thus the v3 source set is complete and day translation is correct; no missing
source, retiming, or boundary shift explains the failure. The remaining
boundary is exact zero versus a positive accepted partition-ledger roundoff.
Production remains paused pending contract authority for bounded
reconciliation because existing authority forbids both losing the positive
accepted bin and inventing/smearing a five-minute runoff shape.

Contract-first gate results are recorded below after direct execution.

Ran 2026-09-02 before production implementation:

- strict Binding Exposure Index: PASS (`2` consolidated rows);
- `cargo nextest run --test subhourly_generation_contract`: PASS, `3/3`;
- focused Rust formatting, workspace formatting check, and owned-file diff
  hygiene: PASS;
- `cargo nextest run --test
  stage3_native_vegetation_laned_throughput_recovery
  wat5_replays_exact_typed_additional_supply_segments_without_retiming`:
  expected RED, exit `100`, solely on five absent typed production/replay
  tokens, eight absent focused proof names, and the current source-kind/
  hourly-aggregate implementation.

## WAT5 subnormal positive closing-ledger adjudication

GDB evidence established the exact non-source-defect case: lane 0, day 0,
hour 2 owns accepted WB14 runoff
`B_h=2.9989032090949053e-19 m`; bins 24--29 each contain
`2.716349146228403e-7 m` and bins 30--35 each contain
`4.672156126607075e-7 m` of exact `LitterPhaseOverflow` additional supply;
there is no rain. The unchanged WAT5 WB14 replay infiltrates all supply and
produces exact zero raw post-depression generation. Exact source identity and
timing are therefore complete, and unconditional `WAT5-E-002` would discard
an authoritative positive source-backed hour.

`TOL-WATBAL-009` cannot authorize a repair because it explicitly never mutates
an hourly bin. Contract-first WAT5 v4 `INV-WAT5-011` /
`OBL-WAT5-P-006` and `TOL-WAT5-002` instead leave the accepted hour unchanged
and define one diagnostic closing-ledger operand:

```text
epsilon_h = checked(B_h - sum(G5raw_h)).
```

Eligibility requires positive `B_h`, exact-zero raw generation, complete
positive same-hour source segments and raw closure, accepted hourly/day closure
under `TOL-WATBAL-009`, and
`0<epsilon_h<=1e-12 m*max(1,S_h,F_h,D_h,B_h)`. The canonical union partition
selects its latest positive combined-source elementary piece by
`arg max(end_s,start_s)`; because 300-second boundaries are already present,
the piece has one containing bin. That bin alone receives
`C5=epsilon_h` and `G5closed=C5`; every other `C5` is positive zero.

`R5/A5/F5/D5/G5raw`, every source support/receipt, and the authoritative hour
remain bit-identical. `C5` is neither supply, raw runoff, nor a debit from
infiltration, depression, or a parcel. Existing public fields independently
reconstruct it, so the output schema remains version `2.0`. First-positive,
uniform, rainfall-shaped, largest-source, caller-last, source-free, split, or
duplicate placement; source/F/D changes; daily-scalar timing; and first-above-
tolerance acceptance are forbidden. Any failure rolls back the complete
output set and runtime state.

Required focused proofs are:

- `wat5_bounded_reconciliation_places_on_latest_positive_source_piece`;
- `wat5_bounded_reconciliation_preserves_raw_supply_infiltration_closure`;
- `wat5_bounded_reconciliation_preserves_authoritative_positive_hour`;
- `wat5_bounded_reconciliation_is_source_order_independent`;
- `wat5_bounded_reconciliation_rejects_first_uniform_or_duplicate_placement`;
- `wat5_bounded_reconciliation_rejects_zero_foreign_or_missing_source_support`;
- `wat5_bounded_reconciliation_accepts_exact_tolerance_boundary`;
- `wat5_bounded_reconciliation_rejects_first_value_above_tolerance`; and
- `wat5_bounded_reconciliation_rolls_back_complete_output_set`.

The already-authorized WAT5 replay/projector, DC01, runner transaction, contract
test, package integration test, and artifact paths cover this amendment; no
write-set expansion or WATBAL contract amendment is required.

Ran 2026-09-02 before production implementation:

- strict Binding Exposure Index: PASS (`3` consolidated rows);
- `cargo nextest run --test subhourly_generation_contract`: PASS, `4/4`;
- focused/workspace formatting and owned-file diff hygiene: PASS;
- `cargo nextest run --test
  stage3_native_vegetation_laned_throughput_recovery
  wat5_bounded_partition_reconciliation_preserves_positive_accepted_hour`:
  expected RED, exit `100`, solely on two absent typed production symbols,
  nine absent focused proofs, and the current unconditional `WAT5-E-002` path.

## WAT5 v4 output-consumer binding

After v4 reconciliation succeeded, publication failed at
`OHOUT-WAT5-E-004` in
`crates/openwepp-hillslope-output/src/hillslope_wat_subhourly.rs`. The output
validator still required exact-zero `additional_supply_depth_mm`, the obsolete
rain-only source code, and
`rainfall_depth_mm=F5+D5+G5raw`. These checks implement pre-v3 authority and
reject the now-authoritative positive typed additional segment even when all
finite, nonnegative, source, raw, and hourly closure evidence is present.

No v5 contract or schema field is required. The v2.0 row already carries rain,
additional supply, raw partition terms, raw and closed generation, accepted
hourly authority, exact v3/v4 source-completeness code, and reported hourly
closure. The streaming writer also retains an hour-final validation state. The
consumer can therefore enforce v3/v4 without trusting a row-local assertion:

- retain every finite/nonnegative identity, clock, unit, method, nullable-
  erosion, ordering, sparse-support, hour, event, file, and rollback guard;
- require exactly
  `rainfall_and_exact_typed_additional_segments_saturation_hourly_zero_order_hold`;
- validate each raw row as
  `rainfall_depth_mm+additional_supply_depth_mm=F5+D5+G5raw`;
- accumulate complete-hour source, infiltration, depression, raw-generation,
  closed-generation, and accepted-hour operands;
- for the ordinary `sum(G5raw)>0` branch, retain the existing authoritative-
  hour closing validation;
- for `sum(G5raw)==0` and an accepted positive hour, identify at most one row
  with positive `closed-G5raw`, require that row itself has positive typed
  `rainfall+additional` support, require it is the latest positive-source row
  in the hour, independently compute the v4 `C5` and `TOL-WAT5-002` bound from
  the complete-hour operands, and require exact closed-hour reconstruction;
  and
- reject missing source, nonlatest/first/uniform/split/duplicate placement,
  above-bound `C5`, changed hourly authority/residual, or incomplete hour before
  publishing any file.

The exact source-completeness code is the public attestation that the positive
additional row derives from receipt-authenticated typed segments; the writer
must not infer a receipt from positive mass alone. Complete-hour validation is
required for latest-bin selection and the tolerance operands. A row-local
relaxation of `validate_row`, approximate acceptance without source code, or
use of `closed-raw` on an ordinary positive-raw scaled hour is forbidden.

Before production work, the package write set was extended to:

- `crates/openwepp-hillslope-output/src/hillslope_wat_subhourly.rs`, including
  its existing embedded focused tests;
- `tests/integration/subhourly_water_output_roundtrip.rs`; and
- `tests/integration/subhourly_generation_properties.rs`.

The contract-derived package guard requires
`WAT5_V4_SOURCE_COMPLETENESS_CODE` and
`validate_v4_bounded_closing_reconciliation`, removal of both stale predicates,
and these focused proof names:

- `output_accepts_exact_typed_additional_supply_source_code`;
- `output_validates_combined_rain_and_additional_raw_closure`;
- `output_validates_single_bounded_closing_reconciliation_on_latest_positive_source_bin`;
- `output_rejects_bounded_reconciliation_without_positive_typed_source`;
- `output_rejects_duplicate_or_nonlatest_bounded_reconciliation`;
- `output_rejects_bounded_reconciliation_above_tolerance`; and
- `output_preserves_hour_and_run_output_set_on_failure`.

Ran 2026-09-02 before output-consumer production implementation:

- `cargo nextest run -p openwepp-hillslope-output`: PASS, `27/27`;
- `cargo nextest run --test subhourly_water_output_roundtrip`: PASS, `3/3`;
- `cargo nextest run --test subhourly_generation_properties`: PASS, `2/2`
  after replacing its superseded pre-v3 refusal strings with v3/v4 exact-
  segment and bounded-reconciliation producer bindings;
- `cargo nextest run --test subhourly_generation_contract`: PASS, `4/4`;
- `cargo fmt --all -- --check` and `git diff --check`: PASS; and
- `cargo nextest run --test
  stage3_native_vegetation_laned_throughput_recovery
  wat5_v4_output_consumer_validates_typed_supply_and_bounded_closing`:
  expected RED, exit `100`, only on the absent v4 consumer constant/helper,
  seven absent focused proofs, exact-zero additional-supply rejection,
  rain-only raw closure, and the obsolete rain-only source-code binding.

Production is authorized only in the three exact output-consumer/test paths
listed above and only with the complete-hour semantics in this section. The
expected-red guard must turn green without a schema-version change, receipt
inference from mass, or relaxation of finite/nonnegative, closure, ordering,
atomic-publication, and rollback checks.

Ran 2026-09-02 after production implementation:

- `cargo test -p openwepp-hillslope-orchestrator
  direct_runtime::subhourly_generation::tests::wat5_bounded_reconciliation
  -- --nocapture`: PASS, `8/8` reconciliation success/poison/boundary proofs;
- `cargo test -p openwepp-runner
  hillslope::tests::wat5_bounded_reconciliation_rolls_back_complete_output_set
  -- --exact --nocapture`: PASS, `1/1` real complete-output rollback proof in
  `74.49 s`;
- `cargo nextest run --test
  stage3_native_vegetation_laned_throughput_recovery
  wat5_bounded_partition_reconciliation_preserves_positive_accepted_hour`:
  PASS, `1/1` package production seam;
- `cargo nextest run --test subhourly_generation_contract`: PASS, `4/4`;
- full subhourly generation module: PASS, `25/25`;
- full DC01 module: PASS, `24/24`;
- full committed Stage3 publication/WAT5 projector module: PASS, `35/35`;
- runner `wat5_` selection: PASS, `5/5` in `76.09 s`;
- output transaction module: PASS, `8/8`;
- `cargo fmt --all -- --check`, `git diff --check`, and a source guard for
  WAT5 debug/probe diagnostics: PASS;
- `bash tools/release/check_authority_suite_antievasion.sh`: PASS; and
- `cargo nextest run --test auth11_required_suite_obligation_guards_contract`:
  PASS, `3/3`.

The first synthetic proof vector produced a legitimate nonzero
`G5raw=2.117582368135751e-22 m`, and the new exact-zero eligibility guard
correctly rejected it. The final proof uses two exact 300-second positive
typed source pieces whose replayed generation is bitwise positive zero; no
production threshold or test assertion was weakened.

The first clean real-runner rerun crossed the prior `WAT5-E-002` boundary and
then failed after `626.90 s` (`633.25 s` including the outer subprocess) at
the separately owned public-row validator:

```text
OHOUT-WAT5-E-004 WAT5-E-003 invalid public row: unsupported supply,
residual, method/source code, or erosion candidate
```

Static tracing identified the exact stale consumer in
`crates/openwepp-hillslope-output/src/hillslope_wat_subhourly.rs`.
`validate_row` still requires `additional_supply_depth_mm == 0`, still accepts
only the pre-v3 `rainfall_complete_saturation_hourly_zero_order_hold` source
code, and still checks raw closure as rainfall-only. The v3/v4 producer
correctly publishes positive typed additional supply, the exact
`rainfall_and_exact_typed_additional_segments_saturation_hourly_zero_order_hold`
code, and combined raw closure
`R5+A5=F5+D5+G5raw`. Existing raw and closed public columns already retain
`C5=G5closed-G5raw` independently; no schema change or relabeling is needed.
No output-crate edit was made before package write-set/consumer-binding
authority.

## Upstream focused closeout and diff-size reconciliation

Ran 2026-09-02 after the canonical solver, candidate-only soil, phase-capacity
spill, and heterogeneous resource joins crossed the real runner into the
separately owned Lane-D WB16 boundary:

- `cargo test -p openwepp-hillslope-orchestrator --lib
  land_surface_energy_shadow::v3_tests::exact_surface_rounding_vectors_cover_sub_ulp_ties_crossing_cancellation_and_subnormal
  -- --exact --nocapture`: PASS, `1/1`;
- `cargo test -p openwepp-hillslope-orchestrator --lib
  land_surface_energy_shadow::v3_tests -- --nocapture`: PASS, `19/19`;
- `cargo fmt --all`: PASS; and
- `git diff --check`: PASS.

The prior `18/19` exact-rounding failure was stale fixture chronology, not a
production regression. The vector uses full-parent `advance_exact`, so its
independently constructed LSE and SurfaceLiquid V2 ending candidates must
carry the final transaction marker, and its V2 continuation must advance from
initial interval zero to interval one. Conversely, the parent-local
high-mirror poison retains its authenticated partial marker. Only the focused
test fixture was corrected; exact-surface production guards were unchanged.

The reconciliation snapshot contained `95` tracked changed/deleted paths and
`12` untracked paths. Intake was clean except for four pre-existing unrelated
review files under `tmp/pdfs/r156-review/`; those files remain untouched. All
`95` tracked paths and the other eight untracked paths are package work: the
new canonical solver, package integration test, and six package log/evidence
files. The exact tracked/untracked path inventory was reported to the package
coordinator from the same snapshot.

Changed text files above 3,000 lines at that snapshot:

- `v11_covered/open_snow_convergence_tests.rs`: `4,327`;
- `v9_real_consumer_shadow/v10_soil_thermal_v2.rs`: `3,544`;
- `v9_real_consumer_shadow/v10_soil_thermal_v2_tests.rs`: `3,167`;
- `snow_stage3_v11_terminal_execution.rs`: `3,130`;
- `direct_runtime/surface_liquid_closure.rs`: `3,130`;
- `SC-SNOWENERGY-001.md`: `3,117`;
- `direct_runtime/surface_liquid_ingress.rs`: `3,098`; and
- `v11_covered/owner_finalization.rs`: `3,004`.

Changed text files from 2,001 through 3,000 lines:

- `v11_covered/execution.rs`: `2,955`;
- `land_surface_energy_shadow/real_hydrology_execution.rs`: `2,882`;
- runner `00_builders_and_authority.rs`: `2,723`;
- `v9_real_consumer_shadow.rs`: `2,707`;
- `SC-WATBAL-001.md`: `2,648`;
- `direct_runtime/03_executor.rs`: `2,608`;
- `direct_runtime/stage3_committed_publication.rs`: `2,564`;
- runner `stage3_runner_qualification.rs`: `2,380`;
- `v11_covered/carrier_phase.rs`: `2,355`;
- `snow_terminal_enthalpy_event_numerics_contract.rs`: `2,295`;
- `SC-LANDSURFACEENERGY-001.md`: `2,293`;
- runner `05_runner_execution_and_outputs.rs`: `2,221`; and
- `direct_runtime/laned_active.rs`: `2,033`.

## V16 exact-owner topology-rank authority

The real multi-OFE path exposed a scale-only custody defect in
`direct_runtime/surface_liquid_owner/v3_exact_enthalpy.rs`: context-free owner
validation compared complete surface keys lexically, so authenticated physical
topology `ofe-9 -> ofe-10` was rejected because the opaque identifier text
orders oppositely. The accepted-energy operand validator used the same false
cross-OFE authority and would have failed next.

Contracts were amended first. SC-SURFACELIQUID-001 v23 adds
`INV-SURFACELIQUID-030` / `OBL-SURFACELIQUID-C-020`, and
SC-LANDSURFACEENERGY-001 v23 adds `INV-LANDSURFACEENERGY-158` /
`OBL-LANDSURFACEENERGY-C-013`. Canonical order is configuration topology rank,
then the existing `DirectSurfaceLiquidStoreKey` order with OFE held equal;
accepted operand groups use the same owner-record rank, then existing kind and
ordinal. OFE IDs remain opaque: lexical comparison and numeric parsing are
both forbidden.

The existing wire schema is sufficient and unchanged. Context-free owner
validation may prove schema, finite coordinates, unique keys, digests, and
lineage, but cannot infer installable topology from an OFE spelling. The
authenticated frozen-parent/configuration join must validate complete exact-
owner record sequence equality against the already topology-ranked
SurfaceLiquid V2 configuration/state sequence, including configuration digest
and LSE V3 key membership. Receipt operand order can use the authenticated
beginning owner record rank passed to its existing structural validator.
Adoption inherits the configuration-ranked surface sequence; advance,
restart/checkpoint, projection, and replay preserve it exactly.

Production is authorized only to:

- replace the bare lexical record-order predicate with exact duplicate-key
  refusal;
- add `validate_topology_ranked_exact_surface_order` at the existing
  authenticated configuration/frozen-parent join;
- compare accepted operands by beginning-record rank, kind, and ordinal; and
- add the seven focused scale/poison proofs named by the package integration
  guard in the already-authorized `land_surface_energy_shadow/v3_tests.rs`.

Omission, substitution, duplicate/foreign keys, topology-relative or within-
OFE reorder, operand-group reorder, stale configuration/digest, cross-owner
topology mismatch, and beginning/ending reorder remain typed failures with
complete rollback. Sorting caller bytes, parsing OFE numerals, trusting mass or
digest alone, schema changes, dual acceptance, or relaxing receipt/custody
guards are not authorized.

Ran 2026-09-02 before production implementation:

- `cargo nextest run --test land_surface_energy_balance_authority_contract`:
  PASS, `19/19`;
- `cargo nextest run --test
  surface_liquid_hydrology_custody_authority_contract`: PASS, `20/20`;
- strict Binding Exposure Index lint: PASS, LSE `8` consolidated rows and
  SurfaceLiquid `15` consolidated rows;
- `cargo nextest run --test
  stage3_native_vegetation_laned_throughput_recovery
  exact_surface_owner_uses_authenticated_topology_rank_for_opaque_ofe_ids`:
  expected RED, exit `100`, solely on the absent helper, seven absent focused
  proofs, and the two stale lexical owner/operand predicates; and
- `cargo fmt --all -- --check` and `git diff --check`: PASS after formatting
  the new package guard.

Ran 2026-09-02 after the topology-ranked exact-owner implementation:

- bare envelope validation now proves nonempty, finite, exact-carry, digest,
  and duplicate-free keys without treating opaque OFE spelling as topology;
- the frozen-parent/configuration join validates the exact record sequence
  against the authenticated topology-ranked SurfaceLiquid V2 configuration
  and state, the configuration digest, and complete LSE V3 key membership;
- accepted operands compare their passed beginning-owner record rank before
  kind and ordinal, preserving exact record/receipt order without sorting or
  identifier parsing;
- the seven named topology proofs passed, `7/7`, including physical
  `1/9/10/19`-OFE adoption and restart replay, `ofe-9 -> ofe-10`, opaque
  nonlexical IDs, within-OFE/operand ordering, duplicate/omission/substitution,
  topology/within-OFE reorder, stale/cross-owner identity, and byte rollback;
- `cargo nextest run --test
  stage3_native_vegetation_laned_throughput_recovery
  exact_surface_owner_uses_authenticated_topology_rank_for_opaque_ofe_ids`:
  PASS, `1/1`;
- focused exact-surface suite: PASS, `17/17`; topology-ranked named subset:
  PASS, `7/7`; V3 complete-owner projection: PASS, `8/8`; parent-local exact
  chronology: PASS, `4/4`; feature-gated V4 restart/projection poisons: PASS,
  `2/2`; persisted SurfaceLiquid restart scale/poison selection: PASS, `9/9`;
  and
- LSE/SurfaceLiquid v23 authority contracts: PASS, `39/39` combined.

## WAT5 v4 public-output consumer closeout

Ran 2026-09-02 after the contract-first output-consumer binding authorized
the existing WAT5 v4 schema. The public writer now attests the exact typed
rain-plus-additional source code, checks row-local
`R5+A5=F5+D5+G5raw`, and validates the bounded zero-raw reconciliation only
after accumulating the complete hour. For an accepted-positive/raw-zero
hour it admits exactly one `C5=G5closed-G5raw` on the latest positive typed
source row, applies `TOL-WAT5-002` to full-hour operands, and requires exact
closed-hour reconstruction. Ordinary positive-raw scaling remains a distinct
unchanged path; the validator does not infer a receipt from mass.

Validation evidence:

- `cargo check -p openwepp-hillslope-output --tests`: PASS;
- six embedded `output_*` WAT5 v4 consumer proofs: PASS, `6/6`;
- `subhourly_water_output_roundtrip::output_preserves_hour_and_run_output_set_on_failure`:
  PASS, `1/1` (seventh complete-output rollback proof);
- package guard
  `wat5_v4_output_consumer_validates_typed_supply_and_bounded_closing`:
  PASS, `1/1`;
- `cargo nextest run -p openwepp-hillslope-output`: PASS, `33/33`;
- `cargo nextest run --test subhourly_water_output_roundtrip`: PASS, `4/4`;
- `cargo nextest run --test subhourly_generation_properties`: PASS, `2/2`;
- `cargo nextest run --test subhourly_generation_contract`: PASS, `4/4`;
- `cargo fmt --all -- --check` and `git diff --check`: PASS;
- diagnostic-source guard over the WAT5 producer/consumer files: PASS;
- `bash tools/release/check_authority_suite_antievasion.sh`: PASS; and
- `cargo nextest run --test auth11_required_suite_obligation_guards_contract`:
  PASS, `3/3`.

## Lane-D public HBP hourly-source binding

The clean real qualification subsequently crossed the WAT5 v4 producer and
public-output consumer and exposed a downstream HBP authority drop. The
authenticated 24-bin Lane-D outlet weights were present on
`DirectDayFrame::laned_active_routing.routed_weights` and already drove the
routed erosion-shape input, but a no-Wave-1 day produced
`DirectPublicationErosionOperands::zero_authority()`. That constructor leaves
both public hourly fields absent, so the unchanged HBP consumer correctly
selected payload minor 0 and the qualification reconstructed zero hourly
volume bins instead of the required 24.

The correction is at the authorized post-erosion public-row seam in
`direct_runtime/03_executor.rs`. Immediately after
`DirectPublicationDayRow::from_day_frame` and before frame commit or row
consumption, the executor transactionally binds the exact authenticated
routed weights to `hourly_runoff_fraction`. An already-present runoff surface
must match every `f64::to_bits`; an inactive Wave-1 day with no sediment
surface receives the exact-zero 24-bin sediment pair required by the existing
HBP minor-1 schema. Existing finite/nonnegative nonzero sediment remains
bit-identical. Missing routing, active Wave-1 without sediment, runoff
mismatch, or invalid sediment fails typed before the row is mutated. Exact-
zero routing days now retain an authenticated all-zero routing record even
when diagnostic trace output is disabled. The HBP consumer remains unchanged:
it forms `V_h = routed_weight_h * runvol_m3`, preserves the routed peak hour,
and keeps manifest-last publication atomicity.

Ran 2026-09-02 after the HBP binding:

- `cargo test -p openwepp-hillslope-orchestrator
  laned_active_public_hourly_pair -- --nocapture`: PASS, `5/5` exact 24-bin /
  peak, nonzero-sediment preservation, mismatch, missing-source, invalid-
  sediment, and rollback proofs;
- `cargo test -p openwepp-hillslope-orchestrator
  direct_runtime::cqr_executor_tests -- --nocapture`: PASS, `13/13`;
- package source/consumer guard
  `stage3_lane_d_hbp_reads_authenticated_public_hourly_surfaces`: PASS, `1/1`;
- `cargo check -p openwepp-runner --tests`: PASS;
- runner `hbp` selection: PASS;
- `cargo test -p openwepp-hillslope-output -- --nocapture`: PASS, `33/33`;
- `cargo test --test subhourly_water_output_roundtrip -- --nocapture`: PASS,
  `4/4`;
- `cargo test --test subhourly_generation_properties -- --nocapture`: PASS,
  `2/2`;
- `cargo test --test subhourly_generation_contract -- --nocapture`: PASS,
  `4/4`;
- runner `output_transaction` selection: PASS, `8/8`;
- focused `rustfmt --check`, owned-file `git diff --check`, and added-line
  diagnostic-source scan: PASS;
- `bash tools/release/check_authority_suite_antievasion.sh`: PASS; and
- `cargo test --test auth11_required_suite_obligation_guards_contract
  -- --nocapture`: PASS, `3/3`.

The first clean rerun after that focused implementation reproduced the HBP
minor-0 assertion (`0` versus `24` hourly volumes) after `645.09 s` in the
inner test and `651.01 s` including the outer subprocess. The correction was
sound but initially called from the ordinary active/no-Stage3 publication
branch. Exact call-order tracing showed that an attached Stage3 owner
deliberately bypasses that branch: `Stage3AcceptedPublicationDayV1` first
routes Lane D and seals its lane frames, then `03_executor.rs` clones the
committed frames, constructs fresh public rows, and sends those rows directly
to the streaming sink. The sink clones the unbound row as its last/event row,
which is the exact copy that lost the hourly pair.

The single attachment call was therefore moved—not duplicated—to the live
committed-Stage3 branch, after the accepted frame clone and
`DirectPublicationDayRow::from_day_frame`, before `consume_row`. The ordinary
active branch is unchanged from its pre-correction behavior. The package HBP
guard now proves this exact ordering and proves one attachment call in the
executor source. After the move, the five binding proofs, the live-branch
source/order guard, focused formatting, and `cargo check -p openwepp-runner
--tests` all pass.

## Terminal Lane-D PASS/HBP runoff binding

The next clean qualification crossed the 24-bin HBP assertion and exposed a
terminal-volume authority mismatch. The committed Lane-D terminal outlet book
was `0.8488061229561459 m3`, while the public PASS row still carried the
pre-route local hydrology projection (`0.8471105124736579 m3`). The difference
is the authenticated router storage/clamp ledger, not an area conversion or a
permitted tolerance. In the same single transactional committed-row binder,
the terminal OFE now receives exact `runvol_m3 = outlet_m3` and exact peak
`max(routed_weight_h * outlet_m3) / 3600`. WAT `q_mm`, `qofe_mm`, duration, and
all nonterminal runoff scalars remain bit-identical. Before any row is
published, the complete committed day validates finite/nonnegative operands,
the hourly-weight sum, and `sum(source)+sum(clamp)=terminal outlet+sum(storage)`;
all failures leave the row/output transaction unchanged.

Ran 2026-09-02 after the terminal binder correction:

- the four exact terminal outlet/peak, nonterminal-preservation,
  invalid-weight rollback, and source/clamp/outlet/storage closure-poison
  selections: PASS, `1/1` each;
- `cargo test -p openwepp-hillslope-orchestrator
  direct_runtime::cqr_executor_tests -- --nocapture`: PASS, `17/17`;
- package live-path/HBP/PASS source-order guard
  `stage3_lane_d_hbp_reads_authenticated_public_hourly_surfaces`: PASS, `1/1`;
- `cargo check -p openwepp-runner --tests`: PASS;
- runner HBP selection and the real `1/10/19` WAT/PASS/HBP/manifest focused
  proof: PASS;
- runner output transaction module: PASS, `8/8`;
- output crate: PASS, `33/33`; roundtrip: PASS, `4/4`; generation properties:
  PASS, `2/2`; generation contract: PASS, `4/4`;
- `cargo fmt --all -- --check`, `git diff --check`, and the owned-file added-line
  diagnostic scan: PASS; and
- authority anti-evasion and AUTH11 obligation guards: PASS (`3/3` AUTH11).

The clean exact runner then crossed both the prior HBP and PASS boundaries and
reached a separate fixture-authority gate after `1306.70 s` (`1307.10 s`
including the outer subprocess):

```text
HS-SIMOUT-E-001 Stage-3 production seed WB14 parameters must be nonempty and
strictly OFE ordered
```

Static tracing identifies `snow_stage3_v11_production_seed.rs`'s
`validate_wb14_authority` string-window comparison as the exact cause. The live
topology correctly emits physical numeric lane order `ofe-1, ..., ofe-9,
ofe-10, ...`; lexical comparison rejects the valid `ofe-9 -> ofe-10`
transition in the 10/19-OFE qualification. No seed-authority edit was made
without an explicit write-set amendment.

Before correction, the package write set was extended to that exact runner
seed file. The authorized fix must validate nonempty, duplicate-free equality
to the already-authenticated physical OFE topology order; it may not sort,
rename, or infer numeric order from identifier strings.

The implemented seed correction follows that rule exactly. The 1/9/10/19
success and duplicate/reordered/missing/foreign no-mutation proofs pass `8/8`;
the broader seed unit module passes `16/16`; runner test compilation,
format/diff/diagnostic, authority anti-evasion, and AUTH11 gates pass.

The next clean exact qualification crossed the corrected WB14 seed gate and
then reached a second restart-layer lexical-order guard after `1306.78 s`
(`1307.23 s` including the outer subprocess):

```text
HS-SIMOUT-E-001 invalid GSI/forcing cardinality or ordering: forcing
destination canonical order
```

The exact source is
`crates/openwepp-persisted-restart-v1/src/gsi_forcing.rs`: restart `restore()`
lexically compares adjacent `(ofe_id,tile_id)` strings and rejects the same
authenticated physical `ofe-9 -> ofe-10` transition. Native static forcing
validation already requires nonempty unique destination identities and does
not impose lexical order. No persisted-restart edit was made before a package
write-set amendment.

Before correction, the package write set was extended to the exact
`gsi_forcing.rs` restart boundary. The authorized correction must accept the
already-authenticated physical destination sequence without sorting or parsing
identifier strings, retain nonempty/duplicate/omission/substitution/reorder and
digest poison rejection, and prove 1/9/10/19-OFE projection/restore behavior.

## Surface-liquid restart physical-order boundary

The corrected release qualification next crossed GSI restore and failed after
`78.94 s` CPU (`79.31 s` wall, build excluded) while bootstrapping the 10-OFE
surface-liquid owner:

```text
HS-SIMOUT-E-001 records or continuations are not strictly ordered and unique
```

Static tracing identifies `openwepp-persisted-restart-v1/src/surface_liquid.rs`
as a second independent lexical-order assumption: bare `restore()` compares
complete record keys and continuation OFE IDs by string order, rejecting valid
authenticated physical `ofe-9 -> ofe-10` order. The production call uses
`restore_with_configuration`, whose native validation already binds every
state record to the configuration's topology-ranked canonical record sequence,
binds continuations positionally to physical `ofe_topology`, and verifies the
order-sensitive state/configuration digests. Before correction, the package
write set was extended to this exact persisted-restart file. The authorized
fix may replace only the lexical-order assumption with duplicate detection;
configuration-relative ordering, omission/substitution/reorder, digest, and
no-mutation poisons must remain fail-closed and receive 1/9/10/19 proofs.

The persisted-restart correction removes only that lexical comparison.
Projection and restoration still call the native nonempty/unique destination
validator, and restoration still reconstructs and compares the order-sensitive
configuration digest. No identifier sorting, suffix parsing, renaming, or
topology inference was introduced.

Ran 2026-09-02:

- `cargo test -p openwepp-persisted-restart-v1
  static_configuration_restart_ -- --nocapture`: PASS, `10/10`;
- scale-shaped projection/restore succeeds for exact physical 1/9/10/19-OFE
  destination order;
- duplicate destinations fail native identity validation;
- omission, substitution, and reorder with the original seal fail the
  order-sensitive configuration digest;
- an explicitly stale configuration digest fails; and
- an intentionally changed physical sequence is admitted locally only after
  projection creates its distinct self-consistent seal, matching the actual
  restart DTO authority boundary. Enclosing checkpoint/topology evidence
  retains its independent seal.

## Canonical-solver line-count splits

Ran 2026-09-02 after the authorized behavior-preserving lexical splits:

- `v11_covered/open_snow_convergence_tests.rs` is `1,332` lines. The later
  terminal verification found that the unreferenced, `#[cfg(any())]`
  `open_snow_superseded_historical_solver_tests.rs` retained superseded solver
  logic without a deletion gate; it was deleted on 2026-09-04 rather than
  carried as diagnostic debt;
- `v11_covered/owner_finalization.rs` is `2,823` lines and its textually
  included `owner_finalization_nitrogen_protocol_tests.rs` is `182` lines;
- `cargo check -p openwepp-hillslope-orchestrator --tests`: PASS;
- `cargo test -p openwepp-hillslope-orchestrator --lib
  v9_real_consumer_shadow::v11_covered::owner_finalization::nitrogen_protocol_cardinality_tests
  -- --nocapture`: PASS, `6/6`;
- `cargo test -p openwepp-hillslope-orchestrator --lib
  v9_real_consumer_shadow::v11_covered::owner_finalization::terminal_custody_lane_set_tests
  -- --nocapture`: PASS, `17/17`;
- `cargo test -p openwepp-hillslope-orchestrator --lib
  v9_real_consumer_shadow::v11_covered::covered_convergence_policy_tests::real_
  -- --nocapture`: PASS, `9/9`;
- `cargo nextest run --test
  stage3_native_vegetation_laned_throughput_recovery
  canonical_stage3_production_source_has_no_historical_solver_family`: PASS,
  `1/1`;
- `cargo nextest run --test
  snow_terminal_enthalpy_event_numerics_contract`: PASS, `38/38`, with `22`
  intentionally skipped retired historical-version checks;
- `cargo fmt --all -- --check`: PASS; and
- `git diff --check`: PASS.

The historical solver test module remains `cfg(any())` and is explicitly
allowlisted only as nonproduction history by the package anti-accretion guard.
The older snow authority source reader now concatenates the active and
historical test files, preserving every existing source-level obligation.
Both production modules use same-scope `include!`; no item visibility, test
path, solver behavior, custody guard, or source transaction changed.

## Validated in-memory custody handoff authority gate

Static adjudication of the current profile identified three redundant trusted
in-process validation paths: frozen-litter V3 resident installation serializes
and reparses its complete accepted-publication history; the surface-liquid V2
resource candidate repeatedly serializes its three nested owner envelopes; and
V10/V11 parent finalization recomputes validation/digest state. None of the
existing contracts requires a serialization roundtrip between trusted
in-process owners of the exact same immutable revision. Existing restart,
external-byte, durable-publication, and untrusted-executor checks are binding
trust boundaries and remain mandatory.

Contract-first authority was added before production work:

- `SC-SURFACELIQUID-001` v24, `INV-SURFACELIQUID-031` and
  `OBL-SURFACELIQUID-C-021`;
- `SC-LANDSURFACEENERGY-001` v24, `INV-LANDSURFACEENERGY-159` and
  `OBL-LANDSURFACEENERGY-C-014`; and
- `SC-VEGETATION-001` v31, `INV-VEGETATION-134` and
  `OBL-VEGETATION-C-006`, cross-linked to retained
  `SC-SNOWENERGY-001#INV-SNOWENERGY-083` restart/history authority.

The authorized carrier is private, immutable, and nonserializable. It may be
minted only by full semantic validation and binds schema/model/configuration,
the complete state/envelope digest, transaction/predecessor/support, exact
revision, and applicable publication-prefix count/head/tail/chain digest. It
has no mutable dereference, public/unchecked constructor, wire or persistence
form. Mutation, replacement, lineage normalization, configuration or support
change consumes the proof and requires a newly validated revision. Optional
cached canonical bytes/digests are derived evidence only, must equal fresh
canonical serialization, are keyed to the exact revision, and are discarded
on mutation. Restart/checkpoint restore, external/durable bytes, and every
untrusted `V11SegmentExecutor` result still perform fresh complete parse,
canonical reconstruction, digest and semantic validation.

The package exact write set was extended before implementation to
`frozen_litter_v3_publication_retention.rs`,
`v9_real_consumer_shadow_wb14_routing_tests.rs`, vegetation `v10_state.rs` and
`v11.rs`, `snow_stage3_v11_parent_chronology_tests.rs`, the Vegetation contract
and authority test, plus the already-authorized frozen-adoption, surface V2
ingress, LSE execution/finalization, and package integration paths.

Ran 2026-09-02:

- strict Binding Exposure Index checks: PASS for Surface (`16` rows), LSE
  (`9` rows), and Vegetation (`18` rows);
- `cargo nextest run --test land_surface_energy_balance_authority_contract
  --test surface_liquid_hydrology_custody_authority_contract`: PASS, `41/41`;
- focused Vegetation v31 authority binding: PASS, `1/1`;
- combined three-suite diagnostic: `66/70` PASS. The four remaining failures
  are inherited/concurrent environment or authority-image failures outside
  this amendment: two immutable WATBAL digest expectations and two V9 isolated
  runtime checks reporting an additional `libz.so.1` mismatch;
- an additional snow-terminal registry compatibility selection remains RED on
  an unrelated pre-existing missing v29 snow-energy index phrase; the retained
  Vegetation v30 index phrase passed that selection;
- package filter `validated_handoff_`: expected RED, exit `100`, `0/2`, solely because
  `ValidatedFrozenLitterV3ResidentHandoffV1` and the named frozen/surface/
  vegetation runtime proof groups do not yet exist; and
- targeted rustfmt check found pre-existing formatting drift elsewhere in the
  three long authority-test files. The newly added blocks were conformed
  manually; no unrelated bulk formatting was applied in the shared worktree.

Production is authorized to proceed only within the recorded exact write set
and these constructor semantics. It may move an already-validated unchanged
resident/candidate/state or validate one appended publication tail. It may not
skip validation across mutation or a trust boundary, mint a proof from digest
alone, expose an unchecked/public constructor, persist the proof/cache, omit
restart prefix replay, or remove the independent validation of untrusted V11
executor output. This record stops before production implementation.

## Multi-OFE vegetation-binding fixture boundary

Static tracing of the corrected release 10-OFE qualification failure found the
typed V8 bijection refusal at `validate_v8_component_bindings`, before any
candidate construction, parent finalization, or install. The production guard
correctly rejected duplicate component/occupancy identities. The defect is in
the exact test-seed expansion: it rebounded OFE and LSE tile identity for each
live lane but retained the same configured `vegetation_tile_id`, so every
multi-OFE covered projection emitted the same `stratum::vegetation_tile`
component and occupancy. This is not topology ordering or incomplete custody.

Existing `SC-VEGETATION-001` authority requires the current hillslope-global
BGC inventory to have exactly one common BGC-bearing OFE. No contract amendment
or production-validator change is authorized. Before the fixture edit, the
package disposition is therefore: preserve the configured vegetation mapping
only on the downstream bearing OFE; give every other OFE a unique
nonconfigured vegetation identity with `OpenRawPrecipitation` ingress; retain
physical OFE order; and prove valid 1/9/10/19 execution plus duplicate-mapping,
10-OFE topology-swap, and byte-exact rollback poisons. The authorized write
set already contains `snow_stage3_v11_production_seed.rs` and
`stage3_runner_qualification.rs`.

The validated-handoff implementation also touches a pre-existing
over-3,000-line `openwepp-vegetation/src/v11.rs`. Before extraction, the exact
write set is extended to `v11_tests.rs` for a same-scope, behavior-preserving
move of the complete `#[cfg(test)] mod tests` block. Production behavior and
nested test-module paths must remain unchanged, and both resulting files must
remain below the hard ceiling.

## Frozen-litter V3 and SurfaceLiquid V2 validated handoffs

Implemented the two authorized in-memory handoffs under
SC-SURFACELIQUID-001 v24 INV-031/C-021 and SC-LANDSURFACEENERGY-001 v24
INV-159/C-014. Frozen-litter installation now moves an immutable resident whose
private revision proof binds both owner revisions and the validated publication
tail; restart restores still replay every canonical publication. Append
validation checks the new tail while retaining the already-validated prefix.
SurfaceLiquid V2 preparation fully validates and privately seals each resource
revision once; ordinary mutation fully validates and remints a successor proof,
and ingress consumes only the unchanged validated revision. Public/untrusted
validation remains a full independent validation.

Ran 2026-09-02:

- `cargo check -p openwepp-hillslope-orchestrator --tests`: PASS;
- exact frozen-litter handoff proof selection: PASS, `5/5`;
- exact SurfaceLiquid V2 handoff proof selection: PASS, `5/5`;
- combined exact handoff proof selection: PASS, `10/10`;
- package runtime proof-group guard: PASS; package static production-seam guard
  reached only the concurrent Vegetation V11 assertion and remained RED there
  (`untrusted V11 executor output must retain independent full validation`),
  after the frozen and surface source assertions passed; and
- targeted `rustfmt` on the four touched Rust files plus targeted
  `git diff --check`: PASS.

No heavy performance or scale qualification was run for this bounded
optimization increment.

## Representative long-run workload admission

Ran 2026-09-02: added the checked-in
`representative-10ofe-100year-workload.json` recipe and the isolated
`stage3_long_run_qualification.rs` release harness. The recipe binds the P102
topology/soil/runfile, native-forest YAML authority, 10 unequal OFE areas,
exact typed Stage-3 owner sidecar, Gregorian repetition/leap policy, and real
default-eligible Lane D. The century management expands to the contract-required
`1,000` explicit year/OFE slots; a rotation-count-only attempt was rejected by
`MAN-YAML-E-007` and corrected before execution.

Ran focused evidence:

- pure calendar/forcing recipe proof: PASS;
- full 100-year management/climate parse, 10-OFE static frame, exact `36,525`
  day count, and exact owner-sidecar cardinality: PASS; and
- combined selection: PASS, `2/2` in `0.959 s`.

The complete-year and 100-year tests remain `#[ignore]` and are reserved for
the required release comparator run. Intake success is not runtime or
throughput qualification.

## Current exact-diff and leaf-quality reconciliation

Ran 2026-09-02 after the long-run harness was admitted:

- compared every modified/deleted/untracked path with the package exact write
  set. Three modified files that earlier prose did not name
  (`direct_runtime.rs`, `snow_stage3_v11_terminal_execution_tests.rs`, and
  `v10_soil_thermal_v2_v49_tests.rs`) were added explicitly during
  reconciliation. Version-specific solver-test shorthand was expanded to full
  paths. The only remaining unlisted files are the inherited unrelated
  `tmp/pdfs/r156-review/` files, which remain untouched;
- `cargo fmt --all -- --check`: PASS under `nix develop`;
- `git diff --check`: PASS;
- package authority/anti-evasion suite: PASS, `16/16`;
- source-level authority anti-evasion shell gate: PASS; and
- required-suite obligation guard: PASS, `3/3`.

Warnings-denied Clippy is green for the two leaf crates whose changed
production surfaces have locally actionable baselines:
`openwepp-hillslope-output` and `openwepp-vegetation`. The WAT5 validator was
split into one private row-against-hour helper without changing behavior;
the output suite remains PASS, `33/33`. A broad affected-crate Clippy attempt
is not a regression gate because the inherited LSE/orchestrator trees emit a
large pre-existing warnings-denied backlog in unchanged production and test
code. No package scope was expanded to rewrite that unrelated backlog.

This is a current-source reconciliation, not the terminal one. Performance
correction, review, and verification may still change the authorized diff.

## Snow-free physical-reuse contract gate

Static/Ran 2026-09-02: amended SC-COUPLEDTIME-001 v12,
SC-VEGETATIONTRANSACTION-001 v16, SC-LANDSURFACEENERGY-001 v25, and
SC-SURFACELIQUID-001 v26 before production edits. The new authority permits
one private move-only fully validated provisional physical ending to reseal
only final accepted-slab-dependent identities. It requires exact final owner
bytes, zero provisional and one final publication, single use, restart
invalidation/fresh execution, exhaustive non-slab poisons, and no physical
replay fallback.

Ran the exact package test
`snow_free_final_receipt_reseal_is_contract_bound_and_production_reachable`.
All four contract/version/invariant/obligation assertions passed and the test
then failed as expected on the absent production symbol
`SnowFreePhysicalReuseSeedV1`. This is the contract-first expected red; no
production source for this increment had been edited when it was recorded.

## Native WB14 parent synchronization repair

Ran 2026-09-02: the real release path exposed both legal synchronization
directions. A represented-snow interval can leave an already-open native V2
parent that must adopt the accepted V1 liquid arithmetic, while day-zero can
open the legacy V1 parent before the native V2 successor has a parent. The
native successor now constructs and fully validates that missing V2 parent
from the authenticated V1 persistent/candidate liquid states while preserving
native ice and enthalpy. At the parent endpoint it still reconstructs the
final V2 owner and clears the working parent only after exact reverse
projection succeeds.

Ran the focused real-child regression
`staged_v1_parent_advances_native_v2_into_the_real_coupled_child_join`: PASS,
including exact equality between an adopted already-open V1 parent and the V2
parent independently produced by the real ingress path. The source guard also
requires adoption, staging, and finalization to avoid restart serialization.

## Native inactive-prefix WB14 transition contract gate

Static 2026-09-03: the authentic real-runner correction exposed a distinct
parent-local chronology gap. A represented-snow prefix correctly performs zero
SurfaceLiquid/WB14 physics and therefore owns no WB14 child receipt, but the
first exact snow-free child cannot enter an ordinary WB14 parent whose working
cursor still names the parent origin. Advancing that cursor without replayable
authority would make restart validation unsound; inventing an under-snow WB14
receipt would violate inactive-custody authority.

Before production edits, the package write set was extended exactly to
`direct_runtime/surface_liquid_wb14.rs` and canonical authority was amended:

- `SC-SURFACELIQUID-001` v28 `INV-035/C-025`;
- `SC-COUPLEDTIME-001` v14 `INV-031/C-014`; and
- `SC-SNOWENERGY-001` revision 58 non-versioned ADR-0044 companion
  `INV-087/C-055`.

The authorized design consumes a complete coupled-receipt-backed inactive-
prefix proof, preserves WB14 beginning cumulative bits, advances only the
parent-local accepted-support cursor, creates no inactive physical receipt,
and begins the first snow-free physical child at ordinal zero. The canonical
proof digest enters per-OFE parent identity and the receipt-chain seed and is
retained through final receipt, replay, and restart. Ordinary parent bytes and
semantics remain unchanged.

Ran 2026-09-03 before the WB14 prefix type or constructor existed:

- `cargo test -p openwepp-hillslope-orchestrator native_inactive_prefix_starts_first_physical_child_without_wb14_receipt --no-run`: expected RED, compiling the contract-derived test failed only on the absent inactive-prefix proof type, transition constructor, and final-receipt proof field.

The completed WB14 type/constructor and real producer/consumer plumbing bind
the canonical proof digest, parent identity and receipt-chain seed, exact
prefix-end cursor, physical ordinal zero, unchanged cumulative bits, and zero
inactive child receipts. Current-source validation passed the native inactive-
prefix suite `6/6`, the advanced-cursor-without-proof poison, package integration
`23/23`, orchestrator test-target check, and the authentic nested/outer runner
qualification `1/1` in 272.51 seconds. The code and QA rereviews APPROVED the
ordered receipt chronology, owner adjacency, restart/suffix equivalence,
poison/rollback coverage, and unchanged ordinary path.

## Native publication-history Arc/COW expected red

Ran 2026-09-03 before the V3/V4 history representation changed:

- `env RUST_MIN_STACK=67108864 nix develop -c cargo test -p openwepp-hillslope-orchestrator --lib v3_clone_shares_history_until_accepted_append_detaches --no-run`: expected RED, exit 101 in 7.0 seconds. Compilation emitted 13 E0599 errors only for the deliberately absent V3/V4 history-sharing, accepted-append, and forced-deep-clone test seams.

This expected red binds the performance correction to immutable shared history
with copy-on-write only at accepted append. It does not authorize value,
ordering, restart-byte, error-precedence, or rollback changes.

## Canonical two-map pending-adjudication contract gate

Static 2026-09-03, before the eventual solver production edit: the first
contract proposal used SnowEnergy revision 59 and CoupledTime v15 to predeclare
an independent `FinalAccepted@1` charge after an apparent initial closure.
Contract-derived tests were authored first, and the required expected-red ran:

- `env RUST_MIN_STACK=67108864 nix develop -c cargo test -p
  openwepp-hillslope-orchestrator
  canonical_covered_two_map_final_failures_never_fall_through_to_predictor
  -- --exact --nocapture`: expected RED, exit 101 in 11.52 seconds. Compilation
  failed only on the deliberately absent `FinalOuterNonclosure` and
  `FinalDependentNonclosure` parity poisons and `FinalConvergence` rejection
  stage (four E0599 errors).

Implementation discovery then falsified the proposal before it was accepted:
the initial candidate is pre-support state while its authentic map output is
post-support state, including a required advancing interval cursor and physical
lifecycle changes. Initial candidate-versus-output closure is therefore not a
sound eligibility predicate. The attempted shortcut and its temporary test
seams were removed without running or retaining a production implementation.
The expected red above remains evidence for the rejected proposal only.

Authority was corrected before renewed production work in SnowEnergy revision
60, CoupledTime v16, LandSurfaceEnergy v27, SurfaceLiquid v29, and
VegetationTransaction v18. Each post-initial charge validates exact physical,
identity, and discrete custody before returning one private, non-Clone,
non-wire pending adjudication map. It is consumed exactly once into:

- iteration history after outer candidate-versus-own-output nonclosure;
- typed adaptive rejection after outer closure but dependent-output
  nonclosure; or
- same-map `FinalAccepted` disposition and complete-envelope construction
  after both closures and exact custody succeed.

The exact roles are `Initial@0`, `FixedPointAdjudication@1`, then contiguous
`MultisecantAdjudication(n)@(n+1)` as needed. `FinalAccepted` is a disposition
of the last charged adjudication, not another physical charge. Consequently
`M=N+2`, `0<=N<=5`, and `2<=M<=7` beneath the unchanged maximum-eight ceiling.
No replay, completed-nonfinal promotion, fallback solver, constitutive change,
tolerance change, adaptive-response change, publication, restart, or wire
change is authorized. A fresh contract-derived expected red for this pending
typestate was required before production implementation.

Independent static rereview APPROVED the corrected authority after requiring
and verifying custody-before-pending sequencing, explicit outer-nonclosure
history with no error, dependent-only typed rejection with no history, all
three consuming dispositions, and the revision-60 no-process-version metadata.

Ran before the pending-adjudication production edit:

- `env RUST_MIN_STACK=67108864 nix develop -c cargo test -p
  openwepp-hillslope-orchestrator
  canonical_covered_two_map_adjudication_failures_never_replay_or_fall_through
  -- --nocapture`: expected RED, exit 101 in 0.58 seconds after a prior compile-
  only exact-name attempt. One test ran and failed only because the current
  V8Persistent path recorded `(charges, validated physical endpoints, final
  constructor attempts)=(3,3,1)` instead of the required `(2,2,1)`, exactly
  exposing the absent same-map pending final disposition and legacy replay.

## Canonical pending-adjudication solver implementation

The canonical covered solver now owns one non-Clone pending value containing
the custody-validated physical endpoint, retained child, exact request, and
physical prefix. Consuming methods expose only history, adaptive rejection, or
same-map final construction. Charged roles are `Initial@0`,
`FixedPointAdjudication@1`, and contiguous
`MultisecantAdjudication(n)@(n+1)`; there is no separately charged final replay.

Focused current-source evidence:

- authentic stable minimum-support equilibrium: PASS `1/1` in 14.65 seconds,
  exactly two charges, two validated physical endpoints, one pending value,
  zero history/rejection, one final disposition, one constructor, and one
  envelope;
- authentic outer-nonclosure history path: PASS `1/1` in 19.36 seconds,
  `Initial -> FixedPointAdjudication(history) -> MultisecantAdjudication ->
  final`;
- physical/constructor/dependent-rejection matrix: PASS `1/1` in 15.09
  seconds. The ordinary dependent-only case is `M=2`, history `0`, rejection
  `1`; NativeMixed is `M=3`, history `1`, rejection `1`, with roles
  `Initial@0 -> FixedPointAdjudication@1 -> MultisecantAdjudication(1)@2`.
  Both have zero final disposition, constructor, replay, or later charge after
  the rejected pending map.

During test authoring, two success-specific helper assertions proved
inapplicable to intentionally transient/nonclosed real maps, and the
NativeMixed case authentically required one history disposition before its
dependent-only rejection. Only the test harness/matrix expectations were
corrected; the production disposition logic was unchanged. A later broader
run also showed that `NativeThawRefreeze` takes the same contract-authorized
dependent-only rejection and that an unfinished test audit record merged
separate adaptive retries. The capture now accepts only the exact
`AdaptiveRefinement`/adjudication-convergence disposition with a validated
pending endpoint and zero final construction/publication, and the test audit
separates terminated retries without changing production behavior.

Final focused evidence for this increment:

- `canonical_covered_`: PASS `11/11`, compile `9.85 s`, tests `39.79 s`,
  command wall `46.89 s`;
- `cargo check -p openwepp-hillslope-orchestrator --tests`: PASS, check
  `12.93 s`, command wall `16.02 s`;
- exact package integration authority/typestate test: PASS `1/1`, test
  `0.01 s`, command wall `0.96 s`; and
- owned-file rustfmt, scoped diff check, and the `<3,000`-line ceiling: PASS
  (maximum owned file `2,762` lines).

Independent Rust review remains required before terminal disposition.

## Release qualification telemetry and harness

Ran 2026-09-03 after wiring default-off accepted-map, Lane-D, and exclusive
transaction-envelope telemetry into the authentic production calls:

- orchestrator `release_qualification_tests`: PASS, `5/5`;
- runner `release_scale_` tests: PASS, `9/9`;
- `cargo check -p openwepp-runner --tests`: PASS in 6.61 seconds;
- exact-file rustfmt and scoped diff check: PASS; and
- the expensive 5-warmup plus 30-measured 1/10/19-OFE matrix was intentionally
  not run before performance corrections completed.

Initial independent code/QA review rejected the first telemetry increment
because an unscoped/zero canonical-map record could false-pass, accepted-map
cardinality was not joined to accepted covered supports, Lane-D events were
not bound to the active Lane-D scope, and the exclusive profiler included
post-run harness work. The corrected implementation fails closed on unscoped,
zero, missing, subset, misplaced, or cardinality-inconsistent evidence; derives
positive Lane-D order/count from source posture while preserving the zero-source
case; requires the relevant scope observations; and takes telemetry immediately
at the runner boundary before RSS/audit collection. Serialized labels state
that native and Stage-3 buckets are conservative transaction-envelope upper
bounds rather than isolated component timing. Code and QA rereview both
APPROVED the correction. Full release qualification remains pending.

## Native publication-history Arc/COW implementation

The V3/V4 publication-history owners now retain accepted immutable history as
`Arc<Vec<_>>`. Exact in-process clones are O(1); accepted append detaches with
copy-on-write only when shared; restore creates a fresh Arc after the existing
full semantic validation. No wire, ordering, revision, identity, error, or
rollback semantics changed.

Ran after implementation:

- focused forced-deep/shared-history filter: PASS, `3/3`;
- orchestrator test-target check: PASS; and
- exact-file rustfmt and scoped diff check: PASS.

The strengthened vectors use real V3/V4 producers and resident acceptance,
prove shared-versus-forced-deep byte and revision equality, poisoned rollback,
V4 nonempty restore, a second accepted append, and two-publication replay.
Independent Rust code and QA reviews both APPROVED the increment.

## Snow-free identity-only final reseal implementation

The snow-free DirectV11 path now executes its provisional physical transaction
once, emits one private move-only proof after complete validation, and consumes
that proof only to reseal accepted-slab-dependent identities. The final step
does not rerun constitutive, land-surface-energy, hydrology, biogeochemistry,
soil, vegetation, or surface/WB14 physics. It reconstructs the final V8/WB14
identity bindings, proves exact equality with the validated physical ending,
and permits the existing single publication only after all joins close.

Initial independent review rejected the first implementation because serialized
receipt equality did not authenticate the non-wire live clock incarnation,
outer-authentication errors could remove the proof without leaving a tombstone,
and the claimed outer-publication counter observed candidate validation rather
than actual parent acceptance. The correction now carries a serde-skipped
same-live-clock-revision proof, gives restart a fresh incarnation, installs a
permanent refusal before every fallible outer authentication, restores the
pre-operation staged ending/support on error, and records publication only
after `parent.accept_segment_with_bgc_scope` succeeds at the real
`accept_direct_v11_segment` seam.

Final focused evidence:

- expanded real DirectV11 identity/reuse vector: PASS `1/1`, `1,398` filtered,
  test `2.36 s`, command `2.58 s` on the final cache;
- persisted checkpoint restore-to-fresh execution with `--features fixtures`:
  PASS `1/1`, `98` filtered, test `0.23 s`, compile `43.60 s`;
- orchestrator and coupled-time test-target checks: PASS (`3.15 s` and
  `1.85 s` command wall on the final cache); and
- exact-file rustfmt and scoped diff check: PASS.

The final vectors prove exact equality with the forced full finalizer for the
accepted candidate, receipts, all six physical owners, staged DirectV10
state/history, and a genuine parent acceptance. They require same-live revision
for reuse; a byte-identical independent incarnation refuses. They cover
provisional and final injected outer-auth failures, retry refusal, restored
fresh execution, parent/support/configuration/topology/forcing/beginning-owner,
constraint/ledger, ending/provider/owner, clone/duplicate/move-out poisons,
exact rollback, zero reuse publication before actual parent acceptance, and
the granular single physical-operation counters. Final independent code and QA
rereview is recorded separately before terminal disposition.

## Validated publication-support capability expected-red setup

Static 2026-09-03: CoupledTime v17 required the implementation increment to add
the private capability test module first and run this exact expected-red test
before production implementation:

- `accepted_publication_support_capability_validates_once_and_appends_by_exact_live_tail`

Ran before production implementation:

- `env RUST_MIN_STACK=67108864 nix develop -c cargo test -p
  openwepp-hillslope-orchestrator
  accepted_publication_support_capability_validates_once_and_appends_by_exact_live_tail
  -- --exact --nocapture`: expected RED, exit `101`, command wall `17.74 s`.
  Compilation failed only on these deliberately absent production/test-audit
  symbols and methods:

- `AcceptedPublicationHistoryLiveRevisionV1`;
- `ValidatedStage3AcceptedPublicationSupportV1`;
- `AcceptedPublicationSupportCapabilityAuditV1`;
- `AcceptedPublicationHistoryV1::live_revision_v1`;
- `begin_accepted_publication_support_capability_audit_v1`; and
- `take_accepted_publication_support_capability_audit_v1`.

The test must exercise the authentic ordinary support constructor and accepted-
history append path. It asserts exact support, receipt, history, cached-tail,
wire, and publication equality with the current fully validating path, while
requiring exactly one full-validation attempt/success, operand seal, receipt
seal, capability mint, trusted-append attempt, live-revision join, chronology/
owner-tail join, and successful append. Append-time full validation, operand or
receipt reconstruction, serialization, full-prefix scan, and support-payload
clone counters must each be zero. The implementation may retain only the
existing byte-preserving WB14 compaction and Arc/COW handle installation after
the exact joins.

The same expected-red-first increment must add and then satisfy these supporting
vectors:

- `accepted_publication_support_capability_rejects_stale_foreign_replayed_and_restart_revisions_atomically`;
- `accepted_publication_support_tail_revision_poison_matrix_is_exhaustive`;
- `accepted_publication_support_untrusted_restore_revalidates_every_support_and_never_restores_capability`;
- `accepted_publication_rotation_invalidates_pre_rotation_support_capability`;
- `snow_free_publication_reseal_mints_one_fresh_validated_support_capability`;
  and
- `accepted_publication_support_capability_is_private_move_only_non_wire`.

The poison matrix must alter each live-revision component independently,
including incarnation, counts, last indices/support/parent/slab, traversed
ending owner, pending-event posture, event identities/ordinals, sealed-prefix
and WB14 checkpoint/tail identity, and aggregate authority digest. It must also
cover intervening support/event append, rotation/replacement, foreign exact-byte
history, replay, partial join, and restart. Every refusal consumes the
capability, applies numeric error precedence, and leaves history/publication
bytes and live revision unchanged; fresh validation against the current live
revision must then succeed. Restart, wire, archive, and untrusted reconstruction
must perform one full independent validation per support, reconstruct the full
chronology, mint no decoded capability, and establish a fresh process-local
incarnation only after validation succeeds.

## Single-pass final covered-owner projection

The same-map final disposition previously called
`canonical_owner_state_bytes()` to construct its ending joint and then entered
the generic ephemeral-candidate constructor, which projected and validated the
same immutable candidate a second time. The corrected private, non-Clone owner-
projection token owns the candidate and its one canonical owner map, binds the
unchanged soil continuation and joint custody, and is consumed to mint the
existing trusted carrier owner-byte proof. Initial, history, and rejected maps
remain physical-only and perform no complete-owner projection.

Ran after implementation:

- real stable success accounting: PASS `1/1`, exactly `2` charged maps, `1`
  final disposition/constructor/envelope, and `1` endpoint owner projection;
- real physical/constructor poison matrix: PASS `1/1` in `22.20 s`; pre-seam
  failures project zero times, while ending-joint and later constructor poisons
  reach the seam once, retain zero publication, and roll back byte-identically;
- orchestrator test-target check: PASS in `18.42 s`; and
- exact rustfmt and scoped diff check: PASS.

One compile-only attempt exposed a boxed candidate at the move boundary; the
token now consumes `*candidate`. No test ran before that type-only correction.

## Post-increment hot-path inventory

Static 2026-09-03 inspection of the combined pending-adjudication, single-pass
owner projection, snow-free identity reseal, WB14 linear reseal, and accepted-
publication capability source found five remaining lifecycle costs to resolve
only as measurement requires:

1. the snow-free reuse proof still serializes and deep-compares the growing
   `DirectV10RealConsumerShadow` accepted history at mint and consumption;
   replacing that comparison with an exact bounded live-shadow revision is
   already within `INV-COUPLEDTIME-029` and is the first follow-on candidate;
2. vegetation V11 execution validates the complete candidate and parent
   acceptance repeats much of the same debit/flux/owner/custody validation;
   reuse of that complete bundle requires a contract-first amendment;
3. every charged covered map rebuilds the immutable V9-to-V8 vegetation and
   LSE binding context; reuse across maps requires contract-first clarification;
4. the parent-end native-V2 path repeatedly serializes the same immutable
   non-soil owner set before and after installation; the existing validated-
   revision authority admits a private exact-byte handoff; and
5. completion deep-clones the active-day support payload and repeatedly scans
   owner/event prefixes. Removing the adjacent duplicate helper call is local,
   but a shared validated active-day view requires a CoupledTime amendment.

No production edit or performance result is claimed by this inventory. The
current three queued lifecycle corrections will be measured first so further
work is selected from observed attribution rather than the static ranking
alone.

## Continued allocation, validation-once, and attribution increment

Ran 2026-09-03 under the same exact one-OFE release probe. Retained production
corrections are:

- vegetation canonical state encoding now reuses one recursive byte-path
  buffer and writes decimal/fixed-hex scalar encodings directly while all
  released preimage, digest, and 155 mutation vectors remain unchanged;
- the private V8 projection family validates one exact immutable
  configuration/beginning revision once per call;
- LSE V1-to-V2 projection no longer repeats a same-call final source
  validation after exact source/target/payload/digest admission;
- the V8 input projection computes one fully validated native-V2 soil snapshot
  digest and threads it through the same private call;
- native V3 potential authorization consumes a private borrowed proof for the
  exact Surface V2 owner validated immediately beforehand;
- the private frozen-litter publication constructor validates its already
  typed projection and receipt chain without immediate serialize/reparse,
  while untrusted/restart/external paths retain full parsing and replay; and
- the move-only covered physical result carries its already validated V9-to-V8
  vegetation projection into the immediate complete-envelope continuation;
- the same-call V9-to-V10 projection admits its constructed V10 state after
  full V10-configuration and reconstructed V9-source validation plus exact
  derived identity, digest, and initial-receipt checks; and
- represented-snow inactive surface construction hands its fully validated,
  owned resource through a private nonserializable capability to the
  immediately adjacent ingress constructor, avoiding repeated full resource
  validation without crossing a mutable, restart, wire, or external boundary;
  and
- nonfinal terminal-child retention compares the exact same-call validated
  typed WB14 parent fields directly instead of serializing and reparsing both
  complete owner sets; restart/external serialization is unchanged; and
- same-call V4 publication consumes an atomic crate-private constructor result
  containing the fully validated projection and its exact canonical bytes,
  instead of validating the unchanged projection two more times through
  canonical serialization and immediate parse; public canonical parsing,
  restart, mutation, and external boundaries retain complete replay.
- the private-field V3 projection constructors likewise remain the one full
  validation gate for their exact returned value; the runtime no longer
  serializes, reparses, revalidates, compares, and discards that unchanged
  value before returning the typed candidate, while the public parser and
  later V4 nested replay remain unchanged; and
- the exact normalized V10 vegetation state/configuration pair is held by a
  private borrowed, non-Clone, non-wire proof while endpoint owner bytes are
  constructed. The proof verifies model/configuration identity, every
  transaction-bearing lineage field, and the recomputed canonical digest;
  pointer identity prevents equal-clone or post-proof mutation reuse. Ordinary,
  restart, external, and independently supplied owner-byte callers retain full
  V10-to-V9/V8 replay validation.
- the physical producer's already authenticated native soil candidate and
  optional continuation are retained together in a private move-only, non-wire
  ending token through the physical result and complete trial. The endpoint
  consumes that exact token instead of rebuilding deferred custody and
  revalidating the same immutable candidate; V1 resident-owner and V2
  unpublished-continuation seals remain distinct, while public, restart,
  external, and independently supplied paths retain complete authentication.
- V11 finalization retains its already validated pre-lineage and lineage-
  mutated parent revisions, plus their exact V10 and derived V9 projections,
  in a private move-only, non-wire proof. The two immediate trusted production
  installers consume it only after exact configuration/beginning/finalized-
  state equality, avoiding a second full V11/V10/V9 replay. The ordinary
  projection, untrusted executor, restart, checkpoint, external, and durable-
  publication boundaries remain fully validated.

The rejected covered-child memo experiment recorded zero captures and zero
hits across three probes and was removed in full. A later single-map owner-byte
storage experiment did not improve its targeted phase and was likewise
reverted. A zero-work arbitration shortcut changed its target bucket by only
about `1 ms` amid larger unrelated movement and was reverted as
noise-dominated. Removing an immediately repeated complete-envelope validator
did not improve its target bucket and was also reverted. Diagnostic-only
enclosing phase telemetry was retained; it never
enters state, receipts, publication, restart, or controller decisions.

Ran focused gates during this continuation:

- full vegetation suite: PASS `286/286`;
- LSE suite: PASS `139/139`;
- canonical covered suite: PASS `13/13`;
- V8 filter: PASS `30/30`;
- V3 filter: PASS `65/65`;
- frozen-litter V3 publication filter: PASS `3/3`;
- V4 projection mutation/round-trip, restart evidence, publication retention,
  exact-surface successor, and real two-lane selection: PASS `6/6`;
- V3 canonical projection mutation/replay suite: PASS `8/8` after the
  same-call replay removal;
- endpoint canonical-owner and carrier custody/proof suites: PASS `30/30`
  serially; two stale native-V2 poison tests were corrected to mutate the
  private typed carry after its seal was minted because the joint intentionally
  retains resident soil-owner bytes;
- canonical covered plus the corrected real two-lane batch path after the
  move-only soil-ending handoff: PASS `14/14` serially;
- full vegetation suite after the V11 parent-finalization handoff: PASS
  `287/287`; exact handoff equality/poison test: PASS `1/1`; existing validated-
  handoff suite: PASS `5/5`; parent-finalization plus real two-lane paths: PASS
  `5/5`;
- runner all-feature check: PASS after each retained source increment;
- forced-complete physical-prefix parity and audit-gated regime matrix: PASS
  `2/2` serially; and
- the corrected fixed real two-active-lane fixture: PASS `1/1`, with lane 1
  terminal and lane 2 surviving in one joint batch at minimum support; a
  bounded diagnostic scan established that the former snow amount genuinely
  exceeded dependent-carrier tolerances, so no solver tolerance or adaptive
  response changed; and
- an over-broad parallel nextest filter was not accepted as gate evidence: it
  produced two global-counter/test-thread interference failures, the known
  two-lane dependent-output instability, and one equilibrium fixture failure.
  Exact guarded tests were rerun serially and passed.

The final retained release probe passed in `5,484,820 us`, with exact source
`0.8488061229561478 m3`, outlet
`0.8471105124736579 m3`, storage `0.0016956104824910018 m3`, clamp `0`, 48
parents (4 covered/44 snow-free), 56 publications, 20 direct trials, 32 split
children, and 4 accepted microsteps. This is not performance acceptance; the
declared micro budget still fails by roughly 21,939x. The inactive-resource
handoff reduced
the targeted inactive surface-ingress bucket from about `93.7 ms` to `57.8 ms`;
the typed WB14 parent comparison then reduced provider retention from about
`309 ms` to `15.8 ms`; the atomic validated V4 projection/byte constructor
then reduced the measured snow-free frozen core from about `296.9 ms` to
`201.2 ms`; removing the discarded same-call V3 round-trip reduced that
bucket again to about `153.7 ms` and improved the whole run by another 2.3%.
Endpoint-exclusive attribution then identified `97.6 ms` in repeated V10
validation. The borrowed normalized-state proof reduced that sub-bucket to
`13.5 ms`, the complete-owner bucket from `322.6 ms` to `232.7 ms`, and whole
run time by another 1.7% from the prior best. The move-only physical-producer
soil-ending token then removed a repeated `147.1 ms` endpoint custody replay:
that sub-bucket measured `23 us`, complete-owner work fell to `87.9 ms`, and
whole-run time improved another 1.4% without changing any public operand or
count. Reusing the exact validated V11 parent-finalization V10/V9 revisions
then improved whole-run time another 2.3%; the parent aggregate fell from
about `3.745 s` to `3.714 s` with all outputs unchanged.
Exact forced-complete parity regressions passed `2/2`
serially, and the complete canonical-covered filter passed `13/13`.

The subsequent typed V4 construction increment retained the same complete
semantic checks but applied them directly to the supplied typed V3, LSE,
exact-owner, and receipt values instead of validating the just-constructed V4
by serializing and reparsing its own nested payloads. Public canonical parsing,
restart, mutation rejection, and external validation remain unchanged. The
focused V4/exact-surface suite passed `33/33`, clean runner compilation passed,
and two consecutive exact release probes preserved source
`0.8488061229561478 m3`, outlet `0.8471105124736579 m3`, storage
`0.0016956104824910018 m3`, zero clamp, 48 parents (4 covered/44 snow-free),
56 publications, 20 direct trials, 32 split children, and 4 accepted
microsteps. Runtime measured `5,345,492 us` and `5,343,745 us`; the targeted
snow-free frozen-runtime bucket fell from about `153.7 ms` to approximately
`110 ms`. The latter is the current retained best, but still fails the
declared micro budget by roughly 21,375x.

The next retained increment removed a second full V9-to-V8 projection of the
same immutable covered-trial beginning. Carrier receipt construction now mints
a private borrowed proof holding the fully validated imported V8 values;
represented-snow physical preparation accepts it only against pointer-identical
V9 configuration/state and the values then move into the existing physical
result. No proof crosses a clone, mutation, restart, wire, or external
boundary. Clean runner compilation passed, and the canonical-covered,
physical-only, represented-snow, and forced-complete focused filter passed
`16/16` serially. Two exact release probes preserved every output and count at
`5,237,702 us` and `5,219,037 us`; physical-evidence time fell from about
`1.385 s` to `1.287 s` and `1.280 s`. The latter is the current retained best
and still fails the micro budget by roughly 20,876x.

The first required isolated 1/10/19-OFE release-matrix launch failed before
measurement because the shared routed HBP/WAT5 proof still hard-coded its
original two-OFE fixture cardinality. The 1-OFE child correctly published one
OFE, while the assertion required two. The proof now receives the exact
surface cardinality from both callers and applies it consistently to HBP and
WAT5 reconstruction; the original two-OFE test continues to pass `2`
explicitly. The failed launch and source/binary hashes are retained in
`artifacts/terminal-heavy-gates/release_qualification_matrix_1_10_19_ofe.log`;
it is harness-defect evidence, not a performance sample.

The corrected matrix then completed all five warm-ups and 30 measured 1-OFE
batches but refused cross-batch comparability. Per-file evidence isolated the
only varying scientific-output bytes to WAT5 Parquet; its dependent manifest
hash therefore varied too, while HBP, LOSS, PASS, PLOT, WAT, Stage-3 evidence,
all closure operands, and every workload count were identical. WAT5 was the
only Parquet writer still allowing Arrow to encode unordered schema metadata
directly. It now uses the same sorted, stable Arrow-schema metadata encoder as
WAT/PASS; that encoder was extended only for WAT5's Utf8 columns. A new
two-write regression proves byte-identical WAT5 output (`1/1` PASS), and clean
runner compilation passes. The failed second launch remains recorded in
`artifacts/terminal-heavy-gates/release_qualification_matrix_1_10_19_ofe_rerun.log`;
its completed timings are diagnostic only because the required comparability
gate failed.

After deterministic WAT5 bytes, a third launch isolated the remaining raw hash
variance to the run manifest alone. This is expected provenance variance:
`invoked_utc`, the unique run root, and all absolute path keys/values differ
between isolated batches. The package's pre-existing terminal-harness design
requires scientific files to remain raw-byte hashed while the manifest uses a
fail-closed semantic projection. That projection is now implemented in the
prospectively authorized `stage3_long_run_output_evidence.rs`: it requires a
nonempty `run_dir` and string `invoked_utc`, replaces only that exact run-root
substring in every string and object key, refuses normalization collisions,
and preserves every other manifest field. Equivalence plus scientific-field
poison passed `1/1`, and clean runner compilation passed. The third failed
launch is retained in
`artifacts/terminal-heavy-gates/release_qualification_matrix_1_10_19_ofe_rerun2.log`;
again, its timings are diagnostic rather than qualification evidence.

The fourth isolated launch completed the full 1/10/19-OFE protocol: five
warm-ups and 30 measured batches per surface, all byte-comparable. Its final
decision was `FAIL`, with exact metrics recorded in
`artifacts/long-run-throughput-and-closure.md` and the raw log retained as
`artifacts/terminal-heavy-gates/release_qualification_matrix_1_10_19_ofe_rerun3.log`.
The run exposed one qualification-classification defect: all four adaptive
receipts in this positive fixture were terminal-regime supports, while the
harness required the empty ordinary canonical-map record set to have the same
cardinality. The correction binds canonical-map records to the independently
reported canonical-covered width histogram and treats its zero-support case as
not applicable. It does not alter a threshold, physical execution, terminal
trial count, timing, memory, closure, or the matrix's performance failure.

A subsequent terminal first-child memo experiment passed its two focused
authority tests, the real two-lane terminal/survivor test, runner compilation,
and exact release output/count parity. The exhaustive-key and sealed-key forms
measured `5,320,227 us` and `5,264,846 us`, respectively, against the retained
`5,219,037 us` best. Because neither improved the whole run or its target
bucket beyond noise, all memo code and tests were reverted under the package's
retention rule; the two release logs remain negative evidence only.

The next retained increment removed immediate self-revalidation from
`PotentialWaterRequestBatch::try_new`. Construction still computes the
canonical digest before applying the same domain, identity, cardinality, and
uniqueness checks, preserving constructor error precedence. Public
`validate()` remains unchanged in effect and recomputes the digest after its
content checks, so mutation, restart, and external callers retain full replay.
The two LSE producers that manually implemented the same digest/construct/full-
validate sequence now call `try_new`. The LSE transaction filter passed
`28/28`, the authentic two-lane terminal/survivor test passed `1/1`, runner
check passed, and formatting/diff hygiene passed. The exact CPU-0 release probe
preserved all scientific values and workload counts while measuring
`5,211,924 us` total and `601,092 us` in the targeted potential bucket. This is
a narrow retained improvement over `5,219,037 us`, not throughput closure.

A follow-up removal of the immediately adjacent aggregate request-batch
validation reduced its exact authorization bucket from `25,232 us` to
`17,113 us`, but total release time regressed to `5,221,788 us`. It passed the
two canonical physical parity/poison tests, authentic terminal test, runner
check, and exact scientific/count parity. Because the whole-run retention
criterion failed, the source and prospective write-set amendment were reverted
in full; only this negative measurement record remains.

The next retained increment bound a private borrowed soil-read capability to
the carrier's exact sealed candidate. The capability is minted only after the
immutable V1 value or V2 unpublished-trial digest still matches, and
`read_view_for` additionally requires pointer identity. It exists only through
the current physical call and has no clone, serialization, restart, external,
or mutable surface. The original full read validation remains for every call
without this capability, while candidate construction and unpublished-
continuation authentication are unchanged. The four soil continuation poison/
reconstruction tests plus two canonical physical parity/rollback tests passed
`6/6`; the authentic terminal test passed, orchestrator/runner checks passed,
and format/diff hygiene passed. The exact release probe preserved all outputs
and counts at `5,204,454 us`, with physical-evidence time `1,264,577 us`, versus
the preceding `5,211,924 us` and `1,267,495 us`. The small deltas are treated
as noise-scale retained cleanup, not qualification.

An allocation-only follow-up moved, rather than cloned, each covered phase's
validated vegetation operands after fixed-final borrowing ended. V3 module and
canonical parity/rollback tests passed `6/6`, the authentic terminal test and
runner check passed, and the exact release result preserved all science and
counts. Its target and total regressed to `1,275,074 us` and `5,253,829 us`,
so the code and prospective write-set description were reverted completely.

The canonical covered solver then adopted one private borrowed immutable-input
proof per solve. Before production code, a contract-derived audit assertion was
expected red solely on its absent begin/take API. The implementation preserves
the existing initial-trial error precedence, validates topology/caps/shortwave/
Stage-3 boundary once, and continues to validate every trial's shape, domain,
constitutive equations, residuals, tolerances, and branches. Public evaluation
and independent V3 calls still perform complete input admission. The count
test proves one immutable admission for a complete solve; full LSE passed
`139/139`; canonical parity/rollback plus authentic terminal passed `3/3`;
runner check, format, and diff hygiene passed. The exact release probe retained
all outputs/counts while moving potential `596,235→532,983 us`, physical
evidence `1,264,577→1,203,666 us`, and total `5,204,454→5,156,696 us`.

## Retained exact Stage-3 anchor-probe reuse

`SC-LANDSURFACEENERGY-001@28#INV-LANDSURFACEENERGY-162/C-017` now permits the
canonical represented-snow solve to reuse the current complete residual vector
for ground/soil identity-anchor Jacobian probes, replacing only the matching
`(T-T_anchor)/1e-9` entry. The unchanged canonical minus-then-plus trials still
receive full domain admission and flow through the same finite-difference
function; non-anchor columns and every base/prospective/backtracking evaluation
still execute the complete evaluator. There is no analytic derivative, sparse
solve, cache across mutation, or fallback.

The contract-derived oracle was expected red only on the missing probe/audit
APIs. It now proves bit-identical complete-evaluator residual vectors and dense
Jacobian entries for every Stage-3 ground/soil column at interior, exact lower,
and exact upper bounds, with zero full evaluations for anchor probes and two
for a centered non-anchor pair. Full LSE passed `139/139`; forced-complete
Stage-3 physical-prefix parity/rollback passed `2/2`; runner check, formatting,
and diff hygiene passed. The exact release probe preserved all science and
counts while improving total `5,156,696→5,005,555 us`, potential
`532,983→390,208 us`, and physical evidence `1,203,666→1,055,762 us`.

## Retained exact leaf reuse and rejected hydraulic probe experiment

`SC-LANDSURFACEENERGY-001@29#INV-LANDSURFACEENERGY-163/C-018` permits one
successful private current leaf state to serve as its immediately adjacent
beta-one maximum-demand result only when beta is bit-exact `1.0` or the
returned `Inactive`/`ExactZeroPar` branch provably never reads beta. The
expected-red call-audit API was absent before implementation. The resulting
oracle compares every private leaf-state field and a complete covered-column
evaluation against a forced exhaustive-call path. Exact beta one, inactive,
and exact-zero-PAR use one call per leaf; positive-PAR beta one ULP below one
retains two. Full LSE passed `140/140`. The exact CPU-0 release probe preserved
all science and workload counts while improving total
`5,005,555→4,964,642 us`, potential `390,208→347,810 us`, and physical evidence
`1,055,762→1,019,292 us`.

The next contract-first experiment factored the unchanged six-row occupancy
hydraulic evaluator for both complete evaluation and four hydraulic Jacobian
probes. Its all-row oracle proved bit-identical minus/plus residuals, every
dense-Jacobian entry, cap/frozen branches, zero-area fallback, and typed
redistribution error parity. An initial release sample measured `4,958,688 us`,
but exact-head repetitions after proof hardening measured `4,997,200`,
`4,968,488`, and `4,984,552 us`, with the potential phase between `361,991`
and `363,133 us`. Those samples failed the target-plus-total retention rule.
The probe shortcut and prospective contract v30 were removed; the preserved
oracle now asserts two complete evaluations for every hydraulic coordinate.
The final retained V29 source measured `4,936,273 us` total, `350,569 us`
potential, and `1,020,838 us` physical evidence, with exact science and all
48/56/20/32/4 counts unchanged.

The next private shared terminal-carrier custody increment passed its genuine
expected red, focused behavioral gates, dual Rust review, and exact science/
count parity, but three warm release runs improved total by only
`6.6--23.3 ms`, below the prospectively declared `0.10 s` retention minimum.
It was reverted in full. A post-revert exact-source run at Rust manifest
`f065ecdb039ce69a0ebb38f08e958ad8152bd9d89ff981ee470787d559a3c1cb`
and release binary
`5d70ce0966222480e20f12a468e47fc32fdc136e77e90a87334270a3289e6564`
passed at `4,920,992 us` total, `352,898 us` potential, `1,018,388 us`
physical evidence, and `60,708 KiB`, with the same exact science and counts.

Ran on the retained source: full LSE `140/140` PASS, LSE all-target
warnings-denied Clippy PASS, focused carrier soil-read proof PASS, runner
all-target check PASS, format PASS, and diff hygiene PASS. Orchestrator
all-target warnings-denied Clippy remains a recorded exact-gate failure on its
broad existing warning inventory; the two package-created dead-code warnings
were removed with feature/test-exact `cfg` guards, but unrelated warnings were
not blanket-suppressed.

## Test-only carrier structural-attribution instrumentation

Static and Ran, 2026-09-04: the existing opt-in adaptive-parent telemetry now
records eight additional non-overlapping successful-path timing buckets. Four
cover the exact duplicate forcing operations (`normalization`, `reseal`,
`full_validation`, and the later standalone `hash`). Two split the V8 input
projection's immutable vegetation configuration/component-binding index prefix
from the remaining dynamic owner/forcing/tile projection. Two split native-V3
complete state/configuration validation plus canonical surface hashing from
the subsequent solver-ready and native rebinding projection. The native-V3
validation/hash bucket is intentionally not called reusable static work: its
inclusive boundary mixes configuration/topology checks with mandatory current
owner validation and hashing.

The added spans feed only the existing thread-local diagnostic recorder and
the ignored release probe's JSON. They change no runtime state, public input or
output schema, error, branch, identity, equation, solver order, allocation
reuse, controller decision, or publication/restart behavior. No edits were
made to the authorized `carrier_phase.rs`, `carrier_engine.rs`, or
`strict_v8_endpoint.rs`; their existing inclusive parent buckets remain intact.

Ran:

- `cargo fmt --all -- --check`: PASS.
- `cargo check -p openwepp-hillslope-orchestrator`: PASS.
- `cargo check -p openwepp-runner --tests`: PASS.
- `cargo test -p openwepp-hillslope-orchestrator carrier_static_attribution_names_remain_closed_and_distinct -- --nocapture`: PASS (`1/1`); proves every new closed recorder name reaches a distinct field.
- targeted `git diff --check`: PASS.
- Heavy release characterization was not run here; the parent executor owns
  the exact CPU-0 probe and interpretation against the prospective `0.10 s`
  pre-kill threshold.

Post-edit line counts for the nine authorized Rust files are: `carrier_phase.rs`
`2877`, `carrier_engine.rs` `996`, `frozen_litter_v3_adoption.rs` `1543`,
`frozen_litter_v4_adoption.rs` `1393`, `strict_v8_endpoint.rs` `1515`,
`v8_input_projection.rs` `2690`, `v3_multitile_adoption.rs` `1818`,
`snow_stage3_v11_profile.rs` `726`, and
`stage3_runner_qualification.rs` `2925`. This diagnostic increment itself
added lines only to `frozen_litter_v3_adoption.rs`,
`frozen_litter_v4_adoption.rs`, `v8_input_projection.rs`,
`v3_multitile_adoption.rs`, `snow_stage3_v11_profile.rs`, and
`stage3_runner_qualification.rs`; all pre-existing edits in the shared
worktree were preserved.

## Rejected revision-61 feed-forward terminal carrier

Ran 2026-09-04: the contract-first typed feed-forward implementation compiled,
passed its three focused one-call/reference/error/rollback tests, six release-
telemetry tests, runner/orchestrator checks, and the complete snow contract
target (`40 passed`, `22` historical ignored). The exact release harness then
recorded 200 real one-call invocations with path, mode, endpoint, lane,
support, role, attempt, and beginning-joint identity. All three multiset
records hashed to
`3e9bb345eb204634c0f3c28165d241d4d180ca586cc418e67717ce5de9956783`.

The three source-identical CPU-0 runs measured total/provider/RSS:
`3_936_777/1_030_868/69_768`, `3_913_131/1_038_467/59_504`, and
`3_895_367/1_032_416/70_484` (`us/us/KiB`). Exact source/outlet/storage/clamp
was `0.8488061229561478/0.8471105124736579/0.0016956104824910018/0`,
and `48/56/20/32/4` counts were unchanged in every run. Median timings passed
the `1_299_833/4_234_488 us` ceilings, but two RSS observations exceeded the
binding `65_536 KiB` ceiling. The conjunctive keep gate therefore failed and
the production request/helper, single-call path, behavioral tests, and release
telemetry were fully reverted. The contract remains rejected design evidence
and its structural production seam returned to the named expected red (exit
`101`, missing `FeedForwardTerminalCarrierRequestV1`). The required comparator
role was dispatched first but its fixed model reported a service usage limit;
the package-authorized parent fallback ran the exact heavy command. Raw evidence:
`artifacts/terminal-heavy-gates/revision61_feed_forward_release_3run.log`.

## Rejected revision-31 component-temperature dependency replay

Static + Ran, 2026-09-04: the corrected same-revision authority was reviewed
twice and independently verified before implementation. Seven immutable
implementation-review cuts then closed the graph, custody, source-real error,
rollback, lifecycle, dense Jacobian/backtracking, full-owner, output-byte, and
feature-isolation findings. Both final implementation reviewers approved the
exact 16-path candidate manifest
`edc3f0b94c393e537b0115403548b779754f33308e53a26b1041932e9915be71`.

Before release measurement, the candidate passed the focused component suite
`14/14`, full LSE `154/154`, feature-enabled rustdoc diagnostics `2/2`, default
rustdoc diagnostics `0/0`, authentic replay-versus-forced release-profile
runner parity `1/1`, all affected crate checks, warnings-denied LSE Clippy,
formatting, diff hygiene, graph/custody/audit reconciliation, and exact output
bytes. The premeasurement Rust diagnostic digest was
`039a312502a5e6ef442b1e81ac78b988141199f6283fedcc86518ba78ba61abc`.

The exact CPU-0 release run used binary
`f9386eec584664f9639da281c15796730240239cd43ad2f158f4fa6d27fbeeaf`
and failed with exit `101` before emitting the benchmark JSON: the real
release aggregate had no authentic completed `N=2,S=6` sweep with the required
`58/14/16/28` accounting. The source digest remained unchanged. Because the
prospective retention protocol is conjunctive, runs 2/3 were correctly not run
and the complete revision-31 production/test-support/runner increment was
reverted.

Post-revert evidence finds all seven declarations absent, the focused
structural seam expected red on exactly those declarations, full LSE
`140/140`, LSE/orchestrator/runner all-target checks passing, and no semantic
revision-31 implementation residue. The current Rust manifest is
`2813f6e8faabb9408bac5e59b9271626ff5bcdc7fe49ab6dda810d3a1c3eee0d`.
Its difference from the frozen pre-v31 baseline is explained by 23 authorized
authority-test patches made after that baseline; reversing those patches in
memory reproduces the exact frozen aggregate
`78d756be1fa11ed85ee92b7d19e6c04427b01b122efaf7804d1b55d60536bbbe`.
Raw release evidence is
`artifacts/terminal-heavy-gates/component_dependency_replay_candidate_3run.log`.
