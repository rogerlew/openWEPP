# Final disposition

Status: `EXECUTED HOLD — BOUNDED CORRECTNESS RETAINED; RELEASE/PERFORMANCE QUALIFICATION FAILED`

Evidence mode: `Static + Ran`

The authorized package executed end to end through architecture replacement,
real-consumer evidence, retained and rejected performance increments, a
completed prior-source 1/10/19 release matrix, contract-first revision 61 and
revision 31 candidates, dual review, exact release gates, mandatory reversion,
and terminal evidence reconciliation.

The retained source deletes the v33--v57 recovery cascade and uses one
canonical solver per admitted regime. It activates real Lane D/native
vegetation/non-CoE Stage 3/frozen-litter paths and retains bounded closure,
receipt, restart, owner, topology, event, rollback, and output evidence. The
post-revert LSE suite passes `140/140`; LSE, orchestrator, and runner all-target
checks pass; formatting, diff hygiene, and revision-31 residue checks pass.
These facts support bounded retained correctness only.

Release and performance qualification do not pass. The historical 1/10/19
matrix preserved science and scaling but failed its wall/RSS budgets. The
latest retained mixed-regime one-OFE diagnostic is about `4.9 s` and is not
relabeled against a different pure snow-free or 10-OFE workload surface. The
year, century, and 5,000-hillslope qualification workloads remain unrun because
the applicable per-day matrix already fails by orders of magnitude. Broad
orchestrator warnings-denied Clippy and exact-workspace correctness also remain
failed or unestablished.

Execution continued after independent verification rejected the first proposed
HOLD. Revision 61 then passed its prospectively fixed median timing ceilings
but failed the per-run RSS conjunct in two of three runs and was fully reverted.
The corrected revision-31 component-temperature dependency replay subsequently
passed dual authority review, dual preimplementation verification, seven
immutable implementation review cuts, both final implementation reviews,
focused `14/14`, full LSE `154/154`, authentic runner/file parity, checks,
feature diagnostics, formatting, and diff hygiene.

Its exact CPU-0 release run 1 nevertheless exited `101` before JSON: the real
release aggregate contained no authentic completed `N=2,S=6` sweep with the
required `58/14/16/28` audit. Under the frozen conjunctive protocol, runs 2/3
were not run and the entire candidate was reverted. The failed candidate used
unchanged source digest `039a3125...1abc` and binary `f9386eec...eeaf`; its raw
record is `artifacts/terminal-heavy-gates/component_dependency_replay_candidate_3run.log`.

The current Rust manifest is
`2813f6e8faabb9408bac5e59b9271626ff5bcdc7fe49ab6dda810d3a1c3eee0d`.
The apparent difference from the frozen pre-v31 manifest is fully explained by
23 authorized authority-test patches applied after baseline capture; reversing
them in memory reproduces frozen aggregate `78d756be...bbbe`. No revision-31
production/test-support/runner mechanism remains, and the structural seam is
again expected red on exactly seven absent declarations.

The earlier HOLD was invalid because safe, explicitly authorized architecture
work remained. That work has now been pursued through the final credible
bounded routes and adjudicated by their prospectively frozen gates. Continuing
would require an owner choice to change the workload/budgets or authorize a
materially different canonical solver/evaluation architecture and its new
science-contract obligations. A broad warnings-denied cleanup is a separate
quality campaign and cannot substitute for throughput qualification.

Terminal disposition is therefore `HOLD`. It is not completion, release
approval, exact-workspace correctness, or performance qualification. The first
lift action is owner authority selecting the revised runtime premise or new
architecture scope described in `artifacts/hold-legitimacy-audit.md`.
