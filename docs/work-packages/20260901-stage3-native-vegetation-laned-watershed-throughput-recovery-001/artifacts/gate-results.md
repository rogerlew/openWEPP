# Gate results

Status: `QUEUED — NOT RUN`

Evidence mode: `Not run`

Record every selected command, exact source/binary identity, start/end time,
exit status, test counts, skipped inventory, retained log/hash, and
`PASS`/`FAIL`/`BLOCKED`/`NOT RUN` disposition. Validators are not workflows.
