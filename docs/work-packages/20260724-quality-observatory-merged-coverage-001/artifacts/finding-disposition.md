# Measurement Review Finding Disposition

Evidence class: Static / Ran.

Open closure-blocking findings: `0`.

| Finding | Disposition | Resolution |
| --- | --- | --- |
| Mutable source / admission TOCTOU | `accepted / fixed` | Exact dirty source is cloned without hardlinks, overlaid, manifest-compared, made read-only, and rechecked; source checkout is checked before and after HEAVY. |
| Asserted rather than mechanical pre-heavy checks | `accepted / fixed` | Admission runs focused gates, artifact/prompt checks, exact write-set reconciliation, line counts, independent inventories, identity capture, and requires this zero-open disposition. |
| Raw input re-glob drift | `accepted / fixed` | Every full/science raw set is content-indexed and revalidated before and after each LCOV derivation. |
| Unsupported legitimate-uncovered label | `accepted / fixed` | Exact crate/file/function/line/complexity joins require positive science-only and merged coverage; otherwise terminal verification blocks for review. |
| Producer-only terminal inventory | `accepted / fixed` | Terminal verification independently enumerates instrumented full, science-manual, and canonical workspace inventories and compares exact identities. |
| Incomplete admission/config/build binding | `accepted / fixed` | Config, collector, ledger, toolchain, inventories, control inputs, and executable artifact bytes are admitted and revalidated; executables are rechecked after each profile and LCOV. |
| Self-hashed standalone HEAVY | `accepted / fixed` | Repository-owned `transition` keeps LIGHT/audit/HEAVY admission in one process; standalone `collect` rejects. |
| Weak compact CRAP verification | `accepted / fixed` | Terminal verification reconstructs typed, finite, ordered raw/adjudicated/actionable partitions and exact canonical-registry adjudication IDs. |
| Verification mutated/followed published evidence | `accepted / fixed` | JUnit parsing is read-only; roots and every published entry reject symlinks and non-regular files. |
| Under-validated payload/envelope/status | `accepted / fixed` | Exact schemas, fields, canonical bytes, admission identity, digests, size policy, execution identity, and status are checked. |
| Manual-science profile contamination | `accepted / fixed` | The tooling test was renamed; independent probe places all five contract tests in `full` and none in `science-manual`. |
| Out-of-write-set roadmap edit | `accepted / fixed` | Existing Order 2 roadmap bytes were preserved in predecessor commit `f0da06bb`; Order 3 changed paths now fit its declared write set. |
| Identical cargo-crap duplicate rows | `accepted / fixed` | Identical exact rows deduplicate; conflicting metrics fail, with self-test coverage. |
| Read-only fixture mode propagation | `accepted / superseded correction` | Attempt `S67HdA` exposed read-only fixture copies. Preserving fixture file modes fixed that symptom, but attempt `NuumUF` proved directory freezing remained incompatible. Tooling defect 02 replaces the permission strategy with a writable, identity-guarded clone. |
| Repository-relative scratch blocked | `accepted / fixed` | Attempt `NuumUF` ran the complete full inventory but failed 26 valid tests because all checkout directories were read-only. The implementation now preserves a writable no-hardlink clone and enforces the exact admitted identity at every profile/LCOV boundary. Both reviewers passed the correction; a 22-test affected-path probe passed and restored the exact fresh-clone identity. |
| Predecessor gate-planner regression | `accepted / fixed by prerequisite` | Package `20260724-gate-planner-quality-deferral-hold-lift-001` passed both reviews, the exact seven, complete coverage-configured gate-planner library, and selected full workspace on clean committed head `0342b9f8`. |

Review A final disposition: `PASS`.

Review B final disposition: `PASS`.

Heavy collection authorization: `READY` for a fresh admission after the
hold-lift evidence is committed.
