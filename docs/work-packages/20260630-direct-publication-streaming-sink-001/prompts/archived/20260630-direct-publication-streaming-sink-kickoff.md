# Kickoff Prompt

Scaffold and execute DIRECT PUBLICATION STREAMING SINK: flatten the residual
run-length-scaling RSS in direct publication by streaming rows and incremental
WAT/PASS parquet row groups. Preserve byte identity for non-parquet outputs and
schema/row-count/value identity for parquet outputs.
