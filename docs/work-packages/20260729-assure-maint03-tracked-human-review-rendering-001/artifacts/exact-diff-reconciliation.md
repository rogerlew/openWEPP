# Exact Diff Reconciliation

Evidence class: Static + Ran

Reconciled against scaffold commit
`e8b46b1d67b956adc09cb74230d61815976b2de9`.

## Intended And Observed Production Diff

| Surface | Disposition |
| --- | --- |
| `usersum/assurance/review-drafts/**` | 92 generated regular files; intended tracked review consumer |
| review-render command and tests | intended |
| report-owned DRAFT adoption and tests | intended after typed adoption gap surfaced |
| retained-SVG research-object staging and test | intended after accessibility consumer failure |
| zero-public review-lane exclusion | intended after real legacy consumer failure |
| CAL-09 manuscript/supplement | editorial rendering corrections only |
| assurance locks and five receipts | generated by three source adoptions and two implementation rebinds |
| assurance maintenance documentation | intended |
| roadmap, package catalog, and package evidence | intended |

The attempted searched-configuration binding relabel was rejected in a typed
dry run and fully reverted. The final `report.yaml` diff is limited to changing
the `transition_count` display symbol from `transitions` to `transition(s)` so
both one and zero render readably under the current invariant-symbol schema.

`draft_adoption.rs` extracts existing adoption-reset helpers from
`amendment.rs` and adds the report-owned drift collector. The net extraction
reduces `amendment.rs` to 2,861 lines; the new module is 191 lines.

## Protected And Absent Diff

- no kernel, runtime, model operand, result, figure source, calibration output,
  science-contract, wepppy, release, snapshot, export, or vendor change;
- no groundwater or snow/frost authored report-source change;
- no `usersum/assurance/README.md`, legacy public catalog, or generated
  WEPPcloud usersum change;
- no public `usersum/assurance/reports/` tree; and
- no lifecycle, approval, release-transfer, export, or vendoring authority.

Protected SHA-256 values exactly match the frozen intake values recorded in
`render-evidence.md`.

## Generated Identity Chain

The retained generation advanced from
`30db4a7e6a691601426428b7772e28143ff9fa1bf10dd9d1ae80062d7f0002a2`
through three typed CAL-09 source-adoption receipts and two typed implementation
rebind receipts to
`b85b2ea962e7a9cf1461d187b8751b70043d1e6aa4f937c648ffb2b8ff49fffc`.
Anchored verification reports 27 retained transitions.

The scoped cached diff whitespace check passes for every non-generated path.
The generated review lane intentionally preserves identified CSV line endings
and sanitized Matplotlib whitespace; an unscoped cached whitespace check flags
those byte-preserved artifacts. Exact renderer comparison, Markdown parsing,
XML/accessibility checks, and the two inventory hashes govern that lane.
Final status and generated inventories were rechecked after review disposition
and documentation closure.

Terminal status contains 101 untracked paths: 92 review files, five receipts,
one Rust module, two renderer files, and the archived execution prompt. The
formerly active prompt is deleted. All 29 modified/deleted tracked paths and
all untracked paths are within the corrected package write set.
