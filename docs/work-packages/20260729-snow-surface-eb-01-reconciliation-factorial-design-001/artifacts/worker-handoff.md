# Worker Handoff

Status: `complete`.

The next actionable science package is `SNOW-SURFACE-EB-03`, not EB-02.
Scaffold it as contract-first reconciliation with these non-negotiable inputs:

- preserve current defaults and the explicit nonpromotion of Stage A/B;
- split sublimation from the melt/liquid enum behind an independent typed
  selector;
- use one shared surface temperature/cold-content state and one signed
  turbulent transfer for latent energy and vapor mass;
- implement the operands and independent equations frozen in
  `mass-energy-operand-lineage.csv` and `factorial-design.md`;
- admit and freeze numeric mass, energy, and latent/mass-equivalence tolerances
  before result-bearing execution; and
- leave EB-02/EB-04 held. Do not substitute air temperature for canopy
  radiometric temperature or add canopy emission over the full sky term.

If longwave source acquisition proceeds in parallel outside implementation,
priorities 1 and 2 in `source-acquisition-needed.csv` are the load-bearing
needs. HJ Andrews or equivalent data is needed only for the warm-maritime
conifer transfer claim.
