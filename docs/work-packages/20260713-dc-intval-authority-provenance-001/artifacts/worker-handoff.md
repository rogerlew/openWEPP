# Worker Handoff

First actionable item: close defect `INTVAL-AUTH-BIND-001` by restoring all
five deleted test targets serving seven active required suites against current
authority without reviving the deleted symbol-map runtime. Do this as the first
batch of `INTVAL-FINAL-001`, then continue that same package through exact
release and the complete integrated-validation restart.
