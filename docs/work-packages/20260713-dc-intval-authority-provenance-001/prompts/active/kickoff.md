# Close INTVAL-AUTH-PROV-001 End To End

Execution mode: `package-end-to-end`.

Close defect `INTVAL-AUTH-PROV-001` exactly as bounded in `package.md`. Add the
source guard first, apply only the Git-proven provenance metadata correction,
and run authority, exact release, stability, and full gates without skips.

Subagent requirement: **REQUIRED**. This prompt explicitly authorizes subagent
spawning/delegation to the runner, two reviewers, and two verifiers for the
bounded scopes and artifacts in `package.md`.
