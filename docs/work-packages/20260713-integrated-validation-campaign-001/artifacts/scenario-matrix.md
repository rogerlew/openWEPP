# Scenario Matrix

Status: `QUEUED`

At intake, expand every phase command into a row containing frozen commit,
fixture, producer, real consumer, output, acceptance evidence, expected result,
log path, and `PASS`, `FAIL`, `BLOCKED`, or `NOT RUN`.
