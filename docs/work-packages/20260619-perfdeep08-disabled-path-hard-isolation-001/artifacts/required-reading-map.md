# Required Reading Map

Status: queued.
Evidence mode: not run.

Populate before production edits.

Required:

- root and package AGENTS guidance;
- package spec and kickoff prompt;
- PERFDEEP07 package, disposition, gate results, disabled-path audit,
  zero-cost proof, and worker handoff;
- R0/R1 planning package hold-lift and no-compatibility proof artifacts;
- array-native runtime specification and ADR-0025;
- `crates/AGENTS.md` and science-contract AGENTS before Rust edits.

Record:

- byte budget for core required reading;
- documents fully read vs skimmed;
- source files inspected before edits;
- any conditional authority docs read because scope expanded.
