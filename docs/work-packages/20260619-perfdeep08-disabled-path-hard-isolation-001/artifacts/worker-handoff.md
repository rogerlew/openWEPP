# Worker Handoff

Status: queued.
Evidence mode: not run.

Populate at closeout with:

- final disposition;
- what changed;
- retained patches;
- rejected candidates;
- validation state;
- first actionable follow-up.

If the package closes `READY-FOR-R2`, the first follow-up should be the next
direct-frame runtime implementation package. If it closes `HOLD`, the first
follow-up must name the remaining disabled-path blocker.
