# PERFDEEP08 Hard-Isolation Plan

Status: queued.
Evidence mode: not run.

Populate after the audit and before broad edits.

For each candidate patch record:

- source file and function;
- opt-in flag or authority that should guard the work;
- expected disabled-path cost removed;
- correctness risk;
- focused tests;
- rollback point;
- whether the candidate touches a line-count WARN file.

Do not include direct-frame hydrology implementation steps.
