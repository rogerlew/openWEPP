# Review Agent A

Status: queued

Evidence mode: not-run

Static:

- Pending independent review.
- Required review format when executed:
  - `BLOCKING`: findings that block package disposition.
  - `MEDIUM`: findings that require fixes or explicit deferral before closure.
  - `LOW`: non-blocking notes or follow-up recommendations.
  - `NONE`: explicit statement when no findings exist.
- Each finding must include a required disposition in
  `artifacts/review-disposition.md`: `accepted`, `rejected`, `deferred`, or
  `follow-up`, with rationale.

Ran:

- Not run yet.
