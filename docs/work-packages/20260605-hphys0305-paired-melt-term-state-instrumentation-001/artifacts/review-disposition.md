# Review Disposition

Status: queued

Evidence mode: not-run

Static:

- No findings yet.
- Required disposition states for executed reviews:
  - `accepted`: fix before closure and verify the fix.
  - `rejected`: explain why no change is required.
  - `deferred`: keep package disposition blocked or explicitly route to a
    named prerequisite.
  - `follow-up`: link the follow-up package/artifact and carry it in
    `worker-handoff.md`.
- Package closure is blocked while any finding is undispositioned.

Ran:

- Not run yet.
