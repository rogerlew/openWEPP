# Gate Results

Ran: the exact mutation fixture and graph-union planner regressions pass 2/2 in
92.203 seconds. The mutation fixture's invalid receipt, source-digest change,
new-path detection, and independent-node blocking assertions remain unchanged.

Static: the executor diff changes only the fixture definition from affected to
global adjudicated CRAP. The separately authorized planner expectation now binds
critical risk and the explicit measurement-only escalation reason.
