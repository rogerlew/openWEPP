# Kickoff Prompt

Scaffold and execute COMPATIBILITY RUNTIME DELETION: remove the legacy symbol-map
hillslope runtime now that direct production is the validated default for all
surfaces. Stage the deletion, preserve direct identity, keep a thin explicit
`--compatibility-runtime` replay/comparator seam unless directed otherwise, refresh
stale docs, and prove no production path reaches compatibility runtime machinery.
