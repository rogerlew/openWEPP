# Mapping Equivalence

Static: no numeric calculation exists. The private definition pairs the exact
former public label and classification for each `BoundaryClass`; exhaustive
public tests prove all mappings, status constructors, and errors unchanged.
