# Worker Handoff

Status: `complete / successor remains blocked`

Evidence mode: `Ran + Static`

The successor may consume only:

- the two pinned MIT source identities and exact raw profile/key/value
  provenance;
- `SC-VEGETATION-001` version 2 topology, strict-schema invariant
  `INV-VEGETATION-052`, Stage A/B/C ownership, and explicit gaps;
- the 71-field, 32-profile, 53-parser-only-default, source, concordance,
  deviation, citation, and gap artifacts from this package; and
- the dependency direction observation -> Jarvis-style conductance -> PM and
  Farquhar, with later carbon/phenology/root feedback, not assimilation-solved
  stomata.

Production remains blocked on `AUTH-RHEC-001..011` and
`AUTH-RHEC-014..016`. Reject hidden defaults, key mismatch semantics, mutable
network inputs, raw invalid optics, PM gamma omission, heat/longwave clamps,
unbounded iteration, forced C3, unsupported initializer, sentinels/floors, and
direct hydrology mutation. `AUTH-RHEC-012` is deferred canopy snow;
`AUTH-RHEC-013` is licensing/provenance only.

First action: create or amend an authority-admission package that closes a
coherent selected deciduous/evergreen/mixed state boundary in canonical
contracts and contract-derived vectors. Do not run the successor kickoff or
write Rust until every required blocker and its pre-implementation gate pass.
