# Contract Implementation Evidence

Status: `PASS / amended and dual-review closed`

Evidence mode: `Static`

Canonical `SC-VEGETATION-001` advanced from version 1 to version 2 without
admitting a constitutive formula, runtime behavior, or parameter value. The
amendment:

- admits the exact MIT-licensed provenance boundary for the two pinned forks;
- preserves source behavior and profile cells as non-authoritative evidence;
- adds strict definition/schema invariant `INV-VEGETATION-052`;
- replaces the obsolete no-license gap with the exact licensed-source limit;
- adds audit-proven format mismatch, hidden-default, profile-parameter,
  conductance, photosynthesis, layer-root-demand, available-energy, and
  initialization gaps;
- adds `BEI-VEGETATION-002` so the active audit sidecar and version-2 binding
  addition cannot be dropped from the contract cycle; and
- keeps all production, calibration, validation, publication, and cutover
  labels non-promotable.

The lifecycle registry note was updated. No adjacent canonical contract needed
amendment because all cross-owner process laws remain explicitly blocked.
