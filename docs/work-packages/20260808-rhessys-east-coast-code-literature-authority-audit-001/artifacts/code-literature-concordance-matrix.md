# Code-To-Literature Concordance Matrix

Status: `audit-complete / successor blocked`

Evidence mode: `Static + Ran`

`C` below denotes a code expression, and `L` an independently structured
literature expression. Source names are resolved in the citation ledger and
literature-acquisition log.

| ID | Source coordinate and code expression | Literature locator/expression | Unit/domain/cadence result | Concordance | Migration |
| --- | --- | --- | --- | --- | --- |
| `CON-001` | GIS generator: write every selected CSV value beside its exact row key; `default` input selects raw GitHub `master` URLs | format has no scientific analogue | explicit local file is deterministic; remote default is mutable and not commit/digest bound | `CODE_ONLY` | local strict input `ADAPT`; remote fallback `REJECT` |
| `CON-002` | parser: absent key -> hard-coded default | no primary source authorizes omission as a parameter value | 53 CSV-absent keys and five mismatches affect every generated profile | `CODE_ONLY` | `REJECT` |
| `CON-003` | parser: `livewood_cn := froot_cn` after reading both | White et al. (2000) treats plant-pool C:N families separately | supplied livewood C:N is discarded | `SILENT_DEVIATION` | `REJECT` |
| `CON-004` | optics: parser reads absorptance, reflectance, and transmittance, but canopy daily uses only reflectance; nine of 32 K/PAR triples exceed unity tolerance, including chestnut oak K=1.03 | physical closure requires each incident component to be partitioned without gain | parsed absorptance/transmittance are dead fields and there is no closure/domain guard | `SILENT_DEVIATION` | `REJECT`; `BLOCK_SUCCESSOR` |
| `CON-005` | direct/diffuse radiation mutates top-down incident streams by stratum cover; diffuse routines ignore their `extinction_coef` argument | Forrester (2014) Eq. 1: `f_abs=1-exp(-kL)`; source instead cites incomplete Chen/BEPS and Jarvis/Leverenz/Norman leads | daily/noon approximations, ignored extinction input, empirical scatter term, and heterogeneous/sparse/mixed domains are unresolved | `CITATION_MISMATCH` | `RE_DERIVE`; `BLOCK_SUCCESSOR` |
| `CON-006` | canopy sunlit/shade split and separate assimilation calls | de Pury/Farquhar (1997), sun/shade derivation and Table 1 | source uses daily aggregates and simplified capacity assumptions not fully mapped to the primary model | `PARTIAL_IMPLEMENTATION` | `BLOCK_SUCCESSOR` |
| `CON-007` | liquid capacity `S_max = PAI_all * specific_rain_capacity`; storage is input-limited and evaporation-limited | Best et al. (2011) Eqs. 46-47: finite store/throughfall update; Gash (1979): event wetting/saturation/stemflow | source uses metres of water per stratum area; stemflow and explicit drainage authority are absent | `PARTIAL_IMPLEMENTATION` | `RE_DERIVE` |
| `CON-008` | wet-canopy evaporation uses Penman-Monteith and store limitation | Best Sect. 2.3 requires store-limited depletion; Pereira et al. requires ventilation-regime distinction | store limit broadly matches; aerodynamic regime selector is absent | `PARTIAL_IMPLEMENTATION` | `BLOCK_SUCCESSOR` |
| `CON-009` | APAR factor `f_q=q a/(1+q a)` | Jarvis (1976) supports bounded multiplicative environmental response; Stewart/Kelliher require explicit scale | hyperbola is bounded for nonnegative PPFD, but source cites only “biome bgc” and profile coefficients lack scale/domain authority | `CODE_ONLY` | `BLOCK_SUCCESSOR` |
| `CON-010` | LWP factor is linear between `psi_open` and `psi_close`, with alternate polynomial branch and `9999` bypass | White (2000) Appendix A supports predawn-LWP threshold family; Jarvis identifies leaf-water-potential response as empirical | default linear family is recognizable; alternate curve, bypass, soil-to-leaf mapping, and profile parameters are unsupported | `PARTIAL_IMPLEMENTATION` | `BLOCK_SUCCESSOR` |
| `CON-011` | VPD factor is one below `vpd_open`, zero above `vpd_close`, linear between | White (2000) Appendix A parameterizes initial/final VPD reduction | family matches, but both GIS keys mismatch parser names so profile thresholds are ignored | `SILENT_DEVIATION` | `BLOCK_SUCCESSOR` |
| `CON-012` | Tmin factor is 1 above 0 C, 0 below -8 C, linear between | code says Running/Coughlan but supplies no exact locator; White parameter table does not establish this curve | daily minimum air temperature; arbitrary threshold acknowledged by source | `CITATION_MISMATCH` | `REJECT` |
| `CON-013` | Tavg response is computed, optionally overridden, then omitted from `m_final` | no named literature locator supports the executed omission | parameters `topt/tmax/tcoef` remain active data but do not affect conductance | `SILENT_DEVIATION` | `REJECT` |
| `CON-014` | CO2 factor `clamp(1-(1-c)(CO2/350-1),0,1)` | source contains “insert paper”; Farquhar does not authorize this stomatal multiplier | ppm basis and coefficient origin are unspecified | `CODE_ONLY` | `REJECT` |
| `CON-015` | leaf conductance `g_l=(g_max-g_cut) product(f_i)+g_cut`; canopy `g_s=g_l LAI_s f_stom`; result floored at `1e-7 m s^-1` | Jarvis Eqs. 4-9 supports a descriptive product; Kelliher Eq. 3 distinguishes leaf/canopy integration | source scales a mean leaf by projected sun/shade LAI; cuticular parallel path is plausible, but floor and parameter transfer lack authority | `PARTIAL_IMPLEMENTATION` | `BLOCK_SUCCESSOR` |
| `CON-016` | aerodynamic resistance from three separate overstory/understory/surface routines | JULES Sect. 2.1 and Shuttleworth-Wallace require compatible roughness, displacement, reference height, stability, and source-height definitions | source provenance is code/person attribution; `ustar_overu=-999.9` sentinel and layer regimes are not admitted | `CODE_ONLY` | `BLOCK_SUCCESSOR` |
| `CON-017` | PM numerator/denominator shape is recognizable, but code sets `gamma=cp P/lambda` instead of `cp P/(EPS lambda)` despite defining `EPS=0.6219` | Penman-Monteith family in reviewed JULES/Shuttleworth-Wallace sources includes the molecular-mass ratio | at 20 C and 101325 Pa, source gamma is about 41.71 Pa K^-1 rather than 67.07 Pa K^-1; snow sublimation elsewhere uses `/0.622` | `SILENT_DEVIATION` | `REJECT`; `RE_DERIVE` |
| `CON-018` | transpiration is reduced by wet-canopy evaporation time/energy and split between saturated/unsaturated zones from one root depth | reviewed layer-demand authority uses normalized layer root share times availability, with explicit zero-denominator handling | source does not produce layer-resolved requests and directly couples to patch stores | `CITATION_MISMATCH` | `RE_DERIVE` |
| `CON-019` | Farquhar Rubisco limitation `A_v=Vmax(C_i-Gamma*)/[C_i+Kc(1+O/Ko)]-R_d`; electron limitation analogous; `A=min(A_v,A_j)` | Farquhar et al. (1980) model development; de Pury/Farquhar (1997) Table 1 | C3 leaf-scale core is recognizable and conductance closes `C_i`; code hardcodes C3 for all profiles | `PARTIAL_IMPLEMENTATION` | `BLOCK_SUCCESSOR` |
| `CON-020` | `Jmax=2.1 Vmax`; nonrectangular electron-transport quadratic with `theta=0.7`; ten fixed growth-respiration iterations | de Pury/Farquhar (1997) supplies scale-aware sun/shade equations; Wullschleger (1993) is not authority for the executed fixed ratio | no convergence criterion; grass profiles are forced through C3; constants and iteration lack complete provenance | `CITATION_MISMATCH` | `RE_DERIVE` |
| `CON-021` | static phenology uses profile day-on/off and fixed ramp lengths; dynamic GSI parameters silently default because absent from CSV | Jolly et al. (2005) is a lead for GSI; existing openWEPP `SC-PLANT-001` retains current GSI authority | source profiles all select static; day values are parameter data with no cell-specific authority | `CODE_ONLY` | `BLOCK_SUCCESSOR` |
| `CON-022` | LAI from leaf C times SLA, distinct sun/shade SLA, all-sided ratio, and woody SAI allometry | White (2000) Appendix A defines SLA and all-sided/projected ratio; de Pury requires scale-aware sun/shade treatment | GIS SLA and LAI-ratio keys do not match parser, so profile-specific values are ignored; woody SAI formula lacks citation | `SILENT_DEVIATION` | `BLOCK_SUCCESSOR` |
| `CON-023` | maintenance respiration uses Ryan/BIOME-BGC Q10 and per-N parameters; growth respiration uses fixed fraction | Ryan (1991) and BIOME-BGC are citation leads; exact regression, acclimation, tissue basis, and profile origin are incomplete | daily carbon flux; profile parameters are not calibrated/transferable authority | `PARTIAL_IMPLEMENTATION` | `BLOCK_SUCCESSOR` |
| `CON-024` | allocation flags choose constant/Dickenson/Waring pathways; turnover and litter fractions move C/N pools | White (2000) supports biome family parameters, not East-Coast cell values or all source branch mechanics | multiple allocation paths and defaults extend beyond the CSV; receiver ownership is incomplete; persistent LAI/root feedback prevents deferral of the minimum selected C transition | `PARTIAL_IMPLEMENTATION` | minimum C state `BLOCK_SUCCESSOR`; alternate/N/material paths `DEFER` to named later owners |
| `CON-025` | mortality profile row is written as `mortality`; parser reads `epc.daily_mortality_turnover` and divides by 365 | no source supports this serialization mismatch | supplied annual values are ignored; parser default controls all profiles | `SILENT_DEVIATION` | `REJECT` |
| `CON-026` | root depth is dynamic from root C under flags; LWP from patch soil water; no explicit layer profile | JULES layer extraction gives normalized root-share weighting, while `SC-VEGETATION-001` requires caller-supplied layer identity and hydrologic arbitration | one-depth source cannot satisfy layer demand/withdrawal ownership | `PARTIAL_IMPLEMENTATION` | `BLOCK_SUCCESSOR` |
| `CON-027` | canopy snow storage/sublimation and snow-dependent optics share stratum state | current `SC-VEGETATION-001` defines single-owner boundary but admits no law | adjacent `SC-SNOWFREEZE-001` amendment would be required | `CODE_ONLY` | `DEFER` |
| `CON-028` | longwave treats all strata as one canopy slab at air temperature; warm-period negative `Lstar`, day, and night components are forced to zero | Stefan-Boltzmann exchange supplies emission physics, but no reviewed source authorizes homogeneous multistratum temperature or sign clipping | daily kJ m^-2; clipping deletes legitimate net longwave loss and breaks component closure | `CODE_ONLY` | `REJECT`; `BLOCK_SUCCESSOR` for the energy chain |
| `CON-029` | heat-flux wet branch `C_v/deltaz * (Tmean_initial-Tmean_final)`; zero-capacity branch `C_v*deltaz*(delta T)` | dimensional storage change is areal heat capacity `C_v*deltaz` times temperature change | the two branches have incompatible length dimensions; wet storage fraction and soil-water substitutions also lack domain authority | `SILENT_DEVIATION` | `REJECT`; `RE_DERIVE` |
| `CON-030` | day/night PM preparation clamps negative energy; when `rnet_evap_day < ZERO`, code assigns `rnet_evap_night=0` rather than clearing day; transpiration then clips sun/shade energy independently | PM requires a consistently defined available-energy operand and explicit condensation policy | executed typo can retain negative daytime energy while deleting a valid nighttime term; later component debit cannot prove closure | `CODE_ONLY` | `REJECT`; `BLOCK_SUCCESSOR` |
| `CON-031` | accepted GIS world initializers derive `leafc=LAI/SLA`, woody/root C by fixed allocation ratios, tissue N by C:N, `leafcalloc=0.05 leafc`, and deadwood N with hardcoded C:N 333.33 | White (2000) provides generic parameter families, not these initial-state formulas, row-index coupling, constants, or site transfer | initial-state area basis, phenological date, root depth, zero-pool assumptions, and calibration domain are unrecorded; parser key mismatch makes initial and runtime SLA inconsistent | `CODE_ONLY` | `REJECT`; `BLOCK_SUCCESSOR` |
| `CON-032` | dynamic GSI combines four environmental indices and hidden thresholds/windows to trigger phenology | Jolly et al. (2005) is a functional-family lead; existing openWEPP GSI authority is separate | source parameters are absent from every GIS profile and soil-potential units are contradictory | `PARTIAL_IMPLEMENTATION` | `REJECT` defaults; `BLOCK_SUCCESSOR` selected phenology |
| `CON-033` | sunlit LAI loop repeats until `abs(old-new) <= 1e-5*new` without iteration limit | Chen et al. (1999) is cited for sunlit fraction, not unbounded solver semantics | zero/nonfinite/oscillatory failure and atomic rollback are absent | `CODE_ONLY` | `REJECT`; bounded re-derivation required |
| `CON-034` | rooting depth uses root C, direction, distribution parameter, and max depth | Arora and Boer Eq. 11 is cited but was not acquired/adjudicated at an exact primary locator | dynamic single depth cannot satisfy canonical layer-profile/request ownership | `PARTIAL_IMPLEMENTATION` | `BLOCK_SUCCESSOR` |
| `CON-035` | nonvascular conductance linearly maps mean relative store after assumed 1 mm drying; aerodynamic routines select separate canopy regimes | Williams and Flanagan (1996) and incomplete Heddeland/Lettenmaier attribution are source leads | moss data domain, units, layer/reference height, stability, clamp, and tree wet-surface applicability are unresolved | `CITATION_MISMATCH` | `BLOCK_SUCCESSOR` for wet-surface/aero use; otherwise named deferral |

No row remains `NOT_YET_VERIFIED`, and no source constitutive chain or
parameter is admitted by a `MATCH` label.
