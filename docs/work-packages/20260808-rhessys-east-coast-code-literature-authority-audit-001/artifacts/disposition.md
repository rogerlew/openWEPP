# Disposition

Status: `queued`

Evidence mode: `not-run`

Terminal value must be `complete` or `executed-hold`. Completion proves audit
and successor-plan closure only; it cannot claim implementation or validation.
