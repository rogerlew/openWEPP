# Kernel-profile compliance

Status: `PASS`

Evidence mode: `Static + Ran`

The correction changes only unpublished numerical iterate selection. It does
not change constitutive physics, solver tolerances, the 96-iteration ceiling,
the exact 60-second floor, event/discrete predicates, topology, custody,
receipts, rollback, persisted state, or output schema. A relaxed intermediate
cannot publish; authentic final replay and every existing comparison still
gate acceptance.

The convex helper reuses the existing discrete/posture guards, reconstructs
derived temperature/thickness/fingerprint, and refuses to blend when any guard
fails. Terminal one-day mass, energy, receipt-energy, and receipt-temperature
closure all pass their unchanged bounds. No numerical tolerance was loosened.
No production microstepping diagnostic was retained.
