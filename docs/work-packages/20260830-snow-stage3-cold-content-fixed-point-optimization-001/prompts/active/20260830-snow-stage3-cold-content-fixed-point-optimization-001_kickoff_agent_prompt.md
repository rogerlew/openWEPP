# Execute the Stage-3 cold-content fixed-point optimization

Execution mode: package-end-to-end

Autonomy: execute every package phase through truthful disposition without
asking for intervention unless a genuine authority, safety, external-state, or
operator-decision blocker is reached.

Subagent authorization: none; do not claim independent delegated review or
verification under the current session policy.

## Required reading

Core: `/workdir/openWEPP/AGENTS.md`, `docs/codex_exec_plans.md`,
`docs/work-packages/AGENTS.md`, `docs/work-packages/README.md`, and this
package's `package.md`.

Conditional before kernel/contract edits:
`docs/standards/kernel-work-package-preparation.md`,
`docs/standards/testing-and-gate-strategy.md`,
`docs/specifications/science-contract-authoring-procedure.md`,
`docs/specifications/science-contracts/kernel-process-contract-profile.md`,
`docs/specifications/science-contracts/index.md`, `crates/AGENTS.md`, and
`tests/AGENTS.md`.

On demand: `SC-SNOWENERGY-001.md`, predecessor optimization evidence, and the
covered fixed-point source/tests. The local-repo reading set is approximately
693 kB (`WARN`, below `REQUIRES-JUSTIFICATION`).

## Execution requirements

Freeze the `792af753e` one-day baseline. Add only transient diagnostics needed
to establish contraction mechanism. If solver policy changes, amend the
canonical contract first, add contract-derived tests, record the failing
pre-implementation gate, and only then edit production code. Preserve the
exact 60-second floor, authentic final replay, exact event/discrete predicates,
96-iteration fail-closed ceiling, unchanged physical tolerances, and strict
mass/energy/receipt closure. Production must not persist diagnostics.

Run focused vectors and the real five-parent fixture before the canonical day.
Report accepted/rejected counts, width and iteration distributions, runtime,
limiting rejection reasons, and closure. Reconcile exact terminal diff,
line-count governance, all findings, and disposition before handoff.
