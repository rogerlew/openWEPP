# Owned File Manifest

Status: `queued`

Static: the authorized write set is defined in `package.md`. At execution
intake, record `FROZEN_BASE`, the exact initial path set, additions/deletions,
and any preexisting user changes. Before disposition, prove that all changes
remain inside the authorized set and that `/home/workdir/wepppy` is unchanged.
