# Gate Results

Status: `queued`

Record every exit criterion as `PASS`, `FAIL`, `BLOCKED`, or `NOT RUN`, with
commands, dates, source identity, log paths, and concise results. Include
focused, full Rust, adjudicated CRAP, line-count, documentation, deterministic
rebuild, review-lock, snapshot, security, intended-write-set, and
cross-repository checks.
