# Worker Handoff

Evidence mode: Static.

Current best bundle:
`coe_liquid_holding_capacity_v1 + physics_bulk_density_compaction_v1`.

Disposition: keep opt-in; do not default-activate until Activation Policy B
full-model-surface no-regression evidence exists.

Next actionable work:

1. Define and run the Policy-B full-surface no-regression basis before any
   default activation.
2. Classify and target the remaining `498` failures by residual class.
3. Treat open-surface ablation as the leading over-persistence follow-on for
   cap-limited mass rows.
4. Investigate the large under-persistence tail separately; test whether it is
   compaction-arm-induced before adding a new physics lever.
5. If the `550 kg m^-3` SNOBAL cap re-anchor is pursued, do it as a separate
   contract-first package with full-surface evidence.
6. Keep frost attribution blocked until snow control is good enough to isolate
   frost residuals.
