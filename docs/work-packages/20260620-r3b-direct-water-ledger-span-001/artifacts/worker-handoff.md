# R3B Worker Handoff

Status: complete.
Evidence mode: Static.

R3B closed the conservative post-R3A harness-growth route. The next package can
choose:

- R3C, if another pre-process direct span is needed for multi-day or multi-lane
  state propagation before process migration;
- R4A, if a narrow hydrology-process span can be scoped with canonical `SC-*`
  authority, non-aliased identity evidence, and no publication cutover.

Carry forward:

- preserve default-disabled H2637 median `<= 676.67 s`;
- keep direct runtime disabled by default;
- keep no-compatibility proof as source scan plus runtime counters;
- keep compatibility handoff counter production-backed;
- do not claim publication/default activation without a dedicated cutover
  package.
