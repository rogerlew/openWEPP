# Required Reading Map

Current Core set: root and work-package instructions, this package, roadmap,
the CQR Nightly ExecPlan, the adopted Order-3 verifier/report schema, the
Order-4 workflow/control receipt contract, CQR templates, aggregate admission,
testing/gate strategy, and nested test/standards instructions.

Instruction discovery was rerun with `tools/agents/find-agents` for the
declared implementation paths. The applicable chain is root `AGENTS.md`,
`docs/work-packages/AGENTS.md`, `tests/AGENTS.md`, and
`docs/standards/AGENTS.md`; no nearer workflow/local-CI instruction file exists.

Disposition: `OK`. The stable report/control contracts from Orders 3-4 were
read before implementation.
