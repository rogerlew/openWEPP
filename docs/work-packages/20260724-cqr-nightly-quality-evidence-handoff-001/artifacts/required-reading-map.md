# Required Reading Map

Current Core set: root and work-package instructions, this package, roadmap,
and the CQR Nightly ExecPlan.

Current bytes: `62432`. Disposition: `OK`.

Recompute after the QA report contract lands and before implementation edits.
