# Execute SNOW-SURFACE-EB-04S

Scope: local repository science-contract and retained-evidence work; flat-file
reads/edits only; no external connectivity.

Execution mode: package-end-to-end.

Phase plan: execute all phases in `package.md` sequentially through disposition.

Required reading:

- Core: root `AGENTS.md`, `docs/work-packages/AGENTS.md`,
  `docs/specifications/science-contracts/AGENTS.md`, this package, and
  `artifacts/required-reading-map.md`.
- Conditional: `docs/specifications/science-contract-authoring-procedure.md`
  and `docs/specifications/unit-governance.md` for the contract amendment.
- On-demand: immutable EB-04E/EB-04R evidence only in the phase permitted by
  the bias firewall.

Task: execute the package objective end-to-end for the declared write set.
Freeze the unit authority before opening EB-04R result-bearing evidence. Do not
run the model. Preserve EB-04R unchanged.

Constraints: contract-first sequencing; canonical SC authority; named unit
conversion; no silent reinterpretation; no tuning; no surrogate physics.

This prompt explicitly authorizes subagent spawning/delegation to two
independent science-contract reviewers and two terminal verifiers for read-only
review with package-local bounded artifact writes.

Autonomy: proceed through disposition unless hard-blocked.
