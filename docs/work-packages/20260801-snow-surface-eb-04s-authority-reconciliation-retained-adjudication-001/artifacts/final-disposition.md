# Final Disposition

Evidence mode: `Static + Ran`.

Status: `COMPLETE / CLOSE_NONPROMOTION_EMPIRICAL_RULE`.

EB-04S resolves the unit/tolerance contradiction without rewriting or rerunning
EB-04R. Result-blind authority established that the canonical `1e-9 m` SWE
closure is `1e-6 kg m^-2` for the same vapor-to-sublimation transfer residual.
`SC-SNOWENERGY-001` version 6 now distinguishes that predicate from both
hourly/daily vapor aggregation and represented-layer lifecycle.

The retained-only consumer binds the complete frozen source, tool, protocol,
fixture, observation, role/filter, selector, and decision-dependency set before
scoring. It rechecks 48 cells, 288 retained file identities, and 761,212 daily
rows without a model subprocess. The EB-04R-specific `1e-12 kg m^-2` vapor-
aggregation gate passes at maximum `7.993605777301127e-15`; vapor-to-
sublimation closure passes at maximum `8.109983287707401e-8 kg m^-2` against
`1e-6`.

Only after the complete physical/provenance gate passed were observations
loaded. The independently reconstructed unchanged rule gives:

- baseline B: score 177, robust failures 16;
- combined LS: score 180, robust failures 16.

The score improves but failures do not decrease. The frozen rule therefore
closes nonpromotion, invokes the stop-loss, and authorizes no further
factorial/calibration round. Longwave and sublimation remain implemented,
default-off physics; this result demonstrates failure to satisfy the available
population's promotion rule, not invalid process science. Warm-maritime conifer
transfer remains withheld for lack of paired evidence.

Both authority reviews and verifications pass. Both terminal reviews returned
`GO_WITH_AMENDMENTS`; all findings were accepted and repaired. Terminal
verification A and B return `PASS_WITH_NOTES`, with no open scientific,
authority, provenance, conservation, or decision blocker. EB-05 assurance
closeout is next.
