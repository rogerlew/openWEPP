# Pre-Implementation Intent

Status: `complete`

Evidence mode: `Static`

Intent is behavior-preserving validation-infrastructure defect closure.
Production snow physics and assurance authority are excluded. The inherited
dirty tree contains EB-03 and EB-03A implementation, so exact terminal quick,
frost, and Critical full profiles remain binding.

Defects:

- `EB03B-CQR-001`: the CQR self-test's generated current fixture is not
  recognized as `CURRENT`.
- `EB03B-ASSURE-001`: two assurance fail-closed negative matrices exceed the
  configured 720-second per-test timeout.
