# Owned File Manifest

Status: `PASS`.

Evidence mode: `Static`.

Changed paths are this package tree, `docs/ROADMAP.md`, the snow campaign
roadmap, and `docs/work-packages/README.md`. Immutable attempt output is under
`target/`. No science contract changed because existing schema-v6 applicability
authority and `TOL-SNOWFREEZE-019` already bind this diagnostic. No assurance
dependency changed.

Production Rust, tests outside this package, fixtures, observations, schemas,
dependencies, outputs, defaults, CoE ownership, persistence, and release
lifecycle are not selected.
