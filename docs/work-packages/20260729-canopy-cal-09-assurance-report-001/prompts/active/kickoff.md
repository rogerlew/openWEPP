# CAL-09 / CANOPY-ASSURE-01 Kickoff

Scope: local repository scientific-assurance task; flat-file reads and edits
plus deterministic local commands only; no external systems or network actions
are required.

Execution mode: package-end-to-end.

Phase plan: execute every phase in `package.md` sequentially through truthful
disposition after the operator has frozen `artifacts/figure-plan.md`.

Required reading:

- Core: `AGENTS.md`, `docs/work-packages/AGENTS.md`, this package,
  `docs/codex_exec_plans.md`,
  `docs/standards/testing-and-gate-strategy.md`,
  `docs/standards/scientific-model-evaluation-report.md`,
  `docs/governance/scientific-assurance-v2-architecture.md`,
  `docs/governance/scientific-assurance-dossier-lifecycle.md`,
  `docs/governance/scientific-assurance-v2-source-build-contract.md`,
  `docs/decisions/0038-manuscript-first-scientific-assurance-publication.md`,
  `docs/planning/canopy-phenology-assurance-roadmap.md`,
  `usersum/openwepp-canopy-phenology.md`, `assurance/v2/README.md`, and
  `assurance/v2/schemas/report.schema.json`.
- Conditional: `docs/specifications/science-contracts/AGENTS.md` and the exact
  `SC-*` contracts cited by a claim; V2 builder/amendment source and tests only
  if the terminal write set is prospectively amended to touch them.
- On demand: claim-bearing source ledgers, strict result objects, figure data,
  final dispositions, reviews, verifications, and primary references from
  completed CANOPY-PHENOLOGY-01 through CAL-07F packages.

Required-reading budget: `248289` local bytes for the Core set, `OK`; map:
`artifacts/required-reading-map.md`.

Files: the paths in the package's intended write set. Treat all other paths as
read-only unless a prospective reviewed amendment expands the set.

Task: produce the manuscript-first native-forest canopy-phenology scientific
evaluation, supplement, strict result objects, accessible figures and tables,
public-safe research objects, independent reproduction, review evidence, and
truthful lifecycle disposition.

Constraints: preserve canonical science contracts and the completed calibration
stop-loss. Do not change process physics, refit parameters, invent authority,
use downstream compensation, or publish a draft. Keep implementation
verification, calibration, independent evaluation, comparison, downstream
response, release transfer, and application fitness distinct.

Conservation/output acceptance: record operand lineage, separate plausible
aliases, reject known wrong formulas, independently reconstruct produced
outputs, retain real closure or magnitude audits, and align metadata and
schema. Do not close a mass or transformation claim on a one-sided bound or
self-consistency alone.

Subagent requirement: REQUIRED for one internal domain-science review, one
independent reproduction/publication review, and two terminal verifications.
This prompt explicitly authorizes subagent spawning/delegation to those roles
for read-only review of the report, results, research objects, reproduction,
validation, and lifecycle boundary. Expected outputs are compact findings,
reconstructions, and review text for package artifacts. If a campaign/release
or other heavy full-workspace gate is selected, REQUIRED: spawn
`comparator_suite_runner` for that run; do not run it on the parent model unless
the runner is unavailable and the package records command-level evidence.

Autonomy: after figure-plan freeze, execute all phases without requesting
additional user direction unless a hard authority or evidence boundary is
reached.

Outputs: complete the V2 draft source and package evidence, then either follow
valid authenticated human authority through publication and release transfer
or close at `HOLD-HUMAN-APPROVAL` without public exposure.
