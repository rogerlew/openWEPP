# Terminal Verification B

Status: `PASS — EXECUTED-HOLD VERIFIED`

Evidence class: `Ran — independent terminal verifier`

The verifier independently:

- reproduced all 32 strict values byte for byte at SHA-256
  `515344ded0cc73b344cc40f7972439a2036adef39401443c432f79f72d605dba`;
- rebuilt all eight figures and five generated CSV/manifest outputs identically
  in scratch space;
- checked all eight SVG hashes, XML parsing, nine source-manifest hashes, and
  declared sidecars;
- passed direct report/result/catalog schemas and exact use of 39 research
  objects, 12 references, 32 values, one table, and one native figure;
- passed path confinement, regular-file/non-symlink, Markdown, American
  English, and `git diff --check`;
- confirmed protected public hashes and no catalog, identity-lock,
  transaction, `usersum`, or generated-output change; and
- confirmed `validate --all` passes for both admitted reports, while direct
  canopy validation and planning fail honestly as unknown report ID.

Verdict: `PASS` for the truthful executed-hold disposition. Canonical canopy
admission/build, human approval, release transfer, and publication remain
blocked or not run and must not be described as complete.
