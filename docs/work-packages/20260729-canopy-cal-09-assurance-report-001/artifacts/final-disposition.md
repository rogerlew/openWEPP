# Final Disposition

Status: `EXECUTED-HOLD`

Evidence class: `Ran`

CAL-09 completed every safe source-level action in scope:

- manuscript, supplement, descriptor, strict result, references, tables, and
  figure/research-object surface authored;
- 32 strict values reproduced byte for byte;
- eight time-series figures rebuilt deterministically;
- 39 research objects, 12 references, 32 values, one table, and one
  schema-native figure close exactly;
- direct schemas, paths, source hashes, XML, Markdown, language, and diff
  checks pass;
- domain-science and reproduction/publication internal reviews pass after
  complete finding disposition; and
- two independent terminal verifiers pass the executed-hold state.

CAL-09 cannot be marked complete. The typed V2 CLI has no new-report admission
operation, so the report is intentionally absent from the catalog and identity
lock. Canopy-specific canonical validation, planning, normalization checking,
staging, build, and check are therefore blocked or not run. Accountable human
scientific approval, human reproduction/publication approval, assurance
authorization, release transfer, and publication are also unavailable.

The existing admitted V2 catalog remains valid, protected public hashes match
the intake baseline, and no draft output was published. Terminal disposition:
`EXECUTED-HOLD — SOURCE COMPLETE; TYPED V2 ADMISSION AND HUMAN APPROVAL
REQUIRED`.
