# Research-Object Manifest

Status: `COMPLETE FOR DRAFT`

The V2 descriptor declares every safe claim-bearing source needed for the
compact synthesis:

- strict JSON result and standard-library reproduction procedure;
- accepted ensemble, Harvard holdout, litter ridge, gradient ensemble, Bezà
  member summary, Elliot comparison, and complete candidate-configuration
  CSVs;
- versioned native-forest canopy narrative;
- deterministic time-series figure builder and source manifest;
- every plotted raw source and derived table, including canopy climatology,
  source-decay design, Southern daily state, Bezà product curves, admitted
  Harvard and Marcell snow rows, and the F1, F4, F6, and F7 retained plot
  tables;
- F1 through F8 SVGs, each with a same-basename Markdown caption/ancillary
  sidecar retained in the package; and
- the disclosed agent-assistance packet.

All declared object paths are confined regular files and are repository-
licensed project outputs. External literature is cited by immutable identity
and is not redistributed. No temporary run root, symlink, protected raw
dataset, credential, or absolute workspace path is published.

The current V2 schema renders only `linear_magnitude_bars`; its one native
figure summarizes Harvard range endpoints. The scientifically preferred
time-series SVGs remain version-bound research objects with adjacent linked
Markdown captions and limitations so the schema limitation does not flatten
or replace them. The descriptor declares 39 public-safe objects, and exact-use
checking confirms all 39 are linked from the manuscript or supplement.

Source-level completeness passes internal review. Canonical V2 admission and
human reproduction/publication approval remain unavailable.
