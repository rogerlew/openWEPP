# Worker Handoff

Status: `READY`

CAL-09 is source-complete and internally reviewed. Its retained internal source
contains a manuscript, supplement, schema-valid descriptor, deterministic
32-value strict result, 39 public-safe research objects, eight SVG figures,
source tables, captions, and exact reproduction procedures.

The source is intentionally absent from `assurance/v2/catalog.yaml`. The first
actionable next step belongs to a separate production-tooling package: design,
implement, review, and test a typed V2 new-report admission transaction that
updates the catalog identity and report members atomically. Do not hand-edit
`identity.lock.json`.

After typed admission, run canopy-specific canonical validation, planning,
normalization checking, two disposable builds/checks, and byte comparison.
Only then seek accountable human scientific and reproduction/publication
review for the exact admitted root. Release transfer and publication remain
later human-authorized operations.

Protected surfaces remain `usersum/assurance/**`, `assurance/generated/**`,
transactions, snapshots, exports, vendors, and WEPPcloud. Do not copy draft
outputs into them.
