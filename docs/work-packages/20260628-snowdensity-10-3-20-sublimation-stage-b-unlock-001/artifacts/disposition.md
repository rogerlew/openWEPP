# Disposition

Evidence class: Static + Ran.

Disposition: `NON-PROMOTION-GATE-NOT-MET`.

The current default remains active. Stage A composition and Stage B are opt-in
diagnostic profiles only.

Basis:

- Current default: `15` robust fails / `179` robust score.
- Partition + Stage A: `19` robust fails / `168` robust score.
- Stage B: `15` robust fails / `178` robust score.
- Stage B conservation closed, but the primary score did not beat the current
  default and the bidirectional guardrail failed.

No activation/default/schema/fixture/cap/frost/user-surface change is authorized.
