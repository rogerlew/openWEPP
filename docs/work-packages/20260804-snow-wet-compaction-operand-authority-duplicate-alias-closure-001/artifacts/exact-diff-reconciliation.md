# Exact-Diff Reconciliation

Status: complete / in envelope

Evidence mode: Static + Ran

## Intent to terminal diff

- Declared intent: Critical science implementation and defect closure plus one
  deterministic `DEVELOPMENT_ONLY` fixture derivative.
- Pre-implementation base: `d41a67c7a2c8d199f9f05f5f309b9b85915e01e1`.
- Scaffold commit: `4a6948ddbcb652310f4ca063a6c57f9b206a3740`.
- Terminal validation identity before review/closure prose: worktree diff
  SHA-256
  `bd07523f8e0f566c52a152ff4ef6d8dd2c2deadfae5ab760c88c7d6d4d4e4119`.
- Materiality execution identity: scaffold HEAD plus implementation/tool/test/
  fixture/assurance diff SHA-256
  `5f60e18c026d86ad77a1669eee22956771ef7a3bc53cbb07d07c81ea8a46b112`.

The base-to-terminal scope contains `86` intended paths: `77` tracked paths
visible in the base-to-worktree diff plus nine implementation/closure-generated
untracked paths that are committed at closure. The later delta from the
terminal validation identity consists only of the two review artifacts,
closure artifacts, prompt archival, and three roadmap/catalog status updates.
No production, contract, test, fixture, or assurance source changed after the
terminal campaign.

## Reconciliation by surface

- Production: five declared orchestrator/runner files implement the private
  exact operand and the aligned internal CoE seam. The density algorithm file
  was intentionally untouched because its formula, constants, cap, selectors,
  and bulk/multilayer consumers were already authoritative.
- Authority: `SC-SNOWFREEZE-001` v125, its index, two typed assurance source-
  adoption transactions, and generated identity/review locks are in scope.
- Tests: one new registered integration target, owning module tests, and the
  existing exact-version contract pins are in scope. Pin edits are mechanical
  v124-to-v125 adoption only.
- Fixture: canonical Snowbird bytes are unchanged; only the declared manifest,
  family README, deterministic generator, and derived development directory
  change.
- Documentation: this package, package catalog, main roadmap, and snow campaign
  roadmap are in scope.

`Cargo.lock` and dependency resolution are unchanged. `Cargo.toml` changes only
to register the owned integration target. No public v1 CoE report JSON, public
replay CSV, runtime trace, WAT/HBP/PASS output, observation, PRCPSA, phase,
energy, radiation, canopy, frost, density coefficient, cap, or default selector
changed.

Terminal `git diff --check`, format, workspace/all-target Clippy, quick, frost,
full workspace, doctest, dependency-policy, assurance, fixture, and authority
guards pass. Documentation-only closure checks are recorded after final prose.
