# Artifacts

The ledgers and narrative record the equation-level authority decision.
`tools/generate.py` deterministically creates the two analytical figures and
checks package-local CSV, SVG, sidecar, and required-file invariants.
