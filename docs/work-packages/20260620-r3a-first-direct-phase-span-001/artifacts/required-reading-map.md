# R3A Required Reading Map

Status: complete.
Evidence mode: Static.

Populate before Rust edits.

Required-reading scope:

- root and work-package governance;
- architecture authority;
- R0/R1 direct-frame planning artifacts;
- R2A package disposition and gates;
- local Rust crate and science-contract guidance before Rust edits;
- touched source files before editing.

Required-reading bytes:

```text
239992 total
```

Disposition: `OK` (`<=400000` bytes).

Static:

- Root `AGENTS.md`, work-package governance, R3A package, roadmap, R0/R1
  planning artifacts, R2A closure artifacts, crate guidance, and
  science-contract guidance were read before Rust edits.
- Source inventory was read for `direct_runtime.rs`, direct runtime tests,
  runner setup selection, runner R2A tests, and the scheduler no-diff status.
