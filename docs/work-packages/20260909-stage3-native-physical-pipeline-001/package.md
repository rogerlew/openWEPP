# Stage 3 native physical-pipeline cost and implementation experiment

Status: COMPLETE — SUPPORTED NO-IMPLEMENTATION DECISION. Production and
integration qualification remain HOLD / NOT QUALIFIED.

## Objective, authority, and boundaries

Execute the owner authorization in
`/tmp/openwepp_native_physical_pipeline_experiment_authorization.md` end to end:
attribute the expensive operation inside the represented-snow native covered
physical-value pipeline, select at most one source- and cost-justified cheaper
treatment, implement it in an isolated B cut when the plausible complete-run
effect clears the default 5% priority criterion, and evaluate it through the
actual authenticated one-OFE runner. A supported no-implementation decision is
an acceptable study outcome.

The review checkpoint is `0328e529f175898fdd77493a8181b6024682d4b0`, which
was current and clean at intake. No equivalent package existed in the current
locator or package search. Accepted predecessor evidence and the preserved
measurement composition come from
`../20260909-stage3-support-capability-representation-001/`; its support-boundary
negative result, incomplete-key reuse exclusions, F/J exclusions, fixture,
collector, release posture, and protected-output semantics remain binding.

Primary-checkout writes are this record, its locator row, and retained raw
evidence. Runtime observation and candidate changes remain isolated and
recoverable; they do not become production-reachable here. No push, deployment,
branch switch/creation, support/Jacobian/reuse campaign, solver/science change,
schema change, persistent-state change, or production activation is authorized.

Implementation intent before edits: observation-only instrumentation will first
subdivide the actual native builder body. If and only if a disjoint operation has
a plausible 5% whole-run treatment, one isolated implementation-only B cut will
replace that operation while preserving arithmetic order, validators/error
precedence, physical outputs, custody, chronology, rollback, restart, canonical
encodings, and work populations. Material write-set changes will be recorded.

## Governing authority and risk

The package follows root and nearest `AGENTS.md`, `docs/work-packages/AGENTS.md`,
the kernel preparation procedure, prompt wording guidance, and the current
testing/gate strategy. The reached authority includes
`SC-LANDSURFACEENERGY-001` `INV-LANDSURFACEENERGY-155/159/161` and its
physical-only/custody chapters, `SC-SNOWENERGY-001`
`INV-SNOWENERGY-084/086`, `SC-COUPLEDTIME-001` charged-role/chronology
obligations, `SC-SURFACELIQUID-001` WB14 parent and physical-prefix custody, and
affected plant/soil obligations reached by fixed-final preparation. Observation
changes do not amend science. Before B, the exact private representation and its
dependency/invalidation proof will be mapped to existing authority or a narrow
experimental amendment will precede code.

Observation is an isolated profiling change requiring one independent review.
A B cut that changes trusted physical preparation/custody is conservatively
critical and requires complementary correctness/authority and QA/evidence
reviews, contract-first confirmation, applicable A0/A1/A3 evidence, real-consumer
closure/error/rollback/restart checks, affected warnings-denied lint, and the
current full-correctness profile. Terminal requirements will be reconciled to
the exact diff; candidate-only gates do not trigger if no candidate is admitted.

## Source-backed operation and dependency map

The real entry is
`build_covered_carrier_physical_value_with_soil_beginning_v1`, which completes
lower boundaries and clears three nonphysical receipt fields before dispatching
to native V4 evaluation or the ordinary prepared constructor. The represented-
snow native evaluator clones/reseals forcing, constructs the interval input,
runs fixed-final V3 preparation, checks its tile posture, constructs a second
hydrology adapter from the original frame, validates/selects the WB14 parent,
constructs the unified hydrology candidate, admits the private physical result,
and returns it to precipitation/boundary/LSE/soil evidence consumers.

| Operation | Inputs and result | Mutable side effect / validation | Downstream consumer |
| --- | --- | --- | --- |
| Lower-boundary completion/native selection | Immutable covered/open destination sets and live boundary maps; complete map and native/ordinary choice | Map/set construction, exact completeness and half-native rejection; ordinary path does not execute native work | Native V4 evaluator or ordinary physical constructor |
| Forcing adaptation/reseal | Live covered LSE forcing plus vegetation/WB14 inputs; snow-neutral inspection/physical forcing | Whole forcing clone, three field changes, canonical encoding/hash, scalar/parcel/meteorology validation in fixed-final preparation | V3 fixed-final preparation |
| Fixed-final preparation | Current V3 resident/configuration/state, forcing, validated V8 projection, caps/boundaries, support/transaction, unpublished soil candidate/continuation/read proof | Transaction/duration/branch checks; effective hydrology-frame clone with optional WB14 candidate-surface overlay; first adapter; LSE/soil projections, validations and physical calls; no publication | Fixed-final candidate and later unified construction |
| Effective-frame hydrology adapter | Clone of current hydrology frame, optionally overlaid with current WB14 working surface, day/transaction/duration/owner/layer maps | Materializes and validates adapter against the effective preparation frame | Fixed-final LSE/soil preparation |
| Original-frame hydrology adapter | Original immutable hydrology frame, same day/transaction/duration/owner/layers | Independently materializes/validates adapter; equivalence to effective-frame adapter is not presumed | Unified native hydrology candidate |
| WB14 parent join | Immutable native resident handoff plus optional live working parent | Validates exact surface configuration and liquid arithmetic; selects live/native parent without mutation | Unified candidate construction |
| Unified native hydrology candidate | Second soil adapter, surface configuration, fixed candidate, resident LSE beginning, active parent, finalization flag and child binding | Executes actual surface/soil/hydrology kernel/evaluator work and constructs candidate; no accepted-state install | Physical-result constructor |
| Private physical admission | Fixed candidate and unified candidate | Validates complete private physical endpoint/custody; destruction remains in owning scopes; no vegetation/BGC/restart/publication | Provisional precipitation, corrected-boundary, LSE-state, soil/energy evidence |
| Existing diagnostics | Runtime-gated compact observer plus test-only physical projection/audit | Bounded clocks/counters when enabled; canonical runtime validation is distinct | Retained receipt/accounting and protected-parity checks |

Immutable configuration includes V3/V4 residents, LSE/surface/vegetation
configuration, topology/layer maps, and validated V8 projection. Live inputs are
forcing, transaction/support identity, current vegetation/LSE/surface/WB14 state,
caps/lower boundaries, and unpublished soil candidate/continuation. Adapter
sharing is forbidden unless the effective and original frames plus all dependent
semantics are proved interchangeable for the exact use.

## Frozen observation protocol and selected checks

The source baseline is the predecessor's preserved detached composition at
`/tmp/openwepp-stage3-support-representation-Q0so8k` (base
`e89befa4678eadec039b3e7f7fe0a176af8e9dc5` plus its retained tracked/untracked
experimental source), not a checkout of the review checkpoint. Its finalized
collector, admitted one-OFE fixture, 180-second timeout, CPU0, 64 MiB Rust stack,
release build with LTO disabled, allocator/toolchain/environment, complete runner
interval, protected identity/output/control/closure comparisons, and observer
off/on posture are inherited. Relevant source/build/input bytes will be admitted
once. The observation, frozen A, and B cuts use isolated source copies and
distinct targets; prior measured A/F/J/source are never overwritten.

Before result-bearing profiling, the bucket order, nested/exclusive relationships,
fields, exclusion policy, and run order are frozen here: whole runner remains the
denominator; builder inclusive and children are reported separately; children
already present elsewhere in the whole-run partition are not added again. RAII
scopes end at the named operation. Timed fields are aggregate nanoseconds and
counts only. Detailed frame/result comparison is a separate untimed admission
check. Valid slow samples remain. Nonzero, timeout, identity/output/work/accounting
failure is adverse evidence and retained; only the prospectively defined
collector-integrity/infrastructure invalidity may exclude a run, without retry in
the frozen six-pair series.

Observation development permits at most two complete runs, followed by two
warmups per posture and six balanced fresh-process off/on pairs using the same
observation-capable executable. One dominant internal subdivision is allowed if
the first partition cannot support selection. Every receipt retains argv, cwd,
environment, executable/source/input identity, exit status, stdout/stderr hashes,
raw profile and final protected record; one complete receipt is validated before
the series.

Selected observation checks: source/patch admission; compiled release workflow;
collector accounting and receipt negatives; source-real participation and exact
off/on protected parity; population stability; isolated formatting and relevant
lint under the standard's prospectively selected matched-command, source-
attributed no-new-diagnostic policy; package JSON/link/diff checks; and one
independent observation-design/evidence review.

Treatment admission uses only its disjoint remove/replace set:
`saving = f * (1 - 1/s) - additional whole-run overhead`. Perfect elimination
below 5% ends in a supported negative decision. If 5% is structurally plausible,
the package admits one private implementation treatment and freezes matched A/B
after correctness. Then two warmups per executable and six balanced fresh-process
A/B pairs run with identical observation posture/workload. Results report raw
paired seconds/ratios, medians/ranges/paired uncertainty, process and runner CPU,
work/invocation changes, RSS, and exact output/closure parity. One extension to
twelve total pairs is allowed only if uncertainty can change disposition, and at
most one performance-driven refinement is allowed.

## Current state and next action

Study execution is complete. No B cut was admitted because the only disjoint
operation with a perfect-elimination ceiling above 5% is the required fixed-final
physical evaluation itself, and no permitted implementation-only treatment can
plausibly accelerate it enough without changing excluded numerical/scientific
work or invoking excluded result reuse. No production, authority, test, schema,
or activation source was changed.

## Observation execution and evidence

The isolated observation cut is base
`e89befa4678eadec039b3e7f7fe0a176af8e9dc5` plus the predecessor composition and
[`artifacts/observation-001/instrumentation-pre-lint-fix.patch`](artifacts/observation-001/instrumentation-pre-lint-fix.patch),
SHA-256 `df1e84a90a485bd270acbe227b08205d196466f2c1e6c4f39bee3a354eb31395`.
The frozen release test binary SHA-256 is
`5f7def47decaffd71943f07e3baf25e30bfa3bdefd024d9fa9c6d020af1bbd1d`;
its generated sidecar SHA-256 is
`7bbe095083fc612187ae8ca53cd1cff570b072878417dcbe367fe1f8f4f714d4`.
That SHA identifies the measured pre-lint source. Final isolated observation
source adds only `#[allow(clippy::too_many_lines)]` to the instrumented V4
function and is retained as `instrumentation.patch`, SHA-256
`5db3436348bdaae05cfe458d33b97075c6a4e820d4fb474d506b0da17e22d943`.
Its rebuild produced the byte-identical binary and sidecar above, directly
proving exclusion from runtime results and permitting reuse of the frozen
series. The binary manifest records source commit `unknown` when invoked from the
isolated series cwd; admission binds that actual value, while reproducible source
identity remains the base plus retained patch and exact binary hash.

Ran: release build passed. A first development run showed the two adapters were
immaterial and justified the one allowed fixed-final subdivision. The next
development run exposed 452 entries because the common function also served 52
non-native calls; it was rejected for native attribution. The corrected scopes
participate only when covered lower boundaries exist. The corrected run exited
zero with 400 entries per native child, zero accounting difference, unchanged
outputs, and unchanged LSE maps/solves/sweeps/evaluations. These development
console results selected/fixed the cut; the finalized primary receipts are the
frozen series below.

The first frozen-series launch stopped after its first off warmup because the
admission object expected the detached SHA while the isolated cwd produced the
actual manifest value `unknown`. Its raw stdout and invalid receipt are retained
under
[`artifacts/observation-001/failed-series-001-source-cwd/`](artifacts/observation-001/failed-series-001-source-cwd/).
No measured pair was produced. The admission correction changed only this
provenance expectation, retained independent source identity, and started a new
series.

Ran: the corrected frozen series completed two warmups per posture and six
balanced fresh-process off/on pairs on CPU0. All 16 processes exited zero and
passed exact fixture, executable, protected input/output/control/closure,
carrier/LSE work, raw-stream hash, population, and additive-accounting checks.
The complete receipts, stdout/stderr, protocol, completion marker, and analysis
are retained under
[`artifacts/observation-001/frozen-series/`](artifacts/observation-001/frozen-series/).
The analyzer/collector/admission SHA-256 values are respectively
`ff4aa38c...fb68c85`, `d77fd48f...f0f5fa`, and `71d4d112...071d2`; final
analysis SHA-256 is `a6e55b97...7d2b8ef`.

Off runner median was 4.9857745 s (4.971771–5.114092 s); on median was
4.9917235 s (4.975169–5.014108 s). Paired observer wall perturbation median was
−0.1025% (−2.0625% to +0.7483%), and paired process-CPU perturbation median was
−0.0425% (−2.1106% to +0.8080%). No perturbation was subtracted from any bucket.
Measured-process CPU was 4.98758–5.139023 s and ordinary RSS was
60,060–73,276 KiB. Memory is observational.

The formerly exclusive builder region is the disclosed sum of 18 mutually
exclusive descendants: median 0.6789172 s / 13.5995% (0.675073–0.681835 s;
13.5264–13.6429%). `CarrierPhysicalValue` alone is now only its 0.442 ms residual.
The true function-inclusive builder envelope adds only the source-proved nested
`CoveredFixedFinal` family (`400` solves, zero sweeps), median 0.0050130 s, and
is 0.6839166 s / 13.6995% (0.680018–0.686848 s; 13.6287–13.7436%). This local
inclusive reconstruction is not added again to the whole-run exclusive
partition. `CarrierPhysicalSetup`, evidence, completion, and subsequent
post-builder work remain outside it.

| Exclusive native operation | Count | Median seconds / runner share | Selection consequence |
| --- | ---: | ---: | --- |
| Fixed-final physical call | 400 | 0.354642 / 7.0955% | Required physical evaluation. Perfect elimination is 7.07–7.13%; 2× saves only 3.53–3.56%, while an unsupported 5× would save 5.65–5.70%. No same-arithmetic implementation route supports that acceleration. |
| Unified hydrology candidate | 400 | 0.221882 / 4.4490% | Actual required surface/soil/hydrology candidate execution. Even perfect elimination is 4.42–4.46%; do not prototype. |
| Fixed-final soil preparation | 400 | 0.042696 / 0.8522% | Required authenticated support/soil beginning and conversion. Perfect elimination is below 0.862%; do not prototype. |
| Fixed-final state projection/hash work | 400 | 0.025006 / 0.5009% | Required live soil/hydrology/forcing identity work; ceiling about 0.506%. |
| Original-frame adapter | 400 | 0.006291 / 0.1262% | Frames remain semantically distinct; even elimination is immaterial. |
| Effective-frame adapter | 400 | 0.004180 / 0.0837% | Same conclusion; adapter sharing cannot approach the priority target. |

Other exclusive medians were: lower-boundary completion 0.884 ms; forcing
adaptation/reseal 4.350 ms; fixed-final preflight 7.977 ms; effective-frame clone
0.660 ms; vegetation projection 4.114 ms; canopy receipt 0.957 ms; WB14 join
0.016 ms; physical-result admission 2.218 ms; native evaluator remainder
0.950 ms; and builder residual 0.442 ms. Existing experimental diagnostics were
compiled but correctly recorded zero participation at this release harness
boundary; they were not relabeled mandatory runtime validation.

The fixed-final physical call is expensive because it performs the necessary
covered fixed-final physical evaluation. Preserving equations, floating-point
order, solver choice, derivative method, convergence/pivot thresholds, cadence,
trajectory, and all independently charged map roles rules out changing or
coarsening that computation. Route C reuse is excluded: every charged covered
map remains independent under `INV-SNOWENERGY-086`, no complete effective-input
interchangeability was established, and F is an already-known distinct
invocation-local mechanism. Preallocation/materialization opportunities measured
outside the physical call have sub-1% ceilings; prior Jacobian/inactive-column
routes are settled exclusions. Therefore no concrete replacement has both a
plausible 5% complete-run effect and unchanged obligations. Forcing a B prototype
would violate the authorization's admission rule.

## Validation, review, and disposition

Ran:

- Isolated `cargo fmt --all -- --check`: PASS before each frozen build.
- Release `cargo test --release --offline -p openwepp-runner --lib --no-run`
  with LTO disabled and 64 MiB stack: PASS. The only rustc warning was the
  predecessor's known `ending_snow_hint` dead code; the instrumentation's one
  initial `unused_mut` diagnostic was corrected before freeze and the repeated
  build showed no new warning.
- Actual ignored one-OFE runner workload: PASS for corrected development and all
  16 frozen processes; exact output hashes and conservation operands matched.
- Analyzer replay and negatives: PASS. Invalid receipt, nonzero accounting
  difference, and population drift were rejected. Independent replay matched the
  retained analysis byte-for-byte before the final wording-only clarification;
  that clarification was regenerated from unchanged receipts.
- Isolated bounded inherited-lint command for orchestrator and runner with
  `--release --no-deps`: exit 0. Matched predecessor/candidate comparison first
  found one new line-count diagnostic (1,121 versus 1,120 inherited orchestrator
  warnings). A narrow observation-only allowance corrected it; rebuild retained
  byte-identical executable/sidecar hashes, and final Clippy returned to exactly
  1,120 inherited orchestrator warnings with no new touched-source diagnostic.
  No warnings-denied critical gate applies
  because no B, production, authority, or test change was admitted.
- A primary-checkout `cargo fmt --all -- --check` was attempted and failed on
  pre-existing formatting in tracked files outside this package
  (`covered_natural_limit_diagnostic_parity_tests.rs` and
  `advisory_linter_authority_contract.rs`). Those files are unchanged in the
  terminal primary diff and were not modified. The applicable isolated source
  formatting check passed.

Independent observation review inspected the isolated diff, all retained raw
receipts and hashes, collector/analyzer, failed attempt, operation endpoints,
native-only participation, and inclusive reconstruction. It found one blocking
unsupported `builder_inclusive` label because the first analysis excluded nested
family cost. The finding was accepted: the old analysis is retained as
superseded, the former exclusive partition was renamed, and the true inclusive
envelope now adds the exact `CoveredFixedFinal` family. Reviewer fix verification
re-executed analysis, independently reconciled
`0.6789171705 + 0.0050129505 = 0.6839166 s`, verified the source/counter guards,
and returned PASS with no remaining blocker. Its final nonblocking wording note
was applied without changing operands or results.

Final diff reconciliation: the primary checkout contains only this package's
record/evidence and the active locator row. Experimental runtime changes remain
in `/tmp/openwepp-native-physical-obs`; no B source exists. Consequently
treatment-only contract amendments, contract-derived tests, A0/A1/A3 campaign
gates, full-workspace correctness, second consequential reviewer, restart/error/
rollback candidate tests, and A/B performance series did not trigger. External
authority-suite and anti-evasion surfaces were not touched.

**Study outcome:** supported no-implementation decision. **Study execution:**
complete. **Production/integration qualification:** HOLD / NOT QUALIFIED. The
bounded result is that the 13.7% inclusive builder consists principally of two
necessary physical operations: a 7.10% fixed-final evaluation for which a 2×
implementation improvement misses the target, and a 4.45% unified candidate
whose perfect-elimination ceiling misses it. Further progress requires separately
authorized evidence for a materially cheaper same-arithmetic physical evaluator
or a science/controller/cadence change; repeating adapter, support, F, Jacobian,
or incomplete-key reuse work is not supported.
