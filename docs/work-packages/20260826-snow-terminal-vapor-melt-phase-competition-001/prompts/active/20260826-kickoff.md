# Kickoff

Scope: local repository science/kernel research; flat-file reads/edits only.
Execution mode: package-end-to-end. Close
`CHILD1-TERMINAL-PHASE-COMPETITION-001` end-to-end through truthful disposition.

Required reading — Core: `/workdir/openWEPP/AGENTS.md`,
`docs/codex_exec_plans.md`, `docs/work-packages/AGENTS.md`,
`docs/work-packages/README.md`, and this package. Conditional:
`docs/defect_closure_execplans.md`, science-contract authoring/profile/index if
contract candidates are authorized. On-demand: SnowEnergy/SnowFreeze contracts,
the prior discrete-root hold/evidence, and touched source. Budget and routing:
`524415` local bytes, `WARN`; `artifacts/required-reading-map.md`.

Task: execute package phases and matrix; do not stop while safe source reading,
test implementation, validation, or finding correction remains in-envelope.
No surrogate physics. Preserve every protected production boundary. Use
independent operand reconstruction and alias-separating poisons.

Subagent requirement: independent science and ownership reviews are required
after model selection. Heavy batch/comparator work requires
`comparator_suite_runner`. This prompt explicitly authorizes subagent
spawning/delegation for those read-only reviews and runs; compact findings and
log paths are required, with bounded package-artifact edits only.

Autonomy: execute end-to-end without additional direction unless a declared
hard boundary is proven. HOLD requires the package legitimacy audit.
