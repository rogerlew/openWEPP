# Terminal diff reconciliation

Status: `RECONCILED / PRODUCTION PHYSICS UNCHANGED`.

The terminal package diff contains:

- the authorized source-governance split of cfg(test) discrete endpoint
  evidence from `snow_stage3_v11_terminal_execution.rs`;
- centralized terminal result/closure assembly and WB14 duration-ceiling
  mapping;
- cfg(test) Candidate A/B allocators, fixture access, matrix tests, and the
  falsifying partition counterexample;
- fail-closed non-finite evidence guards and removal of Candidate B's
  tolerance-based solid deletion;
- package evidence and the terminal HOLD disposition.

No constitutive equation, physical constant, tolerance, carrier floor, public
API/output, owner publication, persistent-support physics, receiver, restart,
runner, selector/default, activation, CoE, Child 3/4, or cutover path changed.
The Candidate A mode and allocator remain cfg(test). Production remains
`BelowCarrierDomain`.

Line counts after final formatting:

- `snow_stage3_v11_terminal_execution.rs`: 1,939;
- `snow_terminal_phase_competition.rs`: 706;
- `snow_stage3_discrete_endpoint_evidence.rs`: 313.

The separate Assurance V2 reconciliation package and retained guards were not
modified.
