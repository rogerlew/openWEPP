# Terminal hold disposition

Status: `COMPLETED / DEFECT-SHAPED HOLD`.

Candidate A closes aggregate support-local algebra but fails the required
partition/order invariant at meltout. Candidate B's tested deposition-lag
surface-frost chronology is also partition dependent, lacks an authorized
owner, and cannot be installed. Candidate C therefore remains typed
`Unsupported`.

The controlling counterexample starts from `0.6 kg m^-2` ice with zero liquid
and cold content. Aggregate deposition `0.002 kg m^-2` plus energy `0.602 L_f`
ends at zero ice (`AtEnd`). Applying energy first melts out earlier with
`667.2 J m^-2` excess (`Interior`); later deposition recreates
`0.002 kg m^-2` ice (`Reappeared`). Reversing order ends with zero ice. This is
material endpoint and chronology dependence, not a root-search defect.

The real fixture remains useful consistency evidence: at
`615737728343 ns`, Candidate A moves only `3.0422e-12 kg m^-2` of additional
ice into liquid while material deposited ice remains. It does not establish a
terminal event or complete jointly converged owner candidate.

Consequences:

- production remains `BelowCarrierDomain`;
- released Stage-3 equations, tolerances, 600-ms floor, APIs, outputs, owners,
  publication, restart, receiver, and runner are unchanged;
- no SnowEnergy/SnowFreeze successor is authored;
- LSE/CoupledTime, Batch V2, Child 3/4, cutover, Stage-3 activation, and CoE
  retirement remain unauthorized;
- `43cc9bbea2fbf5fe6ab6596cee4162de75cef999` remains the last fully
  qualified physical implementation;
- workspace Assurance V2 reconciliation remains separate and untouched.

The package may only be superseded by new science authority that resolves
within-support phase chronology, or explicit owner authorization for a fully
defined residual-frost regime. Search-method work cannot cure this defect.
