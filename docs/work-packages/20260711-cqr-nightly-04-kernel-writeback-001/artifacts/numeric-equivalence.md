# Numeric Equivalence

Ran: no production numeric expression changed. Tests assert exact accepted
boundary values, rejected bound shapes, non-finite priority, sorted symbol/value
application, direct indexed and logical surface values, and no partial mutation
on late resolution failure. Production source identity is exact.
