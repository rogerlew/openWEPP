# Exact diff reconciliation

Status: queued

Evidence mode: not-run

The terminal artifact must reconcile the exact base, changed paths, canonical
contract versions, package/catalog/roadmap lifecycle, protected Child 2B files,
and the absence of production Rust edits outside any explicitly amended scope.
