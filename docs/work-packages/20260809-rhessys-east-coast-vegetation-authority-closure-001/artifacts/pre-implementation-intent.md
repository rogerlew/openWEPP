# Pre-Implementation Intent

Status: `frozen / documentation-evidence execution`

Evidence mode: `Ran + Static`

Base commit: `4237552aa8dbc84a8baeff800014b23c7e75be9f`.

The initial dirty state was the authorized scaffold: six modified lifecycle and
successor files plus the untracked closure-package tree. No production Rust,
Cargo, contract, or test file was dirty.

The frozen candidates are chestnut oak profile `805`, eastern white pine
profile `807`, Coweeta WS18 oak and adjacent WS17 pine observations from
2005-2006, and separately resolved strata. The observations are assigned
`DIAGNOSTIC_ONLY` unless a complete dated initial-state route is admitted. They
are not one mixed-stand observation and are not calibration or
independent-validation data in this package.

Intent is authority characterization and closure where exact authority exists.
Canonical targets were prospectively `SC-VEGETATION-001` and only the minimum
adjacent owners required by a complete family. Contract/test edits are selected
only if a whole family is admitted. Production Rust, Cargo, runtime behavior,
publication, calibration, and deployment remain protected.

Direct validation selected prospectively is Markdown lint for every changed
documentation surface plus `git diff --check`. If the terminal diff contains
no canonical contract, test, Rust, or Cargo change, Rust correctness commands
are not selected solely because the repository contains Rust.

The package-local write manifest includes every scaffolded artifact and prompt
plus the newly required `artifacts/authority-route-attempts.md`, added before
that path was created to answer independent review of Lane 0 exhaustion.
