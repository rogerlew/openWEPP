# CRAP After

Ran: delegated final `cargo crap --lcov --min 0 --format json`, exit `0`.

The target has `13` CRAP rows, `0` above `30`, and maximum CRAP `16.0` for
`parse_invocation` (CC `16`, 100% CRAP coverage). No target function is below
the 75% CRAP coverage floor and no coverage exclusion is used.

CRAP JSON SHA-256:
`60c2cba22b306d05709874a5068f9ee592516d2ce12f4a44e28a1f24198e5c8f`.
