# Characterization Plan

Before decomposition, add real CLI assertions for help/argument errors,
relative/absolute explicit inputs, aggregate versus per-hillslope precedence,
and explicit missing optional paths. The oracle must consume the public binary
and compare exact hard-fail/error behavior, not only private helper values.
