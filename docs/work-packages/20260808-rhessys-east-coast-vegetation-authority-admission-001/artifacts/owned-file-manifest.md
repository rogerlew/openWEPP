# Owned File Manifest

Status: `frozen`

Evidence mode: `Static`

Allowed writes are this package, lifecycle/catalog/backlog records, prospective
successor dependency/status text, references/rights only if acquisition occurs,
and canonical contract/tests after the strict acquisition/schema family passed.

Production Rust, Cargo, runtime consumers, external-authority registries,
observed datasets, deployment, and either pinned external checkout are excluded.
