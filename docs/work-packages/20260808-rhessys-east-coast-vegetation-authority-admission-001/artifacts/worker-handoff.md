# Worker Handoff

Status: `executed-hold / do not begin successor`

Evidence mode: `Ran + Static`

The next authority effort must first select the target stand/plot/date/topology
and area/age structure. It must then complete the selected consumed-field
manifest and reviewed aliases, admit or replace every consumed pine/oak value,
and construct a complete compatible C/N/root/geometry initial state from
observations or an independently authoritative synthesis.

In parallel only within a newly authorized package, close the exact Gate 2 and
Gate 3 family residuals listed in their artifacts. Reuse contract version 3's
local-byte/schema form; do not reinterpret it as value, initializer,
constitutive, or runtime authority. Do not begin the implementation successor
until residual `AUTH-RHEC-001..011` plus `AUTH-RHEC-014/015` authority blockers
close. After its contract-first gate passes, the successor must implement and
test the already-admitted `AUTH-RHEC-016` boundary.
