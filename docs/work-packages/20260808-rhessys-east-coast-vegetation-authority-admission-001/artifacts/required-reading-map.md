# Required-Reading Map

Status: `measured at terminal disposition`

Evidence mode: `Ran + Static`

Core local reading totals `459411` bytes (`WARN`). The large
`docs/work-packages/README.md` catalog remains Core because package discovery
and lifecycle placement are mandatory; the total is below the `800000`-byte
justification threshold.

| Tier | Paths | Reason |
| --- | --- | --- |
| Core | root `AGENTS.md`; `docs/codex_exec_plans.md`; `docs/work-packages/AGENTS.md`; `docs/work-packages/README.md`; package `package.md` | repository/package governance and executable scope |
| Conditional, triggered | science-contract `AGENTS.md`, authoring procedure, kernel profile, contract spec, unit governance, testing strategy | Critical authority package may amend canonical contracts/tests |
| On demand, triggered | predecessor audit artifacts, `SC-VEGETATION-001`, adjacent contracts, exact primary sources and pinned source/data rows | read only for the authority family being evaluated |

Conditional governance totals `73951` bytes and is excluded from the Core
threshold calculation under the tiered-reading standard. The archived kickoff
prompt intentionally retains the scaffold-time `458213`-byte measurement as
byte-identical execution history.
