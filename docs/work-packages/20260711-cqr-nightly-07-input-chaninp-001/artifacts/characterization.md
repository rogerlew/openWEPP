# Characterization

Not entered. Existing focused tests pass `20/20`, but one explicitly blesses a
raw/normalized `nchnum` conflation that conflicts with the source-model contract.
Characterizing that behavior would freeze unresolved accepted-input semantics.
