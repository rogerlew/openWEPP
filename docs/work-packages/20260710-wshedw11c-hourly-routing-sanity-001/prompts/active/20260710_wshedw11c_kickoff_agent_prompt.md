# WSHED-W11C Kickoff Prompt

Scope: local repository routing-characterization task; flat-file reads/edits
and local executable tests only; no external connectivity.

Execution mode: package-end-to-end.

Phase plan: execute all phases in `package.md` sequentially through
disposition.

Required reading:

- Core: root `AGENTS.md`, `docs/codex_exec_plans.md`,
  `docs/work-packages/AGENTS.md`, `docs/work-packages/README.md`, and this
  package's `package.md`.
- Conditional: `crates/AGENTS.md`, `tests/AGENTS.md`,
  `docs/specifications/science-contracts/AGENTS.md`, and
  `docs/standards/local-ci-gate-selection.md` when their triggers apply.
- On-demand: `SC-ROUTE-001`, WSHED-W11B evidence, the hourly kernel/test owner,
  and `chan.inp` parser.

Required-reading budget: measure and record the exact local byte total with an
`OK`, `WARN`, or `REQUIRES-JUSTIFICATION` disposition in
`artifacts/required-reading-map.md` before Rust edits.

Files: the exact bounded write set in `package.md`.

Task: implement and execute the W11C real-CLI branch/timestep sanity matrix,
record independent closure and magnitude evidence, classify results without
inventing physical authority, and complete all package gates.

Constraints: preserve canonical `SC-ROUTE-001` behavior, typed guards, and
existing W11B tests. Do not edit production physics, add silent defaults, or
introduce heuristic production thresholds.

Conservation/output acceptance: use external HBP totals and separately read
terminal/storage publications for reconstruction; do not accept the writer's
balance column as sole closure evidence; record magnitude and ratio results.

Subagent requirement: REQUIRED. Spawn `comparator_suite_runner` for all heavy
closure/release runs. This prompt explicitly authorizes subagent
spawning/delegation to that runner and two read-only reviewer/verifier roles;
outputs are compact metrics, log paths, and the named review/verification
artifacts; write access is bounded by `package.md`.

Autonomy: execute package phases end-to-end without requesting further user
direction unless hard-blocked.

Outputs: update all package artifacts and final disposition truthfully with
`Static:` and `Ran:` evidence labels.
