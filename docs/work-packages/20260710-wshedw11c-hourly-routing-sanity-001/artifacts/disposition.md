# Disposition

Status: `EXECUTED-HOLD-SANITY-FAIL`

Evidence mode: `Static + Ran`

W11C completed its authorized real-CLI characterization and is held at
`SANITY-FAIL`. Exact zero behavior, finiteness, wave sediment closure, and the
serialized-input algebraic water ledger passed, but they do not override:

- material negative ending storage and generated terminal volume in KW/static
  MC cases;
- large MC peak amplification and timestep sensitivity requiring canonical
  adjudication;
- non-terminal serial-network volume/sediment publication in legacy CREAMS;
- the prior `nchnum=0` sidecar's compatibility-default timestep alias.

All non-physical quality gates pass, including corrected debug and exact
release matrices, protected tests, workspace clippy, erosion, full workspace,
deny, formatting, Markdown lint, and diff hygiene. Both reviews are fully
dispositioned. Verification A reports
`PASS — EXECUTED-HOLD-SANITY-FAIL LEGITIMATE`; Verification B reports
`EXECUTED-PASS` and the same legitimate hold.

No production or contract source changed. Queued WSHED-W11D owns contract-first
closure of all four defect families; W11C makes no physical-validation claim.
