# HA-01 Kickoff Prompt

Scope: local repository code-quality refactor task; flat-file reads/edits only
inside `/home/workdir/openWEPP`; no external connectivity required.

Execution mode: package-end-to-end.

Phase plan: execute every phase in
`docs/work-packages/20260711-cqr-preint-ha-01-cascade-001/package.md`
sequentially through disposition.

Required reading:

Core: `AGENTS.md`, `crates/AGENTS.md`, `docs/work-packages/AGENTS.md`, this
package, the High-A ExecPlan, campaign execution contract, mechanical/CQR/test-
enhancement standards, ADR-0021, prompt-wording standard, science-contract
governance, `SC-OFEROUTE-001`, the target, inline tests, and
`artifacts/required-reading-map.md`.

Conditional: local CI selection when narrowing iteration; unit governance only
on unit-boundary contact; pinned baseline only on semantic contact.

On-demand: adjacent `kinematic_wave.rs`, `profile.rs`, and real lane/executor
consumers when proving the current handoff path.

Required-reading budget: `326401` local bytes, `WARN` because the active
SC-OFEROUTE contract is large; map: `artifacts/required-reading-map.md`.

Files: the cascade target and this package directory; catalog/ExecPlan only at
terminal disposition.

Task: execute HA-01 end-to-end. Cover first, close science coverage/floors and
CRAP at most 30, and avoid production edits when characterization legitimately
closes the row.

Constraints: behavior preserving; no science, threshold, tolerance, error,
serialization, API, consumer, float-grouping, or accumulation-order changes.
Eligibility defaults to science. Exact impossible-arm dispositions require
construction proof, annotation, and dual acceptance. Record the conservative
integral real consumer and do not misrepresent the dormant point fallback as a
production consumer.

Subagent requirement: REQUIRED: spawn `comparator_suite_runner` for all heavy
batch/closure/comparator runs. This prompt explicitly authorizes subagent
spawning/delegation to bounded implementation, review, verification, and
comparator/closure-runner roles. Outputs are package artifacts, compact metrics,
commands, timings, hashes, and log paths; write access is read-only except an
explicit bounded assignment.

Autonomy: execute all phases and update artifacts without requesting direction
unless hard-blocked.

Outputs: complete package evidence and leave a committed terminal record.
