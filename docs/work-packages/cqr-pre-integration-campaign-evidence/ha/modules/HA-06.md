# HA-06 — Direct Publication Row

Status: `MODULE-PASS`

## Identity And Classification

- Target: `crates/openwepp-hillslope-orchestrator/src/direct_runtime/01_publication.rs`
- Start SHA-256: `2dc565facfba9ceea6b783434acf234d7e934ade1a320914160c65c99922800e`
- Final SHA-256: `4320ef0d985978e0000b14f322bfc162b163437a87465629cf8b93e97d2bdcec`
- Tier: science/conservation/publication.
- Actionable row: `DirectPublicationDayRow::from_day_frame`, campaign CRAP
  `33.154`.

## Canonical A–H And Named Bindings

| Family | Exact evidence |
| --- | --- |
| A — nominal | `cqr_day_row_reconstructs_distinct_operands_without_aliases`; `cqr_publication_helpers_cover_guards_and_optional_authority_branches`. |
| B — boundary | `cqr_frost_projection_covers_absent_valid_and_invalid_domains`; `cqr_water_temperature_accepts_nonpositive_and_rejects_positive_meltwater`. |
| C — branch/regime | `cqr_publication_helpers_cover_guards_and_optional_authority_branches` covers optional/required erosion, wet/dry peak, and terminal/nonterminal groundwater. |
| D — invalid domain | `cqr_storage_operands_fail_closed_in_operand_order`; frost-projection and water-temperature tests. |
| E — missing/optional seam | `cqr_publication_helpers_cover_guards_and_optional_authority_branches` covers missing weights/erosion producers and optional storage/temperature; frost test covers absent projection. |
| F — non-finite | Publication-helper and frost-projection tests bind non-finite storage, peak, and projection fields. |
| G — conservation | `cqr_storage_operands_fail_closed_in_operand_order` independently separates total-soil, soil-water, snow, and frozen-soil operands; publication-helper test separates terminal groundwater. |
| H — typed fail-closed | All four guard-focused tests assert original `DirectRuntimeError` variants/fields before row publication. |

Named publication bindings are `SC-WATBAL-001#INV-WATBAL-096` to
`cqr_storage_operands_fail_closed_in_operand_order`,
`SC-SED-001#INV-SED-013` to
`cqr_publication_helpers_cover_guards_and_optional_authority_branches`, and
`SC-OFEROUTE-001#INV-OFEROUTE-012` to the same terminal-groundwater and erosion
publication branches. The real executor consumer
still obtains this row before downstream erosion publication and frame commit.
Rejected aliases are diagnostic/shadow storage, nonterminal groundwater, and
DC01 erosion shape when active routed authority is required.

| Published operand | Units/basis | Authority and anti-alias evidence |
| --- | --- | --- |
| Total soil and soil-water storage | m over lane area | Authoritative final frame storage; `cqr_day_row_reconstructs_distinct_operands_without_aliases` proves distinct 21/19 mm outputs and the storage-order test proves exact guard priority. |
| Snow and frozen-soil storage | m over lane area | Authoritative runtime snow/frost surfaces; distinct negative values reject rather than aliasing total soil. |
| Runoff/erosion volume | m and m³ (`depth × lane area`) | Reconstruction proves 2 mm Q/QOFE and independent 2.4 m³ run volume; the rejected `Q × area` alias differs. |
| Terminal groundwater | m and m³ at hillslope outlet | Reconstruction proves distinct 3/5 m³ and 15/25 mm outputs; nonterminal lanes remain zero and lateral volume cannot alias baseflow. |
| Meltwater temperature | °C | Authoritative typed water-temperature surface; nonpositive nominal and positive invalid values are separated. |

## Change And Tests

Cover-first tests characterize constructors/accessors, frost projection,
storage tolerances, erosion authority, peak-runoff, groundwater selection,
water temperature, and missing operands. Production decomposition extracts a
typed precomputed-operand cluster in its original order (runoff, lateral,
interception, storage, water temperature, erosion) and terminal-groundwater
selection. Field order, arithmetic, errors, schemas, and consumers are unchanged.

Ran:

- `cargo nextest run -p openwepp-hillslope-orchestrator cqr_publication_tests`
  — `4/4` passed.
- `cargo clippy -p openwepp-hillslope-orchestrator --tests -- -D warnings` — PASS.
- `cargo fmt --check`; `git diff --check` — PASS.
- `markdown-doc lint --path docs/work-packages/cqr-high-risk-a-execplan.md
  --path docs/work-packages/cqr-pre-integration-campaign-evidence/ha/modules/HA-06.md
  --format json` — two files, zero errors/warnings.
- Serialized shared profile: `cargo llvm-cov clean -p
  openwepp-hillslope-orchestrator`, then `cargo llvm-cov -p
  openwepp-hillslope-orchestrator --lib --no-report -- --test-threads=1` —
  `370/370` passed in `519.62s`. After adding only the fully frozen test branch,
  the tool rejected `--no-report --no-clean`; its supported `--no-clean --json`
  form ran that one test (`1/1`) and merged the current-source production
  coverage without repeating the full suite.

## Same-Source Metrics

The serialized profile emitted both formats without rerunning tests:

    cargo llvm-cov -p openwepp-hillslope-orchestrator --lib --no-clean --json --output-path /tmp/openwepp-cqr-ha06-final5.json -- cqr_frost_projection_covers_absent_valid_and_invalid_domains --test-threads=1
    cargo llvm-cov report -p openwepp-hillslope-orchestrator --lcov --output-path /tmp/openwepp-cqr-ha06-final5.lcov
    cargo crap --workspace --lcov /tmp/openwepp-cqr-ha06-final5.lcov --min 0 --format json --output /tmp/openwepp-cqr-ha06-final5-crap.json

| Artifact | Bytes | SHA-256 |
| --- | ---: | --- |
| LCOV | 1,029,050 | `e05cf88d47a509e921db05e1f974de7f12efd50de4af822668934b0004ee089f` |
| JSON | 5,359,470 | `5e1ab79fee7807abd04c359ff260751b3216707199abc220b0bf6f49238cb708` |
| CRAP | 2,836,060 | `6766b581d125f00fc50158eef077eb0a2073a1f3177a679616a454996f5bdc26` |

Production-only coverage before the inline `#[cfg(test)]` module is lines
`403/416` (`96.875%`) and production-attributed function regions `415/461`
(`90.022%`), including compiler closure functions. The
minimum base-function floor is `from_day_frame`, `82/105` (`78.095%`);
`direct_publication_storage_operands` is `27/32` regions (`84.375%`). No
production CRAP row exceeds 30. Target result: CC `25`, coverage `84.286%`,
CRAP `27.425`.

## Governance And Review

- Line count: `691 -> 1096`, below WARN.
- Review A: PASS after accepted coverage-denominator, obligation-map, and
  evidence corrections.
- Review B: PASS after accepted production-region and independent anti-alias
  reconstruction corrections. No semantic/schema/order drift was found.

Checkpoint: `MODULE-PASS`.
