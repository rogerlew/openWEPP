# HA-07 — Typed Growth Crop Authority

Status: `MODULE-PASS`

## Identity And Target Slice

- Production host: `crates/openwepp-runner/src/hillslope/direct_publication/day_input_and_helpers/00_builders_and_authority.rs`
- Start SHA-256: `13bcb04171e50595741065c75ded9710c11b5eabed8a6c73f2ff920a3c8fb7a7`.
- Final SHA-256: `bb0a229e847feef1dca374c238ab5c4f6deccf1cb36e5501fbbe0f8008ddf087`.
- Host size: `2804 -> 2811` lines.
- Exact target slice: `direct_production_typed_growth_crop_authority` plus
  `direct_growth_projection_required_scalar`, required integral `usize`/`u8`/
  `u16` wrappers, and transitive `direct_growth_integral_usize`.
- Tier: science/typed runtime authority.
- Campaign row: CRAP `32.832`; final focused target CC `27`, coverage
  `100%`, CRAP `27.000`.

The binding large-host rule applies. Final whole-file focused visibility is
lines `1451/2058` (`70.505%`) and regions `1560/2135` (`73.068%`); unrelated sibling
authorities and rows are neither waived nor dispositioned and remain governed
by the tranche-final workspace ratchet. Final slice coverage/CRAP is: target
`100%/27.000`; scalar helper `100%/3.000`; integral `usize` wrapper
`100%/2.000`; `u8` and `u16` wrappers each `87.5%/2.008`; transitive integral
validator `88%/7.085`. The combined slice clears 90/90, every function clears
the 75% floor, and no slice row exceeds 30.

## Canonical Obligations

| Family | Exact test evidence |
| --- | --- |
| A — nominal | Existing `direct_production_typed_growth_authority_uses_typed_projection` plus `cqr_growth_crop_authority_preserves_schedule_precedence_and_integral_guards`. |
| B — boundary | The precedence test binds annual/perennial schedule selection and minimum start/stop dates. |
| C — branch/regime | The precedence test combines schedule-perennial with runtime-annual authority and exercises both management regimes. |
| D — invalid domain | The precedence test rejects fractional integral controls and invalid/missing date controls. |
| E — missing/optional seam | `cqr_growth_crop_authority_binds_every_required_scalar_error_arm` removes each required scalar; the precedence test removes schedule/runtime management. |
| F — non-finite | `cqr_growth_crop_authority_rejects_nan_required_scalar` and `cqr_growth_crop_authority_rejects_infinite_integral_date`. |
| G — conservation | Not applicable: this constructor selects crop/growth authority and publishes no conserved water/sediment operand. |
| H — typed fail-closed | Both CQR tests assert missing/invalid typed authority returns `HillslopeCliError` rather than defaults. |

The crop constructor is called by `direct_production_typed_growth_slot_authority`,
folded by `direct_production_typed_growth_authority`, and assigned to the day-
input builder's `growth` field at the line-800 construction site. The resulting
annual/perennial inputs are consumed by the orchestrator R5D growth span;
`direct_production_typed_growth_authority_uses_typed_projection` is the exact
consumer-path test. Runtime
`imngmt` remains carried while schedule authority selects the canonical date
branch; the mixed-authority test prevents aliasing those sources.

## Focused Evidence

- New dedicated test module:
  `crates/openwepp-runner/src/hillslope/tests03/cqr_growth_authority.rs`, 128
  lines; host registration is a three-line diff and remains 1,921 lines.
- `cargo nextest run -p openwepp-runner cqr_growth_authority` — `4/4` passed.
- `cargo clippy -p openwepp-runner --tests -- -D warnings` — PASS.
- `cargo fmt --check`; `git diff --check` — PASS.
- Shared profile: `cargo llvm-cov clean -p openwepp-runner`; `cargo llvm-cov
  -p openwepp-runner --lib --no-report` — `108/108` passed in `80.88s`.
- LCOV `/tmp/openwepp-cqr-ha07-final.lcov`: 520,704 bytes,
  `9e4fc809dfe2447aa3f49b6dbebba73ef399bfd9294bd952652d6cb7f459021d`.
- JSON `/tmp/openwepp-cqr-ha07-final.json`: 5,575,584 bytes,
  `167bcf7283c0af7da07d5029c6d26eb022c750e13b93f3fc892605213dc3ef95`.
- CRAP `/tmp/openwepp-cqr-ha07-final-crap.json`: 2,828,910 bytes,
  `22d50252e358382f1ddcaaa5301eb9f5b85e93bd96000efcb18be496568a8f89`.

## Review And Governance

- Production file is 2,811 lines: WARN. The seven-line finite-validation
  correction is inside the declared slice. Follow-up split intent remains runner
  authority-family decomposition under the campaign backlog.
- Test host remains below WARN; new test module is small.
- Review A/verification: PASS. Transitive slice, INV-PLANT finite correction,
  consumer path, metrics, sibling ratchet, and WARN disposition accepted.
- Review B/verification: PASS after the whole-host visibility values were
  corrected. No unresolved finding remains.

Checkpoint: `MODULE-PASS`.
