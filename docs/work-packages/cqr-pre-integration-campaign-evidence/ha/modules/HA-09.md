# HA-09 — Hillslope CLI

Status: `MODULE-PASS`

## Identity And Classification

- Target: `crates/openwepp-runner/src/bin/openwepp-cli-hill.rs`.
- Start SHA-256: `b9a66c798c7f34efa98f6a60ce6a127b54ddf69175817567cc9cbf3475431420`.
- Final SHA-256: `dc53f564013f1acf5a7140a56008c10503e91ad2200ba27c047bc53c1919c617`.
- Lines: `161 -> 426`, below WARN.
- Tier: contract-bearing CLI orchestration; actionable row `run`, campaign
  CRAP `41.389`.

## Change And Obligations

The monolithic shell was decomposed into argv acquisition, parser state,
value extraction, runtime/default-selector guards, required-option
finalization, typed option assembly, execution, and warning publication.
Argument order, environment priority, exact error strings, help
short-circuiting, output paths, warning order, and process exit behavior are
unchanged.

| Family | Exact evidence |
| --- | --- |
| A — nominal | `parse_complete_request_preserves_paths_policy_and_runtime_selection` binds all required paths, policy, manifest, sidecar mode, runtime selection, and default activation. |
| B — boundary | `parse_help_short_circuits_and_value_flags_keep_exact_errors` binds both help spellings and every value-taking flag at end of argv. |
| C — branch/regime | The parser matrix covers default and explicit runtime/default activation, legacy discovery, policy, manifest, help, and unknown argument paths. |
| D — invalid domain | Invalid policy, unknown argument, missing values, and duplicate/conflicting selector states retain exact fail-closed errors. |
| E — missing/optional seam | Required run/output paths fail in their original order; optional manifest and legacy discovery remain independently selectable. |
| F — non-finite | Not applicable: this shell accepts no numeric input. |
| G — conservation | Not applicable: the shell performs no arithmetic or publication reconstruction. |
| H — typed fail-closed | Parser errors remain `Result<_, String>` at the existing CLI boundary; `main` prints once and exits `1`, while help prints and returns `0`. |

`execute_cli_runs_real_minimal_fixture_and_publishes_outputs` executes a typed
CLI request through the production runner and asserts the H5 pass, H5 loss,
manifest, and `R7C-DIRECT-PRODUCTION-EXECUTOR` consumer marker. The downstream
test `wshedw2_watershed_cli_generated_mode_accepts_relative_output_dir`
separately invokes the built hillslope binary through the watershed supervisor
and proves generated outputs are read from the requested relative output
directory; it passed `1/1` in `21.361s`.

## Focused Evidence

- `cargo nextest run -p openwepp-runner --bin openwepp-cli-hill` — `5/5`
  passed; the real fixture completed in `22.38s` under coverage.
- Real watershed consumer — `1/1` passed.
- `cargo clippy -p openwepp-runner --bin openwepp-cli-hill --tests -- -D
  warnings`, dedicated rustfmt, and diff checks — PASS.
- Shared binary profile: `cargo llvm-cov clean -p openwepp-runner`, then
  `cargo llvm-cov -p openwepp-runner --bin openwepp-cli-hill --no-report` —
  `5/5` passed.

| Artifact | Bytes | SHA-256 |
| --- | ---: | --- |
| `/tmp/openwepp-cqr-ha09-final2.lcov` | 468,858 | `c55ebbd5fe7cc51a300fb0a17d500937a01f59885b2bad7dfa5fb786d9b98a71` |
| `/tmp/openwepp-cqr-ha09-final2.json` | 5,232,254 | `9e864923a03dcbe987cc8a42a3f82bafd57f6992136473602ac09c30c4c980af` |
| `/tmp/openwepp-cqr-ha09-final2-crap.json` | 2,846,002 | `7869da9d1d59a669253aeedeefca74af9a461a39a8fb126fce9412660edc22cb` |

The original `run` row is eliminated. The largest replacement is
`parse_cli_args`, CC `20`, coverage `100%`, CRAP `20`; no target row exceeds
30. Whole-file binary-test visibility is lines `305/310` (`98.39%`), regions
`394/407` (`96.81%`), and functions `20/22` (`90.91%`). Every eligible
production function clears the 75% floor. `run_process` owns argv acquisition,
error display, and exit-code selection and is directly covered at 100% for
help/success `0` and exact-error `1`. `main` is now an exact
`X-DELEGATING-MAIN`: its single branch-free statement delegates the environment
iterator to `run_process` and applies its returned exit code. This exclusion
does not waive parsing, execution, publication, help, error, or consumer
behavior; those paths are directly covered and the real binary consumer
remains proven. Tranche-final workspace coverage remains authoritative.

## Review

Review A: PASS after the accepted executable-boundary coverage correction.
Review B: PASS after independently accepting the exact `X-DELEGATING-MAIN`
classification and covered process-entry helper. The invalid initial
`R-OBSERVABILITY` proposal was removed. No semantic defect was found during
characterization and no unresolved finding remains.

Checkpoint: `MODULE-PASS`.
