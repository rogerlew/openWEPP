# HA-05 — Active Publication Executor

Status: `MODULE-PASS`

## Identity And Classification

- Target: `crates/openwepp-hillslope-orchestrator/src/direct_runtime/03_executor.rs`
- Start SHA-256: `599ecb74ca4e54d4465b08d1df283f9c368ac0176c8f13428479eeaa765209ad`
- Final SHA-256: `bca7e5364f3f7dfc93229e34ba5f94813985c9b1b8d8e2dd335de535ec7e49c5`
- Tier: science/conservation/publication control flow.
- Classification: the `run_laned_active_publication_stream` row was eligible
  behavior-preserving decomposition work; CRAP was `58.160` at campaign start.
- Write set: the target module, a dedicated focused test module, test-module
  registration, and this record. The pre-existing 3,103-line test host is
  byte-restored and has no diff.

## Obligation Map

| Family | Binding behavior | Exact characterization |
| --- | --- | --- |
| A — nominal | Positive-source routing reaches the real row consumer before downstream transfer and commit. | `cqr_laned_active_positive_source_publication_routes_before_consumption` |
| B — boundary | Zero-source days publish an all-zero routed shape and retain trace/summary ownership. | `cqr_laned_active_zero_source_publication_commits_summary_and_trace` |
| C — branch/regime | Positive/zero source, active selector, topology, trace, and first/later-day carry branches are bound. | All three dedicated active tests; `cqr_active_selector_and_first_day_empty_layer_payloads_take_guarded_branches`; `cqr_publication_layer_inputs_cover_carry_forward_and_cardinality_guards` |
| D — invalid domain | Non-linear topology, invalid payload domains, layer cardinality, and downstream geometry reject before partial publication. | Topology test; `cqr_apply_publication_input_covers_optional_payloads_and_guard_priority`; layer and erosion-publisher tests. |
| E — missing/optional seam | Missing peak, continuity, and hourly sediment operands fail; optional publication, percolation, and subsurface payloads retain explicit absence/carry behavior. | `cqr_erosion_inflow_publisher_covers_errors_and_downstream_publication`; publication input and layer-input tests. |
| F — non-finite | Non-finite effective temperature, canopy fraction, and frost delta reject with field identity. | `cqr_apply_publication_input_covers_optional_payloads_and_guard_priority`; `cqr_day_execution_failure_preserves_context_and_runoff_diagnostics`. |
| G — conservation | Hydrology for all lanes precedes source/window and cascade; closure books pass before the consumer; terminal sediment concentration/class fractions alone feed downstream erosion. | Positive/zero-source active tests; erosion-publisher test; static whole-branch and operand-lineage reviews. |
| H — typed fail-closed | Selector/topology and day-execution errors preserve phase, lane/day, runoff, frozen-storage, and aggregate-storage diagnostics. | Selector/topology tests and both `cqr_day_execution_failure_*` tests. |

Named bindings are exact: `SC-OFEROUTE-001#INV-OFEROUTE-009` maps to the
positive-source cascade test; `INV-OFEROUTE-010` maps to the topology and
positive-source consumer tests; `INV-OFEROUTE-012` maps to both positive- and
zero-source active tests and the erosion-publisher seam test; and
`INV-OFEROUTE-013` maps to active-selector/config validation plus the mesh
policy tests in `laned_active`. `SC-RUNOFFPART-001#INV-RUNOFFPART-031` and
`SC-SED-001#REF-SED-DC01-SHAPE` map to the zero-source ownership test and the
DC01 double-feed guards exercised by both active regimes.
`SC-SED-001#REF-SED-LANED-ROUTED-HYDROGRAPH` and `INV-SED-013` map to the
positive-source consumer test and routed-shape authority assertions. Operand
lineage is unchanged: hourly routed outlet
volume and lane area produce the routed erosion shape; terminal sediment
concentration and class fractions produce downstream hourly `kg/m/s` inflow.
Rejected aliases are DC01 surface-transfer weights on active lanes, unrouted
source weights after positive routing, and producer-only summary counters. The
real row consumer observes routed authority and trace state before frame commit.

## Change And Focused Tests

The refactor extracts whole branches for topology validation, hydrology and
groundwater, source/window derivation, routed/zero-source lanes, and erosion/
row publication. Floating grouping, lane/day order, error mapping, closure
placement, consumer calls, and frame commits are unchanged.

Ran:

- Cover-first active tests: `2/2` passed before extraction.
- Post-extraction crate suite: `356/356` passed in `148.087s`.
- Dedicated active tests after split: `cargo nextest run -p
  openwepp-hillslope-orchestrator cqr_laned_active` — `3/3` passed.
- Focused Clippy: `cargo clippy -p openwepp-hillslope-orchestrator --tests --
  -D warnings` — PASS.
- `cargo fmt --check` and `git diff --check` — PASS.

## Same-Source Coverage And CRAP

Literal final commands:

    cargo llvm-cov clean -p openwepp-hillslope-orchestrator
    cargo llvm-cov -p openwepp-hillslope-orchestrator --lib --no-report
    cargo llvm-cov report -p openwepp-hillslope-orchestrator --lcov --output-path /tmp/openwepp-cqr-ha05-final2.lcov
    cargo llvm-cov report -p openwepp-hillslope-orchestrator --json --output-path /tmp/openwepp-cqr-ha05-final2.json
    cargo crap --workspace --lcov /tmp/openwepp-cqr-ha05-final2.lcov --min 0 --format json --output /tmp/openwepp-cqr-ha05-final2-crap.json

The shared execution took `313.45s` and ran `363/364`. The only failure was the
source-unchanged parallel audit-counter test
`r3c_lane_transfer_span_projects_multilane_topology`; its source SHA-256 is
`9117d2ff4e0a0d9ecc5f30ae1fe1dfd2aecee28574fbe3dea5aed034a9ddaf7c` and
has no package diff. The literal attribution rerun was:

    cargo nextest run -p openwepp-hillslope-orchestrator r3c_lane_transfer_span_projects_multilane_topology

Ran: `1/1` passed. The package did not change that test or its production span.

| Artifact | Bytes | SHA-256 |
| --- | ---: | --- |
| `/tmp/openwepp-cqr-ha05-final2.lcov` | 1,017,668 | `d339bba24fb9b6916ab540cf9fdeebc060f9f8d5cd650faec2a0674dc58c11c0` |
| `/tmp/openwepp-cqr-ha05-final2.json` | 5,307,482 | `c9b32e36ba2861afac4fa2425c371008f3a7246c0e6f5cba30a407575ab6cf3f` |
| `/tmp/openwepp-cqr-ha05-final2-crap.json` | 2,835,004 | `bfe9bb3687afcdb69e16cb983e3af5db79b8ca578e658a12868d4742b0da7749` |

Production-only coverage excludes the inline `#[cfg(test)]` module: lines
`1055/1123` (`93.945%`), regions `778/822` (`94.647%`), and minimum eligible
base-function region floor `17/20` (`85%`) for
`mark_laned_active_zero_source_lane`. The complete file profile, including
inline tests, is lines `1366/1465` (`93.242%`) and regions `1641/1814`
(`90.463%`). CRAP contains 32 unique production rows, zero above `30`; the
target is CC `13`, coverage `93.6937%`, CRAP `13.0424`.

## Line Count And Review Disposition

- Production module: `1274 -> 1769`, below WARN.
- Dedicated new test module: `236` lines.
- Test registration: `21 -> 22` lines.
- Oversized existing test host: unchanged at `3103` lines and absent from the
  final diff; review findings requiring removal of package additions from it
  are accepted and fixed.
- Review A: PASS. All behavior, metric-denominator, artifact, flaky-attribution,
  A–H/named-obligation, test-split, and line-governance findings were accepted,
  fixed, and cleared on final rereview.
- Review B: PASS. All evidence and line-governance findings were accepted,
  fixed, and cleared on final rereview; no semantic drift was found.

Checkpoint: `MODULE-PASS`.
