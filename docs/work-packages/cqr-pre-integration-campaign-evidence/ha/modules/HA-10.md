# HA-10 — Top-Level Runner Hillslope Command

Status: `MODULE-PASS`

## Identity And Slice

- Target: `crates/openwepp-runner/src/bin/open_wepp_runner.rs`.
- Start SHA-256: `bdb9c2ed5db4139d7ad0a0f986faf1b9a5968b6f0ac95a5861e250e907d1933e`.
- Final SHA-256: `8abbd7f3bad77e4c42f278281b715141e93946afc8a7c492b47affef2428a13b`.
- Lines: `345 -> 536`, below WARN.
- Tier: contract-bearing CLI orchestration; actionable row
  `run_hillslope_command`, campaign CRAP `132.000`.

This multi-responsibility binary contains top-level dispatch, hillslope launch,
release lint, release sidecar publication, role parsing, help, and inline tests.
The exact target slice is `run_hillslope_command`, `HillslopeCommandOptions`,
`HillslopeParseState::default`, `parse_hillslope_args`,
`missing_hillslope_argument`, `next_hillslope_value`,
`finalize_hillslope_options`, and `launch_hillslope_options`. It is the complete
transitive private-helper closure for hillslope parsing and launch; no release
or top-level command authority is needed to implement the fixed target row.

## Change And Obligations

The target now delegates through typed parse state, shared value extraction,
typed missing-argument construction, required-option finalization, request
assembly, and the existing `launch_hillslope` consumer. Exact error priority
and text, policy mapping, optional argument order, and child exit propagation
are unchanged.

| Family | Exact evidence |
| --- | --- |
| A — nominal | `hillslope_parse_preserves_complete_request` binds every required and optional field; the real-child test binds nominal launch. |
| B — boundary | `hillslope_parse_preserves_value_and_required_guard_priority` covers all six value-taking flags at argv end and the empty request. |
| C — branch/regime | Complete parsing covers compat policy, legacy discovery, manifest present, while the guard matrix covers their absent/default paths. |
| D — invalid domain | Invalid policy and unknown argument retain exact `RUNNER-E-001` displays. |
| E — missing/optional seam | Four required fields fail in their original order; optional manifest and legacy discovery remain independent. |
| F — non-finite | Not applicable: the command accepts no numeric input. |
| G — conservation | Not applicable: the command performs no arithmetic or publication reconstruction. |
| H — typed fail-closed | Every parser failure remains `RunnerError::MissingArgument`; launch still returns the existing typed child/process failure. |

`hillslope_command_launches_real_consumer_with_exact_argv` launches a real
executable child, captures its argv, and proves the exact run-dir, run-file,
output-dir, policy, legacy-discovery, and manifest sequence read by the
downstream hillslope consumer. This is not a parser-only or shadow consumer.

## Focused Evidence

- `cargo nextest run -p openwepp-runner --bin open_wepp_runner` — `6/6`
  passed.
- Focused binary Clippy, rustfmt, and diff checks — PASS.
- Shared binary coverage profile — `6/6` passed.

| Artifact | Bytes | SHA-256 |
| --- | ---: | --- |
| `/tmp/openwepp-cqr-ha10-final.lcov` | 470,576 | `ec8ca68406c8aeaccf6bcf731d478cf5753a8abe81d0d7ccd5a13441e6bbea9d` |
| `/tmp/openwepp-cqr-ha10-final.json` | 5,238,063 | `8953394bf8e75bb8bb22176112ea9ca8e79c05282ed655b6a54735c85a9f0094` |
| `/tmp/openwepp-cqr-ha10-final-crap.json` | 2,838,000 | `aa67bb5d413fac5005a7ad43c83ac66269fba1f177f99a9785067722d7de912d` |

The complete target slice has 100% line and region coverage and every base
function has 100% coverage. Target CRAP is `2`; parser CRAP `17`; finalizer
CRAP `5`; all remaining slice helpers CRAP `1`. No file row exceeds 30.

Whole-host focused visibility is lines `320/399` (`80.20%`), regions
`442/528` (`83.71%`), and functions `25/31` (`80.65%`). The uncovered production
begins with the unrelated top-level/release-command responsibility after the
target slice. The binding multi-responsibility-host target-slice rule applies;
the tranche-final whole-workspace profile and non-target ratchet remain
authoritative, and this classification waives no target, launch, or nominal
consumer behavior.

## Review

Review A: PASS after correcting the initial target-region metric and directly
covering parse-error propagation through the target boundary. Review B: PASS,
independently accepting the multi-responsibility slice and production control-
flow preservation. No semantic defect or unresolved finding remains.

Checkpoint: `MODULE-PASS`.
