# HA-08 — Runner Execution And Active Trace Output

Status: `MODULE-PASS`

## Identity And Slice

- Target: `crates/openwepp-runner/src/hillslope/05_runner_execution_and_outputs.rs`
- Start SHA-256: `fed50432996ca30fa7eab1251194490b07be85e3a47fec00468683d0b1cfc496`.
- Final SHA-256: `6c949eecd14514e0cdfbfb1eb2ca62e46e6b5d32b12c90e5adeaa71f53830daa`.
- Lines: `1590 -> 1888`, below WARN.
- Actionable rows: `execute_direct_publication_stream` CRAP `44.141` and
  `write_laned_active_trace_output` CRAP `62.279`.

The proven multi-responsibility host owns execution, groundwater, output gates,
checksums, manifests, and trace serialization. Final whole-host focused
visibility is lines `1353/1746` (`77.491%`) and regions `1494/1901`
(`78.590%`); unrelated
siblings remain undispositioned and tranche-final ratchet governed.

Exact transitive slices are:

- execution: selector/configuration decision helpers, environment wrapper,
  frame-application helper, `configure_laned_active_execution`,
  `execute_direct_publication_stream`, and active-summary validator;
- trace: `validated_laned_active_trace_records`, step/detail serializers,
  row serializer, numeric error/finite/nonnegative/positive helpers,
  limiter/step/detail/record validators, and
  `write_laned_active_trace_output`.

Production base-function regions across both slices are `428/457` (`93.654%`).
Every base function meets the 75% floor; execute is the minimum at `42/56`
(`75%`). Pure selector/configuration and frame-application helpers are directly
table-tested, while trace validation/writer/serializer helpers exceed their
floors. Unique source-line coverage remains visibility evidence. The remaining
execution lines are mutually exclusive shadow/profile diagnostic modes; the
binding orchestration-slice rule applies with guard coverage, a completed real
nominal consumer, bounded real-mode attempts, and dual review.

Final CRAP: execute CC `10`, coverage `54.430%`, CRAP `19.463`; writer CC `5`,
coverage `83.333%`, CRAP `5.116`; every other slice row is below 30.

## Obligations And Consumer Evidence

| Family | Exact evidence |
| --- | --- |
| A — nominal | `cqr_execute_direct_publication_stream_runs_real_fixture_consumer` proves production entry, stream sink, PASS/LOSS, and manifest output. |
| B — boundary | Trace writer test covers disabled path, empty detail, zero/one-based indices, and ordered newline termination. |
| C — branch/regime | Selector subprocesses cover active/disable conflict, disable-only, and active/shadow conflict; trace tests cover detail present/absent and nested step records. |
| D — invalid domain | Writer test covers row-count mismatch and malformed/missing trace artifacts. |
| E — missing/optional seam | Missing active summary, trace records, direct-publication artifacts, and optional output path are bound. |
| F — non-finite | `cqr_laned_active_trace_writer_rejects_invalid_numeric_values_without_output` binds row/detail/step/limiter/TVD NaN/infinity to exact typed fields before file creation. |
| G — conservation | The same test binds finite non-negative weights, zero-source all-zero shape, positive-source unit sum at canonical `1e-9`, and no output on violation. |
| H — typed fail-closed | Selector conflicts and trace/artifact mismatches return existing typed runner errors before file publication. |

The real consumer path is `execute_hillslope_direct_production_days` →
`execute_direct_publication_stream` → `DirectPublicationStreamingSink` →
PASS/LOSS/manifest, followed by optional trace JSONL publication. JSON key
names, nesting, index bases, order, null detail, limiter/TVD values, and newline
behavior are asserted exactly.

Named authority mapping: `SC-OFEROUTE-001#INV-OFEROUTE-009/010/012` binds the
selector/configuration table and real execute-stream consumer;
`INV-OFEROUTE-008/012` and `SC-SED-001#INV-SED-013` bind the finite,
non-negative, zero-or-unit routed-weight validation tests; and
`INV-OFEROUTE-013` binds trace row emission, row-count/artifact failure, exact
schema serialization, and output checksum integration.

## Bounded Diagnostic Modes

- Active+profile with an in-memory copied H2637 fixture and all 19 native
  routing-coefficient blocks passed intake/authority construction but did not
  complete within `325.499s`; literal command `cargo nextest run -p
  openwepp-runner cqr_laned_active_selectors_cover_disable_conflict_shadow_and_profile_paths`
  was interrupted once by SIGINT, nextest reported SIGINT, and the command
  exited `100`; no retry.
- Shadow-only with the same authoritative in-memory fixture passed intake but
  exceeded the single `120s` cutoff (`116.288s` test runtime, exit `124`); no
  runtime failure was emitted and no retry occurred.
- Quick selector/config conflict and disable paths pass in isolated subprocesses.
  `Command`-scoped environments are required because Rust 2024 environment
  mutation is unsafe and the runner forbids unsafe code.
- Literal shadow command: `timeout 120s env CQR_HA08_SELECTOR_CASE=shadow_only
  OPENWEPP_LANED_ACTIVE_DISABLE=1 OPENWEPP_LANED_SHADOW=1 cargo nextest run -p
  openwepp-runner cqr_selector_subprocess_probe`.
- Fixture tree digest:
  `25a180cc40808f021c3418103423256666f469b4894a0c575b3aaab9f21ce78c`;
  `p2637.man` SHA-256:
  `732b464650c71792689bebc6897fe6bfc0c1df6c58064e42ee82fd152a50b189`.
- Exact execution-slice uncovered production bodies after focused coverage are
  the shadow collector construction/observation/finalization and active profile
  stderr report. Active config attachment and missing-summary fail-closed are
  separately covered without routing; trace/schema/conservation lines clear the
  ordinary science line threshold (`324/343`, `94.461%`).

## Focused Metrics

- Shared profile: `cargo llvm-cov clean -p openwepp-runner`; `cargo llvm-cov
  -p openwepp-runner --lib --no-report` — `120/120` passed in `94.71s`.
- LCOV `/tmp/openwepp-cqr-ha08-final7.lcov`: 543,246 bytes,
  `4ec7437f90a9cbc83eb9901accdff23fbb3d75cfd25cf4d698b70e54634df00e`.
- JSON `/tmp/openwepp-cqr-ha08-final7.json`: 5,688,683 bytes,
  `abe9d49d2007d1371f72d2707b70e35503f1a55dbd6513308370c2dbcf7aef63`.
- CRAP `/tmp/openwepp-cqr-ha08-final7-crap.json`: 2,845,738 bytes,
  `159075d641b099edc48754b20ba0f0f551b2503b30bce4d370997852926fabf7`.
- `cargo nextest run -p openwepp-runner cqr_laned_active` — `12/12` passed;
  focused runner Clippy, dedicated rustfmt, and diff check PASS.
- Dedicated test module: 808 lines; registration host receives only three
  lines and remains 1,924 lines.
- Focused `markdown-doc lint` over the contract, High-A plan, HA-08 record, and
  DC package scanned six files with zero errors/warnings.

## Review

Review/verification A: PASS. Review/verification B: PASS after correcting the
signed delta-storage domain and final metric/timeout record. All findings were
accepted, fixed, and cleared; no unresolved finding remains.

Checkpoint: `MODULE-PASS`.
