# M-09 Release Sidecar Validation

Status: `MODULE-PASS`

Evidence class: **Ran + Static**

## Scope And Classification

- Before source SHA-256: `0c35a556225f406d9ad76f4d5516ad97a9ff28cd0586b11078cce14d2db3f108`.
- After source SHA-256: `41fd4a9ac3f176b3b4e5e5443dc203c75c00c2517a574293dc72c6c62fedf292`.
- Tier: glue; exact target is `validate_release_sidecar_unlocked`, exercised
  through its public locking wrapper and release lint/write consumers.
- Source line count: 806 before, 863 after; below the 2,000-line warning.
- Focused write set: inline tests, durable metrics, this record, and M-09
  Medium progress only.
- Required reading: root/crate/test/work-package guidance, ADR-0021, CQR guide,
  Medium plan, binding campaign contract, release/manifest/lint authority and
  tests, Review A, and the accepted ledger.

Review A and the accepted ledger classify the target as `E-PRODUCTION` because
release authority must validate fail-closed and in stable order. The start row
was CC 19, 67.442% regions, and CRAP 31.459.

## Cover-First Result

One public-boundary characterization test was sufficient; production code was
not changed.

| Obligation | Binding |
| --- | --- |
| I/O failure | missing sidecar returns typed `Io` with the exact path |
| JSON failure | malformed payload returns typed `JsonParse` with the exact path |
| schema priority | missing schema precedes role; wrong schema precedes invalid role |
| role priority | valid schema plus invalid role returns `binary_role` failure |
| field anti-alias | valid schema/role then missing `binary_name` names that exact field |
| real consumers | sidecar write/reuse/rewrite, directory lint, and real hillslope manifest publication |

Final target metrics are 100.000% regions, CC 19, and CRAP 19.000. File/slice
coverage is 564/640 lines, 88.125%, and 919/1,031 regions, 89.137%, exceeding
the 85% glue threshold. The target exceeds the 75% floor and is below CRAP 30.

Durable artifacts are `M-09-evidence/focused.lcov`,
`M-09-evidence/focused.json`, and `M-09-evidence/focused-crap.json`. Their
SHA-256 values are respectively
`655340c82edafc59030f391d6bf6bcc09ce859b89d08866fd4e596a5b426fd71`,
`ac412cc24829b90d3273efefdd9770657a2e08c1aaba0a39c75dc4d2213a9729`,
and `772986167c497841207d01b242e244ed25f021bf0dffa719ed3047dfb72217ac`.

Exact metric commands were:

```text
cargo llvm-cov clean --workspace
cargo llvm-cov -p openwepp-runner --lib --lcov --output-path .../focused.lcov
cargo llvm-cov report -p openwepp-runner --json --output-path .../focused.json
cargo crap --path crates/openwepp-runner --lcov .../focused.lcov --format json --output .../focused-crap.json
```

## Gates And Review

| Gate | Result |
| --- | --- |
| focused sidecar/write/lint filter | PASS, 9/9 |
| complete runner library cohort under coverage | PASS, 123/123 |
| focused LCOV and CRAP | PASS, metrics above |
| `cargo clippy -p openwepp-runner --lib -- -D warnings` | PASS |
| `cargo clippy -p openwepp-runner --all-targets -- -D warnings` | PASS after the M-08 test-only lint annotation at `43289099` |
| manifest consumer `cli01_contract_conformance_hillslope_run_emits_required_outputs_and_manifest` | PASS, 1/1 |
| `cargo fmt --check`; `git diff --check` | PASS |
| exact campaign `markdown-doc lint` command | PASS |

The named consumers write and reuse real sidecars, rewrite stale metadata, lint
valid releases, and reject missing, malformed, mismatched, and HBP-incoherent
release sets. Production and serialized fields are unchanged, so output
equivalence is exact by identity.

The archived `M-09-evidence/manifest-consumer.log` proves the real hillslope
workflow invokes manifest publication, emits required outputs, and records a
readable generated release sidecar path.

Independent review verified code/tests/metrics and raised R1 compact fields/docs
and R2 manifest-consumer evidence. Both were accepted and fixed. No finding
remains. Checkpoint commit: `4ba55de9`.
