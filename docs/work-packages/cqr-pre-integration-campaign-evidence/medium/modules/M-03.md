# M-03 Snow Parser

Status: `MODULE-PASS`

Evidence class: **Ran + Static**

## Scope And Classification

- Before source SHA-256: `2b6fb627073353a94e32e997d3d1d7f327eaf3fe02afac4f3b264a957de2e023`.
- After source SHA-256: `a1c55864f94064b5cb814ef090622c99cbdb92886845bb044579ca071ca7dd27`.
- Tier: glue; the accepted execution surface is the snow parser and its private
  helpers.
- Source line count: 478 before, 715 after; below the 2,000-line warning.
- Required reading: root/crate/test/work-package `AGENTS.md`, ADR-0021, the CQR
  guide, Medium plan, binding campaign contract, and selection reviews.
- Focused write set: `snow.rs`, this record, durable focused metric artifacts,
  and Medium progress only.

Review A proposed observation-only treatment for `SnowParseError::fmt`, but
Review B rejected it because stable error text is externally consumed. The
binding disagreement rule therefore classifies both live rows as
`E-PRODUCTION`: `SnowParseError::fmt` at CC 10, 0% regions, CRAP 110.000 and
`enforce_invariants` at CC 10, 39.394% regions, CRAP 32.261.

## Cover-First Result

Five private characterization tests were sufficient; production code was not
changed. They bind all nine error display and contract-ID arms, `Error::source`
for I/O and non-I/O variants, every warning code and emitted message, all five
invariant branches, exact combined-invalid first-error priority, finite/range
guard order, and token/prefix helper boundaries.

| Obligation | Binding |
| --- | --- |
| nominal strict/compat parsing | existing `infile_snow_parser_contract` suite |
| deterministic error/warning identity | exhaustive error/source and warning tests |
| missing/default state | parser suite plus private invariant test |
| finite/range rejection | `finite_and_range_error_order_is_exact` |
| missing/token/prefix seam | `private_token_and_prefix_boundaries_are_characterized` |
| first-error priority | combined missing/default plus prefix-invalid case |
| typed I/O/source behavior | exhaustive error/source test |
| downstream consumer | runner `simimpl04_contract_requires_wepp_ui_requested_effective_lane_closure_manifest` |

Final focused metrics:

| Function | Region % | CC | CRAP |
| --- | ---: | ---: | ---: |
| `SnowParseError::fmt` | 100.000 | 10 | 10.000 |
| `SnowParseError::contract_error_id` | 100.000 | 10 | 10.000 |
| `SnowParseError::source` | 100.000 | 3 | 3.000 |
| `enforce_invariants` | 100.000 | 10 | 10.000 |
| `parse_snow_from_str` | 76.056 | 15 | 18.089 |
| `parse_canonical_scalar` | 86.842 | 5 | 5.057 |
| `detect_prefix_variant` | 100.000 | 5 | 5.000 |
| `parse_first_token_f64` | 100.000 | 2 | 2.000 |

All live targets and covered transitive helpers exceed 75% regions and remain
below CRAP 30. Focused file coverage is 436/476 lines, 91.597%, and 478/520
regions, 91.923%, exceeding the 85% glue threshold.

Artifact SHA-256 values are
`652c35b843a509863ff1b8ea523db48e68ff6aeae5242054d2a4f72fc63c725f`
for LCOV, `22baa23c6fb03126101d1509e6a03b194a20a4ebb566902ac0b1b2f83bc172c6`
for LLVM JSON, and
`9211a7f8238731dfe5122faf24e76edb731ecb4fd840a13c16942fec06e35bbe`
for CRAP JSON.

Durable artifacts are `M-03-evidence/focused.lcov`,
`M-03-evidence/focused.json`, and `M-03-evidence/focused-crap.json`; their
hashes exactly match the values above. Exact focused commands were:

```text
cargo llvm-cov clean -p openwepp-input-contract
cargo llvm-cov -p openwepp-input-contract --lib --lcov --output-path .../focused.lcov
cargo llvm-cov -p openwepp-input-contract --lib --json --output-path .../focused.json
cargo crap --path crates/openwepp-input-contract --lcov .../focused.lcov --format json --output .../focused-crap.json
```

## Gates And Review

| Gate | Result |
| --- | --- |
| `cargo nextest run -p openwepp-input-contract --lib` | PASS, 31/31 |
| `cargo nextest run --test infile_snow_parser_contract` | PASS, 12/12 |
| focused runner manifest consumer | PASS, 1/1 |
| focused LCOV and CRAP | PASS, metrics above |
| `cargo clippy -p openwepp-input-contract --all-targets -- -D warnings` | PASS |
| `cargo fmt --all -- --check`; `git diff --check` | PASS |

The unchanged runner consumer
`simimpl04_contract_requires_wepp_ui_requested_effective_lane_closure_manifest`
executed the hillslope workflow and verified the published
`snow_file_present` coupling vector. Since production code and numeric
expressions are unchanged, numeric/output equivalence is exact by identity.

Independent review passed the implementation and reproduced focused tests, but
raised R1 (missing durable metric paths) and R2 (missing reading, write-set,
obligation, and command fields). Both findings were accepted and fixed above.
The nonblocking wording correction now describes the single combined-invalid
priority case precisely. No finding remains; checkpoint commit is the commit
containing the tests, evidence, and record.
