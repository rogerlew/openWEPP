# M-13 Landuse Migration CLI Dispatch

Status: `MODULE-PASS`

Evidence class: **Ran + Static**

## Scope And Classification

- Before CLI SHA-256: `2e5584e8ff63f2efd8db0521148ef8625184256f89f7721955c792976cda86c2`.
- After CLI SHA-256: `00a83578f37071e222701886d02d8719d00e6e9da1c3cf8ca2fa1d72ffecb24b`.
- Before integration-test SHA-256: `c17afa373325eb9bcde819f67d3cc1c1f9862e20a93822fc4dfd8bb283b89b33`.
- After integration-test SHA-256: `8fb1f950efb240fb69921eabd3c1e438c22482d4f5503010dec9b7a8a583038e`.
- Tier: production CLI dispatch, output, and filesystem behavior.
- Line counts: CLI 231 before / 329 after; integration tests 528 before / 735
  after. Both remain below the 2,000-line warning.
- Focused write set: CLI mechanical decomposition, CLI integration tests,
  durable metrics, this record, and M-13 Medium progress.
- Required reading: root/crate/test/work-package guidance, ADR-0021 and CQR
  procedure, Medium plan, binding campaign contract, accepted ledger, and
  target-selection Review A, and CLI/output/schema authority and binary tests.

Review A and the accepted ledger classify `run_cli_args` as `E-PRODUCTION`:
option order controls first-error identity, authority precedence, selected
mode, stdout format, report writes, and migration output effects. The start row
was CC 54, 78.146% regions, and CRAP 84.437.

## Cover-First Characterization

Before production decomposition, real-binary tests locked:

- exact empty, `--help`, and `-h` stdout with empty stderr;
- missing-value errors for all nine value-taking options;
- discovery incompatibility with validate, target, or output;
- args-file target inheritance and explicit-target precedence;
- native-YAML validation defaulting to `latest` while flat validation requires
  `--to`;
- migration-mode missing-target behavior;
- unknown-option, multiple-input, and missing-input first-error priority;
- independently formatted validation stdout and report-file content;
- dry-run non-publication, real output creation, overwrite rejection, and
  native-YAML pass-through.

All seven external cover-first tests passed against the original monolithic
dispatcher in a detached `d8069ee7` worktree. The durable log records the
original source hash, patched test hash, commands, run ID, and 7/7 result.

## Mechanical Decomposition

`run_cli_args` now delegates without changing the prior order:

```text
empty/help -> parse_options -> require input -> discovery
           -> parse_authority/target inheritance
           -> validation or migration
```

`ParsedCliArgs` carries the same mutable option state. The extracted helpers
retain the original statements and ordering: `parse_options`, `run_discovery`,
`parse_authority`, `run_validation`, and `run_migration`. No option, message,
format, target rule, authority merge, report write, migration call, output
path, or overwrite policy changed.

| Function | CC | Regions | CRAP |
| --- | ---: | ---: | ---: |
| `run_cli_args` | 8 | 100.000% | 8.000 |
| `parse_options` | 30 | 100.000% | 30.000 |
| `run_discovery` | 5 | 100.000% | 5.000 |
| `parse_authority` | 9 | 100.000% | 9.000 |
| `run_validation` | 6 | 88.000% | 6.062 |
| `run_migration` | 4 | 89.655% | 4.018 |
| `validate_target_for_input` | 3 | 100.000% | 3.000 |

Every parent/helper is above the 75% function floor and at or below CRAP 30.
The complete CLI glue slice is 94.118% functions, 97.244% lines, and 96.114%
regions, above the required 85% slice threshold.

Durable artifacts are `M-13-evidence/focused.lcov`,
`M-13-evidence/focused.json`, and `M-13-evidence/focused-crap.json`. Their
SHA-256 values are respectively
`3f49d220938e4c68515811e28303a5ad97715c878c8a3d58e20f6fe3be74ba2a`,
`a66741ceb1cbf2e7d545d167409a0a73dea81f5523e3003cdde6e6d1e4e15738`,
and `99220f5993dd3991a7156c5ecf14539209dfbf6b8041cfd3fc3697535f719d6b`.

Exact metric commands were:

```text
cargo llvm-cov clean --workspace
cargo llvm-cov nextest -p openwepp-landuse-migrate --lcov --output-path /tmp/m13-final.lcov
cargo llvm-cov report -p openwepp-landuse-migrate --json --output-path /tmp/m13-final.json
cargo crap --path crates/openwepp-landuse-migrate --lcov /tmp/m13-final.lcov --format json --output /tmp/m13-final-crap.json
```

## Gates And Review

| Gate | Result |
| --- | --- |
| M-13 final error/precedence cohort | PASS, 8/8: 7 binary + 1 unit |
| named output/overwrite/report cohort | PASS, 12/12: 11 binary + 1 unit; 14 skipped |
| complete crate cohort under coverage | PASS, 26/26 |
| focused LCOV and CRAP | PASS, metrics above |
| `cargo clippy -p openwepp-landuse-migrate --all-targets -- -D warnings` | PASS |
| `cargo fmt --all -- --check`; `git diff --check` | PASS |
| pre-decomposition detached characterization | PASS, 7/7 real binary |
| exact campaign `markdown-doc lint` | PASS |

The literal named consumer command was:

```text
cargo nextest run -p openwepp-landuse-migrate -E 'test(/m13_/) | test(/legacy_global_class_writes_default_man_yaml/) | test(/overwrite_without_force_fails_closed/) | test(/dry_run_writes_report_but_no_output_yaml/) | test(/native_yaml_latest_passthrough_preserves_coefficients/)'
```

Independent review verified mechanical ordering, behavior locks, metrics,
hashes, and line governance. R1 documentation/terminal fields, R2 durable
pre-decomposition sequencing proof, and R3 accurate binary/unit counts were
accepted and fixed. No finding remains. Checkpoint commit: `69822725`.
