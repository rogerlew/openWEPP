# M-11 Landuse Migration Errors And File Authority

Status: `MODULE-PASS`

Evidence class: **Ran + Static**

## Scope And Classification

- Before source SHA-256: `d0efe206122f84370183ef882288179b96f2b647caec0ad701a78b56a22937f6`.
- After source SHA-256: `c2a5974d88969125d1c75ae580ff73a4a3e4812917b9b256d2085bca3ef42d5b`.
- Tier: production-observable error and migration-authority behavior.
- Source line count: 1,266 before, 1,565 after; below the 2,000-line warning.
- Focused write set: inline tests, durable metrics, this record, and M-11
  Medium progress only. Production code is unchanged.
- Required reading: root/crate/test/work-package guidance, ADR-0021 and CQR
  procedure, Medium plan, binding campaign contract, accepted ledger, and
  target-selection Review A, and landuse public-API/CLI authority tests.

The accepted ledger classifies both live rows as `E-PRODUCTION`.
`LanduseMigrationError::fmt` publishes stable `LANDUSE-MIGRATE-E-001..016`
diagnostics, and `authority_from_files` defines public merge order and
fail-closed conflict behavior. Start metrics were respectively CC 17, 44.776%
regions, CRAP 65.672 and CC 7, 0%, CRAP 56.

## Tests-Only Result

| Obligation | Binding |
| --- | --- |
| all public errors | exact full strings for all 16 variants, including paths, quoting, joins, and guidance |
| error source | only `Io` exposes its underlying error; all other variants return no source |
| schema conversion | `ManagementYamlError` becomes E-002 without losing the nested MAN-YAML identity |
| no-file authority | public helper returns the initial authority unchanged |
| merge precedence | initial CLI authority -> args file -> class-map file |
| agreeing overlays | global, plant-index, and OFE-index authority survive exact merges |
| conflict priority | initial/global conflict precedes class-map reading; args-map conflict retains ordered classes |
| malformed priority | args-file I/O/parse failures precede class-map parsing; valid args expose malformed class map |
| real CLI agreement | positive global migration, partial-map rejection, and args/map conflict consumers pass |

`authority_from_files` is a public helper with no production call site in the
current repository. The CLI independently performs the same load and merge
sequence with `load_args_file`, `MigrationAuthority::merge_from_args_file`,
`load_class_map`, and `ClassMap::merge_checked`. Therefore helper tests prove
its public contract, while the named executable CLI consumers independently
prove agreement on production behavior; this record does not claim that the
CLI calls the helper.

| Function | CC | Regions | CRAP |
| --- | ---: | ---: | ---: |
| `LanduseMigrationError::fmt` | 17 | 100.000% | 17.000 |
| `authority_from_files` | 7 | 100.000% | 7.000 |

Both targets exceed the 85% accepted-slice threshold and function floor, and
both are below CRAP 30. For context only, the entire mixed-purpose 1,565-line
file is 75.823% lines and 69.356% regions because it also owns unrelated source
inspection, migration conversion, validation, reporting, and formatting paths;
those functions are outside M-11's accepted slice.

Durable artifacts are `M-11-evidence/focused.lcov`,
`M-11-evidence/focused.json`, and `M-11-evidence/focused-crap.json`. Their
SHA-256 values are respectively
`848b4698fd6ff9ed60889d86562ee873cf7bc95b7b1a919a9c766b89795443a7`,
`75c38866eb018a6427ba4c6d9889363d4e25f5178e011a89196b43634b03ad8e`,
and `8ff0d388e4301cdfefa22768b817363447a3b24b960e6b43c5ee6eba1c39c264`.

Exact metric commands were:

```text
cargo llvm-cov clean --workspace
cargo llvm-cov nextest -p openwepp-landuse-migrate --lcov --output-path /tmp/m11-final.lcov
cargo llvm-cov report -p openwepp-landuse-migrate --json --output-path /tmp/m11-final.json
cargo crap --path crates/openwepp-landuse-migrate --lcov /tmp/m11-final.lcov --format json --output /tmp/m11-final-crap.json
```

## Gates And Review

| Gate | Result |
| --- | --- |
| exhaustive M-11 unit characterization | PASS, 4/4 |
| named helper plus real CLI agreement cohort | PASS, 7/7; 9 skipped |
| complete crate cohort under coverage | PASS, 16/16 |
| focused LCOV and CRAP | PASS, metrics above |
| `cargo clippy -p openwepp-landuse-migrate --all-targets -- -D warnings` | PASS |
| `cargo fmt --all -- --check`; `git diff --check` | PASS |
| exact campaign `markdown-doc lint` | PASS, 61 files |

The literal named consumer command was:

```text
cargo nextest run -p openwepp-landuse-migrate -E 'test(/m11_/) | test(/conflicting_args_file_and_class_map_fails_closed/) | test(/partial_class_map_fails_closed/) | test(/legacy_global_class_writes_default_man_yaml/)'
```

Independent review verified the tests-only implementation, exhaustive error
identity, public helper order, CLI agreement limitation, metrics, hashes, and
line governance. R1 documentation nondeferral and R2 review/checkpoint fields
were accepted and fixed. No finding remains. Checkpoint commit: `d8bca733`.
