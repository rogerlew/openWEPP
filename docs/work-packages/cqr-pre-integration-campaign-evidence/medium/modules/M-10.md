# M-10 Iwagaki Characteristics Oracle

Status: `MODULE-PASS`

Evidence class: **Ran + Static**

## Scope And Classification

- Before source SHA-256: `6a6a18163e1c90105b461f6ff4e56f1c49537cb42bd0232d4a35e98787447724`.
- After source SHA-256: `8af3cc808ebaa0696a8fd8e713906e00a05cec105d970cbdd207d1d3b0296f8d`.
- Tier: science-sensitive; the accepted slice is `run_oracle`, extracted
  `final_storage_m2`, and the named oracle/upwind/D10B consumers.
- Source line count: 708 before, 777 after; below the 2,000-line warning.
- Focused write set: the target module's inline tests and mechanical helper,
  durable metrics, this record, and M-10 Medium progress only.
- Required reading: root/crate/test/work-package and science-contract guidance,
  ADR-0021, CQR guide, Medium plan, binding campaign contract, accepted ledger
  and Review B, and `SC-OFEROUTE-001` rev 47 with `INV-OFEROUTE-011` and the
  Iwagaki-primary oracle authority.

Review B and the accepted ledger classify `run_oracle` as `E-SCIENCE` because
its floating-point statement grouping determines the validation oracle's
hydrograph, storage, residual, and headline metrics. The start row was CC 30,
95.313% regions, and CRAP 30.093.

## Behavior-Preserving Result

Cover-first execution confirmed that the existing Case-4 consumer already
exercised the target broadly but did not reduce its complexity. The final-fan
storage block was therefore extracted verbatim into `final_storage_m2`.
Expression grouping, statement order, iteration order, shock averaging, and
tail booking are unchanged. No equation, threshold, guard, public type, or
serialized surface changed, and no science-contract amendment was required.

Before the production extraction, the Case-4 result was captured using every
hydrograph sample's `time_s.to_bits()` and `q_m2_s.to_bits()` in order. The
locked signature is:

```text
hydrograph_len=1031
hydrograph_bit_fingerprint=2273141013813331758
peak_bits=4575933481581572714
time_to_peak_bits=4627596110010980260
rise_10_90_bits=4626227002420352667
mass_residual_bits=13782961209799527883
shocks_tracked=1
```

The same lock passes after extraction. A separate geometry test covers empty,
particle-first, mixed particle/shock, and shock-first storage while retaining
exact `f64::to_bits()` results.

| Function | CC | Regions | CRAP |
| --- | ---: | ---: | ---: |
| `run_oracle` | 27 | 96.089% | 27.044 |
| `final_storage_m2` | 4 | 100.000% | 4.000 |

The parent and extracted helper both exceed the 75% function floor and remain
below CRAP 30. The complete `iwagaki_oracle.rs` science slice is 95.492% lines
and 96.134% regions, above the 90% threshold; all 27 functions were executed.

Durable artifacts are `M-10-evidence/focused.lcov`,
`M-10-evidence/focused.json`, and `M-10-evidence/focused-crap.json`. Their
SHA-256 values are respectively
`f9ec23369cf2521c9594987ed254f6a11af05f9440eb344f75c96c4783b1b498`,
`5774e584013b501a2a8a2778e4cf34d9c3f542eb39aade63fd2d8d380759aee1`,
and `ad1baa2577523e42fd2790eace680a6ca8011e8f61e977b19c62d4c40d53a418`.

Exact metric commands were:

```text
cargo llvm-cov clean --workspace
cargo llvm-cov nextest -p openwepp-hillslope-orchestrator --lib --lcov --output-path /tmp/m10-final.lcov -E 'test(/iwagaki_oracle::tests::/) | test(/d10b_reconciliation_tests::/)'
cargo llvm-cov report -p openwepp-hillslope-orchestrator --json --output-path /tmp/m10-final.json
cargo crap --path crates/openwepp-hillslope-orchestrator --lcov /tmp/m10-final.lcov --format json --output /tmp/m10-final-crap.json
cp /tmp/m10-final.lcov docs/work-packages/cqr-pre-integration-campaign-evidence/medium/modules/M-10-evidence/focused.lcov
cp /tmp/m10-final.json docs/work-packages/cqr-pre-integration-campaign-evidence/medium/modules/M-10-evidence/focused.json
cp /tmp/m10-final-crap.json docs/work-packages/cqr-pre-integration-campaign-evidence/medium/modules/M-10-evidence/focused-crap.json
```

## Gates And Review

| Gate | Result |
| --- | --- |
| named oracle/upwind/D10B consumer cohort | PASS, 17/17; 384 skipped |
| focused LCOV and CRAP | PASS, metrics above |
| pre/post Case-4 bitwise signature | PASS, exact lock above |
| `cargo clippy -p openwepp-hillslope-orchestrator --all-targets -- -D warnings` | PASS |
| `cargo fmt --all -- --check`; `git diff --check` | PASS |
| exact campaign `markdown-doc lint` | PASS |

A complete orchestrator library coverage diagnostic ran 401 tests: 399 passed
and two unrelated direct-runtime R3C/R4B assertions failed on preexisting
lane-count expectations. The required focused science cohort is clean; no M-10
oracle test failed, and the diagnostic failure does not alter focused evidence.
Exact IDs, assertion locations, observed/expected values, commands, and
attribution are archived in `M-10-evidence/full-lib-diagnostic.log`. The
ordinary same-source quick checkpoint passed, and neither failing test calls
the target or extracted helper.

Independent review verified the verbatim arithmetic/order extraction, bitwise
lock, science metrics, hashes, and line governance. R1 named consumer
provenance, R2 diagnostic attribution, R3 documentation gate, R4 compact
reading/copy provenance, and R5 terminal fields were accepted and fixed. No
finding remains. Checkpoint commit: the commit containing implementation,
tests, evidence, record, and Medium progress; SHA pending commit creation.
