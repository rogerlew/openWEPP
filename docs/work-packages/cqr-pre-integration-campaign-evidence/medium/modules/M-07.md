# M-07 Hillslope Runfile Execution Configuration

Status: `MODULE-PASS`

Evidence class: **Ran + Static**

## Scope And Classification

- Production source SHA-256 before/after:
  `9ffac1aa1f2b32864a1973bb27108f50b7d032b0afc74f62d9ac66fba339720c`
  (unchanged).
- Test source SHA-256: `5032e23cfdafbce8f35fc264b3f81ee1a1176350e8a85e4a12f881278c3b8525`
  before and `3cbc15e9a1477bb9a464277d36f7bfffdd91938f8c0d128abf2d41e9610f4460`
  after; response test SHA-256
  `5018254c358da7d28a610dca75e40b2281ca5f97eb5377b9a9d64afa63e84137`.
- Tier: glue; exact production slice is `runfile_helpers.rs`.
- Line counts: production 316 unchanged; CLI03 integration test 1,445 to
  1,592. Both remain below the 2,000-line warning.
- Focused write set: CLI03 tests, durable metrics, this record, and M-07
  Medium progress only.
- Required reading: root/crate/test/work-package guidance, ADR-0021, CQR guide,
  Medium plan, binding campaign contract, runner runfile/output authority,
  CLI03 consumer tests, and M-07 selection review.

Review A and the accepted ledger classify `parse_runfile_execution_config` as
`E-PRODUCTION`: it selects execution-lane inputs, output contracts, and
sidecar authority. The start row was CC 25, 78.333% regions, CRAP 31.357.

## Cover-First Result

Two real-consumer tests were added; production code was not changed. The first
binds strict rejection of a configured but unreadable `inputs.pmetpara` path.
The second uses staged combined-invalid inputs to bind exact error priority for
invalid TOML, schema, run name, unit system, required inputs, and optional
output paths, including whitespace-only required input rejection.

| Obligation | Binding |
| --- | --- |
| strict PMET override | missing path returns `CLIHILL-E-010` with field/path/readability text |
| parser priority | TOML before schema before run-name/unit/input/output validation |
| required inputs | existing unresolved-input consumer |
| output configuration | existing required/optional output and manifest consumer |
| legacy policy | existing legacy discovery ignores runfile sidecar override |
| runtime consumer | complete CLI03 execution/configuration cohort |

Final target metrics are 90.000% regions, CC 25, and CRAP 25.625. The focused
LCOV file coverage is 233/272 lines, 85.662%; whole-file region visibility is
265/313, 84.665%, because unrelated sidecar-discovery siblings are not in the
target obligation closure. The exact multi-responsibility target slice
(`parse_runfile_execution_config` plus its required/optional/relative path
helpers) covers 134/148 regions, 90.541%. Every slice helper is above 91%
regions; the target exceeds the 75% floor and is below CRAP 30.

Durable artifacts are `M-07-evidence/focused.lcov`,
`M-07-evidence/focused.json`, and `M-07-evidence/focused-crap.json`. Their
SHA-256 values are respectively
`922047f8682fed9f23826e777ba6c56ef3422decd2759c3754ceb70d8a084cd9`,
`7e763fe60d026d6f414b4e77117658dc462b9eb846844dda02a9eb2b53652ed5`,
and `10c3a71e3583b8c49a88b095ce7ae502569ff84c66a6f65451e866a55f43c80b`.

Literal metric commands were:

```text
cargo llvm-cov clean --workspace
cargo llvm-cov --workspace --test cli03_runner_contract_derived_tests --lcov --output-path docs/work-packages/cqr-pre-integration-campaign-evidence/medium/modules/M-07-evidence/focused.lcov
cargo llvm-cov --workspace --test cli03_runner_contract_derived_tests --json --output-path docs/work-packages/cqr-pre-integration-campaign-evidence/medium/modules/M-07-evidence/focused.json
cargo crap --path crates/openwepp-runner --lcov docs/work-packages/cqr-pre-integration-campaign-evidence/medium/modules/M-07-evidence/focused.lcov --format json --output docs/work-packages/cqr-pre-integration-campaign-evidence/medium/modules/M-07-evidence/focused-crap.json
```

## Gates And Review

| Gate | Result |
| --- | --- |
| focused `nextest` M-07 filter | PASS, 2/2 |
| complete CLI03 consumer under coverage | PASS, 25/25 |
| focused LCOV and CRAP | PASS, metrics above |
| `cargo clippy -p openwepp-runner --all-targets -- -D warnings` | PASS |
| `cargo fmt --all -- --check`; `git diff --check` | PASS |
| exact campaign `markdown-doc lint` command | PASS |

The complete CLI03 cohort executes real runner intake, output publication,
manifest creation, legacy discovery, and topology guards. Production and
numeric expressions are unchanged, so output equivalence is exact by identity.

Independent review verified the tests-only real-consumer change and production
identity, then raised R1 valid region evidence, R2 combined-invalid priority,
R3 required record fields, and R4 command/docs accuracy. All were accepted and
fixed; the refreshed nonempty JSON and slice audit provide direct region
evidence. No finding remains. Checkpoint commit: the commit containing tests,
evidence, record, and Medium progress; SHA pending commit creation.
