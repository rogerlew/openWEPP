# M-02 Groundwater-Coefficient Parser

Status: `MODULE-PASS`

Evidence class: **Ran + Static**

## Scope And Classification

- Before source SHA-256: `a18aba79181074bf0cecc3a5a8ab2b3ac32842723c8a0b9117a01602c16fa910`.
- After source SHA-256: `f21dac78162723516837fbd5aa22d30876a7feb73827ae0dee7e5c7692626271`.
- Tier: glue; the production slice is `enforce_result_invariants` and its
  extracted private validators in `gwcoeff.rs`.
- Source line count: 517 before, 622 after.

The live eligible row was `enforce_result_invariants`: CC 16, 53.333% region
coverage, and CRAP 42.017. The stale formatter row remains outside this change;
the Medium start reconciliation already removed it from the live census.

## Characterization And Decomposition

The new private test binds the valid missing and parsed branches plus exact
first-error text for `lr_bf`, file-presence, coefficient-presence, and parsed
line-closure failures. Combined-invalid staged-repair cases prove that exact
priority rather than merely exercising each guard. The production edit only
delegates the two match arms to private validators. Check ordering,
short-circuiting, error variants and text, public APIs, serialization, and
numeric expressions are unchanged.

Final focused metrics:

| Function | Region % | CC | CRAP |
| --- | ---: | ---: | ---: |
| `enforce_result_invariants` | 100.000 | 3 | 3.000 |
| `validate_missing_branch` | 100.000 | 7 | 7.000 |
| `validate_parsed_branch` | 100.000 | 8 | 8.000 |

Every target/helper is above the 75% focused floor and below CRAP 30. The
focused source report has SHA-256
`12aef92139a870f35b410856d7c8d4a148dbe2e74516b01de81f4d060d8cd8db`
for LCOV, `1ae1e3302b20643be552bc9cb9cab7500b1ef06ac125008da0f7df20e48bf73c`
for LLVM JSON, and
`b2382bfdc6419e24e7d0571d55e8d5211e27b0ab31b6de200eba585e3584b385`
for CRAP JSON.

## Gates And Review

| Gate | Result |
| --- | --- |
| `cargo nextest run -p openwepp-input-contract --lib` | PASS, 26/26 |
| `cargo nextest run --test infile_gwcoeff_parser_contract` | PASS, 12/12 |
| focused groundwater runtime-consumer filter | PASS, 2/2 |
| focused LCOV and CRAP | PASS, metrics above |
| `cargo clippy -p openwepp-input-contract --all-targets -- -D warnings` | PASS |
| `cargo fmt --check`; `git diff --check` | PASS |

The real-consumer filter ran
`gwbaseflow_lr_bf1_channel_branch_consumes_generated_hbp_not_cbase` and
`gwbaseflow_bftharea_suppresses_below_threshold_side_baseflow`. Both passed
unchanged, so downstream groundwater authority and threshold outputs are
preserved. There is no numerical kernel edit.

Independent review found one Medium gap: single-invalid fixtures did not prove
guard priority. The finding was accepted and fixed with combined-invalid
staged-repair cases for both branches. Review otherwise verified exact region
counts, CRAP, hashes, line count, real consumers, and behavior-equivalent
extraction. The first response review found stale same-source metrics after the
priority tests; the current 622-line source was remeasured and the hashes above
were refreshed. The response tests and formatting check pass; no finding
remains.

Checkpoint commit: `3dcb28aa`.
