# M-12 Yearly Extension YAML Conversion

Status: `MODULE-PASS`

Evidence class: **Ran + Static**

## Scope And Classification

- Before source SHA-256: `f3d4238ab6dd64ec310b598eb274ec16ef46eac37daba8539e620ca48d47afc9`.
- After source SHA-256: `57bf58cb001322fff6a98cee5c7cef9a536f0d0fc2ef6accd0cd03d059a91552`.
- Tier: production serialized-schema conversion.
- Source line count: 603 before, 754 after; below the 2,000-line warning.
- Focused write set: inline tests, durable metrics, this record, and M-12
  Medium progress only. Production code is unchanged.
- Required reading: root/crate/test/work-package guidance, ADR-0021 and CQR
  procedure, Medium plan, binding campaign contract, accepted ledger, and
  target-selection Review B, `SC-INFILE-MANAGEMENT-001`, and the canonical
  management-schema serialization contract/tests.

Review B and the accepted ledger classify `yearly_extension_to_yaml` as
`E-PRODUCTION`: its enum variant, YAML tag, field names, values, and field order
are persisted management-schema behavior. The start row was CC 6, 0% regions,
and CRAP 42.

## Tests-Only Result

| Obligation | Binding |
| --- | --- |
| five variants | herbicide, burn, silage, cut, and remove each retain their exact variant |
| integer anti-alias | distinct 101/202/303/404/505 values bind each day field |
| float anti-alias | burn magnitude, burn residue, cut fraction, and remove fraction use distinct exact `to_bits()` values |
| schema tags | serialized `type` tags remain `herbicide`, `burn`, `silage`, `cut`, and `remove` |
| field names/order | each tag precedes its declared fields; burn preserves `jdburn`, `fbmag`, `fbrnog` order |
| public consumer | all five converted values enter one valid management document, serialize with `to_management_yaml_string`, and parse back with `parse_management_yaml_from_str` |
| roundtrip identity | every reparsed `YearlyAnnualExtension` equals the converted value |

The public roundtrip uses a complete canonical management document and the
real schema validator/serializer/parser boundary. It is not an isolated enum
serde test. Distinct bit patterns prevent swapped float fields from aliasing
the expected result.

| Function | CC | Regions | CRAP |
| --- | ---: | ---: | ---: |
| `yearly_extension_to_yaml` | 6 | 100.000% | 6.000 |

The target exceeds the 85% accepted-slice threshold and 75% function floor,
and is below CRAP 30. For context, the complete mixed-purpose `convert.rs` file
is 84.375% functions, 76.361% lines, and 77.651% regions under the full crate
cohort. Its unrelated plant, initial-condition, schedule, checksum, and source
datver conversion paths are outside M-12's exact target slice.

Durable artifacts are `M-12-evidence/focused.lcov`,
`M-12-evidence/focused.json`, and `M-12-evidence/focused-crap.json`. Their
SHA-256 values are respectively
`9237d8cfb7046072560e50abb6591564e43818386d8becfdfddd32c3dea33778`,
`9a3d1b08a4939bea779ec15158c80dd94646c70fcd1e169b4bf5b705081729cc`,
and `f605e75485ad14eb03a47f1824d86ffbb69b8144ea28eabed1a3fe8847fb4f68`.

Exact metric commands were:

```text
cargo llvm-cov clean --workspace
cargo llvm-cov nextest -p openwepp-landuse-migrate --lcov --output-path /tmp/m12-final.lcov
cargo llvm-cov report -p openwepp-landuse-migrate --json --output-path /tmp/m12-final.json
cargo crap --path crates/openwepp-landuse-migrate --lcov /tmp/m12-final.lcov --format json --output /tmp/m12-final-crap.json
cp /tmp/m12-final.lcov docs/work-packages/cqr-pre-integration-campaign-evidence/medium/modules/M-12-evidence/focused.lcov
cp /tmp/m12-final.json docs/work-packages/cqr-pre-integration-campaign-evidence/medium/modules/M-12-evidence/focused.json
cp /tmp/m12-final-crap.json docs/work-packages/cqr-pre-integration-campaign-evidence/medium/modules/M-12-evidence/focused-crap.json
```

## Gates And Review

| Gate | Result |
| --- | --- |
| five-variant conversion and public roundtrip | PASS, 2/2; 16 skipped |
| complete crate cohort under coverage | PASS, 18/18 |
| focused LCOV and CRAP | PASS, metrics above |
| `cargo clippy -p openwepp-landuse-migrate --all-targets -- -D warnings` | PASS |
| `cargo fmt --all -- --check`; `git diff --check` | PASS |
| exact campaign `markdown-doc lint` | PASS |

The literal focused command was:

```text
cargo nextest run -p openwepp-landuse-migrate -E 'test(/m12_/)'
```

Independent review verified all five mappings, bitwise anti-alias values,
schema tags/order, public validator/serializer/parser roundtrip, metrics,
hashes, and line governance. R1 documentation, R2 reading, R3 copy provenance,
and R4 terminal fields were accepted and fixed. No finding remains. Checkpoint
commit: the commit containing tests, evidence, record, and Medium progress; SHA
pending commit creation.
