# HB-06 — WB13 Daily Water-Balance Row

Status: `MODULE-PASS`

## Identity And Change

- Target: `crates/openwepp-summary-accumulator/src/lib.rs`.
- Final SHA-256: `20ed6937dff93a7422ad0ab661fccadb053930b46026f67ea5df47f00774c131`.
- Lines: `1,767`, below WARN.
- Start row: `Wb13DailyWaterBalanceRow::from_surface`, CC 33, coverage
  77.778%, CRAP 44.951.

Cross-field WB13 relationship validation was mechanically extracted without
changing required-symbol load order, schema order, formatting, tolerance, or
typed-error priority. `DC-CQR-HB06-001` removed the per-OFE marker bypass:
public `QOFE` must equal `Q` within `1e-9` for every publication policy.

Distinct anti-alias operands prove exact mapping for precipitation, rain/melt,
runoff, ET components, deep/lateral/tile losses, upstream and subsurface runon,
soil/frozen/snow stores, area, and profile capacity/FC/WP stores. Independent
guards prove `Total-Soil == SoilWaterTotal` and
`ProfilePorosityCap >= ProfileFCStore >= ProfileWPStore`.

The adoption increment adds a finite-first typed input seam. The production
runner's common WAT adapter requires complete canonical profile inputs,
validates emitted simulation-year keys, bit-compares all three keys and 22
canonical scalars, and reconstructs canonical fields from the validated row.
The direct-publication boundary rejects corrupt storage aliases above
`1e-6 mm`; canonical projection already publishes equal aliases.

## Metrics And Evidence

- Package tests: `20/20` PASS.
- Public output-surface integration: `3/3` PASS.
- Target `from_surface`: CC `28`, coverage `100%`, CRAP `28`.
- Typed constructor `from_input`: CC `7`, coverage `100%`, CRAP `7`.
- Relationship helper: CC `5`, coverage `100%`, CRAP `5`.
- Whole module: lines `1144/1157` (`98.876%`), regions `1589/1651`
  (`96.245%`), functions `75/75`; zero of 46 eligible production functions
  below 75%.
- Format, focused Clippy with `-D warnings`, and diff checks: PASS.

| Archived artifact | SHA-256 |
| --- | --- |
| `coverage/hb06-adoption-final.lcov` | `688120f6803438bf72ac74715825e5cf0369641757c73939fd9d8cd0f5e60cd4` |
| `coverage/hb06-adoption-final.json` | `deda241cda2f76020f435926e458cf608c955f8bd1733d50baee14c9300cbec5` |
| `coverage/hb06-adoption-final-crap.json` | `434f662a776437bce7d0a2d6ce14ab36d242ef00e56ea91273e6d861385a4138` |

## Consumer Boundary And Disposition

The SIMIMPL04 executable is the real downstream consumer proof. Production
execution passed through the common typed adapter and streaming sink; the test
then read `H5.wat.parquet` and verified exact schema order/type/nullability,
two ordered rows, simulation-year keys, and bit-exact `Q == QOFE` and
`Total-Soil == SoilWaterTotal` values. Public output-surface integration remains
supporting evidence only.

Checkpoint: `MODULE-PASS`.
