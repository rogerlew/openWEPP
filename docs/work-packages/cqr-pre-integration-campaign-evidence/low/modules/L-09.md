# L-09 Snowbench Error Identity

Status: `MODULE-PASS`

Evidence class: **Ran + Static**

## Scope And Classification

- Production source: `crates/openwepp-runner/src/hillslope/snowbench.rs`.
- Source SHA-256 before/after:
  `3e834d7bca3f3e91cd5e7826cce433c3cd8f6b765d0a7f8f65bbb04d337b79e7`.
- Tier: glue; target slice is `SnowbenchError::{fmt,source}`. Export,
  validation, forcing, and Parquet responsibilities are distinct siblings.
- Raw/actionable row: `SnowbenchError::fmt`, CRAP 32.619, `E-PRODUCTION`
  because the CLI/export API publishes these stable diagnostic identities.
- Production code is unchanged; a public integration test and evidence are the
  only increment.

## Cover-First Result

One table-driven public test binds all seven variants, exact codes/details, I/O
and JSON paths, nested runner identity, and the three-source/four-no-source
split. The real pysnobal exporter integration selection proves the downstream
consumer still executes successfully.

| Obligation family | Binding |
| --- | --- |
| A nominal in-domain | formatter is error-only; the real exporter pass provides contextual nominal evidence |
| B boundary/near-threshold | no numeric threshold occurs in this error-publication slice |
| C branch | all seven variants execute through the public exhaustive table |
| D domain-reject | exact invalid-input and invalid-forcing details are bound |
| E missing-symbol | runner and openWEPP-snow diagnostic identities are retained without alias |
| F non-finite | no numeric validity decision occurs in the formatter/source slice |
| G conservation/continuity | not applicable to error publication |
| H fail-closed | I/O, JSON, and runner sources remain chained; all other variants prove no source |

| Function | Region % | CC | CRAP |
| --- | ---: | ---: | ---: |
| `SnowbenchError::fmt` | 100.000 | 8 | 8.000 |
| `SnowbenchError::source` | 100.000 | 5 | 5.000 |

The exact target slice exceeds the 85% glue threshold and 75% function floor;
both rows are below CRAP 30. Whole-file focused visibility is 29/1,160 lines
(2.500%) and 45/1,434 regions (3.138%) because the one-test profile isolates
error publication in a multi-responsibility exporter. Every unrelated sibling
remains outside the slice without waiver.

Artifact SHA-256 values under `L-09-evidence/` are:

- LCOV `4cbeebdfd07e47d27d95d8cc6a21b15a294cb1ee27eb48182dc7323ce5860ed5`;
- LLVM JSON `868fc4951e58fd04aff2fa3a85f59aff60e58212c3bf5eca995dbc0f9649e7aa`;
- CRAP JSON `3b646bc2b5846107b73eeff259ff34a7bb3e7bbe0de7bd42e7dfc6c4482478ec`.

One package-scoped `cargo llvm-cov --test snowbench_error_contract
--no-report` execution produced all artifacts through report-only commands and
package-scoped `cargo crap`.

## Gates, Lines, And Review

| Gate | Result |
| --- | --- |
| public Snowbench error contract | PASS, 1/1 |
| real pysnobal exporter consumer | PASS, 1/1 |
| focused LCOV/JSON/CRAP | PASS, target metrics above |
| `cargo fmt --all -- --check` | PASS |
| `git diff --check` | PASS |
| production source identity | PASS, unchanged |

`snowbench.rs` remains 1,641 lines and the new integration test is 67 lines,
below governance thresholds. Exact production identity proves behavior and
numeric equivalence.

Independent module review is complete below. Workspace-wide gates remain
tranche-final.

## Independent Module Review

Verdict: **PASS**.

The review independently reproduced the unchanged production source SHA-256
and all three focused artifact hashes. LLVM JSON reproduces the deliberately
isolated whole-file visibility of 29/1,160 lines (2.500%) and 45/1,434 regions
(3.138%). The two exact target functions have no uncovered LLVM region. CRAP
JSON reproduces 100% region coverage with CC/CRAP 8/8 for `fmt` and 5/5 for
`source`.

The two-function slice is exact and legitimate for the fixed row. No private
helper is called by either function. The table executes all seven variants,
all stable `SNOWBENCH-E-001` through `SNOWBENCH-E-007` details, both path
formats, nested `CLIHILL-E-001` identity, and the three-source/four-no-source
partition. Static inspection confirms the `openwepp-snowbench` command adapter
converts these errors to strings and publishes them on its nonzero CLI path.

The host's exporter, forcing, validation, calendar, YAML/JSON, audit, and
Parquet/openWEPP-snow projection families are separate responsibilities. Their
many uncovered CRAP rows remain fully visible in the artifact and receive no
waiver or disposition from L-09. The real pysnobal exporter test is a nominal
downstream consumer; it does not exercise an error string. Exhaustive direct
public error coverage plus the static CLI handoff supplies the error-path proof
without misrepresenting that nominal consumer.

Review reruns against the uncommitted module increment passed:

| Review command | Result |
| --- | --- |
| `cargo nextest run -p openwepp-runner --test snowbench_error_contract` | PASS, 1/1 |
| named `g0_exporter_emits_pysnobal_schema_and_required_anti_alias_lineage` selection | PASS, 1/1 |
| `cargo fmt --all -- --check` | PASS |
| `git diff --check` | PASS |

### Findings And Dispositions

| Finding | Disposition | Evidence |
| --- | --- | --- |
| Whole-file focused coverage is only 2.500% lines / 3.138% regions | `accepted` | The fixed row is a bounded error-publication slice in a 1,641-line multi-responsibility host; both target functions are at 100% with CRAP at most 8, and every sibling gap remains visible. |
| The real exporter consumer is nominal rather than an error-path subprocess | `accepted` | The direct public test exhausts every error/source branch and static CLI wiring proves publication; no retained function-floor exception is requested. |
| The focused CRAP artifact contains many unrelated rows above 30 | `accepted` | They are outside the fixed L-09 identity, receive no waiver here, and remain subject to their own module and tranche-final accounting. |

No finding is deferred, marked follow-up, or left open within the authorized
slice. Classification remains `E-PRODUCTION`: the formatter itself emits stable
machine-distinguishable Snowbench codes and the CLI publishes its output. The
A-H map is proportional to this error/source surface. Source identity, line
counts, all seven detail/source branches, the 85%/75% target-slice gates, CRAP
bound, and nominal exporter consumer are closed. `MODULE-PASS` authorizes entry
into Low tranche-final validation only.
