# L-11 Symbol Alias Registry Error Display

Status: `DISPOSITIONED-NO-ACTION`

Evidence class: **Ran + Static**

## Source-Bound Classification

- Source: `crates/openwepp-sim-contract/src/symbols.rs:66-120`.
- Source SHA-256:
  `13a475dc0c7376072b91a48f9eaded2f36925022533def98fe296dc98e8fc9cd`.
- Raw row: `SymbolAliasRegistryError::fmt`, CC 9, 0% coverage, CRAP 90.000.
- Classification: exact symbol-level `R-OBSERVABILITY`; denominator retained,
  with reviewed function-floor and CRAP exception.

The function only renders fields from an already-selected typed registry error.
It contains no stable machine code, alias choice, validation, state mutation,
control effect, serialization, or publication behavior. Consumers use successful
registry mappings or match typed variants; no string parser, exact-text
consumer, subprocess, or published field consumes the formatter.

## Public Behavior And Reviews

The `sim_contract_symbol_alias_registry` suite exercises construction, template
validation, duplicate/ambiguity selection, missing lookup, and successful
reverse lookup through typed public behavior. The exception does not exclude
those registry decisions or variants.
Current-source command
`cargo nextest run --test sim_contract_symbol_alias_registry` passes 13/13
tests.

Both `target-selection-review-a.md` and `target-selection-review-b.md`
independently accept this exact source span as `R-OBSERVABILITY`. No reviewer
proposed a broader module, wildcard, infrastructure, or generated-code
exclusion. No finding is deferred. Source is unchanged and the file is 698
lines, below line-count thresholds.

Disposition: no implementation package is legitimate; retain the raw row in
the campaign debt denominator and close L-11 as `DISPOSITIONED-NO-ACTION`.
