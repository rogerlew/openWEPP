# L-06 HBP Adapter Error Identity

Status: `MODULE-PASS`

Evidence class: **Ran + Static**

## Scope And Classification

- Production source: `crates/openwepp-legacy-bridge/src/hbp.rs`.
- Source SHA-256 before/after:
  `7f6d8e45277cf567397039aaf0fcce6b8f544ddacac6ae13e3dc8434ed5954d1`.
- Tier: glue; target slice is `HbpAdapterError::{fmt,code}` plus transitive
  private helper `format_magic`. Header adaptation, contract validation, and
  warning behavior are distinct siblings.
- Raw/actionable row: `HbpAdapterError::fmt`, CC 7, 0% regions, CRAP 56.000,
  `E-PRODUCTION` because all details publish stable `HBP-E-*` identities.
- Production code is unchanged; a public crate integration test and evidence
  are the only increment.

## Cover-First Result

One table-driven public test binds all six variants and codes, exact operand
rendering, uppercase hexadecimal magic serialization, and the absence of a
nested error source. Existing adapter and HBP parser/bridge tests bind both
strict rejection and compatibility acceptance through the real public adapter.

| Obligation family | Binding |
| --- | --- |
| A nominal in-domain | formatter is error-only; canonical adapter acceptance is contextual nominal evidence |
| B boundary/near-threshold | exact minimum-byte operands are rendered for E001 and E004 |
| C branch | all six variants execute through the public exhaustive table |
| D domain-reject | invalid contract, duplicate/canonical alias, short shard, and unknown magic details are bound |
| E missing-symbol | not applicable to this fixed header contract |
| F non-finite | not applicable to byte/header error publication |
| G conservation/continuity | not applicable to error publication |
| H fail-closed | all codes/details are exact; strict legacy rejection remains a real bridge consumer and no variant has a nested source |

| Function | Region % | CC | CRAP |
| --- | ---: | ---: | ---: |
| `HbpAdapterError::fmt` | 100.000 | 7 | 7.000 |
| `HbpAdapterError::code` | 100.000 | 7 | 7.000 |
| `format_magic` | 100.000 | 1 | 1.000 |

The exact target slice exceeds the 85% glue threshold and 75% function floor;
all three rows are below CRAP 30. Whole-file focused visibility is 52/131 lines
(39.695%) and 65/129 regions (50.388%) because the one-test profile deliberately
isolates the formatter/code surface. Adapter, validator, and success/warning
siblings remain out of slice without waiver; their existing consumer tests are
run separately.

Artifact SHA-256 values under `L-06-evidence/` are:

- LCOV `c55e4768812bb6583e9528daddda6a0a0a032ec9172c82a6377037d2abf5763c`;
- LLVM JSON `ca649fb8637b3ec3907e8d64d197f53d2253f3d628903a06de2a411a9d73a921`;
- CRAP JSON `818f7317817a46a40431381ee8bc4db66919c2e6f8c4fa54fc514e564002bd97`.

One `cargo llvm-cov -p openwepp-legacy-bridge --test hbp_error_contract
--no-report` execution produced all three artifacts using report-only commands
and package-scoped `cargo crap` against the retained LCOV.

## Gates, Lines, And Review

| Gate | Result |
| --- | --- |
| public HBP error contract | PASS, 1/1 |
| strict/compat parser-to-bridge consumers | PASS, 2/2 |
| focused LCOV/JSON/CRAP | PASS, target metrics above |
| `cargo fmt --all -- --check` | PASS |
| `git diff --check` | PASS |
| production source identity | PASS, unchanged |

`hbp.rs` remains 362 lines and the new integration test is 50 lines, below
governance thresholds. Exact production identity proves behavior and numeric
equivalence.

Independent module review is complete below. Workspace-wide gates remain
tranche-final.

## Independent Module Review

Verdict: **PASS**.

The review independently reproduced the unchanged production source SHA-256
and all three focused artifact hashes. LLVM JSON reproduces the deliberately
narrow whole-file visibility of 52/131 lines (39.695%) and 65/129 regions
(50.388%). The exact slice functions have no uncovered LLVM regions. CRAP JSON
reproduces 100% region coverage with CC/CRAP 7/7 for `fmt`, 7/7 for `code`, and
1/1 for transitive private helper `format_magic`.

The corrected three-function slice is legitimate. `fmt` and `code` publish all
six stable identities and payloads, while `format_magic` performs the exact
uppercase hexadecimal serialization consumed by four formatter arms. The
public table executes all six variants, all codes/details, both byte-count
operands, every magic operand, and the absence of a nested error source.

Header adaptation, contract validation, compatibility warnings, and success
state are distinct sibling responsibilities outside the focused capture. They
receive no waiver from L-06. Instead, the two independently rerun public
parser-to-bridge tests prove the real strict rejection and compatibility
acceptance/warning consumers. Unrelated `sidecar.rs` rows visible in the
package-scoped CRAP artifact are not L-06 targets and receive no disposition.

Review reruns against the uncommitted module increment passed:

| Review command | Result |
| --- | --- |
| `cargo nextest run -p openwepp-legacy-bridge --test hbp_error_contract` | PASS, 1/1 |
| named strict and compatibility `infile_hbp_parser_contract` selections | PASS, 2/2 |
| `cargo fmt --all -- --check` | PASS |
| `git diff --check` | PASS |

### Findings And Dispositions

| Finding | Disposition | Evidence |
| --- | --- | --- |
| The producer omitted transitive helper `format_magic` from the declared slice | `accepted-fixed` | The scope and metric table now include it; focused evidence is 100% regions with CC/CRAP 1/1. |
| Whole-file focused coverage is intentionally 39.695% lines / 50.388% regions | `accepted` | The profile isolates the error-publication slice; separate real consumers cover strict/compat adapter behavior, and no sibling exception is claimed. |
| Package-scoped CRAP includes unrelated uncovered `sidecar.rs` rows | `accepted` | They are outside the L-06 source file and fixed identity and remain subject to their own module/tranche accounting. |

No finding is deferred, marked follow-up, or left open within the authorized
slice. Classification remains `E-PRODUCTION`: each formatter branch embeds its
stable `HBP-E-*` code, and the bridge boundary consumes the typed results. The
A-H map is proportional to this byte/header error-publication surface. Source
identity, line counts, all six code/detail/source branches, hexadecimal helper,
the 85%/75% target-slice gates, CRAP bound, and both real policy consumers are
closed. `MODULE-PASS` authorizes entry into Low tranche-final validation only.
