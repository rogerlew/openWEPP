# L-01 Frost Error Display

Status: `MODULE-PASS`

Evidence class: **Ran + Static**

## Scope And Classification

- Production source: `crates/openwepp-input-contract/src/parsers/frost.rs`.
- Source SHA-256 before/after:
  `3620917b175a69c557c18616943ad069460fe76d783d8c1ab1bb76ef9bc093e1`.
- Tier: glue; exact target slice is `FrostParseError::fmt` and
  `contract_error_id`.
- Raw/actionable row: `FrostParseError::fmt`, CC 7, 0% regions, CRAP 56.000,
  `E-PRODUCTION` by reviewer disagreement default.
- Write set: `tests/integration/infile_frost_parser_contract.rs`, focused
  evidence, this record, and Low progress only. Production code is unchanged.
- Required reading: root/crate/test/work-package instructions, ADR-0021, CQR
  authoring guide, Low plan, binding execution contract, and both selection
  reviews.

Review A found the runner-published detail eligible; Review B proposed pure
observability. The binding disagreement rule retains `E-PRODUCTION` because
`parse_legacy_frost_sidecar` places the rendered error in
`HillslopeCliError::ParseFailure.detail`.

## Cover-First Result

One table-driven public characterization test constructs all seven variants
and binds every contract ID and exact rendered detail. No production branch,
message, parser rule, or value expression changed.

| Obligation | Binding evidence |
| --- | --- |
| all typed variants | seven-case `frost_error_display_preserves_all_runner_visible_details` table |
| exact public detail and payload fields | exact equality for path, line, field, value, allowed range, message, and token text |
| separate machine identity | exact `FROST-E-000` through `FROST-E-006` equality |
| real downstream consumer | `cli03_legacy_sidecar_discovery_mode_uses_legacy_sidecars_and_ignores_runfile_overrides` passes through the production frost intake |
| anti-alias/priority | distinct E001/E002 shared display arm retains distinct typed IDs and payloads |

Applicable A-H map for this exact output-mapping slice:

| Family | Applicability and binding |
| --- | --- |
| A nominal | all seven constructible typed errors render their representative payload exactly in `frost_error_display_preserves_all_runner_visible_details` |
| B boundary | not applicable; the formatter performs no threshold comparison or normalization |
| C branch | all seven variants execute; E001 and E002 also exercise their shared display arm while retaining distinct IDs |
| D domain-reject | the seven parser rejection identities and exact public details are bound by the same table; parser input selection itself is outside this slice |
| E missing-symbol | not applicable; the formatter has no required-symbol lookup |
| F non-finite | E003's non-finite diagnostic payload is rendered exactly; the formatter performs no numeric finite guard |
| G conservation | not applicable to error identity/detail publication |
| H fail-closed | exhaustive enum matching has no fallback/default arm, and exact ID/detail assertions reject aliasing or silent text substitution |

Focused results:

| Function | Region % | CC | CRAP |
| --- | ---: | ---: | ---: |
| `FrostParseError::fmt` | 100.000 | 7 | 7.000 |
| `FrostParseError::contract_error_id` | 100.000 | 8 | 8.000 |

The exact target slice exceeds the 85% glue threshold and 75% function floor,
and every slice row is below CRAP 30. Whole-file visibility is 433/539 lines
(80.334%) and 450/556 regions (80.935%). The gap is outside the target slice in
strict/compat parser grammar, clamp, tokenization, and range-validation sibling
families; this record claims no closure for those unrelated responsibilities.

Artifact SHA-256 values are:

- LCOV `56d8625b21f1443c1ba9739381615a8404704d3b055393df7430f59e329f8655`;
- LLVM JSON `2f768d33b4daf4bb918036faf3d58464a9f4e5a5e2f2719005640228c04d8dec`;
- CRAP JSON `eaebc7f9cc116bcd757312309d3c943eb447c020049ad1193c08c2fd30070841`.

The same execution produced all three artifacts under `L-01-evidence/` using
`cargo llvm-cov --workspace --test infile_frost_parser_contract --no-report`,
report-only LCOV/JSON for `openwepp` plus `openwepp-input-contract`, and
`cargo crap --workspace` against the retained LCOV.

## Gates, Lines, And Review

| Gate | Result |
| --- | --- |
| `cargo nextest run --test infile_frost_parser_contract` | PASS, 11/11 |
| production frost-intake consumer | PASS, 1/1 |
| focused LCOV/JSON/CRAP | PASS, target metrics above |
| `cargo fmt --all -- --check` | PASS |
| `git diff --check` | PASS |

`frost.rs` remains 751 lines. The integration test grows from 167 to 238 lines;
both are below line-count thresholds. Production identity makes behavior and
numeric equivalence exact.

Independent module review is complete below. No workspace-wide gate is claimed
at this staged checkpoint; those gates remain explicitly tranche-final scope.

## Independent Module Review

Verdict: **PASS**.

Review A independently reproduced the three artifact hashes and the focused
file totals of 433/539 lines (80.334%) and 450/556 regions (80.935%). The CRAP
artifact contains identical per-instantiation rows; identity deduplication
reproduces 100% target coverage, CC/CRAP 7/7 for `FrostParseError::fmt`, and
CC/CRAP 8/8 for `contract_error_id`. The LLVM JSON contains no uncovered
region in either target function. The artifacts remain bound to the unchanged
production SHA-256 stated above.

The target slice is legitimate. `fmt` and `contract_error_id` wholly define
the selected error detail/identity behavior, while the uncovered whole-file
regions belong to separately named parser grammar, compatibility clamp,
tokenization, and range-validation responsibilities. No production line moved
into or out of the slice, and no production source changed.

Review reruns against the dirty module worktree passed:

| Review command | Result |
| --- | --- |
| `cargo nextest run --test infile_frost_parser_contract` | PASS, 11/11 |
| named `cli03_legacy_sidecar_discovery_mode_uses_legacy_sidecars_and_ignores_runfile_overrides` selection | PASS, 1/1 |
| `cargo fmt --all -- --check` | PASS |
| `git diff --check` | PASS |

The selection-review disagreement is correctly closed by the binding default
to `E-PRODUCTION`. Static inspection confirms that
`parse_legacy_frost_sidecar` converts `FrostParseError` with `to_string()` into
`HillslopeCliError::ParseFailure.detail`, whose public display publishes the
detail under `CLIHILL-E-010`. The named runner test is a real nominal frost
consumer; it does not force a malformed frost error through the subprocess.
That is not a closure gap here because the direct public characterization
executes every formatter branch and this module requests no
`R-LOW-COMPLEXITY-PRODUCTION` exception. The record does not claim a
subprocess-level invalid-frost assertion.

### Findings And Dispositions

| Finding | Disposition | Evidence |
| --- | --- | --- |
| Review B proposed `R-OBSERVABILITY` for the formatter | `accepted-fixed` | Review A identified the runner-published detail; disagreement defaults to `E-PRODUCTION`, and the eligible slice now passes its floor and CRAP gates. |
| The producer record described obligations but did not label the A-H map | `accepted-fixed` | The explicit applicability table above binds A, C, D, F, and H and explains why B, E, and G do not apply to this output-mapping slice. |
| The named real consumer covers nominal frost intake, not malformed frost stderr | `accepted` | Static production handoff plus direct exhaustive public formatter coverage is proportional for ordinary `E-PRODUCTION`; no retained floor exception is requested. |
| CRAP JSON repeats identical function-instantiation rows | `accepted` | Deduplicated identities have identical line, CC, coverage, and CRAP values; no conflicting metric exists. |

No finding is deferred, marked follow-up, or left open. L-01 satisfies the
focused glue-tier target-slice threshold, 75% function floor, CRAP bound,
applicable obligation binding, real-consumer evidence, source identity, and
line-count requirements. `MODULE-PASS` authorizes only entry into Low
tranche-final validation; it does not claim workspace or campaign readiness.
