# L-02 Phosphorus Error Identity

Status: `MODULE-PASS`

Evidence class: **Ran + Static**

## Scope And Classification

- Production source: `crates/openwepp-input-contract/src/parsers/phosphorus.rs`.
- Source SHA-256 before/after:
  `7b431f8f56b49ab77085fdcf9e021967c23f9f5ebf9395303547b594f865ddd7`.
- Tier: glue; target slice is the cohesive public error-mapping surface
  `PhosphorusParseError::{fmt,contract_error_id,source}`. Public parser outcomes
  are contextual consumer evidence, not an expansion of the slice.
- Raw/actionable row: `PhosphorusParseError::fmt`, CC 9, 0% regions, CRAP
  90.000, `E-PRODUCTION` because every display arm embeds `PHOS-E-*` identity.
- Write set: `tests/integration/infile_phosphorus_parser_contract.rs`, focused
  evidence, this record, and Low progress. Production code is unchanged.

## Cover-First Result

One table-driven public test binds all eight variants, all eight contract IDs,
exact payload-bearing display strings, and I/O versus non-I/O `Error::source`.
Existing public parser tests bind nominal, absent-sidecar, header, record-count,
token, finite, range, and fanout outcomes.

| Obligation family | Binding |
| --- | --- |
| A nominal in-domain | not directly applicable to error mapping; canonical and numeric-leading parser tests retain contextual accepted-input evidence |
| B boundary/near-threshold | the formatter performs no threshold decision; zero-compatible and large nonnegative parser cases retain contextual boundary evidence |
| C branch | all eight error variants execute through `phosphorus_error_display_preserves_all_contract_identities` |
| D domain-reject | exact E003 finite and E004 range identities/details are bound by the table and by negative/non-finite parser tests |
| E missing-symbol | no required-symbol lookup exists in this slice; required missing-sidecar behavior is separately bound to the typed E000 I/O outcome |
| F non-finite | a real `NaN` E003 payload renders exactly, and the public parser non-finite test selects E003 |
| G conservation/continuity | not applicable to error identity, detail, or source publication |
| H fail-closed | exhaustive IDs and exact strings permit no fallback/alias; only E000 exposes an error source and every other variant proves `None` |

| Function | Region % | CC | CRAP |
| --- | ---: | ---: | ---: |
| `PhosphorusParseError::fmt` | 100.000 | 9 | 9.000 |
| `PhosphorusParseError::contract_error_id` | 100.000 | 9 | 9.000 |
| `PhosphorusParseError::source` | 100.000 | 3 | 3.000 |

Whole-file focused visibility is 248/286 lines (86.713%) and 290/322 regions
(90.062%), exceeding the 85% glue threshold. Every eligible target function
exceeds the 75% floor and is below CRAP 30.

Artifact hashes under `L-02-evidence/` are LCOV
`74148fd12a500c629f0957c8d1d310495eee9fec6953caaa5bac6144eea066ac`,
LLVM JSON
`84c9962b620452988b61507853d1d71470f784eced2c92024ca9ef5f2190e2b1`,
and CRAP JSON
`ceebae18188a690209b888377584cb514889868a0a06ddeb9e9a85adde06e9b1`.
All derive from one `infile_phosphorus_parser_contract` coverage execution and
report-only LCOV/JSON commands.

## Gates, Lines, And Review

| Gate | Result |
| --- | --- |
| `cargo nextest run --test infile_phosphorus_parser_contract` | PASS, 13/13 |
| focused LCOV/JSON/CRAP | PASS, metrics above |
| `cargo fmt --all -- --check` | PASS |
| `git diff --check` | PASS |
| production source identity | PASS, unchanged |

`phosphorus.rs` remains 446 lines. The integration test grows from 248 to 329
lines; both are below governance thresholds. Exact production identity proves
behavior and numeric equivalence.

Independent module review is complete below. Workspace-wide gates remain
explicitly tranche-final and are not claimed here.

## Independent Module Review

Verdict: **PASS**.

The review independently reproduced the production source SHA-256 and all
three focused artifact hashes. LLVM JSON reproduces 248/286 lines (86.713%)
and 290/322 regions (90.062%) for whole-file visibility. The three exact slice
functions have no uncovered LLVM regions. After deduplicating identical
per-instantiation CRAP rows, `fmt`, `contract_error_id`, and `source` reproduce
100% region coverage with CC/CRAP 9/9, 9/9, and 3/3 respectively.

The slice is legitimate but must remain narrow. Its three functions jointly
publish the stable error code, exact payload-bearing detail, and standard error
source. Uncovered parser grammar, fanout, and state-closure branches are
distinct sibling responsibilities and are not claimed closed here. In
particular, whole-file `verify_fanout` and `validate_state_closure` remain below
the 75% function floor in this focused profile; they are outside this target
slice and receive no exception or disposition from L-02.

Review reruns against the uncommitted module increment passed:

| Review command | Result |
| --- | --- |
| `cargo nextest run --test infile_phosphorus_parser_contract` | PASS, 13/13 |
| `cargo fmt --all -- --check` | PASS |
| `git diff --check` | PASS |

### Findings And Dispositions

| Finding | Disposition | Evidence |
| --- | --- | --- |
| The producer's A-H labels did not match the normative family names | `accepted-fixed` | The obligation table now uses the ADR-bound A-H taxonomy and separates directly applicable error-mapping duties from contextual parser evidence. |
| “Public parser outcomes selecting it” could be read as expanding the target slice | `accepted-fixed` | Scope now names only the cohesive three-function error surface; parser outcomes are explicitly contextual, and sub-floor parser siblings are disclosed without waiver. |
| The CRAP artifact repeats identical function-instantiation rows | `accepted` | Identity deduplication yields identical line, CC, coverage, and CRAP values with no conflict. |

No finding is deferred, marked follow-up, or left open. L-02 remains
`E-PRODUCTION`: every display arm embeds its stable `PHOS-E-*` identity, so no
observability exception is available. The exhaustive table covers all eight
codes, payload renderings, and source-selection branches; the glue-tier slice
passes the aggregate threshold, 75% function floor, CRAP bound, source-identity
gate, line-count gate, and applicable obligation binding. `MODULE-PASS`
authorizes entry into Low tranche-final validation only.
