# L-04 TCR Error Identity

Status: `MODULE-PASS`

Evidence class: **Ran + Static**

## Scope And Classification

- Production source: `crates/openwepp-input-contract/src/parsers/tcr.rs`.
- Source SHA-256 before/after:
  `ec271acae336c536a3f4e1af54dbb03da788f9a085493c41f6cb2211d3cd62e4`.
- Tier: glue; target slice is the cohesive public error-mapping surface
  `TcrParseError::{fmt,contract_error_id,source}`. Parser behavior is contextual
  evidence, not an expansion of the slice.
- Raw/actionable row: `TcrParseError::fmt`, CC 10, 0% regions, CRAP 110.000,
  `E-PRODUCTION` because every display arm publishes a `TCR-E-*` identity.
- Production code is unchanged; the increment adds only public contract tests
  and assessment evidence.

## Cover-First Result

One table-driven public test binds all nine variants and IDs, exact
payload-bearing display strings, non-finite rendering, and I/O versus non-I/O
`Error::source`. Existing parser tests exercise nominal, strict/compatibility,
missing/open-error, count, token, domain, relational, cross-file, and curve
outcomes.

| Obligation family | Binding |
| --- | --- |
| A nominal in-domain | not directly applicable to error mapping; the valid override-curve parser test is contextual nominal evidence |
| B boundary/near-threshold | no threshold decision occurs in the slice; count, slope, and denominator cases are contextual |
| C branch | all nine error variants execute through `tcr_error_display_preserves_all_contract_identities` |
| D domain-reject | exact E004 and E008 domain details are bound |
| E missing-symbol | exact E005 cross-file dependency detail is bound without fallback |
| F non-finite | a real `NaN` E003 payload renders exactly |
| G conservation/continuity | not applicable to error publication |
| H fail-closed | exhaustive IDs/details permit no alias; only E000 exposes a source |

| Function | Region % | CC | CRAP |
| --- | ---: | ---: | ---: |
| `TcrParseError::fmt` | 100.000 | 10 | 10.000 |
| `TcrParseError::contract_error_id` | 100.000 | 10 | 10.000 |
| `TcrParseError::source` | 100.000 | 3 | 3.000 |

The exact target slice exceeds the 85% glue threshold and 75% function floor;
every slice row is below CRAP 30. Whole-file focused visibility is 317/397
lines (79.849%) and 379/434 regions (87.327%). Uncovered parser, warning,
compatibility, and curve siblings remain outside the slice without waiver.

Artifact SHA-256 values under `L-04-evidence/` are:

- LCOV `5d0076e16a64affdac49662d3b4b559110685c51b7467bce7440ae680ddc3b63`;
- LLVM JSON `604889ddaf292b1c74dbfcd7bccd1242c3655100147ae9df23f6c2b7dfefc1c7`;
- CRAP JSON `c2c26dc368a1c0ca10798346ba820c9a631f88ec8c8dedb08868ea470402a812`.

One focused `cargo llvm-cov` execution produced all three artifacts using
report-only LCOV/JSON generation and `cargo crap` against the retained LCOV.

## Gates, Lines, And Review

| Gate | Result |
| --- | --- |
| `cargo nextest run --test infile_tcr_parser_contract` | PASS, 17/17 |
| focused LCOV/JSON/CRAP | PASS, target metrics above |
| `cargo fmt --all -- --check` | PASS |
| `git diff --check` | PASS |
| production source identity | PASS, unchanged |

`tcr.rs` remains 610 lines. The integration test grows from 328 to 428 lines;
both are below governance thresholds. Exact production identity proves
behavior and numeric equivalence.

Independent module review is complete below. Workspace-wide gates remain
tranche-final.

## Independent Module Review

Verdict: **PASS**.

The review independently reproduced the unchanged production source SHA-256
and all three focused artifact hashes. LLVM JSON reproduces whole-file
visibility of 317/397 lines (79.849%) and 379/434 regions (87.327%). Each exact
slice function has no uncovered LLVM region. Deduplicating identical
per-instantiation CRAP rows reproduces 100% region coverage with CC/CRAP 10/10
for `fmt`, 10/10 for `contract_error_id`, and 3/3 for `source`.

The three-function slice is legitimate and complete for the fixed row. It
publishes stable ID selection, exact payload-bearing detail, and error-source
behavior for every constructible `TcrParseError` variant. The table covers all
nine IDs, including real `NaN` rendering, cross-file dependency detail, curve
operands, the relational invariant, and the rule that only E000 exposes an
error source. No fallback or wildcard display arm exists.

Whole-file line coverage remains below the glue aggregate, and
`apply_cross_file_override` remains below the 75% function floor in this
focused profile. Parser grammar, warning, compatibility, cross-file override,
and curve construction are distinct sibling responsibilities outside this
fixed error-mapping target. L-04 grants them no exception and does not claim
whole-parser or science-contract closure.

Review reruns against the uncommitted module increment passed:

| Review command | Result |
| --- | --- |
| `cargo nextest run --test infile_tcr_parser_contract` | PASS, 17/17 |
| `cargo fmt --all -- --check` | PASS |
| `git diff --check` | PASS |

### Findings And Dispositions

| Finding | Disposition | Evidence |
| --- | --- | --- |
| Whole-file line coverage is 79.849%, below the glue aggregate | `accepted` | The multi-responsibility-host rule permits the exact target slice; all three slice functions are at 100% with CRAP at most 10, while unrelated gaps remain visible. |
| `apply_cross_file_override` is below the 75% floor in focused whole-file evidence | `accepted` | It is outside the error-mapping slice and receives no waiver or closure disposition from L-04. |
| CRAP JSON repeats identical function-instantiation rows | `accepted` | Identity deduplication yields identical source line, CC, coverage, and CRAP values without conflict. |

No finding is deferred, marked follow-up, or left open within the authorized
slice. Classification remains `E-PRODUCTION`: every display arm embeds a
stable `TCR-E-*` identity, independently of whether a current executable
formats every variant. The A-H map is proportional and correctly distinguishes
direct error-publication duties from contextual parser cases. Source identity,
line counts, all nine variant/code/detail branches, source selection, the
85%/75% target-slice gates, CRAP bound, and focused tests are closed.
`MODULE-PASS` authorizes entry into Low tranche-final validation only.
