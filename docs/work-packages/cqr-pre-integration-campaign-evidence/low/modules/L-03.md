# L-03 PMET Error Identity

Status: `MODULE-PASS`

Evidence class: **Ran + Static**

## Scope And Classification

- Production source: `crates/openwepp-input-contract/src/parsers/pmetpara.rs`.
- Source SHA-256 before/after:
  `f4b09d61143b080bfecbaa37fe0b7a75099400069d378c06e50864a2b932dbb1`.
- Tier: glue; target slice is the cohesive public error-mapping surface
  `PmetparaParseError::{fmt,contract_error_id,source}`. Parser behavior is
  contextual consumer evidence, not an expansion of the slice.
- Raw/actionable row: `PmetparaParseError::fmt`, CC 12, 0% regions, CRAP
  156.000, `E-PRODUCTION` because every display arm publishes a `PMET-E-*`
  identity.
- Write set: `tests/integration/infile_pmetpara_parser_contract.rs`, focused
  evidence, this record, and Low progress. Production code is unchanged.

## Cover-First Result

One table-driven public test binds all 11 variants, all nine contract IDs,
exact payload-bearing display strings, and I/O versus non-I/O `Error::source`.
Existing public parser tests bind nominal, strict/compatibility, missing-file,
header, arity, count, tokenization, range, duplicate-key, and lookup outcomes.

| Obligation family | Binding |
| --- | --- |
| A nominal in-domain | not directly applicable to error mapping; the canonical strict parse remains contextual accepted-input evidence |
| B boundary/near-threshold | the formatter performs no threshold decision; crop-key width and record-count parser cases retain contextual boundary evidence |
| C branch | all 11 error variants execute through `pmetpara_error_display_preserves_all_contract_identities` |
| D domain-reject | exact E003 range and duplicate-key details are bound by the table and parser selections |
| E missing-symbol | required missing-sidecar E007 and missing-crop E005 identities are bound without fallback |
| F non-finite | no numeric decision occurs in the slice; token/range identity publication is exhaustive |
| G conservation/continuity | not applicable to error identity, detail, or source publication |
| H fail-closed | exhaustive IDs and exact strings permit no alias; only E000 exposes an error source and every other variant proves `None` |

| Function | Region % | CC | CRAP |
| --- | ---: | ---: | ---: |
| `PmetparaParseError::fmt` | 100.000 | 12 | 12.000 |
| `PmetparaParseError::contract_error_id` | 100.000 | 10 | 10.000 |
| `PmetparaParseError::source` | 100.000 | 3 | 3.000 |

The exact target slice exceeds the 85% glue threshold and 75% function floor,
and every slice row is below CRAP 30. Whole-file focused visibility is 375/477
lines (78.616%) and 445/519 regions (85.742%). The line gap belongs to parser,
normalization, warning, and compatibility siblings outside this target slice;
this module claims no closure or waiver for those responsibilities.

Artifact SHA-256 values under `L-03-evidence/` are:

- LCOV `0eab9e9a18457cb2e0621b3335a83fa6461fb0aee575dac8593f573d96cdf55f`;
- LLVM JSON `eaf4dd57f0a126d7a348a7e0788bca187dd8d55a1df8d560a353d8f3c495026e`;
- CRAP JSON `7cfa4e43641fb7c086d573bb234b6aeadb2a606089e584e97f2bc3565b297e91`.

One `cargo llvm-cov --workspace --test infile_pmetpara_parser_contract
--no-report` execution produced all three artifacts through report-only
LCOV/JSON commands for `openwepp` plus `openwepp-input-contract` and
`cargo crap --workspace` against the retained LCOV.

## Gates, Lines, And Review

| Gate | Result |
| --- | --- |
| `cargo nextest run --test infile_pmetpara_parser_contract` | PASS, 14/14 |
| native-forest PMET production consumer | PASS, 1/1 |
| focused LCOV/JSON/CRAP | PASS, target metrics above |
| `cargo fmt --all -- --check` | PASS |
| `git diff --check` | PASS |
| production source identity | PASS, unchanged |

`pmetpara.rs` remains 712 lines. The integration test grows from 212 to 325
lines; both are below governance thresholds. Exact production identity proves
behavior and numeric equivalence.

Independent module review is complete below. Workspace-wide gates remain
explicitly tranche-final and are not claimed here.

## Independent Module Review

Verdict: **PASS**.

The review independently reproduced the unchanged production source SHA-256
and all three focused artifact hashes. LLVM JSON reproduces whole-file
visibility of 375/477 lines (78.616%) and 445/519 regions (85.742%). Each exact
slice function has no uncovered LLVM region. Deduplicating identical
per-instantiation CRAP rows reproduces 100% region coverage with CC/CRAP 12/12
for `fmt`, 10/10 for `contract_error_id`, and 3/3 for `source`.

The three-function slice is legitimate. It wholly covers stable ID selection,
payload-bearing rendering, and error-source publication for all 11 variants
and nine IDs. Token/arity share E001 and range/duplicate share E003; the table
proves their distinct details while retaining the intentional shared IDs. Only
the I/O variant exposes a source. Static inspection also confirms that
`parse_legacy_pmetpara_sidecar` publishes `PmetparaParseError::to_string()`
through the real `HillslopeCliError::ParseFailure.detail` path.

Whole-file line coverage is below the glue threshold, and several parser
siblings are below the 75% function floor in this focused profile, including
lookup, declared-count parsing, record parsing, and scalar token parsing. They
belong to separate grammar, normalization, lookup-policy, warning, and
compatibility responsibilities outside this fixed formatter target. L-03
grants them no exception and claims no closure for the draft
`SC-INFILE-PMETPARA-001` HOLD gaps.

Review reruns against the uncommitted module increment passed:

| Review command | Result |
| --- | --- |
| `cargo nextest run --test infile_pmetpara_parser_contract` | PASS, 14/14 |
| named `native_forest_hillslope_runs_end_to_end_with_pmet_hit_and_reconciliation` selection | PASS, 1/1 |
| `cargo fmt --all -- --check` | PASS |
| `git diff --check` | PASS |

### Findings And Dispositions

| Finding | Disposition | Evidence |
| --- | --- | --- |
| Whole-file line coverage is 78.616%, below the glue aggregate | `accepted` | The campaign's multi-responsibility-host rule permits the exact target slice; all three slice functions are at 100% with CRAP at most 12, and unrelated sibling gaps remain visible. |
| Multiple parser siblings remain below the 75% floor | `accepted` | They are outside the selected error-mapping slice and receive no waiver or closure claim from L-03. |
| The PMET science contract retains draft HOLD gaps | `accepted` | This test-only module binds error identity/detail/source behavior and does not disposition parser grammar, fixed-width, or fallback-policy authority. |
| CRAP JSON repeats identical function-instantiation rows | `accepted` | Identity deduplication yields identical source line, CC, coverage, and CRAP values with no conflicting metric. |

No finding is deferred, marked follow-up, or left open within the authorized
L-03 slice. Classification remains `E-PRODUCTION`: every display arm publishes
a stable `PMET-E-*` identity, and the runner consumes the rendered detail. The
normative A-H table correctly distinguishes directly applicable error-mapping
branches from contextual parser behavior. Source identity, line counts, the
85%/75% target-slice gates, CRAP bound, all 11 variants, source-selection
behavior, and the nominal production consumer are closed. `MODULE-PASS`
authorizes entry into Low tranche-final validation only.
