# L-05 WEPP-UI Error Identity

Status: `MODULE-PASS`

Evidence class: **Ran + Static**

## Scope And Classification

- Production source: `crates/openwepp-input-contract/src/parsers/wepp_ui.rs`.
- Source SHA-256 before/after:
  `dc63e32c219d842ff4e92ed1d04c68315b974331464d7fd3609a02bb28d4ae2a`.
- Tier: glue; target slice is
  `WeppUiParseError::{fmt,contract_error_id,source}`.
- Raw/actionable row: `WeppUiParseError::fmt`, CC 6, 0% regions, CRAP 42.000,
  `E-PRODUCTION` because its details publish stable `WUI-E-*` identities into
  the hourly-mode intake path.
- Production code is unchanged; only public contract tests and evidence change.

## Cover-First Result

One table-driven public test binds all five variants and IDs, exact
payload-bearing display strings, open-result identity, and I/O versus non-I/O
`Error::source`. Existing tests bind nominal, missing, compatibility collapse,
payload, soil-version, and mode-closure outcomes.

| Obligation family | Binding |
| --- | --- |
| A nominal in-domain | valid empty-sentinel hourly mode is contextual nominal evidence |
| B boundary/near-threshold | the 7778 version threshold remains bound by parser tests, outside the formatter decision surface |
| C branch | all five variants execute through `wepp_ui_error_display_preserves_all_contract_identities` |
| D domain-reject | exact E001 payload and E002 soil-version details are bound |
| E missing-symbol | exact E004 missing-version-surface detail is bound |
| F non-finite | no numeric validity decision occurs in the formatter slice |
| G conservation/continuity | not applicable to error identity, detail, or source publication; E003 mode-selection closure remains covered under C and H |
| H fail-closed | exhaustive IDs/details permit no alias; only E000 exposes a source |

| Function | Region % | CC | CRAP |
| --- | ---: | ---: | ---: |
| `WeppUiParseError::fmt` | 100.000 | 6 | 6.000 |
| `WeppUiParseError::contract_error_id` | 100.000 | 6 | 6.000 |
| `WeppUiParseError::source` | 100.000 | 3 | 3.000 |

Whole-file focused visibility is 156/171 lines (91.228%) and 181/193 regions
(93.782%), exceeding the 85% glue threshold. Every eligible target function
exceeds the 75% floor and remains below CRAP 30.

Artifact SHA-256 values under `L-05-evidence/` are:

- LCOV `622b27d1e3d5a14cae9aa33c1f9f38183efdf3d3af1ab9aebd8e3b0e8f7867b4`;
- LLVM JSON `7e1e45ac498c7233c507e6e3e8e608f083ed3dec91cd28c4977450573c349538`;
- CRAP JSON `05c857321fb0e003dd8087ba44e86a890482713fe4b85e66a85cc5089989c858`.

One focused `cargo llvm-cov` execution produced all three artifacts using
report-only LCOV/JSON generation and `cargo crap` against the retained LCOV.

## Gates, Lines, And Review

| Gate | Result |
| --- | --- |
| `cargo nextest run --test infile_weppui_parser_contract` | PASS, 12/12 |
| runner WEPP-UI mode-closure consumer | PASS, 1/1 |
| focused LCOV/JSON/CRAP | PASS, metrics above |
| `cargo fmt --all -- --check` | PASS |
| `git diff --check` | PASS |
| production source identity | PASS, unchanged |

`wepp_ui.rs` remains 311 lines. The integration test grows from 251 to 307
lines; both are below governance thresholds. Exact production identity proves
behavior and numeric equivalence.

Independent module review is complete below. Workspace-wide gates remain
tranche-final.

## Independent Module Review

Verdict: **PASS**.

The review independently reproduced the unchanged production source SHA-256
and all three focused artifact hashes. LLVM JSON reproduces whole-file
visibility of 156/171 lines (91.228%) and 181/193 regions (93.782%). Each exact
slice function has no uncovered LLVM region. Deduplicating identical
per-instantiation CRAP rows reproduces 100% region coverage with CC/CRAP 6/6
for `fmt`, 6/6 for `contract_error_id`, and 3/3 for `source`.

The three-function slice is legitimate and complete for the fixed row. The
table covers all five IDs, path and byte-count publication, the soil-version
threshold operands, requested/effective/open-result mode identity, the missing
surface reason, and the rule that only E000 exposes an error source. Static
inspection confirms that `parse_wepp_ui_sidecar` publishes the rendered detail
through `HillslopeCliError::ParseFailure` on the real runner intake path.

Whole-file aggregate metrics also exceed the glue threshold. Untested warning
code formatting and default-construction siblings remain outside this exact
error slice and below the focused function floor; L-05 grants them no exception
or closure disposition. Parser behavior is retained as contextual consumer
evidence rather than silently expanding the selected slice.

Review reruns against the uncommitted module increment passed:

| Review command | Result |
| --- | --- |
| `cargo nextest run --test infile_weppui_parser_contract` | PASS, 12/12 |
| named `simimpl04_contract_requires_wepp_ui_requested_effective_lane_closure_manifest` runner selection | PASS, 1/1 |
| `cargo fmt --all -- --check` | PASS |
| `git diff --check` | PASS |

### Findings And Dispositions

| Finding | Disposition | Evidence |
| --- | --- | --- |
| The producer mapped E003 mode closure to normative family G | `accepted-fixed` | Family G is conservation/continuity and is not applicable here; E003 remains exhaustively bound under branch and fail-closed families C and H. |
| Warning/default siblings remain below the focused function floor | `accepted` | They are outside the selected error-mapping slice, remain visible in whole-file artifacts, and receive no waiver from L-05. |
| CRAP JSON repeats identical function-instantiation rows | `accepted` | Identity deduplication yields identical source line, CC, coverage, and CRAP values without conflict. |

No finding is deferred, marked follow-up, or left open within the authorized
slice. Classification remains `E-PRODUCTION`: every formatter arm publishes a
stable `WUI-E-*` identity into the runner-visible error surface. Source
identity, line counts, all five variant/code/detail/source branches, the
85%/75% target-slice gates, CRAP bound, focused suite, and nominal runner
consumer are closed. `MODULE-PASS` authorizes entry into Low tranche-final
validation only.
