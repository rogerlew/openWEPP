# L-10 CoE Melt Forcing And Canopy Parsers

Status: `MODULE-PASS`

Evidence class: **Ran + Static**

## Scope And Classification

- Production source: `crates/openwepp-runner/src/hillslope/snowbench_coe_melt.rs`.
- Pre-module source SHA-256 and unchanged 926-line production-prefix SHA-256:
  `d754099ca9c3b4ed1b93c20546e7ec9d12b25b34288ca8761d27cef0e5f32487`.
- Post-module whole-file SHA-256, including appended tests:
  `835578a3ed8133a326e2fdfe18baf94cc7b70881219cc74adfa3e217781e5429`.
- Tier: science, because `parse_forcing_line` performs rain/snow water and
  depth partition, radiation conversion, cloud inversion, and dew-point
  projection. `read_canopy_series` is production-tier grammar/state intake.
- Raw rows: `read_canopy_series`, CC 13, 45.000%, CRAP 41.117
  (`E-PRODUCTION`); `parse_forcing_line`, CC 21, 59.341%, CRAP 50.643
  (`E-SCIENCE`).
- Canonical authority remains `SC-SNOWFREEZE-001`; tests bind existing guards
  and projections without changing equations, constants, units, or runtime
  control flow, so no contract amendment is required.

## Cover-First Result

Five private unit tests bind canopy I/O, header, arity, blank-row, numeric,
sequence, finite, unit-interval, duplicate, nonempty, and accepted-map behavior;
and forcing column count, timestamp/hour, numeric, finite, fraction, sign,
density, temperature, longwave, vapor-pressure, and accepted conversion
behavior. Three existing CoE replay and daily-canopy integration tests execute
the real downstream consumer through four model/replay paths; three additional
static integration tests bind the governing contract, package, and direct-path
source markers.

| Obligation family | Binding |
| --- | --- |
| A nominal in-domain | ordered canopy endpoints and a complete forcing row bind accepted outputs; three real replay tests execute four downstream model/replay paths |
| B boundary/near-threshold | canopy 0/1, hour 0/24, fraction above 1, density 0, and absolute-zero temperature boundary are exercised |
| C branch | all top-level parser guards and accepted branches execute; eight LLVM error-propagation regions remain uncovered at seven field-specific `parse_column(...)?` call sites and the shared unit-interval finite-check propagation site |
| D domain-reject | negative radiation/wind/precipitation, nonpositive density, invalid longwave denominator, and invalid vapor pressure fail closed |
| E missing-symbol | missing canopy file, header, rows, date uniqueness, and sequential index obligations fail closed |
| F non-finite | `NaN` numeric input is rejected by the shared finite guard before publication |
| G conservation/continuity | accepted precipitation reconstructs 0.5 mm rain plus 0.5 mm snow water from 1 mm total, with snow depth and radiation unit conversion asserted |
| H fail-closed | malformed inputs return typed `InvalidForcing`/I/O errors; no default, clamp of invalid source state, or partial row is published |

| Function | LCOV/CRAP coverage % | LLVM regions | CC | CRAP |
| --- | ---: | ---: | ---: | ---: |
| `read_canopy_series` | 100.000 | 87/87 (100.000%) | 13 | 13.000 |
| `parse_forcing_line` | 100.000 | 121/128 (94.531%) | 21 | 21.000 |
| `parse_column` | 100.000 | 14/14 (100.000%) | 3 | 3.000 |
| `require_finite` | 100.000 | 8/8 (100.000%) | 2 | 2.000 |
| `require_unit_interval` | 100.000 | 14/15 (93.333%) | 3 | 3.000 |
| `cloud_fraction_from_longwave` | 100.000 | 16/16 (100.000%) | 4 | 4.000 |
| `dewpoint_from_vapor_pressure` | 100.000 | 12/12 (100.000%) | 3 | 3.000 |
| `snowbench_io` | 100.000 | 4/4 (100.000%) | 1 | 1.000 |

The complete exact/transitive slice is 276/284 LLVM regions (97.183%) and
exceeds the science-tier 90% aggregate and 75% function floor; every listed
function also remains below the CRAP 30 limit. Whole-file focused visibility is
416/833 lines (49.940%) and 509/1,032 regions (49.322%); simulation, model
selection, grouping, and writers are separate responsibilities and receive no
waiver or closure claim here.

Artifact SHA-256 values under `L-10-evidence/` are:

- LCOV `13f61514efb12ce8202f0c23c80d052d278eccf739c627303057145d8a913076`;
- LLVM JSON `8182761a7922e138a7fad4a72f8596bf5d2629160a8cca2a3602053d36bdaa08`;
- CRAP JSON `9f6c8f5e9a510362fe17e649995df0962e77d159df81bf1f2e0c5891948d85f8`.

One package-lib `cargo llvm-cov nextest` execution selected the five L-10 tests
and produced all artifacts through report-only commands and package-scoped
`cargo crap`.

## Gates, Lines, And Review

| Gate | Result |
| --- | --- |
| focused L-10 parser matrix | PASS, 5/5 |
| real CoE replay/canopy consumers | PASS, 3/3 named runtime tests covering four model/replay paths |
| static authority/direct-path bindings | PASS, 3/3 |
| focused LCOV/JSON/CRAP | PASS, exact slice 276/284 LLVM regions (97.183%); LCOV/CRAP rows at 100% |
| `cargo fmt --all -- --check` | PASS |
| `git diff --check` | PASS |
| production-prefix identity | PASS, unchanged |

The file grows from 926 to 1,123 lines solely through a terminal `#[cfg(test)]`
module and remains below the 2,000-line warning threshold. Exact production
prefix identity proves behavior, constants, equations, and numeric publication
are unchanged.

## Independent Review

Review A independently reproduced the production-prefix and artifact hashes,
the whole-file line/region totals, the exact/transitive function set, and the
CRAP rows. It also inspected all appended tests, the SC-SNOWFREEZE governance
boundary, and the real runtime consumers. The review corrected the producer's
100% slice claim to the authoritative LLVM region result and separated three
runtime consumer tests from three static integration bindings. These are
evidence-description defects, not implementation blockers: the corrected slice
still clears every science-tier threshold, the runtime tests exercise both CoE
models and both canopy replay purposes, and the unchanged production prefix
means no science-contract amendment is required for this test-only module.

Independent disposition: `MODULE-PASS`. Workspace-wide gates remain
tranche-final.
