# L-07 Sidecar Adapter Error Identity

Status: `MODULE-PASS`

Evidence class: **Ran + Static**

## Scope And Classification

- Production source: `crates/openwepp-legacy-bridge/src/sidecar.rs`.
- Source SHA-256 before/after:
  `b53dbcb4eeac0dabcf3ed3464debd6be2ce988b6a0225fe8c150e2c048ed14b7`.
- Tier: glue; target slice is `SidecarAdapterError::{fmt,code}` plus transitive
  identity accessor `SidecarId::as_str`.
- Raw/actionable row: `SidecarAdapterError::fmt`, CRAP 43.172,
  `E-PRODUCTION` because its `LSB-E-*` details are consumed by runner errors.
- Production code is unchanged; a public integration test and evidence are the
  only increment.

## Cover-First Result

One table-driven public test binds all eight variants and codes, exact operand
rendering, typed sidecar identifiers, and the absence of nested sources. Four
existing integration selections prove required-sidecar strict/compat failure,
unknown-discovery compatibility behavior, and runner error handoff.

| Obligation family | Binding |
| --- | --- |
| A nominal in-domain | formatter is error-only; adapter nominal behavior remains separate contextual evidence |
| B boundary/near-threshold | no numeric threshold occurs in this slice |
| C branch | all eight variants execute through the public exhaustive table |
| D domain-reject | invalid ID/name and all duplicate forms publish exact operands |
| E missing-symbol | E007 binds required sidecar identity and canonical filename |
| F non-finite | not applicable to identifier/file error publication |
| G conservation/continuity | not applicable to error publication |
| H fail-closed | strict alias E008 and runner handoff are bound; all variants lack nested sources |

| Function | Region % | CC | CRAP |
| --- | ---: | ---: | ---: |
| `SidecarAdapterError::fmt` | 100.000 | 9 | 9.000 |
| `SidecarAdapterError::code` | 100.000 | 9 | 9.000 |
| `SidecarId::as_str` | 100.000 | 1 | 1.000 |

The exact target slice exceeds the 85% glue threshold and 75% function floor;
all rows are below CRAP 30. Whole-file focused visibility is 66/237 lines
(27.848%) and 93/313 regions (29.712%) because the one-test profile isolates
error identity. Adapter resolution, validation, warning, and discovery siblings
remain outside the slice without waiver and retain separate consumer evidence.

Artifact SHA-256 values under `L-07-evidence/` are:

- LCOV `0e7171623cee5b5f85664b6824eed8a5e168ed79c96fb08eb3d0aa9d1d5564d9`;
- LLVM JSON `8739366dcb6a48787802006e2b40fbeb347d8464a11a97e1450583b89a0183a0`;
- CRAP JSON `d284737ed94e799dfe7492d5095dc7f62dd3393ab982de44a29ce27274da01d5`.

One package-scoped `cargo llvm-cov --test sidecar_error_contract --no-report`
execution produced all three artifacts through report-only commands and
package-scoped `cargo crap`.

## Gates, Lines, And Review

| Gate | Result |
| --- | --- |
| public sidecar error contract | PASS, 1/1 |
| adapter and runner consumers | PASS, 4/4 |
| focused LCOV/JSON/CRAP | PASS, target metrics above |
| `cargo fmt --all -- --check` | PASS |
| `git diff --check` | PASS |
| production source identity | PASS, unchanged |

`sidecar.rs` remains 659 lines and the new integration test is 77 lines, below
governance thresholds. Exact production identity proves behavior and numeric
equivalence.

Independent module review is complete below. Workspace-wide gates remain
tranche-final.

## Independent Module Review

Verdict: **PASS**.

The review independently reproduced the unchanged production source SHA-256
and all three focused artifact hashes. LLVM JSON reproduces the deliberately
narrow whole-file visibility of 66/237 lines (27.848%) and 93/313 regions
(29.712%). Each exact slice function has no uncovered LLVM region. CRAP JSON
reproduces 100% region coverage with CC/CRAP 9/9 for `fmt`, 9/9 for `code`, and
1/1 for transitive identity accessor `SidecarId::as_str`.

The three-function slice is exact and legitimate. The public table executes
all eight error variants and stable codes, every ID/file/alias operand, the
transitive typed-ID rendering, and the invariant that none exposes a nested
source. No wildcard or fallback formatter branch exists. Static runner
inspection and the selected CLI error test confirm that `LSB-E-007` survives
inside the public `CLIHILL-E-008` error and retains the adapter as its source.

Adapter contract preparation, discovery mapping, binding resolution, warning
publication, and normalization are distinct sibling responsibilities outside
the isolated capture. The artifact keeps their uncovered CRAP rows visible;
L-07 grants them no waiver. The separately rerun strict missing, compatibility
missing, compatibility unknown-discovery, and runner error-handoff selections
provide the proportional real-consumer evidence without expanding the target
slice.

Review reruns against the uncommitted module increment passed:

| Review command | Result |
| --- | --- |
| `cargo nextest run -p openwepp-legacy-bridge --test sidecar_error_contract` | PASS, 1/1 |
| four named adapter/runner `cli01_runner_contract_derived_tests` selections | PASS, 4/4 |
| `cargo fmt --all -- --check` | PASS |
| `git diff --check` | PASS |

### Findings And Dispositions

| Finding | Disposition | Evidence |
| --- | --- | --- |
| Whole-file focused coverage is intentionally 27.848% lines / 29.712% regions | `accepted` | The multi-responsibility-host rule permits the exact error-publication slice; all three slice functions are at 100%, and sibling gaps remain visible without exception. |
| Adapter helpers remain uncovered and include CRAP rows above 30 in this isolated profile | `accepted` | They are not fixed L-07 identities, receive no waiver/disposition here, and retain separate real-consumer and tranche-final accounting. |

No finding is deferred, marked follow-up, or left open within the authorized
slice. Classification remains `E-PRODUCTION`: every display branch publishes a
stable `LSB-E-*` identity, and the runner preserves the nested adapter code and
source. The A-H map is proportional to this identifier/file error surface.
Source identity, line counts, all eight code/detail/source branches, transitive
ID accessor, the 85%/75% target-slice gates, CRAP bound, and all four consumers
are closed. `MODULE-PASS` authorizes entry into Low tranche-final validation
only.
