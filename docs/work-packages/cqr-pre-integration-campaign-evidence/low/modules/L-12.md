# L-12 Watershed Network Frame Error Identity

Status: `MODULE-PASS`

Evidence class: **Ran + Static**

## Scope And Classification

- Production source:
  `crates/openwepp-watershed-orchestrator/src/lib_mod/network_frame.rs`.
- Source SHA-256 before/after:
  `534381932420245750ec9ac032a750479a8921fbda99491f0e636c6e1ce2b79b`.
- Tier: glue; target slice is `WatershedNetworkFrameError::{fmt,source}`.
  Frame construction, routing, projection, and `From` conversion are siblings.
- Raw/actionable row: `WatershedNetworkFrameError::fmt`, CC 11, 21.053%
  coverage, CRAP 70.539, `E-PRODUCTION` because direct watershed execution
  publishes these stable `WSHEDFRAME-E-*` identities. The provisional Low-plan
  value 90.246 was superseded by the reviewed Medium-final metric.
- Production code is unchanged; a public crate integration test and evidence
  are the only increment.

## Cover-First Result

One table-driven public test binds all ten variants and codes, exact topology,
channel, impoundment, slope, runtime-input, groundwater, and terminal operands,
real `NaN` rendering, and the one-source/nine-no-source partition. Four existing
typed watershed integration selections prove fail-closed and nominal frame
consumption through real dispatch.

The four named consumer selections are
`wshedw10_not_applicable_chaninp_cannot_mask_required_channel_sidecar`,
`typed_frame_dispatch_records_and_publishes_direct_routed_state`,
`typed_frame_dispatch_fails_closed_on_non_finite_hillslope_payload`, and
`typed_frame_dispatch_fails_closed_on_impoundment_domain_violation`.

| Obligation family | Binding |
| --- | --- |
| A nominal in-domain | direct typed-frame dispatch and publication pass through the real consumer |
| B boundary/near-threshold | typed range/cardinality operands and negative/NaN publication operands are rendered exactly |
| C branch | all ten display variants and both source branches execute |
| D domain-reject | runtime-input domain, invalid groundwater, and terminal publication identities are bound |
| E missing-symbol | missing chan.inp options, slope, routed channel, and routed impoundment state identities are bound |
| F non-finite | real `NaN` groundwater authority rendering remains explicit |
| G conservation/continuity | nominal direct routed-state publication executes; formatter itself performs no conservation decision |
| H fail-closed | chan.inp readiness and runtime domain consumer tests fail closed; only nested runtime input exposes a source |

| Function | LCOV/CRAP coverage % | LLVM regions | CC | CRAP |
| --- | ---: | ---: | ---: | ---: |
| `WatershedNetworkFrameError::fmt` | 100.000 | 48/48 | 11 | 11.000 |
| `WatershedNetworkFrameError::source` | 100.000 | 6/6 | 3 | 3.000 |

The exact target slice is 54/54 LLVM regions and exceeds the 85% glue
threshold, 75% function floor, and CRAP 30 limit. Whole-file focused visibility
is 43/790 lines (5.443%) and 54/806 regions (6.700%) because the one-test profile
isolates error publication in a multi-responsibility frame host. All routing,
construction, and projection siblings remain out of slice without waiver.

Artifact SHA-256 values under `L-12-evidence/` are:

- LCOV `03141e0bda224a234580b6b88d40458d1b540210415321c68129b35afea7febf`;
- LLVM JSON `2e1ee920663a2048b4f6831b23ec1a7d3306f9487d29f48dd49843a650b9e716`;
- CRAP JSON `ede896a69117a02f69c74692e882ce96fcbfd8b0fe4df4f24979134ab75df4ef`.

One package-scoped `cargo llvm-cov --test network_frame_error_contract
--no-report` execution produced all artifacts through report-only commands and
package-scoped `cargo crap`.

## Gates, Lines, And Review

| Gate | Result |
| --- | --- |
| public network-frame error contract | PASS, 1/1 |
| real typed-frame consumers | PASS, 4/4 |
| focused LCOV/JSON/CRAP | PASS, exact slice 54/54 LLVM regions |
| `cargo fmt --all -- --check` | PASS |
| `git diff --check` | PASS |
| production source identity | PASS, unchanged |

`network_frame.rs` remains 1,998 lines and is not enlarged. The new integration
test is 87 lines. Exact production identity proves behavior and numeric
equivalence while avoiding the 2,000-line warning boundary.

## Independent Review

Review A independently inspected the public test and all ten formatter/source
cases, reproduced the unchanged 1,998-line source hash and all three artifact
hashes, and recomputed the whole-host and exact-slice LLVM counts. The one-test
coverage profile executes only `WatershedNetworkFrameError::{fmt,source}` in
this multi-responsibility host: both functions are 100%, the slice is 54/54
regions, and unrelated construction, conversion, routing, kernel, and
publication functions remain visibly uncovered rather than being waived.

The review reran the public contract and the four named real typed-frame
consumers (5/5 combined). It also corrected the stale provisional CRAP value in
this record to the authoritative reviewed raw value, 70.539. The new test uses
only the crate's public API, production source is byte-identical, and no private
test hook or host growth was introduced.

Independent disposition: `MODULE-PASS`. Workspace-wide gates remain
tranche-final.
