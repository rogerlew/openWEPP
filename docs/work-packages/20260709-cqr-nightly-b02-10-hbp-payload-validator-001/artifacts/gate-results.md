# Gate Results

Status: queued.

Record exact commands, exit codes, and evidence paths for:

- `git diff --check`
- markdown/doc lint for touched docs
- focused tests
- `cargo fmt --check`
- `cargo clippy --workspace --all-targets -- -D warnings`
- `cargo nextest run --workspace --profile full`
- `cargo deny check`

