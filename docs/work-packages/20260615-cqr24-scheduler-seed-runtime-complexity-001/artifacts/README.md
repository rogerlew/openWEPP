# CQR24 Artifacts

Artifact set for CQR24. Evidence entries must label `Static:` versus `Ran:`.

Status: complete pending package commit/push and tracker update.

Static: package artifacts cover the CQR24 WB16 ealpha producer decomposition in
`crates/openwepp-runner/src/hillslope/scheduler_trace/scheduler_seed_and_runtime.rs`.

Ran: before LCOV and CRAP are archived as `lcov_before.info` and
`crap_before.json`.

Ran: after LCOV and CRAP are archived as `lcov_after.info` and
`crap_after.json`.
