# CQR24 Kickoff Prompt

Execute CQR24 per `docs/work-packages/cqr-burndown-execplan.md`, preserving
scheduler seed/runtime public and kernel-facing behavior while reducing the
live target and new helper CRAP values to `<= 30`.
