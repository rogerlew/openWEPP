# Owned File Manifest

Status: `PASS / terminal path set within authorization`

Evidence mode: `Static + Ran`

The authoritative bounded write set is the `Intended Write Set` in
`package.md`. Ran `git diff --name-only` and
`git ls-files --others --exclude-standard` after scientific remediation. Every
path maps to one authorized class:

1. package documentation/artifacts/prompt lifecycle;
2. reference bibliography, tracked rights metadata, affirmatively vendorable
   corpus, or gitignored restricted corpus;
3. canonical science contracts/index and bounded adjacent ownership handoffs;
4. contract-derived tests, fixtures, authority-suite bindings, and independent
   package-local reference calculator;
5. roadmap/backlog/catalog/lifecycle records; or
6. prospective whole-state implementation successor amendment/scaffold.

| Class | Exact terminal path surface |
|---|---|
| package evidence/prompt | entire `20260811-coupled-c3-forest-vegetation-model-stack-authority-001/` tree |
| canonical authority | `SC-VEGETATION-001.md`, new `SC-BIOGEOCHEM-001.md`, contract `index.md` |
| contract-derived test | `tests/integration/vegetation_boundary_authority_contract.rs` |
| reference metadata | `references/annotated_bibliography.md`; restricted full text remains gitignored |
| lifecycle | `docs/ROADMAP.md`, both backlog files, work-package catalog, and the held 20260808 package |
| prospective successors | whole coupled-state implementation package and soil-biogeochemistry transformation package |

No `crates/**`, runtime selector, production consumer, deployment, or
publication path appears in the diff. The path gate is `PASS`.
