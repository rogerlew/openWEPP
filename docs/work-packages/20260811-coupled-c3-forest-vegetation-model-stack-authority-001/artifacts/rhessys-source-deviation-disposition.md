# Rhessys Source Deviation Disposition

Status: `complete`

Evidence mode: `Static`

| Observed RHESSys/GIS behavior | v1 disposition |
|---|---|
| parser-only defaults and profile omissions | `REJECTED_SOURCE_BEHAVIOR`; all consumed fields required |
| mutable network acquisition | rejected; immutable local bytes plus tuple/digest only |
| ignored optical fields/diffuse extinction | rejected; two-stream consumes explicit band/direction optics |
| Jarvis stress chain and conductance floor | rejected; FvCB-linked Medlyn, caller `g0/g1`, no floor |
| hard-coded C3 applied to every profile | rejected; typed lifeform admits only C3 woody and rejects all others |
| defective Penman-Monteith psychrometric expression | rejected; explicit conductance/energy network selected |
| negative-energy assignment and longwave deletion | rejected; signed component energy residuals close independently |
| single-depth root split/direct patch mutation | rejected; interval-equilibrium nodes and hydrology-owned layer arbitration |
| fixed growth-respiration iteration | rejected; finite safeguarded coupled solver and rollback |
| generator-synthesized initial pools/ratios | rejected; complete caller state required |
| stratum/source topology | retained as licensed architecture/format provenance after canonical validation |
| C/N pool naming and transfer sequencing | comparator/reference implementation evidence only; CLM5/BIOME-BGC equations govern |

Pinned commits and MIT license digests are recorded in package intake artifacts.
