# Finding disposition

Ran: pre-review implementation findings are resolved or bounded.

| Finding | Disposition |
| --- | --- |
| Collector process-wall endpoint initially followed receipt validation | Corrected to the immediate child return. The interrupted and pre-lint runs remain only in scratch and are not package evidence; final series-03 was rerun from a newly built frozen executable. |
| Three warnings-denied LSE observer lint findings | Corrected in isolated P (`linear_result` temporaries and `#[must_use]` guard), rebuilt, retested, and fully remeasured. |
| 49.24% runner remainder is unresolved | Retained under its honest name. It qualifies architecture ranking and is not assigned an invented meaning. |
| Sampling refinement unavailable | Bounded: `perf_event_paranoid=4` denied events. No install/privilege campaign. |
| Reduced-system assembly priority | Decided: 1.268% impossible perfect ceiling is below the owner's 5% priority rule; deprioritized. |
| COST-A01: 31.85% bucket was over-labeled as reusable covered-carrier physics | Corrected: it aggregates NativeVegetationEt, terminal provider carrier, carrier physical setup/evidence/completion, and imported physical/frozen envelopes. It is an inclusive upper bound; duplicate/reusable work and achievable saving are UNMEASURED. Recommendation changed to a typed single-pass boundary experiment whose first gate proves actual same-input recomputation. |
| COST-A02: 1.268% was worded as a universal reduced-system ceiling | Corrected: 1.268% applies only to assembly+linear. The disjoint inactive-probe bucket is 1.312%; their impossible full-bucket union is 2.579% (2.572–2.585%). Mapping/reconstruction and other effects remain unmeasured. |
| COST-B01: stale rounded table/range values | Corrected from final series-03 operands: assembly+linear range 1.261–1.270%, inactive 1.312%, and remaining small solver sum 0.749%. |
| COST-A03: two audit-unit logs were mislabeled as source-real solver/error evidence | Corrected labels and limits. Added existing source-real Covered natural-failure and Potential/FixedFinal nonmutation tests. Those focused tests run with compact session inactive; final actual runner series supplies valid observer off/on evidence across all participating families/passes. |
| Terminal custody portability | Analyzer now prefers a retained sibling log and records receipt basenames. This changes only path metadata; reconstructed values are identical. Analyzer/result/bundle hashes were refreshed. |
| Runner/orchestrator warnings-denied lint applicability | Initial run exposed only inherited dead-code and too-many-lines findings. Bounded rerun passed with `-D warnings` and explicit allowances for exactly those two pre-existing classes; both logs retained. |

Independent review findings will be appended and re-reviewed before disposition.
