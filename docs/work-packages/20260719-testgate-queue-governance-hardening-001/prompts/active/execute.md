# Execute TESTGATE-QUEUE-01

Execute `package.md` end to end. Clear only the three authorized stranded runs,
implement permanent single-pending queue governance and stale-head rejection,
run only focused validation, obtain the authorized dual independent review and
verification, push once while normal TESTGATE is deliberately paused, restore
the workflow, and close the package without a broad presentation run.

Subagent requirement: REQUIRED. This prompt explicitly authorizes subagent
spawning/delegation to the two read-only reviewer/verifier roles named in
`package.md`; their outputs are compact findings and terminal verdicts.
