# Coverage After

Focused artifacts:

- LCOV: `/tmp/openwepp-cqr-b02-t01-final.lcov`
- JSON regions: `/tmp/openwepp-cqr-b02-t01-final.json`

Whole target LCOV is `1167 / 1167`; that denominator includes the new test module
and is reported only for reproducibility. The ADR-0021 eligible production-only
calculation excludes lines after the pre-test production boundary (line `1131`):
`603 / 603` lines (`100%`) and `628 / 628` regions (`100%`).
