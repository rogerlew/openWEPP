# TESTGATE Accelerated Cutover

Execute this package from [package.md](package.md). The active kickoff prompt is
under `prompts/active/`; evidence and review records belong under `artifacts/`.

This package replaces elapsed-time observation with an event-driven path from
the `omarchy` self-hosted runner through CRAP cleanup, adversarial patch review,
acceptance, and immediate cutover.
