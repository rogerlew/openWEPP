# Verification Agent A

Status: complete.

Evidence class: Ran + Static.

Checks:

- Confirmed Harvard and Marcell fixture directories contain only WEPP inputs,
  sidecars, and manifests; no installed stratum observation tables.
- Confirmed `.man` lines identify both as mixed forest and seed initial
  `cancov = 0.55`.
- Confirmed 10.3.1 runtime evidence reports `canopy_cover_fraction = 0.55` for
  both fixtures.

Result: PASS.
