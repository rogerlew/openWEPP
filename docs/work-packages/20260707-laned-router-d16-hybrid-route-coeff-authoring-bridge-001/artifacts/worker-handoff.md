# Worker Handoff

Status: EXECUTED-HOLD-ROUTE-COEFFICIENT-BRIDGE-AUTHORITY. Evidence mode: Static + Ran.

## First Actionable Follow-On

Scaffold `D16-HYB-ROUTE-COEFF-SOURCE-ACQUISITION`.

Objective: obtain source-authorized static route coefficients for the selected
D16 cohort before attempting executable active plain-vs-hybrid owcmp suites or
default-promotion ratification.

Required first action:

- Place/import operator- or source-authored native `ow-lanuse-1` management
  inputs with complete `routing_coefficients` for every active Lane-D lane; or
- provide primary/source authority for a legacy-to-native bridge that maps
  named input surfaces to all five static operands:
  `k_o`, form `C_d`, `D_r`, `lambda`, and vegetation `C_d`.

Required gates for the follow-on:

- Native parse/projection tests for every selected coefficient source class.
- Active preflight proving every selected cohort member has complete
  schedule-consistent static coefficient authority.
- Active plain and explicit hybrid runs for each selected cohort member.
- Executable owcmp suite manifest replacing or complementing inventory-only
  manifests after active inputs exist.
- Anti-evasion guards if suite posture, cohort fixture, or required-case
  binding changes.

Do not return to D16 selector promotion or tolerance ratification until this
input-authority hold is lifted.
