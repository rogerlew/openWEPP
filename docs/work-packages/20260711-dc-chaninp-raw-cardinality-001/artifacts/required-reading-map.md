# Required-reading map

Status: active
Evidence mode: Static

Core: root/ExecPlan/work-package governance, catalog, and package. Conditional
applicable: DC procedure, contract procedure/profile/index, ADR-0012/0021, and
module-test/CQR guides. On-demand activated by phase: target contract/spec,
parser/tests/fixtures, pinned legacy sources, network-frame consumer/test, and
originating hold. Applicable instruction chains were resolved with
`tools/agents/find-agents --for` for every declared write path.

Local byte budget: `678351`. Disposition: `WARN` (`>400000`, below `800000`).
The non-deferring package necessarily combines canonical contract/spec,
parser/test, and the real normalized consumer/test; the large network-frame and
WSHED-W5 files are activated on-demand for consumer proof, not treated as
general pre-edit reading. No unrelated contract/source corpus is included.
