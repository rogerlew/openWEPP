# Kickoff

Execute row #7 of
`docs/work-packages/kernel-boundary-cqr-burndown-execplan.md` on `main`.

Primary closure is CRAP: every owned production function in WB publication,
direct publication, and direct seed projections with CRAP > 30 must be reduced
to CRAP <= 30 or explicitly dispositioned under ADR-0021.

Secondary closure is restored typed publication and seed-projection contract
assertions at stable typed surfaces, with no touched-file coverage regression.

Behavior must remain preserving: H2637 protected outputs must be byte/value
identical.
