# R6B Artifacts

Status: executed-hold.
Evidence mode: Static + Ran.

Artifacts for the R6B direct publication parity and manifest cutover hold-lift
package.

Final disposition:
`HOLD-R6B-DIRECT-PUBLICATION-TYPED-OPERAND-BRIDGE-ABSENT`.

R6B executed the first required gate and found that the cutover candidate still
constructs a skeleton direct frame, seeds only lane geometry/calendar metadata,
and captures publication rows from zero/default direct state. The package did
not accept output-family parity, manifest cutover, anti-alias, reconstruction,
or benchmark gates because parity-grade typed direct operands are absent.
