# Exact-Diff Reconciliation

Status: `terminal reconciled`

Evidence class: `Ran + Static`

The terminal intended diff contains:

- the CAL-06 package, deterministic tools, compact result and reconstruction
  tables, six plot-only SVGs with six Markdown sidecars,
  review/verification/closure artifacts, and scoped LFS rule;
- the canopy roadmap Order-6 result and Order-7 handoff;
- the fulfilled CAL-06 figure contract; and
- the work-package catalog entry.

No production Rust, canonical science contract, fixture, observation, or
public schema changed. The Harvard SWE defect is retained as an exclusion and
authority contradiction; the installed source-derived table was not silently
rewritten. Harvard vertical density profiles are explicitly excluded from the
bulk-density operator.

The unrelated untracked
`docs/audits/20260727_gate_planner_demotion_readiness_audit.md` predates CAL-06
and remains outside this package.

The original execution's independent reviews and verifications pass, and every
original finding is dispositioned. The user-requested plot-only follow-on adds
six Markdown sidecars and removes ancillary prose from the SVGs without
changing scientific results. `git diff --check`, local plot/sidecar validation,
both follow-on re-reviews, and both follow-on re-verifications pass. Package,
artifact, gate, exact-diff, and final-disposition statuses are complete.

The targeted Harvard downstream legend correction was also independently
re-reviewed and re-verified twice. Only that SVG digest changed; its plotted
source binding and all five manifest-bound scientific objects are unchanged.
