# Scaffold Evidence

Evidence mode: Static/Ran.

- Ran: created package directories:
  - `prompts/active/`
  - `prompts/archived/`
  - `artifacts/`
- Static: `package.md` declares objective, correction authority envelope,
  included/excluded scope, write set, phase plan, exit criteria, required gates,
  and subagent authorization.
- Static: active prompt requires iterative continuation until all gates close or
  a named `HOLD` blocker is reached.
- Static: required-reading map records the contract conflict that must be
  resolved before production edits.
