# Kernel Profile Compliance

Status: candidate complete

Evidence mode: Static

Both amended contracts retain required metadata, scientific scope, authority
anchors, variables/units, algorithm chronology, invariants, guard maps,
producer/consumer obligations, constants, calibration/identifiability,
contract-derived vectors, binding exposure, gaps, and revision history.

The target is baseline-independent physical migration rather than behavioral
approximation: complete energy satisfies cold content first, positive remainder
uses fixed latent fusion and available-ice bounds, and the resulting liquid
enters one same-substep disposition ledger. Current runtime is explicitly
nonconforming and on `IMPLEMENTATION_HOLD`; no production arithmetic was
changed or silently normalized.
