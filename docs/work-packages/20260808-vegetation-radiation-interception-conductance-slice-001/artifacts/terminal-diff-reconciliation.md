# Terminal Diff Reconciliation

Status: `queued`

Evidence mode: `not-run`

Reconcile base-to-head, index, worktree, and untracked paths against declared
intent, write set, authority surfaces, tests, manifests, assurance dependencies,
and validation selection. Identify unrelated user changes without claiming them.
