# Disposition

Status: `queued`

Evidence mode: `not-run`

Terminal value must be `complete` or `executed-hold`. Summarize achieved scope,
unmet requirements, exact evidence, claim limits, prompt archive/commit status,
and every linked follow-up. Any `FAIL`, `BLOCKED`, or unjustified `NOT RUN`
current-scope criterion prevents `complete`.
