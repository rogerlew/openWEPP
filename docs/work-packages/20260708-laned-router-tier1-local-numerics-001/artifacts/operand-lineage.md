# Operand Lineage

Status: `PLACEHOLDER`

Track each optimized operand from source to consumer: depth `h`, discharge `q`,
friction operands, Reynolds number, Froude number, alpha, celerity, CFL
timestep, outlet hydrograph, closure ledger, and active H2637 consumer path.
