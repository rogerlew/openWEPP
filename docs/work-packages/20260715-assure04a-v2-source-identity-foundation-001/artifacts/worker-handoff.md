# ASSURE-04A Handoff

Status: ready for explicit ASSURE-04B authorization

ASSURE-04A is closed. ASSURE-04B is next eligible, but this handoff does not
authorize scaffolding or execution.

ASSURE-04B owns deterministic transitive dependency planning for one named
report and all reports. It should consume the admitted identities and
relationships through `V2Repository`; it must not duplicate source parsing,
weaken admission, render prose, approve science, alter public catalogs, create
release snapshots, or vendor content.

Required starting facts:

- `assurance/v2/catalog.yaml` contains one internal `DRAFT`, `fixture_only`
  groundwater architecture fixture and zero public reports.
- `validate --report <id>` is selection-aware; `validate --all` traverses all
  admitted reports in stable ID order.
- report-specific `plan`, `build`, and `check` currently fail closed and name
  their later owning packages.
- canonical prose is authored Markdown; structured records provide mechanical
  identities and relationships, not scientific interpretation.
- the protected ASSURE-03 public/export/`usersum` surfaces must remain exact.

ASSURE-04B closure must prove cycles, missing and unused edges, transitive
impact, stable ordering, one/all equivalence, current/stale/blocked/selected
explanations, machine- and human-readable real CLI consumers, no unrelated
output changes, touched-file CRAP at or below 30, workspace gates, dual review,
and dual verification. It must leave report assembly to ASSURE-04C and all
review locks, promotion, catalogs, snapshots, release transfer, and vendoring
to ASSURE-04D or later.
