# ASSURE-04A Verification A

Status: PASS — renewed terminal verification

Evidence class: Static + Ran

Independent terminal Verification A found no remaining actionable finding.

Static verification confirmed:

- exactly `GW-P01` through `GW-P09` are bound by the canonical manuscript and
  supplement;
- `GW-P06` and `GW-P07` retain the exact accepted consumer and router-exclusion
  proof lineages through claims, method, supplement, agent provenance, and
  fixture copying;
- the Srivastava et al. (2013) bibliographic record and DOI are correct;
- Codex authorship, unassigned human accountability roles, incomplete agent
  provenance, and internal-only review are disclosed and block review entry;
- source, lifecycle, roadmap, package, archived prompt, disposition, and
  handoff preserve the internal `DRAFT`, `fixture_only`, zero-public boundary
  and do not authorize ASSURE-04B;
- current source/protected hashes, all 223 production-source hashes, all 429
  measurement-input hashes, terminal JUnit identity, and the passing CRAP
  bundle's 16-file checksum manifest match retained evidence; and
- this is internal coding-agent verification, not scientific peer review or
  scientific approval.

Ran:

- focused Nextest: 24/24 PASS, run
  `a705a3de-5bdc-4987-a86a-307028eae166`;
- named/all validation and public `check --all`: PASS with one internal fixture
  and zero public reports;
- report-specific planning: expected fail-closed exit 2 naming ASSURE-04B;
- Markdown lint/validation, package-scoped UK-to-US preview, and
  `git diff --check`: PASS; and
- protected hashes and aggregate `usersum` identity: unchanged.

The verifier found one low-severity documentation inconsistency: the gate
record used a noncanonical hyphenated spelling while claiming a clean package
spelling preview. The parent normalized it; targeted preview and documentation
checks then passed, and the verifier rechecked the current tree.

Limitation: Verification A did not rerun the full workspace suite or fresh
CRAP acquisition. It independently verified their checksums, JUnit, and current
source manifests, then ran the focused workflow.

Verification B subsequently found a blocking typed/schema parity defect and
the production admission code changed. The earlier disposition therefore
became stale and was renewed after the amended heavy gates passed.

## Renewed Terminal Verification

Evidence class: Static + Ran

Renewed Verification A passed with no actionable finding. It reverified the
nine claims and exact P06/P07 proof lineages, citation, manuscript/supplement
bindings, authorship and agent disclosure, review blockers, internal draft and
zero-public boundaries, B-T01's 16-field closure, the 2,042-line disposition,
all finding dispositions, and the bounded ASSURE-04B handoff.

Independent formatting, strict focused Clippy, dependency policy, named/all
validation, zero-public check, report-specific fail-closed planning,
documentation, spelling preview, and whitespace checks passed. Focused Nextest
passed 25/25 in run `d2e44430-cfe2-4c06-899e-252c388709a7`.

The verifier authenticated the 1,986-test JUnit, all 16 fresh CRAP checksums,
223 production and 429 measurement-input hashes, current executable identities,
protected public bytes, and the explicitly dispositioned post-heavy governance-
document drift. This remains internal coding-agent verification, not external
scientific peer review or approval.

## Final Documentation Closure Confirmation

Status: PASS

After the mechanical completion/roadmap/prompt transition, Verification A
independently reproduced the final 27-row status, 27-path set, 3,258-byte
content manifest, and 42,850-byte full-index diff recorded in the heavy-gate
artifact. All six closure-governance hashes and the unchanged archived-prompt
identity matched. Package, disposition, roadmap, handoff, and prompt states are
consistent; ASSURE-04B remains unscaffolded and unauthorized.

Package/assurance/roadmap Markdown, spelling preview, and whitespace checks
passed. All executable, test, v2 source/prose/schema/result, protected, and
`usersum` identities remain heavy-gate exact. No code workflow was rerun for
this documentation-only confirmation.
