# CAL-07F Decision Rationale

Evidence class: `Ran + Static`

Decision:
`DO NOT RECOMMEND CALIBRATION / ADJUDICATE ECOSYSTEM-MODEL LIMITATION`.

The decision is not based on choosing one GCC product. Both products:

- rank every frozen member identically;
- have no member with all 12 seasonal crossings;
- have no member passing the uncertainty or direction-coherence criteria; and
- identify `GSI-4831` as the lowest-error member while leaving it far outside
  the binding screen.

The product audit therefore resolves the CAL-07E method concern without
rescuing calibration readiness. Product choice changes some falling residual
magnitudes, but not the calibration disposition.

Parameter exploration is also exhausted at the present authority boundary.
The CAL-07D constraint-removal scenarios alter chronology but do not provide a
complete, directionally coherent parameter-only correction. A new water or
seasonal process would require authority that is not available.

The user-directed stop-loss is applied: further acquisition is deferred,
CAL-07F closes the investigation, and the remaining contradiction is retained
as an explicit limitation.
