# CAL-07F Kickoff

Execution mode: end-to-end bounded observation-product audit.

Read `package.md` and `artifacts/required-reading-map.md`. Execute all phases
without fitting parameters or changing production science.

Use separate checksum-bound `gcc_mean` and `gcc_90` lanes, retain uncertainty,
and apply the prospective calibration decision rule mechanically. Never select
the product that fits the model better.

Files: this package, CAL-07E acquisition deferral, canopy roadmap, and
work-package catalog.

Subagent authorization: this package explicitly authorizes subagent
spawning/delegation to two terminal science reviewers/verifiers. Their only
write access is the four review/verification artifacts named in `package.md`.

Autonomy: complete the audit and stop-loss disposition end-to-end.
