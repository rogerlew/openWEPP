# Kickoff

Execute row #1 of
`docs/work-packages/kernel-boundary-cqr-burndown-execplan.md` on `main`.

Primary CRAP scope:

- `crates/openwepp-hillslope-orchestrator/src/runtime_inputs/02_soil_slope.rs`

Secondary test scope:

- `tests/integration/infile_soil_parser_contract.rs`

Baseline source:

- `/tmp/openwepp-crap-row3-after.json`

Close the row with CRAP-after evidence, restored typed soil assertions, full
gates, H2637 identity, line-count governance, dual review, verification,
catalog update, and a row-specific commit before moving to the next row.
