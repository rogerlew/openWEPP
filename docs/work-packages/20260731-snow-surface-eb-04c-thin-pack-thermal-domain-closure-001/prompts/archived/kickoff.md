# SNOW-SURFACE-EB-04C Kickoff

Scope: local repository science-contract/kernel work; flat-file reads and edits
only; no external connectivity.

Execution mode: package-end-to-end.

Execute every phase in `package.md` sequentially through disposition.

Required reading:

- Core: `AGENTS.md`, `docs/codex_exec_plans.md`,
  `docs/work-packages/AGENTS.md`, `docs/work-packages/README.md`, and
  `package.md`.
- Conditional: `docs/defect_closure_execplans.md`, science-contract authoring
  procedure/profile/index, `crates/AGENTS.md`, and `tests/AGENTS.md` when their
  declared surfaces are touched.
- On-demand: `SC-SNOWENERGY-001`, EB-04A/04B evidence, libsnobal thin-pack
  sources, and testing strategy for the touched mechanism.

Required-reading budget: approximately `592000 bytes`, `WARN`; map:
`artifacts/required-reading-map.md`.

Task: close both EB-04B thermal defects end-to-end inside the declared envelope.

Constraints: contract-first sequencing; typed guards; no silent defaults; no
surrogate/provisional/proxy/heuristic physics; no temperature clamp, epsilon
pressure, coefficient tuning, forced melt, or cold-content deletion.

DC closure: do not hold while source reading, contract/test work,
implementation, or validation remains possible inside the envelope. Any HOLD
requires the package's explicit legitimacy audit.

Real consumer proof: the direct-production Stage 3 consumer and runner trace
must read and expose the corrected branch; wrappers, shadows, or test-only
paths cannot close the claim.

Conservation/output acceptance: use the frozen operand lineage, reject aliasing
alternatives, and independently prove unchanged persistent mass/cold content
and zero Stage 3 exchange below the boundary.

Subagent requirement: REQUIRED for the Critical full-workspace profile. This
prompt explicitly authorizes subagent spawning/delegation to the
`comparator_suite_runner`, two independent reviewers, and two terminal
verifiers for the bounded roles in `package.md`.

Autonomy: execute end-to-end without requesting user direction unless a hard
boundary is proven.
