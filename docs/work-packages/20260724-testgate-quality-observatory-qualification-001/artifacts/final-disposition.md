# Final Disposition

Evidence class: Ran and Static.

Disposition: `COMPLETE`.

Order 6 qualified TESTGATE on exact head
`955358449381ab38378d28dac93ba7b21b496d14` in provider run
[`30165527516`](https://github.com/rogerlew/openWEPP/actions/runs/30165527516).
Six LIGHT nodes, all ten pre-heavy checks, and six HEAVY nodes passed. The
receipt records `DEFERRED_TO_QUALITY_CI`, contains no quality execution node,
and truthfully retains `LOCAL_UNTRUSTED`.

Eleven retained pre-split receipts were freshly rejected for missing the
required quality disposition and containing prohibited legacy quality
content. Recovery archive, attempt index, native attestation, 76-entry ledger,
and exact base/head/package predicates verify.

Attempt 1 is retained as a typed inactive-anchor failure. Attempt 2 is retained
as a valid but non-closing documentation-only success. Attempt 3 is the
terminal critical-path success. Both independent result reviews and both
terminal verifications passed; all low-severity notes are closed.

No QA, coverage, CRAP, or CQR workflow ran. Roadmap Order 7 is next and must be
activated separately under its own package authority.
