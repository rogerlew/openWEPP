# Execute dx5 Fine-Reference Hold Lift

Execute
`docs/work-packages/20260708-laned-router-tier2-dx5-fine-reference-hold-lift-001/`
end to end.

Operator posture: fidelity first, speed secondary. Runtime cost is priced and
recorded but is not a promotion blocker. Do not amend the one-third adequacy
rule.

Required first action: run the `mn_corn_h4` `dx0p625` reference and re-run the
strict adequacy gate. If `mn_corn_h4` shape does not converge, stop at a hold
and attribute the shape surface, checking degenerate-shape rows, tail-fold, and
end-window storage counters first.

If adequacy closes, finalize the candidate adjudication and promote `dx5` as
the opt-in active production mesh default only after amending
`SC-OFEROUTE-001`. Decide the shadow-mesh question explicitly. Keep bulk run
outputs out of the commit.
