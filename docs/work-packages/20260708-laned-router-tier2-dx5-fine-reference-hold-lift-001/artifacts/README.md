# Artifacts

Evidence for the LANED Router Tier-2 `dx5` fine-reference hold-lift package.

Bulk run directories live under `artifacts/fine-reference-runs/` and are
ignored. Committed evidence is limited to compact summaries, hashes, command
logs where needed, review, verification, gates, and disposition artifacts.
