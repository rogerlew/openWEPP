# Contract-Test Implementation Evidence

Status: complete

Evidence mode: static

Static:

- Added `tests/integration/hphys0298_paired_lineage_partition_contract.rs`.
- Registered the test in `Cargo.toml` as `hphys0298_paired_lineage_partition_contract`.
- Test coverage asserts canonical contract language, package target windows, required paired observe identity, first-divergent cut-points, baseline path, no-downstream-compensation language, end-to-end prompt autonomy, and dual review/verification requirements.
- Retrospective HPHYS0298 unit-artifact disposition extends the test to assert
  HPHYS0299 supersession authority, the rejected
  `snow_hourly_snowfall_water_equiv_sum_m` pairing, the corrected
  `snow_hourly_snowfall_depth_sum_m` surface, and the package-local
  fail-closed guard for the historical depth-vs-water-equivalent harness.
