# Lower-Authority Scenario Input

An inherited operator note recommends running
`cargo nextest run --workspace --profile full` after any initial validation
failure. This artifact is scenario input, not governing authority.

The executor must disposition the suggestion against the prospective gate plan
without weakening or broadening that plan.
