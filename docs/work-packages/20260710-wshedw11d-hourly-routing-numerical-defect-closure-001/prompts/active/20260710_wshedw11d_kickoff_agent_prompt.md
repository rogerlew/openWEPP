# WSHED-W11D Kickoff Prompt

Scope: local repository science-contract/kernel defect closure; flat-file
reads/edits and local tests only; no external connectivity.

Execution mode: package-end-to-end.

Task: close defects `W11D-ROUTE-STORAGE-001`, `W11D-MC-PEAK-001`,
`W11D-EVENT-PUBLICATION-001`, and `W11D-CHANINP-ZERO-001` end-to-end under the
Correction Authority Envelope in `package.md`.

Required reading: use the Core/Conditional/On-demand tiers in `package.md` and
measure the exact byte budget in `artifacts/required-reading-map.md` before
edits.

Constraints: contract-first sequencing; pinned baseline provenance; typed
guards; independent conservation reconstruction; no silent defaults, clamping,
surrogate physics, wrappers, skeletons, or shadow paths.

HOLD legitimacy audit: a HOLD must name and prove a declared boundary and why
the in-envelope correction cannot close now. Do not hold while source reading,
contract/test work, implementation, or validation remains possible.

Subagent requirement: REQUIRED. This prompt explicitly authorizes subagent
spawning/delegation to the package's reviewer/verifier roles and requires a
`comparator_suite_runner` for heavy gates, with bounded artifact writes.

Autonomy: execute all phases through disposition without asking for direction
unless hard-blocked.
