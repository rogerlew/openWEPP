# Review Finding Disposition

Evidence class: `Static` and `Ran`

Status: initial-review disposition; terminal verification later exposed
additional gaps recorded in `terminal-finding-disposition.md`.

All eleven findings were accepted; none was waived, downgraded, or deferred.

| Finding | Disposition | Closure evidence |
| --- | --- | --- |
| `GOV-001` | Accepted and fixed | Removed generic risk acceptance from the correctness-authority model; source guard forbids its return. |
| `ALIGN-002` | Accepted and fixed | Recorded a bounded finding-driven write-set amendment and aligned both dev-guide surfaces plus the watershed architecture closure section. |
| `ALIGN-003` | Accepted and fixed | Replaced universal wording with selected-terminal/pre-cutover fallback wording; added negative contradiction assertions. |
| `INTAKE-004` | Accepted and fixed | Added the crate instruction chain and recalculated the required-reading budget to 183,315 bytes. |
| `SCHEMA-001` | Accepted and fixed | Added planner controls, execution-context identities, target/features/failure/owner fields, typed assurance axes, and verified zero-work disposition. |
| `SCHEMA-002` | Accepted; initial remediation | Added discriminated immutable campaign events, assurance event ancestry, anchored backstop/certification constraints, and fold-aware currency/result conditionals. Terminal verification found additional representation and cross-field gaps, then remediated them separately. |
| `SCHEMA-003` | Accepted; initial remediation | Aligned execution/admission/scientific outcomes and expanded receipt node snapshots. Terminal verification found additional semantic PASS contradictions, then remediated them separately. |
| `SCHEMA-004` | Accepted and fixed | Replaced generic/duplicated subjects with one required typed receipt subject and structurally distinct artifact subjects. |
| `SCHEMA-005` | Accepted and fixed | Split valid predicate kind/operator/value shapes and required a digest only for the legacy adapter executor. |
| `SCHEMA-006` | Accepted and fixed | Replaced invalid instances with one-mutation descriptors and asserted intended instance/schema error paths. |
| `SCHEMA-007` | Accepted and fixed | Pinned every schema `$id` and `schema_version` constant in the source guard. |

Ran: the remediated schema/authority contract passed `7/7`. Dynamic digest
equality, DAG topology, canonicalization, signature/revocation verification,
fold execution, and hermetic reuse remain correctly assigned to
`TESTGATE-PLAN-01`; v1 now carries the inputs those verifiers require.
