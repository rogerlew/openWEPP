# Kickoff Prompt

Execute `docs/work-packages/20260624-r7g-typed-frost-solver-extraction-001/package.md`.

Required posture:

- Continue until the typed frost solver extraction exit criteria pass or a
  named hard blocker is proven.
- Do not close with only diagnostic evidence if production direct active frost
  still computes through `DirectFrostRunoffSurface` or `HillslopeKernelRequest`.
- Preserve the comparator adapter only as a named test/comparator seam.
- Record `Static:` vs `Ran:` evidence in package artifacts as work proceeds.
