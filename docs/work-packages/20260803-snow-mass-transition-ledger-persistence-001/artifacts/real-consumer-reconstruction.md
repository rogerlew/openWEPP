# Real Consumer Reconstruction

Status: `queued`

Evidence mode: `Not run`

Map and execute the real release CLI from calculation through production
outcome, both compact ledgers, optional payload, runner, schema-v4 writer, and
independent parser. Reconstruct both transition identities, show shared-operand
agreement, report magnitudes/residuals, and prove every rejected alias differs
on retained rows.
