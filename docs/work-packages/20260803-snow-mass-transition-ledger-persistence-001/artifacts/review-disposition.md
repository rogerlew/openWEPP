# Review Finding Disposition

Status: `queued`

Evidence mode: `Not run`

Record one row for every Review A and B finding with source, severity,
decision (`accepted`, `rejected`, `deferred`, or `follow-up`), action, evidence,
and rationale. Accepted findings must be fixed before terminal verification;
any deferred current-scope gate forces `HOLD`.
