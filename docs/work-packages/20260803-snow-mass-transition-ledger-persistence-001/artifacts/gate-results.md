# Gate Results

Status: `queued`

Evidence mode: `Not run`

Record exact current-head commands, source/binary identities, elapsed times,
results, and evidence paths for focused tests, contract checks, real consumers,
paired performance, formatting, warnings-denied Clippy, quick, frost, Critical
full, doctests, Markdown/JSON syntax, assurance identity, line counts, and
write-set reconciliation. Classify every required row `PASS`, `FAIL`,
`BLOCKED`, or `NOT RUN`.
