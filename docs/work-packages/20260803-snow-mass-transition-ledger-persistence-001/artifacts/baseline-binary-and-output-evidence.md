# Baseline Binary And Output Evidence

Status: `queued / not run`

Evidence mode: `Not run`

Before contract or production edits, record the scaffold commit, exact release
build command, runner binary paths, hashes, sizes, mtimes, fixture/runfile,
environment sanitization, the complete input manifest/hash of the retained
`snotel_snowbird_ut` source copied into the package namespace, trace-disabled
protected-output hashes, trace-enabled schema-v4 hash/size/row count, one
warm-up, and all seven baseline timing/RSS samples. Preserve copied binaries
under the package target namespace for terminal paired comparison.
