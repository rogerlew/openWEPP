# Field Ownership And Public API Inventory

Status: `queued`

Evidence mode: `Static scaffold`

Inventory every field of `DirectSnowAccumulationMeltDiagnostics` and
`DirectSnowStage3Diagnostics` as production outcome, compact ledger, verbose
trace, or rejected duplicate alias. Record all exported types, fields,
constructors, methods, workspace consumers, published-crate posture, and the
selected additive or migration-safe API route before production edits.

Any required breaking external API change is a package hold boundary.
