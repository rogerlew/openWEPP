# Schema Compatibility Evidence

Status: `queued`

Evidence mode: `Not run`

Compare scaffold and candidate real schema-v4 JSONL for row count, bytes,
ordered key sequence, values, nulls, strings, and file size. Record any
non-byte-identical formatting difference exactly. No field removal,
reinterpretation, backfill, or schema-v5 bump is authorized.
