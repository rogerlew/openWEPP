# Public API Parity

Status: `queued`

Evidence mode: `Not run`

Record pre/post exported symbol and field inventories, workspace call-site
reconciliation, published-crate posture, CLI/runfile/output parity, and the
reviewed migration-safe route for any diagnostic Rust API decomposition. An
unauthorized breaking external surface forces `HOLD`.
