# Intake And Pre-Implementation Intent

Evidence class: `Ran` and `Static`

- Frozen base: `28daa0339cbc99ceb94b176a506ce0c213685d7c`
- Intake tree: clean and synchronized with `origin/main`.
- Queue authority: `TESTGATE-CI-01` is the current roadmap priority.
- Change class: critical gate-executor, receipt, coverage, and CI workflow work.
- Required current fallback: format, warnings-denied workspace Clippy, full
  workspace Nextest, cargo-deny, and fresh global adjudicated CRAP.
- Intended terminal scope: every declared write path plus any Cargo lockfile or
  generated policy fixture mechanically required by implementation.
- Prohibited inference: an initial benchmark or successful shadow run does not
  satisfy the 14-day/20-increment blocking-cutover scorecard.

At package intake, the planner emits typed DAGs and verifies receipts but has no
executor or receipt builder. The repository has one conservative release
workflow, global-only adjudicated CRAP closure, and no shadow observation lane.
