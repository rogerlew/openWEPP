# Codex Execute Prompt - DX5 Production Mesh Policy Ratification

Scope: local repository science-contract/kernel migration task; flat-file
reads/edits only; no external connectivity.

Execution mode: package-end-to-end (default).

Phase plan: execute all phases in `package.md` sequentially through
disposition.

Required reading:

Core:

- `AGENTS.md`
- `docs/work-packages/AGENTS.md`
- `docs/specifications/science-contracts/AGENTS.md`
- `docs/standards/AGENTS.md`
- `docs/standards/prompt-wording-guidance.md`
- `docs/specifications/science-contracts/contracts/SC-OFEROUTE-001.md`
- `artifacts/required-reading-map.md`

Conditional:

- `crates/AGENTS.md` and `tests/AGENTS.md` before Rust/test edits.

Files:

- `docs/work-packages/20260708-laned-router-dx5-production-mesh-policy-ratification-001/**`
- `docs/specifications/science-contracts/contracts/SC-OFEROUTE-001.md`
- `crates/openwepp-hillslope-orchestrator/src/direct_runtime/laned_active.rs`
- `crates/openwepp-runner/src/hillslope/laned_active.rs`
- focused tests adjacent to touched code
- `docs/work-packages/README.md`
- `docs/ROADMAP.md`

Task: execute package objective end-to-end for declared scope. Rebuild the
rev-44 selected-cohort promotion matrix, amend `SC-OFEROUTE-001` before code
if promotion is supported, implement active production default `target_dx_m =
5.0`, prove no-env active production uses `dx5`, and complete gates, review,
verification, and disposition.

Constraints: contract-first sequencing; canonical SC authority; typed guards;
no silent defaults; no surrogate physics; no tolerance widening; no shadow
mesh change unless contract-amended and evidenced.

Real consumer proof: prove production no-env active execution resolves to the
new mesh policy and the public runner manifest/output path reports/consumes the
new default. Prove wrappers, shadow paths, and old fixed-cell default are not
carrying the production claim.

Subagent requirement: REQUIRED for heavy comparator/timing/full closure gates
when available. This prompt explicitly authorizes subagent spawning/delegation
to review, verification, and comparator/closure-gate subagents for
package-local review, verification, selected-cohort/default comparator work,
and heavy Rust closure gates. Outputs: compact metrics plus artifact/log paths.
Write access: package artifacts only unless implementation fixes are
explicitly assigned.

Autonomy: execute package phases end-to-end and update required artifacts
without requesting additional user direction unless hard-blocked.

Outputs: update package artifacts/disposition for all completed phases.
