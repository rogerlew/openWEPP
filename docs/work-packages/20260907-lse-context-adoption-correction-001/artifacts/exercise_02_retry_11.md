# Independent represented-snow Jacobian reuse requirements review

Static: current canonical contracts, their routed dependencies, governing instructions, and the four explicitly routed frozen experiment documents. Ran: only instruction discovery, source reading/accounting, current `git rev-parse HEAD`, and private report preparation. **No production execution, implementation inspection, numerical test, benchmark, or runtime correctness verdict is claimed.**

Role/session: independent exercise reader `/root/solver_iteration11`. Requested correctness effort: high under the review guide; configured/effective model and runtime effort: **UNOBSERVED**. Reviewed HEAD: `ee899c22c9bb24cf91153f33991402b0978c6af0`, confirmed by `git rev-parse HEAD`. This requirements review owns only `artifacts/exercise_02_retry_11.md` in the current correction package. No other answer, parent evidence, old LSE text, Git history, archive, intake, prediction, rubric, or review record was read. Historical passages encountered inside the required whole **current** external contracts were read in place and subordinated to their current supersession clauses.

The reuse is correct only if it produces the exact canonical signed-probe evaluation that a complete evaluator would produce, including its first error, while removing solely work proved independent of that probe. Success at the current iterate, matching final outputs, a small call count, or a passing structural classifier is insufficient. The principal review boundary is between the LSE finite-difference Newton Jacobian and the enclosing Stage-3 solver: the former retains its prescribed stencils and dense LU; the latter currently forbids coordinate finite differences and historical solver fallback. Their budgets and exceptions cannot be exchanged.

This is a **requirements answer, not a finding that production conforms**. No implementation defect is asserted without implementation evidence. The unresolved next gate belongs to any future implementation/experimental owner: supply the actual inputs and execute the directly required tests below. Missing actual run inputs remain missing.

## Authority route and scope decisions

`SC-LANDSURFACEENERGY-001.md` is the canonical entry. Its candidate selective-reading procedure is admitted only for this bounded v32 exercise. I read the complete interface and complete selected chapter extents, including introductions, definitions, qualifiers, guards, obligations and evidence links. The dependency-replay task routes through `dependency-replay.md`, `common-details.md`, `numerical-methods.md`, `surface-energy.md`, `nonlinear-solve.md`, `solve-boundary.md`, `soil-coupling.md`, `terminal-support.md`, `replay-evidence.md`, and the relevant map-custody/water-vapor dependencies. I read the whole latter two chapters, so that the validation and water/enthalpy dependency boundaries were not inferred from isolated invariant names.

Expansion was necessary for these reasons:

- Numerical and surface dependencies reach `SC-VEGETATION-001.md`: leaf current/maximum evaluation, gas-exchange branch provenance, cap/hydraulic state, canopy routing, optics, shared air and transfer conductance affect the Jacobian. The entry requires the **whole external single-file contract** once selected; the bounded claim is not permission to read only a convenient gas-exchange paragraph.
- Soil, terminal and custody dependencies reach the whole `SC-SNOWENERGY-001.md` and `SC-COUPLEDTIME-001.md`: represented-snow classification, exact optical/lower-boundary ownership, common support, terminal transition, charged-map roles, pending/final custody, rollback and publication constrain what a replay base may mean. Reading their full current text exposes the supersession of the historical 96-evaluation cascade by the current canonical outer solver.
- The replay evidence chapter routes to the prospective experiment protocol, authority-input reproduction, active kickoff and handoff. Those documents distinguish the historical retention gate from the prospective R experiment, including PC1/SG1 and the paused/failed evidence state. Their instructions do not authorize this read-only exercise to resume experiments or follow disallowed historical evidence links.
- The review guide routes to testing strategy §§7–10 and §§17–18 and the findings template. The correctness-authority model was read because §7 explicitly invokes it. Numerical architecture and ADR-0044 prevent treating reuse as a second solver or a numerical fallback.

Conditional expansion is not universal process reimplementation. This review consumes retained forcing/rain and authorized water operands; it does not rederive the snowfree forcing provider, rebuild the active WATBAL/SURFACELIQUID process, reconstruct exact soil-owner arithmetic, or implement a snowfree final reseal. Those conditional provider/exact-storage/active-owner audits would require their own routed chapters/contracts if a proposed change crossed those boundaries. Merely encountering links in whole external contracts is not a demand to import all vegetation seasonal allocation, BGC, Richards or restart implementation work. Their owner boundaries and input/custody predicates remain binding where consumed here.

Root and nearest work-package instructions apply. Package artifacts remain evidence, never replacements for canonical science. No production surrogate, provisional physics, silent dependency fallback, numerical floor, clamp, default or tolerance-based identity repair is permitted without explicit current contract authority. TESTGATE/gate planning machinery remains frozen. This artifact makes no Rust edit; production line-count and source-change test execution are not triggered by writing the review.

## Owners, inputs and physical regime

The LSE interface, common definitions, surface/soil/water chapters and `INV-SNOWENERGY-083` separate owners. LSE owns surface thermal balances; vegetation owns canopy structure/processes and canopy liquid; hydrology owns water mass and layer authorization; soil thermal owns soil temperature/enthalpy; climate/forcing owners supply immutable forcing and phase/enthalpy lineage. Every operand carries its declared area basis, units, model/configuration, interval, tile/occupancy, owner and receipt identity. Thermal evaluation cannot appropriate hydrology's water availability or derive missing forcing from a convenient default.

Represented snow is classified from immutable beginning Stage-3 state **before** physical evaluation. Exactly one standard Stage-3 covered-column call executes under native vegetation/LSE identities in the canonical charged map. Its `Stage3SnowOpticalBoundaryReceiptV1`, typed lower boundary and `CoveredLowerBoundaryEnergyOperands::Stage3SnowCovered` remain the exact receipts/operands from that same map. The snowfree V3 evaluator still rejects a Stage-3 boundary. While snow is represented, litter V3/V4 vapor, phase, storage, current ingress and WB14 work are inactive; physical and owner bytes, predecessor markers and receipt chains remain unchanged. No second legacy inner LSE/hydrology envelope, copied optical receipt, reconstructed substitute lower receipt or uncharged call is allowed.

The snow contract's current ADR-0044 amendment, `INV-SNOWENERGY-082`, takes precedence over historical numerical-dispatch versions 23/25–57. Snowfree support has no covered solver. Represented snow at or below `1 kg m^-2` uses the existing V22 terminal one-volume enthalpy/adaptive process, never CoE. Above that boundary, frozen, cold, mixed, thaw/refreeze, density/layer and terminal-crossing postures are active sets/events inside one canonical covered solver, not switches selected by failure history. An accepted earliest terminal event partitions support; the snowfree successor uses no covered solver.

At the LSE covered lower boundary, the supplied snow-surface temperature drives radiation and ground exchange; the trial ground coordinate does not become a second snow-temperature owner. Stage3-covered LSE soil coordinates are anchors to their beginning values, not additional storage/conduction/ground-vapor work. The snow–soil boundary owns one bottom-snow to first-OFE-soil-node interface, series resistance of the two half layers, beginning/end Crank–Nicolson exchange and exact equal/opposite heat custody. It cannot be counted again as covered LSE ground storage or heat.

One persistent Stage-3 lane column is OFE-ground. All ordered covered and open tile-ground contributions enter as `sum(f_i X_i)` once; fractions close to one under the topology tolerance without covered-subset normalization. They share the lane snow state/temperature/latent heat and retain exact class/model/destination/topology joins. Missing open contributions, duplicate tiles, substituted classes or dividing the entire lane terminal liquid by every tile fraction are invalid.

Precipitation custody uses a present sealed ordered parcel set, including a present empty set for zero. Keys are lane/destination/phase/source/semantic ordinal, not hash-sorted arithmetic order. Covered liquid comes only through the distinct vegetation throughfall, initial drainage, second drainage and stemflow routes; open raw rain is a separate exclusive source class. Solid precipitation bypasses canopy interception. Persistent canopy storage is not ground precipitation. The exact same parcel keys/cardinality/order supply mass and advected energy, with each finite nonnegative mass and its own finite enthalpy provider. Aggregate equality cannot excuse omission, duplication or aliasing; `SNOWENERGY-E-PRECIP-001` rejects before mutation.

The V22 phase process bounds sublimation mass and its latent energy together, retains deposition, and includes vapor latent energy once. With beginning ice/liquid/cold content and actual vapor/external liquid, it reconstructs total water and complete energy, then selects cold ice, mixed ice/liquid, or exact ice exhaustion at fusion capacity. Positive represented ice is never tolerance-deleted. An event belongs only to an accepted exact-zero-ice endpoint with independent mass/liquid/energy closure; terminal liquid transfers once and subsequent atmospheric flux belongs to the successor. Old melt-before-deposition, censored-terminal-energy, separate continuous-root and sub-floor prescriptions are superseded where the current V22 amendment says so. Historical mass-unit lifecycle thresholds are not interchangeable with meter-unit aggregate closure tolerances.

## Canonical LSE solve and finite-difference arithmetic

`numerical-methods.md`, `nonlinear-solve.md`, `solve-boundary.md` and their VEG dependencies define the solver before any reuse exception. For `N` occupancies and `S` soil layers, the vector has `10N+3+S` coordinates. Occupancies are top-to-bottom rank then identity order. Each occupancy has sun/shade/stem/root potentials, sun/shade beta, then sun/shade/wet/stem temperatures; its residuals are the six hydraulic equations and four component energy/anchor equations. Shared canopy air temperature/humidity, ground and ordered soil coordinates follow. Reordering changes error and arithmetic chronology.

Validate configuration before the numerical domain. Temperatures retain `[200,350] K`; liquid-vapor components retain `[273.15,350] K` even at zero area. Shared specific humidity is in `[0,0.1] kg kg^-1`, beta in `[0,1]`, and potential bounds remain those of the canonical hydraulic authority. Invalid current state is rejected before Jacobian assembly; zero area is not permission to ignore its coordinate domain.

For coordinate `j`, the perturbation is `sqrt(binary64 epsilon) * max(abs(x_j), unit_scale_j)`, using the specified unit scale: temperature `1 K`, humidity `0.001 kg kg^-1`, pressure `1 Pa`, water potential `1000 mm`, beta `1`. Construct/admit/evaluate the minus probe before the plus probe. If both canonical signed probes are admissible, use `(R_plus-R_minus)/(2*delta)`. If exactly one is admissible, use the unique inward difference `(R_plus-R_current)/delta` or `(R_current-R_minus)/delta`. If neither is admissible, return the typed `covered_jacobian_bound` failure. Do not shrink delta, clamp a probe, substitute an analytic derivative, reinterpret a bound, or demand a centered stencil when only inward is canonical. Select active branches at the current iterate and freeze them through its Jacobian perturbations; select branches again at a new Newton/backtracking evaluation.

Dense partial-pivot LU remains canonical, with lowest row on pivot ties and singularity below `64*epsilon*matrix_infinity_norm`. Backtracking tries `b=0..20` under strict normalized-residual decrease; the nonlinear cap is 50 completed updates. Energy residual tolerance is `1e-6 + 1e-10*max(1,sum(abs(energy operands)))`; water uses `1e-12 + 1e-9*its prescribed scale`. The accepted iterate must meet every residual and the four governed step norms together: temperature `1e-8 K`, humidity `1e-12 kg kg^-1`, potential `1e-7 mm`, beta `1e-10`. The internal `ci` step diagnostic has **no independent acceptance threshold**. No review may invent one.

The V10 right-scaling exception applies only to the uncapped active V10 nonpositive-assimilation potential solve. D is exactly the finite-difference unit-scale diagonal: form `J_y[:,j]=J_x[:,j]*D[j]`, classify the canonical pivot on J_y, solve `J_y delta_y=-r`, then recover `delta_x[j]=D[j]*delta_y[j]`. It is not authority for V1, positive-assimilation/positive-PAR branches, or fixed-final scaling. Its exact qualifying branch and complete diagonal must be demonstrated, not inferred from a general label such as night. Reducing the pivot multiplier, accepting a rejected pivot, regularization, larger iteration/trust bounds or physiological floors is not equivalent scaling.

The no-update representational witness is also narrow. Evaluate the full `b=0` step first: if domain-valid, reconstruct every exact prospective step, and when all current complete residuals and governed steps pass, accept the unchanged current iterate under that original full-trial witness. Only when all current residuals pass and that full witness cannot be accepted because of its domain or a governed step norm may the bounded sequence inspect smaller witnesses. Fully evaluate prospective potentials, beta, component/shared temperatures, humidity and `ci`. The **first** later domain-valid witness must have all four governed norms within their bounds; it then proves acceptance of the unchanged current iterate only. Do not accept the trial witness, skip a first domain-valid failure for a smaller one, accept an incomplete/nonfinite witness, rescue failing current residuals, or activate the exception when the full step already passes. Record the actual examined exponent in existing cumulative backtracking accounting, without adding a public persisted diagnostic. Tests must poison each governed norm independently and retain the two real fixed-final-cap iteration-4 paths formerly exhausting 20 halvings. Ordinary remaining updates still require strict descent and retain typed rollback on exhaustion.

The V2 FullSupply nonpositive-assimilation final path imports the V1 system with the expressly applicable V10 rules; it does not clamp/relabel gas exchange. Every positive request must have the exact request identity, exact amount `A=D`, and FullSupply posture; a zero request retains its identity and exact zero. Partial positive authorization is unsupported here. Only potential coordinates may initialize the fixed-final solve; everything else—fluxes, branches, candidates, receipts and diagnostics—is recomputed from the same immutable beginning under exact fixed caps. Iteration-zero final acceptance is legal only when all residuals, branch inequalities, domains, `0<=F<=A<=D`, identities and owner checks already pass; step counts are exactly zero and no Jacobian/backtracking occurs. V1/V2 value bit copies do not erase distinct authority identities; mixed receipt, missing night-gas and partial-migration failures remain typed (`109`–`111` in the applicable LSE guards).

Inactive sun/shade/wet energy rows anchor to `max(T_ca,273.15 K)`; stem anchors to `T_ca`. Zero physical area makes its physical flux/demand operands zero. Its Newton row is a direct deterministic anchor, not a finite difference through max; this inactive-coordinate representation must not be confused with the separately governed represented-snow ground/soil finite-difference synthesis. The extra wet anchor under `INV-LANDSURFACEENERGY-113` requires the uncapped V10 nonpositive-assimilation potential regime **and** STORE-CAP **and** preliminary wet rate no larger than the water residual tolerance **and** independently passing unanchored wet energy. A small positive preliminary rate can qualify under that exact tolerance; this does not discard liquid. It is not admitted for V1, positive-assimilation, condensation or the ordinary LAW-wet branch.

The complete caller warm start is required. A monolithic solve or mathematically equivalent nested solve is admitted only if every inner solve converges at every outer residual evaluation and the complete ordered residual passes. Within each OFE, tiles sort by typed tile ID; open tiles omit only shared-air coordinates and use reference-air fluxes. Active branch selection is ordered: typed domain, surface class, covered/open turbulence, positive surface water versus dry-soil vapor, vapor sign, water cap with equality cap-active, then numerical solve. Identity, units, basis, owner, layers, bands, directions and D/A/F inequalities remain exact.

`INV-LANDSURFACEENERGY-108` preserves this global first-error order in addition to the replay node order: malformed serialization; model/configuration/state/transaction identity; missing/duplicate topology or owner; nonfinite operand; unsupported snow/frozen/thawing/calm/nonneutral/domain branch; constitutive domain; request/authorization identity or bound; singular pivot; backtracking limit; iteration limit; accepted-step/residual failure; component closure; control-volume closure; cross-owner join. Return only the first error and diagnostics accumulated up to it. Every failure retains typed model/configuration/state/transaction/OFE/tile/occupancy/pass/solve identity, ordered residuals, iteration/backtracking counts, step, bounds/caps, bracket/pivot/matrix evidence and rollback hashes. No failed iterate or partial owner is reusable.

## The dependency-replay admission and graph

`dependency-replay.md#INV-LANDSURFACEENERGY-164` and `OBL-LANDSURFACEENERGY-C-020` govern component-temperature reuse. A base is a successful complete current-iterate evaluation from the same validated represented-snow map, solve, iteration and Jacobian sweep. Its immutable identity binds every source/configuration/beginning input, trial bits, frozen branch vector, potential-versus-fixed-final posture, every cap and authorization value, and graph schema/version/hash. Its lifetime ends before another iteration, solve, map or adaptive retry. Bitwise-equal values from another invocation do not grant custody.

Each single-coordinate signed probe gets a fresh private non-Clone capability. The constructor proves exactly one altered coordinate, its canonical sign/delta/probe bits, selected centered/inward stencil, bounds and exact base generation. The capability is consumed once on success **or error**; no cloning, transfer, second use, persistence, restoration, publication or cross-invocation reuse is admitted. Input identity is carried by exact typed borrows/generations/seals at natural validation boundaries, not `Debug` text, lengths, hash-only whole-input guesses, full per-probe clones/rescans or address equality.

The graph schema is `covered-component-temperature-dependency-v1`. Node/edge records are unique, lexically ordered and length-framed for the graph identity. The changed set is the full inclusive transitive dependency closure. An unknown node, unrepresented read or missing edge conservatively disables reuse for that coordinate before execution. Any conservative extra edge is explicit, versioned and present in the oracle. Build the typed graph once per immutable topology/configuration generation, not through per-probe strings/maps, graph closure construction or whole-input hashing. Required N=1/S=1 and N=2/S=6 tests compare exact edge sets and golden schema/hash identities and delete individual edges as negative tests; matching reachability or totals alone cannot prove graph completeness.

The operative edges include all of these dependencies; a temperature-to-local-energy sketch is incomplete:

1. Every component-temperature probe reaches its longwave layer, the reciprocal column recurrence, and **all** affected occupancy energy/tolerance/output nodes, lower ground boundary and result assembly. Component temperatures alter fourth-power emission, which propagates in both directions.
2. Sun/shade temperature reaches the matching current leaf, maximum leaf, vapor, hydraulic, sensible, energy, tolerance and output nodes. Current leaf reaches maximum, vapor, hydraulic and energy/tolerance/output readers; maximum reaches vapor/hydraulic/output. Vapor reaches hydraulic, energy/tolerance/output and shared vapor reduction. The maximum call is not independent merely because its explicit beta is one.
3. Wet temperature reaches wet routing and finalization plus occupancy wet/sensible/energy/tolerance/liquid-output readers. Route incident state reaches preparation. Preparation reaches wet and finalization work, longwave layer, and **every reader** of area, wet fraction, store or branch state. Finalization reaches the next incident state and stemflow prefix, route-match comparison and terminal ground release. Both changed downstream incident fields recurse through every lower occupancy, not just the first lower one.
4. Occupancy wet output reaches hydraulic, wet energy/tolerance/liquid output and shared vapor. Sensible reaches energy/tolerance/output and shared heat. Hydraulic, energy/tolerance and liquid assembly reach output. Both liquid finalizations reach the routing consistency comparison, which precedes later occupancy/shared work.
5. Ordered occupancy outputs feed raw residual and tolerance reductions and the result. Lower-ground output feeds shared heat/vapor/tolerances and ground/soil rows. Shared heat and vapor feed their tolerances/rows/result, shared tolerances feed tolerance normalization, raw/tolerance vectors feed normalized residuals, every row feeds the final result, and terminal ground-release/stemflow outputs also reach final assembly.

The canonical source order must survive replay: trial admission; top-rain guard; top-down first preparation/wet/finalization with downstream incident/stemflow propagation; reciprocal longwave; per occupancy current sun then shade, maximum sun then shade, vapor, second wet evaluation, hydraulic/root, sensible, energy/tolerance, second liquid finalization, route-match, occupancy output; lower-ground exchange; shared heat then vapor/tolerances; ground/soil rows; normalization; result. One canonical evaluator body and shared assembly tail must preserve original arithmetic and fallible-call positions. A second hand-coded replay body that silently differs in guard order or accumulation is not authorized.

## Eligibility, error order and guard proof

Ordinary ineligibility is resolved **before capability commitment**. Non-Stage3/noncomponent coordinates, malformed multi-coordinate probes, inadmissible probes or unknown dependencies use the complete evaluator under its existing rules. A graph/topology mismatch discovered before commitment may take the complete path only with the same complete operands available, preserving the original complete-evaluator error. This is selection of an eligible evaluation route, not recovery from a failed replay.

A stale/foreign/transferred/consumed capability or mismatched binding is instead `ConstitutiveDomain("covered_component_dependency_replay_integrity")`. It rejects without a complete-evaluator fallback or state mutation. Once replay starts, a reachable fallible operation or routing mismatch returns the same typed first error directly; later nodes and complete fallback must not execute. An error consumes the single-use capability just as success does.

The crossability inventory is a proof obligation for each real fallible family:

- **Current leaf:** a canonical signed temperature perturbation can fail despite a successful base. Use authentic base/probe construction to demonstrate every naturally crossable error for both sun/shade positions and occupancies. Compare complete versus replay first error in source order, no later calls and exact rollback. Multiple competing poisons test precedence.
- **Maximum leaf:** historical admission has not established natural crossability. Its noncrossability needs an implication from the successful exact base, immutable operands and permitted signed perturbation to every guard, including exact beta/branch boundaries and equality of all returned fields. Reclassify only on an authentic counterexample, not a fabricated internal-state mutation.
- **Wet routing:** historical noncrossability requires the corresponding implication for each guard, with immutable rain, store, area, cap and frozen branch operands and the changed temperature. It covers zero area, wet/dry, exact capacities, route transitions and every lower occupancy, with field-by-field equality. The prospective SG1 permission below replaces only the specified recomputed wet proof, not all guard duties.
- **Longwave:** prove its finite area/temperature bounds, reciprocal branch and zero-area/boundary behavior; compare each component and ground operand bitwise.
- **Hydraulic/root:** prove guard preservation from successful recomputed predecessors and immutable root/soil/cap/frozen-branch inputs. Cover active/inactive and limiting-root/tolerance boundaries and all six residual/tolerance fields. Successful base alone is insufficient if a predecessor changed.
- **Routing match:** prove the two finalizations use the same inputs and canonical function; compare propagated lower drainage and terminal output exactly. A forged mismatch is an integrity test, not proof of a natural physical crossing.
- **Lower boundary:** unchanged lower inputs/caps/branches and resistance domains need their own preservation proof and exact ground/soil outputs.

Pre-admission/integrity failures are distinct from natural physical errors. Exercise real constructors, generations and lifetimes; direct private-field corruption is not an authentic probe. Infallible arithmetic assembly does not acquire invented error variants. No synthetic fault-hook corpus, unconstrained global parameter search, or direct call to a private node establishes physical reachability/noncrossability. A finite no-error search, including the historical 198 trials, is not a proof that an error is impossible. The complete natural-error corpus must preserve first error and byte-identical rollback.

The complete evaluator remains required for hydraulic potentials, beta and shared-air coordinates, other regimes, base evaluation and backtracking. The separate anchor exception is not component replay. No analytic/AD derivative, coloring or simultaneous perturbation, sparse LU, changed pivot, approximate dependency, persistent cache, cross-map reuse or fixture-specific N/S solver is permitted.

## The two independent reuse exceptions

`INV-LANDSURFACEENERGY-162` in replay evidence admits represented-snow ground/soil anchor columns only after a successful complete same-solve evaluation and validated snow boundary. Ground residual is `(T_ground-T_snow)/1e-9`; each soil row is `(T_i-T_i,begin)/1e-9`, using the same subtraction/division order. Snow boundary temperature—not the ground coordinate—drives longwave and lower exchange; there is no covered ground vapor/storage/conduction dependence. Copy the complete current residual vector and replace only that row, then still apply the canonical centered/inward finite-difference arithmetic. This is not an analytic Jacobian entry. Any invalid dependency routes complete before evaluation. Tests include multiple soil layers, changed current coordinates, both bounds and poisons, exact residual/Jacobian comparison, and zero constitutive calls for admitted anchors; other columns stay complete except independently admitted 164 replay.

`INV-LANDSURFACEENERGY-163` admits current-to-maximum leaf result reuse only inside one occupancy/evaluation. Retain a private Copy successful current-leaf state. The maximum request changes exactly beta to one and every other input must match. Reuse is legal if current beta is **exactly one**, or if the returned branch is `Inactive`/`ExactZeroPar` and beta is proved unread. `RespirationDominated`, `PositiveAssimilation` and unclassified branches remain complete unless the exact-beta-one condition separately holds. Current sun and current shade calls still precede maximum sun and maximum shade, preserving earlier failure. Never retain a failed state or transfer it to another evaluation/occupancy/solve. Forced-exhaustive tests compare every field and full solves, exact beta one, zero PAR/inactive, the neighboring beta below one at positive PAR, centered/inward beta columns, natural errors and real closure/call counts.

## Dependency physics that must remain exact

The surface-energy and VEG current authority use reciprocal component longwave with `tau=exp[-0.8*Omega*(L+S)]`, unit emissivity, and area-weighted component fourth-power emission (zero-area branches explicitly zero). Do not replace current sun/shade/wet/stem temperatures by bulk canopy/shared-air temperature or stale output. The shared canopy-air temperature/humidity node has zero storage and closes all component sensible/vapor fluxes plus one ground exchange against one reference-atmosphere exchange. Neither a second ground term per occupancy nor independent stratum air nodes is admitted. Neutral aerodynamic/surface conductance inputs retain their domains and source geometry; no hidden wind floor, fixed forest attenuation, roughness substitution or stability correction is imported.

Signed liquid-vapor energy is `v*(h_l(T)+L_v(T))`, with `h_l=4218*(T-273.15)` and `L_v=2.501e6-2369*(T-273.15)` in the named units. Negative condensation is a mass credit and makes no withdrawal request; positive evaporation is finalized use. Rain keeps its retained phase/enthalpy provider and runon its accepted source temperature; no reference-temperature fallback replaces missing input. Beginning ground/root availability excludes current rain, runon, throughfall, initial/second drainage, stemflow and overflow.

Potential demand is evaluated once from immutable beginning owners. Hydrology authorizes that demand with exact layer/request identity; final evaluation recomputes the full coupled system under immutable caps. The independent law is computed before min-with-cap; equality selects the cap branch with zero cap derivative. Negative accessible water rejects before minimization; frozen/dry/nonroot branches are exact zero. `0<=F<=A<=D` and owner/request identity use exact comparisons, not a tolerance or rounding repair. Root continuity and normalization include law/cap operands in their specified order and scales.

Only after final withdrawal/condensation does current-ingress partition into retained surface, infiltration to the first soil layer and routed outlet occur, with each own sensible enthalpy once. It does not feed back into already accepted fluxes. LSE producer/consumer obligations P-001–P-004 require immutable primitive operands, sealed lineage, independent validation and atomicity, exposed branch/support/tolerance/residual diagnostics rather than defaults, and retained schema-v8 terminal publication rejection until the separately reviewed joint cutover. Errors 030–040 retain complete rollback. Independently reconstruct local balances from physical primitives before `f_t` OFE reduction; producer residual zero, weighted cancellation, gas/latent aliases or duplicate opposite heat do not prove closure.

VEG gas exchange is not a temperature-independent black box. At exact zero PAR (either signed zero) and positive leaf area, gross assimilation is zero, net is `-Rd`, stomatal conductance is `g0`, resistance conversion remains canonical and finite, and direct `cs/ci` calculation requires positive conductance/surface CO2 and `ca<ci<pressure`; hydraulic beta remains one with the specified gas transpiration law. At positive PAR, try the historical bracket first; if unbracketed with `F(ca)<0`, construct the canonical dark upper endpoint only when `ca<ci_dark<pressure` and `F(ci_dark)>=0`, then use the same Brent solver. Positive gross with nonpositive net retains `gs=g0`, `ci>=ca`; compensation equality has no epsilon. Daytime positive-assimilation vulnerability rules do not become night defaults. Inactive zero-area branches have zero physical gas flow/demand and no gas call. Caller profiles/state must be complete and explicitly validated; demonstration parameters do not become calibrated defaults or transferable scientific evidence.

## Calling lifecycle, support and validated custody

`map-custody.md#INV-LANDSURFACEENERGY-159` permits only the specifically named validation-once work removal. Exact immutable resident/configuration/topology revision and trusted prefix custody replace selected duplicate validation; an external/restart/durable reconstruction is untrusted and needs full validation. Cached digests alone are not a resident proof. The parent plan is lazy at the first charged structural check; no charged map means no plan. Do not hoist that check ahead of an earlier failure. Forcing normalization retains the exact pointer/support/transformation-generation/digest/normalized-bit proof, consumed later only for the same forcing. V8 dynamic checks remain fresh. Surface ingress stays after V8 and before native evaluation. Both native validation omissions are allowed only with the exact resident revision, beginning/envelope/topology/transaction/predecessor/support/prefix-head/tail-chain bindings at their original positions. Ordinary paths carry no native proof.

No projected-object cache, broad Arc lifetime, cross-generation proof, wire/restored proof or generic Clone is authorized; the limited inseparable whole-resident private clone preserves its exact revision. Changed generation, tail, model, state or support rejects directly without fallback. Initial/history/final ordering and direct-before-composed, Half1-before-Half2 (the latter from the authentic Half1 ending) survive. C-019's real 52-map workload and `1/52/52` relevant validation counts, exact output parity and every competing-field poison require real instruments; declarations/fake counters are insufficient.

`INV-LANDSURFACEENERGY-161`, `INV-SNOWENERGY-086` and `INV-COUPLEDTIME-030` admit one private physical prefix per charged map. The ordinary path has its full ordinary prefix; the native represented-snow path requires both resident authorities, nonempty covered tiles, empty active-litter receipts and byte-retained inactive custody. `Initial@0` returns a private validated physical-only endpoint. `FixedPointAdjudication@1`, then contiguous `MultisecantAdjudication(n)@(n+1)` for `n=1..N`, `N<=5`, mint a non-Clone/non-wire pending value only after full physical/identity/discrete validation. Its candidate is compared with **its own** output under outer tolerance, then its dependent outputs with the preceding authentic map. Outer nonclosure consumes history without error; dependent-only nonclosure consumes typed adaptive rejection without history/final construction. Full closure consumes that same pending map once as `FinalAccepted` and continues its own prefix through complete constructors. It is not another physical map, predeclared final role or promotion of Initial/history. Constructor failure cannot reinterpret the pending value.

Thus success has `M=N+2`, `2<=M<=7`, under the unchanged eight-map ceiling: M charges/validated physical endpoints, M−1 nonfinal outcomes, one final disposition, one final-constructor attempt, one complete private envelope. Physical failure counts its charge but no validated pending endpoint; dependent-only rejection has its validated pending/rejection but no history/final; final-constructor failure has the final disposition/attempt but no envelope. All maps/direct/rejected/unselected candidates publish zero; only selected composed-parent atomic commit publishes once. Tests require ordinary/native frozen, mixed, thaw/refreeze, wet canopy and multi-OFE prefix equality against forced complete, every role/disposition/topology/identity/one-ULP poison and exact actual failure counts.

Role/ordinal/charge-reserve mismatch is `DirectV11RealConsumerError::AdaptiveRefinement` before physical execution; identity/support/topology/native posture/beginning/discrete custody/leak mismatch is `Identity` before mint or the affected constructor. Physical `CoveredBoundary`/`Stage3` errors and downstream runtime/vegetation/serialization/construction variants retain existing precedence. No collapse into role failure or retry fallback is allowed. Premature publication is coupled-time `ERR-CT-018 PublicationState`, exposing nothing.

The current outer multisecant computes dimensionless residuals using quantity-specific `TOL-SNOWENERGY-007` scales, not mixed-unit Euclidean products. Its depth-one coefficient is `-(r_k dot (r_k-r_prev))/||r_k-r_prev||^2`, bounded to `[-0.75,1]` for finite nonzero values. Zero/nonfinite/degenerate/repeated-endpoint proposals refuse without another charge. Every admitted full proposal is finite, domain-valid and distinct from both endpoints; independent coordinates follow the prescribed proposal, depth reconstructs from selected ice/density, and V22 phase selection is retained. It forbids outer coordinate finite differences, ULP/Q lattice or receipt-cycle searches, second physical replay, historical polish and Picard fallback. This prohibition does **not** repeal inner LSE finite differences. Its tolerance table governs temperature `1e-5 K + 1e-9 relative`, soil temperature `1e-8 K`, heat `1e-5 W m^-2 + 1e-8 relative`, vapor `1e-10 kg m^-2 s^-1 + 1e-6 relative`, water `1e-6 kg m^-2 + 1e-9 relative`, energy `1e-6 J m^-2 + 1e-10 relative`, density `1e-6 kg m^-3`, depth `1e-9 m + 1e-9 relative`, humidity `1e-12 kg kg^-1 + 1e-8 relative`; branch/topology/settling/support/transaction/receipt/carry identities remain exact. The old exact-density convergence rule is superseded only here, not generally.

LSE support admission has an exact `60_000_000_000 ns` physical floor before Newton (`041` below-floor, `042` support join, `043` post-event support errors). A structural 1 ns interval is not a physical promise. The common positive minimum is the maximum over **active** physical participants, excluding inactive owners. Each side of a proposed terminal split must be zero or at least its own minimum. Event candidates also pass independently reconstructed time, snow-mass, liquid-mass and energy bounds. Ranking is smallest tick displacement, then ordered normalized mass/energy score, then earliest tick; zero tolerance requires exact zero error and contributes zero, never a fabricated unit denominator. Receipts retain proposed/accepted ticks, complete candidate digest, both support sides, errors, tie rank, tolerance/policy and owner lineage. No candidate means `ERR-CT-021`, unchanged owners/clock/receipts/reductions and the admitted parent retry; never drop a remainder, freeze snowfree state, scale a longer LSE result or run below the physical floor. Earlier 600-ms evidence is superseded and cannot be reused as 60-second qualification.

Coupled time owns checked u128 ticks, exact half-open support and one staged accepted clock. Numerical seconds are derived once by the specified ties-even conversion and shared bit-identically. Constraints are reduced by tick, closed class order, owner and digest; incompatible coincident facts reject. Each attempt starts from one accepted complete-owner set; inactive owners are carried unchanged. Attempt IDs/retry counts cannot enter physical equations or accepted identity. Rejection discards all candidates and preserves accepted controller/clock/owners/receipts/ledgers/reductions/publication bytes. Slabs install all participants atomically only after exact joins and independently closed ledgers. Zero-duration events integrate no rate, join explicit owners and cannot replay; scheduled-once operations are receipted once per named boundary, not once per retry. Parent sequence advances exactly once at full commit; direct exposure is forbidden before the durable publication outbox transaction. Restart retains accepted chronology, owner bytes, controller state and operand lineage, never rejected iterates or replay capabilities. Coupled-time numeric error precedence is 001–021 then 027, earliest canonical identity within class, not hash-map order.

Candidate-only soil continuation is read-only physical beginning authority, not a publishable owner/cache: exact original prepared resident, predecessor unpublished trial/seal and contiguous next-child support must validate; the soil transaction stays its authenticated target even when outer source differs. Its typed projection has no owner/restart/checkpoint/accepted-receipt bytes. Final authentic acceptance alone replays the complete original prepared owner, accumulated physical operands, exact selected ending and full layer-credit chain, seals once and installs atomically. A deferred same-tick soil-only close requires the exact pending V11 parent-finalization authority and preserves vegetation's persistent predecessor before ordered logical vegetation finalization; no owner rebasing or inferred transaction adjacency is allowed.

At the first positive snowfree child, the complete accepted represented-snow prefix is authenticated through coupled slab/owner/terminal-event/topology/inactive-custody receipts ending exactly at the split (`INV-SNOWENERGY-087`, CT-031). Consuming it establishes only the WB14 local cursor, with no under-snow phase/ingress/WB14/routing/receipt/reseal work and no physical ordinal consumption; the first actual snowfree physical child is ordinal zero with unchanged beginning cumulatives. Restart retains this proof, and missing/reordered/foreign/gapped/duplicated prefix or cursor movement without it rolls back every owner and receipt.

## Required correctness evidence and audit accounting

C-020 requires a test-only forced-complete oracle which bypasses reuse but executes the same canonical evaluator and arithmetic. Compare **every** copied/recomputed node and returned field, raw residuals, tolerances, normalized vector, dense Jacobian, pivot sequence/matrix norm, frozen/reselected branches, full Newton/backtracking trajectory, diagnostics, requested/authorized/final water, liquid routing, final owner/receipt/output bytes and first typed error. The real two-occupancy/six-soil fixture must exercise reciprocal longwave, duplicated wet routing and terminal descendants; both uncapped potential and fixed-final solves and real consumer closure are required. Equality only after output aggregation, same accepted answer with changed iteration path, or synthetic leaf-local fixtures alone cannot close the requirement.

For a fully interior centered sweep, total logical probes are `2*(10N+3+S)`: anchor synthesis `2*(1+S)`, component replay `8N`, and complete evaluation `12N+4`. The real N=2/S=6 fixture therefore has 58 probes: 14 anchors, 16 component replays, 28 complete. The eight hydraulic, four beta and two shared-air columns remain complete. These are per-sweep interior-centered expectations, not whole-run constants and not permission to suppress inward/boundary sweeps. A full run must be compared to an independently reconstructed oracle of its **actual** topology, coordinate bounds, selected stencils, signs and eligibility.

Reset audit state immediately before columns. A `Completed` record means all canonical columns completed. `Failed` records the first actual error and only actual attempts; `ShortCircuited` is permitted only if a real canonical nonerror short-circuit exists, otherwise it is absent. `RejectedBeforeProbe` is a column outcome, not a sweep-lifecycle replacement. Count attempted signs and actual work in the appropriate logical bucket; neither expected counts nor a preallocated record proves execution.

Every sealed sweep joins independently authentic caller map/solve/iteration/sweep identity, potential/final posture, N/S, graph schema, coordinate and actual stencil. A producer's identical ordinal, pointer, hash or self-generated key is not the independent join. Release aggregation retains every sealed record, class/stencil distinction, lifecycle histogram and exact sums, joins the actual map/solve corpus, resets per run and excludes stale/cross-run accumulation. Disabled audit must not allocate. The structural seven-item/top-level classifier is a declaration/source-shape guard, not a reachability test: empty or dead declarations can look structurally green and provide no runtime execution or counter proof.

Required vector groups are independently applicable, not alternatives: exact direct-edge graph/golden hashes and edge-deletion mutations; all centered and admitted inward stencils and neither-admissible rejection; N/S scaling; every unchanged/crossable/infallible node family; authentic generation/capability/once-only/restart poisons; complete-versus-replay first-error order and competing failures; all copied fields and full-solve behavior; real runner outputs and independent closure; truthful scoped and aggregate counts. Missing one group cannot be compensated by more timing samples.

## Historical gate and prospective experimental limits

`replay-evidence.md` retains a historical three-run CPU-0 release keep-or-revert gate. It binds one unchanged candidate executable and exact source/input identity, built once then used three times with `timeout 1800 taskset -c 0 env RUST_MIN_STACK=67108864 CARGO_PROFILE_RELEASE_LTO=false nix develop -c cargo test --release -p openwepp-runner --lib hillslope::tests::stage3_laned_release_one_ofe_positive_baseline_profile -- --ignored --exact --nocapture --test-threads=1`. Select the independently sorted middle integer from the exact JSON fields `run_wall_us` and `physical_phase_wall_us.potential`, not a median of their sum or another interval. The historical gate requires complete run aggregation plus a qualifying 58/14/16/28 named interior-centered sweep. Historical median run wall must be at most `4,803,570 us` and potential solve at most `253,431 us`, each at least `100,000 us` below its bound baseline (`4,903,570` and `353,431 us`). Every run preserves `48/56/20/32/4` controls, source `0.8488061229561478`, outlet `0.8471105124736579`, storage `0.0016956104824910018` and zero clamp count, with peak RSS at most `65,536 KiB`. Any timing, identity, count, closure or RSS failure requires full reversion of that production delta, not partial retention. These numbers are engineering gates, not physical tolerances and not results measured in this exercise.

The separately named prospective EXP-R is **A plus R alone**, not F+R and not a reconstruction from convenient historical binaries. It changes only the historical centered-existential coverage requirement, fixed timing ceilings and 64 MiB experiment retention criterion for the newly frozen comparison. Historical FAIL/HOLD remains unchanged; retained production remains HOLD. No new equation, domain, derivative, stencil arithmetic, LU, branch, stopping criterion, physical process, support/event cadence, owner/restart/publication behavior or hidden production selector is admitted. Actual nonvacuous stencil-aware coverage replaces a fixed fixture-shaped run pattern; do not manipulate state to produce a desired centered sweep.

The PC1 prospective restriction is stronger than general component replay: for sun/shade replay, current base beta must be **exactly one before capability commitment**. Then current leaf still executes and can return its natural error; maximum may reuse through the same current-to-maximum helper. Any other beta or unproved case takes complete evaluation before commitment, without inventing a physical-domain rejection. Wet/stem replay needs proof that **all** leaf-call operands are identical, including changed downstream routing effects. Adjacent beta tests for both leaf kinds/occupancies, runtime evidence and independent correctness/QA review are required before behavioral admission. The finite 198-trial search is not proof of universal absence of a leaf error.

SG1 is R-only and covers the specified five recomputed wet/routing fallible families, including lower descendants. It does not assume base success plus a bounded temperature proves every wet guard: in particular it does not establish the `pressure - 0.378*e_sat` denominator margin. Instead those recomputed nodes execute the unchanged complete guard/call body with the canonical signed-probe operands and frozen branch, at the same source position, producing either the exact successful fields **or the same typed first error**. Copied fallible nodes still need exact unchanged-operand/successful-base dependency proof, including earlier guard order. Unknown dependencies remain pre-commit complete selection; PC1, nonwet proof duties and rollback remain unchanged. Zero area, wet/dry, exact capacities, every affected lower route and authentic natural errors must be tested without fault-injection substitutes, with dual review/verification before experimental admission.

The frozen protocol/inputs/kickoff/handoff further require:

- Freeze complete tracked source, new files and deletions, exact Cargo.lock/Nix/toolchain/compiler/profile flags, runtime fixture/configuration, source-composition manifest and executable identity. Capture Cargo JSON compiler-artifact records and build outside measurement; execute the frozen binary directly with exact harness arguments. Hash source/binary before and after, retain current dirty/adjunct composition, and distinguish common A changes from F and R deltas. The current reproduction recipe identifies v05 manifests, A04/SG1 and separately composed F05/R05 arms; narrow source archives or `/tmp`-dependent `.venv` symlinks are not portable scientific inputs. Those recipe identifiers are **source claims**, not an actual reconstructed input manifest verified here.
- ORACLE_AND_AUDIT runs outside **both** timing and memory measurement. Before timing, force complete evaluation and prove exact full-result/science/output/custody parity, natural/competing errors, rollback and restart. A/R timed runs use matched optional-audit and compact-counter posture. No detailed replay audit is silently charged to only one arm. Science closure is independently reconstructed from produced primitive operands, including WAT5 source, HBP outlet/storage and clamp; volatile fields require an explicit frozen allowlist. Equality is an experiment assertion, not a new solver stopping rule.
- Fix the same allocator, environment and CPU affinity: CPU 0 if permitted, otherwise the prospectively recorded lowest allowed physical performance core; one libtest thread. Any source/executable/harness/toolchain/environment change starts a new series; retain old samples. There are two fresh-process warmups per arm followed by 12 balanced pairs in order `AB BA BA AB AB BA BA AB AB BA BA AB`, with independent F and R comparisons against A. Per-run timeout is 600 seconds for 1/10/19 OFE cases and 1800 seconds for teardown. Unexpected nonzero exit, timeout, missing parity or identity mismatch stops candidate admission; failure is not a fast sample.
- Measure the complete real consumer including publication. `CLOCK_MONOTONIC` brackets the specified interval; the internal Instant and process CPU or `/proc` tick intervals may have slightly different enclosing boundaries and must be labeled. Retain wall and process CPU, absolute paired B−A and B/A, paired medians and descriptive 95% bootstrap uncertainty (10,000 resamples, seed `20260906`). The decision requires at least 5% median wall improvement with lower interval above zero and CPU regression no more than 2%. One extension to 24 total pairs is allowed only when overlapping uncertainty could change the priority decision; otherwise report inconclusive with bounds. At most one performance refinement is authorized.
- Slow/high-RSS samples are valid observations. Exclusion requires documented environmental invalidity such as wrong affinity, missing fields/identity, observer error or competing heavy work. Retain excluded/failed samples and allow at most one infrastructure rerun; never retry selectively until results look favorable.
- Memory uses six fresh pairs `AB BA BA AB AB BA`. The parent samples the ready child PID's VmRSS at 100 ms with monotonic timestamps and reports readiness and gaps. Sampled active peak, endpoint VmRSS and lifetime `wait4` high-water RSS are distinct Linux-KiB metrics; none is live requested heap. Capture pre-fixture, pre-run, end, postvalidation and post-drop boundaries plus selected smaps. Drop snapshot, audit telemetry and parsed JSON before the post-drop observation; fixture deletion is a separate event, not a substitute for dropping live values.
- Teardown runs use three processes per arm with ten one-day runs each; this tests repeated allocation/drop, not seasonal state continuity. If timing is competitive with no memory increase, or a prospectively useful scaling question remains, run three pairs for 10 and 19 OFE. Compare the new engineering budget `128 MiB + 16 MiB/OFE` and post-drop return `8 MiB + 1 MiB/OFE` separately, including baseline failures. The old 64 MiB gate does not decide the prospective experiment. Unresolved memory may warrant one targeted bounded experiment, never global allocator/stack/ASLR/host changes or a fabricated heap interpretation.

The F feed-forward permission in the whole snow dependency is a separate experiment, not a second lever for R. It removes only the second same-invocation carrier call from a structurally feed-forward request that cannot express ending hints/coupling ordinals. Full/Retry/Half1/Half2/Root, discovery/shortened exact endpoint, batch and canonical-final invocations remain independent even with equal payloads. Pre-change 400 calls are direct evidence in that authority; inferring 200 groups is not direct pre-change evidence. Prospective F must measure its actual invocation multiset and force two-call complete-result parity and source-real error order before timing. A result from one outer invocation may never supply another. R's Jacobian cache cannot become that process-global cache.

The current frozen handoff says the experiment is paused by the owner: optimized A/F/R complete runs are FAIL and controlled timing, memory, teardown and scaling are NOT RUN. I did not resume them or read old logs. Those frozen dispositions are neither a measured negative result from this exercise nor evidence of speedup/qualification. No scientific/typed proof can be waived because a prospective timing or relative-memory number improves.

## Inputs still required and review disposition

A concrete correctness review or run must supply, rather than infer: N/S and exact coordinate/rank/identity order; model/schema/configuration and all required canopy geometry/area/optical/aerodynamic/gas/root/soil constants; immutable owner states, soil water/caps and snow boundary/receipts; exact forcing temperature/humidity/pressure/rain/enthalpy/exposure/support bits; current trial and both canonical signed probes; potential versus fixed-final posture, every request/authorization/cap and frozen branch; successful complete base and graph topology/version/hash; authentic map/solve/iteration/sweep/generation/capability custody; expected direct edges and independently selected stencils; complete node and error/rollback corpora; real caller traces and audit records; and full frozen A/R source/build/executable/input/environment identities if experimental claims are made. None of these actual run payloads, independent outputs, measured counters or runtime logs was provided by the reading exercise. A canonical fixture specification is not an actual captured run input.

For production work, applicable contract invariants, typed guards, A0/A1 and touched A3 suites, independent operand reconstruction and direct real-consumer closure remain non-deferrable. Numerical/chronology/custody/operand-lineage changes require the testing strategy's conservative critical impact analysis and immediate campaign-strength correctness regression, with exact source/diff/input evidence. Preserve source-coupled guards semantically; do not delete or weaken assertions to make a migration pass. If required-case bindings/authority suites are touched, the source anti-evasion script and `auth11_required_suite_obligation_guards_contract` must run. Reuse of prior evidence requires identical relevant experiment/evidence inputs or an explicit reviewed exclusion proof; changed criteria cannot retrospectively bless a failure. Dual independent correctness and QA review, accepted-fix re-review, and package-required independent verification remain required. A compiler check, static classifier, comparator agreement or this report cannot replace those workflows.

The terminology in the testing strategy and older authority model differs in some A-class descriptions; the operative common obligation is unchanged here: canonical authority, hard invariants and touched constitutive suites bind, and legacy/observational comparisons alone do not establish correctness. No calibration parameter is introduced by replay. Missing observations constrain empirical/calibration/transferability claims separately; they do not authorize surrogate physics or excuse missing correctness proof.

Verdict: **READY for the bounded requirements reading exercise only.** Production/replay correctness and experimental admission are **NOT ESTABLISHED / NOT RUN** in this review. No implementation finding is asserted. Future execution owner: the explicitly authorized implementation or prospective experiment package, subject to its current paused/HOLD posture and the next independent correctness gate above.


## Inventory-to-answer procedure and source accounting

Before drafting, I froze a private source-derived inventory of 54 independently applicable predicate groups. During a claim-and-omission crosscheck against the actual solve/admission sources, I retained version 1 and wrote version 2 adding three groups: the complete global error/payload order, full-step witness/direct inactive anchor/scaling details, and caller warm-start/nested-equivalence/active-branch order. Those details were added to private draft version 2 **before** report completion. The resulting 57 inventory groups all map to answer sections; the check tests both claims present and independent obligations otherwise omitted. Names alone were not accepted as substitutes for operative conditions. The mapping helper verifies coverage of the inventory indices and selected source lines/hashes; it does not mechanically prove semantic completeness. The semantic check is this reader's responsibility. Private inventory/notes do not replace required authority reading.

I checked the complete requested source coverage and SHA-256 continuity after drafting. Every selected whole source has all lines requested; the testing-strategy file intentionally contains only §§7–10 and §§17–18 plus recorded discovery headings. All original truncated selected reads were recovered by bounded rereads. The recoveries included dependency-replay 120–480, terminal tail/replay introduction, water introduction, VEG 1010–1150/1520–1740/1505–1525, SNOW 550–625/607–614/1340–1444/1535–1590/2090–2160/2895–2960, and later complete solve/admission rereads for the omission check. Previously visible surrounding lines plus these recoveries cover each selected extent. Truncated search output is not an omission in selected authority: the matching files were independently read in full.

Counts below are UTF-8 **source request bytes**, not model tokens, effective context-window bytes, latency, execution time or telemetry. “New” is first line-range coverage within the explicit read ledger, and “repeat” is overlap with earlier explicit reads of the same source path. A truncated request still counts its full requested bytes; recovery adds repeat bytes. Thus requested bytes are not falsely presented as exactly delivered tool-output bytes. File hashing/coverage computation reads files internally without displaying authority text; those I/O bytes are not additional model source exposure. Line endings belong to the source byte counts. A request means one path/range, not one grouped shell invocation.

Measured explicit reads: **77 requests; 1,175,198 new + 308,999 repeated = 1,484,197 requested bytes**. LSE entry/chapters alone: **204,019 new + 63,180 repeated = 267,199 bytes**. Discovery/claim searches: **4 source-bearing/search-pattern requests**, 21,665 matching-source bytes, of which 497 are additional unique heading bytes beyond the selected read ledger. Union of explicit reads and search source coverage: **1,175,695 unique bytes**. The mandatory external contracts contribute substantially; I make no runtime-context saving claim from the smaller unique LSE count. The unique LSE bytes and total LSE request bytes must not be conflated.

Automatically supplied source-equivalent exposure: root AGENTS instructions were present in the initial user message before explicit reading. The current file form is 9,508 UTF-8 bytes, already included as S01 in explicit reads; this is a separately identified source-equivalent copy, **not an assertion of exact automatic-delivery payload length**. Task/environment/system instructions and the later automatic compaction summary are additional context whose exact delivered UTF-8/runtime accounting is UNOBSERVED. The compaction summary was derived from this reader's own source work and private ledger; it was not new authority or parent evidence. These unobserved categories are not silently reported as zero or folded into model-token metrics.

Derived private material: inventory versions, report drafts, ledger summaries and helper source are separately identified below. Inventories/report drafts were authored privately and were not displayed back through a source-read tool. Their constructed byte sizes are reported, not added to source-authority bytes. The one detailed derived per-file summary display was 2,681 UTF-8 bytes. Later compact account/check outputs are derived metadata; their exact JSON bytes can be reproduced from retained helpers/results and are not source reads. File listings, `wc -l`, instruction-discovery paths, failed nonexistent-path requests and `git rev-parse HEAD` are metadata, with zero source-text bytes. Initial unavailable `python` and a nonexistent ADR spelling failed before source exposure; `.venv/bin/python` was then used. No helper was silently edited: exact versions and hashes are retained. No production code or numerical workflow ran.

### Source identities

Each request below resolves to the complete source path and whole-file SHA-256 in this table. Hashes authenticate source bytes, not scientific correctness or remote trust.

| ID | Source | SHA-256 |
|---|---|---|
| S01 | `AGENTS.md` | `9bd7887bcae731debc64689a4774ac222d529bcd527ebea2293e3ffd600289f6` |
| S02 | `docs/work-packages/AGENTS.md` | `29e2f48a0b69fd9f43eb8d3a6dc2b72d30792337f90a2141b89df105726282a5` |
| S03 | `docs/work-packages/20260907-lse-context-adoption-correction-001/package.md` | `0cb2505989026d5e911d696eb7eda8d96c5ebd5498da421e55a2f7682290929b` |
| S04 | `docs/work-packages/20260907-lse-context-adoption-correction-001/artifacts/worker-handoff.md` | `280db6a24f5e3197cbc05e2a521dbd9e1817ab17b9e0dfbba4fe9490642cb217` |
| S05 | `docs/work-packages/role-review.md` | `2193e9538ad22dad5678fc5bb59080f686a543f697fec4983208214a73cc5eb1` |
| S06 | `docs/work-packages/science-obligations.md` | `1a0377a3362a6c34d783796b6756854f8630e1f05e994dc3d963eaafd16c542e` |
| S07 | `docs/specifications/science-contracts/AGENTS.md` | `bace4ab3e33119e0239f700f92c53707d884f297e92d2454a86c477758c930c7` |
| S08 | `docs/standards/AGENTS.md` | `b62a33d9a70a3d3f45e7eccdcf05ca3a194486fcdbfc7bd439371cf8f0ae3015` |
| S09 | `docs/standards/prompt-wording-guidance.md` | `294385612b565fe8be3e9e530caa0792c901406ecf53abc9402649e5568ffe3f` |
| S10 | `docs/specifications/science-contracts/contracts/SC-LANDSURFACEENERGY-001.md` | `391772bfc9345357a654552ae84203809a91e0280f5579693ae73da68119cc53` |
| S11 | `docs/specifications/science-contracts/contracts/SC-LANDSURFACEENERGY-001/interface.md` | `758873659d360c8edfc7bcec7aa6cb58d065d44e221e890166390252810ed481` |
| S12 | `docs/specifications/science-contracts/contracts/SC-LANDSURFACEENERGY-001/dependency-replay.md` | `6ce8ffe21317027f5d1171579a9059c64c7ae3bd9ba9181ceea91323254b6b25` |
| S13 | `docs/standards/numerical-solver-architecture.md` | `46089ca55bb565b7dc67ea1b2075bbcc16db6902eeba1952572b072abadd0738` |
| S14 | `docs/specifications/science-contracts/contracts/SC-LANDSURFACEENERGY-001/common-details.md` | `b930bbd0f0ce2c16d826be6a6d1d0bb4e6fe0155e611232047e0b4205e2dbfc4` |
| S15 | `docs/specifications/science-contracts/contracts/SC-LANDSURFACEENERGY-001/numerical-methods.md` | `172438a4196f13e3b9260845680abc81b98c55d3af044dbda744f5a1ca6932f8` |
| S16 | `docs/specifications/science-contracts/contracts/SC-LANDSURFACEENERGY-001/surface-energy.md` | `2c3afa3f25827508ab9bd4826249d6557d08aa44b8f4ba7f0ecd5c988a900bb8` |
| S17 | `docs/specifications/science-contracts/contracts/SC-LANDSURFACEENERGY-001/nonlinear-solve.md` | `0c0dd52cb94ceafd48aeac5c5555cae9ac4eb1ce81b47f93a2f4708e1e64881b` |
| S18 | `docs/specifications/science-contracts/contracts/SC-LANDSURFACEENERGY-001/solve-boundary.md` | `62e13854fe0bdddb71a4e5aae874aa13c89431bef3d47526b9af973ec8014b54` |
| S19 | `docs/specifications/science-contracts/contracts/SC-LANDSURFACEENERGY-001/soil-coupling.md` | `a85b0aa76a404143b049a56a25225250b4eddeabdf8ac2fc895c1aee27a4434d` |
| S20 | `docs/specifications/science-contracts/contracts/SC-LANDSURFACEENERGY-001/terminal-support.md` | `2222800b0e5fa73d281ba68ec21961a6fcc8b38401f9c59d045139e2671dd709` |
| S21 | `docs/specifications/science-contracts/contracts/SC-LANDSURFACEENERGY-001/replay-evidence.md` | `d29eeca69cf60d1a0953f6fb0cc2ac3841c100159961f01394fdbdf17d5403b7` |
| S22 | `docs/specifications/science-contracts/contracts/SC-LANDSURFACEENERGY-001/map-custody.md` | `8508b3ceaa49f081ce8c2df1d7cce39249d8f3875fea5a017404c0816cdae2c1` |
| S23 | `docs/specifications/science-contracts/contracts/SC-LANDSURFACEENERGY-001/water-vapor.md` | `fda2350a1d4bf826dd3ad6f622e4ee0f41d708860aef6005e8948c27c3d729f8` |
| S24 | `docs/decisions/0044-prohibit-accretive-production-solver-dispatch.md` | `a597aaf802917e1ed9c21db1f4453c571dde09ad48684c0f7d0ff9062b6645e6` |
| S25 | `docs/work-packages/20260906-stage3-prospective-mechanism-experiments-001/artifacts/experiment-protocol.md` | `40033ea2e536984ae5a7455e99509e7e760624b78cdb08ee347597fe2f5f115f` |
| S26 | `docs/work-packages/20260906-stage3-prospective-mechanism-experiments-001/artifacts/authority-input-reproduction.md` | `1c813fd08e34763808d3ac8a033d18d8c5176857aff76378aa126762166c8883` |
| S27 | `docs/work-packages/20260906-stage3-prospective-mechanism-experiments-001/prompts/active/kickoff.md` | `6f3f0852afbfae138c4e40c62e15bf727c569611d00dec08bc8338e05ebdd9b3` |
| S28 | `docs/work-packages/20260906-stage3-prospective-mechanism-experiments-001/artifacts/worker-handoff.md` | `b3ca642a6b4fd94f6e1459f07058cd9fc18fab747368421fc0dc6e591d143183` |
| S29 | `docs/specifications/science-contracts/contracts/SC-VEGETATION-001.md` | `3b1c8525e6d09d7afae6e27d5fa59f90347d55d8bf34bc6292d0d767d3bfc5fa` |
| S30 | `docs/specifications/science-contracts/contracts/SC-SNOWENERGY-001.md` | `94eb7a73ed2d0de50f5a354a200467809154c2e4ecfa0792677942343cc4943d` |
| S31 | `docs/specifications/science-contracts/contracts/SC-COUPLEDTIME-001.md` | `1d10611b5143be201381f283ec5594d9f65c99f8252e944dfae57076a8a699f7` |
| S32 | `docs/standards/testing-and-gate-strategy.md` | `56277d9848d80b0672ebeaaa8277ac2ad9888c90e54b4f31aa7c5b8ca9b04230` |
| S33 | `docs/specifications/correctness-authority-model.md` | `64275bad708f353c2c3b66f5b1475c070254591e26278d0a4ec442cad6e6eb0f` |
| S34 | `docs/prompt_templates/assurance-findings-template.md` | `95373d3c7df1f0a7e3ad7640fa22c0e137095e6cf5c4d92e3803846eb3f6ee83` |

### Complete explicit path/range request ledger

All ranges are inclusive. Phase is the retained helper-recorded phase; `expansion/recovery` includes routed expansions, output recoveries and the final source claim check. The first helper used complete-file arguments; v2 admitted explicit line ranges. No source request is omitted merely because its bytes repeat.

| # | Source | Range | Phase | New UTF-8 bytes | Repeat UTF-8 bytes | Total |
|---:|---|---|---|---:|---:|---:|
| 1 | S01 | 1–121 | reading | 9508 | 0 | 9508 |
| 2 | S02 | 1–105 | reading | 6642 | 0 | 6642 |
| 3 | S03 | 1–75 | reading | 5304 | 0 | 5304 |
| 4 | S04 | 1–36 | reading | 2649 | 0 | 2649 |
| 5 | S05 | 1–7 | reading | 1400 | 0 | 1400 |
| 6 | S06 | 1–98 | reading | 6149 | 0 | 6149 |
| 7 | S07 | 1–84 | reading | 6532 | 0 | 6532 |
| 8 | S08 | 1–58 | reading | 4052 | 0 | 4052 |
| 9 | S09 | 1–202 | reading | 12057 | 0 | 12057 |
| 10 | S10 | 1–67 | reading | 4090 | 0 | 4090 |
| 11 | S11 | 1–66 | reading | 7931 | 0 | 7931 |
| 12 | S12 | 1–533 | reading | 48043 | 0 | 48043 |
| 13 | S13 | 1–152 | reading | 6742 | 0 | 6742 |
| 14 | S12 | 120–275 | expansion/recovery | 0 | 14674 | 14674 |
| 15 | S12 | 276–480 | expansion/recovery | 0 | 12316 | 12316 |
| 16 | S14 | 1–221 | expansion/recovery | 17798 | 0 | 17798 |
| 17 | S15 | 1–107 | expansion/recovery | 9876 | 0 | 9876 |
| 18 | S16 | 1–205 | expansion/recovery | 11494 | 0 | 11494 |
| 19 | S17 | 1–101 | expansion/recovery | 6707 | 0 | 6707 |
| 20 | S18 | 1–114 | expansion/recovery | 8368 | 0 | 8368 |
| 21 | S19 | 1–219 | expansion/recovery | 13780 | 0 | 13780 |
| 22 | S20 | 1–244 | expansion/recovery | 20914 | 0 | 20914 |
| 23 | S21 | 1–255 | expansion/recovery | 19959 | 0 | 19959 |
| 24 | S22 | 1–170 | expansion/recovery | 19110 | 0 | 19110 |
| 25 | S23 | 1–227 | expansion/recovery | 15949 | 0 | 15949 |
| 26 | S21 | 1–18 | expansion/recovery | 0 | 2233 | 2233 |
| 27 | S20 | 235–244 | expansion/recovery | 0 | 2393 | 2393 |
| 28 | S23 | 1–19 | expansion/recovery | 0 | 1922 | 1922 |
| 29 | S24 | 1–79 | expansion/recovery | 3817 | 0 | 3817 |
| 30 | S25 | 1–180 | expansion/recovery | 11541 | 0 | 11541 |
| 31 | S26 | 1–84 | expansion/recovery | 5128 | 0 | 5128 |
| 32 | S27 | 1–263 | expansion/recovery | 25550 | 0 | 25550 |
| 33 | S28 | 1–59 | expansion/recovery | 3718 | 0 | 3718 |
| 34 | S29 | 1–400 | expansion/recovery | 33697 | 0 | 33697 |
| 35 | S29 | 401–900 | expansion/recovery | 32809 | 0 | 32809 |
| 36 | S29 | 901–1400 | expansion/recovery | 54931 | 0 | 54931 |
| 37 | S29 | 1010–1150 | expansion/recovery | 0 | 18712 | 18712 |
| 38 | S29 | 1401–2000 | expansion/recovery | 54662 | 0 | 54662 |
| 39 | S29 | 1520–1740 | expansion/recovery | 0 | 13565 | 13565 |
| 40 | S29 | 2001–2400 | expansion/recovery | 25417 | 0 | 25417 |
| 41 | S29 | 2401–2750 | expansion/recovery | 22438 | 0 | 22438 |
| 42 | S29 | 2751–3080 | expansion/recovery | 24875 | 0 | 24875 |
| 43 | S30 | 1–350 | expansion/recovery | 22717 | 0 | 22717 |
| 44 | S30 | 351–700 | expansion/recovery | 60063 | 0 | 60063 |
| 45 | S30 | 550–625 | expansion/recovery | 0 | 32318 | 32318 |
| 46 | S30 | 701–1050 | expansion/recovery | 20578 | 0 | 20578 |
| 47 | S30 | 607–614 | expansion/recovery | 0 | 8908 | 8908 |
| 48 | S30 | 1051–1300 | expansion/recovery | 25489 | 0 | 25489 |
| 49 | S30 | 1301–1470 | expansion/recovery | 120303 | 0 | 120303 |
| 50 | S30 | 1340–1400 | expansion/recovery | 0 | 17007 | 17007 |
| 51 | S30 | 1401–1430 | expansion/recovery | 0 | 36329 | 36329 |
| 52 | S30 | 1431–1444 | expansion/recovery | 0 | 29464 | 29464 |
| 53 | S30 | 1471–1720 | expansion/recovery | 47139 | 0 | 47139 |
| 54 | S30 | 1535–1590 | expansion/recovery | 0 | 24339 | 24339 |
| 55 | S30 | 1721–2020 | expansion/recovery | 25350 | 0 | 25350 |
| 56 | S30 | 2021–2320 | expansion/recovery | 54449 | 0 | 54449 |
| 57 | S30 | 2090–2160 | expansion/recovery | 0 | 24481 | 24481 |
| 58 | S29 | 1505–1525 | expansion/recovery | 0 | 6555 | 6555 |
| 59 | S30 | 2321–2720 | expansion/recovery | 27951 | 0 | 27951 |
| 60 | S30 | 2721–3120 | expansion/recovery | 60357 | 0 | 60357 |
| 61 | S30 | 2910–2960 | expansion/recovery | 0 | 20874 | 20874 |
| 62 | S30 | 2895–2909 | expansion/recovery | 0 | 11867 | 11867 |
| 63 | S30 | 3121–3350 | expansion/recovery | 16231 | 0 | 16231 |
| 64 | S30 | 3351–3559 | expansion/recovery | 17158 | 0 | 17158 |
| 65 | S31 | 1–240 | expansion/recovery | 13862 | 0 | 13862 |
| 66 | S31 | 241–540 | expansion/recovery | 19946 | 0 | 19946 |
| 67 | S31 | 541–780 | expansion/recovery | 33959 | 0 | 33959 |
| 68 | S31 | 781–1108 | expansion/recovery | 24785 | 0 | 24785 |
| 69 | S32 | 193–345 | expansion/recovery | 8067 | 0 | 8067 |
| 70 | S32 | 439–473 | expansion/recovery | 1262 | 0 | 1262 |
| 71 | S05 | 1–7 | expansion/recovery | 0 | 1400 | 1400 |
| 72 | S33 | 1–221 | expansion/recovery | 11159 | 0 | 11159 |
| 73 | S34 | 1–12 | expansion/recovery | 736 | 0 | 736 |
| 74 | S17 | 1–101 | expansion/recovery | 0 | 6707 | 6707 |
| 75 | S15 | 1–107 | expansion/recovery | 0 | 9876 | 9876 |
| 76 | S18 | 1–114 | expansion/recovery | 0 | 8368 | 8368 |
| 77 | S21 | 19–63 | expansion/recovery | 0 | 4691 | 4691 |

### Search/source-equivalent ledger

Matching source lines, including original newline bytes, are counted separately from generated path/line prefixes. New-before-read search coverage overlaps the later explicit-read new counts and is **not added twice**. The additional-unique column is the source coverage outside the selected explicit line ranges. The first two discovery patterns preceded the respective reads; the later patterns followed full reads. Empty search is retained.

| Phase | Request/pattern and source | Exact matched source lines | SHA-256 reference | Source bytes | New before read | Repeat after read | Additional unique |
|---|---|---|---|---:|---:|---:|---:|
| heading-route-discovery | `rg -n '^#&#124;^<a id=' docs/specifications/science-contracts/contracts/SC-LANDSURFACEENERGY-001/map-custody.md` | 3,13,14,17,18,19,23,24,35,36,37,87,88,89,156,157,164,165 | S22 | 636 | 636 | 0 | 0 |
| heading-route-discovery | `rg -n '^#&#124;^<a id=' docs/specifications/science-contracts/contracts/SC-LANDSURFACEENERGY-001/water-vapor.md` | 3,16,17,20,21,22,65,66,67,118,119,120,146,147,171,172,209,210,218,219 | S23 | 765 | 765 | 0 | 0 |
| review-governance-discovery | `rg -n '^##&#124;findings' docs/standards/testing-and-gate-strategy.md` | 8,17,33,52,77,129,131,137,164,172,182,193,219,224,230,235,240,274,292,314,346,368,386,403,417,427,439,457,474,487 | S32 | 793 | 793 | 0 | 497 |
| claim-crosscheck | `rg -n 'seven&#124;classifier&#124;Structural&#124;structural&#124;minimum&#124;ci&#124;100_000' docs/specifications/science-contracts/contracts/SC-LANDSURFACEENERGY-001/replay-evidence.md` | 3,6,7,8,9,54,60,64,65,69,78,84,89,93,102,104,118,143,183,190,200,205,222,230,236,243,251 | S21 | 3472 | 0 | 3472 | 0 |
| claim-crosscheck | `rg -n 'seven&#124;classifier&#124;Structural&#124;structural&#124;minimum&#124;ci&#124;100_000' docs/specifications/science-contracts/contracts/SC-LANDSURFACEENERGY-001/dependency-replay.md` | 3,6,15,16,41,43,53,79,82,83,93,95,105,107,108,116,129,139,172,173,174,175,176,177,178,183,200,201,205,206,208,212,228,230,247,248,251,266,305,314,335,351,357,392,395,400,405,418,419,420,421,422,426,428,455,457,476,484,488,492,493,494,502,521,522,523,530,531,533 | S12 | 15999 | 0 | 15999 | 0 |
| claim-crosscheck | `rg -n 'classifier&#124;seven&#124;top-level' docs/specifications/science-contracts/contracts/SC-LANDSURFACEENERGY-001/replay-evidence.md` | none | S21 | 0 | 0 | 0 | 0 |

Filename-only discovery requests were `tools/agents/find-agents --for <assigned artifact>`, `rg --files docs/decisions` filtered for `0044`, and `rg --files docs/work-packages` filtered for `/(role-review|findings-template)\.md$`. `wc -l` on VEG/SNOW/CT reported line counts only. The testing-heading search also named nonexistent `docs/work-packages/guidance/role-review.md` and produced no source from that path. The broad late replay search output was truncated; its full matching-source request bytes are counted, and the source files were already completely read. These metadata or failed-path requests do not produce hidden new authority.

### Private procedure, notes and helper identities

These local artifacts are private to this reader and are not repo writes. The source ledger is retained across compaction. Generated helpers are measurement aids, not authority. The final report's own hash is deliberately excluded from its own preimage and is delivered separately.

| Private artifact | Constructed bytes | SHA-256 | Exposure/class |
|---|---:|---|---|
| `/tmp/lse-solver11-read.py` | 685 | `258c0af98abefbf57300fae2e29ec98d86f88149a1e549def6fdb42ea65f3778` | derived private text/helper; not a source read |
| `/tmp/lse-solver11-read-v1.py` | 685 | `258c0af98abefbf57300fae2e29ec98d86f88149a1e549def6fdb42ea65f3778` | derived private text/helper; not a source read |
| `/tmp/lse-solver11-read-v2.py` | 836 | `467b59b8d52465f213231f02c91071ef6291771340c3dbe86727580a775e1103` | derived private text/helper; not a source read |
| `/tmp/lse-solver11-ledger.jsonl` | 20879 | `779e5d9db4db66453185b79d1998effc146c2bddab487a44f23cfd35b5ca918f` | primary explicit request metadata |
| `/tmp/lse-solver11-inventory-v1.md` | 15199 | `d58d1d04d9c37ea3147f11f4160dad537838272b29f28332bcc258d7d2ffeb56` | derived private text/helper; not a source read |
| `/tmp/lse-solver11-inventory-v2.md` | 15661 | `193d4458d29f8ba9a03857e4b367442e01076c30d703ebf736ccaef278d94e1f` | derived private text/helper; not a source read |
| `/tmp/lse-solver11-inventory-v3.md` | 15889 | `b1a402c5d4ad411720c5288bf630defac1dbeeefb48c0ad49a86d66d570a2a95` | derived private text/helper; not a source read |
| `/tmp/lse-solver11-report-draft-v1.md` | 55893 | `15bb6061fd6b37cd8ca0fe1c81f8672d6cae70939c14204afe042ab5f60f51fd` | derived private text/helper; not a source read |
| `/tmp/lse-solver11-report-draft-v2.md` | 58068 | `5af8e2c1369d2a034109a3beda7534c94dbc67671c37ac81d757ee3b8184adaf` | derived private text/helper; not a source read |
| `/tmp/lse-solver11-report-draft-v4.md` | 58577 | `84b280d6536a1ba9bbd9b865d881db0cc5a9f92e154cfdb60c98fd5622b30e96` | derived private text/helper; not a source read |
| `/tmp/lse-solver11-account-v1.py` | 3159 | `bc2704f6c2bb66b4b9c8e0da58c4148b8fa78acda9488d55333dc3c9ddf2c31b` | derived private text/helper; not a source read |
| `/tmp/lse-solver11-account.json` | 325 | `0022a5e479f2410a276089dad4bf1a9358c1fac3cfd7ded77ee4cc93d280bcaf` | derived private text/helper; not a source read |
| `/tmp/lse-solver11-search-ledger.json` | 5262 | `8d2510e8a71bdbe077a6fce95335fa29d7bfa06666e70afa8caa5d4e2814cdb9` | reconstructed search source-line metadata |
| `/tmp/lse-solver11-check-v1.py` | 2434 | `dc96b74c82d10b2806e0625708086c9fd8debc5f9059837269bac33cb9c1fa73` | derived private text/helper; not a source read |
| `/tmp/lse-solver11-check-results-v1.json` | 4412 | `2383eb1341f61b7c7f247180ce8c1d04f274dd612ddc28cfe5ecb707454c0082` | derived private text/helper; not a source read |
| `/tmp/lse-solver11-omission-check-v1.json` | 16938 | `ac170e60100813f47a25efa82d33ae98a1f645e36989042533a9b48370386bb0` | derived private text/helper; not a source read |
| `/tmp/lse-solver11-assemble-v1.py` | 9649 | `202c550741eb89405362bf2090f1a597dcca6c8691f7f720773ddb2203677969` | derived private text/helper; not a source read |
| `/tmp/lse-solver11-assemble-v2.py` | 9689 | `61d9b007749dd2f0e67a77e956699eb85d65c3d9b3d1495fbaedf5728c1074c8` | derived private text/helper; not a source read |

The first completed report is frozen after these checks. No later answer repair is authorized under this exercise.
