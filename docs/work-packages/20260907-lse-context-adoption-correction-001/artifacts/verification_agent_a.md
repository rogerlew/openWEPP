# Independent terminal verification A

Static: authority, instruction, acceptance, preservation, claims and reuse review.
Ran: independent Python tests, public strict/unit checks, primary input hashing,
isolated generator comparison and exact-diff checks below. No Rust suite rerun,
scientific experiment, performance trial or actual adoption by this verifier.
Role/session: NEW terminal verifier A `/root/terminal_verify_a`; requested role
effort medium; effective runtime settings UNOBSERVED. Only this artifact written.

Reviewed identity: `c0081f3e7f7fbdde54b7ff3992ef92fe4c2f8982`, plus the inspected
terminal EOF-count correction in gate-results/finding-disposition. Canonical source
remains frozen f70; candidate-manifest SHA256
`fc8604ac789f5edd00183b3675dfc2dfbeb5dda6ad8fedf14158cb093ba18d89`.
Corrected gate-results SHA `90aae1f170edf309d89cd12f507094b4de7e195bfdd3bfec710ca64f98968f48`;
finding-disposition SHA `29ab85cfbff0cd9dbd9397a5f671de25953cbcc68509163050845a37f1f9a0e9`.

## Independent checks and findings

All commands ran in `/workdir/openWEPP`; temporary reproduction output stayed outside
the repository. Instruction discovery returned root AGENTS.md and work-packages/AGENTS.md.
Read those, role-verification, package/intake, normal handoff, assurance template,
testing strategy sections 7/9/10/18, correctness authority, applicable science
instructions/obligations, format adoption gates, intake masking matrix and stable
review/disposition evidence. The normal handoff is continuation state, not the frozen
reader input. I did not read or copy verifier B's artifact.

- Ran `.venv/bin/python -m pytest -q -p no:cacheprovider tests/python/test_sc_contract_directory.py tests/python/test_check_sc_binding_exposure.py tests/python/test_lse_directory_migration.py`:
  98 passed in 4.09s; `/tmp/lse-terminal-a-pytest.log`. This executes actual parsing,
  original-source statement/scientific-cell/fence preservation and structurally valid
  equation/failure-posture poison rejection, rather than checking artifact presence.
- Ran `.venv/bin/python tools/check_sc_binding_exposure.py --strict docs/specifications/science-contracts/contracts/SC-LANDSURFACEENERGY-001.md`:
  exit0, 17 exposure rows/100 actual definitions, structural evidence only.
- Ran `.venv/bin/python tools/release/check_sc_unit_compliance.py --path docs/specifications/science-contracts/contracts/SC-LANDSURFACEENERGY-001.md`:
  exit0, no unit findings.
- Ran independent inline Python/hashlib/subprocess assertions: exit0; all30 candidate
  hashes,22 evidence log hashes,156 current inputs, source/input manifest ties and
  actual `/tmp/openwepp-lse-correction-bridge` SHA match. Binary SHA is
  `1a28114fcbcf0df9e6987c168143cec175bd5ce148b1c5bbc9b5fdc23c71837b`.
  Corrected membership contains241 experiment/67 evidence_claim/13 publication
  unique paths; publication is included in evidence_claim and every candidate path
  belongs to experiment. Only the two explicitly future verifier records were absent
  at this check. This verifies membership legitimacy and current bytes, not final
  detached identity completion or portable rebuilding.
- Ran generator independently with `.venv/bin/python <artifacts>/build_candidate.py
  --output-root /tmp/lse-terminal-a-modes-cuirul4a/{candidate,adopted}`, adding
  `--adopted` for the latter: both exit0. All20 default files match live exactly;
  adopted output differs solely by159 entry bytes, with exact notice-replacement
  equality. Candidate total369049, bootstrap4090+7931=12021. No live activation.
- Independently applied activation-guide-proof in memory: old/new SHA match,
  exactly one replacement and +187 bytes. Its new text retains historical HOLD,
  full normative authority and the adoption requirements elsewhere in the guide.
- Ran baseline `git diff --exit-code fb32d27f2 --` for crates/tools/Cargo/flake
  paths and separately the complete predecessor package: exit0, empty changes.
  Production, frozen-runtime predicates and predecessor HOLD are not repaired here.

Static: the prospective correction boundary is legitimate. It requires direct
migration evidence while retaining every inherited diagnostic; it does not waive
a touched scientific acceptance gate. All18 failures have individual relevance and
masked-suffix decisions; M1–M7 duties execute independently in the migration tests,
M8 has actual compiled-loader positive/negative/legacy-equality evidence, and M9
retains source-coupled assertions in the affected Rust run. Matching failure names
alone would be inadequate. Bound logs retain111PASS/18FAIL and36 normalized panics;
root Clippy retains17 diagnostics and separately verified helper lint. These Rust,
bridge and lint results are reused attributed execution, not my reruns. Unchanged
production/tool/config inputs plus the inspected test-loader scope justify reuse;
the new document preservation/negative tests pass independently. No scientific
production campaign, external-suite/cohort or required-case mutation is hidden in
this scope. Vegetation2843-line WARN and before3000 split obligation remain intact.

Accepted fix verification: current initialization table preserves EACH positive
final authorization's identity AND amount equality AND FullSupply, and EACH exact
zero request's identity/zero. Surface09 explicitly states those predicates; closure08
retains full accepted-solve and real-consumer/operand duties. Independent byte proof
verified replay-evidence differs from82b9 by exactly the approved251-byte insertion;
both frozen protocol hashes match. Reading the primary kickoff/protocol establishes
late VmRSS endpoint separately from peak and post-cleanup; the65536KiB historical
gate still applies to JSON rss_kib. Identity03 already distinguishes those operands;
solver12 does so under the clarified source/procedure. This is a corrected reading
answer, not a new scientific measurement or changed historical gate.

All four primary report hashes match reading-iteration12. Retain surface09
204063@c2dd, solver12 204270@f70, closure08 208928@645b and identity03 31428@6938.
Science unions meet211890; identity is smaller than264863. Their independently
reviewed selected-duty reuse remains bounded to each original task/cut; the relocated
qualification retains frozen protocols, and physical tasks do not claim retention
qualification. Earlier failed reports remain failed. Current four-report claims do
not pretend four fresh successes on one cut. Solver12 gross LSE345307 exceeds the
original file; its204270 finite unique union is a different quantity. Procedure11
adds1063 governance bytes and changes the treatment; no identical-prompt comparison,
source-only causal inference, general first-try reliability, workflow-token or actual
scientific execution claim is warranted. Primary closure08 retains missing p61
capture, WAT5 exact-vector and unchanged real-consumer duties, without fabricated
numeric closure. These limitations remain explicit in current claims.

RCA-S01 membership correction: independently rechecked and accepted. Claim-bearing
Markdown and signoffs are evidence_claim members, including publication overlap;
command/executable bindings are explicit experiment members. Custody12 is STATIC
reading/source custody; its loader source is not compiled-binary custody. Actual
compiled bytes match the separate evidence manifest, but final explicit custody and
detached three-identity collection are still pending terminal outputs.

TVA-01, low, exact-diff whitespace scope: independently ran
`git diff --check fb32d27f2`, exit2 with THREE EOF findings: audit-details:481,
history:39 and newly committed immutable solver12 report:405. Current-edit
`git diff --check` exits0. The parent corrected the gate row and chronology; I
accept preserving these non-scientific endings without altering report/source
hashes. This is an accurately disclosed nonblocking disposition, not broad PASS.

Non-deferral: no current migration gate is silently reassigned to future work.
Dual terminal readiness, approved actual activation, affected final checks, final
compiled-byte custody/identities and both final activation/status inspections remain
required here before completion. Historical scientific HOLD belongs to its unchanged
authority boundary, not a newly deferred correction obligation. No missing role or
future output is treated as passed.

## Readiness verdict

PASS for proceeding through the exact planned activation after BOTH new terminal
readiness verdicts pass. No unresolved A readiness finding. Adoption/closure itself
is NOT YET VERIFIED: retain the candidate restriction until the parent applies the
authorized transition. Any extra scientific, dependency, threshold, protocol or
substantive claim change reopens affected assurance. Final bounded verification will
be appended here after the actual activation and custody/status/identity checks.
