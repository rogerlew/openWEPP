# Recorded reading results — executing
Static: task/rubric and acceptance remain frozen. Ran: fresh agents' actual source
requests, independently checked by source hash/range arithmetic. Delivered tokens,
automatic runtime context, quota and complete parent/child workflow totals are
UNOBSERVED. Requested exposure includes repetitions and truncated requests; complete
source recovery is assessed separately. These are source-byte measurements.

The LSE comparison baseline is the original264863-byte whole-contract requirement,
not an observed model-token baseline. Each scientific task needs an adequate first
answer and an actual LSE union<=211890; identity must be smaller than the original.
All governance, external authority and measurement-tool requests remain separately
visible in each report; LSE savings are not the same as total reading savings.

Fresh01/02/04 completed frozen b3b7b57f4. Independent A/B results:01 PASS196206,
02 scienceFAIL despite211480 sizePASS,04 scienceFAIL despite196206 sizePASS.
reading-iteration-05.json includes all actual source/search exposure and immutable
report identities. Corrected fresh02/04 and final-cut reuse proof remain required.
Identity03's first adequate report is exercise_03_retry_03.md at6938ca97d:31428 LSE
bytes,149505 all-selected bytes,149667 requested exposure. Both reviewers verified
its23 selected inputs unchanged at7627; at b3b7b57f4 only interface adds11bytes
clarifying executable identity. They independently proved the exact executable
question still triggers the same full qualification and frozen protocol duties.
This is semantic evidence reuse with original observed counts, not a new reader
or23 identical final file hashes, and not four fresh successes on one source cut.

Earlier first answers and failures remain immutable in reading-iteration-01.json
through reading-iteration-05.json and their individual reports. No smaller projected
route substitutes for a failed actual union; no carried-context answer correction
is a fresh success. Current iteration5 projection196206/211480/31439/196206 is only
preflight, not sufficient-reading evidence. First closure exposure in iteration1
was invalid due to cross-agent answer exposure and is explicitly retained as invalid.
Iteration3 closure stopped early without an actual external blocker and remains FAIL.

The full current directory set is348958bytes across18files, still larger than the
original monolith. Bootstrap entry/interface is12117bytes. The optimization claim,
if remaining gates pass, is selective reading for the tested tasks, not smaller
on-disk authority or a measured end-to-end model-token improvement. Original scientific
obligations and unconverted whole-contract reading duties remain binding.
