# Independent intake review A

Static: package/handoff/execute at scaffold `258756d44`; root, work-package and
science AGENTS, reviewer procedure, applicable science obligations, testing strategy
sections 7–10/17–18, directory-format entry/dependency/definition/reference/adoption
requirements, and retained predecessor source/review/exercise evidence. Reuse the
COMPLETE original LSE reading and frozen independent rubrics in predecessor
`artifacts/review_agent_a.md`; no rubric or answer is supplied to fresh exercise agents.
Ran: instruction discovery, read-only status/source inspection, hashes and integer
byte arithmetic only. No implementation, candidate, test or experiment authored/run.
Role `/root/review_a`; requested correctness effort high; effective model/effort
UNOBSERVED. Sole assigned write is this artifact. Others' work is preserved.

Reviewed package SHA-256 `b9a48fe16fc0b5b3f2503d187934c57b6928e3578441329e5af6eb7feacd903b`;
handoff `e7eef4d673f86351009dc62666a0809bf7f3443379da9bfff9aae1760537874d`.
Canonical original LSE independently still hashes to
`7a002bcac2ad640716b52f4bd59326f241d5fd3084d26e56f6a703eb496ed61d`, 264863 bytes.
Predecessor HOLD, initial exercise failures and subsequent carried-context corrections
remain evidence with their original dispositions, not retroactive success.

## Prospective acceptance legitimacy

PASS in principle for a new user-authorized package prospectively bounding adoption
validation before implementation. This is not permission to reclassify predecessor
required failures. Directory-format adoption gate 4 requires all affected checks;
testing strategy permits exact impact classification and identity-bound reuse.
Direct passing migration checks may establish a new bounded migration claim while
unrelated inherited production failures remain FAIL diagnostics, provided every
changed/affected obligation and every potentially masked relevant assertion is
actually exercised. Merely repeating the same 18 failures is insufficient.

Each inherited failure needs individual source-level relevance and masking analysis,
including assertions skipped after its first panic, followed by direct tests for
all affected protected predicates. New expected values must derive from original
science/protected expectations independently of the candidate generator. A test
that simply agrees with generator output or checks IDs/phrases cannot prove qualifier
preservation. The generic loader/reference/inventory tests require real downstream
consumer-path coverage and negative cases. Reviewer B owns the detailed masking
matrix; I do not certify it unseen. Any unresolved current masking/authority gap
blocks adoption. No production fix, assertion weakening, protected identity rewrite,
Clippy suppression or global gate change is authorized.

This prospectively scoped migration makes no changed-physics/output claim, so actual
physics campaigns are not created merely by reorganizing Markdown. It still must
preserve every scientific obligation, source-coupled expectation and historical FAIL.
Actual semantic changes to science, test strength or unbounded tooling behavior
reopen impact classification under testing strategy section 8. Required current
FAIL/BLOCKED/NOT RUN remains incompatible with completion; no silent deferral.

## Findings to freeze before substantive edits

| ID | Severity / location | Evidence and required resolution | Disposition |
| --- | --- | --- | --- |
| ICA-01 | medium; package acceptance 4 | `target <=12 KiB` is ambiguous for a stated exit criterion. Freeze a hard <=12288-byte UTF-8 limit for entry plus interface AND the finite union of any unconditionally required LSE sections. Each science task's sufficient recursive LSE union must be <=211890 bytes (at least 20% below 264863); identity must be <264863. Include all relevant table/definition/assumption dependencies, not just prose bodies. No post-miss weakening. | OPEN, exact prospective wording needed. |
| ICA-02 | medium; acceptance 2/4/5 and chapter freeze | Freeze exact chapter names, applicable section boundaries/triggers, baseline tasks and independent rubric identity before substantive edits. Distinguish predicted finite-union source bytes from observed first-attempt selected/read bytes. A fresh failed answer remains FAIL; a corrected candidate/route needs a new nonforked agent for affected tasks, with no expected-answer hints. Shared-route substantive changes reopen every affected task. | OPEN, record in intake/package freeze. |

These are concrete freeze requirements, not requests for new user permission. Parent
can resolve them within authorized scope. Prospective acceptance is approved once
these exact bounds and route/freshness constraints are frozen; implementation
acceptance is not yet unconditional at the reviewed scaffold. Stable-cut scientific
preservation and final adoption are wholly unassessed in this intake.

## Compact mechanism boundaries

The parent's proposal to retain the existing 14 chapter names and add normative
`common-details.md` is acceptable as a prospective physical layout. Freeze that list
and its necessary live-link updates before substantive changes. Keep the unchanged
format grammar: entry inventory, binding-index definitions/schema coverage, and
actual marked canonical definition rows remain the authority locators.

- Entry: identity/revision/status, complete inventory, precise routes, and an explicit
  distinction between scientific authority, production activation and empirical/
  release qualification. No copied catalog of 100 definitions or generated aliases
  without actual live-reference need.
- Shared interface: short effective regime/model scope, signs/units/bases, exclusive
  owners, immutable inputs, exact identity, atomic rollback and qualification limits.
  These universal constraints cannot become conditional merely to meet 12 KiB.
- Normative common-details: retain original detailed baseline tables, general guards,
  variables, test vectors, gaps and provenance once, with concrete section triggers.
  A physical-equation task needs its variables/units/assumptions; solver work needs
  applicable domain/error/test obligations; closure needs primitive/receipt/receiver
  and real-consumer requirements. If a detailed rule is universally needed, count
  that section in the unconditional budget or move its effective rule into the core.
- Surface-energy/soil-coupling/water-vapor: keep complete affected radiative/turbulent,
  finite/equilibrium CN, signed vapor, immutable authorization and accepted ingress
  rules together with their precise owner boundaries. Soil coupling must reach the
  positive-support admission section even for snow-free work; its location beside
  terminal text does not make the floor optional.
- Nonlinear-solve/dependency-replay/map-custody: keep the complete ordered numerical
  algorithm and V10 exceptions together; dependency replay selects all affected
  evaluator/error/graph authorities. Represented-snow requirements may omit inactive
  litter/WB14 physical algorithms while retaining immutable custody and affected
  anchor/output/error predicates. Full owner implementation/audit claims expand them.
- Litter-phase/soil-custody/surface-custody: accepted-primitive closure selects signed
  phase vapor, bounded fusion, raw/retained/spill, liquid-only WB14, exact dyadic and
  binary64-basis ordering, parent chronology and actual receiver duties. It need not
  preload EXP-R or execute represented-snow algorithms. Accepted primitives cannot
  establish a constitutive recomputation or invent a missing `L_s(T)` family.
- Qualification: precise historical v31 and prospective EXP-R/PC1/SG1 sections. An
  identity-only task selects its actual frozen capture protocol; correctness claims
  select its graph/fallibility/qualification conditions. History contains resolved
  past context only; scientific/production FAIL, uncertain assumptions and unfulfilled
  current duties remain normative.

Section-level routing already exists in directory-format `Mechanism chapters and
dependencies`; no format amendment or global reading relaxation is needed. Selected
sections include their directly referenced definitions/assumptions recursively, with
concrete Required-when conditions. Existing neighboring single-file whole-contract
rules remain unchanged and their bytes are reported separately. Cross-contract
scope/whole-file obligations cannot be silently replaced with a local excerpt.

## Effective rules and source mapping

Reuse the predecessor's complete source-span preservation evidence, but re-review
all changed consolidation, new concise statements, definitions, scope metadata and
section edges against original authority. Raw span retention in common-details does
not by itself prove an effective short route is correct. Bidirectional mapping must
classify identical relocation, permitted presentation encoding, deduplicated identical
wording, and any explicitly superseded clause with exact authority/applicability.
Scientific undecidability requires HOLD, not a convenient historical label.

The predecessor's RA-P01–11 remain the risk inventory. Especially retain: V3's narrow
supersession of named future/missing/M_l-mutated wording while exact-one/failure/owner
rules survive; immutable V1/V2/V3 regimes and different ice constants; V2's physically
interrupted FullSupply/scaling/inactive-wet clauses; exact-bound INV-138 and full/
first-valid INV-139 witness; physical 60-second support versus nanosecond chronology;
INV-156 spill extension to old surface operand enumeration; V27 same pending physical
prefix finalization; distinct INV-162/163/164 reuse scopes; V30 original source-position
and competing-poison order; and historical fallibility versus PC1/SG1's narrowly
specified experiment substitutions. No heading-age heuristic adjudicates authority.

Fresh task rubrics remain those frozen against the COMPLETE original, including the
qualifiers the predecessor agents initially missed. Put required material in primary
mechanisms and concrete routes; do not put the rubric answers into exercise prompts.
A correct first answer from a fresh agent on the final route plus independently
reviewed dependencies and byte ledger establishes bounded usability. Predicted
source-size PASS, structural PASS, or carried-context correction alone does not.
No full workflow/token/quota saving follows from local LSE byte counts.

## Logical references and optional aliases

The parent reports no incoming physical `.md#` LSE fragments in its current search;
that search is parent evidence, not an independent exhaustive verification here.
Existing format permits either explicit entry compatibility aliases or updating every
unpinned live consumer; unused aliases need not be generated. Logical SC-ID#INV/OBL
references must continue resolving through the unique binding registry to the actual
normative definition. Dropping unused entry aliases while preserving that registry
is therefore legitimate and should materially reduce unconditional entry bytes.

Before adopting, bind a complete live-reference inventory covering relevant docs,
tests/tools, registry/templates and actual loader consumers, including encoded or
alternate link forms where present. Distinguish physical fragments from logical IDs.
If any live physical fragment survives, retain a unique direct nondefining alias or
update that consumer; never leave a broken link, alias chain or second definition.
Pinned Git/history/package references remain immutable and resolve their original
revision. Test logical resolution and actual physical-reference behavior directly;
zero textual grep matches alone is not proof of all loader semantics. Do not add a
large universal alias catalog without a demonstrated compatibility obligation.

## Intake disposition

Legitimate direction, with ICA-01/02 requiring prospective freeze before substantive
edits. No scientific narrowing is approved. Later stable-cut review must inspect the
actual compact primary text, applicable recursive section union, coverage, protected
consumer behavior and fresh first-attempt answers. Required dual review/verification
and exact-diff reconciliation remain current. Predecessor HOLD is untouched.


## Focused prospective freeze follow-up

Read `artifacts/intake.md` before canonical implementation. ICA-01 CLOSED: it makes
all numeric thresholds hard and includes unconditional linked sections in the
12288-byte union. The exact 15-chapter envelope, preserved logical IDs, normative
common-details and historical-change-log-only handling are approved prospectively.
Keeping the 18 original universal invariants plus C-004 in the concise interface
is a sensible anchor, provided other universal owner/failure/producer constraints
remain explicit or unconditionally included. A predicted 6 KiB definition inventory
is a sizing hypothesis, not measured complete shared-interface adequacy.

Compact provenance spelling `v31:Lx-Ly` is acceptable when one always-read declaration
binds it unambiguously to the exact full source commit and original contract path.
It abbreviates a duplicate locator, not its authority. Preserve original scientific
citations, exact row/section endpoints, evidence labels, canonical Statements and
actual local guard/test links. The package map is audit evidence, not the only way
a primary-contract reader can determine source identity. Do not use a v31 shorthand
to replace independent external literature/legacy authority.

ICA-02 remains OPEN for an actual prospective task-to-section route matrix with
anchors, extents and concrete applicability/expansion triggers. `Use precise sections`
is direction, not a frozen selection. No full final candidate byte ledger is required
at this stage, but the intended recursive route must be reviewable before substantive
edits; it may conservatively expand later without weakening acceptance. Complete
shared material cannot be made conditionally invisible by naming it common-details.
The required fresh first-attempt posture is now explicit in intake.md.

Read incoming-references.json structurally: 99 recorded rows, all `historical=true`,
from five old report paths plus the predecessor candidate archive. Retaining their
bytes is correct; record their actual frozen-source/historical rationale rather than
inferring it solely from a work-package path. This inspection is not a new exhaustive
repository search. Existing live-link/logical registry integrity remains a direct
validation obligation. Reviewer B's intake report was not yet present at this check;
its detailed masking conclusions have not been independently reviewed here.


## Final prospective intake disposition

Static: complete frozen task/section matrix and final incorporation in `intake.md`,
plus Reviewer B's complete independent 18-failure masking analysis, M1–M9 direct
checks, Clippy boundary and identity-reuse requirements. Ran: read-only hashes only.
Reviewed intake SHA-256
`a3a625519b74f126e8c51fd2e302b4306643f745156503c0496af76a71c702fb`
(7925 bytes); review B SHA-256
`7b69c4ffd25341ef1051129c96fd8d6a2e63be2c3aa99bfcbbee654a9e69ae69`
(17705 bytes). These are source-bound intake approvals, not executed gate evidence.

ICA-02 CLOSED. The matrix now defines complete section extents, marked rows with
table headers, chapter dependency headers/introductions, a named common physical
set, exact existing unique mechanism headings, and role/regime expansion conditions.
All routes include complete entry/interface. Task 01 explicitly reaches positive
support; task 02 preserves full solver/replay/qualification and affected evaluator/
V30 boundaries; task 03 selects the actual frozen capture protocol; task 04 preserves
accepted primitive/phase/receiver/carry/support requirements with implementation
expansion rather than an invented solver audit. No concrete missing applicable
route obligation is identified at this prospective-design stage. Actual section
contents and recursive edges must still be checked after construction.

B's direct M1–M9 requirements are now acceptance by explicit reference. I agree
with the prospective legitimacy distinction: independently execute masked LSE
predicates, preserve reached-but-new-cut-required obligations and already-passing
consumer assertions, prove unchanged external reads and source inputs, and retain
every inherited failure as FAIL diagnostics. Source assertions skipped behind a
stale external check receive their own passing cases; no raw equal-failure-count
criterion replaces them. The direct Rust bridge, mutation and changed-code lint
requirements prevent successful Python string extraction from being mistaken for
actual consumer execution. This agreement relies on B's independent detailed
source/body analysis; I did not duplicate all 18 body inspections in this role.

Incoming physical-reference status is now expressly frozen predecessor evidence/
reproduction, not a blanket path-based exemption. Preserving those bytes while
retaining unique logical definition resolution is an acceptable boundary, subject
to final direct reference/loader validation.

**PASS — approve the prospectively frozen acceptance and exact design boundary for
implementation.** ICA-01 and ICA-02 are both resolved before substantive edits.
No additional user approval is required within this package's existing authorization.
This approves neither a candidate's scientific preservation nor measured usability,
first-attempt sufficiency, validation or adoption. Those remain current exit gates.
Necessary discoveries may expand reading and correction, while hard byte thresholds,
science rubric and fresh-retry criteria remain unchanged. An unresolved current
obligation blocks adoption; predecessor HOLD and all scientific production/experiment
qualification restrictions remain intact. Write ownership here remains this artifact
only; parent owns implementation and later independent review/verification continues.
