# Artifacts

Expected:
- `runoffpart-binding-crosswalk.md` — conservation proof + flagged additions
- `runoffpart-core-size-delta.md` — per-batch + total token/byte reduction
- per-batch `--strict` lint evidence + closure-loop gate logs (FROM comparator_suite_runner subagent) + test-reconciliation record
- promotion review/disposition/verification + `kernel-profile-compliance-checklist.md`
- per-row adjudication ledger with cited authority anchors
