# Final Disposition

Status: `complete`

Evidence mode: `Static + Ran`

Disposition: `HOLD / CLOSE_AS_MODEL_LIMITATION`.

EB-03 successfully established:

- canonical, typed sub-canopy longwave and cloud/daylight guards;
- one Stage 3 temperature/cold-content provider used by all candidate code;
- independent default-off longwave and sublimation selectors;
- exact-one vapor mass and latent-energy derivation;
- independent hourly longwave, mass, latent, and cold-content closure;
- no vapor-to-liquid alias and no legacy/new double debit;
- deterministic figures with human-readable sidecars; and
- byte-identical absent, empty, and explicit-disabled selector equivalence
  through the real direct-production runner.

The package does not satisfy its shared-provider exit criterion. The real S
cell fails after 18 days at `0 K` with `0.0360654 m` SWE remaining
and only `0.0013524 m` cumulative sublimation. LS fails the same typed boundary
after 14 days. Independent reproduction with bulk and multilayer density
providers rules out density geometry. The shortwave-plus-latent composition
can therefore drive the selected cold-content provider to its absolute-zero
bound even though local mass/energy identities close.

No clamp or empirical limiter is authorized. EB-04 is held and the campaign
stop-loss is invoked. `ASSURE-06` must carry this as an ecosystem-model
limitation unless new authoritative coupled-temperature/energy science
justifies reopening.

This is a negative scientific result, not a production-default regression:
both new selectors remain diagnostic/reproduction-only and default-off, and
the three post-change disabled-control forms are byte-identical. This is not a
pre-change binary identity claim.

## Exit-Criterion Disposition

| Criterion | Result | Evidence and consequence |
|---:|---|---|
| 1 | PASS | Both canonical contracts bind provider, selectors, cadence, thresholds, signs, guards, state mutation, and closure identities. |
| 2 | PARTIAL / HOLD | Local B/L/S/LS cells preserve frozen controls and selector orthogonality, but real S/LS execution is mechanically invalid. |
| 3 | PASS, LIMITED | Post-change absent/empty/disabled forms are byte-identical and focused regressions pass. No pre-change binary identity is claimed. |
| 4 | FAIL / HOLD | The real consumer reads the path, but S/LS drive the shared provider to `0 K`; the provider is not viable. |
| 5 | PASS, LOCAL ONLY | Independent longwave, vapor/latent, mass, and cold-content ledgers close locally; this cannot cure the unphysical provider trajectory. |
| 6 | PASS | Vapor is absent from routed melt/liquid and legacy/new sublimation double selection fails closed. |
| 7 | PASS | Polar night and missing provider state fail closed with typed errors. |
| 8 | FAIL / HOLD | The science exit fails and the required full `quick` profile did not pass due to assurance-test timeouts, although each timed-out case passes in isolation. |
| 9 | PASS FOR HOLD CLOSURE | Dual review, dual verification, line-count governance, exact-diff reconciliation, and final disposition are retained. |

The criterion table closes the package truthfully as `HOLD`; it does not
authorize EB-04 or promotion.
