# Transfer Lineage

Status: queued.

## Required Lineage

- `ui_SCrunf(1:24)` current-lane surface-saturation carry output.
- `ui_LfCrf(1:24)` current-lane lateral carry output.
- `ui_SUrunf(1:24)` downstream-lane upstream surface-saturation carry input.
- `ui_LfUrf(1:24)` downstream-lane upstream lateral carry input.
- `UpStrmQ`, `SubRIn`, `QOFE`, and PASS `runvol`/`sbrunv` publication
  consequences.

## Evidence

- Pending.
