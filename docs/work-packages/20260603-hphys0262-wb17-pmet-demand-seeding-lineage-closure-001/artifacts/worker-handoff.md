# Worker Handoff

Status: completed

Evidence mode: static + ran

Static:

- Recommended next package: `HPHYS0263 WB11/WB17 EVAPPM Demand Migration
  Closure`.
- Required authority: `/workdir/wepp-forest_260430_baseline/src/evappm.for`
  and `/workdir/wepp-forest_260430_baseline/src/watbal_hourly.for` at commit
  `dac3c950d8b16cc73774bf5ce2e7e11f80baac70`.
- Required contract scope: amend `SC-EVAP-001` and `SC-WATBAL-001` before code
  edits with explicit equations, aliases, guards, and migration provenance for
  `evappm.for:181-297`.
- Required implementation scope: port baseline-authoritative Penman-Monteith
  reference ET, `kcbadj`, `kcbcon`, `etke`, `TEW/REW`, evaporation
  coefficients, `TAW/RAW/wftrp`, and final `Es`/`Ep` into the openWEPP WB11 ET
  demand seam.
- Required inputs to verify/project: `radly`/`rad`, `radpot`, `tmax`, `tmin`,
  `tdpt`, `vwind`, `rhd`, `elevm`, `canhgt`, `lai`, `rtd`, `kcb`, `rawp`,
  `st`, `thetdr`, `thetfc`, `dg`, `solthk`, `fin`, and `resint`.

Ran:

- HPHYS0262 proves the continuation target: sidecar selection points at
  `evappm`, while current openWEPP demand still reports
  `evap_priestley_taylor`.

Success criteria for HPHYS0263:

- Contract-first `evappm` migration with red/green contract tests.
- H1/H7/H39 day-1 `Ep` demand is generated by the baseline-authoritative PMET
  branch when `iflget != 1`.
- Full H1..H39 metrics are rerun and continuation focus is reassessed from
  semantic residuals.
