# TESTGATE Attempt 30062059667

Status: `EXECUTION-AND-VERIFICATION-PASS; PUBLICATION-TIMEOUT`

Subject:

- base: `622c4e8084102c884a74556306e481a746f2f4b1`
- head: `dde456c6c968a557f2fee53a88b8f48a3d7329cf`
- run: `30062059667`

Ran:

- `openwepp/execute-increment`: PASS in `1h30m3s`.
- `openwepp/verify-increment`: PASS in `13m32s`.
- Full receipt/predicate binding, relocated audit verification, authenticated
  recovery index, native attestation, and exact-head authority checks passed.
- `openwepp/increment-gates`: cancelled at its `10m0s` job timeout while
  `Upload authenticated gate evidence` was still transferring the entire
  independently verified evidence tree.

The verified artifact already exists as
`testgate-verified-30062059667-1` with reported size `4,889,008,186` bytes.
The final job had uploaded `3,800,039,424` bytes when cancelled. Re-uploading
the full verified tree to add the GitHub attestation bundle has no material
verification value and prevented aggregate success after every gate passed.

Correction: retain the full verified artifact, but bound the final
`testgate-<run>-<attempt>` artifact to the receipt, terminal plan,
attestation/predicate/envelope proofs, and reusable global-CRAP report required
by downstream consumers. A workflow contract test forbids whole-tree
republication.
