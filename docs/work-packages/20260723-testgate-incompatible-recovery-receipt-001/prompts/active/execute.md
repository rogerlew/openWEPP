# Execute Incompatible Recovery Receipt Hardening

Treat only `GATE-RESUME-RECEIPT-INVALID` as a retained rejection of a prior
receipt; continue the current admitted attempt without importing it. Preserve
fail-closed handling for all other recovery-integrity errors. After the focused
tests pass, dispatch exactly one changed-head forest1 TESTGATE qualification.
Ignore permanently queued retired-Omarchy records; do not duplicate dispatch.
