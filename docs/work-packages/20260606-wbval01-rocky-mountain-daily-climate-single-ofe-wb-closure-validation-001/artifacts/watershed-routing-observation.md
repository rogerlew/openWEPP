# Watershed Routing Observation

Status: not-run

Evidence mode: Static

Static:

The optional `openwepp-cli-watershed` routing preview was not run.

Rationale:

- WBVAL01 acceptance is single-OFE conservation characterization.
- `pw0.slp` declares `9` OFEs and is outside the single-OFE closure ledger.
- `10/22` single-OFE hillslopes failed closed before WAT publication, so a
  routing preview would be non-acceptance and potentially misleading.
