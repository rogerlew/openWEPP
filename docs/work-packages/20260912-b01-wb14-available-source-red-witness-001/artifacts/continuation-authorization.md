# B01-WB14-AVAILABLE-SOURCE-RED-WITNESS — continuation

DRAFT FOR OWNER ADOPTION. This does not authorize itself.

## One active checkpoint

Continue the SAME checkpoint in
`docs/work-packages/20260912-b01-wb14-available-source-red-witness-001/package.md`.
Finish its offline checker, pre-run evidence and independent fix verification;
then use its one still-unspent authentic invocation and return the result.
Do not create another work package or reopen the failed interface design.

**Pinned evidence/governance:** `a13d9a5137658fd5f931dcd7fcd2a03301bd2d30`.
The existing `artifacts/owner-authorization.md` remains binding except for the
explicit supplemental allowance and unavailable-reviewer replacement below.

**Actual runtime source:** unchanged `reconciled-available145-r1`, at
`/workdir/openwepp-experiments/b01-wb14-source-reconciliation/reconstructed-available145`.
Actual 927-entry map:
`c7aea0707d0c6a0876ec4bee5a7fcf15e751a092e086f517ced7edba8282b71e`.
Historical working145 is not recovered; the two substituted tests retain UNKNOWN
historical semantic differences. Source adoption for cadence qualification stays paused.

**Reuse the corrected frozen binary:**
`/workdir/openwepp-experiments/b01-wb14-available-source-red-witness/artifacts/openwepp_runner-identity-stamped.frozen`.
SHA-256: `958e61764d373a2c183764e7f7ff41607a32ef74a2af5cd7b3f299bf1123906f`.
Size: 48,794,192 bytes.
Embedded build-input digest:
`69de3ea11154652532983f91b3e87e6ed1355aa462a9f9fee66c0463f8a8c994`.
Rehash and bind the retained Cargo row, stamps and compiled listing; do not rebuild
or repeat accepted build review without relevant drift. A missing or mismatching
binary is a stop, not permission to select another executable.

**Allowed writes:** this package's existing checker/tests, small fixture extracts,
necessary preflight/results/review evidence, package record and affected locators.
**Excluded:** Rust/build/feature/fixture/collector/dependency/contract changes,
cadence repairs, new injection or restart interfaces, scientific acceptance
changes, historical source searches, cleanup of unique state, or new branches.

**Acceptance/checks:** close the still-open findings in `correctness-review.txt`
and `qa-review.txt` against their actual current scope; pass source-shaped positive
and independently targeted negative checker tests; complete launch provenance;
obtain two distinct independent review dispositions before launching. Retain all
original failures and accepted fixes. No weaker or new witness definition.

**Run allowance:** exactly the ORIGINAL remaining invocation, 0/1 used at handoff.
Same collector/case/B01/one OFE/physical observations/from-day-zero setup and
3,600-second process timeout. No second run, restart, injection or candidate run.

**Proposed owner amendment:** explicitly grant this same checkpoint 60 ADDITIONAL
active minutes or TWO further unsuccessful correction cycles, whichever occurs
first. Carry 28m45s and 2 failed cycles from the owner's latest report. The new
cumulative failed-cycle ceiling is 4, not a reset to zero. The original authentic
run allowance remains one total. Record supplemental start and consumption in
package.md; no historical-ledger investigation. Charge reading, reasoning,
editing, orchestration and review; deduct only recorded pure waits. Ordinary
corrections count under the original definition.

**Stop:** exhausted supplemental allowance; necessary missing/drifting inputs;
required out-of-scope change; unavailable independent review; a new downstream
failure outside this correction envelope; or return/timeout/infrastructure failure
of the one authentic invocation. Preserve evidence, but do not start another task.
If a launched process is still running at an effort stop, preserve its identity
and collection instructions under repository policy; no additional launch is allowed.

## Complete the existing checker, not another framework

The initial correctness findings remain numbered 1–5 in their attributable
record. Terminal QA findings remain 1–4. Reuse implemented fixes: the current
checker already compares guard/caller raw input and raw beginning bytes. Do not
re-open those as missing. The unresolved state comparison is between the other
supplied state/parent/scalar coordinates.

1. **Use actual schema evidence.** Build the positive inner-packet fixture from
   the already retained guard/caller pair or complete source-bound serialization
   evidence. Stream/extract existing local observations only; preserve provenance
   and raw `{"Ok":[...]}` bytes. A historical fixture is checker-development
   evidence, never this invocation's result. Do not substitute the existing tiny
   hand-written packet and call it a complete authentic context. Any synthetic
   outer process/receipt/binary wrapper must be labeled synthetic.
2. **Finish value/identity joins.** Validate numeric types and finiteness, preserve
   u128 identity precision, and compare binary64 bits for like-for-like cumulative
   supply/infiltration coordinates across guard/caller beginning, caller working,
   parent persistent beginning, parent candidate and scalar working state, using
   the actual per-OFE keys and source-defined relationships. Enforce exact
   membership, not `next(iter(...))`. Require source-defined parent day/interval,
   support/accepted-until, prefix/binding and scalar identity joins and preserve
   all supplied raw payloads and upper-bound bits. Do not require equality of
   distinct owner identities or complete states where the source permits a
   difference. The rule is unchanged cumulative bits across the inactive prefix,
   not a generic assumption that all legitimate beginning cumulatives are zero.
   Bind the specific zero-valued historical case separately to its source evidence.
3. **Finish tests before declaring a corrected submission ready.** Execute the
   original required cases independently: missing record; wrong boundary; wrong
   binary; observer loss; truncated input; generic panic; timeout alone. Add the
   identified state/identity cases: change each cumulative coordinate independently;
   reject nonfinite/nonnumeric values; wrong/missing/extra OFE; raw pair mismatch;
   capture mismatch; missing beginning/parent/working payload; prefix/ordinal/
   prior-receipt mismatch; exact error mismatch; zero exit/infrastructure error;
   run/observation hash mismatch; wrong argv/listing or zero selected tests. Test
   u128 helper parsing above u64 without falsely accepting a non-255 transaction
   as this witness. Mutate one intended predicate at a time, refreshing only the
   synthetic outer hashes needed to reach it. A rejection solely from a stale
   envelope hash does not test a deeper state predicate. Include complete valid
   controls; no success from a malformed positive fixture.
4. **Keep bounded-memory and complete-record handling.** Retain streaming parse and
   bounded-chunk hashes; require complete observations, counts, physical mode,
   absence of poisoning/overflow and exact record pairing. Preserve complete
   extracted rows and locations. Separate checker errors from scientific failure.
   Run with `REPO/.venv/bin/python`, not system Python. Do not add dependencies.

Use ordinary readable Python and named tests, not another dense one-line test
script. No general validator framework or production semantic-prefix replayer is
in scope. Producer hashes/payload consistency are NOT independent prefix validity,
complete rollback, conservation or total-physics proof.

## Close launch evidence without a new build

Retain the corrected command reconstructed from `execution-command-history.json`
as reconstructed history, not a contemporaneous receipt. Bind it to actual
build logs, selected Cargo row, frozen executable and current-build-inputs file.
Explain/reproduce the build-input digest calculation from retained inputs when
available; do not silently restamp or pretend an unrecorded environment was captured.

Before launch, retain the concrete collector argv, cwd, relevant environment,
selected available CPU, actual collector AND imported predecessor-collector
hashes, cases hash, frozen binary/source/build identities and disk-space check.
Verify relevant Nix inputs from the REPO used by `nix develop`, not SRC's older
Nix files; distinguish current verification from historical build evidence and
resolve relevant changes against the recorded build revision. No whole-repository
recovery or ambient-secret dump is needed. An irrecoverable required input remains
a named gap; do not fabricate it or authorize a rebuild here.

CASES remains the original `gradual-warm-tail7-cases.json`, SHA-256
`c48cc560dff31102085ebcf852225b2063a1629ad96cfa9f7283364df44fbf3f`.
Use a create-new durable OUT; do not overwrite prior evidence.
The unchanged invocation, with actual absolute values retained before launch, is:

```sh
"$REPO/.venv/bin/python" \
  "$REPO/docs/work-packages/20260910-stage3-b01-snow-cycle-integration-001/collect.py" \
  "$BINARY" "$OUT" --policy B01 --case gradual_warm_tail7 --ofes 1 \
  --cases "$CASES" --cpu "$CPU" --timeout 3600
```

Keep all forbidden collector options in the original authorization forbidden.
After launch, compare the actual started/process/run/artifact evidence with that
predeclared invocation. Do not use collector exit alone as the runner verdict.

## Independent review and dispatch

Astra/medium orchestrates the same package; Terra/medium handles the bounded
checker/evidence correction. No extra design, runner-agent or verifier pipeline.

On adoption, explicitly permit ONE replacement independent correctness
conversation for the inaccessible `/root/witness_correctness` slot. This is a
replacement, not same-reviewer verification or an additional acceptance role.
Provide the original five findings, pinned current checker/tests/fixture source,
actual test results, relevant diff and unresolved launch evidence. The replacement
must independently verify the affected corrections, retain original attribution
and return a scoped verdict. Use the canonical Sol/high local route or an
owner-mediated independent review; this adviser/author cannot fill that slot.

Retain `/root/witness_qa` when available. Recheck only changed checker/launch
claims and current binary identity, not already accepted historical reconciliation
or unchanged build work. A replacement QA slot is permitted only if that reviewer
is also genuinely unavailable, recorded explicitly and still distinct from the
correctness reviewer and authors. If no independent route is available, stop;
never fabricate review or repeatedly retry a thread-limit failure.

Both required pre-run scopes must be accepted before using the remaining run.
Return actual run evidence to the same available reviewers for their affected
post-run scope; do not create a separate terminal verification wave.

## Return and preservation

Update only the existing package narrative. Report actual checker test inventory,
closed/open finding numbers, binary/source/build IDs, concrete invocation, 0/1 or
1/1 run use, runner result, separate witness verdict, raw evidence locations,
remaining gaps and carried/supplemental budget. A timeout or unrelated failure
cannot be accepted as the intended WB14 refusal. Witness success never changes
`execution_valid=false` into true or grants cadence/source adoption.

Carry existing scoped evidence commit/push permission and byte-verification
requirements. Do not publish a production source change. Preserve all failed
scripts, accepted build evidence and historical HOLD records. Completion of this
witness returns to the owner; it does not resume any other paused work.
