# Implementation intent

Status: `DECLARED — PRODUCTION BLOCKED ON CONTRACT/PRE-RED`

Evidence mode: `Static`

This is an intentional CRITICAL kernel-semantic successor, not a mechanical
refactor. It will introduce immutable LSE V3 and surface-owner/restart V2 while
preserving all legacy LSE V1/V2, owner V1, and complete-owner V2 canonical
bytes and behavior. The exact production/test boundary is the prospective
owned-file manifest.

The intended semantic delta is limited to snow-free `forest_litter`: explicit
liquid/ice custody; phase-specific vapor; one bounded kinetic phase transfer
after finalized vapor and before current ingress; fusion-energy closure; and
successor owner/receipt/restart/public-consumer publication. Exact 60-second
fallback resolution is unchanged and stable supports remain free to use larger
steps. No tolerance, numerical cap, event, topology, soil, snow, WB14,
publication, rollback, or fail-closed requirement is relaxed.

Contract-first gate:

1. amend SC-LSE v14 and SC-SURFACELIQUID v14;
2. amend contract-derived tests;
3. retain expected-red on unchanged frozen-litter production;
4. only then release the LSE and owner/orchestration manifest groups.

The terminal diff must reconcile every changed path to this declaration or a
prospective manifest/package amendment. Independent review must reject hidden
clamps/defaults, source-bit emulation that violates closure, inferred day-zero
ice, same-support phase re-solve, downgrade fallback, producer-only closure,
or any litter-ice alias into WB14 liquid or soil `frozwt`.
