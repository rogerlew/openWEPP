# Execute frozen forest-litter phase authority

Close the package end-to-end with contracts, contract tests, pre-red,
production, independent closure, real consumers, reviews, verification, and
delivery. Read root/crate/test/science-contract/work-package instructions,
the kernel preparation/contract procedures, both canonical contracts, retained
R-156, and package authority decision before edits.

No surrogate physics: implement only the selected R-156/SURFEX-v8 bounded
kinetic forest-litter phase model. Preserve the exact 60-second floor,
conservation, custody, chronology, topology, receipts, rollback, and fail-closed
behavior. Refuse hidden cleanup and soil correction.

Conservation/output acceptance: freeze the operand-lineage table before
production; use phase-distinguishing anti-tautology vectors; reject wrong
formula/alias candidates explicitly; independently reconstruct liquid/ice,
vapor, fusion, complete surface energy, current-ingress, and WB14 closure from
published operands on real consumers; prove metadata/schema alignment. A
producer residual or one-sided bound is supporting evidence only.

Real consumer proof: unchanged `p61` and native-forest endpoints must consume
the V3/V2 owner path and pass; producer-only, shadow-only, or fixture-only
evidence cannot close cutover.

Autonomy: continue through all safe in-scope correction and gates. A HOLD is
legitimate only at a genuine authority, safety, or external-state boundary;
diagnostic uncertainty, effort, or an intermediate failure is not closure.

Subagent authorization: this package explicitly authorizes spawning/delegating
to bounded-write worker subagents for the exact manifest groups, read-only
explorers/reviewers for architecture and science review, comparator-runner
subagents for heavy canonical gates, and two independent read-only review plus
verification agents. Expected outputs are contract/pre-red/implementation
evidence, compact comparator metrics with artifact paths, two review artifacts
with every finding dispositioned, and two verification artifacts. No subagent
may edit outside its assigned prospective write set or revert concurrent work.

Line-count governance: use new included modules at near-limit files, record
terminal counts, and do not close with an undispositioned 3000+ authored file.
