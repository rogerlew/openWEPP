# Required reading map

Static: `tools/agents/find-agents --for` resolved the root `AGENTS.md` for all
paths, plus `docs/work-packages/AGENTS.md` for this package,
`docs/specifications/science-contracts/AGENTS.md` for the four contracts and
registry, `tests/AGENTS.md` for guard interpretation, and `crates/AGENTS.md` if
the source attachment requires correction.

Static: the package also read the canonical testing strategy, science-contract
authoring procedure, kernel process contract profile, prior Child-1 workspace
failure census, and protected V9 runtime incident record before edits.
