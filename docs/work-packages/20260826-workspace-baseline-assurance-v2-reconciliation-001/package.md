# Reconcile the Assurance V2 workspace baseline

Status: `QUEUED / OWNER AUTHORIZATION REQUIRED`.

Date: `2026-08-26`.

Package ID: `20260826-workspace-baseline-assurance-v2-reconciliation-001`.

Plan class: `Workspace qualification baseline reconciliation`.

## Provenance

The canonical clean-SHA workspace run at
`9b1105d683189e46a2df5a23817498fb1430fded` produced 3,250 passed, 101
failed, and 6 skipped tests. The exact historical eleven failures were
unchanged. The additional 90 intake-resident failures comprise 81 Assurance
V2 identity/source failures and nine source/registry guards. Their causal
bytes were unchanged from the Child-1 intake and they are not attributed to
the discrete support-root exploration.

## Objective

Reconcile the 90 intake-resident failures against the currently intended
Assurance V2 and source/registry authority. Establish whether each failure is
stale expected evidence, a stale binding/fixture, a missing landed authority
increment, or an active repository defect. Produce one authoritative clean
workspace baseline before any Child-1 discrete contract freeze or production
implementation is considered.

## Boundaries

- This scaffold does not authorize implementation or failure waiver.
- Do not weaken source/registry anti-evasion guards.
- Do not rebind rejected SnowEnergy v21, LSE v11, SnowFreeze v139, or
  CoupledTime v6 continuous-DAE candidates.
- Do not alter Stage-3 physics, selectors/defaults, production publication,
  restart, receiver, runner, Child 3/4, or CoE posture.
- Preserve the exact historical eleven by name and normalized signature.
- Classify and dispose all 90 failures explicitly; no aggregate waiver is a
  qualifying baseline.

## Required evidence before closure

1. Exact clean intake SHA and full failure-name/normalized-signature census.
2. Source-level causal classification for all 81 Assurance V2 failures and
   all nine retained guards.
3. Explicit authority for any source, fixture, registry, or expected-failure
   modification.
4. Focused anti-evasion guards after every authorized correction.
5. Exact-head full-workspace qualification with no unexplained new failure
   name or normalized signature.
6. Independent authority/guard review and terminal verification.

## Relationship to Child-1

This is separate workspace debt. The Child-1 discrete support-root package
does not modify or waive these failures. This package must close before a
future discrete endpoint contract freeze or production implementation, but it
does not supersede the defect-shaped
`CHILD1-DISCRETE-SUPPORT-ROOT-001 / HOLD`.

## Delegation

Once explicitly authorized, this package permits read-only parallel causal
census and guard review. Implementation delegation requires a separately
declared write set and must preserve all unrelated user changes.
