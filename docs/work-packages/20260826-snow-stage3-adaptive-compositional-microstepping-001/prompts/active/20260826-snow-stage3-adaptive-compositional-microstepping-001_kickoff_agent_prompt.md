# Stage-3 adaptive microstepping kickoff

Scope: local repository science-contract/kernel engineering; flat-file reads
and edits only; no external connectivity.

Execution mode: package-end-to-end (default). Execute every phase in
`package.md` sequentially through truthful disposition.

Required reading:

- Core: `/workdir/openWEPP/AGENTS.md`, `docs/codex_exec_plans.md`,
  `docs/work-packages/AGENTS.md`, `docs/work-packages/README.md`, and this
  package's `package.md`.
- Conditional: `docs/defect_closure_execplans.md`,
  `docs/specifications/science-contract-authoring-procedure.md`,
  `docs/specifications/science-contracts/kernel-process-contract-profile.md`,
  `docs/specifications/science-contracts/index.md`.
- On-demand: the three affected `SC-*` contracts, prior terminal HOLD packages,
  touched nested `AGENTS.md` files, restart/runner/receiver contracts, and
  pinned-baseline sources only if implementation reaches a migrated legacy
  process routine.

Required-reading budget: 560,728 core/conditional bytes before package-local
files, `WARN`; map: `artifacts/required-reading-map.md`. The large package
catalog remains Core because repository governance requires it.

Task: close `CHILD1-TERMINAL-PHASE-COMPETITION-001` with the owner-selected
adaptive compositional model and execute production cutover end-to-end.

Constraints: contract-first sequencing; canonical SC authority; typed guards;
no silent defaults or canonicalize-and-proceed; no surrogate physics; no CoE,
fixed-step, legacy-terminal, or runtime fallback; exact 60-second
(`60_000_000_000 ns`) floor. This is a temporal-only owner amendment: preserve
all conservation, custody, phase, topology, receipt, rollback, and fail-closed
obligations unchanged, and require stable ordinary supports to accept steps
substantially larger than the floor. Treat every prior 600-ms floor-dependent
result as superseded and rerun it after implementation; do not reuse it.

Real consumer proof: the runner and downstream owner/receiver path must consume
the corrected Stage-3 path, with negative proof against old compatibility,
shadow, wrapper, and alternate-model paths.

Conservation/output acceptance: record operand lineage; separate plausible
aliases; reject old phase ordering and raw-latent-as-actual formulas; run
independent reconstruction and real closure/magnitude audits; align metadata.

Subagent requirement: REQUIRED for heavy batch closure and requested independent
reviews when session authorization exists. This prompt explicitly authorizes
subagent spawning/delegation to the roles and bounded outputs in `package.md`.

Autonomy: continue through all corrections, reviews, direct gates, exact-diff
reconciliation, and disposition unless an enumerated scientific stop condition
is proved.
