# Contract implementation evidence

Status: owner-amended implementation and canonical one-day qualification PASS

Static: amended canonical SnowEnergy v27, SnowFreeze v140, CoupledTime v9,
LandSurfaceEnergy v10, SurfaceLiquid v13, Vegetation v30, and the registry. The
successors preserve rejected version ranges as historical and bind bounded
vapor, phase projection, adaptive complete-owner chronology, batch lifecycle,
event/receiver/reappearance, restart, and receipt authority.

Static: SnowEnergy v26 and Vegetation v30 close the accepted-terminal
final-child-only custody defect found by the exact-head broad gate. Every exact
physical child's precipitation, snow--soil heat, water/N debit, and material
proposal now composes into the enclosing accepted support in physical-child
order. The exact sequential BGC ending remains owner authority; only the
independently reconstructable one-ULP binary64 regrouping audit is admitted,
and one bit over rejects.

Static: the 2026-08-27 owner amendment changes only the adaptive temporal floor
from 600 ms to exactly 60 seconds (`60_000_000_000 ns`). Conservation,
bounded-vapor/phase equations, custody, topology, receipt, rollback, and
fail-closed authority are unchanged.

Ran: exact-60 focused runtime, stable-support, restart, receipt-threshold,
fail-closed cap, and poison evidence is recorded in
`implementation-test-evidence.md`. The exact-head canonical one-day consumer
passes all 48 parents, committed publication, downstream consumption, archive
fold, and output transaction with the unchanged ledger thresholds. Its count,
width, limiting-reason, runtime, and closure evidence is recorded in
`one-day-microstep-performance-amendment.md`.

Ran: `SC-SNOWENERGY-001@26` was adopted through the typed DRAFT-report source
workflow. Assurance generation advanced from `931f2f31` to `5c275785`, a
repeat check was a no-op, all-report validation passed, and affected contract
and assurance evidence passed 220/220 tests. The new receipt is
`assurance/v2/transactions/f1ad46344455412f03748bfd7b0fc272322cbf5a7a7ea43123015b275a8e7b0a.json`.

Static: the 2026-08-30 optimization follow-on advances current SnowEnergy
authority to v27. `INV-SNOWENERGY-053` classifies only the persistent
per-layer refreeze-history tracer as within-trial factorization lineage for
adaptive error estimation. Accepted composed owner/restart state and
independent physical refreeze, liquid, mass, and energy closure remain exact;
no tolerance or persisted diagnostic changes.

Ran: all seven affected canonical-contract binaries pass 47/47, the comparison
projection passes 16/16, committed publication passes 21/21, affected crate
checks pass, and the replacement canonical one-day consumer passes with
504 accepted / 227 rejected trials and unchanged closure limits. Detailed
evidence is in `solver-optimization-evidence.md`.
