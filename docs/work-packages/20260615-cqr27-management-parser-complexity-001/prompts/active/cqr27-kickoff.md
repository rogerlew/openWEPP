# CQR27 Kickoff Prompt

Execute CQR27 against
`crates/openwepp-input-contract/src/parsers/management.rs`.

Reduce the current `parse_yearly_annual_fallow` CRAP target and any extracted
helpers to `<= 30` while preserving management parser grammar, error IDs,
diagnostics, defaults, strict/compatibility behavior, public API, and parsed
output shape.
