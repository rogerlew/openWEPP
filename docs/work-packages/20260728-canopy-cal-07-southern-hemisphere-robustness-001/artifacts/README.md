# CAL-07 Artifacts

This directory retains the frozen plan, source/custody manifests, normalized
observations, forcing diagnostics, the fail-closed execution incident,
validation evidence, review dispositions, and human-interpretation figures.
No daily kernel output, model-observation score, or scientific verdict matrix
was published after the forcing gate failed.

Files under `figures/` are plot-only SVGs. Each has a same-basename Markdown
sidecar with a plain-language caption, reading guide, methods, limitations,
accessibility notes, and exact source-table binding.
