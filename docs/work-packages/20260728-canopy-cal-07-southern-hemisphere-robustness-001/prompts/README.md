# CAL-07 Review Prompts

Review and verification scopes are defined in `package.md`. Agent outputs
belong in the named artifacts under `artifacts/`; no agent may alter package
inputs, tools, results, production code, contracts, or prior packages.
