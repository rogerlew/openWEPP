# Contract Implementation Evidence

Evidence class: `Static + Ran`

`SC-SNOWFREEZE-001` v127 adds
`REF-SNOWFREEZE-STAGE3-EVALUATION-SHADOW-AUTHORITY`,
`INV-SNOWFREEZE-094`, `OBL-SNOWFREEZE-P-067`, and
`OBL-SNOWFREEZE-C-009`. It scopes the v124 second-state prohibition without
weakening the single authoritative production snow calculation or linked
ledgers.

The admitted set is exactly:

- `same_state_paired_carrier_v1`: two stable named/versioned carrier arms on
  identical state, forcing, virtual-instrument geometry, cadence, coverage,
  and non-formulation operands, with independent component reconstruction; and
- `sequential_resolved_shadow_v1`: one clone over one bounded resolved interval.
  Physical chronology additionally requires every state-changing process,
  same-substep phase/liquid chronology, and independent initial-plus-flux to
  final mass, enthalpy, and liquid closure. Otherwise its only admitted label
  is a bounded response experiment.

Both are default-off, evaluator-owned, consumer-forbidden, non-persistent, and
non-authoritative. Runtime implementation, persistence, restart/reappearance,
terminal/receiving-surface behavior, CoE ownership change, and cutover remain
under explicit governance `HOLD`.

Ran: strict Binding Exposure validation passes all nine rows.
