# Worker Handoff

Evidence class: `Static`

Next package:
`SNOW-STAGE3-SHADOW-SOLVER-EXTRACTION-AND-OBSERVABILITY`.

Required order:

1. mechanically extract the Stage 3 shadow solver so
   `runoff_reconciliation.rs` falls below 3,000 lines with exact behavior;
2. implement a typed default-off evaluation request/tag conforming to
   `INV-SNOWFREEZE-094`;
3. publish paired-arm IDs/fingerprints and per-arm shortwave, longwave,
   sensible, latent, precipitation-advection, conduction, vapor/cold-content
   export, available-ice, total, residual, and coverage operands; and
4. prove evaluator-only custody, disabled-path nonallocation/noninterference,
   and negative production-consumer reachability.

That successor may implement the same-state paired-carrier operator and the
bounded sequential response surface. It may claim physical within-interval
chronology only when the complete forcing/process and independent clone-ledger
requirements pass. It may not add cross-day persistence, terminal meltout,
receiving-surface energy, CoE retirement, or cutover.
