# ASSURE-04B Artifacts

Artifacts distinguish `Static:` inspection from `Ran:` execution. Required
records cover intake/protected hashes, required reading, planner semantics,
implementation/consumer evidence, gates, fresh CRAP, line counts, delegated
heavy closure, dual reviews and disposition, dual terminal verification, final
disposition, and the ASSURE-04C handoff.
