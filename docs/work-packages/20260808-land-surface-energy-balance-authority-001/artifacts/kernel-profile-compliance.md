# Kernel Profile Compliance

Status: authority-profile complete; runtime profile non-promotable

Evidence mode: Static

The draft includes required lifecycle fields; scientific scope/exclusions;
top-down and pinned-baseline anchors; variables/units; state surfaces;
reproducible conservation sequence; branches/guards; stable invariants;
producer/consumer obligations; aliases; constants; unit governance;
tolerances; calibration posture; test vectors; binding-exposure disposition;
gap register; and change log. The exact snow-free precondition and atomic
postcondition and each ledger step's precondition, intermediate units/domains,
typed failure, and postcondition are defined. Test-vector families bind an
expected observable/result and invariant or typed failure.

Boundary disposition is fail-closed. Unresolved process equations, domains,
tolerances, runtime state, scheduler, and consumer proof are promotion-blocking.
They are not represented as implemented runtime enforcement.
