# Worker Handoff

Status: ready after package closure

Evidence mode: Static

Recommended next slice: default-off, snow-free-only typed
`LandSurfaceEnergyRequest`, state, component ledger, and result using only
fully admitted operands and explicit interval duration. It must reject
snow-present and all terminal schema-v8 payloads, mutate no ET/hydrology/frost
state, and include an independent internal ledger consumer.

That mechanics slice cannot claim production coupling. Constitutive authority
for the component families, typed domains/tolerances, then real ET/hydrology or
soil/frost consumption require prospective packages and atomic ownership gates.
