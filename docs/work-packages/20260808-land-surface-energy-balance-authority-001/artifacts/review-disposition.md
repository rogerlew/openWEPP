# Review Disposition

Status: pass; all findings closed

Evidence mode: Static + Ran

| Finding family | Disposition |
|---|---|
| Liquid crossings absent from first-law ledger | added exact-one precipitation/runon/infiltration/runoff energy pairs and poison vectors |
| Dimensional tolerance collision | split residuals `epsilon_*` from dimensionless relative coefficients `rho_*` |
| Guard-map schema/classes | added per-invariant canonical guard map and normalized classes |
| Step-local profile detail | added nine-step precondition/intermediate-domain/postcondition table |
| Surface-temperature/frost alias | retained `surtmp/Thra` with snow/frost owner; held any future K/Celsius cutover |
| Contract-test underbinding | bound all units, error mappings, invariant guard rows, vectors, and six gap labels |
| Post-snow/legacy provenance overclaim | narrowed registry and pinned `contin.for` descriptions |
| Calibration evidence paths | replaced labels with canonical path/anchor evidence |

No finding was waived. Runtime gaps remain intentionally non-promotable.
