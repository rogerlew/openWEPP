# Owned-File Manifest

Status: `queued`

Evidence mode: `Static`

The intended write set is declared in `package.md`; exact terminal bytes remain
to be reconciled. Scaffold bytes currently comprise this package tree and its
catalog entry in `docs/work-packages/README.md`.
