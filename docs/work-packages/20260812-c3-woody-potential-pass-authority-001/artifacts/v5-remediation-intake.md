# V5 Capped-Pass Remediation Intake

Status: `executing / authority remediation active`

Evidence mode: `Static`

## Preserved Checkpoint

The package's completed V3 authority release remains immutable historical
evidence. Its canonical model digest is
`7768657ca3d03603b66f5cd6677f032ee630fdd46d6ffadf214c713065f73852`.
The archived V3 prompt, reviews, failed attempts, gate results, terminal
verifications, and final disposition are not overwritten or reinterpreted.

The V5 reopen base is commit
`2685a1ea9fcfd51fef426eeb4c3685b419b2f768`. Its active predecessor is exact
`OPENWEPP_C3_WOODY_V4` digest
`8ace38d1148f95261306cd6b0bf6f22e23ac8ead4cb6897dbdb53061b78ee437`.
V5 is `SC-VEGETATION-001@9`; it imports the V4 state/shared-pool corrections and
retains the V3 six-unknown coupled gas--energy--hydraulic residual system.

## Accepted Authority Gap

The completed parent cap-authority audit found that the implementation package
correctly remains fail-closed because existing authority does not bind enough
independent detail to accept the cap-active implementation:

- a fully coupled cap-active vector and equality-tie active-set convention;
- the generalized derivative on each branch;
- independent `q_law`, `A_tile`, and cap-rate operands;
- exact root residual and generalized Jacobian behavior;
- cap-active residual scaling and configured diagnostic ordering;
- exact capped positive, poison, failure, and rollback vectors.

This is an authority/evidence omission. It is not permission to use a
sequential clamp, inferred active set, or authorization as finalized use.

## Dependency-Ordered Remediation

1. Freeze V4 predecessor bytes and preserve V3 completion evidence.
2. Admit exact layer cap operand derivation and identity lineage.
3. Admit minimum, equality-tie, generalized-derivative, root-residual,
   residual-scale, and diagnostic-order rules.
4. Admit exact V4-to-V5 identity rebinding with a bitwise-identical state
   payload, distinct model/configuration identities, recomputed digest, and
   stale-V4 rejection.
5. Generate independent accepted capped, controlled law-branch, equality,
   near-tie, reduction, unavailable-layer, failure, poison, rollback, and
   migration vectors.
6. Bind fixture/generator/section digests into immutable V5 definition bytes.
7. Add focused authority, digest, regeneration, poison, and anti-evasion tests.
8. Complete fresh dual science review, heavy closure, exact-diff
   reconciliation, and dual terminal verification.
9. Archive the V5 remediation prompt and issue the implementation handoff.

No item is deferred or rejected. Production Rust, public-path activation, and
consumer cutover remain outside this authority-only package.

## Focused Gates Before Heavy Closure

- deterministic independent fixture regeneration and byte identity;
- exact V1--V4 protected-byte preservation and V5 canonical digest;
- cap equation/derivative/scale reconstruction from committed operands;
- all named positive, boundary, poison, failure, rollback, and identity-rebind
  vectors;
- vegetation authority-contract suite and strict focused Clippy;
- authority admission, unit compliance, anti-evasion, AUTH11, formatting,
  diff hygiene, and package Markdown lint;
- two fresh independent science reviews with complete disposition.

Heavy gates are legitimate only after all focused rows pass.
