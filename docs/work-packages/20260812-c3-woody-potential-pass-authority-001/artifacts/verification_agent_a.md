# Verification Agent A

Status: `PASS / no unresolved material finding`

Evidence mode: `Static + Ran`

Verification target: exact worktree bytes relative to base
`4f5bb1c599a683b63be56ecd9e7296f8faf01ed0`, before terminal lifecycle
updates and prompt archival.

## Verdict

`PASS`. The package satisfies its pre-archive terminal criteria. It releases
implementation authority only; it changes no production Rust, runtime
selection, owner integration, deployment, publication, or consumer cutover.
The existing vegetation transaction remains fail-closed.

## Static Verification

### Canonical authority and selected model

- `SC-VEGETATION-001` is Version 7 with lifecycle `approved` / `active`; the
  science-contract registry records the same lifecycle and names
  `OPENWEPP_C3_WOODY_V3`.
- The V3 amendment explicitly imports immutable V2 authority except for its
  named supersessions and binds every user-directed selection:
  - conditional `L`, `S`, `P=L+S`, area-weighted leaf/stem optics,
    `K_eff=Omega*K`, whole-column two-stream transport, actual sunlit leaf
    area, and band/component-specific absorptivity ownership;
  - neutral `u_star` from reference wind and momentum roughness, with distinct
    semantic `u_leaf`, `u_wet`, and `u_stem` operands and distinct owner
    dimensions;
  - vegetation-height root-to-stem path, `1000*height_m` gravity head, no
    stem-to-leaf gravity, and one common root node;
  - the exact 15-field V3 occupancy schema and bitwise-identical-only V2 root
    warm-start migration, with exhaustive ambiguity reporting;
  - internal class `beta=1` maximum evaluation separated from the accepted
    hydrology-uncapped, class-resolved six-unknown/six-residual Stage-A solve;
  - one Atkin-owned `Rd25`, the exact Rd response constants, one subtraction
    from gross assimilation, and one identical carbon-ledger debit;
  - complete typed numerical-failure identities, finite/applicable payloads,
    deterministic precedence, and no usable last iterate or partial candidate.
- V3 retains `science_implementation_status=NOT_IMPLEMENTED`,
  `calibration_evidence_status=NOT_CALIBRATION_READY`, and
  `identifiability_status=NOT_ASSESSED`. It makes no empirical-validation or
  transferability claim.

### Exact identities and protected bytes

- Both V3 definition copies are byte-identical at SHA-256
  `7768657ca3d03603b66f5cd6677f032ee630fdd46d6ffadf214c713065f73852`.
- The independent fixture is SHA-256
  `1210e41f13aeffd2e099f9c812b8c5da6109ee9e23c6f51f045af9684a7ae109`.
- The independent generator is SHA-256
  `7b137c1aa9ed0912caf4d14c779eca1819014b4217156d36f98619f06daabd1a`.
- Frozen V1 remains SHA-256
  `003107043e8eb5bda6d9d6476e3ea01690815e3280ac98daf169317ce4d09157`.
- Every V2 definition copy remains SHA-256
  `38e1bb90abd3ff82879f7d9c80b0377bb510a3b97fdd2b6f07c12b7c42b80dc3`.
- The V3 definition binds the current generator, fixture, five live V7
  section digests, and adjacent contract digests.

### Independent evidence and anti-evasion

- The standard-library Python calculator does not invoke Rust and regenerated
  the committed 49,915-byte fixture byte-for-byte.
- The fixture contains radiation, aerodynamic, E07--E15 potential,
  migration, respiration, failure, and precedence families; all eight
  generator self-checks pass.
- All 40 named poison alternatives are executed. Ten typed poisons identify
  their owning validator; quantitative poisons are numerically distinct.
- The Rust authority suite independently reconstructs section identities,
  neutral aerodynamic resistances, biochemical intermediates, E14 root-path
  conductances, coupled residuals, request basis, exact fixed energy
  constants, migration, respiration debit, failure applicability, precedence,
  and the complete poison inventory. Expected values are not generated by the
  Rust implementation under test.

### Review and evidence history

- Both reviewers preserved their initial and intermediate `NO-GO` findings.
  The final Review-A and Review-B addenda return `GO` with every critical/high
  finding resolved and no new material finding.
- Both reviewers separately assessed the terminal-hygiene byte-only delta:
  generator whitespace removal, unchanged fixture, and rebound definition
  identities. Their final GO applies to the exact identities above.
- The finding-disposition artifact accepts and resolves every A/B finding;
  none is deferred or rejected.
- Failed formatter, focused-test, Clippy, review, admission, orchestration, and
  ENOSPC attempts remain visible in the additive gate record. They were not
  rewritten as successful scientific runs.

### Heavy-gate evidence and reuse

- Workspace warnings-denied Clippy, workspace doctests, `cargo deny`, format,
  admission, unit compliance, anti-evasion, AUTH11, and focused authority
  checks passed under the comparator runner.
- The final uninterrupted full-profile execution used unique scratch on
  `/home`, exited zero, and reports 2,481/2,481 tests passed, 51 slow, and 33
  canonical skips in 3,318.773 seconds. The earlier interrupts and root
  filesystem `ENOSPC` run are retained separately.
- Reuse of the successful heavy evidence after terminal hygiene is legitimate:
  the only scientific-artifact change was whitespace in the Python generator
  plus the dependent generator digest in both definition copies. Fixture,
  computed values, contract sections, Rust tests, and production bytes did not
  change; exact regeneration, copy/digest checks, focused Clippy, focused
  authority tests, and both science-review addenda revalidated the affected
  identity lane.

### Diff, lifecycle, and document hygiene

- The terminal diff is confined to the declared contract, registry,
  byte-identical definition copy, package tree, package catalog, and authority
  integration test. No `crates/`, Cargo, runtime, owner, selector, deployment,
  or publication path differs from the base.
- Recorded line counts match the exact files: contract 1,388; authority test
  1,559; independent calculator 1,487; package plan 260. No production-module
  line-count threshold is implicated.
- The measured package size is 844 KiB before verifier artifacts and remains
  under 1 MiB; no nested `target` directory or file above 10 MiB exists.
- During verification, stale current-status labels in six package artifacts
  were identified and corrected without changing their historical bodies.
  The corrected exact bytes pass package Markdown and diff hygiene.
- The kickoff prompt remains correctly active and absent from the archive.
  Archival is intentionally deferred until both terminal verifiers pass.

## Commands Run

- `sha256sum` for both V3 definitions, the fixture, generator, V1, and every
  V2 definition: PASS; identities match those above.
- `cmp` for both V3 definition copies: PASS.
- Independent generator copy executed outside the repository with repo-local
  Python: PASS; regenerated fixture is byte-identical and all self-checks true.
- `cargo clippy --test vegetation_boundary_authority_contract -- -D warnings`:
  PASS.
- `cargo nextest run --test vegetation_boundary_authority_contract --profile
  quick`: PASS, 17/17.
- `cargo nextest run --test
  auth11_required_suite_obligation_guards_contract --profile quick`: PASS,
  3/3.
- `check_science_contract_admission.sh --base-ref 4f5bb1c... --worktree`:
  PASS, `A0_ADMITTED contracts=45 science_surfaces=0`.
- Science-contract unit compliance and authority-suite anti-evasion: PASS.
- `markdown-doc lint` for the package, contract, contract index, and package
  catalog: PASS with zero errors and warnings. The package was rerun after the
  status-label correction and validated 31 files.
- `git diff --check`: PASS, including after the final status-label correction.

## Terminal Finding Disposition

No unresolved material finding. Verifier A authorizes the parent to proceed
with Verifier B reconciliation, byte-for-byte prompt archival, final lifecycle
updates, and local package commit. This verdict does not authorize a push or
any implementation/runtime action.
