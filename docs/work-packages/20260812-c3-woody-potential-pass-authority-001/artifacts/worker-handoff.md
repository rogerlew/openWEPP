# Worker Handoff

Status: `released / implementation continuation authorized`

Evidence mode: `Static`

Resume the existing implementation package rather than scaffold another
implementation package. Preserve its V1
HOLD and V2 Increment 2A evidence, bind the implementation predecessor to the
V3 authority commit, replace the V2 per-layer root warm-start vector with the
V3 scalar root node, then implement whole-column radiation and the accepted
uncapped potential pass. The public transaction must remain fail-closed until
the authorization-capped second pass passes
`STAGE_B_E11_E15_EXACT_ORACLE`.

No runtime activation, production selector change, real consumer cutover,
canopy snow, soil transformation, or calibration claim is transferred.
