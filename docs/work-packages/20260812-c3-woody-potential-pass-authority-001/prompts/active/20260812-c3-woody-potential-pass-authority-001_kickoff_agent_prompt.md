# Execute the C3 Woody V3 Potential-Pass Authority Package

Scope: local `/home/workdir/openWEPP` science-contract work; flat-file reads,
edits, and local commands only. No deployment, publication, external messages,
remote branch creation, PR, runtime activation, production selector change, or
consumer cutover. Do not push unless separately directed by the user.

Execution mode: package-end-to-end.

Phase plan: execute every phase in `package.md` sequentially through final
disposition.

## Required Reading

Core:

- `/home/workdir/openWEPP/AGENTS.md`
- `/home/workdir/openWEPP/docs/codex_exec_plans.md`
- `/home/workdir/openWEPP/docs/work-packages/AGENTS.md`
- `/home/workdir/openWEPP/docs/work-packages/README.md`
- this package's `package.md`

Conditional because canonical kernel authority changes:

- `docs/specifications/science-contract-authoring-procedure.md`
- `docs/specifications/science-contracts/kernel-process-contract-profile.md`
- `docs/specifications/science-contract-spec.md`
- `docs/specifications/unit-governance.md`
- `docs/specifications/science-contracts/index.md`
- `docs/standards/testing-and-gate-strategy.md`

On-demand for touched mechanisms:

- `docs/specifications/science-contracts/contracts/SC-VEGETATION-001.md`
- the V1/V2 model-stack and V2 topology-authority artifacts named in
  `artifacts/required-reading-map.md`
- the prior implementation HOLD audit and active implementation package
- the cited CLM5 surface-albedo, flux, plant-hydraulic, and photosynthesis
  technical-note sections

Required-reading budget: approximately `648494` local bytes before this package
file and prompt; `WARN` (`>400000`, `<=800000`), driven principally by the
canonical package catalog and vegetation contract. Map:
`artifacts/required-reading-map.md`.

## Task

Execute the package objective end-to-end. Admit `SC-VEGETATION-001@7` and
`OPENWEPP_C3_WOODY_V3` with exact mixed leaf/stem radiation, neutral
canopy-surface wind, height/gravity hydraulic geometry, scalar common-root state
and exact V2 migration, uncapped accepted potential coupling distinct from the
internal `beta_hyd=1` maximum evaluation, one-source/one-debit Rd identity,
typed numerical failure payloads, and independent V3 fixtures.

Constraints: contract-first sequencing; canonical `SC-*` authority; immutable
V1/V2 bytes; typed guards; deterministic error precedence; no silent defaults;
no canonicalize-and-proceed for domain violations; no inferred constitutive
math outside the selections in `package.md`.

No surrogate physics: production authority must bind actual selected
reference-backed or explicit `OPENWEPP_CANONICAL_SELECTION` equations.
Surrogate, provisional, proxy, alternate, or heuristic stand-ins are forbidden.

Conservation/output acceptance: bind operand lineage and area/unit basis;
separate every plausible wrong alias in fixtures; reject named poison formulas;
independently reconstruct radiation, hydraulic continuity, energy/transpiration,
and respiration debit; do not close on self-consistency alone.

Subagent requirement: REQUIRED. This prompt explicitly authorizes subagent
spawning/delegation to two independent science reviewers, one
`comparator_suite_runner`, and two independent terminal verifiers for the exact
scopes and bounded outputs in `package.md`. The parent must not run heavy
workspace closure commands when the comparator runner is available.

Autonomy: execute all phases and update every required artifact without asking
for user direction unless a genuine authority contradiction or unavailable
required source remains after all safe in-scope routes are exhausted.
