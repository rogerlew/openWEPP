# Continue the C3 Woody Authority Package for V5 Capped-Pass Authority

Resume the existing package
`docs/work-packages/20260812-c3-woody-potential-pass-authority-001/` in place.
Preserve its completed V3 checkpoint and archived V3 kickoff prompt unchanged.

Admit `SC-VEGETATION-001@9` and immutable `OPENWEPP_C3_WOODY_V5`, importing
exact V4 digest
`8ace38d1148f95261306cd6b0bf6f22e23ac8ead4cb6897dbdb53061b78ee437`.
Bind exact layer cap amount/rate lineage, independently evaluated hydraulic-law
flux, `min(q_law,cap_rate)` selection, equality-tie cap activity, zero
generalized derivative on the cap/tie branch, law derivative otherwise, root
continuity, residual scaling, configured-layer diagnostic order, finalized-use
conversion, and V4-to-V5 identity-only rebinding recorded in the V5 artifacts.

Generate digest-bound independent vectors for accepted capped and controlled
law branches, exact and
near ties, fully authorized value reduction, zero/dry/frozen/inaccessible
layers, typed
domain/singular/iteration failures, rollback, identity rebinding, and every
named poison. Preserve all failed attempts. Obtain two fresh science reviews,
run focused then Critical heavy gates, reconcile exact terminal bytes, and
obtain two fresh terminal verifiers. Archive this prompt byte-for-byte only
after every V5 terminal gate passes.

No production Rust, runtime activation, selector change, deployment,
publication, remote branch, PR, or consumer cutover is authorized. Do not push
unless separately directed by the user.

Subagent authorization: this continuation explicitly authorizes spawning and
delegating to two independent science reviewers, one comparator-suite runner,
and two independent terminal verifiers. Reviewer/verifier access is read-only
except for uniquely V5-suffixed package artifacts; comparator write access is
limited to V5 gate logs and results. The execution owner retains all canonical-
authority corrections and finding disposition.
