# Coverage Baseline

Ran: delegated fresh batch LCOV measurement, as recorded in
`target-selection.md`. The target's LCOV summary is `847/1114` executable lines
(76.032%). Existing test coverage concentrates on disturbed-policy parser rows;
the selected parser orchestration, layer variants, and complete error-code map
have incomplete characterization.

Before decomposition, tests must directly assert profile/error identity for
every datver family touched by a structural extraction.
