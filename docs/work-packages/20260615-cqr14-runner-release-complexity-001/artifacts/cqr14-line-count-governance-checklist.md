# CQR14 Line-Count Governance Checklist

Static: line-count governance:

- `crates/openwepp-runner/src/release.rs`: before `560`, after `773`.
- `docs/work-packages/README.md`: before `547`, after `550`.

Static: no touched Rust file is near the `2000` line WARN threshold or the
`3000` line hard cap.

Static: no line-count follow-up is required for CQR14.
