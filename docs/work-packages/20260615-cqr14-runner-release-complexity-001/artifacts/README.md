# CQR14 Artifacts

Artifact set for CQR14. Evidence entries must label `Static:` versus `Ran:`.

Status: complete.

Static: package artifacts record CQR14 closure for
`crates/openwepp-runner/src/release.rs`.

Ran: before/after LCOV and CRAP, focused release tests, full Rust gates,
markdown-doc lint, and `git diff --check`.
