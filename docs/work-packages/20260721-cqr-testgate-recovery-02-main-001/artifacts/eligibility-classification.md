# Eligibility Classification

Static: all four target rows are `E-PRODUCTION`. They control accepted CLI
options, trust-stage transitions, durable HEAVY lifecycle, fallback audit
selection, error precedence, or public JSON. Aggregate, 75% region floor, and
CRAP treatment remain binding; no retained exception is proposed.

Static: two independent read-only reviewers passed this classification at exact
HEAD `62cb1086`. Reviewer A checked semantic/consumer eligibility and closed-list
fit. Reviewer B independently checked the Attempt-15 rows, source identity, and
authorization for behavior-preserving helper extraction. No eligibility finding
remains open.
