# Behavioral Equivalence

Static: no floating-point or process-physics expression exists in the target
functions. Equivalence means identical option sets, call ordering, error
precedence/codes, ledger records, canonical JSON fields, and exit classification.
Only whole control blocks will move to private typed helpers; expressions and
short-circuiting remain textually unchanged.
