# Exact-Diff Reconciliation

Status: `PASS`

Evidence class: `Static + Ran`

## Reconciled scope

The terminal tree changes attributable to this package are confined to the
declared write set:

- root `Cargo.toml` and `Cargo.lock`;
- management schema and input-contract forest-litter parsing;
- hillslope-orchestrator management projection, direct frames, execution, and
  erosion consumer evidence;
- runner source projection, trace publication, source guards, and focused
  tests;
- the external-boundary integration test;
- `SC-PLANT-001`, `SC-RESIDUE-001`, and the science-contract index;
- this package, the work-package catalog, and the canopy assurance roadmap.

The terminal extraction files are part of those declared crate trees:

- `crates/openwepp-input-contract/src/parsers/management/forest_data.rs`;
- `crates/openwepp-runner/src/hillslope/direct_publication/`
  `day_input_and_helpers/00e_native_canopy_trace.rs`;
- `crates/openwepp-runner/src/hillslope/tests03/`
  `canopy_litter_boundary_helpers.rs`.

No native fixture was persisted. Authenticated sidecars are created only in
the isolated real-run test directory.

## Reconciliation result

Every scoped production edit implements the contract-amended identity-only
external daily boundary or the exact downstream consumer evidence required to
verify it. Every scoped test and documentation edit derives from that same
contract. Root dependency and test-registration changes are declared.

Pre-existing CAL-04B, CAL-05, and gate-planner worktree changes are outside
this package and were neither adopted nor reverted.

The final scoped whitespace check is clean. The final exact-head correctness
receipt is 2,117 passed with 29 profile-declared skips in 757.402 seconds.
