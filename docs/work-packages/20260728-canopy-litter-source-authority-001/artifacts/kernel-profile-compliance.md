# Kernel-profile Compliance

Status: `PASS`

Evidence class: `Static + Ran`

The implementation preserves the kernel-process profile:

- authority is canonical in `SC-PLANT-001#INV-PLANT-039` and
  `SC-RESIDUE-001#INV-RESIDUE-022`;
- the boundary is typed and fail-closed rather than a fallback or surrogate
  predictive law;
- contract-derived tests preceded production edits;
- external influx is separately labeled and never debited from canopy;
- the exact three-source sum is independently reconstructed from output;
- the real parallel residue recurrences and downstream consumers are proven;
- predictive biology and calibration claims remain explicitly withheld; and
- warnings-denied workspace Clippy and exact-head full correctness pass.
