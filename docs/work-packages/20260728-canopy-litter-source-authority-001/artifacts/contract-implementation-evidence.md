# Contract Implementation Evidence

Status: `PASS`

Evidence class: `Static`

`SC-PLANT-001` revision 25 adds `INV-PLANT-039` and defines the authenticated
external daily needle/fine-woody boundary, including independent tissue
status, functional/material applicability, dry-mass/area units, support,
identity or authority-backed derivation, SHA-256 verification, and the exact
source law:

```text
Q_litter(d) = L_leaf(d) + N_ext(d) + W_ext(d)
```

`SC-RESIDUE-001` revision 16 adds `INV-RESIDUE-022` and binds that source
exactly once into each parallel surface, interrill-ground, and rill-ground
recurrence before common decay/actions. External `N_ext + W_ext` is explicitly
open-system influx with no canopy debit.

Predictive needle and fine-woody deposition remain outside the amendment and
retain `AUTHORITY_MISSING / NOT_CALIBRATION_READY / NOT_ASSESSED`.
