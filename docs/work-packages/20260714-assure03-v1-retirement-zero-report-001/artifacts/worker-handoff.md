# ASSURE-03 Handoff

Status: complete; ASSURE-04A is next but requires operator authorization

## Starting State

- tracked public assurance contains only the neutral zero-report catalog;
- the dormant export lists zero documents and explicitly prohibits vendoring;
- v1 source/public/compiler bytes are recoverable from the frozen base and
  exact package manifest, but may not be restored as current publication;
- ordinary CI validates without assembling a candidate;
- explicit release assembly admits only the exact zero-report transition state
  and requires separate successful stability before candidate upload; and
- no v2 source schema, planner, renderer, review lock, or report exists yet.

## Next Authorized Shape

When the operator requests ASSURE-04A, scaffold it from
`docs/planning/scientific-assurance-v2-implementation-roadmap.md`. Its first
action is to freeze the zero-report outputs and build a minimal positive fixture
from the accepted groundwater prototype. The package may add v2 source and
identity admission only; it must not publish a report, reintroduce v1 machinery,
start WEPPcloud vendoring, or absorb ASSURE-04B planning/rendering scope.

Required closure remains real-CLI field consumption, fail-closed identity,
units, path, restriction, and version guards, tracked-public byte identity,
fresh CRAP and workspace gates, dual review, finding disposition, and dual
verification.
