# Disposition

Status: queued

Evidence mode: not-run

Not-run:

Final disposition must be one of:

- completed correction;
- validated non-defect / invalid upstream input;
- legitimate `HOLD` at a declared boundary.

`HOLD` is invalid if an in-envelope, authority-backed, testable, measurable fix
was identified.
