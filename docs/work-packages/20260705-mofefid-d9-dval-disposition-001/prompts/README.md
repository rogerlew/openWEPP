# Prompts

This directory holds execution prompts for the D9 D-val disposition package.

- `active/` contains the current kickoff prompt.
- `archived/` contains superseded prompts if execution needs to restart with
  narrower wording.
