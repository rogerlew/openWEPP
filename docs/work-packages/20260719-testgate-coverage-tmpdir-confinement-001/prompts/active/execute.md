# Execute Confined Coverage TMPDIR Correction

Keep fresh-coverage temp inside the validated artifact root while shortening
it to the root's `tmp` child. Preserve standalone fallback and all output,
coverage, CRAP, test, workflow, and policy semantics. Run focused checks, then
the exact planner-selected terminal plan; complete dual review and verification.
