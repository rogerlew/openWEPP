# Stage 3 bounded attribution and recomputation experiment

Status: executed HOLD; dual review and verification closed; production HOLD. Date: 2026-09-09 America/Los_Angeles.

## Objective and authorization

Execute the owner-directed successor to compact-cost package P. First refine the
existing additive observer enough to rank the dominant portions of the 49.24%
runner remainder and 31.85% mixed envelopes. Then inspect the most expensive
source-backed boundary for actual invocation-local recomputation. Implement no
single-pass production architecture unless a repeated operation is demonstrated
and its removable measured cost is at least 5% of runner time.

This is local repository engineering using flat-file reads/edits only. Production
remains HOLD. The user authorized end-to-end experiment execution, not deployment.

## Included and excluded scope

Included: this package; active/catalog locators; an isolated Q copy of P; a
fixed-size additive observer refinement; bounded tests and scripts; authentic
one-OFE release measurements; offline invocation comparison; source-backed
classification against established F; conditional typed invocation-local
single-pass prototype only if the frozen gate passes; custody, review, and
verification.

Excluded: primary repository Rust; production activation; general cache; collapsing
Full/Half1/Half2/Root or potential/final roles merely because payload bytes match;
solver cadence, physics, authority, mutation, error, validation, or ownership
changes; disabling required integrity work; memory campaign; `perf` privilege
changes; reduced matrices; inactive-column rescue; branch changes or push.

## Dependencies and authority

- P commit `72412a5d1100264659b8e33fe79617eb250c21f6` and its package artifacts.
- P isolated source `/tmp/openwepp-compact-cost-P-vKI6gb`; exact identity is
  revalidated and captured before Q edits.
- `docs/work-packages/AGENTS.md`, author/implementer/runner/reviewer/verifier role
  procedures, `docs/codex_exec_plans.md`, and
  `docs/standards/testing-and-gate-strategy.md`.
- Historical F evidence in package
  `20260906-stage3-prospective-mechanism-experiments-001`; any recurrence is
  classified as F, an extension of F, or distinct work.

## Frozen intended write set

Primary repository: all files under this package plus
`docs/work-packages/active.md` and `docs/work-packages/README.md`.

Isolated Q: only paths enumerated in `artifacts/protocol.md` before edits. Q is
observation-only unless the conditional implementation gate passes and the
package is amended before any prototype edit. No primary Rust is changed.

## Phase plan

1. Validate P identity, preserve recoverable custody, create Q, and freeze the
   phase/source-to-bucket protocol and acceptance.
2. Preserve existing phase labels as exclusive aggregates; separate time outside
   named scopes, named-phase exclusive time, already charged child work, required
   integrity/audit work, and experiment-only observation.
3. Run focused observer/accounting tests, release build, and authentic off/on
   semantic admission. Freeze Q.
4. Run two warmups/posture and six balanced fresh-process off/on pairs on CPU0.
   Subdivide only the largest still-unresolved phase if needed for a defensible
   ranking; do not build a general tracing framework.
5. At the most expensive source-backed candidate boundary, retain compact call
   records outside timing runs. Compare effective beginning state, forcing/support
   interval, lane/topology, caps/branch state, evaluation role, authority,
   result, consumer, error behavior, mutation boundary, and accepted ownership.
6. Measure only proven duplicate work, net of separately charged children and
   retained construction/validation. Classify it against F.
7. Conditional gate: implement a bounded invocation-local typed result handoff
   only if source and runtime evidence prove interchangeable repeated computation
   and its removable median cost is >=5% runner time. Otherwise finish with the
   negative result and actual dominant operation.
8. Preserve custody; reconcile exact diff; run applicable quality/tests; dual
   independent review, finding disposition, correction/re-review, and dual
   independent terminal verification; disposition and local commit.

## Frozen acceptance

- Exact protected A/P-normalized input, output, control, closure, solver, and work
  identities in every accepted run; no extra physical evaluation in timing runs.
- Six valid on profiles and paired off references after warmups; every on profile
  has zero signed accounting difference and no missing/non-LIFO/live/overflow state.
- Report medians/ranges and raw exclusive buckets without observer-overhead
  subtraction. Observer perturbation is reported, not thresholded.
- Outside-named-scope, named-phase exclusive, and child-charged time are distinct;
  required runtime integrity and experiment-only observation are not conflated.
- Recompute claims bind logical role and full effective inputs, not byte equality
  alone; independent roles are never collapsed.
- Any implementation must preserve exact results, errors, mutation, ownership,
  invocation roles/counts, and downstream consumers. No general cache.
- Complete characterization is valid with no prototype. Production remains HOLD.
- Source/build/fixture/command hashes, raw receipts, scripts, patch, external
  dependencies, line-count disposition, gate results, dual reviews/finding
  resolution, and dual terminal verifications are retained.

## Validation plan

Run focused synthetic observer lifecycle/accounting tests, affected release
build/tests, rustfmt and warnings-denied scoped Clippy for every isolated Rust
edit, authentic off/on semantic admission, the frozen measurement series, and
portable analysis reconstruction. If the conditional production-behavior
prototype gate passes, amend risk and select its additional consumer and
correctness gates before implementation.

Subagent requirement: independent correctness and QA reviews and two terminal
verifications are required. This package explicitly authorizes subagent
spawning/delegation to those roles, with writes limited to their assigned package
artifacts. A comparator runner is required only if a heavy broad gate becomes
applicable; none is selected for observation-only isolated Rust.

## Progress

- [x] Owner authorization received.
- [x] Package scaffold and acceptance frozen before substantive Q edits.
- [x] P identity/custody and Q protocol frozen.
- [x] Attribution refinement implemented and validated.
- [x] Authentic measurements and offline recomputation study complete.
- [x] Conditional implementation decision made: no new prototype.
- [x] Review, verification, and disposition complete; local commit follows this
  terminal reconciliation.

## Surprises, decisions, outcomes, recovery

Current state and recovery instructions live in `artifacts/worker-handoff.md`.
The final report will distinguish measured observation, static inference, and
unmeasured opportunity explicitly.
