# Final disposition

Ran + Static: **EXECUTED HOLD / NO NEW PROTOTYPE. Production remains HOLD.**

The bounded experiment completed its attribution and recomputation study. Six
accepted on profiles reconcile exactly, the protected semantics match across all
16 final processes, and the refined partition reduces truly outside-named time to
7.44%. The largest ranked exclusive regions are Stage3 LSE parent-exclusive
16.14%, carrier physical evidence 14.30%, carrier completion/construction 12.51%,
and imported frozen runtime 9.26%.

The detailed trace finds 196 nominal repeated recorded requests, but eight equal
recorded keys produce multiple outputs. The key therefore omits effective dynamic
state or authority. The 200 two-provider-call evaluator pattern is the previously
established F feed-forward mechanism, not a distinct discovery. No complete
interchangeability proof or >=5% removable new operation exists, so the conditional
single-pass gate lawfully rejects implementation. No prototype or cache was made.

Independent correctness and QA review accepted the measurements and negative
prototype decision after documentation corrections. The package cannot be marked
complete: its frozen warnings-denied scoped Clippy requirement exits 101 on both P
and Q with 1,017 inherited diagnostics. Independent source-attributed comparison
finds the P/Q diagnostic multisets equal, which supports no observed new diagnostic
but is not a green gate and cannot replace the frozen requirement. This is the
reason for executed HOLD.

The isolated Q observation patch and executable are preserved by the external
custody bundle identified in `source-and-build-manifest.md`. No primary repository
Rust, production behavior, solver, physics, authority, validation, or ownership
path changed. Remaining limitations include one audit-capable one-OFE executable,
one host/day, no production CLI/scale or memory qualification, and descriptive
scope timing rather than removable-cost claims.
