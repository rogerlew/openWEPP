# Required reading map

Static: bootstrap map for the bounded attribution-and-recomputation package.

Core: root `AGENTS.md`; `docs/work-packages/AGENTS.md`; role authoring,
implementation, runner, review, and verification procedures; package and current
handoff; `docs/codex_exec_plans.md`; testing/gate strategy.

Conditional: compact-cost P protocol/report/source manifest and F handoff/report
for predecessor identity and duplicate-mechanism classification. Isolated source
files are read only as their measured phases become candidates. Science-contract
instructions are required only if the conditional prototype would affect kernel
behavior; that gate must be amended before such edits.

On-demand: local CI instructions for selected commands; evidence-bundle capture
instructions before Q edits/final custody.

Context measurements: bootstrap and workflow-total are UNOBSERVED; required
source expansion will be summarized at disposition. Authority reading is not
limited by this observation gap.
