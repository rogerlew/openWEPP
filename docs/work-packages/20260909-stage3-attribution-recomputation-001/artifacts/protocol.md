# Frozen attribution and recomputation protocol

Static: frozen before Q edits.

P custody verified with `evidence_bundle.py`; manifest SHA-256
`66a927d58d8c00a9edc4ed817d9b529f0176b77ca5314d1adf286b8d46afd4ba`.
Q is `/tmp/openwepp-attribution-Q-3r3eP8`, copied byte-for-byte from P before
edits. The five initial source hashes match P's final custody manifest.

Exact Q Rust write set:

- `crates/openwepp-land-surface-energy/src/solver_mechanism_audit.rs`
- `crates/openwepp-hillslope-orchestrator/src/snow_stage3_v11_profile.rs`
- `crates/openwepp-hillslope-orchestrator/src/snow_stage3_v11_terminal_execution.rs`
- `crates/openwepp-hillslope-orchestrator/src/v11_covered/carrier_phase.rs`
- `crates/openwepp-hillslope-orchestrator/src/v9_real_consumer_shadow.rs`
- `crates/openwepp-runner/src/hillslope/05_runner_execution_and_outputs.rs`
- `crates/openwepp-hillslope-orchestrator/src/snow_stage3_v11_attachment_runtime.rs`

Protocol amendment before editing the sixth path: source inspection showed P's
runner setup/publication scopes already own two compact mappings. Separating
those labels requires changing their two bucket variants; no other runner code
is in scope. The initial five-path list omitted this direct mapping. Acceptance,
run budget, and observation-only risk are unchanged.

Second protocol amendment before editing the seventh path: the first valid Q
series localized the largest unresolved category to the Stage3-LSE parent
(`22.38%`). The existing runtime source has bounded snow-free-parent execution
and end-of-day receipt-finalization regions. The one authorized deeper split
adds scopes around those regions, reusing two sub-0.001% categories rather than
expanding the fixed 32-bucket schema. No call, branch, or ownership changes.

The compact observer remains a fixed array and stack. Its top-level partition
will retain P's solver children and split runner time into: outside any named
scope; native ET; Stage3 LSE parent-exclusive; terminal setup/custody/projection/
carrier/retention/finalization; covered physical setup/evidence/completion and
complete-envelope/adoption/projection/owner; imported validation/setup/physical/
frozen preparation/execution/runtime/envelope/accepted/publication/install; other
named phase; routing; and runner setup/publication. Nested child scopes preempt
parents, so every interval is charged exactly once. Existing detailed telemetry
remains supporting inclusive evidence and is not added to the partition.

Required runtime integrity includes existing typed custody, validation,
projection, and encoding/fingerprinting. The common compact mechanism-audit
counters are experiment-only and active in both timing arms; their cost is not
isolated by the off/on comparison. The comparison estimates only the incremental
compact timing observer, and that perturbation is never subtracted from raw
buckets. Detailed hashing is enabled only in the separate untimed trace.
Uninstrumented time remains outside-named-scope, not guessed into physical work.

Run budget and invalidation are inherited from P: two warmups/posture followed by
six balanced fresh-process pairs on CPU0, same release executable; exact semantic,
control, closure and population admission; zero signed accounting difference;
invalid on nonzero exit, missing/live/non-LIFO/overflow scope, identity mismatch,
or added physical call. Raw timing is CLOCK_MONOTONIC/Instant wall time.

Recomputation capture is separate from timing. Use the existing bounded detailed
mechanism records and source identities; do not add serialization/hashing to a
timed run. Candidate equivalence requires the full logical role and authority
tuple named in package acceptance. The >=5% gate applies to removable exclusive
time after subtracting child work and retained obligations. Equal bytes across
independent roles are not removable work.
