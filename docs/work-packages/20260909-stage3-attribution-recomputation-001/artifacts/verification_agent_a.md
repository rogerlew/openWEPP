# Independent terminal verification A

Static: read verifier/common governance, package, protocol, source/command
manifests, final report/disposition, corrected findings, both independent review
records, gate/line-count evidence and collector/analyzers. Reused unchanged
governance readings from the preceding bounded verifier assignment, including
testing strategy sections 9–10/18 and the assurance template.
Ran: independent read-only custody, source/patch reconstruction, receipt/identity,
accounting, analysis and recomputation checks below. No rebuild, simulation,
production/source edit or external action performed by this verifier.

Role/session: `/root/terminal_verifier_a`; configured effort medium; effective
runtime effort UNOBSERVED. Write ownership is this artifact only.

## Identity and exact scope

Primary HEAD at verification: `299c5a5030488eec0318a6c5a2a26d5a5a4da1c5`,
branch main. `git status --short` and `git diff --stat` show package-local
tracked/untracked artifact work, not primary Rust or science-contract changes.
The primary HEAD is not Q's flat-file source identity.

Reviewed Q `/tmp/openwepp-attribution-Q-3r3eP8`, measured runner
`3e684ac3e8b4dcc93f0dfcffe88faa0c18289f39770237ef4bd404f782d4ce53`,
seven-file patch `a254e07c09953339bb8f7c9ecfe354c799fc8d41db436ceb6684835cef4550f4`,
final series-05 and separate recompute-trace-cut3. Bundle manifest independently
hashes to `de78b15b7fa1f69252fdd073002fe6c25fc45bc0552e354045adcc45615291e5`.

## Independently executed evidence

All own commands exited 0, using primary `.venv/bin/python` with
`PYTHONDONTWRITEBYTECODE=1`; console receipts remain in this session. No additional
log/artifact files were written outside this assigned record.

- Ran `tools/agents/evidence_bundle.py verify
  /tmp/openwepp-attribution-Q-final-bundle`: PASS. Independently matched each of
  seven captured source hashes against the current Q files. This validates
  selected custody, not a complete external source/toolchain rebuild.
- Parsed the full unified patch in memory against retained P. Required exact
  old/context lines without offset/fuzz; all seven reconstructed outputs equal
  current Q bytes. No patch was applied to any filesystem tree.
- Read all 16 final numbered receipts and their retained raw logs. Every child
  is valid/exit zero; all log hashes match; every log has one mechanism record
  exactly equal to its receipt. Re-executed protected identity/work-count
  comparison against final admission and collector posture/accounting/family
  checks on every row. No F-specific relaxed work comparison was introduced.
- Reconstructed the saved final cost analysis with the retained analyzer,
  without its file-writing entry point. Every generated field agrees exactly.
  Final off/on runner medians are 5.033492/5.0364295 s. Each measured on profile
  reconciles to zero ns, rather than normalizing a contradictory sum.
- Independently mutated profiles to negative elapsed, zero denominator and a
  wrong signed accounting difference. All three are rejected by the analyzer.
  This is behavioral verification of accounting refusal, not a physical-error
  campaign or a rerun of the Rust tests.
- Independently grouped the raw carrier trace. Lifecycle counts are 400 Provider,
  400 Carrier, 200 Evaluator and 72 Outer; metadata reports zero errors/drops.
  Provider grouping reproduces 204 recorded keys, 132 repeated groups covering
  328 calls, 196 nominal repeats and eight multi-output keys. All 200 evaluator
  groups have two distinct recorded requests and equal recorded outputs.

Inspected exact-Q `q-compact-tests.log` (3/3 PASS), terminal format/build evidence
and failing P/Q lint records. These are retained executions by the executor,
not this verifier's fresh Rust executions. The broad lint failure is visible
directly in the logs and is not erased by matching diagnostic multisets.

## Corrections, legitimacy and inventory

ATTR-A01/QA-ATTR-01 are resolved as disposition defects: the frozen Clippy gate
remains FAIL and forces executed HOLD. Matching inherited diagnostics is useful
attribution evidence but cannot meet a green gate. No after-the-fact suppression,
threshold amendment or unauthorized broad baseline repair is justified.

ATTR-A02's corrected distinction is sound: the 4.20% named runtime validation/
reseal/acceptance subset is not the common experiment-only mechanism-audit cost.
Common compact-audit counters run in both arms and remain unmeasured within
parent/remainder time; off/on isolates added timing overhead only. The corrected
4.20% claim agrees with final analysis. Required runtime integrity is not labeled
removable experimental observation.

QA-ATTR-02 command/dependency receipts explicitly distinguish reconstructed shell
argv from log-printed evidence. Development series remain historical by source
chronology, not pooled with or selected over final05 for favorable timing.
External environment/source/toolchain and separate untimed hashing are disclosed.

The required artifact inventory initially lacked final-disposition.md. Executor
added it; this verifier read the populated executed-HOLD/no-new-prototype record.
Review/finding/gate/line-count/handoff/source/recovery artifacts are present;
dual verification records and final publication synchronization are separately
owned terminal steps. The other verifier's outcome is not supplied by this file.

Non-deferral explicitly checked: no claim of package complete is authorized
while frozen Clippy fails. The study result may still be published as bounded
characterization. Conversely, the conditional prototype requires proof of a
distinct interchangeable operation and >=5% removable cost; neither exists.
Rejecting incomplete recorded keys and not relabeling established F is the lawful
negative decision, not an omitted mandatory implementation. Matched output bytes
across logical roles do not establish effective-input or ownership equivalence.

No new solver/physics/science behavior is claimed. One audit-capable OFE/day,
honest parent-exclusive/unassigned work and unchanged production HOLD remain
explicit limits. No memory, scale, full-workspace or production-CLI qualification
is inferred. Line counts remain below 3000 with documented inherited WARNs.

Verdict: **PASS for verifier A's bounded evidence and truthful executed-HOLD /
NO_NEW_PROTOTYPE disposition.** No unresolved finding remains in this assigned
scope. The required Clippy gate remains **FAIL**, package remains **HOLD**, and
production remains **HOLD**. This is not approval to start another prototype,
waive inherited debt, push or claim package completion.
