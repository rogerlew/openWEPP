# Current execution handoff

Static + Ran: stable observation-only Q cut, measurements, recomputation study,
and custody complete. Status: dual review and terminal verification closed. The
package is executed HOLD because its frozen warnings-denied Clippy gate is not
green; production HOLD.

Authorized sequence: refine attribution, then inspect and measure actual
recomputation. A typed single-pass prototype is conditional on proven
interchangeable repeated work with removable median cost >=5% of runner time.

Baseline is P at repository commit
`72412a5d1100264659b8e33fe79617eb250c21f6` and isolated source
`/tmp/openwepp-compact-cost-P-vKI6gb`. Its final bundle verified and Q is
`/tmp/openwepp-attribution-Q-3r3eP8`; five initial write-path hashes match P.

Q is `/tmp/openwepp-attribution-Q-3r3eP8`; final binary SHA-256 is
`3e684ac3e8b4dcc93f0dfcffe88faa0c18289f39770237ef4bd404f782d4ce53`.
Final series is `raw/series-05`; all 16 runs pass and accounting closes at 0 ns.

Result: Stage3 parent-exclusive 16.14%, carrier physical evidence 14.30%,
carrier completion/construction 12.51%, imported frozen runtime 9.26%, truly
outside named scopes 7.44%, snow-free parent remainder 6.52%. Recorded request
keys show 196 nominal repeats but eight equal keys produce multiple outputs, so
the key is not effective-input authority. The 200 two-call evaluator pattern is
the already-established F mechanism. Conditional new-prototype gate fails; no
prototype/cache or primary Rust change.

No new prototype is authorized by this package. A future package may decompose a
ranked source-backed region, but must establish removable work before design.
Final custody bundle:
`/tmp/openwepp-attribution-Q-final-bundle`, manifest SHA-256
`de78b15b7fa1f69252fdd073002fe6c25fc45bc0552e354045adcc45615291e5`.
