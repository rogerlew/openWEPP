# Final Disposition

Status: `HOLD / OPENWEPP_ROOT_ZONE_HYDRAULIC_OWNER_V1 not released / production implementation not started`

Evidence class: `Ran + Static`

This HOLD satisfies two explicit future-HOLD conditions in the execution
request:

1. the conductivity audit proves another constitutive owner is required; and
2. explicit root-path configuration cannot be supplied for required rooted
   declared-domain scenarios.

The contradiction is material. `z3` controls root-tissue conductance and the
available geometry is already `dxroot`; saturated/base conductivity cannot be
silently treated as the current unsaturated conductivity. Defaults, aliases,
fixture constants, and caller templates are forbidden.

Production Rust, V10 equations, historical definitions, restart wire, released
restart artifacts, selectors, defaults, outputs and activation are unchanged.
Child 4 and the campaign remain on their existing HOLD.
