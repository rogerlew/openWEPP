# Restart Static-Context Binding

Evidence class: `Static`

The released checkpoint wire must remain unchanged. A future implementation
may extend only `ExpectedRestartStaticContext` and bind the root-zone
configuration through an existing persisted configuration identity if exact
digest lineage proves that identity already covers the new aggregate.

No such binding was implemented or claimed because the configuration itself
cannot yet be instantiated for required rooted scenarios. Persisted
continuation-template root hydraulic values remain validation cache, never
physics authority. No checkpoint schema, vector, codec, or manifest changed.
