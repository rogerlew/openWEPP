# Worker Handoff

First actionable item: admit the missing root-zone hydraulic parameter source,
not a proxy.

1. Select and document where real per-occupancy/per-layer
   `root_path_length_mm` values enter openWEPP. The source must cover every
   required rooted repository scenario and cannot reuse `lateral_root_length_m`.
2. Admit the unsaturated conductivity equation and its saturated-conductivity,
   B, and relative-saturation custody with deterministic binary64 semantics.
3. Populate real configurations from authoritative input data; do not backfill
   fixtures with invented constants.
4. Resume the independent calculator/vector/schema/poison work, then the three
   authority reviews and two verifiers.

Only after authority release may the queued implementation package activate.
