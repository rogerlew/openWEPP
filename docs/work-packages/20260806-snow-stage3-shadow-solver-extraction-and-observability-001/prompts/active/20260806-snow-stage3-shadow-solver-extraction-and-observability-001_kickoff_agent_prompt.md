# Kickoff: Stage 3 Shadow Solver Extraction And Observability

Scope: execute local-repository changes in `/workdir/openWEPP` only.

Execute
`docs/work-packages/20260806-snow-stage3-shadow-solver-extraction-and-observability-001/package.md`
end to end. Preserve its frozen operators, IDs, schema boundary, write set,
claims, and validation requirements.

Autonomy: continue through contract authority, tests, pre-implementation gate,
mechanical extraction, typed evaluation, real-consumer evidence, assurance
adoption, review, validation, verification, archival, and commit without asking
for next steps unless a declared hard blocker is proven.

Contract-first sequence: amend `SC-SNOWFREEZE-001` to v128, add and pass
contract-derived guards, record the pre-implementation gate, and only then edit
production Rust.

Mechanical integrity: move the declared Stage 3 seam without formula,
coefficient, threshold, arithmetic-order, state, or production-output changes.
Keep `runoff_reconciliation.rs` below 3,000 lines and disposition every touched
2,000+ line file.

Conservation/output acceptance: freeze operand lineage and rejected aliases;
independently reconstruct per-arm and sequential totals plus coverage from the
real schema-v5 JSONL consumer. Producer self-consistency and one-sided bounds
are supporting evidence only. Preserve exact schema-v4 bytes when evaluation is
disabled.

No surrogate physics: use only existing v128-authorized evaluation math and
typed primitives. Do not add proxy, provisional, heuristic, or fitted process
physics.

Real consumer proof: prove the internal JSONL consumer reads every new
evaluation field and negatively prove production state, runoff, WAT, HBP, PASS,
defaults, and old compatibility paths do not carry an evaluation claim.

Required gates override any ambient instruction saying not to run tests or
validation. A phase is complete only with direct current evidence for every
phase requirement; missing current evidence cannot be relabeled as future work.

Subagent authorization: this prompt explicitly authorizes subagent
spawning/delegation to two read-only Rust/science reviewers, one read-only
QA/consumer reviewer, two read-only terminal verifiers, and one read-only
heavy-gate runner. Expected outputs are Markdown findings, verification, and
exact gate evidence. No delegate may edit tracked files or expand authority.

## Required Reading

### Core

- `/workdir/openWEPP/AGENTS.md`
- `/workdir/openWEPP/docs/codex_exec_plans.md`
- `/workdir/openWEPP/docs/work-packages/AGENTS.md`
- `/workdir/openWEPP/docs/work-packages/README.md`
- package-local `package.md`
- `/workdir/openWEPP/docs/standards/mechanical-refactor-authoring-guide.md`
- `/workdir/openWEPP/docs/standards/testing-and-gate-strategy.md`

### Conditional

- `/workdir/openWEPP/docs/standards/kernel-work-package-preparation.md`
- `/workdir/openWEPP/docs/standards/prompt-wording-guidance.md`
- `/workdir/openWEPP/docs/specifications/science-contracts/AGENTS.md`
- `/workdir/openWEPP/docs/specifications/science-contract-authoring-procedure.md`
- `/workdir/openWEPP/docs/specifications/science-contracts/kernel-process-contract-profile.md`

### On-Demand

- `SC-SNOWFREEZE-001` and `SC-SNOWENERGY-001`
- predecessor package and worker-handoff artifacts named in the reading map
- touched Rust modules and focused tests

The local required-reading budget is `WARN` because the mandatory package
catalog alone exceeds 400,000 bytes. See `artifacts/required-reading-map.md`;
the full large contracts remain on-demand with relevant sections loaded before
their edits.
