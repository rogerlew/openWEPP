# Owned File Manifest

Evidence class: `Static`.

The authorized categories are exactly those in `package.md#Intended Write Set`.
Record every amended path prospectively and reconcile the exact terminal path
set before disposition.
