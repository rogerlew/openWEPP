# Line-Count Governance

Static: the rejected attempt changed the target from `1295` to `1421` lines,
below both thresholds. After rollback the accepted target is again `1295`
lines. No line-count exception is required.
