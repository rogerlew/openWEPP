# Worker Handoff

Target 01 is locally held with no production/test edit accepted. First create a
dedicated `totalwatsed3.rs` module-test-enhancement package that reaches the
ADR-0021 glue thresholds (`>=85%` line and region), binds every eligible
function to the `>=75%` region floor or reviewed closed-list exclusion, and
characterizes fallible column/type/null/value ordering plus valid optional
soil/element aggregation. Then repeat the behavior-preserving decomposition.

After this hold-evidence commit, continue batch 01 at target 02:
`crates/openwepp-runner/src/watershed_wat.rs`.
