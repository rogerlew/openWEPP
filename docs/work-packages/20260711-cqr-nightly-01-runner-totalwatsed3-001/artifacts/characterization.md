# Characterization

Ran during the subsequently rejected attempt: the existing public CLI suite
remained unchanged and passed before and after decomposition:

```text
cargo nextest run -p openwepp-runner --test totalwatsed3_cli_contract
```

Result: `6` passed, `0` skipped. The numeric oracles cover combined WAT rows,
per-hillslope override/outlet behavior, PASS runoff, lateral flow, area,
sediment deposition/delivery, and required/optional input failures.

The detached scaffold run and after run used the same focused LLVM population;
both passed `6/6` tests.
