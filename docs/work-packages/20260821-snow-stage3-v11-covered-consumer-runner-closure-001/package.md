# Stage-3/V11 Covered Consumer and Runner Closure

Status: `EXECUTING / Child-1 successor / prior package preserved as EXECUTED HOLD`

Date: `2026-08-21`

Package ID: `20260821-snow-stage3-v11-covered-consumer-runner-closure-001`

## Objective

Implement the actual snow-covered V11/Stage-3 shared-carrier consumer,
construct the runner-owned sealed 48-support day capability, and continue
through terminal-liquid consumption, complete typed owner custody, additive
restart, positive physical scenarios, and Child-1 terminal closure.

The snow-free `DirectV11RealConsumerStack` guard remains fail-closed. Covered
execution uses a distinct typed adopter that shares V11 resource and parent
transaction machinery while replacing only the lower-surface environment with
the released Child 2C snow--canopy carrier.

## Required starting identity and historical protection

- Repository: `/workdir/openWEPP`
- Branch: `main`
- Starting `HEAD` and `origin/main`:
  `40a4bb8b53cea4c6b642c70cb036a1a5e2f3de0a`
- No branch creation, reset, rebase, merge, amend, or destructive history
  operation is authorized.
- The predecessor
  `20260821-snow-stage3-terminal-handoff-constitutive-closure-001` is an
  immutable `EXECUTED HOLD` record and must not be rewritten.

## Scope

Included: Child 2C contract mapping; covered V11/shared-carrier execution;
canonical Stage-3 snow custody; terminal event chronology and exact-once
terminal-liquid installation; runner-owned sealed 48-support capability;
complete owner installation; additive restart; rollback and physical scenario
qualification; reviews, comparator, terminal verifiers, and exact-head gates.

Excluded: production activation, selector/default changes, CoE retirement,
canopy-intercepted snow, Richards, empirical calibration, efficacy,
transferability, deployment, and output/API redesign outside the declared
consumer and restart surfaces.

## Exact owner rule

The authoritative V11 `"snow"` owner is the canonical ordered Stage-3
persistent state by lane. Any hydrology winter-column snow fields are checked
compatibility projections only. They are not independently mutable snow
owners. A complete covered segment candidate carries both the ending V11 owner
set and the ending Stage-3 owner envelope, and commits them atomically.

## Required sequence

1. Map every live Child 2C operand and record whether a contract amendment is
   required.
2. Implement the typed covered V11/Stage-3 shared carrier with staged endings,
   actual constitutive calls, and typed owner receipts.
3. Integrate interior terminal chronology: covered pre-event segment, actual
   event/coalescing, zero-duration custody, terminal parcel, snow-free
   remainder, and one parent finalization.
4. Build and install the runner-owned `PreparedStage3V11DayV1` with 48 sealed
   half-hour supports from the existing atmospheric provider clock.
5. Consume terminal liquid exactly once through topology split, surface-liquid
   credit, partial WB14, infiltration/retention/overflow/runoff, and marker.
6. Complete additive restart for owners, position, event/parcel posture,
   surface-liquid/WB14 continuation, GSI/provider cursor, and receipt chain.
7. Run the required positive, poison, multi-lane/tile, cross-midnight, and
   restart scenario matrix.
8. Complete independent science, hydrology/ownership, Rust/API, Rust QA,
   comparator, and dual terminal verification review.

## Intended write set

- This package tree.
- `docs/ROADMAP.md`, `docs/work-packages/README.md`, and the active campaign
  pointer only for truthful successor lifecycle.
- Affected Child 2C contract only if the intake proves an exact unbound field.
- Covered consumer, Stage-3 terminal solver/attachment, V11 consumer,
  runner/provider capability, restart, and package-owned tests discovered by
  the manifest.

The frozen source/test write set is recorded in
`artifacts/owned-file-manifest.md`; any expansion requires an evidence-backed
package update before editing.

## Exit criteria

Child 1 closes only when the actual covered executor passes; the runner builds
sealed 48-support days; Stage 3 is the canonical snow owner; terminal liquid
is consumed exactly once; the complete owner set installs atomically; restart
is equivalent; positive physical scenarios pass; and exact-head gates are
dispositioned. Any current-scope `FAIL`, `BLOCKED`, or unjustified `NOT RUN`
requires `EXECUTED HOLD` with a defect-shaped blocker and first lift step.
Child 3 remains blocked until then.

## Subagent authorization

Subagent authorization: this package explicitly authorizes
spawning/delegating to snow/vegetation/LSE science, hydrology/ownership,
Rust/API, Rust QA, comparator, and terminal-verifier role subagents for the
declared package scope. Expected outputs are compact read-only findings,
exact command/count summaries, comparator logs, and named package artifacts.
Reviewers and verifiers are read-only except their named artifact files;
implementation writes remain within the frozen source/test write set. If
session policy prevents delegation, run the equivalent local gate and record
the policy result.

## Science and calibration intent

This is an authoritative science implementation package, not an empirical
calibration or efficacy claim. `science_implementation_status` starts as
`NOT_IMPLEMENTED`; `calibration_evidence_status` is `NOT_APPLICABLE`; and
`identifiability_status` is `NOT_ASSESSED` until evidence changes those
fields.

## Progress log

- [x] Starting identity, clean tree, instruction discovery, and historical
  HOLD protection.
- [x] Successor package scaffold and intake artifact placeholders.
- [x] Child 2C contract/operand and exact-one snow-owner mapping.
- [x] Provider capability sealing, complete destination binding, and
  conservative terminal-liquid mass basis.
- [x] Parent cadence, sequential GSI/cursor joins, and lane-to-OFE destination
  binding corrections with poison regressions.
- [x] Run-relative support chronology and per-support forcing identity binding,
  including day-boundary and forcing-identity regressions.
- [~] Persistent covered V11/Stage-3 adopter, typed covered input, owner-
  derived carrier operands, exact Stage-3 boundary receipt, canonical ending
  owners, keyed destination receipts, explicit covered lower-boundary masking,
  and rollback. Stage-3 snow VIS/NIR optics now enter the two-stream solve
  before canopy absorption is finalized; the same typed optical result feeds
  the final Stage-3 boundary. Covered latent mass/energy is exact, weighted
  OFE exposes a typed residual decomposition with primitive boundary scale,
  and the persistent positive-radiation case closes through the provisional /
  final reciprocal-longwave correction. Released precipitation-advection and
  soil custody, a component-resolved carrier with heterogeneous physical
  closure, reviewed per-field fixed-point convergence policy, and independent
  ledger closure remain blockers. The keyed candidate-feedback iteration is
  implemented; terminal chronology remains open.
- [~] Runner-owned 48-support capability/provider bind; physical support
  construction and complete restart remain open.
- [ ] Scenario matrix, reviews, exact-head gates, and disposition.
