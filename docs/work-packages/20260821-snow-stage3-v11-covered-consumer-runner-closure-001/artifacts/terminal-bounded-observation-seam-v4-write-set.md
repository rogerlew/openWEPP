# Terminal bounded observation seam V4 write set

Status: `CANDIDATE / NO SOURCE AUTHORITY`

The exact eight files are unchanged from the V3 write set. In particular, the
fixture remains in its owning included test file, carrier projection uses its
existing retained candidate accessors, and no coupled-time source change is
needed. No workspace Cargo, public API, production wire, frozen artifact,
temporal operator, batch, receiver, restart, runner or cutover edit is allowed.
