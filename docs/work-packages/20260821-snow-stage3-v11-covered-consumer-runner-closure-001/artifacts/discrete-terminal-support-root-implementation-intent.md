# Discrete complete-owner terminal support-root implementation intent

Status: `EXECUTED / HOLD / CHILD1-DISCRETE-SUPPORT-ROOT-001`.

The real execution disposition and complete evaluated-tick record are in
`discrete-terminal-support-root-defect-hold.md`.

Base and intake identity: exact clean
`221e94ef3e6ccf646f732bf104b0fb563208d338`, equal to `origin/main` after
`git fetch origin`.

Prior dispositions preserved:

- `CHILD1-REAL-DAE-001` is a closed exploration `HOLD`.
- Hermite--Gauss remains analytical-only future-model evidence.
- production remains `BelowCarrierDomain`.
- no v22/v12/v140/v7 candidate existed at intake.
- `43cc9bbea2fbf5fe6ab6596cee4162de75cef999` is the last fully qualified
  physical implementation.

## Intended implementation

Static: add a private or `cfg(test)` batch-shaped discrete endpoint callback
and exact integer-tick root search. Every callback invocation begins from the
same immutable complete-owner set and evaluates one exact projected positive
support jointly. The returned class is `PreTerminal`, `TerminalAtEndpoint`,
`CrossedTerminal { event_tick }`, or `Invalid`, with a complete endpoint
candidate retained only when all seven owners, Stage-3 endpoint, terminal
physical ledger, snow--soil custody, generated transfers, receipts and joins
validate.

Static: compare result-blind fixed-point, safeguarded-secant and integer
bisection behavior on the same test vectors before freezing the deterministic
algorithm. The admitted implementation must reject support below 600,000,000
ns, no progress, cycles, nonmonotone/ambiguous predicates, root below floor,
bracket-history dependence, candidate-order dependence, replay mismatch and
receipt/owner substitution. It selects the earliest admissible exact tick,
coalesces equal-tick terminal lanes and retains surviving lanes from the same
joint endpoint candidate.

Static: the current real interior fixture is first. Record every evaluated
tick/classification and determine whether its existing 937,500,000-ns fine-1
candidate is a reproducible `TerminalAtEndpoint`. The production call must
continue returning `BelowCarrierDomain`. Probe, endpoint, event and successor
failure evidence must leave beginning owners byte-identical.

## Protected boundaries

No continuous-time derivative, LTE, interpolation or arbitrary-time state is
claimed. No constitutive equation, physical constant, tolerance, 600-ms floor,
ordinary persistent-support behavior, public API/output, production owner
publication, restart wire, terminal receiver, runner, selector/default,
Stage-3 activation, CoE, Child 3/4 or production temporal/Batch path may change.
No external dependency enters production.

## Validation intent

Run focused discrete-root and real-fixture tests, exact production
`BelowCarrierDomain` regression, affected orchestrator compilation/suite,
formatting, warnings-denied affected Clippy when the exact diff permits,
contract/source guards, rollback/poison/replay reconstruction and exact
terminal-diff/write-set/line-count reconciliation. The final heavy affected
and workspace comparators must be delegated to the authorized comparator suite
runner. No new affected or workspace failure name or normalized signature is
accepted; the 90 intake-resident workspace failures remain separate debt and
are neither waived nor repaired here.

Only a passing real discrete checkpoint opens candidate contract authoring and
the two final read-only reviews. Production implementation remains outside this
checkpoint even if both candidate reviews return `GO`.
