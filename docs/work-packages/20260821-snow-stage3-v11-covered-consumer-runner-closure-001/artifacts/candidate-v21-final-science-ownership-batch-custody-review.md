# Candidate-v21 final science/ownership/Batch/custody review

Static plus limited Ran: exact frozen candidate commit
`8cbdc7fb9ce598fed3e138ef01823863058b84f2`; manifest hashes and diff hygiene
verified.

Disposition: `HOLD`.

Critical findings:

1. The v21 floor estimator is disproved by its own matrix and therefore cannot
   satisfy `INV-SNOWENERGY-065` or authorize implementation.
2. CoupledTime v6 contains cyclic digest preimages: temporal arm and ending
   joint include each other, while batch result and lane evidence include each
   other. The declared left-only DAG is not canonically constructible.

Major findings: nonlinear/follower/conservation/error-transport/no-ingress
receipt schemas and preimages remain incomplete; the SCC inventory does not
enumerate executable component cardinalities, residual equations, active-tag
values or canonical order; prospective Batch V2 custody and seven-owner
conservation therefore remain unproven.

The review accepted the production coupling predicate correction and found no
change to tolerance, floor, controller, root, constitutive, publication or
public output. It requested that the synthetic regression invoke the same
typed fail-closure function as production; that ordinary in-scope finding was
corrected immediately after review and revalidated.

Temporal-operator and Batch V2 implementation are not authorized.
