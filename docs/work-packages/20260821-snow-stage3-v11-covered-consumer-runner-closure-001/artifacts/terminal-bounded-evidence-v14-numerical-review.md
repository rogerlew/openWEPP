# V14 numerical/evidence/cardinality review

Disposition: `HOLD`.

Both frozen hashes match. The failure census and the exact live five-component
order, formulas, tolerances, left fold and first-bitwise-equal winner are
accepted.

Blockers:

- `omit_coarse` cannot remove an element from validated
  `[SelectedTrialV14; 3]`; malformed cardinality needs a separate raw `Vec`
  input or optional-slot surface;
- hydrology and WB14 witnesses use the same collection without independent
  exact predicates, so they do not establish separate claims;
- coupling iterations omit convergence evidence and provider/carrier ordinal
  linkage, so selected iteration membership and convergence are unproved.

No source expansion is authorized.
