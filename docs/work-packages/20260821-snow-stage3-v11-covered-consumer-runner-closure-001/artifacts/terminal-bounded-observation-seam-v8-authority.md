# Terminal bounded observation seam V8 implementation correction

Status: `REVIEW CANDIDATE / SOURCE EDITS FORBIDDEN`

The first implementation attempt from V7 was fully rolled back after a
compile-boundary inspection found that the sealed types cannot be named by the
terminal-execution owner through the frozen eight-file set. V8 incorporates
V3--V7 and changes only the exact write set/access plan below.
