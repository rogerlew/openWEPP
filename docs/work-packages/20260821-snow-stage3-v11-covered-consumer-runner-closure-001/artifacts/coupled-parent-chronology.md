# Coupled-parent chronology

Required chronology:

```text
covered beginning owners + sealed support
  -> shared carrier
  -> staged V11 and Stage-3 endings
  -> terminal candidate/coalescing when applicable
  -> zero-duration custody and exact parcel
  -> snow-free remainder from staged V11 ending
  -> one parent finalization
```

Status: `BLOCKED pending implementation`.
