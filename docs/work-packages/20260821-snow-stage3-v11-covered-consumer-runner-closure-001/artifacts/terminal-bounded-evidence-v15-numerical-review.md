# V15 numerical/evidence/cardinality review

Disposition: `HOLD`. All four frozen hashes match.

Accepted: raw `Vec` to validated fixed-array custody, duplicate-floor
representability, exact live component order/formulas, provider-call-key
direction, distinct ingress direction and exhaustion characterization.

Blockers:

- iteration zero has no incoming hint or four real comparison operands, so its
  component array must be optional and absent exactly on that iteration;
- rejection must equal the full live predicate `maximum_scaled > 1.0 &&
  refined.ice > 0.0`, with independent poisons for both conjuncts;
- successful provider calls join exactly one iteration, but failed calls join
  zero; the stated all-provider bijection is incorrect;
- proposed-next duration is pair-level source evidence, not a field independently
  available on every selected trial.

No source edit is authorized.
