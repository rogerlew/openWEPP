# V15 Rust/API/private-compilation/noninterference review

Disposition: `HOLD`. All four frozen hashes match.

Accepted: corrected live type path, lazy associated projection surface, raw
draft/validated array split, duplicate floor retention, provider-key join
direction, caller-only snapshots and raw malformed poisons.

Blockers:

- `Stage3SnowCoveredLowerBoundary` has no declared
  `snow_soil_heat_flux_bits` source field;
- existing WB14 replay validation exposes no decoded rows, and the relevant
  receipt/parser types are private to `direct_runtime`, inaccessible from
  `carrier_phase.rs` within the eleven-file set;
- open-ingress parcels have no receipt digest required by the proposed DTO;
- lower-boundary and LSE live fields lack complete included/excluded
  dispositions;
- provider failures contradict the stated provider/iteration bijection;
- the narrow token guard cannot alone prove NoEvidence helper nonreachability.

No source edit is authorized.
