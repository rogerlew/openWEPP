# Terminal diagnostic correlation V5 checked-schema authority

Status: `CANDIDATE / NO SOURCE IMPLEMENTATION AUTHORITY`

Base and chronology: `fb676c63a55c6733caa9e653eaef2416d6c07109`, forward
from governed V4 HOLD. Last qualified physical implementation remains
`43cc9bbea2fbf5fe6ab6596cee4162de75cef999`.

The primary authority is the machine-readable
`terminal-diagnostic-correlation-v5-schema.json`. Generated prose is evidence,
not an alternate schema. The package-local `syn` checker verifies declared
source types and the complete live `CoveredCarrierPhaseResultV1` field set;
wire mode, ownership, projection membership and native replay are read only
from the schema.

V5 retains V4's accepted ownership split, exact live role and pair mapping,
separate pair/floor decisions, cardinality, raw binary64 convention and actual
`framed_sha256` framing. It replaces V4's heuristic replay classifier, fixed
TARGETS, semantic carrier placeholder, lexical snapshots and open error graph.

The sole evidence root is `rejected_prefix_v5`. Dependency traversal must make
reachable nodes exactly equal declared nodes. Each field binds output tag and
order, source path/expression, resolved Rust type, encoding mode, nested schema,
collection/variant order, owner stage/module and private access. `usize`, an
unresolved/ambiguous declaration, a stale carrier field/type, duplicate or
unreachable node, and unnamed private access fail regeneration.

No native wire is admitted. WB14 child bytes are an explicit byte projection:
the accessor and the separate multi-argument binding validator do not comprise
the exact type-associated serializer/preimage and validator contract required
for native-wire classification.

`TerminalCarrierPhaseProjectionV5` dispositions every live carrier-result
field before encoding. Provider-owned members use individually named nested
projections; terminal transition outcomes and WB14 parent material are
excluded. The whole carrier result may never be serialized and trimmed.

The final error is the narrow exact witness
`DirectSnowStage3EvaluationError::TerminalNumerics(
SnowTerminalNumericsFailure::BelowCarrierDomain)`, encoded as outer tag 2 and
inner tag 2. No outer attachment-error graph is claimed or traversed.

The generated owner-access plan is prospective only. No test helper, source
seam, receipt capture, estimator matrix, final v21 review, temporal operator,
Batch V2, event, receiver, restart, runner, Child 3 or cutover is authorized.
Two independent GO-to-evidence reviews on the exact frozen V5 candidate would
authorize only a later exact-file implementation intent. Either HOLD retains
this checkpoint before source edits.
