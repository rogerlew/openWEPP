# Required-reading map

Static: `tools/agents/find-agents --for` was run before edits for the package,
orchestrator, and runner write paths.

Applicable instruction chain:

- Package artifacts: root `AGENTS.md`, `docs/work-packages/AGENTS.md`.
- Rust sources: root `AGENTS.md`, `crates/AGENTS.md`.
- Kernel work: `docs/specifications/science-contracts/AGENTS.md` and the
  released Child 2C/Stage-3/V11 contracts selected below.

The exact contract files read and byte counts are recorded in
`required-reading-byte-count.md` after the contract census.
