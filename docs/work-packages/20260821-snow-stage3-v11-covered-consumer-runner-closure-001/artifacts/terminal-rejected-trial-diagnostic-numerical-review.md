# Rejected-trial diagnostic numerical/evidence review

Evidence class: `Static`

Reviewed SHA-256:
`af381f66ad624cdb90770bcb454fea3f41f65131c0ad7751146a708eb096b114`.
The hash matched. The reviewer made no edits and did not read the other review.

Recommendation: **HOLD**.

## Findings

1. **Critical — the complete record is unreachable without a prohibited
   temporal-control edit.** At the rejection decision the terminal solver owns
   only aggregate trial state/ledger values. The rich carrier receipts remain
   upstream, the full joint is discarded, and the half joints are consumed
   before the pair-level scaled error is known. A complete correlation needs a
   new accumulator threaded through the production provider/evaluation/solver
   path or duplicate decision logic.
2. **Critical — aggregate flux values cannot reconstruct the promised
   component evidence.** The retained terminal flux/ledger scalars do not bind
   prescribed, rate, generated, topology, hydrology, q_ss and provider receipt
   digests. They may reproduce the known total energy delta but cannot prove
   its authoritative operands.
3. **Major — observer-panic noninterference is incomplete.** An invoked
   callback panic would unwind the active transaction unless a new local
   barrier were authorized. Failure injection instead needs a post-return
   clone or a separately specified unwind boundary.
4. **Major — pair-level correlation is undefined.** The schema does not bind
   the final full/half call triple, coupling iteration, discarded nonlinear
   iterations and one scaled-error decision into an unambiguous cardinality.

Positive static evidence: the declared boundary preserves the floor,
acceptance/controller, public API and output semantics, and the existing
precheck can prove zero provider calls below 600 ms. That does not make the
complete capture seam implementable.
