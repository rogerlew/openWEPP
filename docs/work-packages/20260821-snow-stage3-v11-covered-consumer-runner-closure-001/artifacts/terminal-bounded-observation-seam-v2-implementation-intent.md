# Terminal diagnostic correlation bounded observation-seam authority V2

Status: `REVIEW CANDIDATE / SOURCE EDITS FORBIDDEN`

This forward-only successor retains the first bounded-seam HOLD and corrects
its blockers. The governance object is a private source-level test observation
intent, not a durable wire. It incorporates by reference the exact V2 write
set, Rust intent, validators and test matrix with the same candidate hash.

The physical algorithm, production wrapper signatures and
`BelowCarrierDomain` outcome remain unchanged. The last qualified physical
implementation remains `43cc9bbea2fbf5fe6ab6596cee4162de75cef999`.

Two independent GO-to-evidence reviews are required on the frozen candidate:
numerical/evidence/cardinality and Rust/API/private-compilation/noninterference.
Either HOLD ends the checkpoint without source edits. Two GO reviews authorize
only the nine-file implementation intent. They do not authorize a production
diagnostic seam, temporal operator, Batch V2, receiver, restart, runner, Child
3, cutover, or final review.
