# Terminal diagnostic correlation bounded observation-seam authority V3

Status: `REVIEW CANDIDATE / SOURCE EDITS FORBIDDEN`

This forward-only successor retains both earlier HOLDs. It replaces invented
source tags with three real operands, includes the actual private fixture owner,
uses the live `Option<Joint>` type, adds shared chronology, expands nested state,
and freezes the exact measured energy operands.

The physical algorithm, wrapper signatures and `BelowCarrierDomain` remain
unchanged. Two independent reviews of the same candidate are mandatory:
numerical/evidence/cardinality and Rust/API/private-compilation/noninterference.
Either HOLD requires in-scope correction and renewed review before source edits.
Two GO reviews authorize only the eight-file implementation intent, not any
production diagnostic seam or later temporal/batch/restart/runner/cutover work.
