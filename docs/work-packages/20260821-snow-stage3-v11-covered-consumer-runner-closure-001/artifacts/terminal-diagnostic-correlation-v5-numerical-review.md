# Terminal diagnostic correlation V5 numerical/evidence/cardinality review

Recommendation: **HOLD / NO GO-to-evidence**

Evidence class: `Static`. Independent read-only review; no files changed and no
communication with the Rust/schema reviewer. All 12 candidate-manifest hashes
matched exactly.

Refresh revalidation: all 12 whitespace-normalized frozen hashes also matched;
the HOLD and every substantive finding remain unchanged.

## Findings

1. **Critical:** closure covers ten manually declared record IDs, not the
   transitive Rust-type graph. Five source declarations are resolved while
   owner/joint snapshots, provider calls, receipts, parcels, carrier keys,
   request/child/projection, coupling identities/checks, selection keys and
   `DiagnosticF64V5` retain null nested schemas.
2. **Critical:** “exact fields,” `NamedBytesV5`, `CarrierKeyV5` and similar
   phrases do not enumerate checked tags, paths, types, collections or variants.
3. **Critical:** the carrier check reaches only 13 top-level fields. Its nested
   field lists are unchecked prose and leave provider-evidence selection to
   implementation judgment.
4. **Critical:** selected-trial, pair-position and separate floor-decision
   schemas are absent. Admission omits required-half and minimum duration;
   opaque pair identity cannot prove exact coarse/FINE_1/FINE_2 linkage.
5. **Critical:** noninterference snapshots use open maps, named-byte vectors,
   provider-call placeholders and an owner-plan reference that enumerates no
   exact state locations.
6. **Major:** only hydrology terminal-liquid ingress remains; separate WB14
   credit and surface-ingress zero witnesses are absent.
7. **Major:** `TerminalLedger` is listed but unused, and selected state/ledger
   evidence cannot reconstruct the known energy difference.

The ownership split, raw-binary64/finiteness convention, five-component fold
intent and narrow `BelowCarrierDomain` witness are accepted. The defects above
block evidence implementation.
