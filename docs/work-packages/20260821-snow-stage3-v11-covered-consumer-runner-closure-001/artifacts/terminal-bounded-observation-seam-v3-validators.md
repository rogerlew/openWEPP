# Terminal bounded observation seam V3 executable validator contract

Status: `CANDIDATE / NO SOURCE AUTHORITY`

Validators recompute f64 finiteness, refined-minus-coarse delta, denominators,
scaled errors, exact five-component order/fold and first-bitwise-equal winner.
They validate exact coarse/full, fine-1/half-1, fine-2/half-2 mappings; contiguous
half supports; state and complete-joint joins; selection against the complete
iteration arena; and exhaustive contiguous provider ordinals.

Pair and admission records share `(event_ordinal, chronology_ordinal)`. The
focused rejected pair must be `RejectRetry` and the admission must have the same
event ordinal and pair chronology plus one. It must bind 0.9375 proposed,
0.46875 required half, 0.6 minimum, `BelowCarrierDomain`, and identical provider
counts. The provider call occurs only after the live floor branch.

The exact final energy operands obtained read-only from the unchanged test
executable are coarse `1318.7458556071579` / `0x40949afbc1928120`, refined
`1291.5327277739345` / `0x40942e218363bae1`, and signed refined-minus-coarse
`-27.213127833223325` / `0xc03b368f8bb18fc0`. The reported positive
`27.2131278332233` is only its rounded magnitude and is not the bit authority.

Three validators are independent: hydrology requires every selected
rain-derived external-liquid value to positive-zero bits; WB14 requires no
receipt kind `TerminalReceiver`; surface requires no accepted open-ingress
parcel kind `TerminalReceiver`. Positive poison fixtures insert one exact
offending scalar/receipt/parcel respectively.

The existing private helper `exercise_complete_wb14_cadence(0.000_6, 0.0,
false, None, false, None, true)` remains the only fixture setup. A sibling test
in the same included file invokes the capture entry and all validators.
NoEvidence and CaptureEvidence run fresh copies, compare the exact typed error
and every explicit physical snapshot leaf. Required gates are rustfmt, crate
check, focused positive/poison/noninterference tests, V20/V21 historical guards,
and diff hygiene. Capture/effectivity must execute before final reviews.
