# Terminal bounded observation seam V4 implementation intent

Status: `REVIEW CANDIDATE / SOURCE EDITS FORBIDDEN`

Base is `bda290573a17fd9be5b07542a55b67688500cf5a`. V3 is retained as HOLD.
This candidate incorporates the V3 write set and DTO declarations except where
the V4 correction file replaces them. The correction is normative on conflict.

Two independent GO-to-evidence reviews of the same hashes are required before
the exact eight-file source intent. Either HOLD requires another forward
correction. `BelowCarrierDomain` and physical implementation `43cc9bbe...`
remain authoritative.
