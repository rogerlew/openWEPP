# Successor kickoff — covered Stage-3/V11 consumer and runner closure

Preserve the predecessor package as `EXECUTED HOLD`. Implement a distinct
typed snow-covered V11/Stage-3 shared-carrier consumer; do not remove the
snow guard from `DirectV11RealConsumerStack`. Reuse the released Child 2C
carrier and V11 parent/resource transaction machinery, make canonical ordered
Stage-3 state the sole `"snow"` owner, construct a runner-owned sealed
48-support day from the existing half-hour atmospheric provider, and continue
through terminal-liquid exact-once consumption, complete atomic owner custody,
additive restart, positive physical scenarios, and Child-1 closure.

First map the released contracts and all live operands. Amend only an exact
missing field. Then execute covered chronology, terminal chronology, runner
capability, liquid transaction, restart, scenario, review, comparator,
verification, and exact-head gates in that order. Do not claim closure from a
producer-only skeleton or shadow path.

Subagent authorization: this package explicitly authorizes
spawning/delegating to the named science, ownership, Rust, comparator, and
terminal-verifier roles for the declared scope, with bounded artifact/source
write access as defined in `package.md`.
