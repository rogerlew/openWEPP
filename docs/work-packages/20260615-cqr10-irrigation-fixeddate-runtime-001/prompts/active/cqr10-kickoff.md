# CQR10 Kickoff

Work in `/home/workdir/openWEPP`.

Execute
`docs/work-packages/20260615-cqr10-irrigation-fixeddate-runtime-001/package.md`.

Required before edits:

- read the package dependencies listed in `package.md`;
- capture before line counts, suppression census, LCOV, CRAP, and target
  function identity;
- add focused characterization before production refactor if current tests do
  not freeze the target branches.

Preserve public API, runtime symbols, units, typed guards, error fields,
allowed strings, parser compatibility, furrow formulas, and kernel-facing
projection behavior. Run and record all required gates before package closure.
