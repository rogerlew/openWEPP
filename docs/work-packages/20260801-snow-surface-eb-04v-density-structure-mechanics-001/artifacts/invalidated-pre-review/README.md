# Invalidated Pre-Review Evidence

Status: `INVALIDATED / NOT DECISION ELIGIBLE`.

The first 36-cell execution closed additively and preserved model behavior, but
independent review proved that multilayer wet/internal-cap attribution had been
projected from summed layer-local density deltas rather than reconstructed in
bulk-density space. The defect changed only diagnostic classification, not the
modeled WAT state, but it invalidates the original process magnitudes and every
figure or inference derived from them.

The original receipt, results, summary, synthesis, logs, and figures are kept
here to prevent silent overwrite. Their large target outputs remain at
`target/snow_surface_eb04v_density_diagnostics_invalidated_proxy_attribution_20260801/`.
Only the fresh post-correction execution at the canonical target path may
support EB-04V disposition.
