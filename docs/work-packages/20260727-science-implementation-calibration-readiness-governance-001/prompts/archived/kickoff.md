# Kickoff: Science Implementation and Calibration Readiness Governance

Scope: local repository documentation and governance work; flat-file
reads/edits plus git commit/push only.

Execution mode: `package-end-to-end`.

Execute `package.md` through disposition. Preserve authoritative science,
typed guards, evidence truthfulness, and the separation between implementation
correctness and empirical calibration claims.

This prompt explicitly authorizes subagent spawning/delegation to two
independent read-only reviewers and two terminal verifiers for
scientific-authority coherence, operational/schema enforceability, exact-diff
reconciliation, and closure; outputs are compact findings and final
`PASS`/`HOLD` dispositions.
