# Intent Plan

Status: `PROSPECTIVE FREEZE / COMPLETE`

Evidence class: `Static`

This is a documentation-only, repository-governance increment. The exact
terminal diff is limited to the package's declared write set. It establishes
policy and schema obligations without changing an existing process contract,
production code, tests, fixtures, datasets, or calibration result.

The doctrine has one canonical architectural statement in ADR-0042. The
correctness model owns authority-plane adjudication. Contract specifications
own per-process disclosure requirements. Work-package governance owns
execution and terminal disposition. Root `AGENTS.md` contains only a binding
pointer.

No heavy correctness campaign is selected because runtime behavior is
unchanged. Required gates are scoped documentation lint, path/link integrity
where supported, diff hygiene, root-size policy, prompt state, review,
finding disposition, and exact write-set reconciliation.
