Static: isolated J implementation exists; production HOLD.
Ran: initial core and test-support checks passed. Focused J tests passed 7/7;
measured cut3 matched-release numerical/reference/exclusion/integrity selection
passes15/15 in j-release-cut3-numerical.log (local timing explicitly excluded).
Prior14/14 is retained in j-release-numerical-tests4.log. Earlier logs are
j-core-check2.log, j-test-support-check.log, j-focused-tests.log and
j-authentic-tests.log under /tmp/openwepp-hotpath-jacobian-jmvOZh.

Implemented: private current-base, ordered single-use complete-column assembly;
Covered and GenericV3 providers; scaled-coordinate mapping; unchanged inactive
row replacement and dense solve. Ordinary fallible base preprocessing remains.
No unknown elimination, system resizing, pivoting or production activation.

Passing tests include complete columns, atomic malformed matrix/scale refusal,
actual GenericV3 nonunit coordinate mapping, authentic capability guards and
postadjustment matrix parity. RHS parity uses identical declared anchors, not
reconstruction of original solve-initial anchors absent from captured records.
Actual A/J one-OFE consumer comparison passes exact common input/output/control
identity in final cut3 admission and all28 paired-series processes. Complete
one-OFE J work counts are9844 direct columns with unchanged solve/sweep/iteration
counts. Both10-OFE admissions also pass, with2924 J columns; not all-OFE
physiology or scale timing qualification.

The earlier debug reference test failed strict base replay; matched release
passes with unchanged corpus/comparison rules. Source-real exclusion coverage
now passes nine variants: positive input area, exact wet-only inactivity and
negative zero across Covered Potential and both GenericV3 passes. Each performs
two completed sun FD columns and zero sun direct columns (18 FD total).
Every variant has a successful ordinary base but its later solve rejects;
this is FD-retention evidence, NOT accepted active-foliage/convergence evidence.
Historical authority follows canonical first-error equality or successful all-None
provider checks; no historical accepted-solve/FD coverage is invented.
The inherited forced-complete regression
now concerns historical ground/soil identity reuse, not A/J leaf treatment.

Accepted-operand audit corrections pass actual poisons/on-off checks and
independent review on measured cut3:504 raw sets, zero failures/overflow. This
is bounded candidate-construction closure, not full soil/publication evidence.
Local/one-OFE timing and memory/teardown are executed; the frozen memory-priority
failure yields engineering REJECTED_IMPLEMENTATION. Broad correctness remains
FAIL, with a canonical720s timeout on the formerly interrupted runner case.
Terminal reviews/verification are being finalized; production remains HOLD.
