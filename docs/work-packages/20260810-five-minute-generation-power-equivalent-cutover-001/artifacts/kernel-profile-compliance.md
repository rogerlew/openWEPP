# Kernel Profile Compliance

Status: `PASS — diagnostic projection implemented within canonical authority`

Evidence mode: `Static + Ran`

The existing draft `SC-WATBAL-001` names the unchanged WB14/WB19 producer
surface but remains byte-unchanged by this increment. The approved, active
`SC-OUTPUT-WAT5-001` version 2 owns the optional diagnostic projection,
schema, closure, sparse support, source-completeness, and noninterference obligations.
The new contract's Binding Exposure Index maps all seven invariants into the
canonical core; scoped unit compliance and binding-exposure checks pass.

The implementation changes no Green-Ampt equation or parameter. It advances
the unchanged helper chronologically on source intervals split at exact
300-second boundaries, then closes its diagnostic shape to the unchanged
hourly ledger. Raw event, composed hour, and day identities are guarded by
`WAT5-E-004`; missing timing and invalid domain states fail typed. The event is
publication-only, boxed off the resident frame, and not read by peak, erosion,
HBP, OFE, channel, or watershed routing.

Erosion remains the canonical hourly-mean path under explicit `NO_ADOPTION`.
No provisional physics, candidate arm, selector, or production cutover was
introduced.
