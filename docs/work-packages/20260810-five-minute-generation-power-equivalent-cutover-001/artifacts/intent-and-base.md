# Intent And Base

Status: `queued`

Evidence mode: `not-run`

Before implementation, record the actual starting HEAD and worktree state,
reconcile it with the expected identity in `package.md`, and declare the
Critical implementation/adoption intent and exact-diff envelope.
