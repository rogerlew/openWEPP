# Review Agent A

Status: `PASS / GO — reopened final science review at exact clean SHA`

Evidence mode: `Static + Ran`

## Reopened final review — 2026-08-11

Reviewed exact clean SHA
`e7851f1a626c1e6e77d64c2618f26bb772787b36`. No Critical, High, Medium,
or Low findings remain. Focused public-contract/closure/chronology regressions
passed 3/3 (nextest `3e19261b-73ff-4ffc-bd1a-9f0faa340dab`) and explicit
manifest-construction rollback composition passed 1/1 (nextest
`9ca1d0ff-3924-41fb-9172-fe878506f7dd`). The final assessment confirms typed
`WAT5-E-003` domain versus `WAT5-E-004` closure failures, strictly advancing
calendar keys, finite derived arithmetic, positive-storage real-chain closure,
transactional no-replace publication, run-file-only opt-in, erosion
`NO_ADOPTION`, and the unopened Topanga outcome cohort.

The historical review below is retained as superseded context.

This is the final implementation re-review. I independently inspected the
corrected runtime, contracts, focused vectors, publication path, and refreshed
terminal evidence. I did not consult reviewer B and did not edit production
code or another package artifact.

Ran independently:

    env TMPDIR=/home/workdir/openwepp-task-tmp \
      cargo nextest run -p openwepp-hillslope-orchestrator --lib

Result: `PASS`, 490/490, nextest
`3a1b5967-04b0-4658-92fc-e7dcb2fb609b`.

I also independently read
`/home/workdir/openwepp-wat5-terminal/on/output/H61.wat-subhourly.parquet`
and compared diagnostics-on/off protected-file hashes. Ordered `math.fsum`
reconstruction produced 24 rows, global bins `0..23`, rainfall
`79.56180923624731 mm`, raw infiltration `63.502721051072044 mm`, raw and
closed generation `16.059088185175263 mm`, raw event residual
`7.105427357601002e-15 mm`, and exact-zero reconstructed residual for each of
the two active hours. All 24 exponent, power-rate, and power-duration values
were null. HBP, PASS, WAT, and loss JSON hashes were pairwise identical with
WAT5 enabled and disabled. The p102 terminal stderr records the expected
`WAT5-E-001` fail-closed result.

## Prior Finding Disposition

### `REVIEW-A-MAJOR-001` — closed

Decision: `accepted and corrected`.

`compute_wb14_subhourly_profile` now rejects out-of-day support and positive
untimed additional supply, clones the local-rain producer inputs, splits every
source interval at exact 300-second boundaries, and passes those chronological
pieces through the unchanged Green-Ampt state advance. Cumulative infiltration
therefore advances after each five-minute piece rather than after a whole
source interval.

The new delayed-ponding vector uses the review's counterexample
(`i=2e-5 m/s`, `K=1e-5 m/s`, matric potential `0.01 m`) and independently
requires exact-zero first-bin excess plus positive second-bin excess. Exact
boundary, hour-crossing, multiple-source-interval, and resolution-stability
vectors supplement it. The refreshed real Parquet evidence was generated by
the corrected release binary. No proportional whole-interval temporal
apportionment remains on the requested WAT5 replay path.

### `REVIEW-A-MAJOR-002` — closed

Decision: `accepted and corrected`.

Source existence is now tested with exact-positive predicates, while the
scale-aware `1e-12 m * max(1, authority)` quantity is confined to closure
adjudication. Every positive `hourly_additional_supply_m`, including a value
below the closure tolerance, fails `WAT5-E-001`; positive authority with
exact-zero raw support fails `WAT5-E-002`; and every positive rainfall,
infiltration, raw/closed generation, or saturation bin participates in sparse
support. Focused tests bind both the sub-tolerance additional-supply rejection
and preservation of sub-tolerance positive rain.

The real p102 control also fails at lane 1/day 1 with `WAT5-E-001`, publishes
no WAT5 target, and leaves no WAT5 temporary file. I found no uniform,
rainfall-shaped, tolerance-zero, or silent fallback on this boundary.

### `REVIEW-A-MAJOR-003` — closed

Decision: `accepted and corrected`.

The runtime now validates three distinct closure surfaces through the named
`WAT5-E-004` guard:

- raw event supply equals infiltration plus post-depression generation plus
  depression-storage delta;
- each composed hour's closed WB14 plus saturation return equals its
  authoritative WB14/WB19 closing depth;
- the composed day equals the sum of the unchanged authoritative 24-bin
  ledger.

Negative vectors exercise the raw-event, hourly, and daily error identities.
The terminal Parquet reconstruction independently closes both emitted hours
and the raw event within `TOL-WATBAL-010`.

## Terminal Authority Re-review

Status: `PASS — exact-worktree authority correction accepted`

This targeted re-review followed the terminal finding that the earlier
base-to-identical-head admission command did not observe the dirty/untracked
implementation and that draft `SC-WATBAL-001` could not admit a new production
binding.

Static findings:

- `SC-WATBAL-001` is byte-identical to package base
  `a8a96498ee909c4305fbc0a4db562b72e45efd2b`; it contains no WAT5 invariant,
  tolerance, alias, or contract reference. Its draft lifecycle was not
  promoted or used as the new diagnostic owner.
- Approved, active `SC-OUTPUT-WAT5-001` locally owns `INV-WAT5-001..007`,
  `TOL-WAT5-001`, the branch/guard table, output obligations, aliases, units,
  test vectors, gap limits, and Binding Exposure Index. References to existing
  WB14/WB19 surfaces are explicitly interface/provenance anchors; diagnostic
  replay, closure, noninterference, and publication authority remain local to
  the approved contract.
- The contract's same-directory atomic no-replace hard-link wording matches
  the writer: it closes and validates the completed temporary Parquet inode,
  creates the target with `std::fs::hard_link`, and then removes the temporary
  name. This preserves an existing target and avoids Unix rename replacement.
- Worktree admission unions the base-to-worktree tracked diff with
  `git ls-files --others --exclude-standard`, so the untracked WAT5 runtime,
  writer, contract, and tests cannot disappear from the evaluated increment.
  Its fingerprint covers every changed science path plus the complete active
  contract/input-contract registries and direct authority-policy inputs.
- All 17 changed science-crate paths have one atomic
  `SC-OUTPUT-WAT5-001` entry and an executable blocking A1 definition. The four
  shared frame/executor/runner paths retain distinct atomic `SC-PLANT-001`
  entries. The checker validates every matched authority, rejects multi-contract
  entries and duplicate same-contract bindings, and does not silently choose
  one owner.
- `hard-invariant-wat5-runtime-v1` is an exact-inventory, blocking,
  hard-fail `NEXTEST_V1` A1 gate over `openwepp`, the orchestrator, output,
  runner, and simulation-contract packages. No A3 suite is registered as
  applicable to this diagnostic-only projection.

Ran independently on the targeted authority diff:

    bash tools/release/check_science_contract_admission.sh \
      --base-ref a8a96498ee909c4305fbc0a4db562b72e45efd2b --worktree

Result:

    A0_ADMITTED contracts=43 science_surfaces=17 \
      base=a8a96498ee909c4305fbc0a4db562b72e45efd2b head=WORKTREE \
      authority_sha256=6f95845b5065e9134cded858e69ed359b2e42bd32318f800f87801d4088d1298

Additional independent results:

- `bash tools/release/check_sc_unit_compliance.sh --path
  docs/specifications/science-contracts/contracts/SC-OUTPUT-WAT5-001.md`:
  `PASS`;
- `bash tools/release/check_authority_suite_antievasion.sh`: `PASS`;
- targeted WAT5 contract, property, typed roundtrip, HBP/routing exclusion,
  and authority-policy tests: `PASS`, 15/15, nextest
  `a63ba6cb-f622-4aff-9418-d9d1ebb2ab47`;
- final post-A0 full-workspace nextest: `PASS`, 2,380/2,380 with 33 skipped,
  run `b920db77-070f-4686-a7bf-2e2727094374`;
- final post-A0 workspace doctests: `PASS`, 0 failed.

The final fingerprint includes the parser input registry in the hashed
authority-input surface. The full-workspace and doctest receipts were produced
after that correction, so no terminal correctness gate remains pending.

No scientific or contract-admission finding was identified. The terminal
authority correction is accepted and does not change the prior WAT5 science
or erosion `NO_ADOPTION` verdict.

## Final Science Assessment

- The optional water product satisfies the admitted local-rain diagnostic
  domain. Green-Ampt physics and continuous state are reused without changing
  WB14 authority; five-minute shape is closed back to each authoritative hour.
- Hourly saturation return remains separate, is divided evenly only as an
  explicit `hourly_zero_order_hold`, and carries no finer physical claim.
- Source completeness is fail-closed for hourly-only runon/melt. Multi-OFE,
  snowmelt, frost-release, HBP, routing, peak, and discharge adoption are not
  claimed.
- Erosion remains binding `NO_ADOPTION`. Static inspection and terminal
  Parquet evidence show no admitted exponent, candidate arm, selector,
  power-equivalent rate/duration, Topanga outcome, or production cutover.
- WAT5 remains an additive publication structure. Protected on/off byte
  identities and source-consumer inspection support noninterference with HBP,
  PASS/WAT, peak, routing, and production erosion.

No new hydrology/science finding was identified. Review A recommends `GO` for
final package closure.
