# Artifacts

All evidence for this package is updated in place.

Status convention: begin each artifact with `Status: queued` and
`Evidence mode: not-run`; label subsequent evidence as `Static:` or `Ran:`.
Large Topanga outputs, binaries, and run directories stay in an immutable
external evidence root; commit only compact manifests, hashes, summaries, and logs.
