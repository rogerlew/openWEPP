# Independent Terminal Verification A

Status: `PASS — final reopened-package terminal verification`

Evidence class: `Static + Ran lightweight verification`

## Reopened final receipt — 2026-08-11

Final verdict: `PASS`. The verifier reviewed exact clean closure head
`fe92d56d293ca3ea69d991da6fadb0338b429b52` against exact tested/reviewed
candidate `e7851f1a626c1e6e77d64c2618f26bb772787b36`. It independently accepted
the 2,396/2,396 full run, doctests, four reviews, A0, authority, performance,
transaction/schema/science evidence, and exact 43/43 reopened ownership. An
initial documentation-only HOLD identified stale worker/status/handoff prose;
all lifecycle findings and the three nonblocking QA residual dispositions were
reconciled before final `PASS`. No implementation or evidence finding remains.

The historical receipt below is retained as superseded context.

Verdict: `PASS`

Source identity: repository `HEAD`
`c9f28a7dbe7adf69d8e6d54ebd8da57568af5552` plus the exact reconciled
87-path WAT5 working-source increment. The separately owned predecessor and
backlog closure paths are excluded.

This verification supersedes my earlier 82-path/pre-A0 receipt. I inspected
the final authority repair, implementation, package evidence, four fresh
review receipts, post-A0 Critical logs, real consumer outputs, and exact dirty
path set. I did not rerun the 2.4-hour full workspace campaign and did not rely
on the other terminal verifier's conclusion.

## Findings

None.

The prior terminal findings are closed. The package now documents one
executable user command and its `[outputs] wat_subhourly` runfile entry; exact
worktree A0 observes tracked and untracked science surfaces; shared paths keep
separate atomic Plant and WAT5 authority; contract publication wording matches
the no-replace hard-link implementation; the parser input registry participates
in the authority fingerprint; and a complete post-repair Critical campaign
supersedes the earlier full receipt.

## Authority And Adoption Legitimacy

- The predecessor is terminally closed. Its admitted exact-source Critical
  receipt passed 2,346/2,346, run
  `64cd5e97-d253-4da1-a3cf-3c4e16f83d22`, and both fresh predecessor terminal
  verifiers pass.
- `SC-WATBAL-001.md` is byte-identical to package base
  `a8a96498ee909c4305fbc0a4db562b72e45efd2b`. It remains
  `in_review`/`draft` and contains no WAT5 amendment. Existing peak invariants
  `INV-WATBAL-102..104` remain unchanged.
- Approved, active `SC-OUTPUT-WAT5-001` locally owns
  `INV-WAT5-001..007`, `TOL-WAT5-001`, branch guards, output obligations,
  aliases, units, vectors, Binding Exposure Index, and the bounded gap register.
  Its registry row and front matter agree.
- Exact worktree admission sees all 17 changed science-crate paths, including
  untracked WAT5 files. Each has one atomic `SC-OUTPUT-WAT5-001` mapping and
  executable blocking A1 coverage. Four shared core frame/executor/runner
  paths retain a second, separately atomic `SC-PLANT-001` mapping; no merged
  multi-contract entry or duplicate same-contract mapping is admitted.
- The authority-input fingerprint covers the complete active contract and
  parser-contract sets, science index, parser input registry, external
  authority registry, impact map, gate definitions, checker source, and all
  changed science paths. Independent execution returned:

  ```text
  A0_ADMITTED contracts=43 science_surfaces=17
  base=a8a96498ee909c4305fbc0a4db562b72e45efd2b head=WORKTREE
  authority_sha256=6f95845b5065e9134cded858e69ed359b2e42bd32318f800f87801d4088d1298
  ```

- The prospective constitutive study contains 1,008 records and rejects every
  fixed exponent. Topanga plan/selection/input identities remain pinned, both
  result summaries state `outcomes_opened=false`, no candidate arm or selector
  exists, and every power field is null. Erosion therefore truthfully remains
  `NO_ADOPTION`; production retains hourly-mean erosion.

## Implementation And Protected Boundaries

- Requested WAT5 execution performs chronological Green-Ampt replay after
  WB16 peak by splitting local-rain intervals at exact 300-second boundaries
  while carrying cumulative infiltration continuously. It preserves raw
  depression-storage lineage and validates raw-event, hourly, and daily
  closure before publication.
- Exact source predicates, not closure tolerance, decide source presence and
  sparse support. Positive hourly-only additional supply fails with
  `WAT5-E-001`, positive authority without raw support fails with
  `WAT5-E-002`, invalid support fails with `WAT5-E-003`, and closure failures
  use `WAT5-E-004`.
- Hourly saturation return is exposed separately as a labeled
  `hourly_zero_order_hold`; no finer physical claim is made.
- WAT5 is publication-only. Source inspection and the recursive exclusion
  target show it is not read by public peak, erosion, HBP, OFE/channel routing,
  watershed orchestration, or watershed output. HBP and routing source files
  are absent from the owned diff.
- The typed Parquet writer buffers at most 8,192 rows, validates the completed
  row count plus full schema/metadata, preserves existing targets, and publishes
  with a same-directory atomic no-replace hard link before removing the
  temporary name. Pairwise lexical output aliases are rejected before writers
  open.
- No changed Rust file reaches the 3,000-line blocker. The three 2,000-line
  WARN files have named split intents; the WAT5 runtime and writer remain in
  focused sub-650-line modules.
- No network, credential, secret, environment-selected method, silent fallback,
  or unbounded per-run row accumulation was introduced.

## Real Consumer And Independent Reconstruction

I independently read
`/home/workdir/openwepp-wat5-terminal/on/output/H61.wat-subhourly.parquet`
with PyArrow. It contains 26 columns and 24 rows, global bins `0..23`, hours
`0..1`, exact 300-second support, canonical hour/bin relationships, and 24/24
null exponent, power-rate, and power-duration values.

Independent ordered reconstruction produced:

- rainfall `79.56180923624731 mm`;
- raw infiltration `63.502721051072044 mm`;
- raw post-depression generation `16.059088185175263 mm`;
- raw residual `7.105427357601002e-15 mm`;
- reconstructed hourly closing residuals within `1e-12 mm` and recorded as
  exact zero in the package evidence;
- WAT5 SHA-256
  `71f943f9ff30f74846f74d521c66ecee8dce64f7ddcd5fe2c64e4d12008ed938`.

Enabled/disabled protected outputs were independently byte-identical:

- HBP `fd01aeadc9716923ff12e78e1c589ec4e15f8b855a395a1f390486296f81de3d`;
- PASS `e5b8d5ace2f82b97e4d15d1ab2e02c2e415fde3e6076f411389180ca9c064b7f`;
- WAT `707bdc15fc442a56fd5c47cfa8b531463911f5f3363f2a89d667181eebfea943`;
- loss JSON
  `92c88db6f19b49c062b3e74fb357926e9ac5ed4b15984025d528523c7ed45bf8`.

The p102 real run exits with
`WAT5-E-001 positive additional supply lacks 300-second timing` and leaves no
WAT5 target or temporary file. These are real downstream-consumer and
independent-operand checks, not producer self-consistency alone.

The user path is now explicit: add a fresh
`wat_subhourly = "output/H61.wat-subhourly.parquet"` target under `[outputs]`
and invoke the documented release `openwepp-cli-hill` command. The evidence
manifest binds that command shape to source `c9f28a7d...` and rebuilt release
binary SHA-256
`f264661135cde810ff4914df80f5aba1e176349af89537794f18187e49bbc85a`.

## Reviews And Critical Validation

All four final independent implementation reviews pass:

- Science Review A: `PASS`, recommends `GO` after exact-worktree authority
  correction.
- Science Review B: `PASS`, all findings closed, including the complete
  authority fingerprint.
- Rust correctness: `PASS`, no open correctness blocker.
- Rust QA: `PASS`, no open QA blocker.

`finding-disposition.md` accepts and closes every science, correctness, QA,
authority, publication-wording, usability, and exact-head validation finding.
None is deferred from a current gate.

The retained terminal candidate campaign started after the final executable
A0 repair. Its log records the exact external target and command, run ID
`b920db77-070f-4686-a7bf-2e2727094374`, 2,380/2,380 passed, 33 ordinary skips,
8,753.02 seconds, and exit 0. Post-A0 workspace doctests report zero failures
and exit 0. The 2,331 quick identities are a strict subset of the 2,380 full
identities with zero quick-only tests. The pre-repair 2,379-test receipt and
interrupted post-repair attempt are explicitly superseded/non-admitted.

## Fresh Lightweight Execution

Ran from `/home/workdir/openWEPP` on the final verification worktree:

```text
bash tools/release/check_science_contract_admission.sh \
  --base-ref a8a96498ee909c4305fbc0a4db562b72e45efd2b --worktree
PASS: 43 contracts, 17 science surfaces, authority SHA-256 6f95845b...

cargo nextest run --test advisory_linter_authority_contract \
  --test subhourly_generation_contract \
  --test auth11_required_suite_obligation_guards_contract
PASS: 12/12; run ID b7bf6dac-3e88-4d71-a915-682d47b724c4

markdown-doc lint --path <package> --format plain
PASS: 49 files, 0 errors, 0 warnings before this receipt rewrite

markdown-doc lint --path <SC-OUTPUT-WAT5-001> --format plain
PASS: 1 file, 0 errors, 0 warnings

python tools/check_sc_binding_exposure.py <SC-OUTPUT-WAT5-001>
PASS: one fully consolidated row

bash tools/release/check_sc_unit_compliance.sh --path <SC-OUTPUT-WAT5-001>
PASS

bash tools/release/check_authority_suite_antievasion.sh
PASS

cargo fmt --all -- --check
PASS

jq -e . <authority-policy and package JSON artifacts>
PASS

git diff --check
PASS
```

I also verified by direct JSON queries that the impact map has exactly 17
atomic WAT5 mappings and exactly four paths with the separate Plant/WAT5
authority pair.

## Exact-Diff Reconciliation

Independent extraction found 87 unique paths in `owned-file-manifest.md` and
87 current status paths after excluding the predecessor package and its two
backlog paths; both set differences are empty. The manifest classifies the
runtime/output surface, approved contract and six authority-policy paths, six
integration tests, 56 package/evidence paths, and shared catalog entry.

The only new public serialized surface is
`openwepp-hillslope-wat-subhourly-v1.0`. Green-Ampt equations and coefficients,
WB16/public peak, HBP, routing, and the production erosion selector are
unchanged. No untracked result-bearing artifact exists outside the declared
WAT5 or separately owned predecessor sets.

## Disposition

`PASS`. The terminal result is the optional `DIAGNOSTIC_ONLY` five-minute
water product for source-complete local-rain timing plus labeled hourly
saturation. Erosion remains `NO_ADOPTION`. Authority, implementation,
noninterference, source completeness, publication, user command, reviews,
post-A0 Critical validation, and exact 87-path ownership all close with no
current-scope blocker.
