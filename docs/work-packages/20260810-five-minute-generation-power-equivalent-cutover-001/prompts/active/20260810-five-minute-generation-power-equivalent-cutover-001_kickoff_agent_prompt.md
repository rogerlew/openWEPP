# Kickoff — Five-Minute Generation Power-Equivalent Cutover

Scope: local repository science-contract/kernel engineering work; flat-file
reads/edits only; no external connectivity is required except the package's
already-declared local evidence execution.

Execution mode: package-end-to-end.

Phase plan: execute all milestones in `package.md` sequentially through a
truthful disposition. Do not request incremental operator direction. The
adoption branch is binding: diagnostic water output may pass while production
erosion adoption is rejected.

Required reading:

- Core: `AGENTS.md`, `docs/codex_exec_plans.md`,
  `docs/work-packages/AGENTS.md`, `docs/work-packages/README.md`, and this
  package's `package.md`.
- Conditional: before contract/kernel edits, read the science-contract
  authoring procedure, profile, index, and applicable `SC-*` contracts; before
  validation/prompt work, read the named standards in
  `artifacts/required-reading-map.md`.
- On-demand: load only the named direct-runtime, output, ADR-0036, Topanga,
  and pinned-baseline sources for the mechanism being touched.

Required-reading budget: record the local byte total and threshold disposition
in `artifacts/required-reading-map.md` before production edits.

Files: only the declared write set in `package.md`, plus adjacent
same-process files admitted before first edit and recorded in the Decision Log.

Constraints: preserve contract-first sequencing (contracts, contract-derived
tests, pre-implementation red gate, then production edits); canonical `SC-*`
authority; pinned-baseline provenance where applicable; typed guards; no silent
defaults or canonicalize-and-proceed. Production code may not use surrogate,
provisional, proxy, empirical-stand-in, or heuristic process physics. The
existing hourly water, WB16/public peak, HBP, and routing authority remain
protected unless the package explicitly admits a change.

Real consumer proof: an `ADOPT` outcome must move the real eligible Wave-1
consumer to the admitted forcing and prove old, wrapper, adapter, skeleton, and
shadow paths do not carry the cutover claim. Record operand lineage,
anti-tautology fixtures, rejected formulas, independent reconstruction, and
real closure/magnitude evidence; self-consistency alone is insufficient.

Subagent requirement: REQUIRED for full Topanga execution, full-workspace
nextest, doctests, and other heavy batch/closure gates. This prompt explicitly
authorizes subagent spawning/delegation to bounded read-only hydrology/source
investigators, two science reviewers, `rust_code_reviewer`,
`rust_qa_reviewer`, two terminal verifiers, and
`comparator_suite_runner`. Outputs: compact findings, review/verification
artifacts, command logs, and immutable external cohort evidence. Write access:
investigators read-only; reviewers/verifiers only their named package
artifacts; comparator runner only package logs and external evidence; parent
executor owns declared source, contract, test, and package edits.

Autonomy: execute and document all package phases without additional direction
unless a declared hard blocker is proved. Update artifacts truthfully using
`Static:` and `Ran:` labels, reconcile the exact terminal diff, and do not
close with any required gate missing or any review finding undispositioned.
