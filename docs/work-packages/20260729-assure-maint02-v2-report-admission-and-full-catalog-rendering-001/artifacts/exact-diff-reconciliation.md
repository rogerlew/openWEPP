# Exact Diff Reconciliation

Status: complete / pass

Evidence class: Static + Ran

The terminal tracked and added paths match the authorized implementation scope.
One necessary path, `crates/openwepp-assurance/src/lib.rs`, was omitted from the
prospective path list and added during terminal reconciliation. That file only
re-exports the new typed `admit_report*` API and `V2ReceiptReportRoots`; it is
required public API wiring for the already authorized admission operation. The
late inventory correction is a process deviation, not a scope expansion. No
file was deleted.

Implementation:

- `crates/openwepp-assurance/**` adds typed admission, root-bound receipt
  support, retained-SVG sanitization/assembly, and CLI/API wiring.
- `tests/integration/assurance_*` adds admission, receipt, SVG, assembly,
  planning, and zero-public-boundary coverage.
- `tools/local_ci/run_assurance_amendment.py` accepts archived schema-v1 and
  current schema-v2 receipts.
- `Cargo.lock` and the assurance crate manifest upgrade `quick-xml` to the
  non-advisory `0.41.0` release.

Source, schema, and generated identity:

- V2 report and transaction schemas describe retained figures and symmetric
  v1/v2 receipt forms.
- The catalog contains exactly the two existing reports plus CAL-09.
- The identity lock and existing generated review locks were regenerated by
  the typed rebind transaction.
- CAL-09's initial review lock was generated by typed admission.
- Two canonical schema-v2 receipts record rebind
  (`94df9666...` to `bc236bfe...`) and admission
  (`bc236bfe...` to `30db4a7e...`).
- CAL-09 manuscript, supplement, and descriptor changes add/fix assembly
  directives and retained-figure declarations. They also route the usersum
  canopy narrative through the builder's native `usersum` link instead of
  treating that already staged document as a copied report research object.
  The target document and reader meaning are unchanged.
- CAL-09 review metadata was reset from the agent name `Codex` and an
  explanatory independence sentence to the schema-native pre-review state:
  empty `material_producer_ids` and `not_assessed`. No corresponding typed
  principal, accountable review event, or review authority existed, and the
  report remains `DRAFT` with `review.state: DRAFT` and
  `decision: not_started`. This removes unsupported review metadata; it does
  not erase or create scientific or human approval.
- Supplement wording changed from “top-quartile overlap was 100%” to
  “top-quartile sets overlapped completely.” The sets and scientific assertion
  are identical; the edit avoids presenting an unbound numeric literal as a
  result value.
- The Harvard `identity` to `absolute` transforms are mechanically neutral
  because both values are positive. No scientific result, unit, precision,
  claim, or conclusion changed.

Documentation:

- V2 maintenance, governance, and amendment contracts describe the implemented
  interfaces and security boundary.
- This package, the implementation roadmap, and work-package catalog record
  execution and closure.

Runtime-only outputs:

- `target/assurance-preview/**` is ignored, disposable derived output.
- The two unrelated `/tmp/openwepp-assure-terminal-*` trees are terminal
  reproducibility evidence and remain untracked.
- Superseded uncommitted development receipts were moved outside the repository
  and are not part of the final chain.

Protected-boundary checks against `HEAD` returned no diff for tracked
`usersum/**`, `assurance/generated/**`, legacy `assurance/catalog.yaml`,
kernel/runner crates, CAL work-package evidence, or authored groundwater and
snow/frozen-soil sources. Only their generated review locks changed, as required
by the typed root-bound rebind. No public, approval, release, export, snapshot,
vendor, or WEPPcloud path changed.

`git diff --check`, anchored `verify-generation`, three-report validation, and
the complete gate ledger pass. The exact diff is consistent with implementation
intent.
