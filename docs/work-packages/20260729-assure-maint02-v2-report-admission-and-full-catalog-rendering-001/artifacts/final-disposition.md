# Final Disposition

Status: complete / pass

Evidence class: Static + Ran

ASSURE-MAINT02 achieved its objective. The assurance tool now provides typed
atomic new-report admission, root-bound schema-v2 receipts with archived-v1
compatibility, secure deterministic retained-SVG assembly, and real
complete-catalog rendering.

CAL-09 is admitted at generated identity
`30db4a7e6a691601426428b7772e28143ff9fa1bf10dd9d1ae80062d7f0002a2`.
The anchored chain contains 22 verified transitions. The source catalog
contains exactly three current, non-fixture DRAFT reports and the public catalog
contains zero reports.

The disposable human-inspection preview is:

```text
target/assurance-preview/usersum/assurance/reports/
```

Two unrelated terminal builds and the stable preview each contain the same 91
paths and bytes. All three pass `check --all`; the real Markdown consumer
accepts every report and supplement. CAL-09 renders eight sanitized retained
SVGs and one generated figure with accessible labels.

Focused admission, receipt, SVG, assembly, and source tests pass. The Critical
workspace campaign passes 2,161 tests with 5 skipped. Formatting, strict
Clippy, dependency policy, documentation lint, protected-boundary, generation,
and exact-diff gates pass. Two independent implementation reviews and two fresh
terminal verifications pass with all findings dispositioned.

This disposition creates no human scientific approval, review event, release
transfer, snapshot authority, public catalog inclusion, or tracked
`usersum/**` publication. CAL-09 remains DRAFT.

Residual nonblocking maintenance debt:

- `v2.rs` and `v2/amendment.rs` are close to the mandatory 3,000-line
  decomposition threshold.
- Full-tree transaction and publication fixtures are deliberately thorough but
  slow.
- The package records one process deviation: `src/lib.rs` public re-export
  wiring was within authorized implementation scope but absent from the
  prospective path inventory and added during terminal reconciliation.

No retirement or science-calibration follow-on is required by this package.
