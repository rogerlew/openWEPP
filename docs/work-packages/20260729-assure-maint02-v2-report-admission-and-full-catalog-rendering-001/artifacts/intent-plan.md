# Intent And Gate Plan

Status: frozen before production edits

Evidence class: Static + Ran

Implementation base: `0f87d897745571d329dca423d0903c54742e853a`

The terminal diff is compared to that base. The pre-edit worktree was clean and
`main` was one local scaffold commit ahead of `origin/main`.

## Intent

1. Add `amend admit-report` with check/apply/generation-guard behavior.
2. Reuse the existing candidate-tree exchange transaction and generated
   identity model.
3. Add a typed `retained_svg` figure representation with a public-safe SVG
   research object and public-safe Markdown ancillary object.
4. Admit only `native-forest-canopy-phenology-evaluation`.
5. Render/check the three-report catalog into two temporary roots and
   `target/assurance-preview/`.
6. Preserve report science, existing report sources, and tracked public bytes.

## Classification And Gates

Classification: `Critical`. The diff changes an assurance trust-root
transaction, source schema, CLI, deterministic builder, and SVG security
boundary.

Selected direct gates:

- focused crate and assurance integration tests during edit loops;
- `cargo fmt --all -- --check`;
- strict workspace Clippy;
- full-workspace all-feature Nextest through `comparator_suite_runner`;
- `cargo deny check`;
- applicable assurance source-level guards discovered from the exact diff;
- full-catalog validate/plan/build/check and unrelated-root byte equality;
- CommonMark consumer parsing of all generated Markdown;
- package, roadmap, source-contract, specification, and README Markdown lint;
- `git diff --check`;
- two independent implementation reviews and two terminal verifications.

No kernel, comparator-physics, coverage/CRAP, release, or external-authority
suite gate is selected because the intended diff changes no process physics,
comparator, test inventory classification, release state, or authority cohort.
Reconcile this selection against the exact terminal diff.
