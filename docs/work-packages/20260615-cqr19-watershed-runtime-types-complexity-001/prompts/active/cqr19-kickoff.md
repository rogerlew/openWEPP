# CQR19 Kickoff

Execute
`docs/work-packages/20260615-cqr19-watershed-runtime-types-complexity-001/package.md`
end to end.

Protected boundaries:

- Preserve public enum surface, error codes, display strings, runtime seam
  behavior, parser compatibility, runtime symbols, units, and science-contract
  behavior.
- Perform behavior-preserving decomposition only for the scoped live metric
  target in `types.rs`.
- Record before/after LCOV and CRAP evidence, full gates, dual reviews, dual
  verification, disposition, handoff, commit, push, and tracker update.
