# EB-04X Kickoff

Execute the package end-to-end under `package.md`. Preserve all production,
contracts, tests, fixtures, observations, and predecessor artifacts read-only.
Freeze component gates before analysis; do not infer canopy interception from a
paired residual when no canopy-snow state exists.

Subagent authorization: this prompt explicitly authorizes subagent
spawning/delegation to two independent read-only science/governance reviewers
and two read-only terminal verifiers for their named package-local artifacts.
