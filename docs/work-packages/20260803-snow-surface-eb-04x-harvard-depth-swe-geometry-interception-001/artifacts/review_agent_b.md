# Independent Governance / QA Re-Review B

Verdict: **PASS**

Evidence mode: **Ran + Static**

Review subject: terminal-v2 corrections to the EB-04X package on source HEAD
`9fc9745baf9b21fead8b8bb47b8e36ee09b4be5f`. This re-review supersedes Review
B's initial `HOLD` and evaluates RB-01 through RB-04 only.

## Finding Closure

| Finding | Result | Evidence |
|---|---|---|
| RB-01 missing density trajectory/profile consumption | **CLOSED** | Terminal-v2 reads all 260 open/hardwood profile rows, retains 65 stratum/date groups, and publishes 80 date-keyed B/L observation/model depth-density pairs. S/LS are explicitly `UNAVAILABLE_INCOMPLETE_CELL`, not silently treated as trajectories. `density-trajectory.csv` carries date, water year, observed/modeled depth and density, profile mean, and profile-layer denominator; `profile-density.csv` preserves profile depth and source-record identity. |
| RB-02 aggregate-only precipitation/phase treatment | **CLOSED** | `daily-phase-identity.csv` publishes 16,466 B/LS common-day rows with day index, lane-specific phase selector, precipitation, ground accumulation, and equality flags. Results report explicit paired denominators and reconstruct 1,606 B plus 2 LS equal-precipitation/different-ground-accumulation days. Ground accumulation remains distinguished from intercepted snow. |
| RB-03 prospectivity, executable guards, and validation evidence | **CLOSED** | The separately authored quantitative protocol defines observation admission, component efficacy, open noninferiority, closure, interaction, and full-duration requirements. Its SHA-256 (`8bdaa35...d4d0`) and the exact terminal-v2 tool SHA-256 (`c082f657...f9b`) reproduce the terminal-v2 freeze. The tool requires the protocol before reading results, verifies every committed trace hash, rejects duplicate/nonmonotonic/non-prefix indices, fails on modeled geometry or mass closure, and refuses an existing output directory. Invalid HF237 geometry and incomplete S/LS durations conservatively short-circuit all screens to `NOT_EVALUABLE`; a screen is explicitly not promotion authority. `validation-receipt.md` now records exact commands, exits/results, terminal-v2 output SHA-256 identities, and CSV row counts; every recorded identity independently reproduces. Direct re-review validation receipts are below. |
| RB-04 rejected provider/mechanism aliases | **CLOSED** | `rejected-aliases.md` explicitly rejects the apparent divide-by-ten SWE repair, density-times-depth provider replacement, profile/daily-density substitution, ground accumulation as intercepted snow, ground sublimation as canopy sublimation, and asynchronous maxima as paired residuals. |

The terminal-v2 correction also closes Review A's asynchronous-peak and pairing
guard concerns: `paired-state-extrema.csv` contains 24 true same-day extrema,
and all eight traces prove unique, monotonic, zero-based contiguous day-index
prefixes. B/L are full-duration pairs; S/LS remain explicitly incomplete.

## Direct Validation Receipt

Ran during this re-review:

| Command / check | Result |
|---|---|
| Python `ast.parse` over both package analyzers | **PASS**; 2/2 |
| `json.loads` over every package artifact JSON | **PASS**; 5/5 |
| Independent `csv.DictReader` inventory | **PASS**; trajectory 80, daily phase 16,466, extrema 24, profiles 260 |
| Independent result reconstruction | **PASS**; trajectory-pair sum 80 and all three screens `NOT_EVALUABLE` |
| `markdown-doc lint --path <package>` | **PASS**; 18 files, 0 errors, 0 warnings |
| `git diff --check` | **PASS** |
| Second invocation of `analyze_harvard_pair_v2.py` | **PASS**; rejected existing terminal-v2 output with exit 1 before writes |
| Protected diff over production crates, tests, contracts, predecessor package, and target | **PASS**; empty |

The package-local `validation-receipt.md` additionally records the accepted
terminal-v2 execution (`exit 0`), required overwrite rejection, JSON tooling,
21-file terminal documentation lint, independent reconstruction, protected-diff
and bytecode checks, plus stable identities for all six result-bearing
terminal-v2 outputs. Review B recomputed those SHA-256 values and found no
mismatch.

No generated bytecode remains. The visible write set is limited to this package
tree plus the authorized root roadmap, snow campaign roadmap, and package
catalog. No production, test, contract, observation, dependency, security, or
public-schema path changed.

## Lifecycle And Scientific Disposition

The package, root roadmap, campaign roadmap, and catalog consistently report
terminal-v2 technical pass with re-review pending; the active kickoff prompt is
correctly retained until review/verification closure. They make no promotion,
causal interception, calibration, or implementation claim.

The evidence supports the bounded conclusions: modeled SWE-depth-density
geometry closes; all 746 complete HF237 open/hardwood rows contradict the
supplied SWE identity; B/L pair fully while S/LS terminate asymmetrically; and
the retained traces contain no canopy snow load, intercepted-snow, canopy
sublimation, unloading, or drip operand. All component screens are therefore
truthfully `NOT_EVALUABLE`, and no component is promoted.

Review B finds no remaining closure-blocking issue in the terminal-v2 subject.
Final completion remains correctly reserved for dual re-review, dual terminal
verification, finding disposition, prompt archival, and exact terminal-diff
reconciliation by the primary agent.
