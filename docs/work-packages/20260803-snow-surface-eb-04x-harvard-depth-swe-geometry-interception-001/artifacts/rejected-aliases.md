# Rejected Aliases

Status: frozen before terminal-v2 execution

| Candidate | Disposition | Reason |
|---|---|---|
| Divide supplied HF237 SWE by ten | `REJECTED / NOT PROVIDER-AUTHORIZED` | The apparent scale factor is diagnostic evidence, not unit authority. |
| Reconstruct SWE as supplied density times depth | `REJECTED AS PROVIDER REPAIR` | Valid as an independent contradiction check only; it cannot replace the published SWE field. |
| Substitute profile density for daily supplied density | `REJECTED` | Profile and daily/interpolated products have distinct observation operators. |
| Treat ground `accumulation_m` as intercepted snow | `REJECTED` | It is ground-pack input after phase handling; no canopy load operand exists. |
| Treat ground `sublimation_m` as canopy sublimation | `REJECTED` | The trace exposes only ground-pack sublimation. |
| Treat difference of asynchronous lane maxima as a paired residual | `REJECTED` | Paired claims require same `day_index`; terminal-v2 reports daily extrema instead. |
