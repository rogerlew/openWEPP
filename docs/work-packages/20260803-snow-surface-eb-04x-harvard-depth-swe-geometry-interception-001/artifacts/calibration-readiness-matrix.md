# Calibration Readiness Matrix

| Field | Status |
|---|---|
| `science_implementation_status` | `NOT_IMPLEMENTED` for canopy snow interception; existing ground-snow processes unchanged |
| `calibration_evidence_status` | `NOT_CALIBRATION_READY` |
| `identifiability_status` | `NONIDENTIFIABLE` |

This package has diagnostic intent only. HF237 SWE identity is invalid, the
lanes are not an identical-forcing canopy manipulation, S/LS are incomplete,
and required canopy-state operands are absent. No empirical calibration or
synthetic-recovery claim applies.
