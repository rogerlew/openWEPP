# Terminal-V2 Scientific Synthesis

Evidence mode: **Ran + Inference**

The terminal-v2 correction consumes 260 Harvard HF237 profile-density rows in
65 stratum/date groups and publishes 80 retained observation/model trajectory
pairs for the completed B/L cells. Mean absolute depth error is `0.1144 m` in
open B/L and `0.1611 m` in hardwood B/L; mean absolute density error is
`126.28 kg m^-3` open and `116.86 kg m^-3` hardwood. L is exactly unchanged
from B for these paired operators, so retained longwave does not improve the
Harvard density/depth trajectory.

Day-index guards prove unique, monotonic, zero-based contiguous traces. B/L pair
all 16,437 days. S pairs 75 days and LS 29 days; they remain incomplete cells.
The daily B audit finds 16,074 equal-precipitation days, including 1,606 with
different ground accumulation. Both lanes retain the same phase-model identity,
but different forcing/partition inputs and no canopy snow-load operand mean the
difference cannot be relabeled as interception. LS has 29 equal-precipitation
common days and two different-accumulation days.

True same-day hardwood-minus-open SWE, depth, and density minima/maxima are
published with their day indices. The rejected first generation's subtraction
of asynchronous lane maxima carries no scientific claim.

Model geometry and daily mass/energy identities close. HF237 observed geometry
remains invalid for all 746 complete nonzero rows. The prospectively anchored
quantitative protocol therefore short-circuits longwave, sublimation, and
combined screens to `NOT_EVALUABLE`. Even without that contradiction, S/LS
duration and absent canopy storage/interception/sublimation/unloading/drip
operands prevent a canopy-mechanism claim. No component or default promotion is
admitted.
