# Independent Science Re-Review Agent A

Disposition: **PASS**

Evidence mode: **Static + Ran (cheap independent reconstruction)**

Review scope: terminal-v2 corrections for `RA-001` through `RA-005` only.

## Finding closure

| Finding | Result | Evidence |
|---|---|---|
| `RA-001` missing paired trajectories and phase/input identity | **CLOSED** | Terminal-v2 publishes 260 profile-density rows, 80 available B/L observation/model depth-density pairs, and 16,466 daily B/LS common-day phase/input rows. S/LS observation trajectories remain explicitly `UNAVAILABLE_INCOMPLETE_CELL`, rather than being inferred. Independent CSV inventory reconstruction reproduced all row counts and B/LS precipitation, accumulation, and phase equality totals in `results.json`. |
| `RA-002` asynchronous peak subtraction | **CLOSED** | `paired-state-extrema.csv` contains 24 true same-day hardwood-minus-open minima/maxima: four models, three state metrics, and two operators, each carrying its `day_index`. The asynchronous v1 peak aliases are explicitly rejected and carry no terminal claim. |
| `RA-003` pairing guards | **CLOSED** | The terminal-v2 analyzer rejects nonmonotonic, duplicate, non-zero-based, or noncontiguous day indices before analysis. With every trace proven a contiguous zero-based prefix, `min(open_len, hardwood_len)` is a proven common prefix. Results truthfully distinguish full B/L pairing from incomplete S/LS pairing. |
| `RA-004` non-operational promotion rules and weak failure behavior | **CLOSED** | `promotion-protocol.json` supplies numeric geometry admission, full-duration requirements, mean-absolute-depth efficacy/noninferiority predicates, latent/mass tolerances, and a combined noncompensation predicate. It is separately hash-bound by terminal-v2 (`8bdaa35...`) and identified as frozen before results. The analyzer fails closed on absent prior protocol, existing output, retained-trace hash drift, day-index violations, geometry residual above `1e-6 kg m^-3`, and mass residual above `1e-12 m`. Invalid HF237 geometry prospectively short-circuits all screens to `NOT_EVALUABLE`; this is a diagnostic admission result, not promotion authority. |
| `RA-005` generated bytecode | **CLOSED** | No `__pycache__` directory or `.pyc` file remains anywhere in the package tree. |

## Scientific and governance conclusion

- The corrected trajectory products preserve scale boundaries: daily/interpolated bulk density remains distinct from the profile-density product, and incomplete candidate cells are not fabricated.
- Daily phase/input evidence strengthens the causal restraint. B has 16,074 equal-precipitation days, including 1,606 with different ground accumulation; LS has 29 equal-precipitation common days, including two with different accumulation. Phase-model equality does not establish identical forcing or a canopy-storage mechanism.
- The published same-day extrema resolve the paired-operator defect without reviving the rejected asynchronous maxima.
- HF237's `746/746` supplied SWE/depth/density contradiction remains unrepaired, and absent canopy load, intercepted snow, canopy sublimation, unloading, and drip operands remain absent. The `NOT_EVALUABLE` screen results and no-promotion conclusion are therefore truthful.
- Terminal-v2 does not cross the canopy-snow backlog or `SC-SNOWFREEZE-001` authority boundary and makes no production, selector, coefficient, or default change.

No remaining closure-blocking finding exists within the assigned terminal-v2
re-review scope. **PASS**.
