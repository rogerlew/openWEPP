# Terminal Verification Agent A

Disposition: **PASS**

Evidence mode: **Ran + Static**

Subject: EB-04X terminal-v2 evidence and governance state on frozen source HEAD
`9fc9745baf9b21fead8b8bb47b8e36ee09b4be5f`.

## Acceptance Criteria

| Criterion | Result | Independent evidence |
|---|---|---|
| 1. Frozen input identity | **PASS** | Recomputed SHA-256 for all 15 terminal-v2 freeze entries, including the separately frozen promotion protocol, exact v2 analyzer, observations, committed factorial result, and eight retained traces. Every identity matches. `model_execution_authorized` remains false. |
| 2. Observed geometry | **PASS** | Independently reconstructed the result inventory: all 336 complete nonzero open rows and all 410 complete nonzero hardwood rows are classified nonclosing, for `746/746` contradictions and zero admitted rows. No inferred divide-by-ten or density-times-depth provider repair is applied. |
| 3. Modeled geometry | **PASS** | Parsed all eight retained JSONL traces and independently checked every positive-depth row using `1000 * runtime_swe_after_m / runtime_depth_after_m`. Every row closes within `1e-6 kg m^-3`; all trace day indices are unique, monotonic, zero-based, and contiguous. |
| 4. Factorial pairing and trace reconstruction | **PASS** | Independently reproduced B/L full pairing at 16,437 days each, S common-prefix pairing at 75 days (`2,643` open / `75` hardwood), and LS common-prefix pairing at 29 days (`29` open / `33` hardwood). No production run was invoked. |
| 5. Mechanism and causal boundary | **PASS** | Static inspection confirms that retained evidence exposes ground accumulation, ground pack state, ground sublimation, and energy, while canopy load, intercepted snowfall, canopy sublimation, unloading, and drip remain `NOT_OBSERVED`. The synthesis and rejected-alias table do not relabel ground operands or paired residuals as canopy mechanisms. |
| 6. Prospective component screens | **PASS** | The protocol hash `8bdaa35eb93a295eafa6b0fb359090acf31999d68c08c0e82b585f960553d4d0` matches the terminal freeze and declares itself frozen before terminal-v2 results. Invalid observed geometry short-circuits longwave, sublimation, and combined interaction to `NOT_EVALUABLE`; no screen is reported as promotion authority. |
| 7. Review, verification, and lifecycle gates | **PASS for Verification A** | Both fresh reviews are PASS and all first-review findings are recorded as accepted/corrected. This verifier found no closure blocker. The active kickoff prompt and `verification pending` roadmap/catalog state are correct until Verification B and final reconciliation finish; the primary agent must archive/update them only after that terminal evidence exists. |

## Independent Terminal Checks

Ran from `/home/workdir/openWEPP`:

| Check | Result |
|---|---|
| Frozen SHA-256 reconstruction | **PASS**; 15/15 inputs |
| Recorded terminal-v2 output identities | **PASS**; 6/6 outputs |
| JSON parsing and Python AST parsing | **PASS**; both terminal JSON files and 2/2 analyzers |
| CSV inventory reconstruction | **PASS**; trajectories 80, profiles 260 in 65 stratum/date groups, daily phase 16,466, extrema 24 |
| Daily B/LS phase/input reconstruction | **PASS**; B `16,074` equal-precipitation days and `1,606` equal-precipitation/different-accumulation days; LS `29` and `2`, respectively; phase, accumulation, and paired denominators also match |
| Same-day extrema reconstruction | **PASS**; all 24 model/metric/operator values and day indices reproduce directly from paired trace rows |
| Modeled trace geometry and index guards | **PASS**; all 8 traces |
| Promotion state and absent-operands flags | **PASS**; three `NOT_EVALUABLE`, canopy operands false, modeled geometry true, observed geometry false |
| `markdown-doc lint` over package and three lifecycle documents | **PASS**; 25 files, 0 errors, 0 warnings |
| `git diff --check` | **PASS** |
| Protected tracked diff (`crates`, `tests`, `docs/specifications`, predecessor package) | **PASS**; empty |
| Generated bytecode check | **PASS**; no `__pycache__` or `.pyc` |

The six terminal-v2 output hashes reproduce the validation receipt exactly:

- freeze `ea4af0c2...b71ba`
- results `aab69396...f4b1`
- trajectory `0247e704...9fca`
- profiles `6356db7f...5cb7a`
- daily phase `913c0e82...9b14b`
- extrema `36e065a5...8abac`

## Governance Conclusion

The visible tracked edits outside the new package tree are limited to the three
authorized lifecycle documents. No Rust, test, contract, fixture, observation,
predecessor, dependency, lockfile, authentication, unsafe, or public-schema
surface changed. The two package-local Python evidence tools are 280 and 335
lines; Rust line-count governance is not applicable. Their subprocess use is
limited to read-only `git rev-parse HEAD`, not model execution or an external
write.

The roadmap, campaign roadmap, catalog, package status, and active prompt are
mutually consistent for the current pre-closure stage. EB-04X supports only the
bounded conclusion that modeled geometry closes while the HF237 SWE identity,
incomplete S/LS durations, non-identical lane inputs, and absent canopy
operands prevent a causal interception or promotion claim. Verification Agent
A finds no remaining closure-blocking defect. **PASS**.
