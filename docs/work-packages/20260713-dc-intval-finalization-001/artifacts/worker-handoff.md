# Worker Handoff

First actionable item: close defect `INTVAL-AUTH-BIND-001` as the first batch
inside `INTVAL-FINAL-001`, then continue the same package loop until the exact
release and restarted integrated-validation campaign pass end to end.
