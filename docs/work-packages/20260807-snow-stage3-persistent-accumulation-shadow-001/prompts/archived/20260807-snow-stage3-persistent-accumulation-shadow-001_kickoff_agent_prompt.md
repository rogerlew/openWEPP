# Kickoff: Snow Stage 3 Persistent Accumulation Shadow

Execute this package end to end in `/home/workdir/openWEPP` under the root,
work-package, contract, crate, and test instructions.

Autonomy: complete every phase through disposition without asking for a next
step unless a proven boundary outside the declared write set blocks execution.
Bounded wind/exposure/geometry/magnitude uncertainty limits interpretation; it
is not by itself an implementation blocker for this evaluation-only operator.

Required reading:

- Core: `AGENTS.md`, `docs/codex_exec_plans.md`,
  `docs/work-packages/AGENTS.md`, `docs/work-packages/README.md`, and this
  package's `package.md`.
- Conditional: `docs/specifications/science-contract-authoring-procedure.md`,
  `docs/specifications/science-contracts/kernel-process-contract-profile.md`,
  `docs/specifications/science-contracts/index.md`, `crates/AGENTS.md`, and
  `tests/AGENTS.md`.
- On-demand: `SC-SNOWFREEZE-001`, `SC-SNOWENERGY-001`, ADR-0042, and named
  predecessor package evidence.

Contract-first: contracts, contract-derived tests, recorded pre-implementation
gate, then production code. No surrogate physics: use existing contract-backed
Stage 3 equations only. Conservation/output acceptance: require operand
lineage, anti-tautology tests, rejected aliases, independent reconstruction,
real closure evidence, and schema alignment. Real consumer proof: the internal
trace consumer must read the persistent fields, and production consumers must
be proven unable to read them.

Subagent authorization: this package explicitly authorizes spawning/delegating
to two independent read-only science/Rust reviewers, one independent read-only
consumer/reconstruction reviewer, the `comparator_suite_runner` for heavy
frost/full-workspace and retained-fixture commands, and two independent
read-only terminal verifiers. Expected outputs are compact findings, metrics,
commands, and artifact/log paths; only the comparator may write ignored target
outputs.

Required-reading byte budget and disposition are recorded in
`artifacts/required-reading-map.md`.

