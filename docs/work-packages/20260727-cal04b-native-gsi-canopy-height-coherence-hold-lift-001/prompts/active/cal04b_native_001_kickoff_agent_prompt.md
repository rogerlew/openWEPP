# Kickoff: Close CAL04B-NATIVE-001

Scope: local repository contract-first production correction and proof only;
no deployment, external contact, calibration population, or Harvard access.

Close defect `CAL04B-NATIVE-001` end-to-end under the Correction Authority
Envelope in the package. Do not relay an intermediate diagnostic step into
another package while contract amendment, tests, production correction, or
validation remains possible inside the envelope.

Seven-gate conversion: validate all seven conversion gates in `package.md`.
Because the valid input, expected coherent-state behavior, localized source,
executable reproducer, correction authority, validation authority, and
protected boundaries are present, convert to the contract-first fix. If
authority adjudication disproves a gate, record the exact HOLD audit.

Autonomy: execute the complete package through truthful disposition without
asking for next steps unless a declared authority or evidence boundary is
proven.

No surrogate physics: production code must use actual canonical-contract or
pinned-baseline-authoritative canopy-height physics. Surrogate, provisional,
proxy, empirical, clamped, and heuristic stand-ins are forbidden.

Real consumer proof: prove snow, ET, WB15 interception, erosion, residue/litter,
frost, and Lane D active routing read the corrected same-day post-phenology
state. Prove wrappers, static/stale state, skeletons, shadow paths, and
compatibility paths do not carry the claim. Replay the entire frozen CAL-04B
native-proof case plan, including all six operand perturbations.

HOLD legitimacy audit: if closure is impossible, name the exact boundary, cite
the evidence proving it, list the in-envelope correction route considered, and
explain why it cannot close the defect in this package. Implementation effort
or partial compatibility is not a valid hold.

Required reading:

- Core: `/workdir/openWEPP/AGENTS.md`,
  `/workdir/openWEPP/docs/codex_exec_plans.md`,
  `/workdir/openWEPP/docs/work-packages/AGENTS.md`,
  `/workdir/openWEPP/docs/work-packages/README.md`, and this package's
  `package.md`.
- Conditional:
  `/workdir/openWEPP/docs/defect_closure_execplans.md`,
  `docs/specifications/science-contract-authoring-procedure.md`,
  `docs/specifications/science-contracts/kernel-process-contract-profile.md`,
  `docs/specifications/science-contracts/index.md`,
  `docs/specifications/science-contracts/AGENTS.md`,
  `docs/standards/kernel-work-package-preparation.md`, and
  `docs/standards/testing-and-gate-strategy.md`, plus `crates/AGENTS.md` before
  runner/orchestrator edits.
- On-demand: `SC-PLANT-001`, applicable downstream consumer contracts, the
  retained CAL-04B incident/audit, the exact touched Rust modules and their
  nearest instructions, and pinned legacy canopy-height provenance.

Required-reading budget: `588,326` local bytes / `WARN`, as recorded in
`artifacts/required-reading-map.md`. Read on-demand material only when its
trigger is reached.

Contract-first sequence: contract authority, contract-derived tests,
pre-implementation contract gate, then production edits.

Gate non-deferral: a phase is complete only when every required phase gate has
direct current evidence. An attempted failure or terminally discovered
current-scope obligation cannot be deferred or relabeled to advance.

Numerics and errors: do not add silent defaults, unbounded clamps, or
canonicalize-and-proceed behavior. Domain-invalid state must retain typed
fail-closed handling unless a canonical contract explicitly authorizes a
bounded normalization with threshold, units, provenance, tests, and evidence.

Subagent authorization: this prompt explicitly authorizes two independent
read-only contract/implementation reviewers, `comparator_suite_runner` for
selected heavy/full-workspace and real-consumer gates, and two independent
read-only terminal verifiers. Expected outputs are compact findings, exact test
counts, receipt identities, and durable artifact paths. The primary executor
owns production edits.

Do not weaken the positive-LAI/positive-height guard, change GSI selectors or
calibration domains, force fixture height, filter failed candidates, or access
Harvard.

Run cargo-deny and the critical campaign-strength full-workspace correctness
gate selected by the terminal plan.
