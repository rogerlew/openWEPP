# Kickoff Prompt

Execute `docs/work-packages/20260624-r7h-closure-activation-gates-001/package.md`.

Do not activate direct production as the default unless every closure gate in
the package passes on current code. If any current gate fails, keep default
activation disabled, record the named `HOLD-R7H-*` blocker, and name the first
implementation correction required to clear it.

