# Worker Handoff

Status: `QUEUED / INTAKE NEXT`

Start from current `main` after lifecycle commit `71e6a2651`. Consume
`83cf6eb8e` only as historical Child 1 evidence. First execute the required
reading map, actual-consumer trace, owner/operand map, and pre-implementation
contract gate. Do not edit production Rust until the exact write set is frozen.

The package is authorized to delegate two implementation/code-review roles and
two verification roles within the package scope. Heavy critical workspace or
comparator work belongs to `comparator_suite_runner` when available; record a
tool-policy block before any local substitution.
