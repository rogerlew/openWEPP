# Implementation Intent

Status: `QUEUED / NOT FROZEN`

No production source or test implementation has started. Intake must identify
the actual hillslope scheduler consumer, complete owner set, restart seam, and
the smallest owned write set before any kernel edit. The intended candidate is
default-off and must preserve the current CoE production path byte-for-byte
where it is not explicitly replaced by the candidate seam.

The implementation must consume the five released Child 2C contracts directly,
use typed receipts and fail-closed errors, and stage every owner before one
complete-owner commit. Any missing or contradictory authority is a HOLD and
requires a contract amendment; it cannot be filled with a surrogate or hidden
normalization.
