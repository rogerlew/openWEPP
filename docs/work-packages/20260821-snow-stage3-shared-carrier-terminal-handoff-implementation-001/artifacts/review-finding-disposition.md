# Review Finding Disposition

Status: `QUEUED / NO REVIEWS RUN`

The package requires two independent reviews. Every finding must be explicitly
marked `accepted`, `rejected`, `deferred`, or `follow-up`, with evidence and an
owner. A deferred or follow-up finding cannot be silently treated as closure.
