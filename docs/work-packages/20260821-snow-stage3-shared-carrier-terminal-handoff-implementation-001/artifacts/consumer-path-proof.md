# Actual Consumer Path Proof

Status: `NOT RUN / INTAKE REQUIRED`

The closure claim is not admitted by this scaffold. Intake must trace the real
hillslope scheduler from forcing ingress through the snow-covered carrier,
terminal event, post-event real-owner stack, publication, and persisted
restart. The proof must name concrete files/functions and show that the
default-off candidate is invoked by the actual scheduler rather than a test-
only reference, skeleton, wrapper, adapter, or shadow path.

Required negative proof: the current production selector/default and CoE owner
remain unchanged, and no candidate output is published when the candidate is
disabled or a parent transaction fails.
