# Execute Advisory Linter Thin Slice

Execute roadmap Order 3 end-to-end under
`20260727-gate-planner-advisory-linter-thin-slice-001/package.md`.

Deliver only the frozen read-only, non-authoritative advisory linter and its
focused tests/documentation. Do not import legacy planner control modules,
execute suggested commands, add CI, create lifecycle state, scaffold Order 4,
or run CAL/Harvard/model work.

Subagent authorization: this prompt explicitly authorizes subagent
spawning/delegation to two independent read-only reviewers and two independent
read-only verifiers for the scopes and outputs in `package.md`.
