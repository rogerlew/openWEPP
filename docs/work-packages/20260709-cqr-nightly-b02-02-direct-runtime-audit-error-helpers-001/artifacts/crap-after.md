# CRAP After

Ran: delegated final command, exit `0`.

```text
cargo crap --lcov /tmp/openwepp-cqr-b02-t02-final.lcov --min 0 \
  --format json --output /tmp/openwepp-cqr-b02-t02-final.json
```

The target has `41` CRAP rows, `0` above `30`, and maximum CRAP `21.0` for
`DirectRuntimeError::display_parts` at line `293` (CC `21`, 100% coverage).
All target function coverage observations are at least 75%; no exclusion is
needed. The JSON is `1,138,222` bytes, SHA-256
`b814bfd65efca16977263e84ef2193f5a01d36df1af3a378f86d50ab9435be41`.
