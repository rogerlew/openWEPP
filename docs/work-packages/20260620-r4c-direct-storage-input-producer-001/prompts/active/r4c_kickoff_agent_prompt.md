# R4C Kickoff Agent Prompt

Execute R4C from `package.md`.

Required boundaries:

- complete pre-implementation contract and lineage checks before Rust edits;
- keep direct runtime default-disabled and shadow-only;
- do not edit scheduler, publication, output schemas, or compatibility runtime;
- run and record focused tests, full Rust gates, no-compatibility proof,
  default-disabled H2637 gate, markdown lint, and `git diff --check`;
- complete review, verification, line-count governance, handoff, and final
  disposition before closure.
