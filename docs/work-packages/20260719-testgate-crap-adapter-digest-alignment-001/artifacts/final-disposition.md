# Final Disposition

Evidence class: `Static` and prior `Ran`

Disposition: `EXECUTED / FAIL-GLOBAL-OUTPUT-RELOCATION / REVIEWED`.

The adapter correction is exact and focused evidence passes, but the critical
receipt remains failed because global CRAP rejects its own absolute default
before acquisition. Eleven passing nodes—including full Nextest 2,165/2,165—are
preserved without rerun or relabeling. No GitHub workflow, forest1 runner, or
production action occurred.

Both independent reviewers accept this truthful failed disposition. Exactly one
READY package owns the remaining defect:
`20260719-testgate-global-crap-output-relocation-001`. Its implementation and a
new current-head critical receipt are future work, not evidence claimed here.
