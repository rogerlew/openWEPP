# Protected Output Evidence

Status: `QUEUED`

W4 execution must record protected output identity or contract-governed deltas.

Required surfaces:

- committed focused fixtures used for W4 gates;
- `--jobs 1` and selected `--jobs N` where relevant to protect W3 behavior;
- routed-stage output files named by the watershed runfile output contract;
- identity evidence by decoded row content and order, or explicit
  contract-governed delta disposition.

Conservation-sensitive outputs also require independent reconstruction and
closure or magnitude audit evidence.
