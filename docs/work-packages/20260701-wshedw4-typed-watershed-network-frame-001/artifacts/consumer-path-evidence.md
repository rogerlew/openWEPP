# Consumer-Path Evidence

Status: `QUEUED`

W4 execution must prove the real public runner consumes typed routing and
publication frames.

Required proof fields:

- Producer source:
- Typed network frame object:
- Typed publication frame object:
- Runner handoff:
- Downstream routing consumer:
- Downstream publication consumer:
- Output/API surface:
- Positive evidence:
- Negative proof that production routing/publication does not use
  `WatershedWritebackSurface`, `BoundarySymbol`, or `BoundaryValue`:
- What still reads the old path:

Producer-only, adapter-only, shadow-only, source-counter-only, or test-only
evidence cannot close the W4 consumer-path gate.
