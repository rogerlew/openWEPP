# Execute ASSURE-04D

Scope: local openWEPP repository assurance engineering; flat-file reads, edits,
disposable fixture publication, and tests only; no network or external-system
writes.

Execution mode: package-end-to-end. Execute every phase in `package.md` through
final disposition unless a proven hard blocker prevents a current-scope gate.

Required reading — Core: `AGENTS.md`, `docs/work-packages/AGENTS.md`,
`docs/codex_exec_plans.md`, ADR-0038, the v2 architecture/lifecycle/source-build
contracts, report and usersum standards, prospective ASSURE-04 roadmap, 04C
handoff, this package, and `artifacts/required-reading-map.md`. Conditional:
`crates/AGENTS.md`, `tests/AGENTS.md`, `docs/standards/AGENTS.md`, and prompt
wording before matching edits. On-demand: current assurance source/code/tests
for touched mechanisms. Required-reading budget: 148,677 local bytes, `OK`;
map: `artifacts/required-reading-map.md`.

Files: only the declared write set. Tracked `usersum`, protected zero-report
catalog/template/export, manuscript/result science bytes, science contracts,
kernels, real snapshots/releases, and vendor paths are read-only.

Task: implement deterministic review-subject roots, exact-root review locks,
checked-staging approved-only promotion, reader-facing public catalog,
immutable release snapshots, and test-only snapshot rejection. Use only an
explicit synthetic approved fixture in disposable external roots for positive
publication evidence; keep the canonical groundwater report `DRAFT`.

Constraints: the builder verifies but never grants scientific authority; named
human roles remain data supplied by accountable humans; typed fail-closed
errors; descriptor-confined operations; no timestamp, mtime, shell, network,
agent, hostname, environment interpolation, absolute-path leakage, tracked-
public write, export, vendoring, or application verdict.

Subagent requirement: REQUIRED. This prompt explicitly authorizes subagent
spawning/delegation to one heavy-gate runner plus two independent read-only
reviewer/verifier agents for Phases 4 through 6. The heavy runner may write only
compact package evidence and must run the full workspace, deny, and fresh CRAP
closure. Reviewers/verifiers return compact independent findings to the parent
and have no write authority. Coding agents cannot populate approval records.

Autonomy: execute all phases and update artifacts without requesting next steps
unless hard-blocked. Every current gate needs direct evidence; no ASSURE-04D
gate may be deferred to ASSURE-05 and called complete.

Outputs: publication contract, schemas/source vocabulary, review-root and
publication API/CLI, focused tests, retained test-only public/snapshot consumer,
protected/confinement proof, gate/CRAP/line-count records, dual reviews,
complete disposition, heavy evidence, dual terminal verification, final
disposition, and ASSURE-05 handoff.
