# ASSURE-04D CRAP Remediation Evidence

Status: focused estimate and independent full-workspace closure PASS

Evidence class: Ran

The second independent heavy sequence passed format, workspace strict Clippy,
full Nextest, and dependency policy, then correctly returned HOLD on seven
actionable CRAP rows in four touched assurance files. No new adjudication was
requested. The remediation decomposes each flagged decision path:

| Failed function | Remediation |
| --- | --- |
| `execute_publish` | Separate explicit option binding from the four-way trust-domain/selection dispatch. |
| `parse_options` | Split selection, build, publication, release, verification, and remaining-option parsing into bounded helpers. |
| `validate_report_structure` | Move repeated record-family validation into `validate_report_sections`. |
| `validate_review` | Move review state/finding validation into the lifecycle module and split disposition validation. |
| `open_ambient_platform` | Separate normalized-component classification from open-or-create behavior. |
| `install_receipt` | Separate existing-byte verification, preparation, and no-replace commit. |
| `verify_snapshot_content` | Separate snapshot identity/header, manifest traversal, file verification, and public-file checks. |

Three focused tests were added for complete CLI publication option binding,
ambient directory creation/path rejection, and every finding disposition. No
lint suppression, CRAP adjudication, ignored test, protected-byte change, or
semantic relaxation was introduced.

After the changes, `cargo fmt`, workspace/all-target strict Clippy, 17/17
assurance library tests, 69/69 focused integration contracts, and 1,961/1,961
workspace quick tests passed. The retained synthetic public/snapshot/receipt
tree regenerated byte-for-byte identically.

A fresh focused coverage acquisition combined the assurance library tests and
all five assurance integration suites, then ran `cargo crap` at threshold 30.
It reported no threshold violation:

| Touched production Rust file | Focused maximum CRAP | Rows greater than 30 |
| --- | ---: | ---: |
| `crates/openwepp-assurance/src/cli.rs` | 30.0000 | 0 |
| `crates/openwepp-assurance/src/lib.rs` | no measured function | 0 |
| `crates/openwepp-assurance/src/v2.rs` | 23.0000 | 0 |
| `crates/openwepp-assurance/src/v2/assembly.rs` | 25.0000 | 0 |
| `crates/openwepp-assurance/src/v2/confined.rs` | 30.0000 | 0 |
| `crates/openwepp-assurance/src/v2/lifecycle.rs` | 23.9579 | 0 |
| `crates/openwepp-assurance/src/v2/publication.rs` | 26.5737 | 0 |

The focused estimate was diagnostic only. The subsequent independent
five-gate restart passed with zero actionable full-workspace rows and every
touched-file maximum at or below 30. Exact canonical evidence is retained in
`heavy-gate-runner.md` and `validation-evidence/adjudicated-crap/`.
