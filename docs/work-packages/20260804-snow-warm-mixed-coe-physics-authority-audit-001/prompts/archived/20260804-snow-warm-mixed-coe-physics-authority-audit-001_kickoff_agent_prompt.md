# Kickoff Prompt

Scope: local repository science-authority audit; flat-file reads/edits only;
no external connectivity.

Execution mode: package-end-to-end (default).

Phase plan: execute all phases in `package.md` sequentially through
disposition.

Required reading (read before edits):

- Core: `/home/workdir/openWEPP/AGENTS.md`,
  `/home/workdir/openWEPP/docs/codex_exec_plans.md`,
  `/home/workdir/openWEPP/docs/work-packages/AGENTS.md`,
  `/home/workdir/openWEPP/docs/work-packages/README.md`,
  `/home/workdir/openWEPP/docs/standards/testing-and-gate-strategy.md`, and
  this package's `package.md`.
- Conditional: `docs/specifications/science-contracts/AGENTS.md` and
  `crates/AGENTS.md` before interpreting contract and Rust authority;
  `tests/AGENTS.md` only if execution discovers an authorized test edit (none
  is intended).
- On-demand: `SC-SNOWFREEZE-001.md`, `SC-SNOWENERGY-001.md`, Chapter 3,
  pinned `melt.for`, current CoE Rust producer/caller, Marks 1998/1999,
  Ohmura 2001, Walter 2005, and the accepted 21L package/tables.

Required-reading budget: `470453` local bytes, `WARN`; map:
`artifacts/required-reading-map.md`.

Files: write only the package directory, `docs/work-packages/README.md`,
`docs/ROADMAP.md`, `docs/planning/snow-surface-energy-balance-roadmap.md`, and
the declared target scratch namespace. Treat every production, contract, test,
fixture, observation, reference, and predecessor path as read-only.

Task: execute the read-only CoE physics-authority audit end-to-end. Bind exact
identities, map formula chronology and state assumptions, independently
reconstruct current empirical terms and frozen subcomponents from accepted 21L
tables, apply the prospective disposition matrix, and record the smallest
justified next action.

Constraints: canonical `SC-*` authority; pinned baseline provenance at
`dac3c950d8b16cc73774bf5ce2e7e11f80baac70`; no silent defaults; no tuning;
no result-aware thresholds; no observation, legacy, or comparator discrepancy
as standalone defect authority.

Contract-first sequence: contracts, contract-derived tests, pre-implementation
contract gate, then production edits. This package prohibits all four edit
stages except the read-only pre-implementation gate; any future correction must
use a separately authorized contract-first package.

No surrogate physics: production code must implement actual contract-backed or
baseline-authoritative physics; surrogate, provisional, proxy, and heuristic
stand-ins are forbidden. This package selects no production implementation.

Gate legitimacy: mark a phase complete only when every current-scope gate has
direct current evidence. Do not defer a failed current gate while claiming
completion.

Subagent requirement: REQUIRED dual independent read-only science review and
dual independent read-only terminal verification. This prompt explicitly
authorizes subagent spawning/delegation to two science reviewers and two
terminal verifiers for evidence attack, independent reconstruction, finding
disposition, and terminal gate verification; outputs are compact reports with
evidence paths and verdicts; write access is read-only. No heavy batch or
`comparator_suite_runner` run is selected.

Autonomy: execute package phases end-to-end and update required artifacts
without requesting additional user direction unless hard-blocked.

Outputs: update all package evidence, reviews, verifications, catalogs,
roadmaps, prompt archive, and final disposition for completed phases.
