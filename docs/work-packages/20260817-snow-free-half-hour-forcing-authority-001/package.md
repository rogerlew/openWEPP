# Admit Snow-Free Half-Hour Forcing Authority

Status: `complete / terminal PASS / downstream authority released`

Date: `2026-08-17`

Package ID: `20260817-snow-free-half-hour-forcing-authority-001`

Plan class: `Critical contract-first forcing authority`

## Objective

Admit `OPENWEPP_SNOW_FREE_HALF_HOUR_FORCING_V1` as the exact default-off
repository operator mapping existing daily climate, breakpoint storms,
SIMIMPL28 hourly mechanics, static configuration, and live owner boundaries to
48 consecutive 1,800-second Child-4 receipts. No V9 or LSE constitutive
equation, production selector, default, output, or cutover is changed.

## Implementation intent and write set

Intent: `science-authority admission`. Before production Rust, this package
owns a narrow `SC-SNOWFREEFORCING-001`, necessary provider cross-references in
existing contracts, contract-derived tests, independent reference calculation,
frozen schemas/vectors, source custody, and this package tree. Historical
V3--V9 model definitions are protected.

## Phases

1. Freeze intake, current hourly lineage, definitions, HOLD bytes, and exclusion
   surfaces.
2. Acquire and checksum primary authority; map exact equations and rights.
3. Author the contract, independent calculator, schemas, vectors, and
   contract-derived tests.
4. Pass pre-implementation authority gates and dual independent reviews.
5. Disposition findings, verify exact bytes twice, and release authority only
   if every current-scope gate passes.

## Exit criteria

All requested provider fields, equations, units, branches, typed guards,
temporal/energy/mass closure, multi-OFE restriction, live-owner exclusion, and
test-vector obligations are canonical and executable. Required source custody,
dual review, finding disposition, dual verification, line governance, gate
evidence, and final disposition must be direct PASS evidence. Otherwise this
package remains HOLD and no implementation package may begin production edits.

## Delegation

Subagent authorization: this package explicitly authorizes spawning/delegating
to independent climate/radiation and vegetation/LSE science reviewers and two
read-only terminal verifiers. Expected outputs are package-local review and
verification artifacts; production and contract edits remain primary-owned.

## Security and exclusions

No secrets, network service, unsafe code, activation, calibration, deployment,
publication, PR, remote branch, or push. Restricted source text stays outside
Git; only citations, checksums, equation-use records, and lawful excerpts are
committed.
