# Temporal Refinement Selection

Select a conservative parent-hour operator: each SIMIMPL28 hour is divided into
two exact 1,800-second half-open children. Intensive/mean quantities are held;
each child receives half the parent integrated energy. Breakpoint precipitation
uses exact overlap, and only parent-hour precipitation without finer support is
split equally. No new sub-hour meteorological variability is claimed.

