# Multi-OFE Disposition

V1 permits global atmospheric forcing only when every OFE-required atmospheric
operand is bit-identical. It must reject heterogeneous values with a typed
unsupported-domain error; averaging and first-OFE selection are forbidden.
Per-OFE/tile precipitation destinations and WB14 parameters remain distinct.

