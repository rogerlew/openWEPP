# Required-reading byte count

Status: `MEASURED BEFORE PRODUCTION EDITS`.

The count must be measured before production implementation edits, using byte
counts of the exact files actually read and the complete active directive.
Classify under prompt governance as `OK`, `WARN`, or
`REQUIRES-JUSTIFICATION`, with the threshold/rationale recorded here.

Ran: the complete active directive is `48,386` bytes and matches the source
directive exactly. The initial required-reading set is recorded in
`required-reading-map.md`; repository governance does not specify a numeric
byte ceiling, so this is `OK` with progressive-disclosure routing and no
unread dependency implied. The exact contract/source files read for the
implementation gate are appended as the source audit proceeds.
