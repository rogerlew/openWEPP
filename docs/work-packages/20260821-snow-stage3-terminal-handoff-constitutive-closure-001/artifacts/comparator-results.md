# Comparator results

Status: `NOT RUN / CURRENT PATH CANNOT QUALIFY`.

No comparator result is reported. The new attachment has no repository-backed
runner construction or snow-covered consumer, so a comparator run cannot
qualify the requested terminal handoff. Running the existing compatibility
comparator would exercise the historical path and would not prove this
package's endpoint claim.
