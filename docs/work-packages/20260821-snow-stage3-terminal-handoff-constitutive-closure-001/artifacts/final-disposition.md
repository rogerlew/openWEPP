# Final disposition

Status: `PENDING`.

The package may close `COMPLETE` only when every current-scope gate is PASS on
one exact terminal commit. Otherwise record `EXECUTED HOLD` and the exact
blocker/lift action.
