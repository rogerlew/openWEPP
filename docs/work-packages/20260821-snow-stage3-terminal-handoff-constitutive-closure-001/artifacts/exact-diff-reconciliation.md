# Exact diff reconciliation

Status: `PENDING FINAL LOCAL CHECKPOINT`.

The final reconciliation will record the terminal local commit, exact changed
path list, `git diff --check`, historical-package protection check, and clean
worktree result. No push, branch operation, reset, rebase, merge, or amend is
authorized.
