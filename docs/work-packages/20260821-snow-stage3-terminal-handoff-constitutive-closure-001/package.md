# Stage-3/V11 Terminal-Handoff Constitutive Closure

Status: `EXECUTING / historical HOLDs preserved / constitutive closure active / Child 3 blocked`

Date: `2026-08-21`

Package ID: `20260821-snow-stage3-terminal-handoff-constitutive-closure-001`

Plan class: `critical contract-first default-off kernel closure`

## Objective

Replace the rejected Stage-3 terminal-handoff scaffold with one complete,
default-off, typed Stage-3/V11 attachment. The attachment must own the real
Stage-3 persistent state, execute the released shared V11/snow carrier over
1,800-second coupled parents, localize the actual terminal enthalpy event,
transfer terminal liquid exactly once to the declared receiver topology,
execute the real V11/LSE/hydrology remainder, commit all typed owners
atomically, and round-trip the complete owner set through an additive restart.

This package consumes the released Child 2A--2C authority and preserves all
three historical Child-1/terminal-handoff packages byte-for-byte. It does not
activate production Stage 3, change selectors/defaults, retire CoE, change
production outputs, deploy, calibrate, claim efficacy/transferability, add
canopy-intercepted snow, or implement Richards.

## Required starting identity

- Repository: `/workdir/openWEPP`
- Branch: `main`
- Starting `HEAD` and `origin/main`:
  `85d88fb903b302a33d43304a5001911f13f7d8d5`
- No pull, reset, rebase, merge, amend, branch creation, or push is allowed.

## Historical protection

These packages are immutable historical evidence and must not be rewritten:

- `20260819-snow-stage3-terminal-meltout-lse-handoff-implementation-001`
- `20260821-snow-stage3-shared-carrier-terminal-handoff-implementation-001`
- `20260821-snow-stage3-terminal-handoff-live-owner-closure-001`

## Scope and protected boundary

Included scope is the complete default-off attachment, exact Stage-3 support
projection, live-owner shared carrier, V11 snow-covered executor, terminal
event/coalescing, typed terminal parcel and topology receiver, partial WB14
continuation, complete owner transaction, additive restart, positive and poison
scenarios, stale assertion/assurance maintenance, reviews, verification, and
exact-head closure evidence.

Protected boundaries are the released SC-COUPLEDTIME, SC-LANDSURFACEENERGY,
SC-SNOWENERGY, SC-SNOWFREEZE, SC-SURFACELIQUID, SC-VEGETATION,
SC-VEGETATIONTRANSACTION, SC-ROOTZONEHYDRAULICS, and SC-SNOWFREEFORCING
contracts; existing V10 Restart V1, coupled-time Restart V2, V11 Restart V3,
DirectHydrologyRestartV1, and other released nested restart bytes; ordinary
production frame/publication bytes; selectors/defaults; CoE ownership; and
the declared exclusions above.

## Intended write set

- this package tree;
- `docs/ROADMAP.md`, `docs/work-packages/README.md`, and the active campaign
  package only for the successor pointer and truthful lifecycle;
- canonical science contracts only if the exact intake proves a missing
  cadence, owner-source, topology, or restart boundary;
- affected orchestrator direct-runtime, Stage-3 solver, V11 consumer, runner,
  persisted-restart, and package-owned test paths discovered during intake;
- assurance identity/report artifacts only through the admitted typed workflow.

The exact source/test write set is recorded in
`artifacts/owned-file-manifest.md` before implementation. Any expansion is
blocked until the package records the authority and gate reason.

## Execution intent

Intent is `science implementation + complete restart + independent physical
scenario qualification`; this package does not claim empirical calibration,
independent observational validation, seasonal efficacy, or transferability.

## Phase plan

1. Scaffold, exact intake, required-reading measurement, source/owner/cadence
   audit, assurance census, and historical protection.
2. Complete the preimplementation contract gate. Amend only an exact missing
   authority boundary; otherwise record `NO_CONTRACT_CHANGE_REQUIRED`.
3. Move Stage-3 custody out of day-input construction and implement the
   complete typed attachment and half-hour parent chronology.
4. Integrate actual snow-covered V11/shared-carrier execution, terminal event,
   topology-bound terminal-liquid transfer, and snow-free remainder.
5. Add complete additive restart, failure rollback, positive physical cases,
   independent conservation reconstruction, and production noninterference.
6. Run focused/domain/heavy/exact-head gates, reviews A--D, comparator,
   terminal verifiers A/B, assurance identity maintenance, and exact diff
   reconciliation.

## Exit criteria

Closure requires direct current evidence for every required criterion in the
handoff: actual Stage-3 solver invocation; actual V11 evolution on snow;
live shared carrier with all participants; actual event candidate
coalescing; exact-once terminal liquid transfer over declared topology; real
snow-free remainder; seven canonical owner endings; atomic installation;
complete restart before/at/after event and no replay; repository-backed
positive scenarios; poison rollback; independent conservation/output
reconstruction; default-off production noninterference; clean assurance and
stale assertions; exact-head workspace gates; four reviews; and two terminal
verifiers. Any current-scope `FAIL`, `BLOCKED`, or unjustified `NOT RUN`
forces `EXECUTED HOLD`, with a defect-shaped blocker and first actionable lift
step. Child 3 remains blocked until this package is complete.

## Subagent authorization

Subagent authorization: this package explicitly authorizes
spawning/delegating to snow/vegetation/LSE science, hydrology/ownership/restart,
Rust correctness/API, Rust QA, comparator, and two terminal-verifier role
subagents for the declared package scope. Expected outputs are compact
read-only findings, exact command/count summaries, comparator logs, and named
package artifacts. Implementation writes are limited to the frozen source and
test write set; reviewers/verifiers are read-only except their named artifact
files. If session policy prevents delegation, run the equivalent local gate
where the package permits it and record the policy result.

## Security and data impact

Local repository changes only. Preserve typed fail-closed guards, owner
identity, canonical serialization, restart replay protection, rollback, and
publication ordering. No secrets, network coordination, deployment, release,
or external data mutation is authorized.

## Calibration readiness

`science_implementation_status`: `IMPLEMENTED` only if the constitutive path
actually closes; otherwise `NOT_IMPLEMENTED`.
`calibration_evidence_status`: `NOT_APPLICABLE` for this implementation
package; no empirical calibration is claimed.
`identifiability_status`: `NOT_ASSESSED` with rationale.

## Progress log

- [x] 2026-08-21: exact starting identity and clean-tree intake passed.
- [x] 2026-08-21: package scaffold created; historical packages protected.
- [ ] required-reading and source/owner/cadence intake;
- [ ] contract/assurance reconciliation;
- [ ] constitutive implementation and scenario qualification;
- [ ] reviews, verification, exact-head gates, and terminal disposition.
