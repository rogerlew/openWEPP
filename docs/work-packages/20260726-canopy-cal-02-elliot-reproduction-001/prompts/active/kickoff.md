# Execute CANOPY-CAL-02 Elliot Reproduction

Scope: local repository scientific characterization; flat-file reads/edits and
confined local executable runs only; no external systems, network actions, or
service mutations are required.

Execution mode: package-end-to-end.

Phase plan: execute every phase in `package.md` sequentially through final
disposition, but do not begin unless the CAL-01 dependency-admission gate is
current and admissible.

Required reading:

- Core: `AGENTS.md`, `docs/work-packages/AGENTS.md`, `tests/AGENTS.md`,
  `tests/fixtures/AGENTS.md`, this package, the canopy assurance roadmap, the
  completed CAL-01 package, CAL-01 admission JSON and target ledger, and the
  installed Elliot reference README and report.
- Conditional: `docs/standards/testing-and-gate-strategy.md` for intent and
  terminal gate planning; `docs/standards/prompt-wording-guidance.md` if this
  prompt changes; `tools/owcmp/AGENTS.md` if the harness uses `owcmp`.
- On-demand: native operand lineage and canonical plant/residue contracts only
  when resolving units or rejected aliases.

Required-reading budget: `7225123` scaffold-time local bytes,
`REQUIRES-JUSTIFICATION`; this total uses the exact sibling WEPPcloud report as
the byte-equivalent source because CAL-01 outputs do not yet exist. The report
and complete CAL-01 evidence are load-bearing. Recompute after CAL-01 closes.
Map:
`artifacts/required-reading-map.md`.

Files: edit only the declared write set in `package.md`. Treat CAL-01 evidence,
commissioned references, existing fixtures, contracts, production crates, and
the sibling WEPPcloud repository as read-only.

Task: admit and execute the five-arm Elliot reproduction matrix, build
deterministic fixtures and analysis tooling, reconstruct process and downstream
results, issue the structured reproduction verdict, and complete the CAL-03
handoff, review, verification, and disposition.

Constraints: no model or physics changes; exact executable/input provenance;
separate 7777 from 2006.5 soils; separate hillslope, watershed, and channel
surfaces; run both Hubbard 92/95 branches; no silent substitute; no downstream
compensation for failed biomass/residue reproduction.

Conservation/output acceptance: record operand lineage, units and area/time
basis; separate plausible aliases; independently reconstruct residue-pool sums,
annual transfer, equilibrium, and return-period summaries; use real two-sided
magnitude comparisons where authority exists; do not close on one-sided bounds
or producer self-consistency.

Subagent requirement: REQUIRED - spawn one read-only
`comparator_suite_runner` for admitted long-running 40/100-year multi-arm WEPP
batches; do not run those batches on the parent model unless higher-precedence
session policy explicitly permits an equivalent fallback and the package
records the unavailable-subagent evidence. This prompt explicitly authorizes
subagent spawning/delegation to that runner, two independent read-only
scientific/reproduction reviewers, and two independent read-only terminal
verifiers; outputs are compact metrics, exact log/result paths, findings, and
verdict reconstructions; write access is read-only.

Autonomy: execute all admitted phases without requesting further direction
unless CAL-01 is blocked/stale or a declared source, executable, or authority
boundary prevents a truthful experiment.

Outputs: update every required package artifact and record one truthful
reproduction verdict; scaffold text is not run evidence.
