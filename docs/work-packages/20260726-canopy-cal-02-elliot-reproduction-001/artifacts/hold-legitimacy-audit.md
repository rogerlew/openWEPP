# HOLD Lift Audit

Evidence class: `Ran`

The prior gross annual litter-transfer blocker is lifted by explicit operator
acceptance of the available output-precision-bounded reconstruction.

The operator-authorized Linux 260725/source-native-9002 lane completed all five
100-year arms. Pinned `grow.for` proves the perennial transfer producer:
`delvd = vdmx * (1 - dropfc) / spriod`, followed by identical
`vdmy-vdmt` addition to current residue. Crop output publishes daily live
biomass to `0.001 kg/m2`; using its annual maximum reconstructs the internal
`vdmx` within publication precision. An independent sum of published daily
declines agrees within `0.00088 kg/m2/year`.

`wepp_observe.on` was active, but the stock release contains no
litter-specific Observe callsite. This audit does not invent one. Fixed-callsite
logs and hourly-water-balance activation are retained as runtime evidence.
The litter result rests on the real production formula and rounded crop
output, not a direct Observe measurement or exact internal operand.

The operator explicitly stated that this precision is close enough for the
campaign. No current gate remains blocked. Historical 9002/Windows
incompatibility and the superseded gross-transfer hold remain provenance only.
