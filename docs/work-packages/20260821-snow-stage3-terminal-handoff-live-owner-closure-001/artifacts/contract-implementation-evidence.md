Status: partial / EXECUTED HOLD
Evidence mode: Static

Static:

- `direct_runtime/snow_stage3_shadow.rs` owns a persistent default-off
  attachment and stages a shadow candidate, but the runner does not construct
  or custody it from the complete live Stage-3/V11 owner set.
- The exact terminal parcel identity and INV-010 debit/credit path are present
  in the bounded surface-liquid bridge; the event mass/energy operands are not
  supplied by an actual Stage-3 terminal solve.
- The remainder is passed through the surface-liquid authorization/resource/
  ingress path for the configured scaffold case, not as the snow-free
  continuation of a coupled parent interval.
- `03_executor.rs` advances the attachment after ordinary day spans and
  commits it at the day-frame boundary. `05_runner_execution_and_outputs.rs`
  consumes only the immutable post-transaction batch; this proves batching,
  not the required parent-interval chronology.
- `hydrology_restart.rs` projects/restores the attachment wrapper additively;
  it does not persist the complete coupled owner set and accepted cursor.
