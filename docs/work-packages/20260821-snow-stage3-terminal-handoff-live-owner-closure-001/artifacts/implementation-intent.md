Status: complete
Evidence mode: Static

Intent: implementation and default-off consumer-path closure. Protected
selectors/defaults, CoE ownership, production outputs, and deployment remain
unchanged.

The ordinary closure accepts only the persistent attachment configuration,
sealed transport receipts, static surface configuration, and live scheduler
frames. Carrier/event/ledger/owner DTO construction is internal to the
attachment; the old caller-built request/executor methods are test-only.
