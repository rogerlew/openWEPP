Status: complete
Evidence mode: Static

The ordinary path is ready for downstream review at the current worktree
head. The attachment is default-off unless explicitly configured, and the
runner receives rows only from the committed immutable batch. No deployment,
selector, default, CoE, output, or authority-contract cutover is included.
The repository-wide assurance drift must be repaired by its owning authority
maintenance work before an exact-head workspace release gate can pass.
