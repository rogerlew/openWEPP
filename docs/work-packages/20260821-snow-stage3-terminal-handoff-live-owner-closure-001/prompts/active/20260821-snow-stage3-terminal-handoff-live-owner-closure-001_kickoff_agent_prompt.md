# Resumed Child-1 kickoff

Local repository scope: execute
`20260821-snow-stage3-terminal-handoff-live-owner-closure-001` end-to-end from
commit `56f39487109022f7b01cb18978fa2c5076a285dd`.

Read the root and nearest `AGENTS.md` chain, this package, the canonical
SC-SNOWENERGY, SC-SURFACELIQUID, SC-VEGETATIONTRANSACTION, SC-COUPLEDTIME,
SC-LANDSURFACEENERGY, SC-VEGETATION, and testing/gate authorities before
source edits. Preserve the prior shared-carrier package as EXECUTED HOLD.

Contract-first: bind every caller field prospectively, author tests, pass the
pre-implementation gate, then edit production code. Do not reopen protected
science or change selectors/defaults, CoE ownership, production outputs, or
deployment. Do not introduce surrogate physics, silent defaults, raw payload
acceptance, or fallible work after the consuming owner replacement.

The ordinary closure API must not accept the old carrier/event/owner DTOs or
raw conductances, ledgers, or owner bytes. Build one persistent default-off
attachment internally from sealed forcing and live owners, advance it from
the ordinary scheduler, execute the exact terminal parcel and WB14 remainder,
stage one complete owner candidate, install once, and publish only an
immutable post-transaction batch. Prove restart before/at/after, replay,
late-failure rollback, publication rollback, and dual-terminal behavior.

Conservation/output acceptance: record operand lineage, anti-tautology cases,
independent mass/enthalpy/WB14 reconstruction, real closure/magnitude checks,
and metadata alignment. Correct the stale SC-SNOWENERGY version guard. Split
all nonexempt Rust files above the repository line-count ceiling before review.

Autonomy: continue through implementation, focused/domain/quick/heavy/exact-
head validation, exact diff reconciliation, dual review, dual verification,
and disposition; stop only at a legitimate authority or owner-input HOLD.

Subagent authorization: this prompt explicitly authorizes subagent
spawning/delegation to two independent read-only review agents and two
independent read-only verification agents for the declared package scope;
expected outputs are named artifact findings and command summaries. If the
session cannot delegate, record that limitation and run equivalent local gates.
