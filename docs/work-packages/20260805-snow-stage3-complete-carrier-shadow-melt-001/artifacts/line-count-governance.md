# Line-Count Governance

Static: current shadow increment.

`runoff_reconciliation.rs` is `3,177` lines after the within-day shadow
implementation, above the `3,000`-line closure threshold. The result-bearing
execution is dispositioned as an executed HOLD rather than left active, but it
cannot be marked complete and no further feature work may enter this module
until the shadow solver is extracted into a bounded submodule (or the file
otherwise falls below the threshold) and focused gates are rerun. The user
directed package closeout plus roadmap sequencing on 2026-08-06; that direction
ends this failed result-bearing increment without waiving the structural gate.

This open obligation does not change the independent scientific and authority
holds on per-term carrier observability, persistent cross-day shadow state,
thin-pack disposition, and same-substep liquid routing.
