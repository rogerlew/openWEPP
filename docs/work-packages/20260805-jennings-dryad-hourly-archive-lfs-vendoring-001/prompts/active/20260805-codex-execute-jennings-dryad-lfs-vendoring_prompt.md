# Execute Jennings Dryad Hourly Archive Git LFS Vendoring

Scope: local repository external-data custody task; flat-file reads/edits and
the already installed local Dryad artifact only. No external download, push,
or remote mutation is required.

Execution mode: package-end-to-end.

Read `package.md` and `artifacts/required-reading-map.md`, then execute every
phase through truthful disposition. Preserve exact DOI/version/license/hash
provenance, add the exact path-scoped LFS rule before staging, keep the
1.2 GB file opt-in for ordinary CI, and do not modify observation bytes or
scientific/runtime behavior.

This prompt explicitly authorizes subagent spawning/delegation to the read-only
reviewer and verifier roles declared by `package.md`; expected outputs are
compact findings for package artifacts, with no file edits or remote pushes.

Autonomy: continue through all in-scope validation and disposition without
requesting user input unless a genuine rights, byte-identity, LFS, or remote
capacity boundary blocks local completion.
