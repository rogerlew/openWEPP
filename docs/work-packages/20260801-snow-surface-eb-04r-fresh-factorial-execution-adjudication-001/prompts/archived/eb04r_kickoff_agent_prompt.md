# SNOW-SURFACE-EB-04R Kickoff

Scope: local repository science execution; flat-file reads and bounded local
model subprocesses only; no external connectivity.

Execution mode: package-end-to-end.

Execute every phase in `package.md` through disposition. Read root and
work-package AGENTS, `docs/codex_exec_plans.md`, the frozen protocol, testing
strategy, prompt wording guidance, `SC-SNOWFREEZE-001`, `SC-SNOWENERGY-001`,
and the EB-04/04E predecessor evidence.

Freeze tool/protocol/source/binary/population identities before results. Strip
all inherited `OPENWEPP_*` state, install and serialize only the seven frozen
experiment keys, execute the 48 cells once without scientific retry, require
independent physical/consumer gates before scoring, apply the unchanged rule,
and preserve all contrary evidence and claim limits. Do not edit production,
tests, contracts, fixtures, observations, forcing, coefficients, thresholds,
rubrics, selectors, or defaults.

Subagent authorization: this package explicitly authorizes subagent
spawning/delegation to one `comparator_suite_runner` for population and Critical
full execution, two independent read-only reviewers, and two read-only terminal
verification agents. Expected outputs are compact metrics/log paths and the
package's review/verification artifacts. No subagent may edit protected paths.
