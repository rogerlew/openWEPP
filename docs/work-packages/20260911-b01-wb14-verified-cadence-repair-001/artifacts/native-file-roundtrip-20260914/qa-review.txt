Reviewer: /root/native_qa (rust_qa_reviewer, Terra/medium), independent replacement for prior unavailable QA. New conversation, no continuity claim.

Initial static QA assessment (pre-resume; no commands that build/test/lint/fmt/capture/probe): the claimed architectural stop is NOT established. The adopted authorization expressly permits immediately necessary private restoration/accessor plumbing and says to use retained canonical native-admission/constructor/install/replay paths; prior package evidence already identifies persisted-restart V1 representation as not a substitute for missing state, rather than forbidding a private diagnostic projection. Correctness independently reaches the same conclusion. Do not treat the non-importable persisted-restart DTO as a boundary or copy it wholesale. Concrete current blocker: raw native frame serialization is statically invalid, so the current source is not source-ready; this is an in-envelope implementation defect, not proof that the canonical frame/restore must cross an illegal dependency cycle. QA acceptance remains HOLD: no executed native producer→production recorder→file-only reader→owned native context test; no connected V1/non-native refusal; no zero-physics/no accepted-state or parcel-custody mutation control; no current fmt, clippy -D warnings, test, or cargo-deny evidence. The preserved snapshot is source identity only, never behavior/gate evidence. Maintain a small private exact projection/restore seam in the orchestrator and test it against independently constructed producer values; do not widen public API, add dependencies, or assert a restart/capture qualification.

Final initial response: Static QA initial assessment sent to parent: the claimed architecture stop is unsupported; raw-frame serde is an in-envelope defect, and all connected behavior and gate acceptance remains HOLD.
