# Independent correctness review — reference input recovery

**Reviewer:** `/root/recovery_correctness`, authorized replacement for the unavailable prior correctness reviewer; independent of the parent executor, source writer, adviser, and QA reviewer. No continuity with the prior reviewer is claimed.

**Scope:** restored diagnostic-reference support-input custody, unchanged reference/candidate Rust identity, reference compilation and exact native-characterization discovery, and the resulting affected recorder-preparation disposition. The accepted static source findings in `artifacts/recorder-fixture-20260915/correctness-review.md` are reused unchanged and were not reopened.

**Evidence class:** Static: inspected the authorization, package record, recovery custody records, restored support trees, historical source receipts, and prior accepted correctness review. Ran evidence inspected: parent-executed reference library/test compilation and Nextest discovery. The reviewer ran no Rust build, test, model, comparator, recorder, or raw-corpus command.

## Findings

### HIGH — required numerical and custody parity remains unverified

`reference-list.json` and `reference-list.stdout` establish successful discovery only. The frozen native reference/candidate characterization pair never launched, and no native payload, canonical-field comparison, original 540-second regression, recorder isolation result, or retained producer/control result exists in this recovery artifact. Therefore there is no current execution evidence that the candidate recorder hooks preserve numerical results, physical/canonical fixture state, terminal chronology, owner and parcel custody, or disabled/non-target/error behavior relative to the pre-hook reference.

This is an unmet current-scope acceptance requirement, not a scientific mismatch. B01-CONT-026 and recorder preparation remain **HOLD / NOT APPROVED**. A successful reference compile and exact discovery cannot substitute for the pair or recorder controls.

### MEDIUM — discovery postprocessing rejected a valid exact selection and exhausted the correction ceiling

`reference-list.stdout` reports `test-count: 1470`, which is the complete discovered library inventory. Within `rust-suites.*.testcases`, exactly one entry has `filter-match.status: "matches"`: `v9_real_consumer_shadow::tests::adaptive_production_path_coverage::native_mixed_phase_fixture_characterization_payload`; all other 1,469 entries are explicit expression mismatches. Parent postprocessing instead required the top-level inventory count to equal one. That assertion failed after the valid exit-0 listing and conservatively consumed correction 29/29, preventing the pair from launching.

The package must describe this as a parent verification/tooling failure after a valid exact listing. It must not describe discovery as failed, claim zero or 1,470 selected tests, or infer any candidate/reference behavior from the listing. No correction is authorized after the hard stop.

## Fix verification and accepted limited result

- **PASS:** `input-custody-before.json` preserves the originally absent reference `tests/` state, the failed reference evidence identities, and the historical 741-entry source receipt rather than redefining its membership.
- **PASS:** `input-custody.json` binds 7,980 restored regular files and 1,318 internal symlinks to the immutable producing base and candidate. Independent read-only comparison found the three `tests/` trees byte/link-equal, with zero broken links. The formerly missing include has SHA-256 `5bd1ed07e3712bb1bc023de5eb1a47677806fc0a7d9d197dea420f4d42d4a47e` in all three trees. The separately referenced `candidate-v22-hermite-gauss-matrix.json` is recorded base/candidate-equal. Support membership digest is `b1041779721f7432f85a1b0c8a62ffb75482572b94e63c3c288180e434400402`.
- **PASS:** The initial no-symlink assertion error is disclosed rather than used to discard legitimate inputs; all 1,318 links resolve internally. Charging it as correction 28/29 is conservative.
- **Ran, PASS:** `reference-check.json` records exit 0 for the reference library and tests under the locked features and retained wrapper, with source tree `996e87f5a4aad77fc906691dc1d0ce466eb769767af87c32bd83b849cc593016`, patch `e8a700579d6dfad9bac8db2f716b5004104b3215fa2321f52aa71e950f0492b6`, input-custody digest `2e9e27eeb0a355672e7a74c8fd645a2ce95182ae88bf78741bc8f98ecac9b082`, and unchanged source during execution.
- **Ran, PASS:** `reference-list.json` records exit 0 with the same source and input identities. Direct inspection of `reference-list.stdout` confirms 1,470 discovered tests and exactly one filter match for the frozen native-characterization test.
- **PASS:** No Rust, fixture contents, assertions, dependencies, arithmetic, clamps, units, domain guards, production serialization, or typed error taxonomy changed in this recovery. The pre-hook reference did not acquire candidate recorder plumbing. The substantial manual test projection duplication remains intentionally retained for oracle independence, with the maintenance limitation already recorded by the prior correctness review.

## Residual risk and missing tests

- Reference/candidate native payload equality is **NOT RUN**; every canonical comparison field remains unverified dynamically.
- The original 540-second regression and covered/snow-free recorder modes, independent operands, disabled/enabled/non-target/forced-error, wrong-phase, omission, selector, and exact-selection controls are **NOT RUN** on this recovery cut.
- Terminal formatting and matched owning-crate/separate-runner lint are **NOT RUN** here.
- The previously recorded dynamic-hydrology mutation-sensitivity limitation remains.
- The outer day 4 / interval 22 versus nested day 0 / interval 0 defect, authentic context acquisition, complete prefix authentication, native reader, conservation/restart/science qualification, and cadence repair remain unresolved and outside this recovery result.

## Verdict

**Limited recovery PASS; recorder preparation HOLD / NO APPROVAL.** The omitted reference inputs were restored with adequate provenance, and the reference now compiles and lists the one intended test exactly. The 29/29 stop occurred because parent postprocessing misread total discovery count as selected count. Since no native pair or recorder/control execution followed, comparator-sensitive behavior and recorder isolation remain unproved.
