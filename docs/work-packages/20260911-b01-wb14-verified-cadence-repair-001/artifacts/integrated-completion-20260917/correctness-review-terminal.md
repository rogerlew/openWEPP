# Independent correctness review: terminal integrated promotion cut

Reviewer: `/root/completion_correctness` (independent correctness and science-contract review). Final detached source: `/home/roger/openwepp-experiments/b01-wb14-integrated-promotion-20260917`, SHA-256 `e93175d626081d52219c5053ff7899521f7ce3010a720335bec8b462095ee1e7`, 746-file `closure-compile-source.json`; exact selected binary SHA-256 `3ee09e2d800d145494dd38280e26a89fb95447284120112041bfef3e00b65169`. Raw receipts reside in `/home/roger/openwepp-experiments/b01-wb14-integrated-completion-evidence-20260917`.

**Findings, highest severity first — Static:** No unresolved correctness or science-contract blocker in the bounded local-control cut. The earlier medium duplication of the probe identity JSON recipe is resolved by one test-only helper. The N8 selected-ending, N10 accepted-source, N11 late V4 constructor, N12 outer rollback, parent inventory, and source/receipt correspondence findings received same-reviewer fix verification. Two production reseal staging edits remain behavior-preserving as described below; neither changes physical arithmetic, canonical guard order, public wire format, or promotion authority.

**Findings — Ran:** The final source/binary-bound 14-case local matrix passes. Exact receiver, donor, install, and parent reconstructions and corruption controls pass on the final positive output. Strict Clippy and the no-default/evidence-only `cargo check --tests` configurations still exit 101: the 27 strict, 66 no-default, and 36 evidence-only diagnostic instances match inherited baselines exactly. The separate no-deps Clippy comparison has only two changed instances of the same inherited parent-helper length diagnostic (253 to 264 lines), from one naturally formatted 11-line test-only observer call; two inherited redundant-closure diagnostics disappear. This is a retained quality failure, not a strict-lint or compatibility pass or full qualification.

**Static:** I checked relevant active source files against the v67 source manifest. The N8 selected-ending negative now changes a genuinely distinct physical temperature; N9 receipt predecessor chain and N10 canonically constructed alternative expected-source identity target separate final replay predicates. N3 and N5 respectively target missing accepted custody and wrong accepted-replay restart proof at actual promotion. N2/N4/N6/N7 target original custody, publishable posture and combined precedence. N11 checks a late V4 receipt mismatch after private projection mutation with exact typed error and authoritative receiver preservation. N12 injects an actual native mixed-phase `FinalOwnerJoinCompleted` fault after the inner promotion and checks the parent, shadow, clock, prepared/context, Stage3, and empty owned parcel outer roots. It keeps a `Result` in an `Option` across `catch_unwind`, then checks the exact injected `Err`; an unrelated panic cannot satisfy its typed sentinel. Test hooks and new observation prints are gated by `#[cfg(test)]` and an explicit audit flag.

**Static fix verification:** The previously duplicated 12-field probe child identity JSON recipe is now one `#[cfg(test)]` helper in `snow_stage3_v11_terminal_execution_carrier_phase.rs`, called by both carrier and preterminal reseal records. The test-only installed V3/V4 audit was extracted into capture/assert helpers without losing any v57 checks: non-V fields, exact V3/V4 publication prefix plus one append, V3 surface/LSE and V4 exact owner, V4 projection bytes, V3 publication projection bytes/hash/receipt chain, V3 predecessor chain and WB14 parent. Two production reseal seams do change staging: the former borrow of `prior_child`/`prior_identity` becomes a clone, the new child is first constructed in a local, and the same new child is then assigned to the authoritative field. This preserves the `try_new` operands, error order and final assigned identity; the associated observation remains `#[cfg(test)]`/audit-gated. I found no changed arithmetic or production guard precedence in the reviewed diff. The medium duplication finding is resolved.

**Static v68 formatter verification:** Exactly four of 746 files differ from the v67 source manifest. I applied the same edition-2024 `rustfmt` with `skip_children=true` and stdout emission to each v67 file in `terminal-source-reconstruction`; after removing the Nix shell banner and rustfmt file header, each complete output byte stream matched its v68 source file. All other 742 file hashes are unchanged. Thus the v68 correction changes layout only, preserving the reviewed v67 predicates and control flow.

**Static v70 delta verification:** Compared with the v68 source manifest, only `snow_stage3_v11_adaptive_production_tests.rs` changed. Its diff contains exactly five closure-tail `)` to `);` changes in the N6–N10 test-only control enablers and no other changed bytes. The v70 targeted format check has exit zero with unchanged source/pins. The earlier v69 targeted check bound the pre-edit v68 hash and is not used as v70 evidence.

**Ran, own verification:** I did not execute a Rust fixture, physical simulation, or campaign. I ran the four Python reconstructions independently on the owner-run v67 positive stdout and reviewed its 40/40 mutation result; earlier independent checks appear in `correctness-review-interim-v18.md`, `correctness-review-interim-v22.md`, and `correctness-review-interim-v46.md`. I also compared every final source-manifest hash against both the live tree and reused recovery copy (746/746 exact), and checked all 14 final raw stdout hashes and one-passed-test summaries against their receipts.

**Ran, inspected owner executions on final v70:** `closure-matrix-results.json` records 14/14 separately selected cases, each exit zero with source, binary, pinned inputs and stdout hash unchanged. The positive case reaches one exact V3/V4 pair install and accepted parent return. N2/N3/original support/predecessor/receipt-chain/N5–N12 reach their declared actual-entry guard, typed payload, custody precedence or rollback boundary. The test-only sentinel cannot accept an unrelated panic. `closure-reconstruction-checks.json` binds the positive stdout SHA-256 `58640feb6e8c7e1dc1370ae930eab701cfb81ba2048e0fff57156566164d5df3` to unchanged scripts and five passing reconstruction/control results. Receiver: 90/90 ordered credit rows and exact accumulation arithmetic. Donor: nine accepted endpoint records, 11 phases/reseals, six selected and five unselected phases, 92 selected primitives (84 internal, six top, two infiltration), canonical old/new reseals and selected-ending/source joins. Soil install: exact selected transaction 47 to installed transaction 41 with complete state, receipt, expected-source and restart/checkpoint/orchestrator seals. Parent: six represented-prefix plus two physical snow-free successor segments, complete eight-segment local parent history, retained WB14 parent-replay correspondence, one observed terminal event group, two clock event receipts and consumed parcel. The donor/receiver/install corruption suite rejects 40/40 alterations; the parent suite rejects 28/28 and accepts its no-op control. The final targeted formatter check exits zero.

**Static custody and atomicity:** N2 checks original prepared-owner custody and exact runtime wrapper; N3 checks absent accepted replay; N5 checks a wrong restart proof while keeping the resident accepted replay valid. N6/N7 distinguish candidate-only from publishable posture and combined early-custody/later-promotion precedence. N8/N9/N10 alter a real selected ending, receipt lineage and canonically valid alternative accumulated source respectively, then reach the owning final-replay validator. N11 fails at the late V4 exact owner/receipt constructor after private projection mutation and proves the authoritative receiver remains unchanged while the disposable candidate is discarded. N12 injects the native mixed-phase late final-owner-join fault and checks receiver plus parent/shadow/clock/prepared/context/Stage3 and owned-parcel caller roots. Successful V3/V4 audit asserts exact prior publication prefix plus one append, changed V3/V4 state/projection and full appended V3 projection bytes, hashes, receipt chain, predecessor and WB14 parent. These claims are limited to the API boundaries and fields actually owned and observed.

**Residual risk and missing qualification:** LP-C2's missing local donor-to-credit/selected-ending and LP-C4's local parent-history correspondence are now evidenced for this NativeMixedPhase fixture. WB14-RC1/RQ2's actual-entry semantic/precedence/rollback and single replay/install portions are likewise covered locally. These identifiers are not global package acceptance: the parent reconstruction explicitly reports `checkpoint_finalized=false`, `clock_committed=false`, `global_commit_authenticated=false` and `installation_authenticated=false`; the separate local soil-install reconstruction authenticates only its selected-state transform. Empty owned parcels are value-equivalent at the observed rollback boundary, not an external outbox or nonempty-parcel proof. Original-B01 accepted-prefix/corpus correspondence, the captured E008, constitutive conservation, runtime restart, full default/Critical campaign, WB14-RQ1 quality acceptance, A-001 and cadence release remain unproven or HOLD. No original physical input/campaign was run here.

**Correctness disposition:** APPROVE the stated bounded local-control correctness evidence, with no unresolved source correctness blocker. Independent QA and owner package disposition remain separate; this review does not accept A-001 or WB14-RQ1 and does not authorize production Rust adoption.
