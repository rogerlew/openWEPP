Attribution: `/root/event_qa_attribution`, rust_qa_reviewer role, independent of
the author/extractor/orchestrator and correctness reviewer. Original responses
are retained below in order. The first response inspected the pre-row cut.

---

**Static QA review — attribution scope; no commands run.**

### Findings

- **Blocker — package.md:** The requested three attribution rows are absent from the stable active section. Only the adopted-continuation preamble is present, so I cannot verify final claim wording, references, test dispositions, or review the completed record.

- **High — soil attribution row, required constraint:** The source proves an admission refusal, not a representable restoration posture. R’s diagnostic constructor always authenticates the resident as `InProgressSupportCurrent`; `authenticate` requires its accepted support end to equal the restored clock endpoint. The retained operands establish `385200000000000 != 385920000000000`, while the deferred candidate is explicitly non-installable custody. Any claim that the deferred posture is restore-valid, or that the API is proven mismatched to a valid producer state, would exceed the evidence.

- **High — GSI attribution row, required constraint:** The `85ca…` prepared-day receipt and captured `e1ca…` field belong to different lifecycle phases by source trace, but the exact day-3 captured preimage was not retained. Their mapping and recommended comparison surface must be labeled **SOURCE-INFERRED**, not execution-proven.

### Non-blocking debt / follow-ups

- Position must retain outer parent position `214` and captured native scheduler `0` as distinct namespaces; no wording should imply that `4 * 48 + 22` proves the native field invariant.
- Q suppresses interruption while deferred custody exists, then passes that custody into the snow-free successor and clears it only after successor success. A prospective soil regression should distinguish this lifecycle from the single-resident constructor’s current admission predicate.
- Preserve `cargo fmt`, strict `clippy -D warnings`, test, and deny status accurately: no new execution evidence was supplied for this attribution-only cut; inherited strict-lint and campaign gates remain HOLD/NOT RUN.

### QA verdict

The continuity record and soil operand extraction are coherent and appropriately limited. **QA disposition is BLOCKED pending the final three package rows.**

---

**Static QA review — final attributed-decision cut; inspected retained execution evidence, no commands run by reviewer.**

### Findings

No blocking QA finding. The final rows preserve the required evidence boundaries:

- Position keeps outer `214`, native physical counter `0`, clock slabs, and deferred credits as separate domains; the proposed reader correction does not install or derive a scalar.
- GSI explicitly labels the day-3 receipt mapping as lifecycle **inference** because its exact preimage is unavailable, and does not treat unequal digests as a producer defect.
- Soil is correctly limited to a source-representable execution posture incompatible with the current single-resident restore API. It does not claim restore validity, physical validity, successor success, or an admitted restart. The proposed future design retains non-owner custody and requires canonical authority.

The revised repository-root extraction command accurately represents the same read-only script invocation. `git diff --check` passes.

### Non-blocking debt / follow-ups

- The row states that captured GSI equals the day-4 prepared receipt’s beginning state, but the current attribution artifact directory has no compact GSI/position extraction equivalent to `soil-operands.json`. Add the planned read-only receipt/position extraction or identify the precise existing retained operand receipt when preserving this claim.
- Future implementation must run the declared meaningful tests and applicable formatting, strict Clippy, test, and deny gates. This attribution cut correctly leaves them NOT RUN/HOLD.

### QA pass

The prior earlier-cut blocker is superseded by the completed three-row cut. QA/evidence review **passes for the bounded authority-domain attribution**, with native restoration, production, strict lint, campaign correctness, conservation, and restart qualification remaining HOLD or unmet as recorded.

---

**QA supplement — same-reviewer final evidence verification.**

### Findings

No blocking finding. The added [position/GSI extraction](docs/work-packages/20260911-b01-wb14-verified-cadence-repair-001/artifacts/authority-domain-attribution-20260916/position-gsi-operands.json) binds the 457,913,228-byte native member and provider member to retained event hashes. It directly establishes count `0`, null WB14 parents, day-0/interval-0 zero-flux surface custody, captured GSI state/cursor equality with the day-4 receipt beginning, and preserves `e1ca…` as a separate captured receipt.

The GSI correction now properly requires an independently authenticated bootstrap/provider prefix or retained receipt preimage. It states that the tuple alone cannot authenticate `e1ca…` and leaves that join unresolved. This resolves the prior evidence-boundary concern.

### Non-blocking debt / follow-ups

- `e1ca…` remains source-lifecycle inferred until its authoritative prior receipt preimage or equivalent authenticated prefix is retained and checked.
- Future implementation retains the previously stated canonical-authority, test, formatting, strict-Clippy, test, and deny obligations.

### QA pass

The final attribution record passes QA/evidence review. I inspected the new extraction, script, package links and labels; I also ran `git diff --check`, which passed. The extraction’s reported Python execution is inspected evidence, not a command I ran.

---

**QA addendum — producer-path citations verified.**

The GSI addition is accurate: the attachment prepares repository V11 intervals, projects the prepared receipt onto a cloned candidate for returned forcing, and the successor consumes the prepared interval. It does not imply live receipt installation before end-of-day commit.

The soil addition is accurate: deferred native installation retains the exact beginning resident in the outward provisional candidate while the selected trial remains separate transient custody.

No new execution claim, gate claim, or restoration-success claim was introduced. The prior QA pass remains valid.
