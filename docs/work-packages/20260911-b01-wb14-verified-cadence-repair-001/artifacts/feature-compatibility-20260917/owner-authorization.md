# B01 WB14: recover the selected RQ1 feature builds

DRAFT FOR ROGER'S ADOPTION. This proposal does not authorize itself.

## One active checkpoint, exact source, changes, acceptance, runs and stops

**Active checkpoint:** `B01-WB14-CURRENT-CONTEXT-CAPTURE / A-001`, maintained in `docs/work-packages/20260911-b01-wb14-verified-cadence-repair-001/package.md`. The next bounded deliverable is the **feature-build portion of WB14-RQ1**: repair the already-required no-default and evidence-only test compilation, preserve the selected full-feature workflow and test inventory, and obtain affected independent review. This does not reopen the completed local promotion matrix or declare all of RQ1 or A-001 accepted.

**Evidence revision:** `d3baae40b598a058b910729dc7a50f8dac54d419` in `rogerlew/openWEPP`. Read the current integrated-completion disposition and `artifacts/integrated-completion-20260917/`. The evidence commit is not the detached execution source.

**Exact starting source:** `/home/roger/openwepp-experiments/b01-wb14-integrated-promotion-20260917`, tree SHA-256 `e93175d626081d52219c5053ff7899521f7ce3010a720335bec8b462095ee1e7`, 746 recorded entries. Preserve frozen binary SHA-256 `3ee09e2d800d145494dd38280e26a89fb95447284120112041bfef3e00b65169` and its 14-case results. Recovery is verified T plus `closure-quality-nodeps-incremental-from-T.patch`, SHA-256 `66806ad7ba7cfd29ef82ebc3d91ff4c707ea70139071d8a4a92194dfd9237614`, under the existing `closure-source-reconstruction.json` recipe. T remains `d9d4006221cb858d7e4a205f7cd818b834808714d054ecb5c887033a69c8eb01`. Do not apply the T-relative patch atop U or the already-patched integrated tree. Preserve T/U/v20, earlier source cuts, original inputs and unique binaries. Verify durable baseline recovery, then use one mutable development copy and the existing build infrastructure; do not duplicate the raw corpus.

**Prospective scope amendment:** earlier local-control permissions excluded inherited workspace cleanup. This proposal additionally authorizes **targeted inherited feature-compilation repairs** in the selected orchestrator/restart dependency and test surfaces, including conditional compilation, test imports/helper availability and private diagnostic representation wiring where the actual source establishes the defect. No particular `cfg` edit is preapproved. Do not change numerical algorithms, physical operands, runtime guard order, scientific/admission semantics, trusted capability traits, public APIs/wires, dependency versions, toolchain, default features or the feature-dependency graph. A necessary change beyond these boundaries is a distinct owner decision. Broad inherited lint cleanup and unrelated refactoring are not included.

**Acceptance/checks:** both formerly failing test-compilation commands must complete on the exact corrected source, with no omitted required target or silently removed valid test. The default library and existing selected full-feature test build must remain valid. Establish the intended feature-specific test membership from actual source/authority; retain all 14 promotion selectors and the existing required restoration/capability controls in their supported configurations. Execute affected focused behavioral/negative controls where a correction changes their compiled implementation or availability. Build/check/listing alone is not test execution. Preserve unchanged scientific evidence with a precise source-impact explanation; do not relabel old runs as new-source runs. Obtain distinct correctness and QA review of changed feature boundaries, inventory and primary results. Strict Clippy, broader compatibility execution, Critical campaign, A-001 and scientific qualification remain independently unmet unless their actual requirements are separately satisfied.

**Run allowance:** bounded local source inspection, compilation, exact test listing, feature/dependency inspection, focused compatibility tests and affected verification. The prior isolated local promotion-control allowance remains available only for justified affected verification, not a new investigation or another matrix by default. No original-input model, captured day-4 successor, collector/acquisition, multi-day campaign, full physical qualification, original-regression repair, production publication or cadence resumption. All three historical acquisition/witness allowances remain exhausted at 1/1 each. No canonical promotion or production Rust adoption.

**Budget:** add NO time and no failure-count cutoff. Carry **103,580.768 charged seconds**, including publication/return reserves, under the unchanged **106,868.925-second cumulative ceiling**. Maximum remaining balance: **3,288.157 seconds (54m48.157s)**. Read `/workdir/openwepp-experiments/b01-wb14-cadence/logs/integrated-completion-final-20260917.json` once for final precision; greater verified consumption reduces the balance, and smaller precision does not reclaim reserves. Carry failure floor **at least 251**, preserving uncertainty and prior failures. Reanchor only the paused balance at actual continuation start. All reading, implementation, commands, orchestration, review, preservation, publication and return charge concurrent wall time once, without new wait deductions. Reserve review/preservation within the balance; Astra cannot extend it.

**Stops/reassessment:** hard owner time limit; owner stop; uncontained integrity issue; indispensable unavailable input after bounded recovery; completed bounded disposition; or a necessary excluded change after useful independent work is exhausted. Workers return new failures to Astra for classification. Two unsuccessful corrections or 60 active minutes are internal reassessment points, not renewed owner handoffs or extra resources. Routine build/test integration corrections stay with Astra. Preserve a new out-of-domain behavioral or scientific failure rather than tuning it away.

## Repair the failed configurations without making their tests disappear

Start from retained primary compiler JSON, not the number of messages. `closure-quality-disposition.json` records 66 no-default and 36 evidence-only diagnostic records; these counts include more than compiler errors. The no-default stderr reports 61 errors. Use `closure-quality-no-default.json`, `closure-quality-evidence-only.json`, their raw streams and corresponding v6 comparisons to classify root causes and cascading errors once. The terminal source has no newly attributed compiler errors relative to that baseline; this proposal explicitly targets the inherited failure rather than pretending another no-new-error comparison repairs it.

Run the same configurations through the established pinned Nix route and explicit experiment manifest. The essential commands, with that manifest supplied, are:

```bash
cargo check --manifest-path "$manifest" -p openwepp-hillslope-orchestrator \
  --locked --tests --no-default-features --message-format=json

cargo check --manifest-path "$manifest" -p openwepp-hillslope-orchestrator \
  --locked --tests --no-default-features --features restart-authority-evidence \
  --message-format=json
```

Also preserve the existing selected build with `persisted-restart-v1,restart-authority-evidence` and the default library check. Run feature configurations separately and inspect resolved dependency features when needed; a broad all-features build cannot establish no-default compatibility. Do not change the commands to enable the missing feature and call the original configuration fixed.

For each conditional-compilation correction, establish whether the affected test actually requires the omitted capability. Keep ordinary no-feature tests active. A feature-specific test may be compiled under its genuine prerequisite only when that prerequisite is source/contract-supported and the same test is retained and verifiably selectable there. Do not gate the entire test module to hide unrelated failures, add `cfg(any())` or ignored tests, erase assertions, convert a runtime capability to Serde/Clone, add placeholders, or silently declare an already-required configuration unsupported. If a documented feature relationship itself needs changing, return that explicit proposal rather than changing public/default feature semantics in this envelope.

Fix private representation or helper wiring only at its owning boundary, preserving exact operands and existing trust semantics. Adding serialization to a trusted runtime capability is not a feature-build fix. Preserve current tests for ordinary admission refusal, non-wire single-use proofs, and source/phase custody.

## Demonstrate what changed and preserve accepted work

Use existing discovery/recording tools. Reconcile before/after source and expected test membership, not only total test counts; baseline listing failures remain failures. Where a previously broken configuration cannot produce a runtime listing, identify its source-declared expected tests, then verify the corrected listing and appropriate focused execution. The full-feature 14-case matrix and its physics/expected values must not disappear. Do not rerun the complete positive/reconstruction campaign solely because a build-gating file changed; inspect the effective compiled path and rerun the genuinely affected controls. If the effect cannot be bounded, use conservative affected verification and retain any unreleased full qualification as BLOCKED.

A passing compiler check satisfies only the selected compilation claim. Separately record actual test execution, intended exclusions with authority, formatting and quality outcomes. Run affected-file formatting and source-quality checks as applicable. The strict 27-diagnostic failure and complementary no-deps debt remain recorded; this feature-build deliverable does not require an unrelated thousands-of-warnings cleanup or authorize a warnings cap as acceptance. Newly exposed scientific behavior is not repairable by changing fixture inputs or solver/cadence guards here.

LP-C2/LP-C4 and WB14-RC1/RQ2 stay satisfied **locally on their reviewed frozen cut**. Do not reopen their reviews unless the corresponding implementation/evidence changes. Original-input correspondence, E008, the original regression, constitutive/conservation/restart qualification, global commit/outbox claims and cadence remain HOLD. Repairing feature builds cannot promote the unpromoted deferred-composite draft.

## Execution ownership, review and return

Astra owns the complete in-scope repair and verification loop. Reuse a useful implementer; at most two concurrent children, one source writer and no nested spawning. Reuse independent `/root/completion_correctness` and `/root/completion_qa` where reachable, otherwise record attributable independent replacements. Review only the new feature-boundary/compatibility work and affected claims. An author cannot independently review their own changed source or reconstruction script; preserve the previous explicit exclusion for QA-authored parent reconstruction.

Apply the current root/nearest/package, Pro-role, bounded-execution and testing instructions. Use `tools/agents/find-agents --for` for new write surfaces. Preserve relevant canonical state/capability invariants and the existing numerical/cadence pause. Check current host disk/memory; keep large build/log output off the previously constrained `/workdir` and retain established bounded subprocess settings. No repeated source-copy, broad formatter or raw-corpus campaign is needed.

Keep `package.md` the only maintained narrative. Carry scoped evidence/patch commit/push and exact-commit remote verification permission; no branch switch, production Rust adoption or deletion of unique evidence. Preserve exact final source/recovery/build identities and failed attempts without per-typo publication ceremonies.

Return: each configuration's exact result; identified root causes and correction sites; supported test membership and executed checks; effect on the full-feature path; independent reviewer verdicts; strict-lint and remaining qualification status; and final charged ledger/failure floor. The required outcome is a verified feature-build repair or an attributable remaining blocker, not a promise that all of RQ1 or A-001 will become green within the allowance.

## Adviser basis and access boundary

Inspected at the immutable evidence revision: current integrated-completion record, both terminal reviews, matrix entries, exact feature/strict commands and terminal quality comparisons, no-default stderr, recovery recipe and the preceding adopted handoff. Root/package/testing guidance was reused after matching current blob identities. Cargo's official feature documentation confirms separate feature flags and feature-unification behavior. The complete detached implementation, frozen binaries, full compiler JSON and large scientific streams were not independently decoded or rehashed here. The uncompressed compiler stdout path returned 404; its gzip is published, but transfer into this runtime failed on DNS resolution. This is not a missing-source finding against the executor, who retains the raw streams. No source-specific `cfg` repair is claimed proved by this adviser. No Rust, physical test, provider computation or independent acceptance review ran here.

Evidence base: https://github.com/rogerlew/openWEPP/tree/d3baae40b598a058b910729dc7a50f8dac54d419/docs/work-packages/20260911-b01-wb14-verified-cadence-repair-001/artifacts/integrated-completion-20260917
