Static review of the evolving feature repair; Ran: no commands by this reviewer. I inspected the frozen source, current diffs, contracts, and retained compiler records. **No final acceptance verdict yet.**

- **High, C1 — correction pending verification:** An intermediate cut added an empty no-feature `record_pre_child_context` in `snow_stage3_v11_current_context_capture.rs`. Ungated recorder tests in `snow_stage3_v11_adaptive_production_tests.rs` require a captured row, so that stub would silently break ordinary tests. The implementer removed it; I have not yet verified the terminal source or executed controls.
- **Medium, C2 — correction pending verification:** An intermediate test-only `Serialize` addition for `DirectGrowthActiveContext` in `direct_runtime/growth.rs` omitted the matching conditional `serde(rename_all = "snake_case")`, producing different diagnostic values across feature modes. That broad-derive approach is being reverted.
- **Default-library risk:** The frozen source itself has ungated diagnostic capture calling feature-gated getters, and native soil admission calling feature-gated validation. The earlier reported “default check” included the persisted-restart package, enabling feature unification; it did not prove standalone default-library compilation. This needs a narrow repair and direct check.
- The owned archive wire is used by persisted decoding; ordinary writing uses its separate borrowed projection. The current source needs reinspection once stable. The apparent duplicate raw test projection is intentionally independent of the production projection and is **not** a centralization finding.

Residual evidence needed: terminal diff and source identity; direct checks for both requested feature configurations and standalone default library; exact test membership including all 14 promotion selectors; actual no-feature recorder controls and affected negative tests; focused full-feature diagnostic parity if projection code changes. I will verify fixes and give the final correctness verdict on the stable cut.
