Replacement reviewer: `/root/test_first_correctness`. This is an independent replacement conversation with no continuity claim to the prior reviewer.

Evidence class: **Static source review plus supplied execution evidence.** I ran no compiler, test, formatter, lint, capture, or probe command. Static review covered detached source `/workdir/openwepp-experiments/b01-wb14-cadence/current-context-capture-20260913`. Supplied `compiler-repair-check.json` binds source-tree SHA-256 `71567c133f3ac8ff43263eb1d8ca2037fbbbc6aeed21215d0b9df1833d25d9e3`, patch SHA-256 `a566ee09e5637b50df17427a684eb298c1ab5dafb9c2d7ef5aaaec3290cd3abf`, and unchanged source during execution.

Findings, ordered by severity:

- **HIGH — successful current-source compilation was not yet supplied.** The compiler-repair check failed with one E0282 at `snow_stage3_v11_current_context_capture.rs:2332`. The subsequent explicit `Stage3ArchivedReceiptPrefixV1` annotation is statically correct, but its running check was unavailable at this review boundary. The other five unique inherited compiler sites are statically corrected: qualification types use the existing crate re-export, archived-day vector inference is explicit, and the archive restorer retains the V11 error domain rather than inventing a broad V11-to-V10 conversion.

- **HIGH — the required connected fixture remains absent.** The only existing test at `v9_real_consumer_shadow_tests.rs:994` still calls `capture_row_for_test`, retains the live producer `shadow`, uses empty Stage-3 and parcel maps, supplies no prepared support, bypasses `finish_to_file`, and never calls `restore_provider_bound_context_from_file`. It cannot establish recorder-to-file behavior, producer-lifetime separation, owned restoration, native archive/prefix custody, or an independent producer comparison.

- **HIGH — the canonical cross-owner joins remain open.** `restore_captured_pre_child_context` restores the parent and clock but does not apply the exact staged-owner membership/digest/byte join represented by `snow_stage3_v11_owner_join.rs::exact_checkpoint_owner_join`. `import_and_reconstruct_pre_child_context` separately compares clock and consumer owners to file-supplied manifests at `snow_stage3_v11_current_context_capture.rs:1724`, without joining the consumer projection directly to the unique clock owners. The Stage-3 map, pending parcels, lane/tile receipts, and snow-enthalpy material owner likewise are not reconstructed into the corresponding canonical clock owner. A consistently substituted bundle can remain internally self-consistent at these seams.

- **HIGH — provider/consumer chronology remains self-supplied.** `restore_provider_bound_context_from_rows` restores the prepared day independently, but the consumer GSI state, cursor, installed receipt, and scheduler posture are still restored from the target row without a phase-specific equality join to the provider day’s authoritative beginning/current position. The comparison at `snow_stage3_v11_current_context_capture.rs:1733` only compares the restored consumer cursor with the same target-row bytes.

- **MEDIUM — the outer native reader still flattens typed failures.** Archive parsing and `restore_provider_bound_context_from_file` return `Result<_, String>` and convert typed V11, provider, chronology, and serialization failures with `to_string()`. The compiler repair preserves the V11 taxonomy within `restart_authority_restore_archived_publication_day_v1`, but the declared reader boundary still cannot support stable predicate-specific error assertions.

- **MEDIUM — substantial mirrored restoration logic remains without parity evidence.** The dynamic frame transport, native consumer field transport, committed-day frame parser, archive-history reconstruction, and active-snow DTOs mirror canonical structures across modules. Explicit justification, independent producer equality, and distinguishing poisons have not been supplied.

Legitimate existing local fixture pieces are available: `native_v2_shadow_for_parent` in `v10_soil_thermal_v2_tests.rs:67`, `capture_prepared_day_row_for_test` at `snow_stage3_v11_current_context_capture.rs:1466`, the real `begin_spooled`/`finish_to_file` recorder path at `snow_accuracy_observer.rs:190` and `:203`, and committed-day archive acknowledgement at `snow_stage3_v11_attachment_runtime.rs:1522`. Their existence supports local fixture feasibility but is not acceptance evidence until one source-valid test composes the genuine producer, archive lifecycle, recorder file, producer drop, and owned reader.

Residual risk and missing tests: no connected native positive, separate V1 refusal, exact selected/executed case count, independent expected-value comparison, producer-lifetime proof, cross-owner/provider chronology poisons, 48-support omission/duplicate/reorder/wrong-day controls, archive/prefix/receipt/history poisons, parcel/lane controls, zero-mutation/custody audit, disabled/non-target/once-only recording, diagnostic-failure isolation, formatter result, or matched owning/runner lint was supplied.

Interim verdict: **HOLD / NOT ACCEPTED.** The compiler repairs appear directionally correct, but this is not a frozen fixture review and no acceptance-bearing behavior has executed.

Correction appendix — 2026-09-14 compiler repair result

Supplied `prefix-type-check.json` records **PASS, exit 0**, from `2026-09-14T06:34:02.492122+00:00` through `2026-09-14T06:34:29.273583+00:00`, with source unchanged during execution. It binds detached source-tree SHA-256 `6dfc03ab36d4f178a6d0ddb9242f678240d1c2e79699a6bf123f13db1316d72d` and patch SHA-256 `421e55e15c02df1c38e3ecd20dcf2ace0e2409c61233d5fad3c0c63752ebe316`. The original six source-site compiler diagnostics are addressed, and the newly exposed archive-prefix E0282 is fixed by the explicit `Stage3ArchivedReceiptPrefixV1` annotation. This closes only the compiler finding above. The connected fixture and all remaining correctness findings, controls, and behavioral evidence remain open.

Bounded pre-execution fixture review — 2026-09-14

Evidence class: **Static frozen-source review plus supplied list evidence.** I ran no command. Supplied `native-fixture-list.json` records `cargo nextest list` **PASS, exit 0**, from `2026-09-14T06:43:50.094801+00:00` through `2026-09-14T06:44:58.008127+00:00`, with source unchanged during the command. It binds detached source-tree SHA-256 `a4a9e7eaec17537510c70e6d2375786b41699f90c58e2f33724319039484a2f3` and patch SHA-256 `3bc7d047ec8f8d08f80277a15bac4e47789a983d04705df1d09fd91789e6513c`. The new exact test is source-valid and non-ignored; list evidence is not execution evidence.

Findings, ordered by severity:

- **HIGH — the positive has a deterministic publication-lifecycle failure before its first archive.** `v9_real_consumer_shadow_tests.rs:1076-1081` calls `stage_prepared_day` and then `commit_staged_day` directly. `snow_stage3_v11_attachment_runtime.rs:568-587` shows staging only executes and stores `pending_candidate`; `commit_staged_day` requires `pending_publication_day` and returns `missing completed Stage-3 publication day` at lines 601-604. The only setter is `complete_pending_publication_day` at lines 720-741, used by the production scheduler after `Stage3AcceptedPublicationDayV1::try_complete` at `direct_runtime/snow_stage3_v11_scheduler.rs:451-486`. The test therefore cannot reach qualification, archive writing, acknowledgement, target recording, file restoration, or its assertions as frozen.

- **HIGH — even after that lifecycle blocker, the positive does not independently compare the restored consumer or provisional operands with producer evidence retained before producer drop.** It retains only the expected archive prefix. Assertions at `v9_real_consumer_shadow_tests.rs:1152-1160` check day/count/presence and `diagnostic_native_soil_v2().is_ok()`, but do not compare canonical consumer owners, provider/GSI position, reconstructed provisional reduction/ledger/receipt/binding, Stage-3/parcels, or snow-enthalpy owner with independently retained producer values. A well-formed but wrong restored context could satisfy the current positive assertions.

- **HIGH — the earlier cross-owner and provider-chronology findings remain acceptance blockers.** The new fixture exercises those paths only if it reaches the reader; it adds no distinguishing foreign parent/clock/consumer/Stage-3 owner or provider/GSI/cursor poison and therefore cannot close the self-supplied joins identified above.

- **MEDIUM — the negative is a missing-field native-litter control, not evidence for a complete structurally valid V1 capture.** It changes only `native_consumer_constructor_operands.frozen_litter_residents` to `null` and asserts a `frozen-litter` substring. This is useful predicate-specific omission evidence if executed, but the broader V1/non-native refusal claim must remain bounded to exactly that mutation unless a genuine V1/non-native capture is exercised.

Static fixture feasibility: the corrected test now uses `native_v2_shadow_for_parent` only for its `DirectV10RealConsumerShadow`, obtains an independent `EndpointFixture`, installs the existing paired V3/V4 fixture, uses actual provider-day preparation and Stage-3 execution, streams canonical archive bytes through the existing archive writer, drops the producer scope before restore, writes through `snow_accuracy_observer::finish_to_file`, and calls the actual owned file reader. Those are legitimate local components. They do not cure the omitted publication completion or supply the missing independent comparisons and controls.

Pre-execution verdict: **HOLD / DO NOT CLAIM POSITIVE EVIDENCE.** The frozen test compiles and lists, but its first `commit_staged_day` must fail under the source-defined lifecycle. If executed unchanged, that first downstream failure is diagnostic evidence only under the adopted stopping rule.

Final affected correctness disposition — hard-stop source, 2026-09-14

Evidence class: **Static frozen-source review plus supplied execution evidence.** I ran no compiler, test, formatter, lint, capture, or probe command. Supplied evidence binds detached source-tree SHA-256 `3ddd919cf5857dfc4e2c9cd74078de2f23f3c4b1c394e127f77589f291368a0a` and patch SHA-256 `b1d4a1d80cc834f4c25206d39bfceeaba1ff4c60bdeda232338d615ca06c7b55`, with source unchanged during each command. This replacement review makes no continuity claim with the prior reviewer.

Findings, ordered by severity:

- **HIGH — the connected recorder-file-owned-reader positive failed in fixture setup and supplies no restoration evidence.** Supplied `native-fixture-run.json` records **FAIL, exit 100**, from `2026-09-14T06:53:23.767883+00:00` through `2026-09-14T06:53:24.418092+00:00`: one exact test selected and executed, zero passed, one failed, and 1,468 skipped. `v9_real_consumer_shadow_tests.rs:1050-1064` expands the diagnostic hydrology constructor to five days by calling `lane.day_inputs.first().expect("native hydrology seed day")`; the existing constructor has an empty `day_inputs` vector, so the test panics at line 1062. Execution therefore never reaches provider-day preparation, Stage-3 staging/publication/archive, the canonical archive writer, observation-file completion, producer drop, or `restore_provider_bound_context_from_file`. This is a fixture setup/integration failure, not a restoration, serialization, chronology, or physical-model failure. My earlier static review did not detect the empty seed. The failure does not establish that a valid local fixture is impossible.

- **HIGH — the canonical cross-owner joins remain open.** `restore_captured_pre_child_context` restores parent and clock state without applying the exact checkpoint staged-owner membership/digest/byte join represented by `snow_stage3_v11_owner_join.rs::exact_checkpoint_owner_join`. `import_and_reconstruct_pre_child_context` compares consumer and clock owners separately with file manifests, rather than joining the consumer projection directly to the unique clock owners. Stage-3 maps, pending parcels, lane/tile receipts, and snow-enthalpy ownership are likewise not reconstructed into their corresponding canonical clock owner. The failed fixture supplies no poison or independent equality evidence to close these seams.

- **HIGH — provider/consumer chronology remains self-supplied.** The prepared provider day is restored independently, while consumer GSI state, cursor, installed receipt, and scheduler posture are restored from the target row without a phase-specific equality join to the provider day's authoritative beginning/current position. The failed fixture never reached this seam and supplies no provider/GSI/cursor poison.

- **HIGH — independent producer operands and exact restored values remain untested.** The positive retains an archive prefix, but its assertions do not independently compare the restored canonical consumer owners, provider/GSI position, provisional reduction/ledger/receipt/binding, Stage-3/parcels, or snow-enthalpy owner with producer values captured before producer drop. Because the test failed during hydrology setup, none of its existing assertions executed.

- **HIGH — the required negative and distinguishing controls remain absent as executed evidence.** No separate V1 refusal passed. The drafted null `frozen_litter_residents` mutation, if retained, is a useful missing-field native-litter control but is not a structurally complete V1/non-native capture. Cross-owner, provider chronology, 48-support membership/order/day, archive/prefix/receipt/history, parcel/lane, once-only, and custody controls remain unsupplied.

- **MEDIUM — the outer native reader still flattens typed failures.** Archive parsing and `restore_provider_bound_context_from_file` return `Result<_, String>` and stringify typed V11, provider, chronology, and serialization errors. The corrected internal V11 restorer taxonomy does not preserve stable predicate identity across the declared reader boundary.

- **MEDIUM — substantial mirrored restoration logic remains without parity evidence.** Dynamic frame transport, native consumer fields, committed-day parsing, archive-history reconstruction, and active-snow DTOs mirror canonical structures across modules. No explicit justification, independent producer equality evidence, or distinguishing poison suite was supplied.

Reconciled fixes and supplied passing evidence:

- The original six compiler-site diagnostics and the subsequently exposed archive-prefix E0282 are closed by supplied `prefix-type-check.json`: **PASS, exit 0**, source unchanged.
- The prior deterministic publication-lifecycle finding is statically corrected. The final fixture calls `restart_authority_complete_commit_and_stage_archive_day_v3`, whose existing implementation completes the accepted publication day, commits it, builds the qualification delta, and stages its archive. Execution did not reach this helper.
- Supplied `fixture-format-check.json` records **PASS, exit 0**, and `native-fixture-final-list.json` records **PASS, exit 0** for the exact non-ignored test, both on the final unchanged source. Formatting and listing do not establish behavior.
- The final test statically composes legitimate existing provider preparation, Stage-3 execution, canonical archive writing, recorder file completion, producer-scope drop, and owned file-reader components. Its empty diagnostic hydrology seed prevents that composition from executing.

Residual risk and missing tests: the connected native positive and separate V1 refusal have not passed. There is no executed independent expected-value comparison or owned-reader lifetime proof; no cross-owner/provider chronology poisons; no 48-support omission, duplicate, reorder, or wrong-day controls; no archive/prefix/receipt/history, parcel, or lane poisons; no zero-mutation/custody audit; no disabled/non-target/once-only recorder case; no diagnostic-failure isolation; and no matched owning/runner lint evidence. The exact target uniqueness and adaptive lifecycle remain unexecuted. The stopping rule is exhausted at 18 of 18 corrective checks, so no correction or further check is available in this package run.

Final verdict: **HOLD / NOT ACCEPTED.** The supplied run is useful diagnostic evidence for an empty fixture seed only. It supplies zero acceptance-bearing evidence for recorder-to-file behavior, producer-lifetime separation, native archive/prefix custody, owned restoration, or restored physical/chronological correctness. The milestone remains incomplete; no broader impossibility conclusion follows from this setup failure.
