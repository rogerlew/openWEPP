## Replacement QA preparation — 2026-09-14

Reviewer: `/root/test_first_qa`, an independent replacement for genuinely unavailable `/root/integration_qa`; new conversation and no continuity claim. Scope: test-first native recorder/file/owned-reader fixture, predicate inventory, producer lifetime, zero-physics/state/observation isolation, source custody, and required quality evidence. **Static:** inspected the owner-adopted authorization, the current package section, `role-review.md`, applicable gate strategy, retained `artifacts/native-reader-integration-20260914/qa-review.txt` including its correction appendix, supplied compiler records, and the mutable detached source. No build, formatter, test, lint, deny, capture, or probe command was run by this reviewer.

### Retained findings and corrections

The prior QA review remains retained evidence, not this reviewer's continuity claim. Its final HOLD findings remain open: no connected file-reader behavior; no predicate-specific poison reachability or state/observer isolation; failed/incomplete final compilation and absent matched owning/runner lint. Its correction remains binding: only `terminal-format-check.json`, not the preceding mutating formatter record, proves formatting for its historical frozen source. The earlier witness/helper work is closed as a witness only and is excluded from re-review.

### Current preparation findings

1. **HIGH — the fixture must exercise the actual recorder and outer file reader after producer lifetime ends** — `crates/openwepp-hillslope-orchestrator/src/snow_accuracy_observer.rs`; `snow_stage3_v11_current_context_capture.rs:2464`; `v9_real_consumer_shadow_tests.rs:994`. The current test still uses `capture_row_for_test` and then the non-file importer with live `&shadow`. It cannot satisfy the adopted `producer -> actual recorder/file -> producer lifetime end -> restore_provider_bound_context_from_file` predicate. `restore_provider_bound_context_from_file` has the correct narrow reader shape: path plus static context and immutable provider/LSE/frozen-litter/hydrology pins, with no producer argument. The positive fixture must obtain its V2 bundle via `begin_spooled`/actual recording and `finish_to_file`, retain expected canonical/provisional values before producer scope ends, then invoke that reader only after the producer-owned objects are out of scope.

2. **HIGH — positive acceptance needs independent results and zero-physics evidence** — `snow_stage3_v11_current_context_capture.rs:2464`; `v9_real_consumer_shadow_tests.rs:994`. A successful return alone does not prove restored context integrity. The fixture must compare independently retained expected canonical values and producer provisional operands to restored values, and run the covered-native physical-path audit around import, asserting all three physics counters remain zero. It must also snapshot relevant accepted clock/state, parent, consumer, ProducedUnconsumed custody, and observation contents before/after restoration or otherwise prove no mutation. The retained three-counter assertion alone is insufficient.

3. **MEDIUM — controls must name their predicate and reach it** — `v9_real_consumer_shadow_tests.rs:994`; `snow_stage3_v11_current_context_capture.rs:2464`. The current missing-LSE and wrong-run tests assert only `is_err()`. The next cut needs a separate V1/non-native refusal and selected one-field-at-a-time poisons for the admitted provider/support and owner/custody boundaries, each asserting its owning error/predicate so parser failure or an unrelated early guard cannot count. Required recording controls remain disabled/non-target no row, target once-only row, and diagnostic recording failure without model-decision mutation.

4. **MEDIUM — source remains mutable; no acceptance evidence exists for the fixture yet** — `compiler-repair-check.json`; `prefix-type-check.json`. The supplied compiler-repair check is `FAIL` exit 101 at source SHA `71567c…`, while the prefix type check is `PASS` exit 0 at distinct SHA `6dfc03…`; both bind unchanged source during their own commands. The latter is development-only type evidence with zero behavioral cases. Neither substitutes for the final frozen fixture result, focused test/list counts, format check, matched owning/runner Clippy, or exact-diff reconciliation. `cargo deny` is conditionally inapplicable only if the final reconciliation again confirms unchanged manifest/lockfile/toolchain/resolution inputs.

### QA preparation verdict

**HOLD / NOT ACCEPTED while source is mutable.** The required fixture shape and evidence predicates are now explicit. Final QA review must bind the writer's frozen source and parent-supplied command results; it will recheck the actual fixture and evidence rather than accept this preparation as source acceptance.

## Final QA disposition — 2026-09-14

Reviewer: `/root/test_first_qa`, the same independent replacement scope named above. **Static plus supplied execution evidence:** reviewed frozen detached source-tree SHA-256 `3ddd919cf5857dfc4e2c9cd74078de2f23f3c4b1c394e127f77589f291368a0a`, the exact selected-test list/run records, and formatting records. No reviewer command or source/package edit was performed.

### Findings

1. **HIGH — the connected fixture did not reach its claimed recorder/file/reader behavior** — `crates/openwepp-hillslope-orchestrator/src/v9_real_consumer_shadow_tests.rs:1059`; `native-fixture-run.json`. The selected non-ignored test ran once and failed (exit 100, 06:53:23–06:53:24 UTC; frozen source unchanged) at `lane.day_inputs.first().expect("native hydrology seed day")`. This occurs before day staging, commit/archive, `finish_to_file`, producer-scope exit, file reading, zero-physics audit, and the V1/non-native control. Thus it is a local fixture setup/integration failure, not successful restoration or a physics result. The adopted hard stop is reached at **18/18 unsuccessful corrections**; no repair or further command is authorized.

2. **HIGH — required independent restoration comparison and cross-owner/provider evidence remain absent** — `v9_real_consumer_shadow_tests.rs:1183-1229`; `snow_stage3_v11_current_context_capture.rs`. The frozen source contains a substantially improved intended path: it uses real committed-day archival records, `finish_to_file`, producer lifetime separation, and the owned reader with pins. It independently retains and compares only `expected_prefix`. It does not retain/compare full producer provisional operands or canonical consumer, parent/clock, Stage-3, pending-parcel, snow-enthalpy owner, provider/GSI/cursor, receipt, ledger, and binding results. It also lacks distinguishable owner/provider chronology poison coverage. Since execution stopped before the reader, even its narrower static assertions have no behavioral evidence.

3. **MEDIUM — negative and isolation coverage remains incomplete and unexecuted** — `v9_real_consumer_shadow_tests.rs:1200-1264`. The planned V1/non-native control changes `frozen_litter_residents` to `null` and asserts a `frozen-litter` error substring. That is useful narrow predicate coverage, but it is not a complete structurally valid V1 capture. Broader one-field poison reachability, clock/state/parent/consumer/ProducedUnconsumed custody and observer immutability, disabled/non-target, once-only, and diagnostic-error/model-decision isolation remain unimplemented or unexecuted.

4. **MEDIUM — only final formatting and test discoverability are evidenced; source-quality gates remain open** — `fixture-format-check.json`; `native-fixture-final-list.json`; `native-fixture-run.stderr`. The nonmutating formatter check passed (exit 0, 06:51:51–06:51:53 UTC) against frozen `3ddd…` with source unchanged. The exact selected test list passed (exit 0, one non-ignored matching case) against the same source. The run/list compiler output reports eight dead-code warnings in the new restoration surface; no warnings-denied matched owning-crate Clippy or separate runner Clippy was run, and no exact-diff lint attribution exists. `cargo deny` remains unrun and is only conditionally inapplicable if final reconciliation proves unchanged manifests, lockfile, toolchain, and resolution inputs.

### Non-blocking debt / follow-ups

- If new authority later permits repair, retain independent expected values for every restoration authority used by the positive and compare them after the producer scope ends.
- Keep the native-litter omission control predicate-specific, and add valid V1/non-native plus owner/provider/custody poison cases that cannot be satisfied by an earlier parser failure.

### QA verdict

**HOLD / NOT ACCEPTED.** The frozen fixture is a material structural improvement over the retired live-helper witness and formatting/list evidence is valid for its exact source. Its only execution failed before any connected native recorder/file/owned-reader behavior, all required controls and matched lint remain unexecuted, and the hard stop prohibits further work. No authentic capture/probe was run or authorized.
