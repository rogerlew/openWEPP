Replacement reviewer: `/root/integration_correctness` (the retained `/root/native_correctness` conversation was unavailable). This is a distinct independent correctness conversation. The predecessor's original findings and frozen-terminal response remain unchanged in `artifacts/native-file-roundtrip-20260914/correctness-review.txt` and are carried into this review.

Initial current-cut assessment — 2026-09-14T05:21:30Z

Evidence class: **Static source review only.** No build, test, formatter, lint, capture, or probe command was run. This assessment binds the inspected files to SHA-256 `630970749622f15d9bbec38abe57e45912cf434bab348b6351a2294f016dfc85` (`snow_stage3_v11_current_context_capture.rs`), `efc007918a52232fc5fcf5353d7a1e335943c62d83aba2246095abd43ffaa654` (`v9_real_consumer_shadow.rs`), and `ebd0d675b423d62a3b89c47ef941700f313da23225080855566c1e0a888294ab` (`v9_real_consumer_shadow_tests.rs`). The writer remained active, so this is not a frozen final review or fix acceptance.

Findings, ordered by severity:

- **HIGH — the four native type-boundary compiler failures remain in the inspected cut.** [`v9_real_consumer_shadow.rs:475`](/workdir/openwepp-experiments/b01-wb14-cadence/current-context-capture-20260913/crates/openwepp-hillslope-orchestrator/src/v9_real_consumer_shadow.rs:475) still passes `DirectSurfaceLiquidConfiguration`, GSI state, provider static configuration, and provider cursor through generic `serde_json::json!` / `from_value` at lines 475–584. These are the same non-Serde boundaries recorded by the retained terminal report. No successful current-source compile evidence exists.

- **HIGH — frozen-litter V3/V4 custody is still absent.** [`capture_row`](/workdir/openwepp-experiments/b01-wb14-cadence/current-context-capture-20260913/crates/openwepp-hillslope-orchestrator/src/snow_stage3_v11_current_context_capture.rs:1226) transports neither resident, and [`diagnostic_restore_native_consumer_v1`](/workdir/openwepp-experiments/b01-wb14-cadence/current-context-capture-20260913/crates/openwepp-hillslope-orchestrator/src/v9_real_consumer_shadow.rs:501) returns the canonical constructor's `None` defaults. The restored consumer therefore cannot reproduce the target composite LSE owner when native frozen-litter custody is active.

- **HIGH — active-lane admission remains a reduced duplicate fed by file state.** [`restore_provider_bound_context_from_rows`](/workdir/openwepp-experiments/b01-wb14-cadence/current-context-capture-20260913/crates/openwepp-hillslope-orchestrator/src/snow_stage3_v11_current_context_capture.rs:1958) recomputes lanes with a local resolved/terminal/snowfall predicate, then calls `retain_active_snow_lanes`. It does not first authenticate the complete Stage-3 snow owner against the clock, including canonical topology, pending parcels, lane receipts, and tile receipts. A consistently substituted Stage-3 map can still select a self-consistent reduced support.

- **HIGH — pending terminal-parcel restoration is incomplete.** [`restore_captured_pre_child_context`](/workdir/openwepp-experiments/b01-wb14-cadence/current-context-capture-20260913/crates/openwepp-hillslope-orchestrator/src/snow_stage3_v11_current_context_capture.rs:1765) checks posture, parent, support bounds, map key, and parcel digest only. It does not join parcel receiver topology and forcing/admission to the restored prepared support or reconstruct the complete canonical Stage-3 snow owner with lane/tile receipts.

- **HIGH — attachment/history custody remains absent from the returned context.** [`RestoredCapturedPreChildContextV1`](/workdir/openwepp-experiments/b01-wb14-cadence/current-context-capture-20260913/crates/openwepp-hillslope-orchestrator/src/snow_stage3_v11_current_context_capture.rs:1700) retains parent, clock, Stage-3 map, parcel map, and provisional values, but no attachment receipt chain/archive prefix, lane/tile receipt maps, or snow-enthalpy chronology. This does not satisfy the exact chronology obligations in `SC-COUPLEDTIME-001` INV-COUPLEDTIME-024/026 or the retained package acceptance.

- **MEDIUM — the result still duplicates mutable provider custody.** [`RestoredProviderBoundContextV1`](/workdir/openwepp-experiments/b01-wb14-cadence/current-context-capture-20260913/crates/openwepp-hillslope-orchestrator/src/snow_stage3_v11_current_context_capture.rs:1680) owns both `provider` and `prepared_day`, although the validated prepared day owns the provider day. The two copies may diverge after return.

- **MEDIUM — substantial manual mirror logic remains without parity evidence.** The dynamic hydrology-frame DTO/restorer, active-snow scalar/enum DTO, native consumer field map, and active-lane predicate duplicate canonical owner structure across modules. Under the repository duplication policy this requires explicit justification, independent producer equality, and distinguishing poisons; none is executed on this cut.

Reconciled closed or materially improved predecessor findings:

- Scheduler count, provider GSI receipt, and reader-call wiring remain present.
- Parent evaluator-cache replay is now performed through `restore_evaluator_cache_from_accepted_slabs` at [`snow_stage3_v11_current_context_capture.rs:1757`](/workdir/openwepp-experiments/b01-wb14-cadence/current-context-capture-20260913/crates/openwepp-hillslope-orchestrator/src/snow_stage3_v11_current_context_capture.rs:1757). No executed poison or history-rich positive establishes the complete parent/clock join yet.

Residual risk and missing tests: the only added connected test in the inspected source still uses `capture_row_for_test` and the same live `shadow`, with empty Stage-3/parcels and no prepared support. There is no recorder -> observation file -> owned reader test, producer-lifetime separation, independently pinned expected consumer comparison, V1/non-native refusal, native frozen-litter target, nonempty accepted-history/cache case, archive/prefix/lane/tile/snow-enthalpy custody, parcel/active-lane poison matrix, or current-source lint evidence. Its zero-physics audit covers three counters around an in-memory importer only and does not prove the full file reader preserves accepted state/custody.

Initial verdict: **HOLD / NOT ACCEPTED.** This is an intermediate cut. A later response must review the frozen final source and actual command evidence; none of the open findings is waived by this assessment.

Final affected correctness disposition — frozen terminal cut, 2026-09-14

Reviewer: `/root/integration_correctness`, reusing the independent replacement conversation above. Evidence class: **Static frozen-source review plus supplied execution logs.** I ran no compiler, test, formatter, lint, capture, or probe command. This review binds source-tree SHA-256 `f45fb8b230d6b4b0acd7ecf944e555519467d16250cd245bb988c469dbe76721` and patch SHA-256 `d87321eb9cefc58b53f9c7f580b8f5930a0bf3836e3d28a35ed8ca62d7b70245`, as recorded by the unchanged-source terminal format check and terminal type check.

Findings, ordered by severity:

- **HIGH — the frozen source does not compile.** `terminal-check.json` records exit 101 at 06:05:17 UTC with source unchanged. The six unique diagnostics are two E0433 references to a nonexistent crate-root `snow_stage3_v11_production_qualification` module in [`snow_stage3_v11_current_context_capture.rs:1793`](/workdir/openwepp-experiments/b01-wb14-cadence/current-context-capture-20260913/crates/openwepp-hillslope-orchestrator/src/snow_stage3_v11_current_context_capture.rs:1793) and line 1889, three E0277 failures where [`v9_real_consumer_shadow_publication_retention.rs:1793`](/workdir/openwepp-experiments/b01-wb14-cadence/current-context-capture-20260913/crates/openwepp-hillslope-orchestrator/src/v9_real_consumer_shadow_publication_retention.rs:1793) and lines 1812–1813 try to propagate `DirectV11RealConsumerError` through `DirectV10RealConsumerError`, and one E0282 missing vector element type at [`snow_stage3_v11_current_context_capture.rs:2360`](/workdir/openwepp-experiments/b01-wb14-cadence/current-context-capture-20260913/crates/openwepp-hillslope-orchestrator/src/snow_stage3_v11_current_context_capture.rs:2360). Repeated lib/lib-test reports are not additional unique errors. Failure 16/16 stops correction; zero behavioral cases executed.

- **HIGH — parent, clock, consumer, and Stage-3 custody are not joined into one authenticated owner set.** [`restore_captured_pre_child_context`](/workdir/openwepp-experiments/b01-wb14-cadence/current-context-capture-20260913/crates/openwepp-hillslope-orchestrator/src/snow_stage3_v11_current_context_capture.rs:1988) checks the parent transaction ID and replays the evaluator cache, but does not perform the canonical checkpoint staged-owner/clock byte join used by `exact_checkpoint_owner_join`. Later, [`import_and_reconstruct_pre_child_context`](/workdir/openwepp-experiments/b01-wb14-cadence/current-context-capture-20260913/crates/openwepp-hillslope-orchestrator/src/snow_stage3_v11_current_context_capture.rs:1713) separately compares restored clock owners and reconstructed consumer owners to two manifests from the same file, without comparing those owner values to each other. The decoded `beginning_stage3` plus pending parcels are likewise never reconstructed with lane/tile receipts and compared to the authenticated clock's canonical snow owner. A consistently substituted file bundle can therefore retain mutually inconsistent current owners while satisfying the local self-comparisons.

- **HIGH — current consumer provider/GSI position is not joined to the restored provider day's beginning authority.** [`restore_provider_bound_context_from_rows`](/workdir/openwepp-experiments/b01-wb14-cadence/current-context-capture-20260913/crates/openwepp-hillslope-orchestrator/src/snow_stage3_v11_current_context_capture.rs:2109) canonically restores the independently pinned provider day. The native consumer is later restored from its separate file-supplied GSI state and cursor, but the reader never requires them to equal that provider day's beginning GSI state/cursor. The only cursor comparison at lines 1733–1739 compares the reconstructed consumer to the same target-row bytes. This leaves the provider/consumer day-position seam self-supplied.

- **HIGH — the required actual file round-trip and distinguishing controls are absent.** The only added test remains [`b01_current_context_importer_restores_fixture_without_physics`](/workdir/openwepp-experiments/b01-wb14-cadence/current-context-capture-20260913/crates/openwepp-hillslope-orchestrator/src/v9_real_consumer_shadow_tests.rs:994), which constructs an in-memory row with `capture_row_for_test`, passes the live producer shadow to the older importer, and uses empty Stage-3/parcels with no prepared support. It does not exercise recorder -> observation file -> `restore_provider_bound_context_from_file`, end producer lifetime, use immutable pins only, compare independently retained producer values, or cover native frozen-litter/archive/prefix custody. No V1 refusal, 48-support omission/duplicate/reorder/wrong-day, parent/clock/prefix/binding, active-lane/parcel, target/duplicate/conflict, zero-mutation, disabled/non-target/once-only, or diagnostic-error isolation case exists or ran.

- **MEDIUM — diagnostic error taxonomy remains flattened and is already inconsistent at the archive seam.** The outer reader and archive decoder return `Result<_, String>` and repeatedly convert typed domain errors with `to_string()`. The terminal E0277 failures show an unresolved V11-to-V10 taxonomy boundary in the new publication archive adapter. This prevents stable typed distinctions for configuration, chronology, archive, parcel, and serialization failures and makes predicate-specific poison evidence impossible at the declared API.

- **MEDIUM — substantial mirrored restoration remains without parity evidence.** The manual dynamic-frame DTO, active-snow scalar/enum transport, native consumer field transport, committed-day frame parser, and publication-history reconstructor mirror canonical structures across modules. The active-lane predicate itself is now centralized, which closes that duplication finding, but the remaining mirrors can still drift silently. No explicit retained justification plus independent producer equality and distinguishing poison evidence exists for the frozen cut.

Reconciled fixes and improvements:

- The four original generic-Serde type boundaries now use the existing canonical surface bytes, GSI projection/restore, provider-configuration representation, and provider-cursor bytes. The earlier adapter-only check passed on an earlier source; the frozen combined check fails later integration code, so this is static fix evidence only.
- [`diagnostic_restore_native_consumer_v1`](/workdir/openwepp-experiments/b01-wb14-cadence/current-context-capture-20260913/crates/openwepp-hillslope-orchestrator/src/v9_real_consumer_shadow.rs:576) now requires and restores paired frozen-litter V3/V4 residents, joins independently pinned main/frozen LSE and surface configuration, restores their publication/WB14 history, and installs them through the canonical restored-resident validator.
- Current native soil and committed bootstrap soil remain distinct inputs and pass through `NativeSoilRestartAdmissionV1::authenticate`; scheduler count, provider GSI receipt, parent evaluator-cache replay, and reader wiring remain present.
- Active-lane selection now calls the production `restart_authority_state_derived_active_snow_lanes_v1` seam, and the returned context no longer duplicates the provider beside the prepared day.
- Archive records are retained as authenticated decoded parent/publication/qualification values, the manifest and prefix are joined to the committed day-start clock, and snow-enthalpy owner chronology is reconstructed and retained. These paths are uncompiled and unexecuted, so no behavioral fix acceptance is claimed.
- `terminal-format-check.json` is a valid nonmutating rustfmt PASS on the frozen source. Recorder helpers statically report diagnostic failures instead of propagating them into model decisions, but the required isolation controls did not run.

Residual risk and missing evidence: no successful frozen-source compilation, connected positive test, independent producer comparison, poison matrix, zero-work/state/custody proof, matched owning-crate lint, separate runner lint, capture, or probe. The archive semantic decoder and frozen-litter restoration have no executed source-valid positive or negative evidence. Authentic capture/probe remain outside this milestone and at 0/1.

Final verdict: **HOLD / NOT ACCEPTED.** The milestone is incomplete because the frozen source fails compilation, required canonical cross-owner joins remain absent, and all acceptance-bearing reader behavior is untested.
