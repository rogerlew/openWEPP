## Replacement QA review — 2026-09-14

Reviewer: `/root/integration_qa` (independent replacement for unavailable `/root/native_qa`; new conversation, no continuity claim). Scope: diagnostic native-reader evidence, test independence and poison reachability, zero-work/mutation/observation isolation, changed-source scope, and final source-quality evidence. **Static:** inspected the adopted authorization, current package record, retained native-roundtrip artifacts, exact current detached source, and `role-review.md`. No build, formatter, test, lint, cargo-deny, capture, or probe command was run by this reviewer.

### Findings

1. **HIGH — current source has no immutable successful evidence identity** — `artifacts/native-file-roundtrip-20260914/native-format-check.json`, `native-terminal-check.json`; `/workdir/openwepp-experiments/b01-wb14-cadence/current-context-capture-20260913/crates/openwepp-hillslope-orchestrator/src/snow_stage3_v11_current_context_capture.rs`; `v9_real_consumer_shadow.rs`. The retained format PASS and type-check FAIL bind source-tree SHA-256 `d9ce…b93fa` and patch `37a9…ce526`; the former is expressly a terminal, failed cut. The currently inspected reader and consumer file hashes (`37f9…b898`, `6bdc…dd48`) differ from the corresponding terminal identities (`6309…c85`, `efc0…654`). Therefore neither retained format PASS nor the failed check supplies final evidence for the writer's mutable adapter correction, and no behavior/gate outcome may be reused for it.

2. **HIGH — the available test does not establish the required file-only, independent native-reader path** — `crates/openwepp-hillslope-orchestrator/src/v9_real_consumer_shadow_tests.rs` (`b01_current_context_importer_restores_fixture_without_physics`); `crates/openwepp-hillslope-orchestrator/src/snow_stage3_v11_current_context_capture.rs` (`capture_row_for_test`, `import_and_reconstruct_pre_child_context`, `restore_provider_bound_context_from_file`). The test creates a JSON row through the test-only helper and calls the non-file importer while passing the live producer `&shadow`. It neither writes/reads the observation bundle through `restore_provider_bound_context_from_file` nor drops producer objects before restoration. It consequently cannot prove independent producer agreement, pinned-only construction, or file-reader compatibility. The package record correctly marks behavioral cases zero; this source shape must not be represented as a round-trip test.

3. **HIGH — zero-work and import isolation proof is incomplete** — `crates/openwepp-hillslope-orchestrator/src/v9_real_consumer_shadow_tests.rs`; `crates/openwepp-hillslope-orchestrator/src/snow_stage3_v11_current_context_capture.rs`. The sole audit assertions cover three physical-call counters after the injected importer. They do not snapshot and compare accepted clock/state, parent, ProducedUnconsumed parcel custody, consumer state, or observation rows before/after import. No executed control demonstrates disabled/non-target zero recording, target once-only recording, or that recorder failure records diagnostic detail without altering the model decision. These are separate acceptance obligations in the adopted authorization.

4. **MEDIUM — negative coverage is too narrow and does not demonstrate intended predicate reachability** — `crates/openwepp-hillslope-orchestrator/src/v9_real_consumer_shadow_tests.rs`. The available mutations remove `lse_configuration_canonical_json` and alter `static_run_identity`, then only assert `is_err()`. There is no V1/non-native refusal, no provider/support (omission, duplicate, reorder, wrong-day) poison, and no parent/clock/prefix/binding, lane, parcel, target, duplicate, or conflict poison. No assertion identifies the expected typed/predicate failure, so a failure at an earlier deserialization or unrelated guard would satisfy the test. All are currently unexecuted in any event.

5. **MEDIUM — final source-quality and lint attribution remain unmet** — `artifacts/native-file-roundtrip-20260914/native-format-check.json`; `native-terminal-check.json`; `package.md`. A rustfmt `--check` PASS exists only for the superseded terminal identity; its all-targets type check then failed. There is no exact-current-source formatting result, no candidate matched owning-crate Clippy result, and no separate runner Clippy result matched to the same toolchain/features and attributed against the failed baseline. `cargo deny` is not selected by the retained terminal scope because manifests/lockfiles were unchanged, but that non-applicability must be reconfirmed against the final exact diff rather than inferred from the mutable source.

### Non-blocking debt / follow-ups

- Keep the private reader seam narrow and name each independently pinned constructor authority in the final test; do not make the live producer a reader parameter or a hidden fallback.
- A completed test should write the recorder's actual observation bundle, destroy producer-owned objects, load it with only immutable pins, compare independently retained expected canonical values, and assert nonzero selected/executed counts.
- Poisons should alter one admitted field at a time and assert the owning typed/predicate failure, with a reachability assertion where a shared early firewall could otherwise mask it.

### QA verdict

**HOLD / NOT ACCEPTED.** The retained record is valid evidence of the historical terminal failure and its exact identity, but it contains no valid final native-reader behavior, isolation, formatting, or matched-lint evidence for the current correction.

## Final QA/evidence disposition — 2026-09-14

Reviewer: `/root/integration_qa`, reusing the independent QA scope above. **Static plus supplied execution evidence:** inspected the frozen `f45fb8b230d6b4b0acd7ecf944e555519467d16250cd245bb988c469dbe76721` source record, terminal formatter records, supplied adapter and custody check records, package disposition, and current frozen source. No reviewer command, source edit, formatter, lint, test, capture, or probe was run.

### Findings

1. **HIGH — the native-reader milestone has no connected behavioral acceptance** — `crates/openwepp-hillslope-orchestrator/src/v9_real_consumer_shadow_tests.rs`; `crates/openwepp-hillslope-orchestrator/src/snow_stage3_v11_current_context_capture.rs`; `artifacts/native-reader-integration-20260914/terminal-check.json`. The available test remains a helper-built row passed to the non-file importer with the live producer shadow; it does not write/read the observation bundle via `restore_provider_bound_context_from_file`, retain independent expected values, end producer lifetime, or construct from immutable pins alone. No focused test result is retained for the frozen source, and the required connected positive fixture and controls are absent. This blocks the local milestone.

2. **HIGH — required poison reachability and observation/state isolation remain unproved** — `crates/openwepp-hillslope-orchestrator/src/v9_real_consumer_shadow_tests.rs`; `crates/openwepp-hillslope-orchestrator/src/snow_stage3_v11_current_context_capture.rs`. The sole available negative mutations only remove LSE bytes or change run identity and assert generic `is_err()`. There is no executed V1/non-native refusal; provider/support omission, duplicate, reorder, or wrong-day poison; parent/clock/prefix/binding, lane, parcel, target, duplicate, or conflict poison; or owning predicate assertion. The three physical-call counters do not prove unchanged accepted clock/state, parent, consumer, ProducedUnconsumed custody, or observer contents. Disabled/non-target, once-only target, and diagnostic-error/model-decision isolation controls are also absent.

3. **HIGH — current compilation and lint acceptance are unmet** — `artifacts/native-reader-integration-20260914/custody-check.json`; `terminal-check.json`; `adapter-check.json`. `custody-check` is a retained `FAIL` (exit 101) on its distinct `3bfd…0260` source and exhausts correction 15/16. The combined frozen-source owning-orchestrator plus runner type check was started on `f45fb8…76721` but has no exit/end/unchanged-source completion record yet, so its partial stderr is not a completed validation result. `adapter-check` passed on the earlier `1d51…503b` adapter cut, but its 24 warnings include unused native restoration/reader functions and it establishes neither reader use nor warnings-denied lint. Candidate matched Clippy and the required separate runner attribution are **NOT RUN**; prior failed baseline measurements do not fill either gap.

4. **MEDIUM — source-quality evidence is limited but formatting is correctly evidenced for the frozen cut** — `artifacts/native-reader-integration-20260914/terminal-format.json`; `terminal-format-check.json`. The mutating formatter and subsequent nonmutating `rustfmt --check` both record source identity `f45fb8…76721`; the latter passed with `source_unchanged_during_command: true`. This satisfies only formatting for its listed 15 files. It does not establish compilation, tests, or lint. The current exact scope includes the orchestrator diagnostic surfaces and `openwepp-runner/src/hillslope/05_runner_execution_and_outputs.rs`; no manifest, lockfile, toolchain, or dependency change is recorded, so cargo-deny remains not triggered by this diff, subject to terminal diff reconciliation.

5. **MEDIUM — freeze sequencing deviation remains material evidence debt** — `artifacts/native-reader-integration-20260914/custody-check.json`; `post-freeze-pin-edit.json`; `terminal-format-check.json`. After the custody-check failure, pin edits were preserved on `69ee…1c14` without an awaited command result. The later formatter/check establish the final `f45…76721` identity, but the adapter PASS and custody FAIL cannot be reused as final-cut results. The package must retain this sequence rather than describe development checks as final verification.

### Non-blocking debt / follow-ups

- If this work is resumed under new authority, make the file reader exercised entry point and pins explicit in a single narrow fixture; keep producer-derived objects out of the reader call after capture.
- Bind each poison to its expected owning guard/error so a prior parser failure cannot count as coverage.
- Retain a frozen final command/source record for every completed check and compare candidate lint diagnostics with baseline by target, code, span, and content, including the separate runner invocation.

### QA verdict

**HOLD / NOT ACCEPTED — local native milestone incomplete.** The frozen source has a valid formatting-check PASS. The adapter type-check PASS is valid only for its earlier cut. All required connected reader behavior, poison/isolation evidence, completed frozen-source compilation, and matched lint attribution remain unmet.

### Terminal-check completion (supersedes finding 3's pending-check sentence)

Ran (supplied record): `terminal-check.json` completed **FAIL exit 101**, 06:04:43–06:05:17 UTC, against frozen source `f45fb8…76721` and patch `d87321…70245`, with `source_unchanged_during_command: true`. Both lib and lib-test report six errors: two `E0433` unresolved production-qualification module references, three source-site `E0277` V11-to-V10 error-conversion failures, and one `E0282` unconstrained `Vec` annotation (the repeated stderr diagnostics cover both target kinds). This is failed correction **16/16**. No source fix, test, lint, or recheck follows. The QA verdict remains **HOLD / NOT ACCEPTED**.

### Formatter evidence correction

Finding 4's statement that both formatter records share `f45fb8…76721` is corrected. `terminal-format.json` is a mutating formatter PASS on pre-format source `c7ab4583a4a6dec4902dfc30e7aba8e6abaaf6147c3c595c2c94a9283729dd52`, with `source_unchanged_during_command: false`. Only the subsequent nonmutating `terminal-format-check.json` binds the final `f45fb8…76721` source and records PASS with `source_unchanged_during_command: true`. The valid final formatting conclusion and every other HOLD finding remain unchanged.
