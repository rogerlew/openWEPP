# B01-WB14-INGRESS-RED-REGRESSION — bounded continuation

DRAFT FOR OWNER ADOPTION. This document grants no execution permission until adopted.

## Active checkpoint, source, permitted work, acceptance and limits

**Same checkpoint:** `B01-WB14-INGRESS-RED-REGRESSION` in
`docs/work-packages/20260911-b01-wb14-verified-cadence-repair-001/package.md`.
Finish the existing test-only deliverable; do not create another package or resume production cadence correction.

**Evidence cut:** `c21dfe91b0962e28febccf427b430486e06bc355` in `rogerlew/openWEPP`.
This is the repository evidence/governance revision, not the detached executable-source identity.

**Actual source:** the existing diagnostic copy
`/workdir/openwepp-experiments/b01-wb14-cadence/baseline-red`, based on immutable
`reconciled-available145-r1` at
`/workdir/openwepp-experiments/b01-wb14-source-reconciliation/reconstructed-available145`.
The unchanged base's 927-entry map is
`c7aea0707d0c6a0876ec4bee5a7fcf15e751a092e086f517ced7edba8282b71e`.
The diagnostic copy additionally contains the stopped context-test changes, three owner-authorized compatibility pattern alternatives and two immutable fixture copies described below. Do not label this modified copy with the base hash alone.

**Allowed work:** fix the new test's missing semicolon; complete the existing new-test/helper work and select or minimally add the required ordinary non-prefix real-ingress control within the already authorized test surfaces. Resolve the fixture-decoding justification. Preserve the three approved compatibility alternatives and every pre-existing assertion. Perform the selected focused builds/listings/tests, formatting, warnings-denied owning-crate Clippy, evidence reconciliation and two independent affected-scope reviews. Production functions, other existing assertions, fixture bytes, features/cfg/visibility, dependencies, manifests, locks, toolchain and scientific authority remain read-only.

**Acceptance:** one compiled, intended-admission regression that reports the exact known baseline E008; passing separate source-defined context validation; a passing ordinary non-prefix real-ingress control; all 18 precedence and four raw-hash controls passing; required source-quality checks and complete source/fixture/command/result evidence; both independent reviews accepted within this limited scope. A compile failure or unrelated panic is not expected-red. The wider cadence package remains HOLD.

**Run allowance:** focused Rust unit/regression execution and its compilation/listing only. ZERO runner/collector calls, full-tail/warm/performance simulations, saved-checkpoint operations or candidate-green production runs. The completed witness and its exhausted 1/1 authentic invocation stay closed.

**Proposed allowance amendment:** raise this checkpoint's cumulative unsuccessful-correction ceiling from **2 to 4**, adding no active time. Carry the owner's latest **39m14s consumed** and **2 failed cycles**, leaving **20m46s** of the original 60-active-minute allowance. The owner's figure includes later publication time beyond the package's 38m54s timestamp. Do not investigate or reset that history. Reading, editing, orchestration, review and preservation consume the remainder; exclude only newly recorded pure waits. All work, including delegates, uses this one allowance.

**Stops:** the earlier of the original active-time ceiling or the revised four-failure ceiling; a new downstream setup/context/consumer failure beyond the prescribed E008; necessary source/fixture corruption or unexplained drift; or a required forbidden production/authority change. Preserve the result and return, without fixing the downstream defect. Missing required reviews or checks prevent acceptance. Completion of this checkpoint also returns to the owner; it grants no subsequent repair.

## Bind the existing stopped cut

Use `artifacts/ingress-regression/compatibility-continuation/` under the owning package at the evidence cut. Its `terminal-source-identities.json` and retained patches identify:

| Diagnostic file | Stopped SHA-256 |
| --- | --- |
| `crates/openwepp-hillslope-orchestrator/src/direct_runtime/surface_liquid_ingress_context_tests.rs` | `0c86634bc0eb7843ed2494b15a877db3fce9380304dcd081ac2a768e45f9e228` |
| `tests/integration/land_surface_energy_real_hydrology_shadow_contract/precedence_tests.rs` | `b2df4d81905c92095070a4aa7f5ff89c4883eeb934c3f2b43c646cb0d8f1f7e5` |
| `tests/integration/land_surface_energy_real_hydrology_shadow_contract/raw_hash_tests.rs` | `afc499fa26c946c9a1122ed08c49aee7f270d0b646ee8c935d414cf2aa375e62` |

The final uncompiled test is retained in `final-red-green-cycle2-failed.patch`.
Preserve that failed cut before editing. Reuse accepted base reconstruction and fixture custody; verify relevant current files rather than repeating whole-corpus hashing or historical recovery. Current packet hash is
`d8f1fe07d73ac09a6eb002b1a49a714cad7a0af1bb58ffab7612282ea002cc76`;
owner-seed hash is
`d1c467265dc5c718cc6d1a18fd1fbf71289e3ad5760967f9556f33109283f515`.
Historical substitution semantics remain UNKNOWN. Diagnostic source permission is not qualification-baseline adoption.

## Preserve the corrected regression semantics

At the retained `surface_liquid_ingress_context_tests.rs:247`, terminate the `let result = execute_surface_liquid_ingress_with_parent_state_and_coupled_binding(...)` statement with `;`.
Keep conditional inspection of `Err` for the exact canonical E008, `IngressCandidate`, transaction 255 and detail `WB14 day or interval continuation mismatch`, followed by the unconditional **admission assertion** `result.expect(...)`.

Do not restore `.expect_err()` followed by unconditional panic, add `#[should_panic]`, suppress the failure, or change production to obtain acceptance. The same admission assertion must be capable of passing unchanged after a separately authorized future repair. No future green result is claimed here.

Retain the earlier context PASS and E008 transcript as evidence from their identified earlier source cut. They do not replace final-cut execution. The planned exact-E008 expected-red is a successful baseline observation for this checkpoint, not an unsuccessful corrective verification. A mismatched error, setup failure, zero selection or compilation failure is not that expected result.

## Finish the selected checks, without expanding scope

Before execution, bind the final source/fixture hashes, effective manifest/cwd/target, features, exact selections and intended outcomes. Reuse the retained Nix setup, the diagnostic copy's explicit manifest and a separate disposable target; use `persisted-restart-v1,restart-authority-evidence` and `RUST_MIN_STACK=67108864`. Record the effective target rather than assuming an override was honored. One build/test command at a time; do not launch duplicates while a command is running.

The two established test names are:

- `direct_runtime::surface_liquid_ingress::context_tests::b01_wb14_captured_parent_context_passes_source_defined_joins` — one selected case, PASS.
- `direct_runtime::surface_liquid_ingress::context_tests::b01_wb14_captured_first_physical_child_must_enter_real_ingress` — one selected case, exact baseline E008 and failed intended-admission assertion, classified EXPECTED_RED separately from the test process exit.

Select an actual ordinary non-prefix test reaching real ingress; inspect its body and confirm its fully qualified compiled name before running. If no suitable existing test exists, a minimal new control in the authorized context-test module is allowed. The previously run `native_inactive_prefix_starts_first_physical_child_without_wb14_receipt` is a native-prefix helper, NOT this control.

Execute all 18 `precedence_tests` and four `raw_hash_tests` in their registered orchestrator crate-test modules. Preserve count and assertion coverage. A successful compilation or a filtered zero-test result cannot substitute for these 22 controls.

Finish affected-test formatting and the already selected warnings-denied owning-crate Clippy check with the same feature posture. Do not waive lint, modify unrelated production code, or revive the prospectively withdrawn external-authority anti-evasion selection solely because these compatibility alternatives exist. Its existing NOT_APPLICABLE disposition remains unless the actual diff changes the triggering scope. Wider campaign requirements remain outstanding; no new full-suite campaign is authorized here.

Address the existing medium decoding finding without an unrelated refactor: inspect whether the canonical restart decoder is usable under current visibility and dependencies. Reuse it only if this requires no forbidden change. Otherwise document the concrete reason for local test-only decoding, its exact binary64 extraction, canonical configuration-constructor/digest check and immutable owner-seed binding. Do not add a crate dependency or visibility change to close this note.

## Execution, review and return

Astra orchestrates; Terra remains the sole test writer. Preserve each corrected cut and command/result before another edit. A failed corrective verification returns immediately to Astra with its consumed cycle; no deferred reporting or further edits after the limit.

Reuse `/root/ingress_correctness` and `/root/ingress_qa` for affected fix verification when accessible. Both must remain independent of the writer/adviser and distinct from each other. Carry original findings and same-reviewer corrections: the ordinary-control requirement remains open, while QA's filename-transposition finding was withdrawn. Do not reopen accepted witness work or request another general reviewer wave. Unavailable required review remains an acceptance gap, never an invented approval.

Keep the single existing package record. Preserve raw stdout/stderr, numeric exit, selected/executed counts and exact source identity for each command. Reconcile the terminal diff against the immutable available base, retaining test/compatibility patches and unchanged fixtures. Carry scoped evidence/test-patch commit/push permission on the existing branch; verify small published files without claiming full remote source/corpus custody.

Return separate outcomes for final context, expected-red regression, ordinary ingress, 18/4 compatibility controls, formatting/Clippy, evidence and independent reviews, plus the remaining allowance. B01-CONT-005 full-parent progression, C-025 complete-prefix authentication, total inactive physics, rollback, independent accounting, runner coverage and restart remain unfulfilled. No cadence patch or production repair follows automatically.
