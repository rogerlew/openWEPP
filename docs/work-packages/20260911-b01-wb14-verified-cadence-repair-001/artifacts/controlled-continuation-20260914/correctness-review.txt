Reviewer: /root/controlled_correctness (rust_code_reviewer, Sol/high), independent replacement for the unavailable prior correctness scope. This is a new conversation and makes no continuity claim.

Scope and evidence: Static review of the frozen developmental-check-11 cut recorded in `developmental-check-11.json` and `developmental-check-11.source.patch`: source-tree SHA-256 `70a802c1167e7ce98a87e63e81c72165eb0e0a44293abb0b837a8bce8d4741d2`, patch SHA-256 `10fc3d377a8ad80399aa953f67634a1f216f7747960543e0558cc35a7c0febbd`. Reviewer inspected the retained source, prior correctness findings, canonical provider binder, forcing digest, native admission proof and consumer history restoration seams. Reviewer ran no compiler, test, formatter, lint, capture, probe or model command. Root-supplied execution record reports the frozen check failed exit 101 with E0425 because `crate::canonical_parent_forcing_digest` is not a crate-root function; the canonical function exists privately under the attachment module. Subsequent writer changes are moving and are not assessed by this section.

Findings, ordered by severity:

- HIGH — The outer file deliverable remains incomplete. `snow_stage3_v11_current_context_capture.rs:927-1094` returns only an owned provider, one prepared child and a forcing digest. It restores no owned consumer, parent, coupled clock/history, staged owners, accepted prefix, terminal ProducedUnconsumed custody, or independently checked provisional operands. The older `import_and_reconstruct_pre_child_context` still requires a live caller-supplied consumer and remains disconnected. This cannot qualify B01-CONT-015/016 or the authentic fresh-process probe.

- HIGH — Native restoration is transport-only. The cut records lane maps and native soil from the day-beginning committed consumer and separately from the target pre-child consumer (`snow_stage3_v11_current_context_capture.rs:547-573,589-632`; `v9_real_consumer_shadow.rs:435-484`), but the importer never decodes those operands, calls `diagnostic_restore_native_lane_maps_v1`, authenticates `NativeSoilRestartAdmissionV1`, or invokes `restart_authority_try_new_with_native_soil`. It also does not install scheduler position, provider cursor, accepted publication supports/event handoffs, native surface/WB14 custody, or final logical owners. Day-start bootstrap and target resident must remain distinct operands; serializing both is not restoration.

- HIGH — Provider static configuration is still supplied by the file rather than independently pinned authority. `restore_provider_bound_context_from_rows` accepts only `DirectSnowStage3V11StaticContext`, then reconstructs `SnowFreeHalfHourStaticConfiguration` from row bytes. Run/topology digest filters do not independently bind CO2 pressure, reference height, GSI owner configuration, destination membership or WB14 configuration hashes. A self-consistent substituted provider/configuration bundle can therefore become its own authority.

- HIGH — Adaptive active-lane selection is derived from row-supplied `beginning_stage3_canonical_json` before any restored consumer/clock owner join (`snow_stage3_v11_current_context_capture.rs:1021-1050`). A substituted Stage3 state map can select a different active lane subset and make the child-support projection self-consistent. Active lanes must come from restored owned state, or the decoded map must first be joined to its canonical restored consumer/clock projection.

- HIGH — The frozen cut does not compile. At `snow_stage3_v11_current_context_capture.rs:1053`, it calls an inaccessible/nonexistent crate-root forcing-digest function. None of the new provider/file behavior has executable evidence on this source identity.

- MEDIUM — Exact target selection remains incomplete. Target rows are selected by kind/day/interval/run/topology, then the importer accepts row-supplied child support so long as it is a valid subslab of parent interval 22. There is no direct assertion of the exact B01 child bounds, exact enclosing parent support, or parent transaction identity at this new outer API. A consistently substituted child within the same parent interval can pass the current projection.

- MEDIUM — No test calls either new provider-bound rows/file API. There is no current evidence for the actual observation file schema path; duplicate, omitted, reordered or wrong-day supports; exact target/parent poisons; pinned-configuration poison; diagnostic-error isolation; or zero physics/accepted-state mutation.

Limited fixes on this frozen cut:

- Static: the actual prepared-day recorder is wired before execution in `snow_stage3_v11_attachment_runtime.rs:753-759`. It transports the provider, ordered 48 live prepared supports and a distinct committed day-beginning native bootstrap.
- Static: the importer restores the provider, reconstructs all 48 supports and invokes the canonical `PreparedStage3V11DayV1::bind_production_provider_day`. This is the correct join for cardinality, order, day, GSI/provider cursor, destination coverage and support/provider receipt identity.
- Static: the importer reconstructs a child with the production `coupled_subslab(...).retain_active_snow_lanes(...)` path and intends to derive the forcing digest from the restored provider-bound parent support rather than trusting the row receipt. The inaccessible function call prevents compilation, and the untrusted active-lane source prevents acceptance even after that narrow correction.

Residual risk and missing tests: All prior consumer, parent/clock/history/custody, typed restoration, independent producer comparison, duplicate logic justification, observation isolation, zero-physics, composed file, poison, formatting, matched lint, capture and authentic-probe requirements remain unmet where this cut supplies no current evidence. Capture and probe remain unspent according to the parent record. Verdict for developmental-check-11 cut: HOLD / NOT ACCEPTED.

Final same-reviewer assessment — frozen partial terminal cut

Scope and evidence: Static review of source-tree SHA-256 `a052176184d332f205fb985f03fed7035cf3c89553e11e8949c42b1fbba07686`, eight-file patch SHA-256 `f7cb2b9fa3d641e5da0b87b75351256f7c57db05cd2c7a70d23b2b35652caa82`, as recorded in `partial-terminal-check.json` and `partial-terminal-check.source.patch`. Reviewer ran no compiler, test, formatter, lint, capture, probe or model command. Supplied root-owned evidence records: owning-crate all-target check PASS exit 0; exact eight-file rustfmt check PASS exit 0; compiled nextest listing PASS exit 0; the sole selected helper regression executed one test and FAIL exit 100, zero passed, at 2026-09-14 02:43:38 UTC. Source was unchanged through each result-bearing command.

Findings, ordered by severity:

- HIGH — The new recorder breaks its only existing focused regression before import begins. `snow_stage3_v11_current_context_capture.rs:880-882` unconditionally requests `diagnostic_native_soil_v2()` while `v9_real_consumer_shadow_tests.rs:397-415` constructs the canonical fixture through `DirectV10RealConsumerShadow::try_new`, which installs a V1 resident (`v9_real_consumer_shadow.rs:1991-2027`). The exact selected test fails at `v9_real_consumer_shadow_tests.rs:1093` with `V2 soil accessor on V1 resident`. This is a concrete runtime regression and leaves zero passing tests for the final cut. The appropriate acceptance fixture must exercise the intended native V2 source; silently making native custody optional would not prove the authentic path.

- HIGH — The final outer API still returns only `RestoredProviderBoundContextV1 { provider, prepared_support, forcing_receipt }` (`snow_stage3_v11_current_context_capture.rs:1273-1277`). The canonically bound complete 48-support `ValidatedPreparedStage3V11DayV1` is dropped after local validation. No owned consumer, native admission proof/capability, parent, coupled clock, accepted history/prefix, staged owners, terminal ProducedUnconsumed parcels, or provisional comparison is returned. The older importer still accepts a live consumer. B01-CONT-015/016 and the fresh-process owned-context requirement remain unmet.

- HIGH — Native bootstrap remains serialized but unused. Neither day-start bootstrap nor target resident/lane-map operands are decoded in the file consumer; `diagnostic_restore_native_lane_maps_v1`, `NativeSoilRestartAdmissionV1::authenticate`, and `restart_authority_try_new_with_native_soil` have no importer call. Scheduler/provider position, accepted publication supports and event handoffs, native surface/WB14 custody, and final logical owners likewise remain unrestored and unjoined.

- HIGH — The adaptive child projection still lets file-supplied `beginning_stage3_canonical_json` determine active lanes before any restored owner join (`snow_stage3_v11_current_context_capture.rs:1404-1436`). The predicate duplicates the runtime branch in `snow_stage3_v11_adaptive_execution.rs:1005-1021`; although presently aligned, the file value can be substituted and later logic drift can silently change which lanes are retained. Derive from restored owned state and centralize the predicate or provide explicit source-bound parity justification/tests.

- MEDIUM — There is still no actual recorder once-only guard. Import rejects multiple matching day or target rows, but repeated execution can emit duplicate day rows; no current test distinguishes duplicate-valid from conflicting duplicate custody. Parent transaction identity is not part of the new outer row selector or returned provider context and awaits the full parent/clock join.

- MEDIUM — Substantial duplicated restoration logic remains without parity evidence: manual enum/scalar arrays in `snow_stage3_v11_current_context_capture.rs:25-631`, the adaptive active-lane predicate at :1414-1425, and provisional construction at :1019-1080 mirror canonical production code. No file-backed test reaches these reverse mappings on the final cut. This must be centralized or explicitly justified with independent producer/parity tests.

Verified limited fixes:

- Ran, root-owned: the inaccessible forcing-digest defect from check 11 is corrected by crate-private attachment visibility and the exact final source compiles.
- Static plus compile: the file API now requires independently pinned `SnowFreeHalfHourStaticConfiguration` and compares every recorded scalar/destination field with bit-exact floats before provider reconstruction (`snow_stage3_v11_current_context_capture.rs:1283-1345`). This resolves the prior pinned-provider-configuration finding at the code level; no poison test ran.
- Static plus compile: exact B01 child bounds and the recorded/restored parent support are checked at :1356-1403. This resolves the narrow support-selector finding at the code level; full parent transaction/prefix identity remains open.
- Static plus compile: all 48 supports are reconstructed and passed through `PreparedStage3V11DayV1::bind_production_provider_day`, and forcing is derived with the canonical parent-support digest at :1368-1452. This is the correct provider/day/forcing join, but no test executes it and the returned context discards the bound day.

Residual risk and missing tests: No source-valid composed file fixture exists and repo source contains no test caller of `restore_provider_bound_context_from_rows` or `restore_provider_bound_context_from_file`. Required support omissions/reorder/duplicate/wrong-day poisons, provider/owner/binding/prefix/custody poisons, diagnostic-error isolation, independent real-producer comparison and zero constitutive/ingress/WB14/accepted-state mutation evidence are absent. Selected matched orchestrator/runner lint, authentic capture and distinct file probe did not run. The compile emits multiple unused-item warnings for the uncalled restoration path; these are not runtime evidence and selected warnings-denied lint remains unmet. The helper failure consumed the final correction allowance; no fix verification exists for it.

Final verdict: HOLD / NOT ACCEPTED. The provider/day/forcing subcontext is materially closer and compiles, but it is unexercised, breaks the existing focused regression, and does not restore the acceptance-critical native consumer or complete owned file context.
