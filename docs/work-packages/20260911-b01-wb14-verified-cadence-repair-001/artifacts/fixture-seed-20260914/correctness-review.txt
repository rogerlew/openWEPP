### Replacement affected correctness review — `/root/fixture_seed_correctness`, 2026-09-14

Identity and scope: independent replacement conversation with no continuity claim to `/root/test_first_correctness`; review was limited to B01-CONT-019 explicit fixture-day construction and its source/evidence scope. I did not reopen the previously accepted compiler and publication-lifecycle fixes or the prior reader/cross-owner/provider findings. Reviewed frozen detached tree SHA-256 `35031e4b5af68681e2cae276090a8700a1f869466e6ac04e0ee83f5238330b6d`, test-file SHA-256 `1243a31b07566bcb5940e7de2b7af30b62b399ed7ee6ef5994a768e4ba7fefa5`, and supplied full source patch SHA-256 `88d53d0907e66e79f6ed00d5f84c14f4e499092b6d028fd3fb27100125afa29b` against the pinned a3100159 reference builder and detached declarations.

Evidence class: **Static frozen-source review plus supplied execution evidence.** I ran no Rust, build, test, format, lint, capture, or probe command. Supplied `fixture-format-check.json` is PASS exit 0, `fixture-list.json` is PASS exit 0 with exactly the one required nonignored test selected, and `fixture-connected.json` is FAIL exit 100; each records source unchanged during execution.

Findings, ordered by severity:

- **HIGH — the connected positive still supplies no recorder-file-owned-reader acceptance evidence.** `fixture-connected.json` records exactly one selected/executed test, zero passed and one failed at `v9_real_consumer_shadow_tests.rs:1311` with `Support("active snow lane requires complete destination boundary set")`. The source guard at `snow_stage3_v11_attachment.rs:2200-2205` means a `ResolvedSnow` lane's represented destination set did not equal its expected complete set. The raw output does not record the loop day index, so it does not independently prove which `0..4` stage attempt failed; no exact-day claim should be made. The panic occurs inside `stage_prepared_day`, before that attempt's publication completion/commit/archive, and prevents the later `finish_to_file`, producer-scope exit, owned reader, restoration assertions and negative control. No completed earlier stage/publication/archive is evidenced by the retained result. This is the mandatory first new downstream support/setup result, not a reader, serialization, canonical-restoration, receipt, or custody result.

Affected fix verification:

- The empty-seed defect is corrected within the authorized test-local scope. `native_archive_day_input` constructs five explicit days from the typed parsed climate forcing, retains the cold `(-3 + -7)/2 = -5 C` daily midpoint and zero precipitation, and derives profile depth, porosity cap, field-capacity storage, wilting storage, and subsurface configuration from each installed lane's actual layers using the pinned reference formulas. It neither defaults an absent seed nor changes production code.
- Constructor, installation and pin consistency are substantive: `DirectRunFrame::from_constructor_inputs` validates the complete five-day constructor, the continuation installer retains its cardinality/topology checks, and exact snapshots compare the installed frame and immutable pins. Publication inputs come from the same constructed day. Their `initial_soil_water_m` is captured from the live committed lane immediately before each stage attempt, matching the production Stage-3 builder's live-storage rule rather than resetting every day to one fixture-wide constant.
- The connected test reached `stage_prepared_day`, so all earlier explicit construction, constructor validation, continuation installation, immutable-pin and pre-stage publication assertions executed without the former `native hydrology seed day` panic. This closes only B01-CONT-019's empty fixture-day prerequisite. The previously accepted completion/commit/archive helper remains unchanged.
- The small local profile/day helper intentionally mirrors the pinned private downstream test builder, cites its source, and is justified by the adopted prohibition on adding a downstream-crate dependency or broadening visibility. It is test-only and does not create a second production science path; no duplication finding is raised for this bounded adaptation.

Residual risk and missing tests: the required connected positive has not passed, and no recorder file, producer-lifetime separation, owned-reader entry, canonical restoration, restored-value comparison or negative refusal executed. The prior open cross-owner/provider joins, independent producer comparisons, typed outer-error concern, mirrored restoration concern, poison/isolation/zero-physics inventory, separate structurally valid V1 refusal and matched owning/runner lint remain unchanged and unmet. The new support-boundary failure is diagnosed only to the exact fail-closed set mismatch above; no repair or broader physical conclusion is supported in this stopped cycle.

Bounded verdict: **B01-CONT-019's explicit fixture-day construction is source-correct and its former empty-seed failure is closed, with no correctness blocker found in that changed helper. Overall package/milestone disposition remains HOLD / NOT ACCEPTED because the required connected behavior failed at the next Stage-3 support boundary and all downstream acceptance evidence remains absent.**
