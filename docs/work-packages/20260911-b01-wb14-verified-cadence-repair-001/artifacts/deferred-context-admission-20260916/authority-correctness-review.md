# Independent authority correctness review — deferred-context admission

Reviewer: `/root/event_correctness_attribution`. Scope: current-main draft changes to `SC-LANDSURFACEENERGY-001.md`, `soil-custody.md`, the LSE binding index, and the science-contract registry, against the adopted B01-WB14 deferred-context authorization and relevant existing custody, coupled-time, and GSI authority. **Static:** canonical text and Q source inspected. **Ran:** no Rust, physics, provider, model, or contract-derived red tests. This is a draft-authority review, not a final implementation or promotion gate.

## Findings, in severity order

1. **High — successful promotion contradicts byte preservation.** `soil-custody.md:415-423` first authorizes the existing final full replay and atomic install, then says **any attempted promotion** preserves every live and captured owner byte. A successful install necessarily changes the live soil owner. Amend to require byte-exact preservation on failed validation or failed promotion, and exact canonical owner/receipt/restart installation on successful promotion. Consume the private proof on either promotion outcome. This distinction is required by existing `INV-LANDSURFACEENERGY-155` (`soil-custody.md:155-164`) and `INV-SURFACELIQUID-027`.

2. **High, evidence gate rather than a demonstrated impossibility — complete support/event/credit correspondence has not yet been established for this corpus.** The draft now correctly requires an *independently retained* complete mapping and fails closed if unavailable (`soil-custody.md:393-404`, `OBL-LANDSURFACEENERGY-C-023`). Q's serialized deferred record contains original beginning owner, final trial, accumulated operands and ordered layer-credit chain (`snow_stage3_v11_current_context_capture.rs:1491-1512`); its accepted operand and layer-credit types lack child support, transaction, and event keys (`exact_dyadic_enthalpy.rs:398-433`). This proves only that the serialized deferred credit groups alone are insufficient, not that no other retained record can establish the relation. Before proof minting, identify the independently retained record, validate its source-defined one-to-many mapping to accepted clock events and child supports, and test missing/foreign/duplicate/reordered cases. If no such record exists, retain HOLD; cardinality or ordinal proximity cannot substitute. No contract wording change is required for this finding unless the later evidence shows that the stated correspondence is unconstructible or misstated.

## Verified draft corrections and remaining gate

The current cut keeps the promoted contract at v34 (`SC-LANDSURFACEENERGY-001.md` frontmatter/change log; registry row) and exposes the proposal as an unpromoted binding in the LSE index. It retains ordinary resident-only refusal and keeps deferred custody non-owner, consistent with `INV-LANDSURFACEENERGY-155`, `INV-SURFACELIQUID-027`, and the existing native soil constructor's reached-position equality (`soil-custody.md:248-289`). The draft distinguishes accepted outer clock from inactive physical counter (`soil-custody.md:340-365`) and committed from prepared GSI receipt phases. Its error text now reserves `LSEB-E-049` for soil custody and requires owning typed failures plus source-defined precedence. These corrections address the earlier overbroad form; final precedence still needs contract-derived red evidence.

Residual risk: the prior committed GSI receipt needs an independently authenticated preimage or the separately authorized deterministic provider prefix; a captured digest tuple is insufficient. Source-derived red tests have not yet been supplied, so the prescribed authority gate remains open even after the text contradiction is repaired. No claim of physical validity, restart qualification, or conservation follows from this draft.

**Disposition:** HOLD for authority promotion and implementation while finding 1 is unresolved and source-derived red/correspondence evidence is pending. Recheck the corrected paragraph and new evidence only; unrelated accepted attribution does not require repetition.

## Same-reviewer follow-up: corrected text and provider evidence

**Static fix verification:** `soil-custody.md:415-425` now distinguishes failed validation or failed promotion (byte-exact preservation) from a successful canonical owner/receipt/restart install, invalidates the private proof on both outcomes, and explicitly forbids direct installation of deferred custody. Finding 1 is **resolved in draft text**. The BEI row remains `active` / `unpromoted-binding` / `science-review-follow-on`, names the owner and next dual-review/red-test gate, and grants no runtime or wire authority. The promoted LSE version remains 34. The expanded test vector requires independently retained raw correspondence and forbids using physical solver/provider, accepted-child advance, install, or promotion for its reconstruction control.

**Supplied Ran evidence, independently inspected, not rerun by this reviewer:** `provider-prefix-authentication.json` records exit 0 for the exact detached `gsi_forcing::b01_native_provider_prefix_authentication_tests::b01_pinned_bootstrap_provider_prefix_retains_receipts_and_keeps_day4_prepared` test. It pins the original seed, original climate, captured expectation and source/binary hashes and reports all unchanged. The test source (`gsi_forcing.rs:1051-1224` in S) decodes pinned bootstrap GSI/configuration/cursor, parses the original climate, prepares and commits local provider/GSI days 0–3, prepares day 4 without committing it, then compares the prior receipt, prepared receipt, complete captured GSI state/date/history and cursor. The canonical `prepare_owned` path validates each generated receipt (`09_snow_free_half_hour_forcing.rs:176-222`). I independently parsed the recorded stdout and checked it equals `authenticated-gsi-preimages.json`; the pinned seed, climate and expectation hashes still match the execution receipt. Day-3 committed receipt `e1ca5d7e…c961887`, day-4 prepared receipt `85ca5300…11ab28`, and the day-3 ending/day-4 beginning/captured GSI state `fc2fb427…dd2a` match exactly. This **closes the prior-receipt preimage gap for this bounded provider-prefix lane**, while recording four local provider/GSI commits plus day-4 preparation rather than zero provider computation. It does not establish a live restoration, a physical successor, or semantic composite admission. The test's altered digest on a prepared receipt is a receipt-integrity negative; the draft's separate valid-receipt phase-swap and precedence red controls remain pending.

**Raw-source status:** `trial-preimage-extraction.json` reports exit 0 for pure extraction from the pinned 8.7 GB original observation source. `retained-soil-trial-primitives.json` contains 24 raw soil-trial primitive records covering all 12 selected credit groups, with source offsets and per-record SHA-256. The extractor selects source occurrences by top-boundary debit/credit receipt identity and labels 51 other literal occurrences outside the extracted primitives. These records are promising independent preimages, but each trial is unpublished; this extraction alone does **not** establish which trial was selected into each complete committed credit, its exact child support/event, or an admitted composite. Finding 2 remains an open evidence gate pending the source-defined lineage/mapping proof and its red controls.

**Current disposition:** HOLD for authority implementation and promotion pending complete correspondence and contract-derived red evidence. The corrected text and bounded provider authentication are accepted for their stated scopes only. This reviewer ran only local read-only JSON/hash comparisons; no Rust, provider, soil, WB14, or coupled-model operation was rerun here.

## Selected external-contract reading closure

**Static:** I read each selected external single-file contract in full, including the complete 4,093-line `SC-SNOWENERGY-001.md`, as required by `SC-LANDSURFACEENERGY-001/interface.md`. The other complete files were `SC-SNOWFREEFORCING-001.md`, `SC-COUPLEDTIME-001.md`, and `SC-SURFACELIQUID-001.md`. This closes my earlier reading-scope gap; links alone did not trigger recursive expansion. No new contradiction changes the draft corrections or the HOLD disposition.

The relevant independent constraints reinforce that disposition. `SC-SNOWENERGY-001#INV-SNOWENERGY-084` and `OBL-SNOWENERGY-C-052` restrict an unpublished V2 continuation to authenticated read-only candidate input and require final authentic replay of the original prepared owner, complete accumulated operands, exact selected ending, and full credit chain before one atomic owner/receipt/restart install. `SC-SURFACELIQUID-001#INV-SURFACELIQUID-027` likewise bars candidate projection from owner or restart state; `SC-COUPLEDTIME-001#INV-COUPLEDTIME-031` retains exact accepted-prefix and first physical child chronology; `SC-SNOWFREEFORCING-001` retains one committed daily GSI receipt versus a separate uncommitted preparation phase. None licenses ordinal/count inference or acceptance from final-trial hash and flattened operands alone.

**Additional evidence limit:** the current source validator's final-trial/hash and ordered-state checks and the newly extracted raw per-child soil trials are not yet a proof that *each* intermediate selected trial seal, predecessor seal, child support, credit, and accepted event correspond under the canonical source definition. A component match or equal count cannot replace those joins. The reported component test is narrow; I have not inspected an actual positive contract-derived RED or private proof. Authority implementation and promotion remain **HOLD** pending complete per-child correspondence and RED review. No physical or Rust execution was performed by this reviewer.

## Same-reviewer static review: recursive unpublished trial seals

**Static:** I inspected detached S `snow_stage3_v11_member_restoration.rs:955-1054` and its corpus caller in `snow_stage3_v11_current_context_capture.rs:4819-5020` against canonical `owner_envelope.rs:1313-1365,1458-1470,1629-1668,1940-2035` and `identity.rs:92-115`. The test-only inverse uses the producer's eight-field ordered tuple, domain string, snake-case predecessor enum, serde JSON, and exponent normalization. It starts from the captured original state's seal, carries ordered per-layer high/carry/temperature and transaction markers, reseals each typed ending state, and links each successor to the preceding computed unpublished-trial seal. The first predecessor is the original accepted receipt chain. Those choices match the canonical sequential constructor at field and serialization level. The two initial compile errors (uninferred seal vector and `Digest32` versus `Sha256Digest`) were corrected in S before the supplied subsequent run; I did not build or run it.

**Supplied Ran evidence, not rerun by this reviewer:** `raw-correspondence-v2.json`/`.stdout` report one passing corpus test and exact final and penultimate computed seal matches. Given an independently authenticated final candidate seal, exact producer serialization, and collision resistance, the final nested digest binds the whole computed intermediate seal chain; separate captured hashes for every intermediate trial are not inherently necessary. This is a valid narrow seal-lineage improvement, not proof that the physical receipts were selected into the accepted clock history.

**High remaining correspondence gate:** the caller currently uses extractor-provided `credit_group_index`, one matching top-boundary operand per group, contiguous 60-second support, final support end, and final event energy (`snow_stage3_v11_current_context_capture.rs:4835-5010`). It does not join all twelve selected source receipts to the *independently validated* eight accepted `ordered_owner_joins`/slab receipts. The retained clock has seven single-child slabs at `385200000000000..385620000000000` and one terminal slab at `385620000000000..385920000000000` containing five children. The needed source-defined check is a unique, ordered partition of all twelve supports under those eight exact accepted slabs, with each first-seven source receipt authenticated into its corresponding slab's canonical snow–soil receipt set and the last five joined to the terminal event's exact child receipt/ordered heat and accepted event custody. Require exact owner/slab/event identity, no unassigned or duplicated child/slab, and appropriate missing/foreign/reordered/phase poisons. Primitive physical source transaction `255` and unpublished soil child transactions `5763..5774` are distinct domains; neither may be inferred from the other. Support overlap and equal counts alone do not establish this relationship.

**Other gate limits:** the new caller checks the extracted artifact's claimed original-source SHA string but does not itself rehash source offsets/raw JSON or canonical-validate each raw receipt; the separately retained extraction receipt and independent exact-rational 72-layer-credit reconstruction address provenance and arithmetic only in their recorded scopes. The caller also does not explicitly invoke the existing full `validate_deferred_custody`/`validate_trial` before using the final candidate hash. Final composite admission must revalidate that candidate and the original owner under the ordinary guards and prove exact accumulated-operand/credit consistency; no test-only inverse should mint a capability. The new test has no semantic negative controls, source-derived expected RED, or private admission constructor. **Disposition remains HOLD** for implementation and promotion; the seal result is accepted only as component evidence.

## Same-reviewer evidence update: accepted-clock to credit mapping

**Static and supplied Ran, not rerun by this reviewer:** I inspected `reconstruct_clock_credit_mapping.py`, its exit-zero `independent-clock-credit-reconstruction.json`, `independent-clock-credit-mapping.json`, the eight retained raw `candidate_subslab_primitives` records, and Q's observer producer at `snow_stage3_v11_terminal_execution.rs:3115-3168`. The observer labels each record candidate-only until accepted-day membership. The independent script compares each raw subslab's exact slab ID, support, parent transaction, and owner join with both the captured phase join and the separately decoded accepted clock receipt; it checks each accepted beginning/ending complete-owner-set hash. Its framing matches `openwepp-coupled-time/src/identity.rs:86-111` byte-for-byte (`OPENWEPP\0`, big-endian version/domain/tag/value lengths and ordered fields) and reproduces the accepted owner's terminal snow–soil receipt-set digest. I independently checked that all eight retained `source_canonical_json` strings parse to their record values, match their recorded per-slice SHA-256, and have unique source offsets; I did not re-read the 8.7 GB original. The prior full-source hash and source-slice checks remain separate retained extraction evidence.

The accepted mapping is source-defined and unique for this specimen: slabs 0–6 each match one adaptive terminal snow–soil *trial* receipt and credit child 0–6; slab 7 maps exactly children 7–11. The final slab has one terminal snow–soil *heat* receipt (`6e92cc24…5fc66f05b2`), not an ordinary or adaptive trial-set entry. That receipt's limiting-boundary digest equals child 11, its support and ordered five-child heat equal the recorded terminal event, and the event receipt joins the accepted clock's event receipt and ending owner set. All twelve children are consumed once in order with no uncovered support. The earlier high finding that a source-defined eight-slab/twelve-credit correspondence had **not yet been established for this corpus is resolved at retained-operand evidence level**. The independent exact-rational 72-credit reconstruction and recursive seal match remain separate supporting components; no numeric transaction rebasing was used.

**Remaining authority gate:** a private admission validator must perform the canonical typed receipt/subslab/clock/event validation and exact one-to-many correspondence on inert decoded inputs, with missing/foreign/reordered/duplicate/phase/owner poisons and unchanged-state refusal. The Python script's positive assertions and extracted candidate records do not mint that proof or qualify a live restoration. The newly named ordinary-edge expected RED was not compiled or executed at this review checkpoint. **HOLD remains** for authority implementation and promotion until the contract-derived RED, semantic controls, independent review of the completed Rust validator, and private proof boundary pass; this evidence update alone changes no physics or production status.

## Same-reviewer sequencing finding: preimplementation RED versus proof lifecycle

**High — clarify a literally unexecutable lifecycle gate.** The draft test-vector row in `soil-custody.md:442` says to add red tests for *every* join and rejection, including proof serialization/reuse and promotion/rollback, and only then implement the private constructor. Those lifecycle behaviors cannot be meaningfully executed before the proof type and private entrypoint exist; a missing-symbol compile error or fabricated stand-in is not evidence that reuse, serialization, or rollback guards work. The canonical authoring procedure (`science-contract-authoring-procedure.md:103-111`) requires contract-derived tests and preimplementation gate evidence before production edits, but does not say every future branch must execute before its type exists. The owner authorization requires a source-bound RED before implementation and retains all later semantic controls.

**Seam correction after direct source inspection:** the currently named `deferred_context_complete_bundle_is_red_at_ordinary_restore_edge` authenticates the component bundle and calls `member_restoration::restore`, a `cfg(test)` private file-backed composition reader (`snow_stage3_v11_member_restoration.rs:578`). At present that reader delegates to `DirectV10RealConsumerShadow::diagnostic_restore_native_consumer_v1` (`snow_stage3_v11_member_restoration.rs:526`), which calls the ordinary `NativeSoilRestartAdmissionV1::authenticate` (`v9_real_consumer_shadow.rs:660`). The reader itself is **not** the ordinary resident-only admission API. Rename the RED to identify missing admission at the private composition reader, and state that its present expected failure is the reached ordinary native soil guard after full component authentication. It may turn green only when that reader explicitly routes the fully authenticated composite through a **distinct private constructor**; direct `NativeSoilRestartAdmissionV1::authenticate` and `diagnostic_restore_native_consumer_v1` must continue refusing the same resident/clock mismatch. Keep separate direct ordinary-entry typed-refusal/rollback regressions before and after implementation, along with the frozen historical negative. A generic `expect` failure or an earlier position/GSI refusal does not establish the intended RED provenance.

Recommended replacement for the test-vector sequencing sentence, without weakening any guard: “Before private constructor implementation, add and run source-bound tests executable at existing seams for the authenticated complete bundle, canonical ordinary-entry refusal and rollback, retained-record correspondence, and current receipt/counter/owner poisons; record a runtime expected-red at the test-only private composition reader, presently delegated to ordinary native admission, with the exact reached soil-guard refusal. Specify the full proof-lifecycle and promotion/rollback negative matrix before implementation. As soon as the distinct private constructor and proof type exist, execute every specified join, precedence, nonserialization, reuse/invalidation, promotion and atomic-rollback control before diagnostic acceptance or authority promotion. The direct ordinary resident-only entrypoints continue to refuse.” The correspondence reconstruction's no-physics limits and every mandatory scientific/custody guard remain unchanged.

**Disposition:** draft wording and RED name/provenance correction required before treating the authority/test sequence as an implementable gate. No Rust test was run by this reviewer; the newly added private-reader assertion was inspected statically.

**RED provenance qualification:** `restore_native_result` invokes the ordinary native constructor before combining the reader's position/GSI booleans (`snow_stage3_v11_member_restoration.rs:526-564`). Captured stdout and an exact soil-admission error assertion can therefore prove the ordinary soil guard was reached even if the reader later reports a combined position/GSI error. Such a run is a staged soil-guard RED, not yet a fully qualified complete-bundle RED capable of turning green through the private route alone. The separately authorized position/GSI annotation corrections and their positive prechecks must pass before the final expected-red is attributed solely to missing private composite admission. Preserve a direct typed ordinary-entry refusal test after the private reader changes route.

## Same-reviewer draft wording verification

**Static:** I reread the amended `soil-custody.md:442` test-vector row. It now separates the required preimplementation executable seam controls and exact reached native soil-guard RED from the proof-lifecycle and remaining join/precedence controls, which are specified before implementation and executed after the distinct private proof/constructor exist but before positive diagnostic acceptance or authority promotion. It explicitly preserves direct ordinary resident-only refusal and excludes missing-symbol/fabricated-red substitutes. The prior **high sequencing/ordinary-seam wording finding is resolved in draft authority text**. The renamed test and its runtime attribution, independent direct ordinary refusal regression, complete mapping controls, private proof behavior, and final implementation review remain unverified here. This wording verification is not admission or promotion approval; HOLD continues for those outstanding gates. **Ran:** no Rust or physical test by this reviewer.

## Same-reviewer mapping helper review (detached S snapshot)

**High remaining semantic join:** `snow_stage3_v11_current_context_capture.rs:4854-5300` validates the twelve raw trial receipt seals, credit operand IDs/heat, first-layer beginning/ending carry and state, all eight accepted slabs/owner joins, the terminal event and one-to-many child support partition, and the recursive final/penultimate unpublished trial seals. The source-defined trial receipt additionally seals `ending_soil_candidate_sha256`, but this helper does not recompute that digest from the raw first-soil ending candidate. Producer `v11_covered/physical_outcome_ledger.rs:417-450` defines its V2 preimage as the exact serde tuple `("OPENWEPP_TERMINAL_FIRST_SOIL_CANDIDATE_V2", beginning_soil_owner_id, beginning_soil_state_sha256, transaction_id, ending_first_soil)`. All these raw fields are retained. Receipt self-validation alone authenticates the claimed digest, not its equality to the raw ending state and transaction that the helper joins to a deferred credit. Recompute this producer digest for every selected child and add an altered-ending/transaction/owner-state negative. The final terminal heat receipt likewise includes an `ending_soil_owner_sha256` produced from the serialized ending OFE soil owner (`v11_covered/open_snow_terminal_accepted_endpoint.rs:567-585`; alternate terminal physical support path uses the same field). The helper validates this receipt's self-seal but does not join that digest to the independently authenticated ending OFE owner; establish the appropriate retained preimage and exact comparison, or state that this particular identity is not yet proven. These are semantic correspondence gaps, not a claim that the source-backed mapping is impossible.

**Supplied Ran, not rerun by this reviewer:** `raw-mapping-v5.json` records an unchanged frozen binary and inputs, one passing positive mapping test and one failing poison test (exit 101). Its positive stdout reports zero probe/direct/native physical mechanism counters. The helper's final `member_restoration::validate_current_phase` call **transitively invokes** `validate_deferred_custody` via the current phase validator (`snow_stage3_v11_member_restoration.rs:704-708`); my earlier statement that the positive omitted complete final deferred-candidate validation was incorrect. This validates final trial/resident separation, ordered state chain, and accumulated operand consistency in the positive. It still mints no proof and installs no live owner. The combined expected-red and position/GSI prechecks remain pending. The test helper itself does not authenticate original-source byte offsets or rehash every raw record, so its source-provenance claim depends on the separately recorded extraction receipt and frozen input hashes. **Disposition:** mapping evidence materially improves, but authority implementation/promotion remains HOLD for the missing source-defined joins, complete RED, and private admission review.

**Subsequent supplied Ran correction:** `raw-mapping-v6-fresh-*` records report both positive and semantic-poison tests passing when run in separate fresh libtest processes. The v5 two-test process failure arose from the B01 policy being already installed in that process, not from the missing policy installation that I initially inferred from the parent's preliminary diagnosis. I did not rerun v6. This resolves the reported v5 poison-run failure at the component test level; it does not supply the missing candidate-state digest or terminal ending-soil-owner joins.

## Bounded future-path restoration safety check

**Permitted validation arithmetic, with a disclosure requirement:** The existing diagnostic native constructor, after the ordinary soil proof, constructs a resident, restores accepted publication rotation and WB14 parent bytes, then restores frozen-litter V3/V4 residents (`v9_real_consumer_shadow.rs:660-845`). The V3 `restore_restart_authority` decodes every historical entry through `FrozenLitterHistoryEntryV1::from_canonical_bytes` (`frozen_litter_v3_adoption.rs:513-543`; `frozen_litter_v3_publication_retention.rs:453-468`). A historical native snow-liquid `Drain` entry calls `NativeSnowLiquidPublicationV1::validate` → `reconstruct` → `prepare_native_snow_liquid_candidate_v1` (`native_snow_liquid_publication.rs:313-370,397-460`; `native_snow_liquid_custody.rs:33-160`). That path invokes `allocate_positive_support_snow_liquid_v1`, exact dyadic retained-enthalpy accumulation, and derives candidate surface/LSE warm-start temperatures. The source first requires `ending_marker == beginning_lse.0.last_accepted_transaction_id` (`native_snow_liquid_custody.rs:43-48`), reconstructs a local unpublished candidate from a previously accepted zero-duration custody entry, and compares it with retained ending owners/receipts; it does not advance a physical marker or accept a new support. The adopted authorization `/tmp/openwepp_b01_wb14_deferred_context_admission_authorization.md:74` expressly separates harmless construction/reseal/validation from physical integration, so this existing historical validation arithmetic is **not a blocker under that wording**. It must be disclosed as nonzero accounting reconstruction, not described as wholly hash-only or zero computation. The future positive/refusal diagnostic run still needs counters proving zero LSE/soil/WB14 *physical evolution*, child advance, commit and publication.

**No new WB14/soil integration found in inspected callees:** accepted publication rotation restoration decodes/reseals history (`v9_real_consumer_shadow_publication_retention.rs:1389-1417`); WB14 `from_restart_bytes` validates schema, candidate/persistent states, configuration, per-OFE coordinator bindings and receiving owner (`surface_liquid_ingress_coordinator.rs:723-812`; `direct_v9_real_consumer_shadow_impl.rs:271-303`). Frozen-litter V4 `try_restore` validates already retained exact/physical projection pairs and successor chain (`frozen_litter_v4_adoption.rs:483-570`). Constructor `try_new_with_soil_resident` projects versions and validates owner/configuration identities (`v9_real_consumer_shadow.rs:2473-2667`); no execute-day, accepted-child advance, soil solve, or WB14 integration was found in this bounded call graph. This is static source inspection, not a runtime counter result or a claim about uninspected future private-constructor code. Authority implementation/promotion remains HOLD.

## Bounded GSI and position precheck review (detached S snapshot)

**High — complete-bundle prechecks are presently narrower than their names.** `snow_stage3_v11_member_restoration.rs:181-188` decodes the captured native counter but returns `(captured_position == 0, true)` while ignoring both `_position` and `_expected_gsi`. Zero is the source-observed *persistent native consumer* count for this still-covered specimen (`position-gsi-operands.json`); the outer clock/current Stage3 position is a separate domain and is validated by `validate_current_phase` at lines 702-740. These domains must remain distinct. However the unconditional GSI boolean cannot attest any native/provider phase join. `validate_authenticated_gsi_preimages` at lines 190-228 does validate the four self-sealed daily receipts, day ordering and adjacent state chain, previous committed receipt digest against captured native and prepared day-4 digest against provider. It does **not** bind the first receipt's beginning state to pinned seed `checkpoint.phase.committed.gsi_state`, each receipt's owner/run/configuration to pinned seed static GSI/provider identities, or the original climate source digest to the independently authenticated input. It also does not compare the captured native `gsi_owner_state` to the prior committed ending/prepared beginning state, nor the native and outer captured provider cursor bytes to the authenticated prepared day-4 beginning cursor. The captured state and cursor equality were separately observed in `authority-domain-attribution-20260916/position-gsi-operands.json`, and the independent provider-prefix test authenticates them for the bounded corpus; those facts are not all enforced by these S prechecks. Receipt self-validation does recompute each GSI daily result from its own forcing (`runtime_inputs/09_snow_free_half_hour_forcing.rs:230-257`), but is not a substitute for the external seed/climate anchors.

**Source-supported closure before the final complete-bundle RED:** decode and validate the pinned seed GSI configuration/bootstrap state and cursor, compare the receipt chain's first beginning state and stable owner/run/configuration/parameters to that seed and pinned provider, bind source-climate digests to the separately authenticated original climate, preserve day-3 committed versus day-4 prepared digest roles, and compare native captured state and both captured cursors with the prepared beginning state/cursor via typed `DirectGsiOwnerStateV1` and `SnowFreeHalfHourProviderCursor::restore_json`/canonical bytes. `restore_bound_provider` already constructs a typed day-4 `PreparedSnowFreeGsiDayV1` from captured beginning/ending cursors and receipts (`member_restoration.rs:361-388`; `current_context_capture.rs:1331-1367`), so its `gsi_receipt()` and `forcing_receipts().beginning_cursor()` provide the joined prepared side without a new provider computation. Keep the native count `0` check explicit for this specimen, and prove the independent outer position through prefix plus interval and current Stage3/clock; do not equate persistent native count with outer position or assign either scalar. The future private path also must initialize the authenticated prior committed receipt into its private consumer state: ordinary `try_new_with_soil_resident` starts `provider_gsi_receipt_sha256` at 64 zeroes (`v9_real_consumer_shadow.rs:2620-2625`), while `diagnostic_restore_captured_residents_v1` requires the captured prior digest (`v9_real_consumer_shadow.rs:757-782`). That downstream mismatch is currently masked by the earlier soil guard and should not be mistaken for a completed composite admission.

**Evidence limit:** this is static inspection of current S only. I did not run the prechecks, rewrite them, or verify a future correction. Candidate/OFE digest joins and the private proof remain separate open gates; no authority admission follows.

## Same-reviewer GSI correction verification (frozen S snapshot)

**Static fixes accepted for their exact scope:** `snow_stage3_v11_member_restoration.rs:68-101` now decodes the pinned seed's GSI configuration from exact hex floats, validates it, and validates its bootstrap state. The amended `validate_authenticated_gsi_preimages` (`:227-317`) validates all four committed receipts and day-4 prepared receipt; binds committed day 0 to the pinned bootstrap, consecutive states, owner/run/configuration/parameters/latitude, common climate-source digest, exact day-4 typed prepared receipt, captured native GSI state to day-3 ending/day-4 beginning, native cursor to the typed prepared beginning cursor, and native prior committed receipt digest. `native_authority_joins` (`:215-225`) now delegates to the shared typed inactive scheduler validator (`v9_real_consumer_shadow.rs:849-898`), which checks native physical counter, WB14 absence, frozen surface/frame and clock custody; it does not equate native count with outer position. The new actual-member GSI test at `member_restoration.rs:690-765` supplies positive and alternate valid state/configuration/cursor/prepared-phase poisons. These changes resolve the earlier unconditional GSI-boolean, naive count, and missing native state/cursor/seed-config checks at static source level.

**Remaining narrow joins/evidence:** the separately retained outer `target["provider_cursor_canonical_json"]` is not read in `member_restoration.rs`; only the nested native cursor is joined to the prepared day. The outer cursor should be typed/canonical compared to that same prepared beginning cursor and native bytes, with a poison. Climate-source SHA is now consistent across the authenticated receipt sequence and prepared provider receipts, but this helper does not itself bind it to an independently pinned original climate file or the provider-prefix execution receipt; the separately retained frozen input/provenance evidence may satisfy that external anchor, but the helper alone should not be described as validating original climate-source *provenance*. The new test lacks explicit bootstrap, external climate-anchor and outer-cursor poisons. C3 candidate/OFE digest joins remain a separate open finding. **Ran:** no Rust test by this reviewer; parent's build/run was pending at this review point. Authority admission and promotion remain HOLD.

## Remaining executable preimplementation controls after the scoped join fixes

**Static inventory, not a run result:** The draft `soil-custody.md:442` requires executable controls at existing seams before the distinct private constructor. Once the outer cursor/climate provenance and C3 candidate/OFE digest joins are corrected, the remaining gate is a **recorded result** for the exact frozen source/input corpus, not merely compilable tests. It must show: (1) positive authenticated complete bundle through retained raw credit/receipt/clock/event correspondence, exact carry/state/trial seals, source-defined candidate and final OFE identity, final deferred-custody validator and pinned provider/GSI phase; (2) semantic negatives for absent/foreign/duplicate/reordered *deferred credits* and raw correspondence, invalid support/predecessor/carry/state, terminal event/owner, committed-versus-prepared phase, seed/state/cursor/climate anchor, native counter/WB14/frozen surface/clock custody, each rejecting at the owning precheck before the private reader; (3) a direct **ordinary** `NativeSoilRestartAdmissionV1`/diagnostic constructor typed-refusal and unchanged-state check on the real resident/clock mismatch, retained independently from the historical composed-reader negative; and (4) a real expected-red from `deferred_context_complete_bundle_is_red_at_private_composition` **after all positive prechecks pass**, with its failure attributed to that same canonical ordinary soil guard, no earlier cursor/GSI/position refusal, no owner byte change and zero forbidden physical work. Existing `deferred_context_raw_correspondence_refuses_semantic_poison` covers some raw-source/receipt/support/carry/event negatives but does not by itself cover reordered or duplicated *deferred credits*; reversing raw extracted observation order is intentionally a positive control. The amended GSI member test covers alternate native state/config/cursor/receipt but not outer cursor, seed bootstrap, or original-climate provenance. Shared inactive-scheduler unit poisons exist in `v10_soil_thermal_v2_tests.rs:3107-3118`; actual-member precheck attribution remains to be recorded. Proof nonserialization, reuse/invalidation and final promotion/rollback branches are specified now but run once the distinct private proof/constructor exists and before positive acceptance/promotion. No source or scientific authority was edited by this reviewer. HOLD persists until these executable results and independent implementation review are inspected.

## Same-reviewer frozen GSI/C3 source verification

**Static, no run claim:** I inspected S `snow_stage3_v11_member_restoration.rs` while the writer was active; my subsequent file hash was `ec7e0507…ba004`, not the parent's earlier frozen `f92ac49c…d64885`, so this member-file inspection needs a final exact-source recheck. The other two inspected files matched supplied hashes: `snow_stage3_v11_current_context_capture.rs` SHA `e37e258a…d0f0e` and `runtime_inputs/09_snow_free_half_hour_forcing.rs` SHA `d8d44040…ba30`. In the observed S member source, `validate_authenticated_gsi_preimages` accepts the *actual separate outer* cursor field from the target, restores it through the typed cursor validator, and requires equality with native and prepared day-4 beginning cursors (`member_restoration.rs:231-367,652-660`). It parses the original climate and checks each committed day 0–3 and prepared day 4 against that day's canonical source digest. The new `cfg(test)` accessor (`09_snow_free_half_hour_forcing.rs:1196-1203`) calls the existing `select_day_forcing`/`source_climate_sha256` functions and no provider prepare/commit. This corrects the earlier invalid cross-day equal-digest check. The member-backed test poisons outer cursor, seed bootstrap and altered climate as well as native state/config/cursor and prepared/committed phase (`member_restoration.rs:773-872`). These source-level joins provisionally close my prior outer-cursor and original-climate findings, subject to the exact-source recheck and pending frozen build/run/input pins.

The C3 helper now recomputes every raw `ending_soil_candidate_sha256` from the producer's exact typed four-field tuple and the physical transaction without rebasing it to the native credit transaction (`current_context_capture.rs:4971-5000`; producer `v11_covered/physical_outcome_ledger.rs:417-450`). It also compares the final raw subslab ending V2 soil state with the validated phase candidate ending state, selects the sole OFE by the terminal receipt OFE identity and hashes that typed OFE state to the terminal `ending_soil_owner_sha256` (`current_context_capture.rs:5285-5310`). The raw poison test now alters a physical transaction and final raw ending state in addition to earlier controls. These close the *specific* candidate-state and final OFE digest omissions found above at static source level. The full positive/negative Rust results, direct ordinary typed-refusal/rollback, deferred-credit-chain poison matrix, and exact reached-guard RED remain pending; there is no private proof/admission yet. **Disposition:** HOLD for implementation and promotion.

## Same-reviewer C3 component execution disposition

**Supplied Ran, inspected but not rerun:** the candidate-custody build source receipt pins tree `ffa3db7845e0ad43d815044431f65feb1ff8a5753e60bcab0b60006ba701698d`, including member-restoration SHA `ec7e050716ec9338a5719ccaf761a473201f16089afbbebf61f2ee07566ba004` (the writer's earlier `f92ac…` message was stale, not observed drift). The `custody-v8-mapping-positive.json` and `custody-v8-mapping-poisons.json` receipts use that same unchanged tree, unchanged binary `1899502317a307d438b46d77e38a738cedfca8d59767ce89603bae5bfd22ade0`, unchanged pinned inputs, and each exit 0 in a fresh process. The positive test passes the exact twelve-child raw candidate tuple hashes, eight-slab/terminal-event mapping, final raw ending state/OFE hash and transitive deferred final-candidate validation; the poison test passes transaction and final-ending substitutions plus its earlier raw receipt/support/carry/event controls. Both stdout receipts report zero probe, direct and covered-native physics counters. My static inspection of the member source corresponds to this frozen `ec7e…` build snapshot. The prior **C3 candidate-state/final OFE digest omission is closed for this test-only, pinned-corpus correspondence component**. Its original-source provenance depends on the separate extraction and frozen-input receipts already noted; no capability is minted and no live owner installed.

**Remaining gate:** the separate GSI member test from this binary failed with a memory allocation error during repeated full target cloning, so it has no semantic positive or poison result yet. The writer is replacing that test's large clones with a small GSI projection; the fix requires a fresh exact-source execution. Direct ordinary typed refusal/rollback, deferred-credit-chain poison matrix, exact reached-guard RED, and subsequent private proof/constructor review remain open. **Authority implementation and promotion stay HOLD.**

## Same-reviewer preimplementation v9 controls review

**Static:** build source receipt `candidate-preimplementation-controls-build-source.json` pins tree `35046682115013913b1247de40565612ae727f407d55a342ffea032b99f44846`, including `member_restoration.rs` SHA `ffae6012…12be49` and `current_context_capture.rs` SHA `f85ffed8…313bc0`, matching my direct read. The GSI member test uses a small four-field projection of native operands instead of repeatedly cloning the 457 MB target (`member_restoration.rs:746-885`). The ordinary test directly calls `NativeSoilRestartAdmissionV1::authenticate` on actual resident/bootstrap/clock and asserts the typed `Identity("native soil proof accepted support or exact bootstrap")` plus unchanged serialized inputs (`:887-962`). The inactive-counter test begins with actual count-zero custody and separately poisons count, WB14, frame day/zero-bit supply and run identity (`:965-1047`). The phase test adds missing, duplicate, reordered and foreign deferred *credit groups*, with exact expected error precedence and unchanged original bytes (`current_context_capture.rs:4669-4743`). These are executable existing-seam controls; they create no private proof.

**Supplied Ran, inspected but not rerun:** v9 GSI, direct ordinary and phase-poison receipts each exit 0 with source/input/binary unchanged. The GSI stdout reports `prepare_advances: 0`, `validator_advances: 28`; all these receipts report zero mechanism/covered-native physical calls and no direct compute/commit/publication. This resolves the earlier GSI memory failure for the tested projection, proves the actual ordinary resident-only typed refusal/unchanged inputs, and covers the added deferred-credit-group poison cases in their isolated tests.

**Counter control still RED for an expected-error mistake:** `preimplementation-v9-counter-poisons` exits 101. Count-one and WB14-present assertions execute first; poison case 0 then mutates `foreign_frame.surface_liquid_shadow.continuations[0].day_index` directly and receives typed `Runtime(Identity("native inactive scheduler frame surface"))`, rather than the test's expected `"native inactive scheduler exact continuation"` (`member_restoration.rs:1038`; recorded stderr). The mutated frame is rejected at its canonical surface-byte guard before the deeper continuation comparison. This is source-defined guard precedence, not evidence of acceptance; an intended deeper exact-continuation control needs a separately valid altered surface-owner preimage. The later negative-zero and foreign-run cases did not execute in this failing run. No all-pass counter claim is available until an exact-source correction and rerun. The complete-bundle expected RED, no-mutation checks on that private-reader path, and private proof remain pending. **Disposition:** HOLD for implementation and promotion.

## Same-reviewer v10 counter correction and pending RED

**Static fix verification:** In the v10 S counter test, case 0 mutates a cloned frame's day cursor, recomputes its state SHA-256 and self-validates the altered state before asking the shared inactive-scheduler validator. It now reaches the intended typed `"native inactive scheduler exact continuation"` refusal. Case 1 preserves negative zero as a noncanonical frame poison and correctly expects the earlier typed `"native inactive scheduler frame surface"`; case 2 tests foreign run identity. The test also checks count-one and WB14-present typed refusals, original frame canonical bytes, and original projected native bytes. This is correct guard precedence; it does not weaken the validator.

**Supplied Ran, inspected but not rerun:** `preimplementation-v10-counter-poisons.json` and `preimplementation-v10-gsi-poisons.json` each record exit 0 in separate fresh processes with unchanged tree `af9fb17806e761cc60e3c690925d014660f7bc713d694247fd17decd30bc7070`, unchanged binary `19bcaa1542eb01a0c0705469ed46a896d9d2d8a697bcf05bff6ed73b4b6d1c50`, and unchanged inputs. Counter stdout reports one allowed run-frame construction and zero direct computation/commit, mechanism and covered-native physical calls; GSI stdout reports `prepare_advances: 0`, `validator_advances: 28` and the same zero forbidden physical work. The v9 counter-test expected-error issue is resolved for the exact v10 component. The v10 complete-bundle RED receipt was still running when this note was written; no RED disposition or private prototype gate is claimed yet.

## Same-reviewer complete-bundle RED and prototype gate

**Supplied Ran, independently inspected, not rerun:** `preimplementation-v10-complete-bundle-red.json` records exit 101 after 224.72 s, no timeout, unchanged source tree/binary/inputs; stderr shows the expected `expect` failure at the test-only private composition reader with exact error `canonical native constructor refusal: V9 real-consumer identity failure: native soil proof accepted support or exact bootstrap`. Stdout proves ordered archive days 0–3 and provider prefix PASS, actual native member decoded, prepared/original-climate GSI validation PASS, outer position 214 and independent native count 0/frozen surface/clock custody PASS, current/deferred/provisional joins PASS, then the ordinary native constructor returned precisely that soil guard. It reports zero mechanism maps/solves/probes, zero direct computation/commit/publication and zero covered-native litter/surface/WB14 physics calls; GSI `validator_advances: 7`, `prepare_advances: 0` are separate permitted validation work. The RED failure is therefore attributable to the missing **distinct private composite admission route**, not a position/GSI annotation or earlier correspondence error. Its test name correctly identifies private composition; its `expect` message still says “ordinary edge” and should be clarified when it becomes a positive test.

The previously reviewed v8 C3 positive/poisons, v9 direct ordinary typed refusal and credit-chain/phase poisons, and v10 GSI/counter poisons complete the **executable preimplementation evidence gate for beginning a bounded private-constructor prototype** under the draft sequencing rule. Only `snow_stage3_v11_member_restoration.rs` changed between v9 and v10 source receipts, for the scoped counter-test correction; the other prior component results remain attributable to unchanged source/input files. This is approval to start the separately authorized **private implementation work**, not approval of its future positive behavior, authority promotion, production restart, soil installation, physical successor, or conservation/restart qualification. The future implementation must revalidate the complete composite inside the private admission boundary, retain direct ordinary refusal, prove nonserialization/reuse/invalidation and exact join/typed-precedence controls, and demonstrate atomic unchanged-state refusal plus successful canonical later replay/install behavior before any positive diagnostic acceptance or promotion. The current RED asserts a future `Ok` through the test-only reader and prints guard provenance; it does not itself assert a byte-for-byte before/after snapshot of every live owner, so later rollback tests must supply that explicit evidence.

## Same-reviewer frozen-litter full-history factor review

**Static:** `v9_real_consumer_shadow.rs:787-877` factors the existing diagnostic frozen-litter restoration into one shared `diagnostic_decode_validate_frozen_litter_residents_v1` body. It retains the pinned LSE and surface-parent checks, canonical model/configuration/owner decodes, `FrozenLitterV3Resident::try_new`, full `restore_restart_authority` history validation, exact owner decode and `FrozenLitterV4Resident::try_restore`, returning the validated pair. The existing restore path invokes that helper and only then calls `install_restored_frozen_litter_v4_residents` on `self`; the new preflight invokes the same helper and discards the returned pair. Its static signature has no mutable consumer, so the preflight itself cannot install. The test-only file reader calls preflight after inert position/GSI/frame/current-phase joins and before ordinary native soil admission (`member_restoration.rs:696-699`). The new actual-member test projects only frozen residents, compares its bytes before/after and calls preflight (`:1059-1086`). This centralizes canonical logic rather than duplicating an alternate validator. Its historical native-drain reconstruction still performs the previously reviewed permitted local accounting validation; it is not a new physical successor.

**Build blocker, not a semantic refusal:** supplied `candidate-fullhistory-preflight-build.json` exits 101; stderr reports Rust E0282 at `v9_real_consumer_shadow.rs:868` because `diagnostic_native_field`'s generic type is not inferred through `.ok_or` into the declared `DiagnosticFrozenLitterResidentsV1` variable. An explicit `Option<DiagnosticFrozenLitterResidentsV1>` generic or intermediate binding is needed. No preflight runtime result or new authority-domain conclusion follows from this compile failure. After correction, a full-history refusal must be classified by its exact validator and retained preimage: an actual inconsistency in the frozen-litter authoritative history could be outside this private soil-custody admission, while a reader decode/typing defect remains in this bounded diagnostic implementation. Do not infer either category without the reached source guard and input evidence. **HOLD remains** on positive admission and authority promotion.

**Subsequent build and first reached guard:** the corrected build exits 0. `fullhistory-v11-member-preflight.json` then exits 101 after 37.10 s, unchanged source/binary/inputs and zero recorded mechanism/direct/GSI/covered-native physical work. The first failure is typed `Runtime(Identity("diagnostic frozen-litter LSE configuration/pinned authority join"))`; no later full-history validator was reached. The current member reader/test pass the authenticated main `pin.lse` as the frozen-litter pin, but the existing file reader explicitly carries distinct `pinned_lse_configuration` and `pinned_frozen_litter_lse_configuration` (`current_context_capture.rs:3470-3502,4016-4046`). The original B01 fixture derives the latter with `migrate_v2_configuration_to_v3(&shadow.lse_configuration)` (`v3_publication_retention_tests.rs:926-936`), and that canonical function validates V2, switches to the defined V3 model/version, reseals and validates V3 (`openwepp-land-surface-energy/src/v3_state.rs:119-135`). Deriving the frozen pin from already authenticated `pin.lse` through this canonical migration, then comparing captured frozen configuration, is a source-supported diagnostic reader correction. Copying captured frozen configuration as its own pin would be circular. This first guard alone does **not** establish an out-of-domain frozen-history defect or prove later preflight success; rerun must reach the full validator before classification. The accepted preimplementation RED remains prior evidence; positive admission/promotion remains HOLD.

**v12 same-reviewer fix verification:** The v12 source receipt pins tree `637ab022fe3f905d762b88903c58553ebd99c24f83a8b224bcce62f0ae82add5`, member reader SHA `973148e6ef98667cfa6123063b4af15833544a748915a35948ad671656020e5e` and shared diagnostic body SHA `acaccec24470e7d5d0140bde09301a540646f4c4903941c8859a76e1217a374f`; these matched my direct file hashes. `pinned_frozen_litter_lse` now calls canonical `migrate_v2_configuration_to_v3` on authenticated `pin.lse`, and the reader passes that result both to preflight and as the native diagnostic constructor's distinct frozen LSE pin (`member_restoration.rs:103-109,703-706,724-733`). The isolated test first confirms the wrong V2 pin yields the exact typed identity refusal, then runs the canonical V3 pin and compares projected native bytes before/after (`:1070-1105`). `fullhistory-v12-member-preflight.json` reports exit 0 after 265.14 s, unchanged source/binary/inputs and zero recorded mechanism, direct computation/commit/publication, GSI advance and covered-native physical calls. The **V2-versus-V3 pin finding and full-history preflight component are closed for this pinned specimen**; the preflight still installs no consumer owner. The long runtime is observational, not physical evolution evidence. This result does not establish a private proof, successful diagnostic restore, ordinary-entry change, or authority promotion. Those remain HOLD pending the separately reviewed prototype and postimplementation controls.

## Independent v13 prototype static rejection

**Static only:** I inspected preserved `prototype-v13-static-rejection.json` and its frozen `source-03fd1dedcec2a120d31ccb6c4686da1c41f13a059a67a6b1846d12d0a3876191.patch` for tree `e04417971f720c40cb46e24ad8ce291c8487b7293209e7ae430c95b2be404da8`. Parent rejected this implementation cut before build; no Rust or physical run is claimed. The earlier preimplementation RED gate remains satisfied; these are new prototype blockers.

**High — generic deferred soil admission bypasses complete-composite authority.** New crate-visible `NativeSoilRestartAdmissionV1::authenticate_deferred_original_v1` (`native_soil_restart_admission.rs`, patch around line 13995) accepts resident/bootstrap/LSE/clock/position/logical revision and rejects only zero clock/next day after ordinary-style identity checks. Its signature has no deferred candidate, ordered credits, child support/clock/event correspondence, authenticated archive/current phase, or private complete-bundle token. Other crate code could mint the same later-clock proof without the source-defined correspondence required by draft `INV-LANDSURFACEENERGY-167`. `diagnostic_mint_deferred_composite_admission_v1` (`v9_real_consumer_shadow.rs`, patch around 11481) calls that generic helper after validating naked GSI receipt arguments and frozen history, but its own signature likewise carries no authenticated archive, current phase, trial chain or C3 correspondence capability. Sequential checks in the file reader are not an ownership-bound proof input. Gate the exceptional soil constructor behind a reader-private, unforgeable, one-use token that is minted only from the *complete* validated composite and bound to the exact captured owner/clock/archive/receipt revision; direct ordinary entrypoints must retain their existing refusal.

**High — prior GSI and native count installation order prevents the claimed restoration.** The v13 private restore passes its proof to `diagnostic_restore_native_consumer_from_proof_v1` before installing the inactive scheduler count (`v9_real_consumer_shadow.rs`, patch around 11684-11725). The ordinary `try_new_with_soil_resident` initializes provider GSI digest to 64 zeroes, and `diagnostic_restore_captured_residents_v1` compares it with the captured committed day-3 digest; v13 stores the typed prior receipt but never installs that digest before the unchanged equality. The new count installation is also after that equality. A positive private restore cannot pass these existing checks as ordered. Any correction must install only the authenticated prior receipt and count within the private, typed, fully bound constructor before those exact validations, without assigning a prepared digest or relaxing ordinary entrypoints.

**High — one-use/non-wire proof lifecycle is asserted by type shape but untested and incompletely bound.** `DeferredNativeCompositeAdmissionV1` lacks `Clone`/serde and is moved into the constructor (`v9_real_consumer_shadow.rs`, patch around 11289,11684), but no retained test demonstrates forbidden serialization, stale capture/clock revision, double use, reconstruction invalidation, premature promotion or atomic rollback. The capability stores native proof plus receipts/state/cursor without a validated archive/phase/correspondence witness or complete captured-owner digest. Its private use rechecks only captured GSI state/cursor before invoking a general proof-based constructor. The mandated lifecycle and typed-precedence controls in `soil-custody.md:442` remain open; absence of derive traits is not sufficient acceptance evidence.

**Medium — authenticated full history is discarded and replayed instead of carried across the proof boundary.** The v13 mint calls `diagnostic_preflight_frozen_litter_residents_v1`, which decodes and fully validates V3/V4 history, then drops the pair; proof-based restoration later calls the same shared validator through `diagnostic_restore_captured_residents_v1`. This does not duplicate Rust algorithm text, but it repeats the expensive canonical work and leaves the mint without an owned typed resident pair bound to its use. The v12 preflight was 265 s on the specimen. Carry the validated pair as a consuming private capability, or justify mandatory revalidation at use with exact identity binding and bounded cost. Neither repeated validation nor a bare checksum may replace complete validation.

**Disposition:** v13 prototype rejected; do not build or run this cut as positive admission evidence. Correct the complete-composite ownership boundary, GSI/count sequencing and lifecycle controls, then independently review the next frozen source and its focused tests. No production authority, physical successor, or promotion is granted.

## Same-reviewer corrected private core static review

**Static only:** I inspected the corrected private core at the source receipt `candidate-private-core-fixed-build-source.json`, tree `e8708930670bd647cfd838fed0abc64c95bf34bcb4cd80d6c8d6623b57c228eb`. No build or runtime result is claimed here. The exceptional soil authenticator is now private to the included native-admission module (`v9_real_consumer_shadow/native_soil_restart_admission.rs:92-123`), and the reader-private `ValidatedDeferredContextV1` is minted only in `snow_stage3_v11_member_restoration.rs:799-807`. Actual `member_restoration::restore` calls the raw retained-trial/subslab correspondence validator before archive/provider/current-phase processing (`:1193-1207`). The private consumer consumes that capability before minting a separate native proof (`v9_real_consumer_shadow.rs:931-979`). The private path carries the fully validated frozen V3/V4 resident pair, and passes `Some` authenticated prior GSI receipt to the common constructor; it installs the authenticated prior receipt and inactive scheduler before captured-state equality (`:907-930,1013-1057`). The ordinary path still passes `None` and follows its own original admission. These are source-level improvements to the v13 blockers, not positive admission evidence.

**High — incomplete binding of arguments at the one-use boundary.** `ValidatedDeferredContextV1` stores digests of captured native, soil, clock, phase and *raw* lane-map JSON (`member_restoration.rs:240-298`). The consuming private method separately accepts `lane_maps: Vec<DiagnosticNativeLaneMapV1>` and passes that vector to restoration without comparing it to the token-bound raw JSON (`v9_real_consumer_shadow.rs:931-979`); the current reader happens to decode them from one value (`member_restoration.rs:799-826`), but the private consumer interface permits substitution. Likewise the committed bootstrap and full dynamic hydrology frame are separate use arguments absent from token binding. The bootstrap is only structurally validated for configuration/run by the private soil helper and then unused by the proof-based constructor (`native_soil_restart_admission.rs:92-123`; `v9_real_consumer_shadow.rs:857-862`). Frame custody checks cover the inactive surface and run identity, and the later six-owner comparison covers selected hydrology projection fields, while the dynamic capture also contains transfer/publication/carry values outside that projection (`canonical_owner_bytes.rs:1164-1205`; `snow_stage3_v11_current_context_capture.rs:384-420`). This is an interface authority gap, not demonstrated divergence of the pinned reader call. Bind the exact typed bootstrap/frame/lane-map use to the reader-minted capability, or derive all use operands from the validated reader bundle within the private boundary, and test substitution refusal before positive qualification.

**Residual gate:** The private proof lifecycle tests for serialization refusal, one-use/invalidation, stale capture/revision, premature promotion and rollback are still absent in this cut. The fixed build was pending when reviewed. Therefore no positive restore, numerical equivalence or promotion claim follows; authority admission remains HOLD.

## Same-reviewer lifecycle-cut source review

**Static only:** The inspected `candidate-private-lifecycle-build-source.json` pins tree `a354a190d84a64e1018c6f69170dd9e0f0ae6109dbe516665d799915e16807d0`. The reader-minted token now binds the exact committed bootstrap, captured dynamic frame and constructor inputs as well as the prior native/soil/clock/phase/raw-lane values (`snow_stage3_v11_member_restoration.rs:261-390`). The private entry takes and removes the `Option` token before checking its source digest (`v9_real_consumer_shadow.rs:1003-1040`), and the reader's actual lifecycle control first authenticates the real bundle and then tests altered-phase refusal and consumed reuse (`member_restoration.rs:919-1010`). The retained raw correspondence still precedes reader mint, and the ordinary admission path remains distinct. These are source-level corrections; no execution result is inferred.

**High — this frozen cut does not compile.** The private entry signature retains an extra `pinned_hydrology_constructor_inputs_ref: &DirectRunConstructorInputs` (`v9_real_consumer_shadow.rs:1013`), but reader calls supply only the `Arc` input argument (`member_restoration.rs:956-964,979-986,1014-1027`). Its call into `diagnostic_restore_native_consumer_from_proof_v1` also passes `lane_maps` without defining it after removal of the independent typed lane-map parameter (`v9_real_consumer_shadow.rs:1090-1097`). The token's frame-digest mint and validation call `capture_direct_run_frame_dynamic(frame)` (`member_restoration.rs:312,353`), although its canonical function requires `(frame, pinned_inputs, surface_configuration)` (`snow_stage3_v11_current_context_capture.rs:196-200`). Correct the signature/call sites and derive typed maps directly from the token-bound raw value before a positive probe; a build receipt is required.

**High — surface use remains unbound at token validation.** The token stores `surface_configuration` at mint (`member_restoration.rs:261,291`), but `validate_bound_v1`/`consume_gsi_v1` neither take nor compare the private constructor's separate `pinned_surface_configuration` (`:320-390`; `v9_real_consumer_shadow.rs:1003-1040`). The actual reader passes the same pinned surface, and downstream semantic validators may reject a foreign substitute, so this is an unclosed private-interface use boundary rather than demonstrated specimen divergence. Check the exact surface value at token consumption, or carry that validated value as the only constructor input; include a source-bound substitution refusal control.

**Residual risk:** The non-Clone/non-serde compile-time trait test (`v9_real_consumer_shadow.rs:3998-4027`) and attempted reuse control address part of the lifecycle requirement. They do not yet supply executed evidence for the real altered-phase/reuse control, successful exact-owner restoration, no premature publication/physical advancement, or rollback on late failure. Positive admission and authority promotion remain HOLD.

## Same-reviewer completed-controls cut before first positive probe

**Static only:** Source receipt `candidate-private-controls-complete-build-source.json` pins tree `56735f01aa82e66f230c4ae74f1694bb2a3e7d27eab83672758b37ee40d6559b`; build/format results were pending during this read. The previous compile findings are corrected: token frame capture now calls the canonical three-argument function; private entry takes one installed `Arc` input, removes its token on entry, checks that same input, and decodes typed lane maps from token-bound raw JSON (`snow_stage3_v11_member_restoration.rs:261-413`; `v9_real_consumer_shadow.rs:1003-1108`). Token consumption compares canonical pinned surface configuration before returning the authenticated GSI preimages. The real-reader lifecycle control uses the authenticated source path, checks another valid resident in the bootstrap slot, changed raw lanes, dynamic transfer carry, constructor run and missing native fields; it then calls the actual private entry with altered deferred posture and proves that the failed attempt consumes the slot (`member_restoration.rs:955-1150`). Native, soil and frame originals are compared unchanged. This closes the specific earlier **use-boundary source findings** at static level. I found no remaining source-level binding blocker to attempting the first original-corpus positive diagnostic run, conditional on the pending build passing.

**Residual required controls before acceptance/promotion:** The new actual-entry lifecycle control reaches an early source-bound refusal on altered posture; its other substitution assertions call the borrowed token validator rather than the full native constructor. This is appropriate bounded control, but it does not alone prove the draft `OBL-LANDSURFACEENERGY-C-023` independent committed/prepared receipt error precedence, combined custody-plus-later-promotion poison, or byte-exact rollback after a late validation failure (`soil-custody.md:193-202,442`). Supplied earlier GSI phase and custody poisons may cover portions, but exact integrated results and precedence still need reconciliation. The positive diagnostic test's comment at `snow_stage3_v11_current_context_capture.rs:5630-5636` still describes an ordinary-admission expected red and should be updated to match the new private route; this is documentation drift, not an authority bypass. No positive run, no comparator parity, and no promotion are claimed by this review.

## Same-reviewer v15 terminal source and trait evidence

**Static and supplied Ran, not rerun:** `candidate-private-controls-array-fixed-build-source.json`, `candidate-v15-capability-traits-source.json` and the still-running lifecycle source receipt all pin tree `ab221bc5280eaee458be9a6066ba628efe292c0a17394ed1e754ff8f265d8eed`; my direct SHA checks match member reader `a8887bf5…`, v9 consumer `a1b68d17…` and native soil admission `d4f39a77…`. The only observed v15 correction to the scoped lifecycle logic is indexing the first dynamic transfer carry element in the borrowed substitution control (`member_restoration.rs:1040`), matching the array type. The build receipt exits 0 after 20.13 s with source and pins unchanged. `candidate-v15-capability-traits.json` runs frozen binary `d9158bba…` on the same source/input custody, exits 0 and reports one passed `deferred_capabilities_are_non_wire_and_non_copyable` test. This is direct evidence for the defined negative trait bounds, not evidence of an actual restore.

**Disposition:** I see no remaining source-level binding blocker to the first exploratory original-corpus positive probe **after** the actual lifecycle control returns a passing, unchanged-source receipt. At this review instant `candidate-v15-lifecycle-controls.json` has no `exit_code`; it is running and must not be counted as passed. Even with a passing positive diagnostic, authority acceptance/promotion remains HOLD pending integrated source-defined precedence, combined custody/later-promotion refusal and exact rollback controls, plus independent inspection of those results. No physical or comparator test was run by this reviewer.

**Lifecycle terminal update — supplied Ran:** `candidate-v15-lifecycle-controls.json` subsequently completed exit 0, no timeout, 401.6366 s, on tree `ab221bc5…` and frozen binary `d9158bba…`, with source, binary and all pinned inputs unchanged. Its stdout reports four authenticated archive days, provider prefix, original-climate and GSI phase, native outer position 214 versus inactive captured count 0, and current/deferred/provisional prechecks before the refusal control. The real private entry refused the altered deferred posture through the bound-use guard, consumed the slot, and refused reuse; the separate borrowed checks covered bootstrap/lane/frame/input/native substitutions. Audit counters record twelve run-frame constructions, seven validator-only GSI advances, zero provider prepares, zero direct compute/state mutation/commit/publication, and zero mechanism or covered-native physical calls. This **closes the scoped early-bound-use and consumed-reuse lifecycle control for this pinned corpus** and permits an exploratory positive diagnostic probe. It does not execute a full native proof or late install/rollback branch, and does not discharge the remaining integrated precedence and combined-poison obligations. The main science draft remains unpromoted; positive authority acceptance remains HOLD.

## Same-reviewer v15 full private probe terminal classification

**Supplied Ran, independently inspected receipt; not rerun:** `candidate-v15-private-restoration-probe.json` completed exit 101 after 502.5070 s, no timeout, on unchanged tree `ab221bc5280eaee458be9a6066ba628efe292c0a17394ed1e754ff8f265d8eed`, frozen binary `d9158bba8e25413aa15bc316ee5f09618a11de3f3f5ef3daf62076e02ef05e96`, and unchanged pinned files/inputs. Stdout reaches archive days 0–3, provider prefix, original-climate/GSI authentication, outer position 214 versus native inactive count 0, and current/deferred/provisional checks. It then reports `canonical native constructor: accepted original operands`. The final panic at `snow_stage3_v11_current_context_capture.rs:5664` reports reader error `"native six-owner projection"` from `snow_stage3_v11_member_restoration.rs:1155-1162`. Thus the private constructor **did accept its original operands**, but the returned consumer failed the exact retained six-owner-byte comparison; complete diagnostic admission did **not** pass. The receipt does not identify which owner byte differs, so neither a producer defect nor a safe reader correction can be inferred yet.

The audit reports seven run-frame constructions, nine validator-only GSI advances, zero provider preparation, zero direct compute, state mutation, commit or publication, and zero mechanism/covered-native physical calls. This failure is downstream of the private proof and native constructor; it is not the historical ordinary soil-support guard. The result supports bounded investigation of the projection discrepancy only, preserving the original retained target and ordinary refusal. The missing integrated precedence, combined custody/later-promotion and late-failure rollback controls remain open. Authority acceptance, canonical promotion, conservation/restart qualification and WB14 cadence remain HOLD.

**Static reach distinction:** The producer captures `complete_owner_canonical_bytes` by the same `consumer.canonical_v11_parent_owner_state_bytes()` call used by the reader after restoration (`snow_stage3_v11_current_context_capture.rs:1709-1716`; `snow_stage3_v11_member_restoration.rs:1155-1162`). That serializer produces exactly six named owner entries, vegetation, land-surface-energy, surface-liquid, hydrology, biogeochemistry and soil-thermal (`canonical_owner_bytes.rs:1254-1271`). There is no evident alternate schema function or missing seventh owner in this comparison. The actual differing owner/key and byte preimage are absent from the probe receipt. Source inspection alone therefore cannot identify an obvious representation mismatch versus a missing or reconstructed state field; it only establishes that some returned owner bytes differ from the retained live producer projection. A byte-level six-owner diagnostic, without altering admission guards or source authority, is needed before causal classification.
