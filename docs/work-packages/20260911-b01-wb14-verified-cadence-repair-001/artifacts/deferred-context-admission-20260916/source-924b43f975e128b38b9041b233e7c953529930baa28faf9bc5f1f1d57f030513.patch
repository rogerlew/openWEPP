--- a/crates/openwepp-coupled-time/src/policy14_lse_owner.rs
+++ b/crates/openwepp-coupled-time/src/policy14_lse_owner.rs
@@ -1,7 +1,7 @@
 //! Experimental adopter-owned LSE component identity transport. This module
 //! authenticates opaque canonical bytes, never physical state or regime.
+use crate::{CoupledTimeError, Digest32, digest_bytes};
 use serde::{Deserialize, Serialize};
-use crate::{CoupledTimeError, Digest32, digest_bytes};
 
 pub const POLICY14_LSE_COMPONENT_SCHEMA: &str =
     "OPENWEPP_B01_LSE_CANONICAL_COMPONENT_BYTES_V2_POLICY14";
@@ -29,62 +29,106 @@
 }
 
 impl Policy14LseOwnerTransport {
-    pub fn new(runtime_authority: Policy14RuntimeAuthority, active: Vec<u8>, native: Vec<u8>, exact: Vec<u8>) -> Result<Self, CoupledTimeError> {
+    pub fn new(
+        runtime_authority: Policy14RuntimeAuthority,
+        active: Vec<u8>,
+        native: Vec<u8>,
+        exact: Vec<u8>,
+    ) -> Result<Self, CoupledTimeError> {
         if active.is_empty() || native.is_empty() || exact.is_empty() {
             return Err(CoupledTimeError::NonCanonicalIdentity);
         }
-        Ok(Self {schema: POLICY14_LSE_COMPONENT_SCHEMA.into(), policy_id: 14, runtime_authority,
-            active_covered_sha256: digest_bytes(&active), active_covered_runtime_lse_v1: active,
-            native_litter_sha256: digest_bytes(&native), native_litter_lse_v3: native,
-            native_exact_sha256: digest_bytes(&exact), native_exact_surface_enthalpy_v4: exact})
+        Ok(Self {
+            schema: POLICY14_LSE_COMPONENT_SCHEMA.into(),
+            policy_id: 14,
+            runtime_authority,
+            active_covered_sha256: digest_bytes(&active),
+            active_covered_runtime_lse_v1: active,
+            native_litter_sha256: digest_bytes(&native),
+            native_litter_lse_v3: native,
+            native_exact_sha256: digest_bytes(&exact),
+            native_exact_surface_enthalpy_v4: exact,
+        })
     }
 
     pub fn canonical_bytes(&self) -> Result<Vec<u8>, CoupledTimeError> {
-        if self.schema != POLICY14_LSE_COMPONENT_SCHEMA || self.policy_id != 14
-            || self.active_covered_runtime_lse_v1.is_empty() || self.native_litter_lse_v3.is_empty()
+        if self.schema != POLICY14_LSE_COMPONENT_SCHEMA
+            || self.policy_id != 14
+            || self.active_covered_runtime_lse_v1.is_empty()
+            || self.native_litter_lse_v3.is_empty()
             || self.native_exact_surface_enthalpy_v4.is_empty()
             || self.active_covered_sha256 != digest_bytes(&self.active_covered_runtime_lse_v1)
             || self.native_litter_sha256 != digest_bytes(&self.native_litter_lse_v3)
-            || self.native_exact_sha256 != digest_bytes(&self.native_exact_surface_enthalpy_v4) {
+            || self.native_exact_sha256 != digest_bytes(&self.native_exact_surface_enthalpy_v4)
+        {
             return Err(CoupledTimeError::NonCanonicalIdentity);
         }
         serde_json::to_vec(self).map_err(|_| CoupledTimeError::NonCanonicalIdentity)
     }
 
     pub fn from_canonical_bytes(bytes: &[u8]) -> Result<Self, CoupledTimeError> {
-        let value: Self = serde_json::from_slice(bytes).map_err(|_| CoupledTimeError::NonCanonicalIdentity)?;
-        if value.canonical_bytes()? != bytes { return Err(CoupledTimeError::NonCanonicalIdentity); }
+        let value: Self =
+            serde_json::from_slice(bytes).map_err(|_| CoupledTimeError::NonCanonicalIdentity)?;
+        if value.canonical_bytes()? != bytes {
+            return Err(CoupledTimeError::NonCanonicalIdentity);
+        }
         Ok(value)
     }
 }
-
 
 #[cfg(test)]
 mod tests {
     use super::*;
     #[test]
     fn policy14_mode_is_required_closed_and_changes_complete_identity() {
-        let value = Policy14LseOwnerTransport::new(Policy14RuntimeAuthority::V11SnowCovered,
-            b"active".to_vec(), b"native".to_vec(), b"exact".to_vec()).unwrap();
+        let value = Policy14LseOwnerTransport::new(
+            Policy14RuntimeAuthority::V11SnowCovered,
+            b"active".to_vec(),
+            b"native".to_vec(),
+            b"exact".to_vec(),
+        )
+        .unwrap();
         let bytes = value.canonical_bytes().unwrap();
         let mut changed = value.clone();
         changed.runtime_authority = Policy14RuntimeAuthority::V10NonpositiveAssimilation;
-        assert_ne!(digest_bytes(&bytes), digest_bytes(&changed.canonical_bytes().unwrap()));
+        assert_ne!(
+            digest_bytes(&bytes),
+            digest_bytes(&changed.canonical_bytes().unwrap())
+        );
         for index in 0..3 {
             let mut poison = serde_json::to_value(&value).unwrap();
             match index {
-                0 => { poison.as_object_mut().unwrap().remove("runtime_authority"); }
+                0 => {
+                    poison.as_object_mut().unwrap().remove("runtime_authority");
+                }
                 1 => poison["runtime_authority"] = serde_json::json!("foreign"),
-                _ => poison["schema"] = serde_json::json!("OPENWEPP_B01_LSE_CANONICAL_COMPONENT_BYTES_V1_POLICY14"),
+                _ => {
+                    poison["schema"] =
+                        serde_json::json!("OPENWEPP_B01_LSE_CANONICAL_COMPONENT_BYTES_V1_POLICY14")
+                }
             }
-            assert!(Policy14LseOwnerTransport::from_canonical_bytes(&serde_json::to_vec(&poison).unwrap()).is_err());
+            assert!(
+                Policy14LseOwnerTransport::from_canonical_bytes(
+                    &serde_json::to_vec(&poison).unwrap()
+                )
+                .is_err()
+            );
         }
     }
     #[test]
     fn policy14_transport_checks_each_order_preserving_poison() {
-        let original = Policy14LseOwnerTransport::new(Policy14RuntimeAuthority::V11SnowCovered, b"active".to_vec(), b"native".to_vec(), b"exact".to_vec()).unwrap();
+        let original = Policy14LseOwnerTransport::new(
+            Policy14RuntimeAuthority::V11SnowCovered,
+            b"active".to_vec(),
+            b"native".to_vec(),
+            b"exact".to_vec(),
+        )
+        .unwrap();
         let bytes = original.canonical_bytes().unwrap();
-        assert_eq!(Policy14LseOwnerTransport::from_canonical_bytes(&bytes).unwrap(), original);
+        assert_eq!(
+            Policy14LseOwnerTransport::from_canonical_bytes(&bytes).unwrap(),
+            original
+        );
         for index in 0..8 {
             let mut poison = original.clone();
             match index {
@@ -97,10 +141,21 @@
                 6 => poison.native_litter_sha256 = Digest32::zero(),
                 _ => poison.native_exact_sha256 = Digest32::zero(),
             }
-            assert!(Policy14LseOwnerTransport::from_canonical_bytes(&serde_json::to_vec(&poison).unwrap()).is_err());
+            assert!(
+                Policy14LseOwnerTransport::from_canonical_bytes(
+                    &serde_json::to_vec(&poison).unwrap()
+                )
+                .is_err()
+            );
         }
         let mut swapped = original;
-        std::mem::swap(&mut swapped.active_covered_runtime_lse_v1, &mut swapped.native_litter_lse_v3);
-        assert!(Policy14LseOwnerTransport::from_canonical_bytes(&serde_json::to_vec(&swapped).unwrap()).is_err());
+        std::mem::swap(
+            &mut swapped.active_covered_runtime_lse_v1,
+            &mut swapped.native_litter_lse_v3,
+        );
+        assert!(
+            Policy14LseOwnerTransport::from_canonical_bytes(&serde_json::to_vec(&swapped).unwrap())
+                .is_err()
+        );
     }
 }
--- a/crates/openwepp-coupled-time/src/transaction.rs
+++ b/crates/openwepp-coupled-time/src/transaction.rs
@@ -41,16 +41,24 @@
 }
 
 pub(crate) fn owner_set_digest(owners: &[OwnerState]) -> Result<Digest32, CoupledTimeError> {
-    complete_owner_digest_set_digest(&owners.iter().map(|owner|
-        (owner.owner_id().to_owned(), owner.state_digest())).collect::<Vec<_>>())
+    complete_owner_digest_set_digest(
+        &owners
+            .iter()
+            .map(|owner| (owner.owner_id().to_owned(), owner.state_digest()))
+            .collect::<Vec<_>>(),
+    )
 }
 
 /// Hash an authenticated ordered owner-digest projection with the exact same
 /// framing as the complete byte-bearing owner set. This verifies canonical
 /// structure only; callers must authenticate the source of each digest.
-pub fn complete_owner_digest_set_digest(owners: &[(String, Digest32)]) -> Result<Digest32, CoupledTimeError> {
-    if owners.is_empty() || owners.iter().any(|(name, _)| name.is_empty())
-        || owners.windows(2).any(|pair| pair[0].0 >= pair[1].0) {
+pub fn complete_owner_digest_set_digest(
+    owners: &[(String, Digest32)],
+) -> Result<Digest32, CoupledTimeError> {
+    if owners.is_empty()
+        || owners.iter().any(|(name, _)| name.is_empty())
+        || owners.windows(2).any(|pair| pair[0].0 >= pair[1].0)
+    {
         return Err(CoupledTimeError::NonCanonicalIdentity);
     }
     let count = u32::try_from(owners.len()).map_err(|_| CoupledTimeError::ArithmeticOverflow)?;
@@ -796,10 +804,18 @@
     use super::*;
     #[test]
     fn authenticated_digest_projection_matches_owner_bytes_and_rejects_reordering() {
-        let owners = vec![OwnerState::new("a".into(), vec![1, 2]).unwrap(),
-            OwnerState::new("b".into(), vec![3, 4]).unwrap()];
-        let mut records = owners.iter().map(|owner| (owner.owner_id().to_owned(), owner.state_digest())).collect::<Vec<_>>();
-        assert_eq!(complete_owner_set_digest(&owners).unwrap(), complete_owner_digest_set_digest(&records).unwrap());
+        let owners = vec![
+            OwnerState::new("a".into(), vec![1, 2]).unwrap(),
+            OwnerState::new("b".into(), vec![3, 4]).unwrap(),
+        ];
+        let mut records = owners
+            .iter()
+            .map(|owner| (owner.owner_id().to_owned(), owner.state_digest()))
+            .collect::<Vec<_>>();
+        assert_eq!(
+            complete_owner_set_digest(&owners).unwrap(),
+            complete_owner_digest_set_digest(&records).unwrap()
+        );
         records.reverse();
         assert!(complete_owner_digest_set_digest(&records).is_err());
         assert!(complete_owner_digest_set_digest(&[]).is_err());
--- a/crates/openwepp-hillslope-orchestrator/src/canonical_owner_bytes.rs
+++ b/crates/openwepp-hillslope-orchestrator/src/canonical_owner_bytes.rs
@@ -2387,16 +2387,30 @@
 impl DirectV10RealConsumerShadow {
     /// Physical observation only: enclosing projections are retained for joins,
     /// and the independent inventory reader must count each physical store once.
-    pub(crate) fn accuracy_inventory_owners(&self) -> Result<serde_json::Value, DirectV11RealConsumerError> {
-        let owners = self.canonical_v11_parent_owner_state_bytes()?.into_iter()
-            .map(|(name,bytes)| Ok((name,serde_json::from_slice::<serde_json::Value>(&bytes)?)))
-            .collect::<Result<BTreeMap<_,_>,serde_json::Error>>()?;
-        let native_surface_owner = self.frozen_litter_v3_resident().map(|v| {
-            let c=v.surface_configuration();
-            let bytes=v.surface_owner().canonical_bytes(c.parent(),Some(c))
-                .map_err(|_|DirectV11RealConsumerError::Identity("inventory native surface canonical bytes"))?;
-            serde_json::from_slice::<serde_json::Value>(&bytes).map_err(DirectV11RealConsumerError::from)
-        }).transpose()?;
+    pub(crate) fn accuracy_inventory_owners(
+        &self,
+    ) -> Result<serde_json::Value, DirectV11RealConsumerError> {
+        let owners = self
+            .canonical_v11_parent_owner_state_bytes()?
+            .into_iter()
+            .map(|(name, bytes)| Ok((name, serde_json::from_slice::<serde_json::Value>(&bytes)?)))
+            .collect::<Result<BTreeMap<_, _>, serde_json::Error>>()?;
+        let native_surface_owner = self
+            .frozen_litter_v3_resident()
+            .map(|v| {
+                let c = v.surface_configuration();
+                let bytes = v
+                    .surface_owner()
+                    .canonical_bytes(c.parent(), Some(c))
+                    .map_err(|_| {
+                        DirectV11RealConsumerError::Identity(
+                            "inventory native surface canonical bytes",
+                        )
+                    })?;
+                serde_json::from_slice::<serde_json::Value>(&bytes)
+                    .map_err(DirectV11RealConsumerError::from)
+            })
+            .transpose()?;
         Ok(serde_json::json!({"owners":owners,"native_surface_owner":native_surface_owner}))
     }
 }
--- a/crates/openwepp-hillslope-orchestrator/src/direct_runtime.rs
+++ b/crates/openwepp-hillslope-orchestrator/src/direct_runtime.rs
@@ -198,8 +198,8 @@
     zero_duration_snow_liquid_receipt_set_sha256,
 };
 pub(crate) use surface_liquid_owner::{
-    DirectZeroDurationSnowLiquidInputV1, validate_zero_duration_snow_liquid_surface_delta_v1,
-    PositiveSupportSnowLiquidAllocationV1, allocate_positive_support_snow_liquid_v1,
+    DirectZeroDurationSnowLiquidInputV1, PositiveSupportSnowLiquidAllocationV1,
+    allocate_positive_support_snow_liquid_v1, validate_zero_duration_snow_liquid_surface_delta_v1,
 };
 pub use surface_liquid_owner_v3_exact_enthalpy::*;
 pub use surface_liquid_owner_v4_projection::*;
--- a/crates/openwepp-hillslope-orchestrator/src/direct_runtime/laned_active.rs
+++ b/crates/openwepp-hillslope-orchestrator/src/direct_runtime/laned_active.rs
@@ -70,7 +70,10 @@
 /// seed for geometry. Daily `LAI`/`canhgt` are consumed from the post-growth
 /// day frame at route time.
 #[derive(Debug, Clone, Copy, PartialEq)]
-#[cfg_attr(feature = "persisted-restart-v1", derive(serde::Serialize, serde::Deserialize))]
+#[cfg_attr(
+    feature = "persisted-restart-v1",
+    derive(serde::Serialize, serde::Deserialize)
+)]
 #[cfg_attr(feature = "persisted-restart-v1", serde(deny_unknown_fields))]
 pub struct DirectLanedActiveLaneConfig {
     pub slplen_m: f64,
@@ -133,7 +136,10 @@
 /// Active mesh policy. Rev 45 production default is target `dx = 5 m`; the
 /// explicit target-`dx` selector remains a diagnostic override.
 #[derive(Debug, Clone, Copy, PartialEq)]
-#[cfg_attr(feature = "persisted-restart-v1", derive(serde::Serialize, serde::Deserialize))]
+#[cfg_attr(
+    feature = "persisted-restart-v1",
+    derive(serde::Serialize, serde::Deserialize)
+)]
 #[cfg_attr(feature = "persisted-restart-v1", serde(deny_unknown_fields))]
 pub enum DirectLanedActiveMeshPolicy {
     FixedCells {
@@ -295,7 +301,10 @@
 /// when `OPENWEPP_LANED_ACTIVE=1`. Its presence IS the activation selector
 /// inside the orchestrator.
 #[derive(Debug, Clone, PartialEq)]
-#[cfg_attr(feature = "persisted-restart-v1", derive(serde::Serialize, serde::Deserialize))]
+#[cfg_attr(
+    feature = "persisted-restart-v1",
+    derive(serde::Serialize, serde::Deserialize)
+)]
 #[cfg_attr(feature = "persisted-restart-v1", serde(deny_unknown_fields))]
 pub struct DirectLanedActiveConfig {
     pub lanes: Vec<DirectLanedActiveLaneConfig>,
@@ -341,7 +350,10 @@
 /// Optional diagnostic trace detail target. Indices are zero-based inside the
 /// orchestrator; runner env parsing accepts one-based `sim_day:lane`.
 #[derive(Debug, Clone, Copy, PartialEq, Eq)]
-#[cfg_attr(feature = "persisted-restart-v1", derive(serde::Serialize, serde::Deserialize))]
+#[cfg_attr(
+    feature = "persisted-restart-v1",
+    derive(serde::Serialize, serde::Deserialize)
+)]
 #[cfg_attr(feature = "persisted-restart-v1", serde(deny_unknown_fields))]
 pub struct DirectLanedActiveTraceDetailFilter {
     pub day_index: usize,
@@ -356,7 +368,10 @@
 }
 
 #[derive(Debug, Clone, PartialEq)]
-#[cfg_attr(feature = "persisted-restart-v1", derive(serde::Serialize, serde::Deserialize))]
+#[cfg_attr(
+    feature = "persisted-restart-v1",
+    derive(serde::Serialize, serde::Deserialize)
+)]
 #[cfg_attr(feature = "persisted-restart-v1", serde(deny_unknown_fields))]
 pub struct DirectLanedActiveTraceDetail {
     pub mesh_cell_count: usize,
@@ -371,7 +386,10 @@
 }
 
 #[derive(Debug, Clone, Copy, PartialEq)]
-#[cfg_attr(feature = "persisted-restart-v1", derive(serde::Serialize, serde::Deserialize))]
+#[cfg_attr(
+    feature = "persisted-restart-v1",
+    derive(serde::Serialize, serde::Deserialize)
+)]
 #[cfg_attr(feature = "persisted-restart-v1", serde(deny_unknown_fields))]
 pub struct DirectLanedActiveStageLimiterTrace {
     pub reductions: u32,
@@ -381,7 +399,10 @@
 }
 
 #[derive(Debug, Clone, Copy, PartialEq)]
-#[cfg_attr(feature = "persisted-restart-v1", derive(serde::Serialize, serde::Deserialize))]
+#[cfg_attr(
+    feature = "persisted-restart-v1",
+    derive(serde::Serialize, serde::Deserialize)
+)]
 #[cfg_attr(feature = "persisted-restart-v1", serde(deny_unknown_fields))]
 pub struct DirectLanedActiveTvdTrace {
     pub scale: f64,
@@ -392,7 +413,10 @@
 }
 
 #[derive(Debug, Clone, Copy, PartialEq)]
-#[cfg_attr(feature = "persisted-restart-v1", derive(serde::Serialize, serde::Deserialize))]
+#[cfg_attr(
+    feature = "persisted-restart-v1",
+    derive(serde::Serialize, serde::Deserialize)
+)]
 #[cfg_attr(feature = "persisted-restart-v1", serde(deny_unknown_fields))]
 pub struct DirectLanedActiveStepTraceRecord {
     pub step_index: u64,
@@ -438,7 +462,10 @@
 /// Run-level evidence summary accumulated by the executor and surfaced by
 /// the runner manifest.
 #[derive(Debug, Clone, PartialEq)]
-#[cfg_attr(feature = "persisted-restart-v1", derive(serde::Serialize, serde::Deserialize))]
+#[cfg_attr(
+    feature = "persisted-restart-v1",
+    derive(serde::Serialize, serde::Deserialize)
+)]
 #[cfg_attr(feature = "persisted-restart-v1", serde(deny_unknown_fields))]
 pub struct DirectLanedActiveRunSummary {
     pub mesh_policy: DirectLanedActiveMeshPolicySummary,
@@ -476,7 +503,10 @@
 /// Rev-38 diagnostic mesh-adjudication row. The runner serializes these rows
 /// only under the explicit `OPENWEPP_LANED_ACTIVE_TRACE=1` evidence opt-in.
 #[derive(Debug, Clone, PartialEq)]
-#[cfg_attr(feature = "persisted-restart-v1", derive(serde::Serialize, serde::Deserialize))]
+#[cfg_attr(
+    feature = "persisted-restart-v1",
+    derive(serde::Serialize, serde::Deserialize)
+)]
 #[cfg_attr(feature = "persisted-restart-v1", serde(deny_unknown_fields))]
 pub struct DirectLanedActiveTraceRecord {
     pub day_index: usize,
@@ -2110,15 +2140,26 @@
     fn deserialize<D: serde::Deserializer<'de>>(deserializer: D) -> Result<Self, D::Error> {
         #[derive(serde::Deserialize)]
         #[serde(deny_unknown_fields)]
-        struct Wire { mode: String, fixed_cells: Option<usize>, target_dx_m: Option<f64>, min_cells: usize, max_cells: usize }
+        struct Wire {
+            mode: String,
+            fixed_cells: Option<usize>,
+            target_dx_m: Option<f64>,
+            min_cells: usize,
+            max_cells: usize,
+        }
         let value = Wire::deserialize(deserializer)?;
         let mode = match value.mode.as_str() {
             "fixed_cells" => "fixed_cells",
             "target_dx" => "target_dx",
             _ => return Err(serde::de::Error::custom("unknown native mesh summary mode")),
         };
-        Ok(Self { mode, fixed_cells: value.fixed_cells, target_dx_m: value.target_dx_m,
-            min_cells: value.min_cells, max_cells: value.max_cells })
+        Ok(Self {
+            mode,
+            fixed_cells: value.fixed_cells,
+            target_dx_m: value.target_dx_m,
+            min_cells: value.min_cells,
+            max_cells: value.max_cells,
+        })
     }
 }
 
@@ -2127,8 +2168,12 @@
 fn native_routing_summary_wire_retains_nonzero_fields_and_refuses_foreign_mode() {
     let mut wire = serde_json::to_value(DirectLanedActiveRunSummary::default()).unwrap();
     for (key, value) in wire.as_object_mut().unwrap() {
-        if key.starts_with("total_") || key.starts_with("max_day_") { *value = serde_json::json!(0.125); }
-        if key.contains("groundwater_storage") || key.contains("terminal_groundwater") { *value = serde_json::json!(0.25); }
+        if key.starts_with("total_") || key.starts_with("max_day_") {
+            *value = serde_json::json!(0.125);
+        }
+        if key.contains("groundwater_storage") || key.contains("terminal_groundwater") {
+            *value = serde_json::json!(0.25);
+        }
     }
     wire["days_seen"] = serde_json::json!(3);
     wire["days_routed"] = serde_json::json!(2);
--- a/crates/openwepp-hillslope-orchestrator/src/direct_runtime/snow_stage3_v11_scheduler_tests.rs
+++ b/crates/openwepp-hillslope-orchestrator/src/direct_runtime/snow_stage3_v11_scheduler_tests.rs
@@ -39,7 +39,8 @@
 
     DirectFrameExecutor::new(DirectExecutorMode::ProductionDirect)
         .run_publication_stream_with_stage3_day_preparation_and_commit_hook(
-            0, false,
+            0,
+            false,
             &mut frame,
             scheduler_metadata(),
             {
@@ -130,7 +131,8 @@
 
     DirectFrameExecutor::new(DirectExecutorMode::ProductionDirect)
         .run_publication_stream_with_stage3_day_preparation_and_commit_hook(
-            0, false,
+            0,
+            false,
             &mut frame,
             scheduler_metadata(),
             {
@@ -208,7 +210,8 @@
 
     DirectFrameExecutor::new(DirectExecutorMode::ProductionDirect)
         .run_publication_stream_with_stage3_day_preparation_and_commit_hook(
-            0, false,
+            0,
+            false,
             &mut frame,
             scheduler_metadata(),
             {
--- a/crates/openwepp-hillslope-orchestrator/src/direct_runtime/stage3_committed_publication.rs
+++ b/crates/openwepp-hillslope-orchestrator/src/direct_runtime/stage3_committed_publication.rs
@@ -1208,19 +1208,17 @@
                 .collect::<Vec<_>>();
             if matches.len() != 1 {
                 if crate::snow_accuracy_observer::physical_enabled() {
-                    crate::snow_accuracy_observer::record(
-                        serde_json::json!({
-                            "kind":"accepted_liquid_receiver_join_rejection",
-                            "support_start_ns":support.support().start_ns().get().to_string(),
-                            "support_end_ns":support.support().end_ns().get().to_string(),
-                            "parent":format!("{:?}",support.parent_transaction_id().digest()),
-                            "ending_owner":format!("{:?}",support.ending_complete_owner_set_sha256()),
-                            "expected_context":format!("{context:?}"),"event_matches":matches.len(),
-                            "mass_kg_m2":mass_kg_m2,"sensible_enthalpy_j_m2":enthalpy_j_m2,
-                            "outputs_debug":format!("{material_outputs:?}"),
-                            "events_debug":format!("{event_handoffs:?}")
-                        }),
-                    );
+                    crate::snow_accuracy_observer::record(serde_json::json!({
+                        "kind":"accepted_liquid_receiver_join_rejection",
+                        "support_start_ns":support.support().start_ns().get().to_string(),
+                        "support_end_ns":support.support().end_ns().get().to_string(),
+                        "parent":format!("{:?}",support.parent_transaction_id().digest()),
+                        "ending_owner":format!("{:?}",support.ending_complete_owner_set_sha256()),
+                        "expected_context":format!("{context:?}"),"event_matches":matches.len(),
+                        "mass_kg_m2":mass_kg_m2,"sensible_enthalpy_j_m2":enthalpy_j_m2,
+                        "outputs_debug":format!("{material_outputs:?}"),
+                        "events_debug":format!("{event_handoffs:?}")
+                    }));
                 }
                 return Err(stage3_publication_guard(
                     "accepted snow-liquid real receiver event",
@@ -1248,19 +1246,34 @@
                 stage3_publication_guard("accepted snow-liquid receiver subslab seal")
             })?;
             if let Some(packet) = &receiver.post_support_native_custody {
-                let native = packet.publication().map_err(|_| stage3_publication_guard("native donor record"))?;
-                if native.outputs != material_outputs.iter().map(|output| (*output).clone()).collect::<Vec<_>>() {
-                    return Err(stage3_publication_guard("native complete accepted donor vector"));
+                let native = packet
+                    .publication()
+                    .map_err(|_| stage3_publication_guard("native donor record"))?;
+                if native.outputs
+                    != material_outputs
+                        .iter()
+                        .map(|output| (*output).clone())
+                        .collect::<Vec<_>>()
+                {
+                    return Err(stage3_publication_guard(
+                        "native complete accepted donor vector",
+                    ));
                 }
-                apply_native_support_liquid_to_lane_day(&mut out, receiver, binding, surface_configuration, day_start_ns)?;
+                apply_native_support_liquid_to_lane_day(
+                    &mut out,
+                    receiver,
+                    binding,
+                    surface_configuration,
+                    day_start_ns,
+                )?;
             } else {
-            apply_support_liquid_custody_v2_to_lane_day(
-                &mut out,
-                receiver,
-                binding,
-                surface_configuration,
-                day_start_ns,
-            )?;
+                apply_support_liquid_custody_v2_to_lane_day(
+                    &mut out,
+                    receiver,
+                    binding,
+                    surface_configuration,
+                    day_start_ns,
+                )?;
             }
         }
         for output in support
@@ -2392,11 +2405,11 @@
             } else {
                 Wb11HydrologyKernel::project_stage3_surface_state_v1(ending)
             })
-                .map_err(|error| DirectRuntimeError::DirectKernelGuardFailure {
-                    phase: "stage3_committed_publication",
-                    detail: format!("Stage-3 surface projection: {error}"),
-                })?
-                .surface_temperature_k
+            .map_err(|error| DirectRuntimeError::DirectKernelGuardFailure {
+                phase: "stage3_committed_publication",
+                detail: format!("Stage-3 surface projection: {error}"),
+            })?
+            .surface_temperature_k
                 - 273.15,
         )
     };
--- a/crates/openwepp-hillslope-orchestrator/src/direct_runtime/stage3_committed_publication_tests.rs
+++ b/crates/openwepp-hillslope-orchestrator/src/direct_runtime/stage3_committed_publication_tests.rs
@@ -746,15 +746,21 @@
 #[test]
 #[ignore = "fresh process B01 policy identity"]
 fn actual_day_four_thin_snow_publication_preserves_owner() {
-    openwepp_land_surface_energy::snow_accuracy_policy::Policy::B01.install().expect("B01");
+    openwepp_land_surface_energy::snow_accuracy_policy::Policy::B01
+        .install()
+        .expect("B01");
     publication_projection_regime_vectors_preserve_state();
     let ending: DirectSnowStage3PersistentState = serde_json::from_str(r###"{"schema_version": 2, "experimental_accuracy": [14, false], "terminal_event_model": "EnthalpyEventV1", "fingerprint": 4430680846865068613, "lane_id": 1, "next_interval_index": 192, "layers": [{"mass_swe_m": 0.0004300877158356576, "thickness_m": 0.004300877158356585, "density_kg_m3": 99.9999999999998, "settle_day_count": 0.0, "temperature_c": 0.0, "liquid_water_m": 0.0007144978421999323, "cold_content_j_m2": 0.0, "refrozen_liquid_m": 0.0003326108662195986}], "detached_retained_liquid_kg_m2": 0.0, "initial_ice_kg_m2": -0.0, "initial_retained_liquid_kg_m2": 0.0, "cumulative_snowfall_kg_m2": 11.999999718902094, "cumulative_external_liquid_kg_m2": 2.8109790785872166e-07, "cumulative_deposition_kg_m2": 0.6033821552684834, "cumulative_sublimation_kg_m2": 0.5901930165073069, "cumulative_melt_kg_m2": 11.915712267531866, "cumulative_unresolved_liquid_kg_m2": 10.868603580725626, "cumulative_complete_energy_j_m2": 3859427.129069834, "cumulative_cold_energy_change_j_m2": -115654.48337879096, "cumulative_terminal_unallocated_energy_j_m2": 0.0}"###).expect("authentic day four snow state");
     let before = serde_json::to_vec(&ending).expect("owner bytes");
     assert!(crate::hydrology::stage3_is_terminal_event_domain(&ending));
     assert!(Wb11HydrologyKernel::project_stage3_surface_state_v1(&ending).is_err());
     let expected = Wb11HydrologyKernel::project_stage3_terminal_surface_state_v1(&ending)
-        .expect("valid canonical thin snow").surface_temperature_k - 273.15;
-    let actual = publication_surface_temperature_c(&ending).expect("publish valid terminal snow").expect("represented snow");
+        .expect("valid canonical thin snow")
+        .surface_temperature_k
+        - 273.15;
+    let actual = publication_surface_temperature_c(&ending)
+        .expect("publish valid terminal snow")
+        .expect("represented snow");
     assert_eq!(actual.to_bits(), expected.to_bits());
     assert_eq!(actual.to_bits(), 0.0_f64.to_bits());
     assert_eq!(serde_json::to_vec(&ending).expect("after bytes"), before);
@@ -764,16 +770,20 @@
 // Invoke in the existing fresh-process B01 test AFTER installing its policy.
 fn publication_projection_regime_vectors_preserve_state() {
     let make = |swe_m: f64| {
-        Wb11HydrologyKernel::initialize_stage3_persistent_state(1, vec![crate::DirectSnowLayerState {
-            mass_swe_m: swe_m,
-            thickness_m: swe_m * 1000.0 / 200.0,
-            density_kg_m3: 200.0,
-            settle_day_count: 0.0,
-            temperature_c: 0.0,
-            liquid_water_m: 0.0,
-            cold_content_j_m2: 0.0,
-            refrozen_liquid_m: 0.0,
-        }]).expect("canonical state initializer, no manual fingerprint/lineage repair")
+        Wb11HydrologyKernel::initialize_stage3_persistent_state(
+            1,
+            vec![crate::DirectSnowLayerState {
+                mass_swe_m: swe_m,
+                thickness_m: swe_m * 1000.0 / 200.0,
+                density_kg_m3: 200.0,
+                settle_day_count: 0.0,
+                temperature_c: 0.0,
+                liquid_water_m: 0.0,
+                cold_content_j_m2: 0.0,
+                refrozen_liquid_m: 0.0,
+            }],
+        )
+        .expect("canonical state initializer, no manual fingerprint/lineage repair")
     };
     let threshold = 0.001_f64;
     for (swe_m, terminal_expected) in [
@@ -784,15 +794,25 @@
         let state = make(swe_m);
         let before = serde_json::to_vec(&state).unwrap();
         assert!(!crate::hydrology::stage3_is_bulk_experiment_domain(&state));
-        assert_eq!(crate::hydrology::stage3_is_terminal_event_domain(&state), terminal_expected);
-        assert_eq!(crate::hydrology::stage3_is_resolved_thermal_domain(&state), !terminal_expected);
+        assert_eq!(
+            crate::hydrology::stage3_is_terminal_event_domain(&state),
+            terminal_expected
+        );
+        assert_eq!(
+            crate::hydrology::stage3_is_resolved_thermal_domain(&state),
+            !terminal_expected
+        );
         let canonical = if terminal_expected {
             Wb11HydrologyKernel::project_stage3_terminal_surface_state_v1(&state)
         } else {
             Wb11HydrologyKernel::project_stage3_surface_state_v1(&state)
-        }.expect("actual canonical regime projection");
+        }
+        .expect("actual canonical regime projection");
         let actual = publication_surface_temperature_c(&state).unwrap().unwrap();
-        assert_eq!(actual.to_bits(), (canonical.surface_temperature_k - 273.15).to_bits());
+        assert_eq!(
+            actual.to_bits(),
+            (canonical.surface_temperature_k - 273.15).to_bits()
+        );
         assert_eq!(serde_json::to_vec(&state).unwrap(), before);
     }
     let bulk = make(0.00005); //0.05kg/m², within actual B01 entry threshold.
@@ -800,8 +820,13 @@
     assert!(crate::hydrology::stage3_is_terminal_event_domain(&bulk));
     let before = serde_json::to_vec(&bulk).unwrap();
     let canonical = Wb11HydrologyKernel::project_stage3_terminal_surface_state_v1(&bulk).unwrap();
-    assert_eq!(publication_surface_temperature_c(&bulk).unwrap().unwrap().to_bits(),
-        (canonical.surface_temperature_k - 273.15).to_bits());
+    assert_eq!(
+        publication_surface_temperature_c(&bulk)
+            .unwrap()
+            .unwrap()
+            .to_bits(),
+        (canonical.surface_temperature_k - 273.15).to_bits()
+    );
     assert_eq!(serde_json::to_vec(&bulk).unwrap(), before);
 
     let empty = Wb11HydrologyKernel::initialize_stage3_persistent_state(1, vec![])
@@ -819,10 +844,16 @@
     invalid_cold.layers[0].cold_content_j_m2 = -1.0;
     for state in [foreign_policy, invalid_model, invalid_cold] {
         let before = serde_json::to_vec(&state).unwrap();
-        assert!(matches!(publication_surface_temperature_c(&state),
-            Err(DirectRuntimeError::DirectKernelGuardFailure {
-                phase: "stage3_committed_publication", ..
-            })), "invalid represented snow must preserve typed failure");
+        assert!(
+            matches!(
+                publication_surface_temperature_c(&state),
+                Err(DirectRuntimeError::DirectKernelGuardFailure {
+                    phase: "stage3_committed_publication",
+                    ..
+                })
+            ),
+            "invalid represented snow must preserve typed failure"
+        );
         assert_eq!(serde_json::to_vec(&state).unwrap(), before);
     }
 }
--- a/crates/openwepp-hillslope-orchestrator/src/direct_runtime/surface_liquid_closure.rs
+++ b/crates/openwepp-hillslope-orchestrator/src/direct_runtime/surface_liquid_closure.rs
@@ -1187,7 +1187,12 @@
                 release,
                 runon_parcels,
                 ..
-            } | DirectTileGroundIngress::CoveredCanopyReleaseAndTerminalGround{release,direct_ground_parcels:runon_parcels,..} => {
+            }
+            | DirectTileGroundIngress::CoveredCanopyReleaseAndTerminalGround {
+                release,
+                direct_ground_parcels: runon_parcels,
+                ..
+            } => {
                 capture_canopy_release(configured, release, &mut result, input.transaction_id)?;
                 for parcel in runon_parcels {
                     capture_amount(
--- a/crates/openwepp-hillslope-orchestrator/src/direct_runtime/surface_liquid_ingress.rs
+++ b/crates/openwepp-hillslope-orchestrator/src/direct_runtime/surface_liquid_ingress.rs
@@ -247,8 +247,12 @@
                 surface_id,
                 ..
             }
-            | Self::CoveredCanopyReleaseAndTerminalGround {ofe_id,tile_id,surface_id,..}
-                => (ofe_id, tile_id, surface_id),
+            | Self::CoveredCanopyReleaseAndTerminalGround {
+                ofe_id,
+                tile_id,
+                surface_id,
+                ..
+            } => (ofe_id, tile_id, surface_id),
         }
     }
 
@@ -257,7 +261,9 @@
             Self::OpenRawPrecipitation { .. } | Self::OpenLiquidParcels { .. } => {
                 DirectGroundIngressMode::OpenRawPrecipitation
             }
-            Self::CoveredCanopyRelease { .. } | Self::CoveredCanopyReleaseAndRunon { .. } | Self::CoveredCanopyReleaseAndTerminalGround{..} => {
+            Self::CoveredCanopyRelease { .. }
+            | Self::CoveredCanopyReleaseAndRunon { .. }
+            | Self::CoveredCanopyReleaseAndTerminalGround { .. } => {
                 DirectGroundIngressMode::CoveredCanopyRelease
             }
         }
@@ -1530,7 +1536,10 @@
                 DirectTileGroundIngress::CoveredCanopyReleaseAndRunon { runon_parcels, .. } => {
                     Some(runon_parcels.as_slice())
                 }
-                DirectTileGroundIngress::CoveredCanopyReleaseAndTerminalGround{direct_ground_parcels,..} => Some(direct_ground_parcels.as_slice()),
+                DirectTileGroundIngress::CoveredCanopyReleaseAndTerminalGround {
+                    direct_ground_parcels,
+                    ..
+                } => Some(direct_ground_parcels.as_slice()),
                 _ => None,
             })
             .flatten()
@@ -1898,12 +1907,31 @@
         DirectTileGroundIngress::CoveredCanopyRelease { release, .. } => {
             append_canopy_release(configured, release, transaction_id, interval_s, parcels)
         }
-        DirectTileGroundIngress::CoveredCanopyReleaseAndTerminalGround {ofe_id,tile_id,release,direct_ground_parcels,..} => {
-            if !direct_ground_parcels.iter().any(|p|p.kind==DirectSurfaceLiquidParcelKind::TerminalReceiver) {
-                return Err(DirectSurfaceLiquidError::Identity("covered terminal ground missing terminal source"));
+        DirectTileGroundIngress::CoveredCanopyReleaseAndTerminalGround {
+            ofe_id,
+            tile_id,
+            release,
+            direct_ground_parcels,
+            ..
+        } => {
+            if !direct_ground_parcels
+                .iter()
+                .any(|p| p.kind == DirectSurfaceLiquidParcelKind::TerminalReceiver)
+            {
+                return Err(DirectSurfaceLiquidError::Identity(
+                    "covered terminal ground missing terminal source",
+                ));
             }
-            append_canopy_release(configured,release,transaction_id,interval_s,parcels)?;
-            append_external_ground(configured,ofe_id,tile_id,direct_ground_parcels,interval_s,parcels,true)
+            append_canopy_release(configured, release, transaction_id, interval_s, parcels)?;
+            append_external_ground(
+                configured,
+                ofe_id,
+                tile_id,
+                direct_ground_parcels,
+                interval_s,
+                parcels,
+                true,
+            )
         }
         DirectTileGroundIngress::CoveredCanopyReleaseAndRunon {
             ofe_id,
@@ -1971,20 +1999,31 @@
     interval_s: f64,
     parcels: &mut Vec<TimedParcel>,
 ) -> Result<(), DirectSurfaceLiquidError> {
-    append_external_ground(configured,ofe_id,tile_id,ingress_parcels,interval_s,parcels,false)
+    append_external_ground(
+        configured,
+        ofe_id,
+        tile_id,
+        ingress_parcels,
+        interval_s,
+        parcels,
+        false,
+    )
 }
 
 #[allow(clippy::too_many_arguments)]
 fn append_external_ground(
     configured: &DirectSurfaceLiquidConfigurationRecord,
-    ofe_id: &OfeId, tile_id: &TileId,
+    ofe_id: &OfeId,
+    tile_id: &TileId,
     ingress_parcels: &[DirectOpenLiquidIngressParcel],
-    interval_s: f64, parcels: &mut Vec<TimedParcel>, allow_terminal: bool,
+    interval_s: f64,
+    parcels: &mut Vec<TimedParcel>,
+    allow_terminal: bool,
 ) -> Result<(), DirectSurfaceLiquidError> {
     let mut identities = BTreeSet::new();
     for parcel in ingress_parcels {
         if !(parcel.kind == DirectSurfaceLiquidParcelKind::UpstreamRunon
-                || (allow_terminal && parcel.kind == DirectSurfaceLiquidParcelKind::TerminalReceiver))
+            || (allow_terminal && parcel.kind == DirectSurfaceLiquidParcelKind::TerminalReceiver))
             || parcel.destination_ofe_id != *ofe_id
             || parcel.destination_tile_id != *tile_id
             || !identities.insert(parcel.parcel_id.clone())
--- a/crates/openwepp-hillslope-orchestrator/src/direct_runtime/surface_liquid_ingress_tests.rs
+++ b/crates/openwepp-hillslope-orchestrator/src/direct_runtime/surface_liquid_ingress_tests.rs
@@ -2997,27 +2997,61 @@
     let resource = resource_candidate(&configuration, &beginning, transaction_id, None, &[]);
     let record = &configuration.records[0];
     let zero = amount(0.0, 280.0, 0.0, INTERVAL_S);
-    let terminal = external_open_parcel(record, "terminal-1203", DirectSurfaceLiquidParcelKind::TerminalReceiver,
-        "snow-owner", "upstream", 0.25, 274.0);
-    let runon = external_open_parcel(record, "runon-1203", DirectSurfaceLiquidParcelKind::UpstreamRunon,
-        "upstream-owner", "upstream", 0.4, 289.0);
+    let terminal = external_open_parcel(
+        record,
+        "terminal-1203",
+        DirectSurfaceLiquidParcelKind::TerminalReceiver,
+        "snow-owner",
+        "upstream",
+        0.25,
+        274.0,
+    );
+    let runon = external_open_parcel(
+        record,
+        "runon-1203",
+        DirectSurfaceLiquidParcelKind::UpstreamRunon,
+        "upstream-owner",
+        "upstream",
+        0.4,
+        289.0,
+    );
     let input = DirectSurfaceLiquidIngressInput {
-        transaction_id, day_index: 3, interval_index: 0, interval_s: INTERVAL_S,
-        tile_ingress: vec![DirectTileGroundIngress::CoveredCanopyReleaseAndTerminalGround {
-            ofe_id: record.key.ofe_id.clone(), tile_id: record.key.tile_id.clone(), surface_id: record.key.surface_id.clone(),
-            release: DirectCanopyLiquidRelease {
-                throughfall: amount(0.1, 281.0, 0.0, INTERVAL_S), initial_drainage: zero.clone(),
-                second_drainage: zero.clone(), stemflow: zero,
-            }, direct_ground_parcels: vec![terminal.clone(), runon.clone()],
-        }], wb14_parameters: parameters(&configuration),
+        transaction_id,
+        day_index: 3,
+        interval_index: 0,
+        interval_s: INTERVAL_S,
+        tile_ingress: vec![
+            DirectTileGroundIngress::CoveredCanopyReleaseAndTerminalGround {
+                ofe_id: record.key.ofe_id.clone(),
+                tile_id: record.key.tile_id.clone(),
+                surface_id: record.key.surface_id.clone(),
+                release: DirectCanopyLiquidRelease {
+                    throughfall: amount(0.1, 281.0, 0.0, INTERVAL_S),
+                    initial_drainage: zero.clone(),
+                    second_drainage: zero.clone(),
+                    stemflow: zero,
+                },
+                direct_ground_parcels: vec![terminal.clone(), runon.clone()],
+            },
+        ],
+        wb14_parameters: parameters(&configuration),
     };
     let before = resource.clone();
-    let candidate = execute_surface_liquid_ingress(&configuration, &resource, &input).expect("terminal plus runon plus canopy");
+    let candidate = execute_surface_liquid_ingress(&configuration, &resource, &input)
+        .expect("terminal plus runon plus canopy");
     assert_eq!(candidate.open_ingress_parcels(), &[terminal, runon]);
-    candidate.validate(&configuration, &resource, &input).expect("independent frozen identity and closure");
+    candidate
+        .validate(&configuration, &resource, &input)
+        .expect("independent frozen identity and closure");
     for poison in 0..5 {
         let mut poisoned = input.clone();
-        let DirectTileGroundIngress::CoveredCanopyReleaseAndTerminalGround { direct_ground_parcels, .. } = &mut poisoned.tile_ingress[0] else { panic!("terminal posture"); };
+        let DirectTileGroundIngress::CoveredCanopyReleaseAndTerminalGround {
+            direct_ground_parcels,
+            ..
+        } = &mut poisoned.tile_ingress[0]
+        else {
+            panic!("terminal posture");
+        };
         match poison {
             0 => direct_ground_parcels[0].kind = DirectSurfaceLiquidParcelKind::UpstreamRunon,
             1 => direct_ground_parcels[0].kind = DirectSurfaceLiquidParcelKind::RawPrecipitation,
@@ -3026,7 +3060,10 @@
             4 => direct_ground_parcels[0].amount.end_s = INTERVAL_S + 1.0,
             _ => unreachable!(),
         }
-        assert!(execute_surface_liquid_ingress(&configuration, &resource, &poisoned).is_err(), "poison {poison}");
+        assert!(
+            execute_surface_liquid_ingress(&configuration, &resource, &poisoned).is_err(),
+            "poison {poison}"
+        );
         assert_eq!(resource, before);
     }
 }
--- a/crates/openwepp-hillslope-orchestrator/src/direct_runtime/surface_liquid_owner/v2_ingress_adapter.rs
+++ b/crates/openwepp-hillslope-orchestrator/src/direct_runtime/surface_liquid_owner/v2_ingress_adapter.rs
@@ -204,19 +204,26 @@
     /// operands advance. The existing arithmetic adapter enforces all cursors,
     /// continuation and marker preservation before native revalidation.
     pub(crate) fn with_native_custody_candidate(
-        &self, configuration: &SurfaceLiquidConfigurationV2,
-        beginning: &SurfaceLiquidOwnerEnvelopeV2, ending: &SurfaceLiquidOwnerEnvelopeV2,
+        &self,
+        configuration: &SurfaceLiquidConfigurationV2,
+        beginning: &SurfaceLiquidOwnerEnvelopeV2,
+        ending: &SurfaceLiquidOwnerEnvelopeV2,
     ) -> Result<Self, DirectSurfaceLiquidError> {
         self.validate(configuration)?;
         if &self.candidate_owner != beginning {
-            return Err(DirectSurfaceLiquidError::Identity("native drain stale WB14 candidate owner"));
+            return Err(DirectSurfaceLiquidError::Identity(
+                "native drain stale WB14 candidate owner",
+            ));
         }
         ending.canonical_bytes(configuration.parent(), Some(configuration))?;
-        let ending_state = ending.v2_state().ok_or(DirectSurfaceLiquidError::Identity("native drain WB14 V2 ending"))?;
+        let ending_state = ending.v2_state().ok_or(DirectSurfaceLiquidError::Identity(
+            "native drain WB14 V2 ending",
+        ))?;
         let arithmetic = extract_v2_liquid_arithmetic_state(configuration, ending_state)?;
         let mut candidate = self.clone();
-        candidate.liquid_arithmetic = self.liquid_arithmetic.with_zero_duration_receiver_candidate(
-            configuration.parent(), arithmetic)?;
+        candidate.liquid_arithmetic = self
+            .liquid_arithmetic
+            .with_zero_duration_receiver_candidate(configuration.parent(), arithmetic)?;
         candidate.candidate_owner = ending.clone();
         candidate.validate(configuration)?;
         Ok(candidate)
--- a/crates/openwepp-hillslope-orchestrator/src/direct_runtime/surface_liquid_owner/v2_ingress_tests.rs
+++ b/crates/openwepp-hillslope-orchestrator/src/direct_runtime/surface_liquid_owner/v2_ingress_tests.rs
@@ -1231,45 +1231,110 @@
     let configuration = configuration_v2_one_litter();
     let beginning = owner_v2(&configuration);
     let tx = TransactionId(703);
-    let resource = prepare_surface_liquid_resource_candidate_v2(&configuration, &beginning, &beginning,
-        tx, &zero_closure(&beginning)).unwrap();
+    let resource = prepare_surface_liquid_resource_candidate_v2(
+        &configuration,
+        &beginning,
+        &beginning,
+        tx,
+        &zero_closure(&beginning),
+    )
+    .unwrap();
     let mut forcing = input(&configuration, tx, 900.0);
     zero_ingress_mass(&mut forcing);
     let first = execute_surface_liquid_ingress_v2_with_parent_state_and_coupled_binding(
-        &configuration, &resource, &forcing, None, false, None).unwrap();
+        &configuration,
+        &resource,
+        &forcing,
+        None,
+        false,
+        None,
+    )
+    .unwrap();
     let parent = first.parent_working_state().unwrap();
     let before = parent.clone();
     let native_beginning = parent.candidate_owner();
     let state = native_beginning.v2_state().unwrap();
     let mut records = state.records().to_vec();
     records[0].liquid_kg_m2_tile += 0.125;
-    let ending = SurfaceLiquidOwnerEnvelopeV2::wrap_v2(&configuration,
-        SurfaceLiquidOwnedStateV2::try_new(&configuration, records.clone(), state.continuations().to_vec()).unwrap()).unwrap();
-    let updated = parent.with_native_custody_candidate(&configuration, native_beginning, &ending).unwrap();
+    let ending = SurfaceLiquidOwnerEnvelopeV2::wrap_v2(
+        &configuration,
+        SurfaceLiquidOwnedStateV2::try_new(
+            &configuration,
+            records.clone(),
+            state.continuations().to_vec(),
+        )
+        .unwrap(),
+    )
+    .unwrap();
+    let updated = parent
+        .with_native_custody_candidate(&configuration, native_beginning, &ending)
+        .unwrap();
     assert_eq!(parent, &before);
     assert_eq!(updated.candidate_owner(), &ending);
-    assert_eq!(ending.v2_state().unwrap().continuations(), state.continuations());
-    for (old, new) in state.records().iter().zip(ending.v2_state().unwrap().records()) {
-        assert_eq!(old.last_accepted_transaction_id, new.last_accepted_transaction_id);
+    assert_eq!(
+        ending.v2_state().unwrap().continuations(),
+        state.continuations()
+    );
+    for (old, new) in state
+        .records()
+        .iter()
+        .zip(ending.v2_state().unwrap().records())
+    {
+        assert_eq!(
+            old.last_accepted_transaction_id,
+            new.last_accepted_transaction_id
+        );
         assert_eq!(old.litter_ice_kg_m2_tile, new.litter_ice_kg_m2_tile);
-        assert_eq!(old.surface_enthalpy_j_m2_tile, new.surface_enthalpy_j_m2_tile);
+        assert_eq!(
+            old.surface_enthalpy_j_m2_tile,
+            new.surface_enthalpy_j_m2_tile
+        );
     }
     let scrub = |value: &DirectWb14ParentWorkingStateV2| {
-        let mut wire: serde_json::Value = serde_json::from_slice(&value.restart_bytes(&configuration).unwrap()).unwrap();
-        let bytes: Vec<u8> = serde_json::from_value(wire["liquid_arithmetic_bytes"].clone()).unwrap();
+        let mut wire: serde_json::Value =
+            serde_json::from_slice(&value.restart_bytes(&configuration).unwrap()).unwrap();
+        let bytes: Vec<u8> =
+            serde_json::from_value(wire["liquid_arithmetic_bytes"].clone()).unwrap();
         let mut arithmetic: serde_json::Value = serde_json::from_slice(&bytes).unwrap();
-        arithmetic.as_object_mut().unwrap().remove("candidate_state");
-        wire.as_object_mut().unwrap().remove("candidate_owner_bytes");
+        arithmetic
+            .as_object_mut()
+            .unwrap()
+            .remove("candidate_state");
+        wire.as_object_mut()
+            .unwrap()
+            .remove("candidate_owner_bytes");
         wire["liquid_arithmetic_bytes"] = arithmetic;
         wire
     };
     assert_eq!(scrub(parent), scrub(&updated));
-    assert!(updated.with_native_custody_candidate(&configuration, native_beginning, &ending).is_err());
-    assert_eq!(updated.with_native_custody_candidate(&configuration, &ending, &ending).unwrap(), updated);
-    let next_resource = prepare_surface_liquid_resource_candidate_v2(&configuration, &ending, &ending,
-        tx, &zero_closure(&ending)).unwrap();
+    assert!(
+        updated
+            .with_native_custody_candidate(&configuration, native_beginning, &ending)
+            .is_err()
+    );
+    assert_eq!(
+        updated
+            .with_native_custody_candidate(&configuration, &ending, &ending)
+            .unwrap(),
+        updated
+    );
+    let next_resource = prepare_surface_liquid_resource_candidate_v2(
+        &configuration,
+        &ending,
+        &ending,
+        tx,
+        &zero_closure(&ending),
+    )
+    .unwrap();
     let next = execute_surface_liquid_ingress_v2_with_parent_state_and_coupled_binding(
-        &configuration, &next_resource, &forcing, Some(&updated), true, None).unwrap();
+        &configuration,
+        &next_resource,
+        &forcing,
+        Some(&updated),
+        true,
+        None,
+    )
+    .unwrap();
     assert!(next.parent_working_state().is_none());
     assert_eq!(next.beginning_owner(), &ending);
 }
--- a/crates/openwepp-hillslope-orchestrator/src/direct_runtime/surface_liquid_owner/v3_exact_enthalpy.rs
+++ b/crates/openwepp-hillslope-orchestrator/src/direct_runtime/surface_liquid_owner/v3_exact_enthalpy.rs
@@ -796,7 +796,9 @@
         Ok(owner)
     }
 
-    pub(crate) fn recomputed_state_sha256(&self) -> Result<Sha256Digest, LseSurfaceEnthalpyErrorV1> {
+    pub(crate) fn recomputed_state_sha256(
+        &self,
+    ) -> Result<Sha256Digest, LseSurfaceEnthalpyErrorV1> {
         let mut value = self.clone();
         value.state_sha256 = wire_digest(ZERO_SHA256)?;
         // The scientific state digest and the receipt-chain digest are
--- a/crates/openwepp-hillslope-orchestrator/src/land_surface_energy_shadow/covered_derived_ingress.rs
+++ b/crates/openwepp-hillslope-orchestrator/src/land_surface_energy_shadow/covered_derived_ingress.rs
@@ -185,8 +185,13 @@
             tile_id,
             surface_id,
             ..
-        } | DirectTileGroundIngress::CoveredCanopyReleaseAndTerminalGround{ofe_id,tile_id,surface_id,..}
-            => (ofe_id.as_str(), tile_id.as_str(), surface_id.as_str()),
+        }
+        | DirectTileGroundIngress::CoveredCanopyReleaseAndTerminalGround {
+            ofe_id,
+            tile_id,
+            surface_id,
+            ..
+        } => (ofe_id.as_str(), tile_id.as_str(), surface_id.as_str()),
     }
 }
 
@@ -255,10 +260,16 @@
                 surface_id: identity.surface_id.clone(),
                 release,
             }
-        } else if runon_parcels.iter().any(|p|p.kind==crate::DirectSurfaceLiquidParcelKind::TerminalReceiver) {
+        } else if runon_parcels
+            .iter()
+            .any(|p| p.kind == crate::DirectSurfaceLiquidParcelKind::TerminalReceiver)
+        {
             DirectTileGroundIngress::CoveredCanopyReleaseAndTerminalGround {
-                ofe_id:identity.ofe_id.clone(),tile_id:identity.tile_id.clone(),surface_id:identity.surface_id.clone(),
-                release,direct_ground_parcels:runon_parcels,
+                ofe_id: identity.ofe_id.clone(),
+                tile_id: identity.tile_id.clone(),
+                surface_id: identity.surface_id.clone(),
+                release,
+                direct_ground_parcels: runon_parcels,
             }
         } else {
             DirectTileGroundIngress::CoveredCanopyReleaseAndRunon {
--- a/crates/openwepp-hillslope-orchestrator/src/land_surface_energy_shadow/covered_v8_owner.rs
+++ b/crates/openwepp-hillslope-orchestrator/src/land_surface_energy_shadow/covered_v8_owner.rs
@@ -2651,7 +2651,9 @@
                 "beginning_hydrology_snapshot_sha256":v.beginning_hydrology_snapshot_sha256,
                 "beginning_vegetation_state_sha256":v.beginning_vegetation_state_sha256})
         }
-        fn covered(v: &openwepp_land_surface_energy::FinalCoveredTileCandidate) -> serde_json::Value {
+        fn covered(
+            v: &openwepp_land_surface_energy::FinalCoveredTileCandidate,
+        ) -> serde_json::Value {
             serde_json::json!({"variant":"covered","identity":identity(&v.identity),
                 "evaluation":v.final_solver_candidate.evaluation,"column":v.energy_operands.column,
                 "lower_boundary":v.energy_operands.lower_boundary,
@@ -2662,14 +2664,17 @@
                 super::multi_tile_runtime::FinalizedRuntimeTile::Covered(v) => covered(v),
                 super::multi_tile_runtime::FinalizedRuntimeTile::Open(v) => serde_json::json!({
                     "variant":"open","identity":identity(&v.identity),"evaluation":v.final_solver_candidate.evaluation}),
-                super::multi_tile_runtime::FinalizedRuntimeTile::Stage3OpenSnow {identity:v,..} => serde_json::json!({
+                super::multi_tile_runtime::FinalizedRuntimeTile::Stage3OpenSnow {
+                    identity: v,
+                    ..
+                } => serde_json::json!({
                     "variant":"open_snow_owned_by_stage3","identity":identity(v)}),
             }
         }
         let tiles: Vec<_> = match &self.physical {
             CoveredV8PhysicalOwner::Legacy(v) => vec![covered(v.final_tile())],
             CoveredV8PhysicalOwner::MultiTile(v) => v.finalized_tiles().iter().map(tile).collect(),
-            CoveredV8PhysicalOwner::FrozenLitterV3 {fixed,..} => {
+            CoveredV8PhysicalOwner::FrozenLitterV3 { fixed, .. } => {
                 let mut rows: Vec<_> = fixed.legacy_tiles.iter().map(tile).collect();
                 for v in &fixed.frozen_litter_tiles {
                     let c = &v.fixed_final.complete_physical_candidate;
@@ -2686,19 +2691,37 @@
                 rows
             }
         };
-        let problem_row = |id: &openwepp_land_surface_energy::RuntimeTileIdentity, problem: &openwepp_land_surface_energy::OpenSurfaceProblem| serde_json::json!({
+        let problem_row =
+            |id: &openwepp_land_surface_energy::RuntimeTileIdentity,
+             problem: &openwepp_land_surface_energy::OpenSurfaceProblem| {
+                serde_json::json!({
             "ofe_id":id.ofe_id,"tile_id":id.tile_id,"transaction_id":id.transaction_id,"soil_transaction_id":id.soil_thermal_transaction_id,
-            "interval_s":id.interval_s,"beginning_soil_thermal_state_sha256":id.beginning_soil_thermal_state_sha256,"ground_problem":problem});
+            "interval_s":id.interval_s,"beginning_soil_thermal_state_sha256":id.beginning_soil_thermal_state_sha256,"ground_problem":problem})
+            };
         let problem_inputs: Vec<_> = match &self.physical {
-            CoveredV8PhysicalOwner::Legacy(v) => vec![problem_row(v.potential().identity(), &v.potential().accuracy_observation_beginning().ground)],
-            CoveredV8PhysicalOwner::MultiTile(v) => v.potential_tiles().iter().filter_map(|v| {
-                match v {
-                    super::multi_tile_runtime::PotentialTilePhase::Open {phase,..} => Some(problem_row(&phase.identity,phase.accuracy_observation_beginning())),
-                    super::multi_tile_runtime::PotentialTilePhase::Covered {phase,..} => Some(problem_row(phase.identity(),&phase.accuracy_observation_beginning().ground)),
-                    super::multi_tile_runtime::PotentialTilePhase::Stage3OpenSnow {..} => None,
-                }
-            }).collect(),
-            CoveredV8PhysicalOwner::FrozenLitterV3 {fixed,..} => fixed.accuracy_problem_inputs.clone(),
+            CoveredV8PhysicalOwner::Legacy(v) => vec![problem_row(
+                v.potential().identity(),
+                &v.potential().accuracy_observation_beginning().ground,
+            )],
+            CoveredV8PhysicalOwner::MultiTile(v) => v
+                .potential_tiles()
+                .iter()
+                .filter_map(|v| match v {
+                    super::multi_tile_runtime::PotentialTilePhase::Open { phase, .. } => Some(
+                        problem_row(&phase.identity, phase.accuracy_observation_beginning()),
+                    ),
+                    super::multi_tile_runtime::PotentialTilePhase::Covered { phase, .. } => {
+                        Some(problem_row(
+                            phase.identity(),
+                            &phase.accuracy_observation_beginning().ground,
+                        ))
+                    }
+                    super::multi_tile_runtime::PotentialTilePhase::Stage3OpenSnow { .. } => None,
+                })
+                .collect(),
+            CoveredV8PhysicalOwner::FrozenLitterV3 { fixed, .. } => {
+                fixed.accuracy_problem_inputs.clone()
+            }
         };
         let soil_children: Vec<_> = self.hydrology().pre_ingress_soil_thermal_candidates().iter().map(|v| serde_json::json!({
             "ofe_id":v.ofe_id,"tile_id":v.tile_id,"beginning_state_sha256":v.beginning_state_sha256,"beginning_identity":v.beginning_identity})).collect();
--- a/crates/openwepp-hillslope-orchestrator/src/land_surface_energy_shadow/multi_tile_runtime.rs
+++ b/crates/openwepp-hillslope-orchestrator/src/land_surface_energy_shadow/multi_tile_runtime.rs
@@ -1239,10 +1239,15 @@
         if let Some(optical) = &value.stage3_optical {
             self.stage3_optical(optical);
         }
-        if let Some(boundary)=&value.transparent_liquid_boundary {
+        if let Some(boundary) = &value.transparent_liquid_boundary {
             self.string("OPENWEPP_POLICY14_TRANSPARENT_LIQUID_V1");
-            let bytes=serde_json::to_vec(boundary).map_err(|_| LandSurfaceEnergyShadowError::Identity("transparent liquid projection serialization"))?;
-            self.usize(bytes.len()); self.bytes.extend_from_slice(&bytes);
+            let bytes = serde_json::to_vec(boundary).map_err(|_| {
+                LandSurfaceEnergyShadowError::Identity(
+                    "transparent liquid projection serialization",
+                )
+            })?;
+            self.usize(bytes.len());
+            self.bytes.extend_from_slice(&bytes);
         }
         Ok(())
     }
--- a/crates/openwepp-hillslope-orchestrator/src/land_surface_energy_shadow/receiver_validation.rs
+++ b/crates/openwepp-hillslope-orchestrator/src/land_surface_energy_shadow/receiver_validation.rs
@@ -45,7 +45,8 @@
     error: &LandSurfaceEnergyShadowError,
 ) -> DirectSurfaceLiquidErrorCode {
     match error {
-        LandSurfaceEnergyShadowError::Identity(_) | LandSurfaceEnergyShadowError::InputProjection(_) => DirectSurfaceLiquidErrorCode::E002,
+        LandSurfaceEnergyShadowError::Identity(_)
+        | LandSurfaceEnergyShadowError::InputProjection(_) => DirectSurfaceLiquidErrorCode::E002,
         LandSurfaceEnergyShadowError::Operand(_) => DirectSurfaceLiquidErrorCode::E003,
         LandSurfaceEnergyShadowError::Bound(_) => DirectSurfaceLiquidErrorCode::E006,
         LandSurfaceEnergyShadowError::UnsupportedCustody(_) => DirectSurfaceLiquidErrorCode::E004,
--- a/crates/openwepp-hillslope-orchestrator/src/land_surface_energy_shadow/unified_entry_preflight.rs
+++ b/crates/openwepp-hillslope-orchestrator/src/land_surface_energy_shadow/unified_entry_preflight.rs
@@ -714,9 +714,12 @@
             tile_id,
             surface_id,
             release,
-            direct_ground_parcels:runon_parcels,
+            direct_ground_parcels: runon_parcels,
         } => {
-            out.string("tile_ingress_mode", "covered_canopy_release_and_terminal_ground");
+            out.string(
+                "tile_ingress_mode",
+                "covered_canopy_release_and_terminal_ground",
+            );
             frame_ingress_identity(out, ofe_id, tile_id, surface_id);
             frame_ingress_amount(out, "throughfall", &release.throughfall);
             frame_ingress_amount(out, "initial_drainage", &release.initial_drainage);
@@ -724,9 +727,15 @@
             frame_ingress_amount(out, "stemflow", &release.stemflow);
             out.count("covered_runon_parcel_count", runon_parcels.len());
             for parcel in runon_parcels {
-                out.string("direct_ground_kind",&format!("{:?}",parcel.kind));
-                out.string("direct_ground_destination_ofe",parcel.destination_ofe_id.as_str());
-                out.string("direct_ground_destination_tile",parcel.destination_tile_id.as_str());
+                out.string("direct_ground_kind", &format!("{:?}", parcel.kind));
+                out.string(
+                    "direct_ground_destination_ofe",
+                    parcel.destination_ofe_id.as_str(),
+                );
+                out.string(
+                    "direct_ground_destination_tile",
+                    parcel.destination_tile_id.as_str(),
+                );
                 out.string("runon_parcel_id", parcel.parcel_id.as_str());
                 out.string("runon_source_owner_id", parcel.source_owner_id.as_str());
                 out.string("runon_source_ofe_id", parcel.source_ofe_id.as_str());
--- a/crates/openwepp-hillslope-orchestrator/src/land_surface_energy_shadow/v3_input_projection.rs
+++ b/crates/openwepp-hillslope-orchestrator/src/land_surface_energy_shadow/v3_input_projection.rs
@@ -127,23 +127,36 @@
 }
 
 pub(crate) fn order_native_litter_phase_inputs(
-    configuration:&crate::SurfaceLiquidConfigurationV2,
-    inputs:impl IntoIterator<Item=FrozenLitterV3PhaseFreeInput>,
-)->Result<Vec<FrozenLitterV3PhaseFreeInput>,crate::DirectSurfaceLiquidError> {
-    let mut indexed=std::collections::BTreeMap::new();
+    configuration: &crate::SurfaceLiquidConfigurationV2,
+    inputs: impl IntoIterator<Item = FrozenLitterV3PhaseFreeInput>,
+) -> Result<Vec<FrozenLitterV3PhaseFreeInput>, crate::DirectSurfaceLiquidError> {
+    let mut indexed = std::collections::BTreeMap::new();
     for input in inputs {
-        let key=(input.ofe_id.clone(),input.tile_id.clone());
-        if indexed.insert(key,input).is_some() {
-            return Err(crate::DirectSurfaceLiquidError::Identity("duplicate native litter phase input"));
-        }
-    }
-    let mut ordered=Vec::new();
-    for record in configuration.records().iter().filter(|record|record.litter_depth_m.is_some()) {
-        ordered.push(indexed.remove(&(record.key.ofe_id.clone(),record.key.tile_id.clone()))
-            .ok_or(crate::DirectSurfaceLiquidError::Identity("missing native litter phase input"))?);
+        let key = (input.ofe_id.clone(), input.tile_id.clone());
+        if indexed.insert(key, input).is_some() {
+            return Err(crate::DirectSurfaceLiquidError::Identity(
+                "duplicate native litter phase input",
+            ));
+        }
+    }
+    let mut ordered = Vec::new();
+    for record in configuration
+        .records()
+        .iter()
+        .filter(|record| record.litter_depth_m.is_some())
+    {
+        ordered.push(
+            indexed
+                .remove(&(record.key.ofe_id.clone(), record.key.tile_id.clone()))
+                .ok_or(crate::DirectSurfaceLiquidError::Identity(
+                    "missing native litter phase input",
+                ))?,
+        );
     }
     if !indexed.is_empty() {
-        return Err(crate::DirectSurfaceLiquidError::Identity("foreign native litter phase input"));
+        return Err(crate::DirectSurfaceLiquidError::Identity(
+            "foreign native litter phase input",
+        ));
     }
     Ok(ordered)
 }
--- a/crates/openwepp-hillslope-orchestrator/src/land_surface_energy_shadow/v3_multitile_adoption.rs
+++ b/crates/openwepp-hillslope-orchestrator/src/land_surface_energy_shadow/v3_multitile_adoption.rs
@@ -48,85 +48,88 @@
 };
 
 fn project_native_litter_beginning(
-    identity:&RuntimeTileIdentity,beginning:&mut OpenSurfaceProblem,
-    lse_configuration:&LandSurfaceEnergyConfiguration,lse_beginning:&LandSurfaceEnergyV3State,
-    surface_configuration:&SurfaceLiquidConfigurationV2,surface_beginning:&SurfaceLiquidOwnerEnvelopeV2,
-)->Result<(LitterPhaseConfiguration,BeginningLitterPhaseState),LandSurfaceEnergyShadowError> {
-                    let configured = lse_configuration
-                        .ofes
-                        .iter()
-                        .find(|ofe| ofe.ofe_id == identity.ofe_id)
-                        .and_then(|ofe| {
-                            ofe.tiles
-                                .iter()
-                                .find(|configured| configured.tile_id == identity.tile_id)
-                        })
-                        .ok_or(LandSurfaceEnergyShadowError::Identity(
-                            "V3 LSE forest-litter configuration tile",
-                        ))?;
-                    let SurfaceConfiguration::ForestLitter {
-                        liquid_capacity_kg_m2_tile_ground,
-                        thickness_m,
-                        dry_density_kg_m3,
-                        dry_specific_heat_j_kg_k,
-                    } = configured.surface
-                    else {
-                        return Err(LandSurfaceEnergyShadowError::Identity(
-                            "V3 runtime class/configuration forest-litter join",
-                        ));
-                    };
-                    let (surface_configured, surface_state) = find_surface_v2_records(
-                        &identity,
-                        surface_configuration,
-                        surface_beginning,
-                    )?;
-                    let lse_state = lse_beginning
-                        .0
-                        .tiles
-                        .iter()
-                        .find(|state| {
-                            state.ofe_id == identity.ofe_id && state.tile_id == identity.tile_id
-                        })
-                        .ok_or(LandSurfaceEnergyShadowError::Identity(
-                            "V3 LSE forest-litter beginning tile",
-                        ))?;
-                    let ice_capacity = surface_configured.litter_ice_capacity_kg_m2_tile.ok_or(
-                        LandSurfaceEnergyShadowError::Identity("V3 litter ice capacity"),
-                    )?;
-                    let surface_depth = surface_configured
-                        .litter_depth_m
-                        .ok_or(LandSurfaceEnergyShadowError::Identity("V3 litter depth"))?;
-                    let dry_heat_capacity =
-                        thickness_m * dry_density_kg_m3 * dry_specific_heat_j_kg_k;
-                    if surface_depth.to_bits() != thickness_m.to_bits()
-                        || lse_state.surface_enthalpy_j_m2_tile_ground.to_bits()
-                            != surface_state.surface_enthalpy_j_m2_tile.to_bits()
-                        || surface_configured.key.surface_class != SurfaceClass::ForestLitter
-                        || surface_configured.key.source_type != WaterSourceType::LitterLiquid
-                    {
-                        return Err(LandSurfaceEnergyShadowError::Identity(
-                            "V3 native litter beginning/configuration join",
-                        ));
-                    }
-                    beginning.surface_liquid_kg_m2_tile = surface_state.liquid_kg_m2_tile;
-                    beginning.surface_enthalpy_j_m2_tile =
-                        surface_state.surface_enthalpy_j_m2_tile;
-                    beginning.surface_temperature_warm_start_k =
-                        lse_state.surface_temperature_warm_start_k;
-                    beginning.surface_conductivity_w_m_k =
-                        super::v8_input_projection::project_forest_litter_conductivity(
-                            surface_state.liquid_kg_m2_tile,
-                            thickness_m,
-                        )
-                        .map_err(|_| {
-                            LandSurfaceEnergyShadowError::Operand(
-                                "native V3 forest-litter conductivity",
-                            )
-                        })?;
-    Ok((LitterPhaseConfiguration{litter_depth_m:thickness_m,dry_heat_capacity_j_m2_k:dry_heat_capacity,
-        liquid_capacity_kg_m2_tile:liquid_capacity_kg_m2_tile_ground,ice_capacity_kg_m2_tile:ice_capacity},
-        BeginningLitterPhaseState{liquid_kg_m2_tile:surface_state.liquid_kg_m2_tile,ice_kg_m2_tile:surface_state.litter_ice_kg_m2_tile,
-            sensible_energy_j_m2_tile:surface_state.surface_enthalpy_j_m2_tile,temperature_k:lse_state.surface_temperature_warm_start_k}))
+    identity: &RuntimeTileIdentity,
+    beginning: &mut OpenSurfaceProblem,
+    lse_configuration: &LandSurfaceEnergyConfiguration,
+    lse_beginning: &LandSurfaceEnergyV3State,
+    surface_configuration: &SurfaceLiquidConfigurationV2,
+    surface_beginning: &SurfaceLiquidOwnerEnvelopeV2,
+) -> Result<(LitterPhaseConfiguration, BeginningLitterPhaseState), LandSurfaceEnergyShadowError> {
+    let configured = lse_configuration
+        .ofes
+        .iter()
+        .find(|ofe| ofe.ofe_id == identity.ofe_id)
+        .and_then(|ofe| {
+            ofe.tiles
+                .iter()
+                .find(|configured| configured.tile_id == identity.tile_id)
+        })
+        .ok_or(LandSurfaceEnergyShadowError::Identity(
+            "V3 LSE forest-litter configuration tile",
+        ))?;
+    let SurfaceConfiguration::ForestLitter {
+        liquid_capacity_kg_m2_tile_ground,
+        thickness_m,
+        dry_density_kg_m3,
+        dry_specific_heat_j_kg_k,
+    } = configured.surface
+    else {
+        return Err(LandSurfaceEnergyShadowError::Identity(
+            "V3 runtime class/configuration forest-litter join",
+        ));
+    };
+    let (surface_configured, surface_state) =
+        find_surface_v2_records(&identity, surface_configuration, surface_beginning)?;
+    let lse_state = lse_beginning
+        .0
+        .tiles
+        .iter()
+        .find(|state| state.ofe_id == identity.ofe_id && state.tile_id == identity.tile_id)
+        .ok_or(LandSurfaceEnergyShadowError::Identity(
+            "V3 LSE forest-litter beginning tile",
+        ))?;
+    let ice_capacity = surface_configured.litter_ice_capacity_kg_m2_tile.ok_or(
+        LandSurfaceEnergyShadowError::Identity("V3 litter ice capacity"),
+    )?;
+    let surface_depth = surface_configured
+        .litter_depth_m
+        .ok_or(LandSurfaceEnergyShadowError::Identity("V3 litter depth"))?;
+    let dry_heat_capacity = thickness_m * dry_density_kg_m3 * dry_specific_heat_j_kg_k;
+    if surface_depth.to_bits() != thickness_m.to_bits()
+        || lse_state.surface_enthalpy_j_m2_tile_ground.to_bits()
+            != surface_state.surface_enthalpy_j_m2_tile.to_bits()
+        || surface_configured.key.surface_class != SurfaceClass::ForestLitter
+        || surface_configured.key.source_type != WaterSourceType::LitterLiquid
+    {
+        return Err(LandSurfaceEnergyShadowError::Identity(
+            "V3 native litter beginning/configuration join",
+        ));
+    }
+    beginning.surface_liquid_kg_m2_tile = surface_state.liquid_kg_m2_tile;
+    beginning.surface_enthalpy_j_m2_tile = surface_state.surface_enthalpy_j_m2_tile;
+    beginning.surface_temperature_warm_start_k = lse_state.surface_temperature_warm_start_k;
+    beginning.surface_conductivity_w_m_k =
+        super::v8_input_projection::project_forest_litter_conductivity(
+            surface_state.liquid_kg_m2_tile,
+            thickness_m,
+        )
+        .map_err(|_| {
+            LandSurfaceEnergyShadowError::Operand("native V3 forest-litter conductivity")
+        })?;
+    Ok((
+        LitterPhaseConfiguration {
+            litter_depth_m: thickness_m,
+            dry_heat_capacity_j_m2_k: dry_heat_capacity,
+            liquid_capacity_kg_m2_tile: liquid_capacity_kg_m2_tile_ground,
+            ice_capacity_kg_m2_tile: ice_capacity,
+        },
+        BeginningLitterPhaseState {
+            liquid_kg_m2_tile: surface_state.liquid_kg_m2_tile,
+            ice_kg_m2_tile: surface_state.litter_ice_kg_m2_tile,
+            sensible_energy_j_m2_tile: surface_state.surface_enthalpy_j_m2_tile,
+            temperature_k: lse_state.surface_temperature_warm_start_k,
+        },
+    ))
 }
 
 /// Solver-ready projection after every forest-litter row has been rebound to
@@ -306,17 +309,29 @@
         // The validated predecessor projection supplies only structural and
         // forcing operands. Every solver identity is rebound to the native V3
         // configuration/state before either potential or fixed-final solve.
-        let predecessor_identity=identity.clone();
+        let predecessor_identity = identity.clone();
         identity.configuration_sha256 = lse_configuration.configuration_sha256.clone();
         identity.beginning_lse_state_sha256 = lse_beginning.0.state_sha256.clone();
-        if let V8SolverReadyTilePhysics::Covered(column)=&mut physics {
-            let mut sources=projection.tiles.iter().filter(|source|
-                source.ofe_id==predecessor_identity.ofe_id && source.tile_id==predecessor_identity.tile_id);
-            let source=sources.next().ok_or(LandSurfaceEnergyShadowError::Identity("native transparent source tile"))?;
-            if sources.next().is_some() || source.identity!=predecessor_identity {
-                return Err(LandSurfaceEnergyShadowError::Identity("native transparent original runtime source"));
-            }
-            column.rebind_transparent_liquid_native_configuration(&source.forcing,&predecessor_identity,&identity)?;
+        if let V8SolverReadyTilePhysics::Covered(column) = &mut physics {
+            let mut sources = projection.tiles.iter().filter(|source| {
+                source.ofe_id == predecessor_identity.ofe_id
+                    && source.tile_id == predecessor_identity.tile_id
+            });
+            let source = sources
+                .next()
+                .ok_or(LandSurfaceEnergyShadowError::Identity(
+                    "native transparent source tile",
+                ))?;
+            if sources.next().is_some() || source.identity != predecessor_identity {
+                return Err(LandSurfaceEnergyShadowError::Identity(
+                    "native transparent original runtime source",
+                ));
+            }
+            column.rebind_transparent_liquid_native_configuration(
+                &source.forcing,
+                &predecessor_identity,
+                &identity,
+            )?;
         }
         let projected = match (identity.surface_class, physics) {
             (SurfaceClass::ForestLitter, V8SolverReadyTilePhysics::Covered(mut beginning)) => {
@@ -352,8 +367,14 @@
                             "snow-free native litter retained Stage-3 optical custody",
                         ));
                     }
-                    let (litter_configuration,litter_beginning)=project_native_litter_beginning(
-                        &identity,&mut beginning.ground,lse_configuration,lse_beginning,surface_configuration,surface_beginning)?;
+                    let (litter_configuration, litter_beginning) = project_native_litter_beginning(
+                        &identity,
+                        &mut beginning.ground,
+                        lse_configuration,
+                        lse_beginning,
+                        surface_configuration,
+                        surface_beginning,
+                    )?;
                     let beginning_trial = replace_ground_temperature_trial(
                         beginning_trial,
                         beginning.ground.soil_nodes.len(),
@@ -367,7 +388,8 @@
                             potential_initial_trial: beginning_trial.clone(),
                             final_initial_trial: beginning_trial,
                             soil_thermal,
-                            litter_configuration, litter_beginning,
+                            litter_configuration,
+                            litter_beginning,
                         },
                     )
                 }
@@ -378,8 +400,14 @@
                         "open litter has roots",
                     ));
                 }
-                let (litter_configuration,litter_beginning)=project_native_litter_beginning(
-                    &identity,&mut beginning,lse_configuration,lse_beginning,surface_configuration,surface_beginning)?;
+                let (litter_configuration, litter_beginning) = project_native_litter_beginning(
+                    &identity,
+                    &mut beginning,
+                    lse_configuration,
+                    lse_beginning,
+                    surface_configuration,
+                    surface_beginning,
+                )?;
                 let beginning_trial = replace_ground_temperature_trial(
                     beginning_trial,
                     beginning.soil_nodes.len(),
@@ -390,7 +418,8 @@
                     beginning,
                     initial: beginning_trial,
                     soil_thermal,
-                    litter_configuration, litter_beginning,
+                    litter_configuration,
+                    litter_beginning,
                 })
             }
             (SurfaceClass::ForestLitter, V8SolverReadyTilePhysics::Stage3OpenSnow(_)) => {
@@ -1476,7 +1505,10 @@
                 surface_id: identity.surface_id.clone(),
                 release,
             }
-        } else if runon.iter().any(|p| p.kind == crate::DirectSurfaceLiquidParcelKind::TerminalReceiver) {
+        } else if runon
+            .iter()
+            .any(|p| p.kind == crate::DirectSurfaceLiquidParcelKind::TerminalReceiver)
+        {
             DirectTileGroundIngress::CoveredCanopyReleaseAndTerminalGround {
                 ofe_id: identity.ofe_id.clone(),
                 tile_id: identity.tile_id.clone(),
@@ -1646,7 +1678,9 @@
                 "native_litter_context":native_litter_context,
                 "beginning_soil_thermal_state_sha256":id.beginning_soil_thermal_state_sha256,"ground_problem":problem}))
         }).collect()
-    } else { Vec::new() };
+    } else {
+        Vec::new()
+    };
     let mut legacy = Vec::new();
     let mut frozen = Vec::new();
     let mut open_litter = Vec::new();
--- a/crates/openwepp-hillslope-orchestrator/src/land_surface_energy_shadow/v3_tests.rs
+++ b/crates/openwepp-hillslope-orchestrator/src/land_surface_energy_shadow/v3_tests.rs
@@ -638,11 +638,7 @@
         accepted_phase_free_temperature_k: accepted_temperature_k,
         air_density_kg_m3: 1.1,
         air_pressure_pa: 93_000.0,
-        recipient_specific_humidity_kg_kg: if evaporating {
-            0.0
-        } else {
-            saturation
-        },
+        recipient_specific_humidity_kg_kg: if evaporating { 0.0 } else { saturation },
         litter_to_canopy_resistance_s_m: 80.0,
     };
     let raw =
@@ -2207,8 +2203,12 @@
     use super::v3_input_projection::order_native_litter_phase_inputs;
     // Lexical OFE order deliberately differs from configured flow order.
     let fixture = exact_surface_topology_fixture(&["z-upstream".into(), "a-downstream".into()]);
-    let records = fixture.surface_configuration.records().iter()
-        .filter(|record| record.litter_depth_m.is_some()).collect::<Vec<_>>();
+    let records = fixture
+        .surface_configuration
+        .records()
+        .iter()
+        .filter(|record| record.litter_depth_m.is_some())
+        .collect::<Vec<_>>();
     assert_eq!(records.len(), 2);
     // The ordering seam consumes accepted payloads independently of the open
     // or covered evaluator that supplied them; distinct payloads detect swaps.
@@ -2219,18 +2219,39 @@
     covered.ofe_id = records[1].key.ofe_id.clone();
     covered.tile_id = records[1].key.tile_id.clone();
     let expected = vec![open.clone(), covered.clone()];
-    assert_eq!(order_native_litter_phase_inputs(&fixture.surface_configuration,
-        vec![covered.clone(), open.clone()]).expect("configuration order"), expected);
-    assert_eq!(order_native_litter_phase_inputs(&fixture.surface_configuration,
-        expected.clone()).expect("already ordered"), expected);
-    assert!(order_native_litter_phase_inputs(&fixture.surface_configuration,
-        vec![open.clone(), open.clone(), covered.clone()]).is_err());
-    assert!(order_native_litter_phase_inputs(&fixture.surface_configuration,
-        vec![open.clone()]).is_err());
+    assert_eq!(
+        order_native_litter_phase_inputs(
+            &fixture.surface_configuration,
+            vec![covered.clone(), open.clone()]
+        )
+        .expect("configuration order"),
+        expected
+    );
+    assert_eq!(
+        order_native_litter_phase_inputs(&fixture.surface_configuration, expected.clone())
+            .expect("already ordered"),
+        expected
+    );
+    assert!(
+        order_native_litter_phase_inputs(
+            &fixture.surface_configuration,
+            vec![open.clone(), open.clone(), covered.clone()]
+        )
+        .is_err()
+    );
+    assert!(
+        order_native_litter_phase_inputs(&fixture.surface_configuration, vec![open.clone()])
+            .is_err()
+    );
     let mut foreign = covered.clone();
     foreign.ofe_id = OfeId::try_new("foreign").expect("test key");
-    assert!(order_native_litter_phase_inputs(&fixture.surface_configuration,
-        vec![open, covered, foreign]).is_err());
+    assert!(
+        order_native_litter_phase_inputs(
+            &fixture.surface_configuration,
+            vec![open, covered, foreign]
+        )
+        .is_err()
+    );
 }
 
 #[test]
@@ -2239,86 +2260,199 @@
     let mut fixture = exact_surface_topology_fixture(&["z-upstream".into(), "a-downstream".into()]);
     let mut physical_records = fixture.surface_owner.v2_state().unwrap().records().to_vec();
     for record in &mut physical_records {
-        if record.key.surface_class != SurfaceClass::ForestLitter { continue; }
+        if record.key.surface_class != SurfaceClass::ForestLitter {
+            continue;
+        }
         record.litter_ice_kg_m2_tile = 0.1;
         let capacity = 0.04 * 24.0 * 3370.5 + 0.1 * 2106.0;
         record.surface_enthalpy_j_m2_tile = -5.0 * capacity;
-        let tile = fixture.lse_state.0.tiles.iter_mut().find(|tile|
-            tile.ofe_id == record.key.ofe_id && tile.tile_id == record.key.tile_id).unwrap();
+        let tile = fixture
+            .lse_state
+            .0
+            .tiles
+            .iter_mut()
+            .find(|tile| tile.ofe_id == record.key.ofe_id && tile.tile_id == record.key.tile_id)
+            .unwrap();
         tile.surface_enthalpy_j_m2_tile_ground = record.surface_enthalpy_j_m2_tile;
         tile.surface_temperature_warm_start_k = 268.15;
     }
-    fixture.surface_owner = SurfaceLiquidOwnerEnvelopeV2::wrap_v2(&fixture.surface_configuration,
-        SurfaceLiquidOwnedStateV2::try_new(&fixture.surface_configuration, physical_records,
-            fixture.surface_owner.v2_state().unwrap().continuations().to_vec()).unwrap()).unwrap();
+    fixture.surface_owner = SurfaceLiquidOwnerEnvelopeV2::wrap_v2(
+        &fixture.surface_configuration,
+        SurfaceLiquidOwnedStateV2::try_new(
+            &fixture.surface_configuration,
+            physical_records,
+            fixture
+                .surface_owner
+                .v2_state()
+                .unwrap()
+                .continuations()
+                .to_vec(),
+        )
+        .unwrap(),
+    )
+    .unwrap();
     fixture.lse_state.0.state_sha256 = fixture.lse_state.canonical_sha256().unwrap();
     fixture.exact_owner = LseSurfaceEnthalpyOwnerEnvelopeV1::adopt_from_frozen_v2_v3(
-        fixture.exact_owner.owner_id.clone(), &fixture.lse_configuration, &fixture.lse_state,
-        &fixture.surface_configuration, &fixture.surface_owner).unwrap();
-    let inputs = fixture.surface_configuration.parent().records.iter()
+        fixture.exact_owner.owner_id.clone(),
+        &fixture.lse_configuration,
+        &fixture.lse_state,
+        &fixture.surface_configuration,
+        &fixture.surface_owner,
+    )
+    .unwrap();
+    let inputs = fixture
+        .surface_configuration
+        .parent()
+        .records
+        .iter()
         .filter(|record| record.key.ofe_id.as_str() == "z-upstream")
-        .map(|record| crate::direct_runtime::DirectZeroDurationSnowLiquidInputV1 {
-            output_receipt_sha256: [1;32], output_set_sha256: [2;32],
-            predecessor_owner_set_sha256: [3;32], receiver_context_sha256: [4;32],
-            support_start_ns: 60_000_000_000, support_end_ns: 120_000_000_000,
-            receiver_ordinal: 0, ofe_id: record.key.ofe_id.clone(),
-            tile_id: record.key.tile_id.clone(), tile_fraction: record.tile_fraction,
-            mass_kg_m2_tile_ground: 0.25, sensible_enthalpy_j_m2_tile_ground: 0.0,
-        }).collect::<Vec<_>>();
+        .map(
+            |record| crate::direct_runtime::DirectZeroDurationSnowLiquidInputV1 {
+                output_receipt_sha256: [1; 32],
+                output_set_sha256: [2; 32],
+                predecessor_owner_set_sha256: [3; 32],
+                receiver_context_sha256: [4; 32],
+                support_start_ns: 60_000_000_000,
+                support_end_ns: 120_000_000_000,
+                receiver_ordinal: 0,
+                ofe_id: record.key.ofe_id.clone(),
+                tile_id: record.key.tile_id.clone(),
+                tile_fraction: record.tile_fraction,
+                mass_kg_m2_tile_ground: 0.25,
+                sensible_enthalpy_j_m2_tile_ground: 0.0,
+            },
+        )
+        .collect::<Vec<_>>();
     let before = fixture.surface_owner.clone();
     let exact_before = fixture.exact_owner.clone();
     let candidate = prepare_native_snow_liquid_candidate_v1(
-        &fixture.surface_configuration, &fixture.lse_configuration, &fixture.lse_state,
-        &fixture.surface_owner, &fixture.exact_owner, &inputs, TransactionId(1), None
-    ).expect("native positive-origin allocation");
+        &fixture.surface_configuration,
+        &fixture.lse_configuration,
+        &fixture.lse_state,
+        &fixture.surface_owner,
+        &fixture.exact_owner,
+        &inputs,
+        TransactionId(1),
+        None,
+    )
+    .expect("native positive-origin allocation");
     assert_eq!(fixture.surface_owner, before);
     assert_eq!(fixture.exact_owner, exact_before);
-    for (begin, end) in before.v2_state().unwrap().records().iter()
-        .zip(candidate.surface_owner.v2_state().unwrap().records()) {
-        assert_eq!(begin.litter_ice_kg_m2_tile.to_bits(), end.litter_ice_kg_m2_tile.to_bits());
-        assert_eq!(begin.surface_enthalpy_j_m2_tile.to_bits(), end.surface_enthalpy_j_m2_tile.to_bits());
+    for (begin, end) in before
+        .v2_state()
+        .unwrap()
+        .records()
+        .iter()
+        .zip(candidate.surface_owner.v2_state().unwrap().records())
+    {
+        assert_eq!(
+            begin.litter_ice_kg_m2_tile.to_bits(),
+            end.litter_ice_kg_m2_tile.to_bits()
+        );
+        assert_eq!(
+            begin.surface_enthalpy_j_m2_tile.to_bits(),
+            end.surface_enthalpy_j_m2_tile.to_bits()
+        );
     }
-    for (begin, end) in exact_before.records().iter().zip(candidate.exact_owner.records()) {
+    for (begin, end) in exact_before
+        .records()
+        .iter()
+        .zip(candidate.exact_owner.records())
+    {
         assert_eq!(begin.enthalpy_carry, end.enthalpy_carry);
     }
-    let cold_litter = candidate.lse_state.0.tiles.iter().find(|tile|
-        tile.ofe_id.as_str() == "z-upstream" && tile.tile_id.as_str() == "forest-1").unwrap();
-    let expected_temperature = 273.15 - 5.0 * (0.04 * 24.0 * 3370.5 + 0.1 * 2106.0)
-        / (0.04 * 24.0 * 3370.5 + 0.1 * 2106.0 + 0.25 * 4218.0);
+    let cold_litter = candidate
+        .lse_state
+        .0
+        .tiles
+        .iter()
+        .find(|tile| tile.ofe_id.as_str() == "z-upstream" && tile.tile_id.as_str() == "forest-1")
+        .unwrap();
+    let expected_temperature = 273.15
+        - 5.0 * (0.04 * 24.0 * 3370.5 + 0.1 * 2106.0)
+            / (0.04 * 24.0 * 3370.5 + 0.1 * 2106.0 + 0.25 * 4218.0);
     assert!((cold_litter.surface_temperature_warm_start_k - expected_temperature).abs() < 1e-12);
     assert!(cold_litter.surface_temperature_warm_start_k > 268.15);
     for record in &mut fixture.exact_owner.records {
-        record.enthalpy_carry = ExactDyadicEnthalpy::exact_product_binary64(&[2.0_f64.powi(-60)]).unwrap();
+        record.enthalpy_carry =
+            ExactDyadicEnthalpy::exact_product_binary64(&[2.0_f64.powi(-60)]).unwrap();
     }
     reseal_exact_surface_owner(&mut fixture.exact_owner);
-    let parcels = [1.0, 2.0_f64.powi(-54), 2.0_f64.powi(-54)].into_iter()
-        .enumerate().flat_map(|(ordinal, energy)| inputs.iter().cloned().map(move |mut input| {
-            input.output_receipt_sha256 = [u8::try_from(ordinal + 10).unwrap(); 32];
-            input.sensible_enthalpy_j_m2_tile_ground = energy; input
-        })).collect::<Vec<_>>();
+    let parcels = [1.0, 2.0_f64.powi(-54), 2.0_f64.powi(-54)]
+        .into_iter()
+        .enumerate()
+        .flat_map(|(ordinal, energy)| {
+            inputs.iter().cloned().map(move |mut input| {
+                input.output_receipt_sha256 = [u8::try_from(ordinal + 10).unwrap(); 32];
+                input.sensible_enthalpy_j_m2_tile_ground = energy;
+                input
+            })
+        })
+        .collect::<Vec<_>>();
     let multiple = prepare_native_snow_liquid_candidate_v1(
-        &fixture.surface_configuration, &fixture.lse_configuration, &fixture.lse_state,
-        &fixture.surface_owner, &fixture.exact_owner, &parcels, TransactionId(1), None
-    ).expect("multiple actual energy operands");
+        &fixture.surface_configuration,
+        &fixture.lse_configuration,
+        &fixture.lse_state,
+        &fixture.surface_owner,
+        &fixture.exact_owner,
+        &parcels,
+        TransactionId(1),
+        None,
+    )
+    .expect("multiple actual energy operands");
     let mut distinguishes_rounded_aggregate = false;
     for begin in fixture.exact_owner.records() {
-        let end = multiple.exact_owner.records().iter().find(|end| end.surface_key == begin.surface_key).unwrap();
-        let aggregate = multiple.allocation.retained_enthalpy_credits.iter().find(|credit|
-            credit.ofe_id == begin.surface_key.ofe_id && credit.tile_id == begin.surface_key.tile_id)
+        let end = multiple
+            .exact_owner
+            .records()
+            .iter()
+            .find(|end| end.surface_key == begin.surface_key)
+            .unwrap();
+        let aggregate = multiple
+            .allocation
+            .retained_enthalpy_credits
+            .iter()
+            .find(|credit| {
+                credit.ofe_id == begin.surface_key.ofe_id
+                    && credit.tile_id == begin.surface_key.tile_id
+            })
             .map_or(0.0, |credit| credit.enthalpy_j_m2_tile_ground);
-        let rounded = ExactDyadicEnthalpy::exact_sum_binary64(begin.enthalpy_hi_j_m2_tile,
-            &begin.enthalpy_carry, &[aggregate]).unwrap().rounded_high_and_remainder().unwrap();
-        distinguishes_rounded_aggregate |= rounded != (end.enthalpy_hi_j_m2_tile, end.enthalpy_carry.clone());
+        let rounded = ExactDyadicEnthalpy::exact_sum_binary64(
+            begin.enthalpy_hi_j_m2_tile,
+            &begin.enthalpy_carry,
+            &[aggregate],
+        )
+        .unwrap()
+        .rounded_high_and_remainder()
+        .unwrap();
+        distinguishes_rounded_aggregate |=
+            rounded != (end.enthalpy_hi_j_m2_tile, end.enthalpy_carry.clone());
     }
-    assert!(distinguishes_rounded_aggregate, "individual receipt remainders survive");
-    println!("NATIVE_DRAIN_EXACT_OPERANDS={}", serde_json::json!({
+    assert!(
+        distinguishes_rounded_aggregate,
+        "individual receipt remainders survive"
+    );
+    println!(
+        "NATIVE_DRAIN_EXACT_OPERANDS={}",
+        serde_json::json!({
         "beginning":fixture.exact_owner, "ending":multiple.exact_owner,
-        "receipts":multiple.allocation.receipts}));
-    let mut bad = inputs.clone(); bad[0].support_end_ns = bad[0].support_start_ns;
-    assert!(prepare_native_snow_liquid_candidate_v1(
-        &fixture.surface_configuration, &fixture.lse_configuration, &fixture.lse_state,
-        &fixture.surface_owner, &fixture.exact_owner, &bad, TransactionId(1), None
-    ).is_err());
+        "receipts":multiple.allocation.receipts})
+    );
+    let mut bad = inputs.clone();
+    bad[0].support_end_ns = bad[0].support_start_ns;
+    assert!(
+        prepare_native_snow_liquid_candidate_v1(
+            &fixture.surface_configuration,
+            &fixture.lse_configuration,
+            &fixture.lse_state,
+            &fixture.surface_owner,
+            &fixture.exact_owner,
+            &bad,
+            TransactionId(1),
+            None
+        )
+        .is_err()
+    );
 }
 
 #[test]
@@ -2327,47 +2461,91 @@
     let mut fixture = exact_surface_topology_fixture(&["upstream".into()]);
     for ofe in &mut fixture.lse_configuration.ofes {
         for tile in &mut ofe.tiles {
-            if let openwepp_land_surface_energy::SurfaceConfiguration::ForestLitter { thickness_m, dry_density_kg_m3,
-                dry_specific_heat_j_kg_k, .. } = &mut tile.surface {
+            if let openwepp_land_surface_energy::SurfaceConfiguration::ForestLitter {
+                thickness_m,
+                dry_density_kg_m3,
+                dry_specific_heat_j_kg_k,
+                ..
+            } = &mut tile.surface
+            {
                 *dry_specific_heat_j_kg_k = 1e9 / (*thickness_m * *dry_density_kg_m3);
             }
         }
     }
-    fixture.lse_configuration.configuration_sha256 = fixture.lse_configuration.canonical_sha256().unwrap();
-    fixture.lse_state.0.configuration_sha256 = fixture.lse_configuration.configuration_sha256.clone();
+    fixture.lse_configuration.configuration_sha256 =
+        fixture.lse_configuration.canonical_sha256().unwrap();
+    fixture.lse_state.0.configuration_sha256 =
+        fixture.lse_configuration.configuration_sha256.clone();
     fixture.lse_state.0.state_sha256 = fixture.lse_state.canonical_sha256().unwrap();
     fixture.exact_owner = LseSurfaceEnthalpyOwnerEnvelopeV1::adopt_from_frozen_v2_v3(
-        fixture.exact_owner.owner_id.clone(), &fixture.lse_configuration, &fixture.lse_state,
-        &fixture.surface_configuration, &fixture.surface_owner).unwrap();
-    let inputs = fixture.surface_configuration.parent().records.iter().map(|record|
-        crate::direct_runtime::DirectZeroDurationSnowLiquidInputV1 {
-            output_receipt_sha256:[1;32], output_set_sha256:[2;32], predecessor_owner_set_sha256:[3;32],
-            receiver_context_sha256:[4;32], support_start_ns:60_000_000_000, support_end_ns:120_000_000_000,
-            receiver_ordinal:0, ofe_id:record.key.ofe_id.clone(), tile_id:record.key.tile_id.clone(),
-            tile_fraction:record.tile_fraction, mass_kg_m2_tile_ground:1e-6,
-            sensible_enthalpy_j_m2_tile_ground:1e-4,
-        }).collect::<Vec<_>>();
-    let before_surface=fixture.surface_owner.clone();
-    let before_lse=fixture.lse_state.clone();
-    let before_exact=fixture.exact_owner.clone();
-    assert!(matches!(prepare_native_snow_liquid_candidate_v1(
-        &fixture.surface_configuration, &fixture.lse_configuration, &fixture.lse_state,
-        &fixture.surface_owner, &fixture.exact_owner, &inputs, TransactionId(1), None),
-        Err(crate::DirectSurfaceLiquidError::Closure("native drain litter temperature coordinate"))));
-    assert_eq!(fixture.surface_owner,before_surface);
-    assert_eq!(fixture.lse_state,before_lse);
-    assert_eq!(fixture.exact_owner,before_exact);
+        fixture.exact_owner.owner_id.clone(),
+        &fixture.lse_configuration,
+        &fixture.lse_state,
+        &fixture.surface_configuration,
+        &fixture.surface_owner,
+    )
+    .unwrap();
+    let inputs = fixture
+        .surface_configuration
+        .parent()
+        .records
+        .iter()
+        .map(
+            |record| crate::direct_runtime::DirectZeroDurationSnowLiquidInputV1 {
+                output_receipt_sha256: [1; 32],
+                output_set_sha256: [2; 32],
+                predecessor_owner_set_sha256: [3; 32],
+                receiver_context_sha256: [4; 32],
+                support_start_ns: 60_000_000_000,
+                support_end_ns: 120_000_000_000,
+                receiver_ordinal: 0,
+                ofe_id: record.key.ofe_id.clone(),
+                tile_id: record.key.tile_id.clone(),
+                tile_fraction: record.tile_fraction,
+                mass_kg_m2_tile_ground: 1e-6,
+                sensible_enthalpy_j_m2_tile_ground: 1e-4,
+            },
+        )
+        .collect::<Vec<_>>();
+    let before_surface = fixture.surface_owner.clone();
+    let before_lse = fixture.lse_state.clone();
+    let before_exact = fixture.exact_owner.clone();
+    assert!(matches!(
+        prepare_native_snow_liquid_candidate_v1(
+            &fixture.surface_configuration,
+            &fixture.lse_configuration,
+            &fixture.lse_state,
+            &fixture.surface_owner,
+            &fixture.exact_owner,
+            &inputs,
+            TransactionId(1),
+            None
+        ),
+        Err(crate::DirectSurfaceLiquidError::Closure(
+            "native drain litter temperature coordinate"
+        ))
+    ));
+    assert_eq!(fixture.surface_owner, before_surface);
+    assert_eq!(fixture.lse_state, before_lse);
+    assert_eq!(fixture.exact_owner, before_exact);
 }
 
 #[test]
 fn native_snow_liquid_publication_authenticates_replay_and_rejects_substitution() {
-    use openwepp_coupled_time::{Digest32,ModelTimeNs,TimeSupport,digest_bytes,framed_sha256,FramedField};
+    use crate::v9_real_consumer_shadow::Stage3AcceptedSnowLiquidOutputV1;
     use crate::v9_real_consumer_shadow::native_snow_liquid_publication::{
-        NativeSnowLiquidPublicationV1,NativeSnowCustodyIdentityV1,NativeSnowCustodyKindV1};
-    use crate::v9_real_consumer_shadow::Stage3AcceptedSnowLiquidOutputV1;
-    let fixture=exact_surface_topology_fixture(&["upstream".into()]);
-    let support=TimeSupport::new(ModelTimeNs::new(60_000_000_000),ModelTimeNs::new(120_000_000_000)).unwrap();
-    let binding=&fixture.surface_configuration.parent().ofe_bindings[0];
+        NativeSnowCustodyIdentityV1, NativeSnowCustodyKindV1, NativeSnowLiquidPublicationV1,
+    };
+    use openwepp_coupled_time::{
+        Digest32, FramedField, ModelTimeNs, TimeSupport, digest_bytes, framed_sha256,
+    };
+    let fixture = exact_surface_topology_fixture(&["upstream".into()]);
+    let support = TimeSupport::new(
+        ModelTimeNs::new(60_000_000_000),
+        ModelTimeNs::new(120_000_000_000),
+    )
+    .unwrap();
+    let binding = &fixture.surface_configuration.parent().ofe_bindings[0];
     let mut output=Stage3AcceptedSnowLiquidOutputV1 {
         support,lane_id:binding.production_lane_id,ofe_id:binding.ofe_id.clone(),
         topology_sha256:Digest32::from_bytes([1;32]),beginning_snow_owner_sha256:Digest32::from_bytes([2;32]),
@@ -2381,76 +2559,203 @@
             })).unwrap()).collect(),refreeze_kg_m2_ofe_ground:0.0,
         physical_ledger_receipt_sha256:Digest32::from_bytes([30;32]),receipt_sha256:Digest32::zero(),
     };
-    output.receipt_sha256=digest_bytes(&serde_json::to_vec(&output).unwrap());
+    output.receipt_sha256 = digest_bytes(&serde_json::to_vec(&output).unwrap());
     output.validate().expect("sealed test donor output");
-    let output_set=framed_sha256("stage3-v11-positive-support-liquid-output-set",&[FramedField {
-        tag:"snow_liquid_output",value:output.receipt_sha256.as_bytes()}]).unwrap();
-    let mut identity=NativeSnowCustodyIdentityV1 {policy_id:14,kind:NativeSnowCustodyKindV1::Drain,
-        parent_transaction_sha256:Digest32::from_bytes([40;32]),transaction_id:TransactionId(1),
-        predecessor_transaction_id:None,parent_start_ns:0,parent_end_ns:1_800_000_000_000,
-        support_start_ns:support.start_ns().get(),support_end_ns:support.end_ns().get(),receiver_ordinal:0,
-        donor_ending_complete_owner_sha256:Digest32::from_bytes([41;32]),receiver_context_sha256:Digest32::from_bytes([42;32]),
-        output_set_sha256:output_set,predecessor_physical_receipt_chain_sha256:fixture.lse_state.0.state_sha256.to_string()};
-    identity.receiver_context_sha256=identity.expected_receiver_context(&[output.clone()]).unwrap();
-    let record=NativeSnowLiquidPublicationV1::try_new(&fixture.surface_configuration,&fixture.lse_configuration,
-        identity.clone(), openwepp_coupled_time::Policy14RuntimeAuthority::V11SnowCovered, crate::land_surface_energy_shadow::endpoint_fixture().lse_state,fixture.lse_state.clone(),&fixture.surface_owner,fixture.exact_owner.clone(),vec![output.clone()])
-        .expect("native drain publication arithmetic");
-    let begin_composite: serde_json::Value = serde_json::from_slice(&record.lse_owner_bytes(false).unwrap()).unwrap();
-    let end_composite: serde_json::Value = serde_json::from_slice(&record.lse_owner_bytes(true).unwrap()).unwrap();
-    assert_eq!(begin_composite["runtime_authority"], serde_json::json!("V11SnowCovered"));
-    assert_eq!(begin_composite["runtime_authority"], end_composite["runtime_authority"]);
+    let output_set = framed_sha256(
+        "stage3-v11-positive-support-liquid-output-set",
+        &[FramedField {
+            tag: "snow_liquid_output",
+            value: output.receipt_sha256.as_bytes(),
+        }],
+    )
+    .unwrap();
+    let mut identity = NativeSnowCustodyIdentityV1 {
+        policy_id: 14,
+        kind: NativeSnowCustodyKindV1::Drain,
+        parent_transaction_sha256: Digest32::from_bytes([40; 32]),
+        transaction_id: TransactionId(1),
+        predecessor_transaction_id: None,
+        parent_start_ns: 0,
+        parent_end_ns: 1_800_000_000_000,
+        support_start_ns: support.start_ns().get(),
+        support_end_ns: support.end_ns().get(),
+        receiver_ordinal: 0,
+        donor_ending_complete_owner_sha256: Digest32::from_bytes([41; 32]),
+        receiver_context_sha256: Digest32::from_bytes([42; 32]),
+        output_set_sha256: output_set,
+        predecessor_physical_receipt_chain_sha256: fixture.lse_state.0.state_sha256.to_string(),
+    };
+    identity.receiver_context_sha256 = identity
+        .expected_receiver_context(&[output.clone()])
+        .unwrap();
+    let record = NativeSnowLiquidPublicationV1::try_new(
+        &fixture.surface_configuration,
+        &fixture.lse_configuration,
+        identity.clone(),
+        openwepp_coupled_time::Policy14RuntimeAuthority::V11SnowCovered,
+        crate::land_surface_energy_shadow::endpoint_fixture().lse_state,
+        fixture.lse_state.clone(),
+        &fixture.surface_owner,
+        fixture.exact_owner.clone(),
+        vec![output.clone()],
+    )
+    .expect("native drain publication arithmetic");
+    let begin_composite: serde_json::Value =
+        serde_json::from_slice(&record.lse_owner_bytes(false).unwrap()).unwrap();
+    let end_composite: serde_json::Value =
+        serde_json::from_slice(&record.lse_owner_bytes(true).unwrap()).unwrap();
+    assert_eq!(
+        begin_composite["runtime_authority"],
+        serde_json::json!("V11SnowCovered")
+    );
+    assert_eq!(
+        begin_composite["runtime_authority"],
+        end_composite["runtime_authority"]
+    );
     let mut wrong_mode = record.clone();
-    wrong_mode.runtime_authority = openwepp_coupled_time::Policy14RuntimeAuthority::V10NonpositiveAssimilation;
-    assert!(wrong_mode.validate(&fixture.surface_configuration, &fixture.lse_configuration).is_err());
-    assert_eq!(begin_composite["active_covered_runtime_lse_v1"], end_composite["active_covered_runtime_lse_v1"]);
-    assert_ne!(begin_composite["native_litter_lse_v3"], end_composite["native_litter_lse_v3"]);
-    assert_ne!(begin_composite["native_exact_surface_enthalpy_v4"], end_composite["native_exact_surface_enthalpy_v4"]);
-    let bytes=record.canonical_bytes(&fixture.surface_configuration,&fixture.lse_configuration).unwrap();
-    assert_eq!(NativeSnowLiquidPublicationV1::from_canonical_bytes(&fixture.surface_configuration,
-        &fixture.lse_configuration,&bytes).unwrap(),record);
-    let mut poison=record.clone();poison.ending_lse.0.last_accepted_transaction_id=Some(TransactionId(1));
-    assert!(poison.validate(&fixture.surface_configuration,&fixture.lse_configuration).is_err());
-    let mut poison=record.clone();poison.identity.policy_id=11;
-    assert!(poison.validate(&fixture.surface_configuration,&fixture.lse_configuration).is_err());
-    let mut poison=record.clone();poison.outputs[0].support=TimeSupport::new(ModelTimeNs::new(0),support.end_ns()).unwrap();
-    assert!(poison.validate(&fixture.surface_configuration,&fixture.lse_configuration).is_err());
-    assert!(NativeSnowLiquidPublicationV1::try_new(&fixture.surface_configuration,&fixture.lse_configuration,
-        identity, openwepp_coupled_time::Policy14RuntimeAuthority::V11SnowCovered, crate::land_surface_energy_shadow::endpoint_fixture().lse_state,fixture.lse_state.clone(),&fixture.surface_owner,fixture.exact_owner.clone(),vec![output.clone(),output]).is_err());
+    wrong_mode.runtime_authority =
+        openwepp_coupled_time::Policy14RuntimeAuthority::V10NonpositiveAssimilation;
+    assert!(
+        wrong_mode
+            .validate(&fixture.surface_configuration, &fixture.lse_configuration)
+            .is_err()
+    );
+    assert_eq!(
+        begin_composite["active_covered_runtime_lse_v1"],
+        end_composite["active_covered_runtime_lse_v1"]
+    );
+    assert_ne!(
+        begin_composite["native_litter_lse_v3"],
+        end_composite["native_litter_lse_v3"]
+    );
+    assert_ne!(
+        begin_composite["native_exact_surface_enthalpy_v4"],
+        end_composite["native_exact_surface_enthalpy_v4"]
+    );
+    let bytes = record
+        .canonical_bytes(&fixture.surface_configuration, &fixture.lse_configuration)
+        .unwrap();
+    assert_eq!(
+        NativeSnowLiquidPublicationV1::from_canonical_bytes(
+            &fixture.surface_configuration,
+            &fixture.lse_configuration,
+            &bytes
+        )
+        .unwrap(),
+        record
+    );
+    let mut poison = record.clone();
+    poison.ending_lse.0.last_accepted_transaction_id = Some(TransactionId(1));
+    assert!(
+        poison
+            .validate(&fixture.surface_configuration, &fixture.lse_configuration)
+            .is_err()
+    );
+    let mut poison = record.clone();
+    poison.identity.policy_id = 11;
+    assert!(
+        poison
+            .validate(&fixture.surface_configuration, &fixture.lse_configuration)
+            .is_err()
+    );
+    let mut poison = record.clone();
+    poison.outputs[0].support = TimeSupport::new(ModelTimeNs::new(0), support.end_ns()).unwrap();
+    assert!(
+        poison
+            .validate(&fixture.surface_configuration, &fixture.lse_configuration)
+            .is_err()
+    );
+    assert!(
+        NativeSnowLiquidPublicationV1::try_new(
+            &fixture.surface_configuration,
+            &fixture.lse_configuration,
+            identity,
+            openwepp_coupled_time::Policy14RuntimeAuthority::V11SnowCovered,
+            crate::land_surface_energy_shadow::endpoint_fixture().lse_state,
+            fixture.lse_state.clone(),
+            &fixture.surface_owner,
+            fixture.exact_owner.clone(),
+            vec![output.clone(), output]
+        )
+        .is_err()
+    );
     // This is a record/parser test. Resident history acceptance and fresh
     // process restart are separate required tests, not implied by round-trip.
 }
 
-
 #[test]
 fn native_composite_owner_preserves_roles_and_refuses_component_poisons() {
-    use crate::v9_real_consumer_shadow::composite_lse_owner::{composite_lse_bytes, CompositeLseOwnerV1};
+    use crate::v9_real_consumer_shadow::composite_lse_owner::{
+        CompositeLseOwnerV1, composite_lse_bytes,
+    };
     let fixture = exact_surface_topology_fixture(&["ofe-1".into()]);
     let active = endpoint_fixture().lse_state;
-    let bytes = composite_lse_bytes(openwepp_coupled_time::Policy14RuntimeAuthority::V11SnowCovered, &active, &fixture.lse_state, &fixture.exact_owner).unwrap();
+    let bytes = composite_lse_bytes(
+        openwepp_coupled_time::Policy14RuntimeAuthority::V11SnowCovered,
+        &active,
+        &fixture.lse_state,
+        &fixture.exact_owner,
+    )
+    .unwrap();
     CompositeLseOwnerV1::from_canonical_bytes(&bytes).unwrap();
     let value: serde_json::Value = serde_json::from_slice(&bytes).unwrap();
-    for key in ["active_covered_runtime_lse_v1", "native_litter_lse_v3", "native_exact_surface_enthalpy_v4"] {
+    for key in [
+        "active_covered_runtime_lse_v1",
+        "native_litter_lse_v3",
+        "native_exact_surface_enthalpy_v4",
+    ] {
         let mut poison = value.clone();
         poison.as_object_mut().unwrap().remove(key);
-        assert!(CompositeLseOwnerV1::from_canonical_bytes(&serde_json::to_vec(&poison).unwrap()).is_err());
+        assert!(
+            CompositeLseOwnerV1::from_canonical_bytes(&serde_json::to_vec(&poison).unwrap())
+                .is_err()
+        );
     }
-    for (key, bad) in [("policy_id", serde_json::json!(13)), ("schema", serde_json::json!("foreign")),
-        ("active_covered_sha256", serde_json::json!(vec![0; 32]))] {
-        let mut poison = value.clone(); poison[key] = bad;
-        assert!(CompositeLseOwnerV1::from_canonical_bytes(&serde_json::to_vec(&poison).unwrap()).is_err());
+    for (key, bad) in [
+        ("policy_id", serde_json::json!(13)),
+        ("schema", serde_json::json!("foreign")),
+        ("active_covered_sha256", serde_json::json!(vec![0; 32])),
+    ] {
+        let mut poison = value.clone();
+        poison[key] = bad;
+        assert!(
+            CompositeLseOwnerV1::from_canonical_bytes(&serde_json::to_vec(&poison).unwrap())
+                .is_err()
+        );
     }
     let mut poison = value.clone();
     poison["active_covered_runtime_lse_v1"] = value["native_litter_lse_v3"].clone();
     poison["native_litter_lse_v3"] = value["active_covered_runtime_lse_v1"].clone();
-    assert!(CompositeLseOwnerV1::from_canonical_bytes(&serde_json::to_vec(&poison).unwrap()).is_err());
-    assert!(CompositeLseOwnerV1::from_canonical_bytes(&serde_json::to_vec(&active).unwrap()).is_err());
+    assert!(
+        CompositeLseOwnerV1::from_canonical_bytes(&serde_json::to_vec(&poison).unwrap()).is_err()
+    );
+    assert!(
+        CompositeLseOwnerV1::from_canonical_bytes(&serde_json::to_vec(&active).unwrap()).is_err()
+    );
     let mut changed = active.clone();
     changed.tiles[0].surface_enthalpy_j_m2_tile_ground += 1.0;
-    assert!(composite_lse_bytes(openwepp_coupled_time::Policy14RuntimeAuthority::V11SnowCovered, &changed, &fixture.lse_state, &fixture.exact_owner).is_err());
+    assert!(
+        composite_lse_bytes(
+            openwepp_coupled_time::Policy14RuntimeAuthority::V11SnowCovered,
+            &changed,
+            &fixture.lse_state,
+            &fixture.exact_owner
+        )
+        .is_err()
+    );
     changed.state_sha256 = changed.canonical_sha256().unwrap();
-    let changed_bytes = composite_lse_bytes(openwepp_coupled_time::Policy14RuntimeAuthority::V11SnowCovered, &changed, &fixture.lse_state, &fixture.exact_owner).unwrap();
+    let changed_bytes = composite_lse_bytes(
+        openwepp_coupled_time::Policy14RuntimeAuthority::V11SnowCovered,
+        &changed,
+        &fixture.lse_state,
+        &fixture.exact_owner,
+    )
+    .unwrap();
     assert_ne!(bytes, changed_bytes);
     let after: serde_json::Value = serde_json::from_slice(&changed_bytes).unwrap();
     assert_eq!(value["native_litter_lse_v3"], after["native_litter_lse_v3"]);
-    assert_eq!(value["native_exact_surface_enthalpy_v4"], after["native_exact_surface_enthalpy_v4"]);
-}
+    assert_eq!(
+        value["native_exact_surface_enthalpy_v4"],
+        after["native_exact_surface_enthalpy_v4"]
+    );
+}
--- a/crates/openwepp-hillslope-orchestrator/src/snow_accuracy_bulk.rs
+++ b/crates/openwepp-hillslope-orchestrator/src/snow_accuracy_bulk.rs
@@ -14,8 +14,7 @@
     Phase,
     Conservation,
 }
-#[derive(Clone, Copy, Debug, PartialEq)]
-#[derive(serde::Serialize)]
+#[derive(Clone, Copy, Debug, PartialEq, serde::Serialize)]
 pub struct State {
     pub water: f64,
     pub enthalpy: f64,
@@ -28,8 +27,7 @@
     pub cold: f64,
     pub depth: f64,
 }
-#[derive(Clone, Copy, Debug, PartialEq)]
-#[derive(serde::Serialize)]
+#[derive(Clone, Copy, Debug, PartialEq, serde::Serialize)]
 pub struct Forcing {
     pub seconds: f64,
     pub solid: f64,
@@ -45,8 +43,7 @@
     pub soil_temperature: f64,
     pub soil_capacity: f64,
 }
-#[derive(Clone, Copy, Debug, PartialEq)]
-#[derive(serde::Serialize)]
+#[derive(Clone, Copy, Debug, PartialEq, serde::Serialize)]
 pub struct Outcome {
     pub ending: State,
     pub liquid_released: f64,
@@ -263,8 +260,7 @@
     })
 }
 
-#[derive(Clone, Copy, Debug, PartialEq)]
-#[derive(serde::Serialize)]
+#[derive(Clone, Copy, Debug, PartialEq, serde::Serialize)]
 pub struct Receipt {
     pub policy: u8,
     pub beginning: State,
--- a/crates/openwepp-hillslope-orchestrator/src/snow_accuracy_observer.rs
+++ b/crates/openwepp-hillslope-orchestrator/src/snow_accuracy_observer.rs
@@ -1,7 +1,7 @@
 //! Isolated experiment observation; never installed in primary runtime source.
 use serde_json::Value;
+use std::collections::BTreeMap;
 use std::io::Write as _;
-use std::collections::BTreeMap;
 use std::sync::Mutex;
 use std::sync::atomic::{AtomicBool, Ordering};
 static STARTED: AtomicBool = AtomicBool::new(false);
@@ -113,12 +113,18 @@
                 let count = capture.row_count;
                 if let Some((_, writer)) = capture.spool.as_mut() {
                     let result = (|| -> Result<(), ObservationWriteError> {
-                        if count != 0 { writer.write_all(b",")?; }
+                        if count != 0 {
+                            writer.write_all(b",")?;
+                        }
                         serde_json::to_writer(writer, &row)?;
                         Ok(())
                     })();
-                    if result.is_err() { POISONED.store(true, Ordering::Relaxed); }
-                } else { capture.rows.push(row); }
+                    if result.is_err() {
+                        POISONED.store(true, Ordering::Relaxed);
+                    }
+                } else {
+                    capture.rows.push(row);
+                }
                 capture.row_count += 1;
             } else {
                 capture.overflow = true;
@@ -189,11 +195,20 @@
 /// Select bounded-memory physical recording before any worker starts.
 pub fn begin_spooled(physical: bool, path: &std::path::Path) -> Result<(), ObservationWriteError> {
     begin(physical);
-    if POISONED.load(Ordering::Relaxed) { return Err(ObservationWriteError::State("session start")); }
+    if POISONED.load(Ordering::Relaxed) {
+        return Err(ObservationWriteError::State("session start"));
+    }
     if physical {
-        let file = std::fs::OpenOptions::new().write(true).create_new(true).open(path)?;
-        let mut slot = CAPTURE.lock().map_err(|_| ObservationWriteError::State("capture poisoned"))?;
-        let capture = slot.as_mut().ok_or(ObservationWriteError::State("capture absent"))?;
+        let file = std::fs::OpenOptions::new()
+            .write(true)
+            .create_new(true)
+            .open(path)?;
+        let mut slot = CAPTURE
+            .lock()
+            .map_err(|_| ObservationWriteError::State("capture poisoned"))?;
+        let capture = slot
+            .as_mut()
+            .ok_or(ObservationWriteError::State("capture absent"))?;
         capture.spool = Some((path.to_owned(), std::io::BufWriter::new(file)));
     }
     Ok(())
@@ -202,8 +217,11 @@
 /// Write the same V2 records without materializing the complete physical array.
 pub fn finish_to_file(path: &std::path::Path) -> Result<(), ObservationWriteError> {
     PHYSICAL.store(false, Ordering::Relaxed);
-    let capture = CAPTURE.lock().map_err(|_| ObservationWriteError::State("capture poisoned"))?
-        .take().ok_or(ObservationWriteError::State("capture absent"))?;
+    let capture = CAPTURE
+        .lock()
+        .map_err(|_| ObservationWriteError::State("capture poisoned"))?
+        .take()
+        .ok_or(ObservationWriteError::State("capture absent"))?;
     let metadata = serde_json::json!({"schema":"snow_accuracy_observation_v2",
         "session":{"process":std::process::id(),"ordinal":1,"one_session_per_process":true},
         "physical_enabled":capture.physical,
@@ -212,18 +230,26 @@
         "physical_ledgers":std::mem::take(&mut *LEDGERS.lock().map_err(|_| ObservationWriteError::State("ledgers poisoned"))?),
         "counts_poisoned":POISONED.load(Ordering::Relaxed)});
     let mut header = serde_json::to_vec(&metadata)?;
-    if header.pop() != Some(b'}') { return Err(ObservationWriteError::State("metadata object")); }
-    let file = std::fs::OpenOptions::new().write(true).create_new(true).open(path)?;
+    if header.pop() != Some(b'}') {
+        return Err(ObservationWriteError::State("metadata object"));
+    }
+    let file = std::fs::OpenOptions::new()
+        .write(true)
+        .create_new(true)
+        .open(path)?;
     let mut output = std::io::BufWriter::new(file);
     output.write_all(&header)?;
     output.write_all(b",\"physical\":[")?;
     if let Some((spool_path, mut writer)) = capture.spool {
-        writer.flush()?; drop(writer);
+        writer.flush()?;
+        drop(writer);
         let mut input = std::fs::File::open(spool_path)?;
         std::io::copy(&mut input, &mut output)?;
     } else {
         for (i, row) in capture.rows.iter().enumerate() {
-            if i != 0 { output.write_all(b",")?; }
+            if i != 0 {
+                output.write_all(b",")?;
+            }
             serde_json::to_writer(&mut output, row)?;
         }
     }
@@ -239,20 +265,44 @@
     std::fs::create_dir(&root).expect("unique test directory");
     begin_spooled(true, &root.join("rows.jsonpart")).expect("begin");
     std::thread::scope(|scope| {
-        for lane in [1,2] { scope.spawn(move || {
-            record(serde_json::json!({"kind":"worker","lane":lane}));
-            ledger(format!("lane-{lane}"), serde_json::json!({"lane":lane}));
-            count("workers");
-        }); }
+        for lane in [1, 2] {
+            scope.spawn(move || {
+                record(serde_json::json!({"kind":"worker","lane":lane}));
+                ledger(format!("lane-{lane}"), serde_json::json!({"lane":lane}));
+                count("workers");
+            });
+        }
     });
-    assert!(CAPTURE.lock().expect("capture").as_ref().expect("session").rows.is_empty());
+    assert!(
+        CAPTURE
+            .lock()
+            .expect("capture")
+            .as_ref()
+            .expect("session")
+            .rows
+            .is_empty()
+    );
     finish_to_file(&root.join("observations.json")).expect("streamed finish");
-    let value: Value = serde_json::from_slice(&std::fs::read(root.join("observations.json")).expect("bytes")).expect("JSON");
-    assert_eq!(value["physical"].as_array().expect("rows").len(),2);
-    assert_eq!(value["physical_ledgers"].as_object().expect("ledgers").len(),2);
-    assert_eq!(value["counts"]["workers"],2);
-    assert_eq!(value["overflow"],false); assert_eq!(value["counts_poisoned"],false);
-    for (i,row) in value["physical"].as_array().expect("rows").iter().enumerate() {
-        assert_eq!(row["capture"]["ordinal"],i);
-    }
-}
+    let value: Value =
+        serde_json::from_slice(&std::fs::read(root.join("observations.json")).expect("bytes"))
+            .expect("JSON");
+    assert_eq!(value["physical"].as_array().expect("rows").len(), 2);
+    assert_eq!(
+        value["physical_ledgers"]
+            .as_object()
+            .expect("ledgers")
+            .len(),
+        2
+    );
+    assert_eq!(value["counts"]["workers"], 2);
+    assert_eq!(value["overflow"], false);
+    assert_eq!(value["counts_poisoned"], false);
+    for (i, row) in value["physical"]
+        .as_array()
+        .expect("rows")
+        .iter()
+        .enumerate()
+    {
+        assert_eq!(row["capture"]["ordinal"], i);
+    }
+}
--- a/crates/openwepp-hillslope-orchestrator/src/snow_stage3_terminal_handoff.rs
+++ b/crates/openwepp-hillslope-orchestrator/src/snow_stage3_terminal_handoff.rs
@@ -607,33 +607,72 @@
     pub shared_specific_humidity: f64,
 }
 impl NativeSnowNodeReceiptV1 {
-    fn validate_for(&self, support: TimeSupport, destination: &(OfeId, TileId), snow_temperature: f64,
-        sensible: f64, vapor: f64) -> Result<(), SnowStage3HandoffError> {
+    fn validate_for(
+        &self,
+        support: TimeSupport,
+        destination: &(OfeId, TileId),
+        snow_temperature: f64,
+        sensible: f64,
+        vapor: f64,
+    ) -> Result<(), SnowStage3HandoffError> {
         self.exposure.validate()?;
-        let resistance = openwepp_vegetation::energy::neutral_resistance(self.exposure.transfer_height_m, 0.0,
-            self.exposure.roughness_m, self.exposure.roughness_m, self.exposure.wind_m_s)
-            .map_err(|_| SnowStage3HandoffError::InvalidCarrier("native snow exposure law"))?;
-        if self.exchange.exposure_sha256.as_str() != self.exposure.diagnostic_digest().as_bytes().iter().map(|byte| format!("{byte:02x}")).collect::<String>()
-            || self.exchange.heat_conductance_m_s.to_bits() != (1.0/resistance).to_bits()
-            || self.exchange.vapor_conductance_m_s.to_bits() != (1.0/resistance).to_bits() {
-            return Err(SnowStage3HandoffError::InvalidCarrier("native snow exposure custody"));
-        }
-        self.exchange.validate().map_err(|_| SnowStage3HandoffError::InvalidCarrier("native snow exchange seal"))?;
-        let t = openwepp_unit_boundary::TemperatureCelsius::try_new(self.exchange.snow_temperature_k - 273.15)
-            .map_err(|_| SnowStage3HandoffError::InvalidCarrier("native snow saturation temperature"))?;
+        let resistance = openwepp_vegetation::energy::neutral_resistance(
+            self.exposure.transfer_height_m,
+            0.0,
+            self.exposure.roughness_m,
+            self.exposure.roughness_m,
+            self.exposure.wind_m_s,
+        )
+        .map_err(|_| SnowStage3HandoffError::InvalidCarrier("native snow exposure law"))?;
+        if self.exchange.exposure_sha256.as_str()
+            != self
+                .exposure
+                .diagnostic_digest()
+                .as_bytes()
+                .iter()
+                .map(|byte| format!("{byte:02x}"))
+                .collect::<String>()
+            || self.exchange.heat_conductance_m_s.to_bits() != (1.0 / resistance).to_bits()
+            || self.exchange.vapor_conductance_m_s.to_bits() != (1.0 / resistance).to_bits()
+        {
+            return Err(SnowStage3HandoffError::InvalidCarrier(
+                "native snow exposure custody",
+            ));
+        }
+        self.exchange
+            .validate()
+            .map_err(|_| SnowStage3HandoffError::InvalidCarrier("native snow exchange seal"))?;
+        let t = openwepp_unit_boundary::TemperatureCelsius::try_new(
+            self.exchange.snow_temperature_k - 273.15,
+        )
+        .map_err(|_| {
+            SnowStage3HandoffError::InvalidCarrier("native snow saturation temperature")
+        })?;
         let e = openwepp_meteorology::surface_energy::saturation_vapor_pressure_snobal_pa(t)
-            .map_err(|_| SnowStage3HandoffError::InvalidCarrier("native snow saturation pressure"))?.as_pascals();
+            .map_err(|_| SnowStage3HandoffError::InvalidCarrier("native snow saturation pressure"))?
+            .as_pascals();
         let q = 0.622 * e / (self.exchange.pressure_pa - 0.378 * e);
-        if self.exchange.pressure_pa <= 0.378 * e || q.to_bits() != self.exchange.snow_saturation_specific_humidity.to_bits() {
-            return Err(SnowStage3HandoffError::InvalidCarrier("native snow saturation constitutive custody"));
-        }
-        let expected = self.exchange.outward_fluxes(self.shared_temperature_k, self.shared_specific_humidity)
+        if self.exchange.pressure_pa <= 0.378 * e
+            || q.to_bits() != self.exchange.snow_saturation_specific_humidity.to_bits()
+        {
+            return Err(SnowStage3HandoffError::InvalidCarrier(
+                "native snow saturation constitutive custody",
+            ));
+        }
+        let expected = self
+            .exchange
+            .outward_fluxes(self.shared_temperature_k, self.shared_specific_humidity)
             .map_err(|_| SnowStage3HandoffError::InvalidCarrier("native snow node law"))?;
-        if self.exchange.start_ns != support.start_ns().get() || self.exchange.end_ns != support.end_ns().get()
+        if self.exchange.start_ns != support.start_ns().get()
+            || self.exchange.end_ns != support.end_ns().get()
             || (&self.exchange.ofe_id, &self.exchange.tile_id) != (&destination.0, &destination.1)
             || self.exchange.snow_temperature_k.to_bits() != snow_temperature.to_bits()
-            || expected.0 != sensible || expected.1 != vapor {
-            return Err(SnowStage3HandoffError::InvalidCarrier("native snow final constitutive custody"));
+            || expected.0 != sensible
+            || expected.1 != vapor
+        {
+            return Err(SnowStage3HandoffError::InvalidCarrier(
+                "native snow final constitutive custody",
+            ));
         }
         Ok(())
     }
@@ -774,11 +813,23 @@
     pub snow_net_longwave_w_m2: f64,
 }
 
-fn validate_native_snow_node_inputs(inputs: &FinalStage3CanopyBoundaryReceiptInputs) -> Result<(), SnowStage3HandoffError> {
-    match (&inputs.native_snow_node, openwepp_land_surface_energy::snow_accuracy_policy::Policy::current().identity()) {
-        (Some(node), Some(14)) => node.validate_for(inputs.support, &inputs.destination, inputs.snow_temperature_k,
-            inputs.sensible_to_canopy_air_w_m2, inputs.vapor_to_canopy_air_kg_m2_s),
-        (None, Some(14)) | (Some(_), _) => Err(SnowStage3HandoffError::InvalidCarrier("native snow node policy/posture")),
+fn validate_native_snow_node_inputs(
+    inputs: &FinalStage3CanopyBoundaryReceiptInputs,
+) -> Result<(), SnowStage3HandoffError> {
+    match (
+        &inputs.native_snow_node,
+        openwepp_land_surface_energy::snow_accuracy_policy::Policy::current().identity(),
+    ) {
+        (Some(node), Some(14)) => node.validate_for(
+            inputs.support,
+            &inputs.destination,
+            inputs.snow_temperature_k,
+            inputs.sensible_to_canopy_air_w_m2,
+            inputs.vapor_to_canopy_air_kg_m2_s,
+        ),
+        (None, Some(14)) | (Some(_), _) => Err(SnowStage3HandoffError::InvalidCarrier(
+            "native snow node policy/posture",
+        )),
         (None, _) => Ok(()),
     }
 }
--- a/crates/openwepp-hillslope-orchestrator/src/snow_stage3_v11_adaptive_execution.rs
+++ b/crates/openwepp-hillslope-orchestrator/src/snow_stage3_v11_adaptive_execution.rs
@@ -46,14 +46,23 @@
         // The V1 clock projection is not a second physical inventory and omits
         // native ice. Observe the actual V2 owner separately without changing
         // the canonical clock owner set or inventing a digest member.
-        let native_surface_owner_canonical_json = consumer.frozen_litter_v3_resident()
+        let native_surface_owner_canonical_json = consumer
+            .frozen_litter_v3_resident()
             .map(|physical| {
                 let configuration = physical.surface_configuration();
-                let bytes = physical.surface_owner().canonical_bytes(
-                    configuration.parent(), Some(configuration))
-                    .map_err(|_| DirectSnowStage3V11AttachmentError::Identity("cycle native surface canonical bytes"))?;
-                String::from_utf8(bytes).map_err(|_| DirectSnowStage3V11AttachmentError::Identity("cycle native surface UTF-8"))
-            }).transpose()?;
+                let bytes = physical
+                    .surface_owner()
+                    .canonical_bytes(configuration.parent(), Some(configuration))
+                    .map_err(|_| {
+                        DirectSnowStage3V11AttachmentError::Identity(
+                            "cycle native surface canonical bytes",
+                        )
+                    })?;
+                String::from_utf8(bytes).map_err(|_| {
+                    DirectSnowStage3V11AttachmentError::Identity("cycle native surface UTF-8")
+                })
+            })
+            .transpose()?;
         let clock_owners = clock
             .owners()
             .iter()
@@ -2210,6 +2219,13 @@
                     forcing_receipt,
                     ending_snow_owner_bytes,
                     native_inactive_wb14_prefix,
+                    crate::snow_stage3_v11_current_context_capture::SnowFreePreChildHistoryV1 {
+                        owner_joins: &owner_joins,
+                        event_groups: &event_groups,
+                        current_stage3: &stage3,
+                        current_snow_enthalpy_material_owner: snow_enthalpy_material_owner.as_ref(),
+                        pending_terminal_parcels: &pending_terminal_parcels,
+                    },
                     deferred_native_v2_soil_custody.clone(),
                 )?;
                 // A deferred soil candidate is custody, not state. Keep it
--- a/crates/openwepp-hillslope-orchestrator/src/snow_stage3_v11_adaptive_execution_stack_helpers.rs
+++ b/crates/openwepp-hillslope-orchestrator/src/snow_stage3_v11_adaptive_execution_stack_helpers.rs
@@ -33,6 +33,7 @@
     forcing_receipt: Digest32,
     ending_snow_owner_bytes: Vec<u8>,
     native_inactive_wb14_prefix: Option<crate::direct_runtime::ValidatedNativeInactiveWb14PrefixV1>,
+    recorder_history: crate::snow_stage3_v11_current_context_capture::SnowFreePreChildHistoryV1<'_>,
     deferred_native_v2_soil_custody: Option<
         crate::v9_real_consumer_shadow::DeferredNativeV2SoilCustodyV1,
     >,
@@ -49,6 +50,7 @@
         forcing_receipt,
         ending_snow_owner_bytes,
         native_inactive_wb14_prefix,
+        recorder_history,
         deferred_native_v2_soil_custody,
         false,
     )?;
@@ -1426,8 +1428,14 @@
 
 impl PendingParentPhysicalPostureV1 {
     pub(crate) fn expected_record_source(&self, predecessor: u128, source: u128) -> Option<u128> {
-        if predecessor.checked_add(1) != Some(source) { return None; }
-        Some(if self.retained_predecessor { predecessor } else { source })
+        if predecessor.checked_add(1) != Some(source) {
+            return None;
+        }
+        Some(if self.retained_predecessor {
+            predecessor
+        } else {
+            source
+        })
     }
 }
 
@@ -1442,17 +1450,31 @@
     // The consumer is not its own physical authority: join the exact active
     // BGC and composite LSE protocols to the independent live clock owners.
     let consumer_owners = consumer.canonical_owner_state_bytes()?;
-    let bgc = consumer_owners.get("bgc").ok_or(DirectSnowStage3V11AttachmentError::Identity(
-        "pending parent consumer BGC owner missing"))?;
-    let lse = consumer.canonical_v11_lse_owner_bytes()
+    let bgc = consumer_owners
+        .get("bgc")
+        .ok_or(DirectSnowStage3V11AttachmentError::Identity(
+            "pending parent consumer BGC owner missing",
+        ))?;
+    let lse = consumer
+        .canonical_v11_lse_owner_bytes()
         .map_err(crate::v9_real_consumer_shadow::DirectV11RealConsumerError::Runtime)?;
-    for (owner_id, bytes) in [("bgc", bgc.as_slice()), ("land_surface_energy", lse.as_slice())] {
-        let mut owners = clock.owners().iter().filter(|owner| owner.owner_id() == owner_id);
-        let owner = owners.next().ok_or(DirectSnowStage3V11AttachmentError::Identity(
-            "pending parent physical clock owner missing"))?;
+    for (owner_id, bytes) in [
+        ("bgc", bgc.as_slice()),
+        ("land_surface_energy", lse.as_slice()),
+    ] {
+        let mut owners = clock
+            .owners()
+            .iter()
+            .filter(|owner| owner.owner_id() == owner_id);
+        let owner = owners
+            .next()
+            .ok_or(DirectSnowStage3V11AttachmentError::Identity(
+                "pending parent physical clock owner missing",
+            ))?;
         if owners.next().is_some() || owner.state_bytes() != bytes {
             return Err(DirectSnowStage3V11AttachmentError::Identity(
-                "pending parent consumer physical clock owner mismatch"));
+                "pending parent consumer physical clock owner mismatch",
+            ));
         }
     }
     endpoint.validate()?;
@@ -1464,32 +1486,56 @@
         || endpoint.owner_join.parent_transaction_sha256 != clock.parent_transaction_id().digest()
         || endpoint.wb14_ofe_topology != context.surface_liquid_configuration.ofe_topology
     {
-        return Err(DirectSnowStage3V11AttachmentError::Identity("pending parent physical posture endpoint"));
+        return Err(DirectSnowStage3V11AttachmentError::Identity(
+            "pending parent physical posture endpoint",
+        ));
     }
     let inactive = crate::direct_runtime::stage3_covered_native_inactive_child_custody_binding(
-        &endpoint.wb14_child_replay_bytes, &context.surface_liquid_configuration.ofe_topology,
-    ).map_err(|_| DirectSnowStage3V11AttachmentError::Identity("pending parent physical posture replay"))?;
+        &endpoint.wb14_child_replay_bytes,
+        &context.surface_liquid_configuration.ofe_topology,
+    )
+    .map_err(|_| {
+        DirectSnowStage3V11AttachmentError::Identity("pending parent physical posture replay")
+    })?;
     // validate() above authenticates either canonical inactive custody or the
     // actual WB14 child and parent replay. Require the parent discriminator.
-    if inactive.is_none() && (endpoint.wb14_parent_replay_bytes.is_none()
-        || endpoint.wb14_parent_receipt_set_sha256.is_none()) {
-        return Err(DirectSnowStage3V11AttachmentError::Identity("pending parent advanced physical replay missing"));
+    if inactive.is_none()
+        && (endpoint.wb14_parent_replay_bytes.is_none()
+            || endpoint.wb14_parent_receipt_set_sha256.is_none())
+    {
+        return Err(DirectSnowStage3V11AttachmentError::Identity(
+            "pending parent advanced physical replay missing",
+        ));
     }
     let publications = consumer.accepted_publication_supports_for_day(day_index)?;
-    let mut matches = publications.iter().copied().filter(|p|
-        p.parent_transaction_id() == clock.parent_transaction_id() && p.support() == endpoint.support);
-    let publication = matches.next().ok_or(DirectSnowStage3V11AttachmentError::Identity(
-        "pending parent physical publication missing"))?;
-    if matches.next().is_some() || publication.ending_complete_owner_set_sha256()
-        != endpoint.owner_join.ending_complete_owner_set_sha256 {
-        return Err(DirectSnowStage3V11AttachmentError::Identity("pending parent physical publication join"));
+    let mut matches = publications.iter().copied().filter(|p| {
+        p.parent_transaction_id() == clock.parent_transaction_id()
+            && p.support() == endpoint.support
+    });
+    let publication = matches
+        .next()
+        .ok_or(DirectSnowStage3V11AttachmentError::Identity(
+            "pending parent physical publication missing",
+        ))?;
+    if matches.next().is_some()
+        || publication.ending_complete_owner_set_sha256()
+            != endpoint.owner_join.ending_complete_owner_set_sha256
+    {
+        return Err(DirectSnowStage3V11AttachmentError::Identity(
+            "pending parent physical publication join",
+        ));
     }
     let chain = consumer.accepted_publication_ordered_owner_chain(publication)?;
     if !chain.contains(&endpoint.effective_ending_complete_owner_set_sha256())
-        || chain.last() != Some(&complete_owner_set_digest(clock.owners())?) {
-        return Err(DirectSnowStage3V11AttachmentError::Identity("pending parent physical publication event chain"));
-    }
-    Ok(PendingParentPhysicalPostureV1 { retained_predecessor: inactive.is_some() })
+        || chain.last() != Some(&complete_owner_set_digest(clock.owners())?)
+    {
+        return Err(DirectSnowStage3V11AttachmentError::Identity(
+            "pending parent physical publication event chain",
+        ));
+    }
+    Ok(PendingParentPhysicalPostureV1 {
+        retained_predecessor: inactive.is_some(),
+    })
 }
 
 #[cfg(test)]
@@ -1498,9 +1544,16 @@
     #[test]
     fn pending_parent_physical_posture_requires_exact_successor() {
         for retained_predecessor in [false, true] {
-            let posture = PendingParentPhysicalPostureV1 { retained_predecessor };
-            assert_eq!(posture.expected_record_source(40, 41), Some(if retained_predecessor {40} else {41}));
-            for invalid in [0, 39, 40, 42] { assert_eq!(posture.expected_record_source(40, invalid), None); }
+            let posture = PendingParentPhysicalPostureV1 {
+                retained_predecessor,
+            };
+            assert_eq!(
+                posture.expected_record_source(40, 41),
+                Some(if retained_predecessor { 40 } else { 41 })
+            );
+            for invalid in [0, 39, 40, 42] {
+                assert_eq!(posture.expected_record_source(40, invalid), None);
+            }
             assert_eq!(posture.expected_record_source(u128::MAX, 0), None);
         }
     }
@@ -1633,8 +1686,11 @@
         ));
     }
 
-    let endpoint = owner_joins.last().ok_or(DirectSnowStage3V11AttachmentError::Identity(
-        "pending parent physical endpoint missing"))?;
+    let endpoint = owner_joins
+        .last()
+        .ok_or(DirectSnowStage3V11AttachmentError::Identity(
+            "pending parent physical endpoint missing",
+        ))?;
     let physical_posture = authenticate_pending_parent_physical_posture_v1(
         context, prepared, consumer, clock, day_index, endpoint,
     )?;
@@ -1646,8 +1702,12 @@
             let poisoned = consumer.pending_parent_physical_bgc_poison_for_evidence()?;
             if authenticate_pending_parent_physical_posture_v1(
                 context, prepared, &poisoned, clock, day_index, endpoint,
-            ).is_ok() {
-                return Err(DirectSnowStage3V11AttachmentError::Identity("physical BGC poison was admitted"));
+            )
+            .is_ok()
+            {
+                return Err(DirectSnowStage3V11AttachmentError::Identity(
+                    "physical BGC poison was admitted",
+                ));
             }
             let mut checks = 1;
             for poison_kind in 0..4 {
@@ -1661,17 +1721,25 @@
                 // Singleton topology reversal is not a poison; require the
                 // actual mixed topology for this explicit evidence harness.
                 if poisoned == *endpoint {
-                    return Err(DirectSnowStage3V11AttachmentError::Identity("pending parent poison was vacuous"));
+                    return Err(DirectSnowStage3V11AttachmentError::Identity(
+                        "pending parent poison was vacuous",
+                    ));
                 }
                 if authenticate_pending_parent_physical_posture_v1(
                     context, prepared, consumer, clock, day_index, &poisoned,
-                ).is_ok() {
-                    return Err(DirectSnowStage3V11AttachmentError::Identity("pending parent replay poison was admitted"));
+                )
+                .is_ok()
+                {
+                    return Err(DirectSnowStage3V11AttachmentError::Identity(
+                        "pending parent replay poison was admitted",
+                    ));
                 }
                 checks += 1;
             }
             if consumer.canonical_owner_state_bytes()? != original {
-                return Err(DirectSnowStage3V11AttachmentError::Identity("pending parent poison mutated original"));
+                return Err(DirectSnowStage3V11AttachmentError::Identity(
+                    "pending parent poison mutated original",
+                ));
             }
             crate::snow_accuracy_observer::record(serde_json::json!({
                 "kind":"pending_parent_actual_poison_checks", "passed":checks,
--- a/crates/openwepp-hillslope-orchestrator/src/snow_stage3_v11_adaptive_frontend.rs
+++ b/crates/openwepp-hillslope-orchestrator/src/snow_stage3_v11_adaptive_frontend.rs
@@ -1,3 +1,24 @@
+#[cfg(test)]
+std::thread_local! {
+    static SNOW_FREE_SUCCESSOR_FIXTURE_AUDIT_V1:
+        std::cell::RefCell<Option<Vec<Stage3SnowFreeSuccessorReceiptV1>>> =
+        const { std::cell::RefCell::new(None) };
+}
+
+#[cfg(test)]
+pub(crate) fn begin_snow_free_successor_fixture_audit_v1() {
+    SNOW_FREE_SUCCESSOR_FIXTURE_AUDIT_V1.with(|audit| {
+        *audit.borrow_mut() = Some(Vec::new());
+    });
+}
+
+#[cfg(test)]
+pub(crate) fn take_snow_free_successor_fixture_audit_v1() -> Vec<Stage3SnowFreeSuccessorReceiptV1> {
+    SNOW_FREE_SUCCESSOR_FIXTURE_AUDIT_V1
+        .with(|audit| audit.borrow_mut().take())
+        .unwrap_or_default()
+}
+
 #[cfg(test)]
 #[derive(Clone, Copy, Debug, Eq, PartialEq)]
 pub(crate) struct AdaptiveControllerTestPolicyV1 {
@@ -539,7 +560,7 @@
         groups,
         parcels,
         _adaptive_support_receipt,
-        _snow_free_successor_receipts,
+        snow_free_successor_receipts,
     )) = execute_covered_real_v11_parent_with_evidence::<crate::hydrology::NoEvidence>(
         context,
         beginning_parent,
@@ -569,6 +590,11 @@
             receipts.push(_adaptive_support_receipt);
         }
     });
+    SNOW_FREE_SUCCESSOR_FIXTURE_AUDIT_V1.with(|audit| {
+        if audit.borrow().is_some() {
+            *audit.borrow_mut() = Some(snow_free_successor_receipts);
+        }
+    });
     Ok((
         parent, consumer, clock, finalized, stage3, receipts, groups, parcels,
     ))
--- a/crates/openwepp-hillslope-orchestrator/src/snow_stage3_v11_adaptive_production_tests.rs
+++ b/crates/openwepp-hillslope-orchestrator/src/snow_stage3_v11_adaptive_production_tests.rs
@@ -1,13 +1,13 @@
+use crate::STAGE3_V11_ADAPTIVE_MINIMUM_SUPPORT_NS;
 use crate::snow_stage3_v11_attachment::{
+    AcceptedPublicationCaptureAuditV1, AcceptedPublicationCaptureRegimeV1,
+    AdaptiveControllerTestPolicyV1, Stage3AdaptiveStepDecisionV1,
     begin_adaptive_comparison_test_audit, begin_adaptive_controller_test_audit,
     begin_adaptive_performance_test_audit, begin_terminal_batch_production_audit,
     take_accepted_publication_capture_audit, take_adaptive_comparison_test_audit,
     take_adaptive_controller_test_audit, take_adaptive_performance_test_audit,
-    take_terminal_batch_production_audit, AcceptedPublicationCaptureAuditV1,
-    AcceptedPublicationCaptureRegimeV1, AdaptiveControllerTestPolicyV1,
-    Stage3AdaptiveStepDecisionV1,
+    take_terminal_batch_production_audit,
 };
-use crate::STAGE3_V11_ADAPTIVE_MINIMUM_SUPPORT_NS;
 
 const MINIMUM_STEP_NS: u128 = STAGE3_V11_ADAPTIVE_MINIMUM_SUPPORT_NS;
 
@@ -39,8 +39,8 @@
     static CANONICAL_PARITY_CASE: std::cell::Cell<Option<crate::v9_real_consumer_shadow::v11_covered::CanonicalCoveredPhysicalParityCaseV1>> = const { std::cell::Cell::new(None) };
 }
 
-pub(super) fn canonical_parity_case_requested(
-) -> Option<crate::v9_real_consumer_shadow::v11_covered::CanonicalCoveredPhysicalParityCaseV1> {
+pub(super) fn canonical_parity_case_requested()
+-> Option<crate::v9_real_consumer_shadow::v11_covered::CanonicalCoveredPhysicalParityCaseV1> {
     CANONICAL_PARITY_CASE.with(std::cell::Cell::get)
 }
 
@@ -62,8 +62,8 @@
     FINAL_PHYSICAL_COMPARISON.with(|value| *value.borrow_mut() = Some(comparison));
 }
 
-fn take_final_physical_comparison(
-) -> crate::v9_real_consumer_shadow::AdaptiveCompleteOwnerComparisonV1 {
+fn take_final_physical_comparison()
+-> crate::v9_real_consumer_shadow::AdaptiveCompleteOwnerComparisonV1 {
     FINAL_PHYSICAL_COMPARISON
         .with(|value| value.borrow_mut().take())
         .expect("final complete-owner physical comparison")
@@ -94,6 +94,384 @@
         .expect("join adaptive production coverage")
 }
 
+struct PreChildRecorderProductionOutcomeV1 {
+    production: AdaptiveProductionOutcomeV1,
+    fixture: Option<serde_json::Value>,
+}
+
+#[derive(Clone, Copy)]
+enum PreChildRecorderPhaseV1 {
+    Covered,
+    FirstSnowFree,
+}
+
+impl PreChildRecorderPhaseV1 {
+    fn label(self) -> &'static str {
+        match self {
+            Self::Covered => "covered",
+            Self::FirstSnowFree => "first-snow-free",
+        }
+    }
+}
+
+fn pre_child_recorder_mode_label(
+    mode: crate::snow_stage3_v11_current_context_capture::PreChildRecorderTestModeV1,
+) -> &'static str {
+    use crate::snow_stage3_v11_current_context_capture::PreChildRecorderTestModeV1 as Mode;
+    match mode {
+        Mode::Disabled => "disabled",
+        Mode::ProductionNonTargetObserverEnabled => "production-non-target-observer-enabled",
+        Mode::EnabledCovered => "enabled-covered",
+        Mode::ForcedCoveredCaptureFailure => "forced-covered-capture-failure",
+        Mode::EnabledSnowFree => "enabled-snow-free",
+        Mode::ForcedSnowFreeCaptureFailure => "forced-snow-free-capture-failure",
+        Mode::ForcedSnowFreeWrongPhaseFailure => "forced-snow-free-wrong-phase-failure",
+        Mode::ForcedSnowFreeRequiredOmissionFailure => "forced-snow-free-required-omission-failure",
+    }
+}
+
+fn run_pre_child_recorder_fixture_on_large_stack(
+    mode: crate::snow_stage3_v11_current_context_capture::PreChildRecorderTestModeV1,
+) -> (
+    PreChildRecorderProductionOutcomeV1,
+    crate::snow_stage3_v11_current_context_capture::PreChildRecorderTestAuditV1,
+) {
+    use crate::v9_real_consumer_shadow::v11_covered::CanonicalCoveredPhysicalParityCaseV1 as Case;
+
+    CANONICAL_PARITY_CASE.with(|slot| slot.set(Some(Case::NativeMixedPhase)));
+    crate::snow_stage3_v11_current_context_capture::begin_pre_child_recorder_test_audit_v1(mode);
+    let native = run_native_mixed_phase_terminal_on_large_stack();
+    let outcome = PreChildRecorderProductionOutcomeV1 {
+        fixture: Some(native.fixture.canonical_comparison_payload()),
+        production: native.production,
+    };
+    let audit =
+        crate::snow_stage3_v11_current_context_capture::take_pre_child_recorder_test_audit_v1();
+    (outcome, audit)
+}
+
+fn run_with_pre_child_recorder(
+    mode: crate::snow_stage3_v11_current_context_capture::PreChildRecorderTestModeV1,
+    phase: PreChildRecorderPhaseV1,
+) -> (
+    PreChildRecorderProductionOutcomeV1,
+    crate::snow_stage3_v11_current_context_capture::PreChildRecorderTestAuditV1,
+) {
+    let phase_label = phase.label();
+    let fixture_label = "native-mixed-phase-terminal";
+    let parent_support_duration_ns = 1_800_000_000_000_u128;
+    let mode_label = pre_child_recorder_mode_label(mode);
+    eprintln!(
+        "B01_RECORDER_CONTROL_START phase={phase_label} mode={mode_label} fixture={fixture_label} parent_support_ns={parent_support_duration_ns} operation=producer-execution"
+    );
+    let result = std::thread::Builder::new()
+        .name(format!("recorder-{phase_label}-{mode_label}"))
+        .stack_size(64 * 1024 * 1024)
+        .spawn(move || run_pre_child_recorder_fixture_on_large_stack(mode))
+        .expect("spawn pre-child recorder production fixture")
+        .join()
+        .unwrap_or_else(|_| {
+            panic!(
+                "pre-child recorder control failed: phase={phase_label} mode={mode_label} fixture={fixture_label} parent_support_ns={parent_support_duration_ns} operation=producer-execution"
+            )
+        });
+    for row in &result.1.recorded_rows {
+        if row["kind"] == "b01_wb14_pre_child_current_context_capture_error_v1" {
+            eprintln!(
+                "B01_RECORDER_CAPTURE_ERROR phase={phase_label} mode={mode_label} fixture={fixture_label} operation=recorder-capture detail={}",
+                row["detail"]
+            );
+        }
+    }
+    let selected_support = result
+        .1
+        .live_inputs
+        .first()
+        .map_or_else(|| "none".to_owned(), |row| row["support"].to_string());
+    eprintln!(
+        "B01_RECORDER_CONTROL_PASS phase={phase_label} mode={mode_label} fixture={fixture_label} parent_support_ns={parent_support_duration_ns} selected_support={selected_support} operation=producer-model-complete"
+    );
+    result
+}
+
+fn assert_recorder_preserved_model_result(
+    expected: &PreChildRecorderProductionOutcomeV1,
+    actual: &PreChildRecorderProductionOutcomeV1,
+) {
+    assert_eq!(
+        expected.fixture, actual.fixture,
+        "recorder changed native fixture result"
+    );
+    let expected = &expected.production;
+    let actual = &actual.production;
+    assert_eq!(
+        serde_json::to_vec(&expected.0).expect("expected receipt bytes"),
+        serde_json::to_vec(&actual.0).expect("actual receipt bytes"),
+        "recorder changed adaptive receipt/proof work",
+    );
+    assert_eq!(expected.1, actual.1, "recorder changed physical supports");
+    assert_eq!(expected.2, actual.2, "recorder changed terminal result");
+    assert_eq!(
+        expected.4, actual.4,
+        "recorder changed complete owner state"
+    );
+    assert_eq!(
+        expected.6, actual.6,
+        "recorder changed comparison proof work"
+    );
+    let performance_topology =
+        |spans: &[crate::snow_stage3_v11_attachment::AdaptivePerformanceSpanV1]| {
+            spans
+                .iter()
+                .map(|span| (span.label, span.support_duration_ns))
+                .collect::<Vec<_>>()
+        };
+    assert_eq!(
+        performance_topology(&expected.5),
+        performance_topology(&actual.5),
+        "recorder changed controller evaluation topology",
+    );
+    let topology = |captures: &[AcceptedPublicationCaptureAuditV1]| {
+        captures
+            .iter()
+            .map(|capture| (capture.support, capture.regime))
+            .collect::<Vec<_>>()
+    };
+    assert_eq!(
+        topology(&expected.3),
+        topology(&actual.3),
+        "recorder changed publication topology",
+    );
+}
+
+type PreChildAuditV1 = crate::snow_stage3_v11_current_context_capture::PreChildRecorderTestAuditV1;
+
+fn assert_common_pre_child_row(row: &serde_json::Value, live: &serde_json::Value, phase: &str) {
+    assert_eq!(row["support"], live["support"]);
+    assert_eq!(
+        row["provisional_receipt_canonical_json"],
+        live["receipt_canonical_json"]
+    );
+    assert_eq!(row["coupled_binding"], live["binding"]);
+    assert_eq!(
+        row["provisional_constructor_inputs"]["forcing_receipt"],
+        live["forcing_receipt"]
+    );
+    for field in [
+        "parent_checkpoint_canonical_json",
+        "clock_complete_owner_canonical_bytes",
+        "parent_support",
+        "accepted_until",
+        "beginning_stage3_canonical_json",
+        "complete_owner_canonical_bytes",
+        "prepared_support_constructor_operands",
+        "native_consumer_constructor_operands",
+        "beginning_snow_enthalpy_material_owner",
+        "constraint_reduction_canonical_json",
+        "complete_owner_ledger_canonical_json",
+        "provider_cursor_canonical_json",
+        "native_lane_map_constructor_operands",
+        "native_soil_v2_constructor_operands",
+        "native_hydrology_frame_dynamic_constructor_operands",
+        "native_hydrology_constructor_inputs",
+        "surface_liquid_configuration_canonical_json",
+        "vegetation_configuration_canonical_json",
+        "lse_configuration_canonical_json",
+        "terminal_parcels_produced_unconsumed_canonical_json",
+    ] {
+        assert_eq!(row[field], live[field], "{phase} {field}");
+    }
+}
+
+fn assert_actual_covered_recorder_controls() {
+    use crate::snow_stage3_v11_current_context_capture::PreChildRecorderTestModeV1 as Mode;
+    let (covered_disabled_result, covered_disabled) =
+        run_with_pre_child_recorder(Mode::Disabled, PreChildRecorderPhaseV1::Covered);
+    assert!(covered_disabled.live_inputs.is_empty() && covered_disabled.recorded_rows.is_empty());
+    let (covered_enabled_result, covered) =
+        run_with_pre_child_recorder(Mode::EnabledCovered, PreChildRecorderPhaseV1::Covered);
+    let (covered_failure_result, covered_failure) = run_with_pre_child_recorder(
+        Mode::ForcedCoveredCaptureFailure,
+        PreChildRecorderPhaseV1::Covered,
+    );
+    assert_recorder_preserved_model_result(&covered_disabled_result, &covered_enabled_result);
+    assert_recorder_preserved_model_result(&covered_disabled_result, &covered_failure_result);
+    assert_eq!(
+        covered.live_inputs.len(),
+        1,
+        "covered live producer selection"
+    );
+    assert_eq!(covered.recorded_rows.len(), 1, "covered recorder row");
+    let covered_live = &covered.live_inputs[0];
+    let covered_row = &covered.recorded_rows[0];
+    assert_eq!(covered_row["capture_phase"], "covered");
+    assert_eq!(
+        covered_row["provisional_constructor_inputs"]["constraint_source_owner_id"],
+        "v11-snow-covered-real-consumer"
+    );
+    assert!(covered_row["prepared_support_constructor_operands"]["covered_interval"].is_object());
+    assert_common_pre_child_row(covered_row, covered_live, "covered");
+    assert_eq!(covered_failure.live_inputs.len(), 1);
+    assert_eq!(covered_failure.recorded_rows.len(), 1);
+    assert_eq!(
+        covered_failure.recorded_rows[0]["kind"],
+        "b01_wb14_pre_child_current_context_capture_error_v1"
+    );
+    assert_eq!(covered_failure.recorded_rows[0]["capture_phase"], "covered");
+}
+
+fn assert_snow_free_enabled_row(enabled: &PreChildAuditV1) {
+    let live = &enabled.live_inputs[0];
+    let row = &enabled.recorded_rows[0];
+    assert_eq!(
+        row["kind"],
+        "b01_wb14_snow_free_pre_child_current_context_v1"
+    );
+    assert_eq!(row["capture_phase"], "snow_free");
+    assert_eq!(
+        row["provisional_constructor_inputs"]["constraint_source_owner_id"],
+        "v11-real-consumer"
+    );
+    assert!(row["prepared_support_constructor_operands"]["covered_projection"].is_null());
+    assert_common_pre_child_row(row, live, "snow-free");
+    for field in [
+        "ordered_owner_joins",
+        "ordered_owner_join_support_liquid_custody_v2",
+        "ordered_event_groups",
+        "ordered_event_group_terminal_liquid_custody_v2",
+        "validated_native_inactive_wb14_prefix",
+        "current_stage3",
+        "current_snow_enthalpy_material_owner",
+        "pending_terminal_parcels",
+        "ending_snow_owner_canonical_bytes",
+        "deferred_native_v2_soil_custody",
+    ] {
+        assert_eq!(
+            row["phase_diagnostics"][field], live["phase_live"][field],
+            "{field}"
+        );
+    }
+    assert_eq!(
+        row["phase_diagnostics"]["prefix_validation_parent_support"],
+        live["parent_support"]
+    );
+    assert_eq!(
+        row["phase_diagnostics"]["prefix_validation_surface_configuration_canonical_json"],
+        live["surface_liquid_configuration_canonical_json"]
+    );
+    assert!(row["phase_diagnostics"]["validated_native_inactive_wb14_prefix"].is_object());
+    assert!(
+        row["phase_diagnostics"]["ordered_owner_joins"]
+            .as_array()
+            .is_some_and(|rows| !rows.is_empty())
+    );
+    assert!(
+        row["phase_diagnostics"]["ordered_event_groups"]
+            .as_array()
+            .is_some_and(|rows| !rows.is_empty())
+    );
+}
+
+fn assert_actual_snow_free_recorder_controls(
+    non_target_result: &PreChildRecorderProductionOutcomeV1,
+) {
+    use crate::snow_stage3_v11_current_context_capture::PreChildRecorderTestModeV1 as Mode;
+    let (disabled_result, disabled) =
+        run_with_pre_child_recorder(Mode::Disabled, PreChildRecorderPhaseV1::FirstSnowFree);
+    assert!(disabled.live_inputs.is_empty() && disabled.recorded_rows.is_empty());
+    assert_recorder_preserved_model_result(&disabled_result, non_target_result);
+    let (enabled_result, enabled) = run_with_pre_child_recorder(
+        Mode::EnabledSnowFree,
+        PreChildRecorderPhaseV1::FirstSnowFree,
+    );
+    let (failure_result, failure) = run_with_pre_child_recorder(
+        Mode::ForcedSnowFreeCaptureFailure,
+        PreChildRecorderPhaseV1::FirstSnowFree,
+    );
+    let (wrong_phase_result, wrong_phase) = run_with_pre_child_recorder(
+        Mode::ForcedSnowFreeWrongPhaseFailure,
+        PreChildRecorderPhaseV1::FirstSnowFree,
+    );
+    let (omission_result, omission) = run_with_pre_child_recorder(
+        Mode::ForcedSnowFreeRequiredOmissionFailure,
+        PreChildRecorderPhaseV1::FirstSnowFree,
+    );
+    assert_recorder_preserved_model_result(&disabled_result, &enabled_result);
+    assert_recorder_preserved_model_result(&disabled_result, &failure_result);
+    assert_recorder_preserved_model_result(&disabled_result, &wrong_phase_result);
+    assert_recorder_preserved_model_result(&disabled_result, &omission_result);
+    assert_eq!(
+        enabled.live_inputs.len(),
+        1,
+        "snow-free live producer selection"
+    );
+    assert_eq!(enabled.recorded_rows.len(), 1, "snow-free recorder row");
+    assert_snow_free_enabled_row(&enabled);
+    assert_eq!(
+        failure.live_inputs.len(),
+        1,
+        "forced-failure live selection"
+    );
+    assert_eq!(
+        failure.recorded_rows.len(),
+        1,
+        "forced-failure diagnostic row"
+    );
+    assert_eq!(
+        failure.recorded_rows[0]["kind"],
+        "b01_wb14_pre_child_current_context_capture_error_v1"
+    );
+    assert_eq!(failure.recorded_rows[0]["capture_phase"], "snow_free");
+    for rejected in [&wrong_phase, &omission] {
+        assert_eq!(rejected.live_inputs.len(), 1);
+        assert_eq!(rejected.recorded_rows.len(), 1);
+        assert_eq!(
+            rejected.recorded_rows[0]["kind"],
+            "b01_wb14_pre_child_current_context_capture_error_v1"
+        );
+        assert_eq!(rejected.recorded_rows[0]["capture_phase"], "snow_free");
+    }
+    assert!(
+        wrong_phase.recorded_rows[0]["detail"]
+            .as_str()
+            .is_some_and(|detail| detail.contains("covered"))
+    );
+    assert!(
+        omission.recorded_rows[0]["detail"]
+            .as_str()
+            .is_some_and(|detail| detail.contains("missing transformed prepared support"))
+    );
+}
+
+fn run_snow_free_non_target_control() -> PreChildRecorderProductionOutcomeV1 {
+    use crate::snow_stage3_v11_current_context_capture::PreChildRecorderTestModeV1 as Mode;
+    let (non_target_result, non_target) = run_with_pre_child_recorder(
+        Mode::ProductionNonTargetObserverEnabled,
+        PreChildRecorderPhaseV1::FirstSnowFree,
+    );
+    assert!(non_target.producer_calls > 0);
+    assert!(non_target.live_inputs.is_empty() && non_target.recorded_rows.is_empty());
+    non_target_result
+}
+
+#[test]
+fn actual_covered_producer_recorder_controls_are_independently_selectable() {
+    assert_actual_covered_recorder_controls();
+}
+
+#[test]
+fn actual_first_snow_free_producer_recorder_controls_are_independently_selectable() {
+    let non_target_result = run_snow_free_non_target_control();
+    assert_actual_snow_free_recorder_controls(&non_target_result);
+}
+
+#[test]
+fn actual_covered_and_snow_free_producers_record_phase_correct_operands_without_model_effect() {
+    let non_target_result = run_snow_free_non_target_control();
+    assert_actual_covered_recorder_controls();
+    assert_actual_snow_free_recorder_controls(&non_target_result);
+}
+
 fn run_equilibrium_complete_owner() -> AdaptiveProductionOutcomeV1 {
     run_equilibrium_complete_owner_with_policy(AdaptiveControllerTestPolicyV1::default())
 }
@@ -113,6 +491,335 @@
     Vec<crate::snow_stage3_v11_attachment::AdaptivePerformanceSpanV1>,
     Vec<crate::snow_stage3_v11_attachment::AdaptiveComparisonAuditV1>,
 );
+
+struct NativeMixedPhaseFixtureOutcomeV1 {
+    production: AdaptiveProductionOutcomeV1,
+    fixture: super::CompleteWb14CadenceFixtureResultV1,
+}
+
+fn run_native_mixed_phase_terminal_on_large_stack() -> NativeMixedPhaseFixtureOutcomeV1 {
+    SHORT_SUPPORT_REQUESTED.with(|value| value.set(false));
+    SHORT_SUPPORT_DURATION_NS.with(|value| value.set(1_800_000_000_000));
+    begin_adaptive_controller_test_audit(AdaptiveControllerTestPolicyV1::default());
+    begin_adaptive_performance_test_audit();
+    begin_adaptive_comparison_test_audit();
+    crate::v9_real_consumer_shadow::v11_covered::begin_covered_carrier_support_audit();
+    crate::v9_real_consumer_shadow::v11_covered::begin_covered_carrier_live_consumption_audit();
+    begin_terminal_batch_production_audit();
+    crate::snow_stage3_v11_attachment::begin_snow_free_successor_fixture_audit_v1();
+
+    let fixture =
+        super::execute_complete_wb14_cadence_fixture(super::CompleteWb14CadenceFixtureRequestV1 {
+            runtime_swe_m: 0.000_6,
+            initial_cold_delta_k: 0.0,
+            include_child_17: false.into(),
+            hard_boundary_ns: Some(60_000_000_000),
+            expect_dynamic_proposal: false.into(),
+            second_lane_swe_m: None,
+            terminal_event: true.into(),
+            capture_terminal_failure: false.into(),
+            solid_reappearance: false.into(),
+            production_only: true.into(),
+        })
+        .expect("native mixed-phase terminal fixture result");
+
+    let receipts = take_adaptive_controller_test_audit();
+    let carrier_supports =
+        crate::v9_real_consumer_shadow::v11_covered::take_covered_carrier_support_audit();
+    let batches = take_terminal_batch_production_audit();
+    let publication_captures = take_accepted_publication_capture_audit();
+    let performance_spans = take_adaptive_performance_test_audit();
+    let comparison_audits = take_adaptive_comparison_test_audit();
+    let _live_carrier_audit =
+        crate::v9_real_consumer_shadow::v11_covered::take_covered_carrier_live_consumption_audit();
+    let receipt = receipts
+        .last()
+        .cloned()
+        .expect("native mixed-phase adaptive production receipt");
+    let production = (
+        receipt,
+        carrier_supports,
+        batches,
+        publication_captures,
+        take_final_physical_comparison(),
+        performance_spans,
+        comparison_audits,
+    );
+    assert_native_mixed_phase_terminal_fixture(&production, &fixture);
+    NativeMixedPhaseFixtureOutcomeV1 {
+        production,
+        fixture,
+    }
+}
+
+fn assert_native_mixed_phase_terminal_fixture(
+    production: &AdaptiveProductionOutcomeV1,
+    fixture: &super::CompleteWb14CadenceFixtureResultV1,
+) {
+    assert_eq!(
+        fixture.fixture_inputs["parity_profile"],
+        "native-mixed-phase"
+    );
+    assert_eq!(
+        fixture.fixture_inputs["runtime_swe_m_bits"],
+        serde_json::json!(0.000_6_f64.to_bits())
+    );
+    assert_eq!(
+        fixture.fixture_inputs["hard_boundary_ns"],
+        serde_json::json!(60_000_000_000_u128)
+    );
+    assert_eq!(fixture.beginning_stage3_lane_count, 1);
+    assert!(fixture.beginning_native_v3_resident_present);
+    assert!(fixture.beginning_native_v4_resident_present);
+    assert!(
+        fixture
+            .beginning_complete_owner_canonical_bytes
+            .contains_key("soil_thermal")
+    );
+    assert!(
+        fixture
+            .beginning_complete_owner_canonical_bytes
+            .contains_key("land_surface_energy")
+    );
+    assert_native_mixed_phase_transition(fixture);
+    assert_eq!(
+        production.0.parent_support,
+        openwepp_coupled_time::TimeSupport::new(
+            openwepp_coupled_time::ModelTimeNs::new(0),
+            openwepp_coupled_time::ModelTimeNs::new(1_800_000_000_000)
+        )
+        .expect("native parent support")
+    );
+    assert!(!production.0.accepted_microsteps.is_empty());
+    assert!(
+        production.2.is_empty(),
+        "single-lane terminal fixture must not execute the multi-lane batch path"
+    );
+    assert_eq!(
+        fixture.ending_clock_accepted_until,
+        openwepp_coupled_time::ModelTimeNs::new(1_800_000_000_000)
+    );
+}
+
+fn assert_native_mixed_phase_transition(fixture: &super::CompleteWb14CadenceFixtureResultV1) {
+    assert_eq!(fixture.event_groups.len(), 1);
+    let group = &fixture.event_groups[0];
+    let accepted_event = group
+        .accepted_event_receipt
+        .as_ref()
+        .expect("native terminal event receipt");
+    assert_eq!(accepted_event.tick(), group.tick);
+    assert_eq!(fixture.terminal_parcels.len(), 1);
+    let parcel = &fixture.terminal_parcels[0];
+    assert_eq!(
+        parcel.posture,
+        crate::snow_stage3_v11_attachment::DirectSnowStage3V11TerminalParcelPosture::Consumed
+    );
+    assert!(
+        group
+            .produced_unconsumed_parcel_digests
+            .contains(&parcel.parcel_digest)
+    );
+    assert!(!fixture.subslabs.is_empty());
+    assert_eq!(
+        fixture.subslabs[0].support.start_ns(),
+        openwepp_coupled_time::ModelTimeNs::new(0)
+    );
+    assert!(fixture.subslabs.iter().all(|receipt| {
+        receipt.lane_receipts.len() == 1 && receipt.lane_receipts.contains_key(&1)
+    }));
+    assert!(
+        fixture
+            .subslabs
+            .windows(2)
+            .all(|pair| pair[0].support.end_ns() == pair[1].support.start_ns())
+    );
+    assert_eq!(
+        fixture
+            .subslabs
+            .last()
+            .expect("terminal native subslab")
+            .support
+            .end_ns(),
+        group.tick
+    );
+    let first_successor = fixture
+        .snow_free_successor_receipts
+        .first()
+        .expect("first snow-free successor receipt");
+    assert_eq!(first_successor.successor_ordinal, 0);
+    assert_eq!(first_successor.support.start_ns(), group.tick);
+    assert_eq!(
+        first_successor.beginning_complete_owner_set_sha256,
+        accepted_event.ending_owner_set_digest()
+    );
+    assert!(first_successor.receiver_pending);
+    assert_ne!(
+        first_successor.beginning_pending_terminal_parcel_set_sha256,
+        first_successor.ending_pending_terminal_parcel_set_sha256
+    );
+    assert!(fixture.snow_free_successor_receipts.windows(2).all(|pair| {
+        pair[0].successor_ordinal + 1 == pair[1].successor_ordinal
+            && pair[0].support.end_ns() == pair[1].support.start_ns()
+            && pair[0].ending_complete_owner_set_sha256
+                == pair[1].beginning_complete_owner_set_sha256
+            && pair[0].ending_snow_owner_sha256 == pair[1].beginning_snow_owner_sha256
+            && pair[0].ending_pending_terminal_parcel_set_sha256
+                == pair[1].beginning_pending_terminal_parcel_set_sha256
+    }));
+    assert_eq!(
+        fixture
+            .snow_free_successor_receipts
+            .last()
+            .expect("ending snow-free successor receipt")
+            .support
+            .end_ns(),
+        openwepp_coupled_time::ModelTimeNs::new(1_800_000_000_000)
+    );
+}
+
+fn adaptive_discrete_surface_kind_tag(
+    kind: crate::v9_real_consumer_shadow::AdaptiveDiscreteSurfaceKindV1,
+) -> &'static str {
+    use crate::v9_real_consumer_shadow::AdaptiveDiscreteSurfaceKindV1 as Kind;
+    match kind {
+        Kind::ActiveSet => "active-set",
+        Kind::DerivedIntegrityCache => "derived-integrity-cache",
+        Kind::ExactBinary64 => "exact-binary64",
+        Kind::Identity => "identity",
+        Kind::Membership => "membership",
+        Kind::NumericalWarmStart => "numerical-warm-start",
+        Kind::Ordering => "ordering",
+        Kind::Posture => "posture",
+        Kind::ReceiptLineage => "receipt-lineage",
+        Kind::ReceiptOrdering => "receipt-ordering",
+        Kind::Schema => "schema",
+        Kind::Topology => "topology",
+    }
+}
+
+fn adaptive_complete_owner_comparison_payload(
+    comparison: &crate::v9_real_consumer_shadow::AdaptiveCompleteOwnerComparisonV1,
+) -> serde_json::Value {
+    serde_json::json!({
+        "scalars": comparison.scalars.iter().map(|scalar| serde_json::json!({
+            "owner_id": scalar.owner_id,
+            "path": scalar.path,
+            "value_bits": scalar.value.to_bits(),
+            "contract_id": scalar.tolerance_authority.contract_id,
+            "tolerance_id": scalar.tolerance_authority.tolerance_id,
+            "absolute_tolerance_bits": scalar.absolute_tolerance.to_bits(),
+            "relative_tolerance_bits": scalar.relative_tolerance.to_bits(),
+        })).collect::<Vec<_>>(),
+        "exact_discrete_surfaces": comparison.exact_discrete_surfaces.iter().map(|surface| serde_json::json!({
+            "owner_id": surface.owner_id,
+            "path": surface.path,
+            "kind": adaptive_discrete_surface_kind_tag(surface.kind),
+            "exact_value": surface.exact_value,
+        })).collect::<Vec<_>>(),
+        "exact_discrete_sha256": comparison.exact_discrete_sha256,
+    })
+}
+
+fn native_mixed_phase_comparison_payload(
+    value: &NativeMixedPhaseFixtureOutcomeV1,
+) -> serde_json::Value {
+    let production = &value.production;
+    serde_json::json!({
+        "observer_posture": "disabled",
+        "pre_child_recorder_posture": "disabled",
+        "controller_policy": {
+            "initial_proposal_quanta": serde_json::Value::Null,
+            "growth_factor": 2,
+        },
+        "fixture": value.fixture.canonical_comparison_payload(),
+        "adaptive_receipt": production.0,
+        "carrier_supports": production.1,
+        "terminal_batches": production.2.iter().map(|batch| serde_json::json!({
+            "support": batch.support,
+            "lane_ids": batch.lane_ids,
+            "event_ticks": batch.event_ticks,
+            "ending_terminal_lanes": batch.ending_terminal_lanes,
+            "ending_surviving_lanes": batch.ending_surviving_lanes,
+            "provider_call_count": batch.provider_call_count,
+            "join_call_count": batch.join_call_count,
+            "beginning_joint_sha256": batch.beginning_joint_sha256,
+            "ending_joint_sha256": batch.ending_joint_sha256,
+        })).collect::<Vec<_>>(),
+        "publication_topology": production.3.iter().map(|capture| {
+            let regime = match capture.regime {
+                AcceptedPublicationCaptureRegimeV1::Standalone => "standalone",
+                AcceptedPublicationCaptureRegimeV1::ProvisionalClock => "provisional-clock",
+                AcceptedPublicationCaptureRegimeV1::FinalClock => "final-clock",
+            };
+            serde_json::json!({"support": capture.support, "regime": regime})
+        }).collect::<Vec<_>>(),
+        "complete_owner_comparison": adaptive_complete_owner_comparison_payload(&production.4),
+        "performance_topology": production.5.iter().map(|span| {
+            serde_json::json!({
+                "label": span.label,
+                "support_duration_ns": span.support_duration_ns,
+            })
+        }).collect::<Vec<_>>(),
+        "comparison_audits": production.6.iter().map(|audit| serde_json::json!({
+            "support": audit.support,
+            "maximum_owner_id": audit.maximum_owner_id,
+            "maximum_path": audit.maximum_path,
+            "maximum_tolerance_authority": audit.maximum_tolerance_authority,
+            "maximum_direct_value_bits": audit.maximum_direct_value.map(f64::to_bits),
+            "maximum_composed_value_bits": audit.maximum_composed_value.map(f64::to_bits),
+            "maximum_tolerance_denominator_bits": audit.maximum_tolerance_denominator.map(f64::to_bits),
+            "maximum_scaled_error_bits": audit.maximum_scaled_error.to_bits(),
+            "first_discrete_surface_kind": audit.first_discrete_surface_kind,
+            "first_discrete_surface_delta": audit.first_discrete_surface_delta,
+        })).collect::<Vec<_>>(),
+    })
+}
+
+fn native_mixed_phase_diagnostic_payload(
+    value: &NativeMixedPhaseFixtureOutcomeV1,
+) -> serde_json::Value {
+    serde_json::json!({
+        "timing_disposition": "diagnostic_fixture_mismatch_not_acceptance_oracle",
+        "legacy_default_fixture_expected_tick_ns": 540_000_000_000_u128,
+        "native_fixture_observed_tick_ns": value.fixture.event_groups[0].tick.get(),
+        "performance_elapsed_ns": value.production.5.iter().map(|span| {
+            span.elapsed.as_nanos()
+        }).collect::<Vec<_>>(),
+        "publication_elapsed_ns": value.production.3.iter().map(|capture| serde_json::json!({
+            "projection": capture.projection_elapsed.as_nanos(),
+            "initial_seal": capture.initial_seal_elapsed.as_nanos(),
+            "validation": capture.validation_elapsed.as_nanos(),
+            "total": capture.total_elapsed.as_nanos(),
+        })).collect::<Vec<_>>(),
+    })
+}
+
+#[test]
+fn native_mixed_phase_fixture_characterization_payload() {
+    let value = std::thread::Builder::new()
+        .name("native-mixed-phase-characterization".to_owned())
+        .stack_size(64 * 1024 * 1024)
+        .spawn(|| {
+            use crate::v9_real_consumer_shadow::v11_covered::
+                CanonicalCoveredPhysicalParityCaseV1 as Case;
+            CANONICAL_PARITY_CASE.with(|slot| slot.set(Some(Case::NativeMixedPhase)));
+            run_native_mixed_phase_terminal_on_large_stack()
+        })
+        .expect("spawn native mixed-phase characterization")
+        .join()
+        .expect("join native mixed-phase characterization");
+    println!(
+        "B01_NATIVE_COMPARISON_PAYLOAD {}",
+        serde_json::to_string(&native_mixed_phase_comparison_payload(&value))
+            .expect("native comparison payload")
+    );
+    println!(
+        "B01_NATIVE_DIAGNOSTIC {}",
+        serde_json::to_string(&native_mixed_phase_diagnostic_payload(&value))
+            .expect("native diagnostic payload")
+    );
+}
 
 fn run_equilibrium_complete_owner_for_duration(
     duration_ns: u128,
@@ -194,7 +901,8 @@
     crate::v9_real_consumer_shadow::CoveredNativePhysicalPathAuditV1,
     crate::v9_real_consumer_shadow::v11_covered::CanonicalCoveredMapRoleAuditV1,
 ) {
-    let native_context_assertions = crate::v9_real_consumer_shadow::v11_covered::native_context_assertions_requested_for_test();
+    let native_context_assertions =
+        crate::v9_real_consumer_shadow::v11_covered::native_context_assertions_requested_for_test();
     std::thread::Builder::new()
         .name("canonical-covered-physical-prefix-capture".to_owned())
         .stack_size(64 * 1024 * 1024)
@@ -550,8 +1258,8 @@
         .expect("join adaptive transient production coverage")
 }
 
-fn run_full_duration_complete_owner_qualification(
-) -> crate::snow_stage3_v11_attachment::Stage3AdaptiveSupportReceiptV1 {
+fn run_full_duration_complete_owner_qualification()
+-> crate::snow_stage3_v11_attachment::Stage3AdaptiveSupportReceiptV1 {
     std::thread::Builder::new()
         .name("adaptive-full-parent-qualification".to_owned())
         .stack_size(64 * 1024 * 1024)
@@ -717,12 +1425,16 @@
         comparison_audits,
     ) = run_equilibrium_complete_owner();
     assert!(comparison_audits.is_empty());
-    assert!(performance_spans
-        .iter()
-        .any(|span| span.label == "complete_owner_direct_trial"));
-    assert!(!performance_spans
-        .iter()
-        .any(|span| span.label == "complete_owner_composed_trial"));
+    assert!(
+        performance_spans
+            .iter()
+            .any(|span| span.label == "complete_owner_direct_trial")
+    );
+    assert!(
+        !performance_spans
+            .iter()
+            .any(|span| span.label == "complete_owner_composed_trial")
+    );
     receipt
         .validate()
         .expect("stable adaptive receipt validates");
@@ -917,8 +1629,13 @@
 }
 
 pub(crate) fn run_equilibrium_with_canonical_covered_poison_audit(
-    poison: Option<crate::v9_real_consumer_shadow::v11_covered::CanonicalCoveredFinalConvergencePoisonV1>,
-) -> (bool, crate::v9_real_consumer_shadow::v11_covered::CanonicalCoveredMapRoleAuditV1) {
+    poison: Option<
+        crate::v9_real_consumer_shadow::v11_covered::CanonicalCoveredFinalConvergencePoisonV1,
+    >,
+) -> (
+    bool,
+    crate::v9_real_consumer_shadow::v11_covered::CanonicalCoveredMapRoleAuditV1,
+) {
     std::thread::Builder::new()
         .name("canonical-covered-role-audit".to_owned())
         .stack_size(64 * 1024 * 1024)
@@ -1617,9 +2334,11 @@
             .map(|(ofe_id, _)| ofe_id)
             .collect::<std::collections::BTreeSet<_>>();
         assert!(!lower_boundary_ofes.is_empty());
-        assert!(lower_boundary_ofes
-            .iter()
-            .all(|ofe_id| topology.contains(ofe_id)));
+        assert!(
+            lower_boundary_ofes
+                .iter()
+                .all(|ofe_id| topology.contains(ofe_id))
+        );
         let binding = crate::direct_runtime::stage3_covered_native_inactive_child_custody_binding(
             &endpoint.wb14_child_replay_bytes,
             &topology,
@@ -1918,17 +2637,23 @@
             .rejected_candidate_count
             >= 1
     );
-    assert!(receipt.accepted_microsteps.iter().any(|accepted| accepted
-        .context
-        .step_support
-        .duration_ns()
-        == MINIMUM_STEP_NS));
-    assert!(carrier_supports
-        .iter()
-        .all(|support| support.duration_ns() >= MINIMUM_STEP_NS));
-    assert!(comparison_audits
-        .iter()
-        .any(|audit| audit.maximum_scaled_error > 1.0));
+    assert!(
+        receipt.accepted_microsteps.iter().any(|accepted| accepted
+            .context
+            .step_support
+            .duration_ns()
+            == MINIMUM_STEP_NS)
+    );
+    assert!(
+        carrier_supports
+            .iter()
+            .all(|support| support.duration_ns() >= MINIMUM_STEP_NS)
+    );
+    assert!(
+        comparison_audits
+            .iter()
+            .any(|audit| audit.maximum_scaled_error > 1.0)
+    );
 }
 
 #[test]
@@ -2104,9 +2829,11 @@
     assert!(batches.iter().any(|batch| {
         batch.ending_terminal_lanes == vec![1] && batch.ending_surviving_lanes == vec![2]
     }));
-    assert!(carrier_supports
-        .iter()
-        .all(|support| support.duration_ns() >= MINIMUM_STEP_NS));
+    assert!(
+        carrier_supports
+            .iter()
+            .all(|support| support.duration_ns() >= MINIMUM_STEP_NS)
+    );
 }
 
 #[test]
@@ -2114,8 +2841,8 @@
 #[ignore = "contract-first expected red: retained 52-map terminal-parent validation audit is not implemented"]
 fn carrier_parent_static_and_same_map_validation_once_has_authentic_1_52_52_counts() {
     use crate::v9_real_consumer_shadow::v11_covered::{
-        begin_carrier_parent_validation_once_audit_v1,
-        take_carrier_parent_validation_once_audit_v1, CarrierValidationCallStageV1 as Stage,
+        CarrierValidationCallStageV1 as Stage, begin_carrier_parent_validation_once_audit_v1,
+        take_carrier_parent_validation_once_audit_v1,
     };
 
     let (outcome, audit) = std::thread::Builder::new()
@@ -2185,9 +2912,8 @@
 #[ignore = "contract-first expected red: forced-full versus admitted validation oracle is not implemented"]
 fn carrier_validation_once_is_bitwise_equal_for_every_role_path_and_regime() {
     use crate::v9_real_consumer_shadow::v11_covered::{
-        carrier_parent_validation_once_parity_for_test,
         CanonicalCoveredPhysicalParityCaseV1 as Case, CarrierValidationMapPathV1 as Path,
-        CarrierValidationRoleV1 as Role,
+        CarrierValidationRoleV1 as Role, carrier_parent_validation_once_parity_for_test,
     };
 
     for case in [
@@ -2279,9 +3005,9 @@
 #[ignore = "contract-first expected red: validation-once poison injection is not implemented"]
 fn carrier_validation_once_poisons_keep_first_error_no_fallback_and_rollback() {
     use crate::v9_real_consumer_shadow::v11_covered::{
+        CarrierParentValidationOncePoisonV1 as Poison,
         carrier_parent_validation_once_competing_poisons_for_test,
         carrier_parent_validation_once_poison_for_test,
-        CarrierParentValidationOncePoisonV1 as Poison,
     };
 
     for poison in [
--- a/crates/openwepp-hillslope-orchestrator/src/snow_stage3_v11_attachment.rs
+++ b/crates/openwepp-hillslope-orchestrator/src/snow_stage3_v11_attachment.rs
@@ -868,6 +868,16 @@
     SolidPrecipitationPending,
 }
 
+pub(crate) type DirectSnowStage3V11SnowFreeCapturePartsV1<'a> = (
+    &'a BTreeMap<u32, DirectActiveSnowPartitionInputs>,
+    &'a BTreeMap<u32, DirectSnowStage3SupportInput>,
+    &'a DirectV9ShadowIntervalInput,
+    &'a [ModelTimeNs],
+    &'a BTreeMap<u32, Vec<PreparedStage3V11SupportIdentityV1>>,
+    &'a BTreeMap<(OfeId, TileId), SealedStage3TileBoundaryForcingV1>,
+    &'a BTreeSet<(OfeId, TileId)>,
+);
+
 include!("snow_stage3_v11_precipitation.rs");
 include!("snow_stage3_v11_snow_soil_heat.rs");
 include!("snow_stage3_v11_terminal_chronology.rs");
@@ -999,6 +1009,31 @@
             self.covered_v11_interval
                 .as_ref()
                 .ok_or_else(|| "capture covered support missing V11 interval".to_owned())?,
+            &self.hard_boundaries,
+            &self.support_identity_by_lane,
+            &self.snow_surface_forcing_by_destination,
+            &self.open_snow_destination_requests,
+        ))
+    }
+
+    /// Immutable diagnostic view of the transformed snow-free successor.
+    /// Unlike `diagnostic_capture_parts_v1`, this requires the covered
+    /// projection to be genuinely absent and therefore cannot let a recorder
+    /// silently substitute covered forcing for the successor's live inputs.
+    pub(crate) fn diagnostic_snow_free_capture_parts_v1(
+        &self,
+    ) -> Result<DirectSnowStage3V11SnowFreeCapturePartsV1<'_>, String> {
+        if self.covered_v11_interval.is_some()
+            || !self.snow_surface_forcing_by_destination.is_empty()
+            || !self.open_snow_destination_requests.is_empty()
+            || !self.atmospheric_receipt_by_destination.is_empty()
+        {
+            return Err("capture snow-free successor retained covered projection".to_owned());
+        }
+        Ok((
+            &self.snow_inputs_by_lane,
+            &self.support_forcing_by_lane,
+            &self.v11_interval,
             &self.hard_boundaries,
             &self.support_identity_by_lane,
             &self.snow_surface_forcing_by_destination,
@@ -1849,6 +1884,28 @@
         Ok(self)
     }
 
+    /// Private diagnostic reconstruction through the same support projection
+    /// used by the adaptive frontend. Does not execute the successor.
+    #[cfg(all(test, feature = "persisted-restart-v1"))]
+    pub(crate) fn diagnostic_snow_free_successor_v1(
+        &self,
+        support: TimeSupport,
+        ordinal: u32,
+        pending: &BTreeMap<Digest32, DirectSnowStage3V11TerminalParcel>,
+    ) -> Result<Self, String> {
+        let successor = self
+            .coupled_subslab(support, ordinal)
+            .and_then(Self::snow_free_successor)
+            .map_err(|error| error.to_string())?;
+        if pending.is_empty() {
+            Ok(successor)
+        } else {
+            successor
+                .with_terminal_receiver_parcels(pending)
+                .map_err(|error| error.to_string())
+        }
+    }
+
     fn snow_free_successor(mut self) -> Result<Self, DirectSnowStage3V11AttachmentError> {
         // The snow-free successor terminal V4 owner remains the authoritative
         // hydrology state; this projection only removes snow-boundary forcing.
--- a/crates/openwepp-hillslope-orchestrator/src/snow_stage3_v11_attachment_runtime.rs
+++ b/crates/openwepp-hillslope-orchestrator/src/snow_stage3_v11_attachment_runtime.rs
@@ -1178,6 +1178,15 @@
                         forcing_receipt,
                         ending_snow_owner_bytes,
                         None,
+                        crate::snow_stage3_v11_current_context_capture::SnowFreePreChildHistoryV1 {
+                            owner_joins: &coupled_subslabs,
+                            event_groups: &terminal_event_groups,
+                            current_stage3: &candidate.stage3_by_lane,
+                            current_snow_enthalpy_material_owner: candidate
+                                .snow_enthalpy_material_owner
+                                .as_ref(),
+                            pending_terminal_parcels: &candidate.terminal_parcels,
+                        },
                         None,
                         true,
                     )?;
--- a/crates/openwepp-hillslope-orchestrator/src/snow_stage3_v11_constructor_inverse.rs
+++ b/crates/openwepp-hillslope-orchestrator/src/snow_stage3_v11_constructor_inverse.rs
@@ -0,0 +1,972 @@
+#![cfg(test)]
+//! Private field-complete inverse of the diagnostic constructor capture.
+//! No solver or process transition is evaluated here. The native constructor
+//! remains the authority for installing the reconstructed operands.
+use serde_json::Value;
+
+trait Decode: Sized {
+    fn decode(value: &Value) -> Result<Self, String>;
+}
+fn field<T: Decode>(value: &Value, name: &str) -> Result<T, String> {
+    T::decode(
+        value
+            .get(name)
+            .ok_or_else(|| format!("missing constructor field {name}"))?,
+    )
+    .map_err(|error| format!("{name}: {error}"))
+}
+macro_rules! scalar {
+    ($($ty:ty),* $(,)?) => {$(impl Decode for $ty {
+        fn decode(value: &Value) -> Result<Self, String> {
+            serde_json::from_value(value.clone()).map_err(|error| error.to_string())
+        }
+    })*};
+}
+scalar!(
+    f64,
+    bool,
+    u8,
+    u16,
+    u32,
+    u64,
+    usize,
+    i8,
+    i16,
+    i32,
+    i64,
+    String,
+    (f64, f64, f64)
+);
+impl<T: Decode> Decode for Option<T> {
+    fn decode(value: &Value) -> Result<Self, String> {
+        if value.is_null() {
+            Ok(None)
+        } else {
+            T::decode(value).map(Some)
+        }
+    }
+}
+impl<T: Decode> Decode for Box<T> {
+    fn decode(value: &Value) -> Result<Self, String> {
+        T::decode(value).map(Box::new)
+    }
+}
+impl<T: Decode> Decode for Vec<T> {
+    fn decode(value: &Value) -> Result<Self, String> {
+        value
+            .as_array()
+            .ok_or("constructor array required")?
+            .iter()
+            .map(T::decode)
+            .collect()
+    }
+}
+impl<T: Decode, const N: usize> Decode for [T; N] {
+    fn decode(value: &Value) -> Result<Self, String> {
+        Vec::<T>::decode(value)?
+            .try_into()
+            .map_err(|v: Vec<T>| format!("constructor array length {}, expected {N}", v.len()))
+    }
+}
+impl Decode for openwepp_unit_boundary::TemperatureCelsius {
+    fn decode(value: &Value) -> Result<Self, String> {
+        Self::try_new(f64::decode(value)?).map_err(|error| error.to_string())
+    }
+}
+macro_rules! captured_struct {
+    ($ty:path { $($name:ident),* $(,)? }) => {
+        impl Decode for $ty {
+            fn decode(value: &Value) -> Result<Self, String> {
+                Ok(Self { $($name: field(value, stringify!($name))?),* })
+            }
+        }
+    };
+}
+macro_rules! captured_enum {
+    ($ty:path { $($variant:ident),* $(,)? }) => {
+        impl Decode for $ty {
+            fn decode(value: &Value) -> Result<Self, String> {
+                $(if serde_json::to_value(Self::$variant).map_err(|error| error.to_string())? == *value {
+                    return Ok(Self::$variant);
+                })*
+                Err(format!("unknown {} captured variant", stringify!($ty)))
+            }
+        }
+    };
+}
+macro_rules! active_context {
+    ($ty:path) => {
+        impl Decode for $ty {
+            fn decode(value: &Value) -> Result<Self, String> {
+                match value.as_str() {
+                    Some("inactive") => return Ok(Self::Inactive),
+                    Some("missing") => return Ok(Self::Missing),
+                    Some("ambiguous") => return Ok(Self::Ambiguous),
+                    _ => {}
+                }
+                if let Some(fields) = value.get("annual_or_fallow") {
+                    return Ok(Self::AnnualOrFallow {
+                        active_slot_index: field(fields, "active_slot_index")?,
+                        active_crop_slot_index: field(fields, "active_crop_slot_index")?,
+                        runtime_day_of_year: field(fields, "runtime_day_of_year")?,
+                    });
+                }
+                if let Some(fields) = value.get("perennial") {
+                    return Ok(Self::Perennial {
+                        active_slot_index: field(fields, "active_slot_index")?,
+                        active_crop_slot_index: field(fields, "active_crop_slot_index")?,
+                        runtime_day_of_year: field(fields, "runtime_day_of_year")?,
+                    });
+                }
+                Err(format!("unknown {} captured context", stringify!($ty)))
+            }
+        }
+    };
+}
+impl Decode for crate::DirectSnowMassTransitionLedgers {
+    fn decode(value: &Value) -> Result<Self, String> {
+        Self::try_from_parts(
+            field(value, "solid_to_liquid")?,
+            field(value, "liquid_disposition")?,
+            field(value, "stage3_outcome")?,
+        )
+        .map_err(|error| error.to_string())
+    }
+}
+// Source fields: direct_runtime/00_core_frames.rs:508
+captured_struct!(crate::direct_runtime::DirectDayConstructorInputs {
+    forcing,
+    normalization_inputs,
+    interception_m,
+    storage_bounds_inputs,
+    decomposition_inputs,
+    residue_partition_inputs,
+    annual_growth_inputs,
+    perennial_growth_inputs,
+    storage_input_inputs,
+    liquid_input_inputs,
+    runon_carry_inputs,
+    infiltration_depression_inputs,
+    saturation_addback_inputs,
+    runoff_partition_inputs,
+    percolation_inputs,
+    subsurface_compute_inputs,
+    deep_seepage_inputs,
+    subsurface_loss_inputs,
+    evapotranspiration_compute_inputs,
+    evapotranspiration_inputs,
+    snow_coupling_inputs,
+    storage_reconciliation_inputs,
+    hydrology_projection_inputs,
+    erosion_inputs,
+    frost_storage_liquid_delta_m,
+    winter_frost_compute_inputs,
+    winter_frost_outcome,
+    frost_layer_carry_projection,
+    snow_runtime_carry,
+    frost_runtime_carry
+});
+// Source fields: direct_runtime/02_state_reports.rs:57
+captured_struct!(crate::direct_runtime::DirectDayForcing {
+    precipitation_m,
+    effective_temperature_c
+});
+// Source fields: direct_runtime/decomposition.rs:389
+captured_enum!(crate::direct_runtime::DirectDecompositionAction {
+    None,
+    Herbicide,
+    Burn,
+    Silage,
+    Cut,
+    Remove,
+    Grazing
+});
+// Source fields: direct_runtime/decomposition.rs:358
+active_context!(crate::direct_runtime::DirectDecompositionActiveContext);
+// Source fields: direct_runtime/decomposition.rs:407
+captured_struct!(crate::direct_runtime::DirectDecompositionInputs {
+    active_context,
+    active_action,
+    residue_type_selector,
+    surface_residue_seed_kg_m2,
+    interrill_ground_seed_kg_m2,
+    rill_ground_seed_kg_m2,
+    residue_cover_factor,
+    root_residue_seed_kg_m2,
+    surface_litter_input_kg_m2,
+    residue_depth_conversion_m_per_kg_m2,
+    temperature_max_c,
+    temperature_min_c,
+    precipitation_m,
+    water_stress_fraction,
+    surface_decomposition_rate,
+    root_decomposition_rate,
+    burn_surface_fraction,
+    remove_surface_fraction,
+    cut_transfer_fraction,
+    grazing_digest_fraction
+});
+// Source fields: direct_runtime/storage.rs:1163
+captured_struct!(crate::direct_runtime::DirectDeepSeepageInputs {
+    deep_seepage_handoff_m
+});
+// Source fields: direct_runtime/erosion.rs:157
+captured_struct!(crate::direct_runtime::DirectErod13Inputs {
+    ie_m_s,
+    te_s,
+    fs,
+    ft,
+    taufe_pa,
+    q_m2_s,
+    g_kg_s_m,
+    di_kg_s_m2,
+    beta,
+    vf_m_s,
+    dgdx_kg_s_m2,
+    cntlen_m,
+    kr_s_m,
+    kradjf,
+    tcadjf,
+    shrsol_pa,
+    tcend_kg_s_m,
+    shcrit_pa,
+    detinr_kg_s_m2,
+    effdrr_m,
+    effdrn_m,
+    veleff_m_s,
+    pkro_m3_s,
+    tc_k,
+    tc_m,
+    q_runoff_m,
+    peakro_m_s,
+    watdur_s
+});
+// Source fields: direct_runtime/erosion.rs:144
+captured_enum!(
+    crate::direct_runtime::DirectErosionHydrographShapeAuthority {
+        Dc01SourceShape,
+        RoutedHydrograph
+    }
+);
+// Source fields: direct_runtime/erosion.rs:90
+captured_struct!(crate::direct_runtime::DirectErosionInputs {
+    wave1_enabled,
+    wave1,
+    wave1_continuity,
+    wave1_operand_seed,
+    hydrograph_shape_authority,
+    routed_hydrograph_runoff_fraction
+});
+// Source fields: direct_runtime/evapotranspiration.rs:658
+captured_struct!(
+    crate::direct_runtime::DirectEvapotranspirationComputeInputs {
+        et_demand_m,
+        leaf_area_index,
+        canopy_height_m,
+        canopy_cover_fraction,
+        residue_interception_m,
+        same_pass_infiltration_m,
+        outside_water_depth_m,
+        root_depth_m,
+        plant_tolerance,
+        growth_context_required,
+        stage_state,
+        pmet,
+        pmet_compute
+    }
+);
+// Source fields: direct_runtime/storage.rs:1291
+captured_struct!(crate::direct_runtime::DirectEvapotranspirationInputs {
+    evapotranspiration_handoff_m
+});
+// Source fields: direct_runtime/evapotranspiration.rs:634
+captured_struct!(
+    crate::direct_runtime::DirectEvapotranspirationPmetComputeInputs {
+        runtime_day_of_year,
+        radiation_ly,
+        wind_m_s,
+        dew_point_c,
+        temperature_max_c,
+        temperature_min_c,
+        latitude_degrees,
+        elevation_m,
+        kcb,
+        rawp,
+        canopy_height_m,
+        radpot_ly,
+        solthk_m
+    }
+);
+// Source fields: direct_runtime/evapotranspiration.rs:620
+captured_struct!(crate::direct_runtime::DirectEvapotranspirationPmetInputs {
+    soil_evaporation_m,
+    plant_transpiration_m,
+    soil_evaporation_storage_return_m
+});
+// Source fields: direct_runtime/evapotranspiration.rs:605
+captured_struct!(crate::direct_runtime::DirectEvapotranspirationStageState {
+    s1_m,
+    s2_m,
+    threshold_m,
+    counter
+});
+// Source fields: hydrology/03_kernel_support_00_support_helpers.rs:1140
+captured_struct!(crate::hydrology::DirectFrostControlInputs {
+    frost_file_present,
+    wint_red_enabled,
+    fine_top_count,
+    fine_bot_count,
+    ksnowf,
+    kresf,
+    ksoilf,
+    kfactor1,
+    kfactor2,
+    kfactor3,
+    landuse_class_proxy
+});
+// Source fields: direct_runtime/00_core_frames.rs:312
+captured_struct!(crate::direct_runtime::DirectFrostFineLayerCarry {
+    layer_index,
+    fine_index,
+    fgfrst,
+    slfsd_m,
+    slsic_m,
+    slsw_theta,
+    sltime_s
+});
+// Source fields: hydrology/03_kernel_support_00_support_helpers.rs:1350
+captured_struct!(crate::hydrology::DirectFrostFineLayerProjection {
+    layer_index,
+    fine_index,
+    fgfrst,
+    slfsd_m,
+    slsic_m,
+    slsw_theta,
+    sltime_s
+});
+// Source fields: hydrology/03_kernel_support_00_support_helpers.rs:1115
+captured_struct!(crate::hydrology::DirectFrostHourlyForcing {
+    radiation_mj_m2,
+    air_temperature_c,
+    cloud_fraction
+});
+// Source fields: direct_runtime/01_publication.rs:108
+captured_struct!(crate::direct_runtime::DirectFrostLayerCarryProjection {
+    layer_index,
+    fine_layer_count,
+    fine_layer_thickness_m
+});
+// Source fields: hydrology/03_kernel_support_00_support_helpers.rs:1316
+captured_struct!(crate::hydrology::DirectFrostLayerProjection {
+    layer_index,
+    theta_after_m,
+    frozen_depth_m,
+    frozen_water_m
+});
+// Source fields: direct_runtime/00_core_frames.rs:293
+captured_struct!(crate::direct_runtime::DirectFrostLayerShadowCarry {
+    layer_index,
+    st_m,
+    soil_water_m,
+    frozen_depth_m,
+    frozen_water_m,
+    soilf_m,
+    yst_m,
+    nwfrzz_m
+});
+// Source fields: hydrology/03_kernel_support_00_support_helpers.rs:1331
+captured_struct!(crate::hydrology::DirectFrostLayerShadowProjection {
+    layer_index,
+    st_m,
+    soil_water_m,
+    frozen_depth_m,
+    frozen_water_m,
+    soilf_m,
+    yst_m,
+    nwfrzz_m
+});
+// Source fields: direct_runtime/00_core_frames.rs:252
+captured_struct!(crate::direct_runtime::DirectFrostRuntimeCarry {
+    active_frost_coupling,
+    dfrost_m,
+    dthaw_m,
+    nft,
+    ws_frz_m,
+    infcap_frz_m_s,
+    frwatc_soil_water_before_m,
+    frwatc_soil_water_after_m,
+    frwatc_frozen_water_before_m,
+    frwatc_frozen_water_after_m,
+    frwatc_freeze_debit_m,
+    frwatc_thaw_credit_m,
+    frwatc_net_liquid_delta_m,
+    frdp_m,
+    thdp_m,
+    tfrdp_m,
+    tthawd_m,
+    fgthwd_flag,
+    total_fine_layer_count,
+    conductivity_tilled_w_m_k,
+    conductivity_untilled_w_m_k,
+    conductivity_residue_w_m_k,
+    shadow_total_water_before_m,
+    shadow_total_water_after_m,
+    shadow_wb_delta_m,
+    shadow_frwatc_residual_m,
+    watpdg_m,
+    watbtm_m,
+    layer_shadows,
+    fine_layers
+});
+// Source fields: hydrology/03_kernel_support_00_support_helpers.rs:1162
+captured_struct!(crate::hydrology::DirectFrostThermalInputs {
+    snow_depth_m,
+    snow_density_kg_m3,
+    residue_depth_m,
+    wind_m_s,
+    albedo,
+    canopy_height_m,
+    random_roughness_m,
+    day_of_year,
+    seasonal_temperature_curve
+});
+// Source fields: direct_runtime/growth.rs:317
+captured_enum!(crate::direct_runtime::DirectGrowthAction {
+    None,
+    PrePlantSkip,
+    PlantingReset,
+    HarvestReset,
+    StopReset,
+    Cut,
+    Grazing,
+    TypedStateOverride
+});
+// Source fields: direct_runtime/growth.rs:260
+active_context!(crate::direct_runtime::DirectGrowthActiveContext);
+// Source fields: direct_runtime/growth.rs:415
+captured_struct!(crate::direct_runtime::DirectGrowthInputs {
+    active_context,
+    active_action,
+    state_before,
+    planting_day,
+    harvest_day,
+    stop_day,
+    water_stress,
+    temperature_max_c,
+    temperature_min_c,
+    radiation_mj_m2,
+    monthly_temperature_max_c,
+    monthly_temperature_min_c,
+    soil_depth_m,
+    btemp,
+    otemp,
+    gddmax,
+    dlai,
+    dropfc,
+    decfct,
+    spriod,
+    bb,
+    bbb,
+    hmax,
+    beinp,
+    extnct,
+    hi,
+    xmxlai,
+    rsr,
+    rtmmax,
+    rdmax,
+    et_demand_m,
+    residue_interception_m,
+    plant_tolerance
+});
+// Source fields: direct_runtime/growth.rs:361
+captured_struct!(crate::direct_runtime::DirectGrowthStateSurface {
+    sumgdd,
+    live_biomass_kg_m2,
+    interception_live_biomass_kg_m2,
+    canopy_height_m,
+    canopy_cover_fraction,
+    leaf_area_index,
+    root_mass_kg_m2,
+    root_depth_m,
+    harvest_index
+});
+// Source fields: direct_runtime/projection.rs:321
+captured_struct!(crate::direct_runtime::DirectHydrologyProjectionInputs {
+    aggregate_storage_tolerance_m,
+    snow_water_m,
+    frozen_soil_water_m,
+    frost_depth_m,
+    profile_depth_m,
+    profile_porosity_cap_m,
+    profile_field_capacity_m,
+    profile_wilting_point_m
+});
+// Source fields: direct_runtime/runoff_result_types.rs:145
+captured_struct!(crate::direct_runtime::DirectInfiltrationDepressionInputs {
+    cumulative_infiltration_handoff_m,
+    depression_storage_delta_handoff_m,
+    producer_inputs
+});
+// Source fields: direct_runtime/runoff_result_types.rs:9
+captured_struct!(crate::direct_runtime::DirectLiquidInputInputs {
+    liquid_input_handoff_m
+});
+// Source fields: direct_runtime/normalization.rs:173
+captured_struct!(crate::direct_runtime::DirectNormalizationInputs {
+    precipitation_m,
+    effective_temperature_c,
+    storage_initial_m,
+    surface_transfer_m,
+    lateral_transfer_m,
+    upstream_flow_m,
+    subsurface_input_m
+});
+// Source fields: direct_runtime/subsurface.rs:784
+captured_struct!(crate::direct_runtime::DirectPercolationInputs {
+    soil_water_initial_m,
+    reconcile_legacy_soil_water_from_layers,
+    same_pass_infiltration_m,
+    same_pass_infiltration_lineage,
+    tillage_depth_m,
+    lane_substeps,
+    restrictive_layer_enabled,
+    restrictive_layer_conductivity_m_s,
+    restrictive_layer_thickness_m,
+    layers
+});
+// Source fields: direct_runtime/01_publication.rs:3
+captured_struct!(crate::direct_runtime::DirectPublicationFrame {
+    runoff_m,
+    infiltration_m,
+    evapotranspiration_m,
+    drainage_m,
+    lateral_flow_m
+});
+// Source fields: direct_runtime/decomposition.rs:842
+captured_struct!(crate::direct_runtime::DirectResiduePartitionInputs {
+    standing_residue_kg_m2,
+    flat_residue_offset_kg_m2,
+    buried_residue_kg_m2,
+    cover_fraction,
+    rescov_interrill_weight
+});
+// Source fields: direct_runtime/runoff_result_types.rs:476
+captured_struct!(crate::direct_runtime::DirectRunoffPartitionInputs {
+    liquid_input_m,
+    runon_input_m,
+    cumulative_infiltration_m,
+    depression_storage_delta_m,
+    surface_saturation_runoff_m,
+    frost_retained_local_liquid_m,
+    frost_preprojected_local_liquid_m
+});
+// Source fields: direct_runtime/runoff_result_types.rs:73
+captured_struct!(crate::direct_runtime::DirectRunonCarryInputs {
+    surface_runon_handoff_m,
+    subsurface_carry_handoff_m
+});
+// Source fields: direct_runtime/runoff_result_types.rs:412
+captured_struct!(crate::direct_runtime::DirectSaturationAddbackInputs {
+    surface_saturation_runoff_handoff_m
+});
+// Source fields: direct_runtime/storage.rs:1355
+captured_struct!(crate::direct_runtime::DirectSnowCouplingInputs {
+    snow_coupling_handoff_m,
+    snow_state_projected,
+    active_snow_coupling,
+    mass_transition_ledgers,
+    hourly_routed_melt_m,
+    sublimation_m,
+    post_winter_rain_m,
+    runtime_swe_after_m,
+    runtime_depth_after_m,
+    runtime_density_after_kg_m3,
+    runtime_settle_day_count_after,
+    coe_boundary_depth_after_m,
+    coe_boundary_density_after_kg_m3,
+    coe_boundary_settle_day_count_after,
+    liquid_holding_capacity_after_m,
+    liquid_water_retained_after_m,
+    liquid_water_released_m,
+    snow_albedo_state_after,
+    snow_layers_after
+});
+// Source fields: winter_column.rs:44
+captured_struct!(crate::winter_column::DirectSnowLayerState {
+    mass_swe_m,
+    thickness_m,
+    density_kg_m3,
+    settle_day_count,
+    temperature_c,
+    liquid_water_m,
+    cold_content_j_m2,
+    refrozen_liquid_m
+});
+// Source fields: hydrology/support_helpers_mod/snow_mass_transition.rs:49
+captured_struct!(crate::hydrology::DirectSnowLiquidDispositionLedger {
+    incoming_liquid_m,
+    routed_liquid_m,
+    retained_liquid_delta_m,
+    refrozen_liquid_m,
+    liquid_closure_residual_m
+});
+// Source fields: direct_runtime/00_core_frames.rs:172
+captured_struct!(crate::direct_runtime::DirectSnowRuntimeCarry {
+    runtime_swe_m,
+    runtime_depth_m,
+    runtime_density_kg_m3,
+    runtime_settle_day_count,
+    coe_boundary_depth_m,
+    coe_boundary_density_kg_m3,
+    coe_boundary_settle_day_count,
+    liquid_water_retained_m,
+    snow_albedo_state,
+    layers
+});
+// Source fields: hydrology/support_helpers_mod/snow_mass_transition.rs:33
+captured_struct!(crate::hydrology::DirectSnowSolidToLiquidLedger {
+    raw_signed_melt_m,
+    redistributed_positive_melt_m,
+    snowpack_swe_loss_m,
+    rain_released_m,
+    liquid_handoff_m
+});
+// Source fields: hydrology/support_helpers_mod/snow_mass_transition.rs:65
+captured_struct!(crate::hydrology::DirectSnowStage3Outcome {
+    enabled,
+    meltwater_temperature_c,
+    sublimation_m
+});
+// Source fields: direct_runtime/normalization.rs:387
+captured_struct!(crate::direct_runtime::DirectStorageBoundsInputs {
+    storage_initial_m,
+    total_accounted_input_m,
+    closure_tolerance_m
+});
+// Source fields: direct_runtime/storage.rs:1093
+captured_struct!(crate::direct_runtime::DirectStorageInputInputs {
+    precip_input_handoff_m
+});
+// Source fields: direct_runtime/storage.rs:1602
+captured_struct!(crate::direct_runtime::DirectStorageReconciliationInputs {
+    storage_initial_m,
+    precip_input_m,
+    snow_coupling_m,
+    frost_liquid_delta_m,
+    runon_input_m,
+    interception_m,
+    evapotranspiration_m,
+    evapotranspiration_storage_return_m,
+    deep_seepage_m,
+    subsurface_loss_m,
+    closure_tolerance_m
+});
+// Source fields: direct_runtime/subsurface.rs:898
+captured_struct!(crate::direct_runtime::DirectSubsurfaceComputeInputs {
+    avg_slope,
+    slope_length_m,
+    lateral_anisotropy_ratio,
+    soil_depth_m,
+    solwpv_mode,
+    mofe_hourly_carry_arrays_enabled,
+    lane_substeps,
+    drainage_capacity_m,
+    drain_enabled,
+    drain_depth_m,
+    drain_spacing_m,
+    drain_diameter_m,
+    layers
+});
+// Source fields: direct_runtime/subsurface.rs:651
+captured_struct!(crate::direct_runtime::DirectSubsurfaceLayerInputs {
+    theta_m,
+    field_capacity_m,
+    upper_limit_m,
+    conductivity_m_s,
+    depth_m,
+    residual_theta,
+    frozen_depth_m,
+    frozen_water_m,
+    porosity,
+    field_capacity_theta,
+    coca,
+    lateral_conductivity_m_s
+});
+// Source fields: direct_runtime/subsurface.rs:713
+captured_struct!(crate::direct_runtime::DirectSubsurfaceLayerState {
+    theta_m,
+    field_capacity_m,
+    upper_limit_m,
+    conductivity_m_s,
+    depth_m,
+    residual_theta,
+    frozen_depth_m,
+    frozen_water_m,
+    porosity,
+    field_capacity_theta,
+    coca,
+    lateral_conductivity_m_s
+});
+// Source fields: direct_runtime/storage.rs:1227
+captured_struct!(crate::direct_runtime::DirectSubsurfaceLossInputs {
+    subsurface_loss_handoff_m
+});
+// Source fields: direct_runtime/02_state_reports.rs:73
+captured_struct!(crate::direct_runtime::DirectTransferBuffers {
+    surface_carry_m,
+    surface_hourly_weights,
+    lateral_carry_m,
+    upstream_flow_m,
+    subsurface_input_m
+});
+// Source fields: direct_runtime/erosion_continuity.rs:161
+captured_struct!(crate::direct_runtime::DirectWave1ContinuityInputs {
+    enabled,
+    inter_ofe,
+    enrichment,
+    segments,
+    peakro_m_s,
+    runoff_depth_m,
+    qin_m2_s,
+    efflen_m,
+    slplen_m,
+    cntlen_m,
+    rspace_m,
+    width_m,
+    field_width_m,
+    effdrn_s,
+    effdrr_s,
+    kr_s_m,
+    kradjf,
+    shcrit_pa,
+    tcadjf,
+    detinr_kg_s_m2,
+    shrsol_pa,
+    tcend_kg_s_m,
+    ktrato,
+    veleff_m_s,
+    beta,
+    strldn,
+    surface_frozen,
+    theta_suppressed
+});
+// Source fields: direct_runtime/erosion_seed.rs:78
+captured_struct!(crate::direct_runtime::DirectWave1OperandSeed {
+    enabled,
+    is_cropland,
+    segments,
+    slplen_m,
+    efflen_m,
+    cntlen_m,
+    rspace_m,
+    field_width_m,
+    avg_slope,
+    slpend,
+    sand,
+    ssasol,
+    classes,
+    veleff_m_s,
+    baselines,
+    kr_s_m,
+    ki,
+    shcrit_pa,
+    hmax_m,
+    flivmx,
+    random_roughness_m,
+    initial_daydis
+});
+// Source fields: direct_runtime/erosion_continuity.rs:112
+captured_struct!(crate::direct_runtime::DirectWave1SlopeSegment { xu, xl, a, b });
+// Source fields: direct_runtime/runoff_result_types.rs:349
+captured_struct!(crate::direct_runtime::DirectWb14HyetographInterval {
+    start_s,
+    end_s,
+    intensity_m_s
+});
+// Source fields: direct_runtime/runoff_result_types.rs:210
+captured_struct!(
+    crate::direct_runtime::DirectWb14InfiltrationProducerInputs {
+        hyetograph,
+        hourly_additional_supply_m,
+        effective_conductivity_m_s,
+        matric_potential_m,
+        storage_capacity_m,
+        depression_storage_capacity_m
+    }
+);
+// Source fields: hydrology/03_kernel_support_00_support_helpers.rs:1298
+captured_struct!(crate::hydrology::DirectWinterFrostComputeInputs {
+    controls,
+    thermal,
+    theta_residual,
+    theta_field_capacity,
+    soil_conductivity_m_s,
+    layer_bulk_density_kg_m3,
+    hourly
+});
+// Source fields: hydrology/03_kernel_support_00_support_helpers.rs:1368
+captured_struct!(crate::hydrology::DirectWinterFrostPartitionOutcome {
+    active_frost_coupling,
+    dthaw_after_m,
+    nft_after,
+    infcap_frz_m_s,
+    soil_water_after_frwatc_m,
+    frwatc_soil_water_before_m,
+    frwatc_soil_water_after_m,
+    frwatc_frozen_water_before_m,
+    frwatc_frozen_water_after_m,
+    frwatc_freeze_debit_m,
+    frwatc_thaw_credit_m,
+    frwatc_net_liquid_delta_m,
+    frozen_water_after_m,
+    frost_depth_after_m,
+    thdp_after_m,
+    tfrdp_after_m,
+    tthawd_after_m,
+    fgthwd_flag_after,
+    total_fine_layer_count,
+    conductivity_tilled_w_m_k,
+    conductivity_untilled_w_m_k,
+    conductivity_residue_w_m_k,
+    shadow_total_water_before_m,
+    shadow_total_water_after_m,
+    shadow_wb_delta_m,
+    shadow_frwatc_residual_m,
+    watpdg_m,
+    watbtm_m,
+    layer_projection,
+    layer_shadow_projection,
+    fine_layer_projection
+});
+// Source fields: direct_runtime/erosion_adjustments.rs:72
+captured_struct!(crate::direct_runtime::ErosionConsolidationBaselines {
+    kicrat,
+    krcrat,
+    tccrat,
+    bconsd
+});
+// Source fields: direct_runtime/erosion_operands.rs:144
+captured_struct!(crate::direct_runtime::ErosionParticleClass {
+    dia_m,
+    spg,
+    frac,
+    fall_m_s,
+    frcly,
+    frslt,
+    frsnd,
+    frorg
+});
+// Source fields: hydrology/03_kernel_support_00_support_helpers.rs:1186
+captured_struct!(crate::hydrology::FrostSeasonalTemperatureCurve {
+    annual_mean_c,
+    amplitude_c,
+    phase_shift_days
+});
+// Source fields: hydrology/08_snow_albedo.rs:68
+captured_enum!(crate::hydrology::SnowAlbedoModel {
+    Brock2000TemperatureAgeV1
+});
+// Source fields: hydrology/08_snow_albedo.rs:89
+captured_struct!(crate::hydrology::SnowAlbedoState {
+    model,
+    albedo,
+    accumulated_positive_temperature_c_day
+});
+// Source fields: direct_runtime/erosion_enrichment.rs:45
+captured_struct!(crate::direct_runtime::Wave1EnrichmentInputs {
+    classes,
+    tcf1,
+    fidel,
+    ssasol,
+    inflow_fractions
+});
+// Source fields: direct_runtime/erosion_continuity.rs:134
+captured_struct!(crate::direct_runtime::Wave1InterOfeContinuity {
+    shrspv_pa,
+    tcprev_kg_s_m,
+    ktrprv,
+    prior_shear_last,
+    prior_transport_last
+});
+
+fn lane(value: &Value) -> Result<crate::DirectLaneConstructorInputs, String> {
+    let water: [f64; 6] = field(value, "water")?;
+    let winter_snow: crate::DirectSnowRuntimeCarry = field(value, "winter_snow")?;
+    let winter_frost: crate::DirectFrostRuntimeCarry = field(value, "winter_frost")?;
+    Ok(crate::DirectLaneConstructorInputs {
+        lane_id: field(value, "lane_id")?,
+        upstream_lane_id: field(value, "upstream_lane_id")?,
+        downstream_lane_id: field(value, "downstream_lane_id")?,
+        upstream_area_ratio: field(value, "upstream_area_ratio")?,
+        area_m2: field(value, "area_m2")?,
+        runoff_publication_q_scale: field(value, "runoff_publication_q_scale")?,
+        runoff_publication_qofe_scale: field(value, "runoff_publication_qofe_scale")?,
+        runoff_publication_efflen_m: field(value, "runoff_publication_efflen_m")?,
+        runoff_publication_cumulative_length_m: field(
+            value,
+            "runoff_publication_cumulative_length_m",
+        )?,
+        runoff_publication_ofe_length_m: field(value, "runoff_publication_ofe_length_m")?,
+        water: crate::DirectWaterState {
+            soil_water_m: water[0],
+            infiltration_m: water[1],
+            runoff_m: water[2],
+            evapotranspiration_m: water[3],
+            drainage_m: water[4],
+            lateral_flow_m: water[5],
+        },
+        transfer: field(value, "transfer")?,
+        publication: field(value, "publication")?,
+        subsurface_layers: field(value, "subsurface_layers")?,
+        evapotranspiration_stage_state: field(value, "evapotranspiration_stage_state")?,
+        plant_growth_state: field(value, "plant_growth_state")?,
+        plant_water_stress: field(value, "plant_water_stress")?,
+        winter_column: Box::new(crate::DirectWinterColumnState {
+            snow: winter_snow.into(),
+            frost: winter_frost.into(),
+        }),
+        snow_runtime_carry: field(value, "snow_runtime_carry")?,
+        frost_runtime_carry: field(value, "frost_runtime_carry")?,
+        day_inputs: field(value, "day_inputs")?,
+    })
+}
+
+pub(super) fn restore(value: &Value) -> Result<crate::DirectRunConstructorInputs, String> {
+    let identity = value
+        .get("identity")
+        .ok_or("constructor identity missing")?;
+    let identity = crate::DirectRunIdentity::new(
+        field(identity, "run_id")?,
+        field(identity, "hillslope_id")?,
+        field(identity, "lane_count")?,
+        field(identity, "day_count")?,
+    )
+    .map_err(|error| error.to_string())?;
+    let phase_plan = crate::DirectPhasePlan::default();
+    let captured_ranks: Vec<usize> = field(value, "phase_plan_ranks")?;
+    let canonical_ranks = phase_plan
+        .phases()
+        .iter()
+        .map(|phase| phase.rank())
+        .collect::<Vec<_>>();
+    if captured_ranks != canonical_ranks {
+        return Err("captured constructor phase plan differs from canonical ordered plan".into());
+    }
+    let lanes = value
+        .get("lanes")
+        .and_then(Value::as_array)
+        .ok_or("constructor lanes missing")?
+        .iter()
+        .map(lane)
+        .collect::<Result<Vec<_>, _>>()?;
+    let restored = crate::DirectRunConstructorInputs {
+        identity,
+        lanes,
+        phase_plan,
+    };
+    if super::capture_direct_run_constructor_inputs(&restored) != *value {
+        return Err("constructor inverse canonical recapture mismatch".into());
+    }
+    Ok(restored)
+}
--- a/crates/openwepp-hillslope-orchestrator/src/snow_stage3_v11_current_context_capture.rs
+++ b/crates/openwepp-hillslope-orchestrator/src/snow_stage3_v11_current_context_capture.rs
@@ -5,14 +5,24 @@
     v9_real_consumer_shadow::DirectV10RealConsumerShadow,
 };
 use openwepp_coupled_time::{
-    AcceptedSlabReceiptV1, ConstraintClass, ConstraintReductionReceiptV1, CoupledClockStateV1,
-    CoupledSlabCandidateV1, CoupledTimeRestartV2, DiagnosticReductionV1, LedgerEntryV1,
-    StepConstraintV1, TimeSupport, accept_slab, complete_owner_set_digest, digest_bytes,
-    reduce_constraints,
+    AcceptedSlabReceiptV1, ConstraintReductionReceiptV1, CoupledClockStateV1, CoupledTimeRestartV2,
+    DiagnosticReductionV1, LedgerEntryV1, TimeSupport, digest_bytes,
+};
+#[cfg(test)]
+use openwepp_coupled_time::{
+    ConstraintClass, CoupledSlabCandidateV1, StepConstraintV1, accept_slab,
+    complete_owner_set_digest, reduce_constraints,
 };
 use openwepp_vegetation::V11ParentTransaction;
 use serde::{Deserialize, Serialize};
 use std::collections::{BTreeMap, BTreeSet};
+
+#[cfg(feature = "persisted-restart-v1")]
+#[path = "snow_stage3_v11_constructor_inverse.rs"]
+mod constructor_inverse;
+#[cfg(feature = "persisted-restart-v1")]
+#[path = "snow_stage3_v11_member_restoration.rs"]
+mod member_restoration;
 
 const CLOCK_MODEL_DOMAIN: &[u8] = b"OPENWEPP_STAGE3_V11_COUPLED_CLOCK_RESTART_MODEL_V1";
 const CLOCK_AUTHORITY_DOMAIN: &[u8] = b"SC-SNOWENERGY-001@22+SC-COUPLEDTIME-001";
@@ -75,8 +85,82 @@
 }
 
 #[derive(Clone, Debug, Deserialize, Serialize)]
+struct DirectSubsurfaceLayerCaptureV1 {
+    theta_m: f64,
+    field_capacity_m: f64,
+    upper_limit_m: f64,
+    conductivity_m_s: f64,
+    depth_m: f64,
+    residual_theta: f64,
+    frozen_depth_m: f64,
+    frozen_water_m: f64,
+    porosity: f64,
+    field_capacity_theta: f64,
+    coca: f64,
+    lateral_conductivity_m_s: f64,
+}
+
+impl From<&crate::DirectSubsurfaceLayerState> for DirectSubsurfaceLayerCaptureV1 {
+    fn from(value: &crate::DirectSubsurfaceLayerState) -> Self {
+        Self {
+            theta_m: value.theta_m,
+            field_capacity_m: value.field_capacity_m,
+            upper_limit_m: value.upper_limit_m,
+            conductivity_m_s: value.conductivity_m_s,
+            depth_m: value.depth_m,
+            residual_theta: value.residual_theta,
+            frozen_depth_m: value.frozen_depth_m,
+            frozen_water_m: value.frozen_water_m,
+            porosity: value.porosity,
+            field_capacity_theta: value.field_capacity_theta,
+            coca: value.coca,
+            lateral_conductivity_m_s: value.lateral_conductivity_m_s,
+        }
+    }
+}
+
+impl From<DirectSubsurfaceLayerCaptureV1> for crate::DirectSubsurfaceLayerState {
+    fn from(value: DirectSubsurfaceLayerCaptureV1) -> Self {
+        Self {
+            theta_m: value.theta_m,
+            field_capacity_m: value.field_capacity_m,
+            upper_limit_m: value.upper_limit_m,
+            conductivity_m_s: value.conductivity_m_s,
+            depth_m: value.depth_m,
+            residual_theta: value.residual_theta,
+            frozen_depth_m: value.frozen_depth_m,
+            frozen_water_m: value.frozen_water_m,
+            porosity: value.porosity,
+            field_capacity_theta: value.field_capacity_theta,
+            coca: value.coca,
+            lateral_conductivity_m_s: value.lateral_conductivity_m_s,
+        }
+    }
+}
+
+fn capture_subsurface_layers(
+    layers: &[crate::DirectSubsurfaceLayerState],
+) -> Vec<DirectSubsurfaceLayerCaptureV1> {
+    layers
+        .iter()
+        .map(DirectSubsurfaceLayerCaptureV1::from)
+        .collect()
+}
+
+fn restore_subsurface_layers(
+    layers: &[DirectSubsurfaceLayerCaptureV1],
+) -> Vec<crate::DirectSubsurfaceLayerState> {
+    layers
+        .iter()
+        .cloned()
+        .map(crate::DirectSubsurfaceLayerState::from)
+        .collect()
+}
+
+#[derive(Clone, Debug, Deserialize, Serialize)]
 struct DirectLaneDynamicCaptureV1 {
     water: [f64; 6],
+    subsurface_layers: Vec<DirectSubsurfaceLayerCaptureV1>,
     transfer_surface_carry_m: [f64; 24],
     transfer_surface_hourly_weights: [f64; 24],
     transfer_lateral_carry_m: [f64; 24],
@@ -149,7 +233,6 @@
                         != expected.runoff_publication_ofe_length_m.to_bits()
                     || actual.erosion_downstream_operands != expected.erosion_downstream_operands
                     || actual.erosion_inflow_intake != expected.erosion_inflow_intake
-                    || actual.subsurface_layers != expected.subsurface_layers
                     || actual.evapotranspiration_stage_state
                         != expected.evapotranspiration_stage_state
                     || actual.plant_growth_state != expected.plant_growth_state
@@ -183,6 +266,7 @@
                     lane.water.drainage_m,
                     lane.water.lateral_flow_m,
                 ],
+                subsurface_layers: capture_subsurface_layers(&lane.subsurface_layers),
                 transfer_surface_carry_m: lane.transfer.surface_carry_m,
                 transfer_surface_hourly_weights: lane.transfer.surface_hourly_weights,
                 transfer_lateral_carry_m: lane.transfer.lateral_carry_m,
@@ -316,6 +400,7 @@
             drainage_m: captured.water[4],
             lateral_flow_m: captured.water[5],
         };
+        lane.subsurface_layers = restore_subsurface_layers(&captured.subsurface_layers);
         lane.transfer = crate::DirectTransferBuffers {
             surface_carry_m: captured.transfer_surface_carry_m,
             surface_hourly_weights: captured.transfer_surface_hourly_weights,
@@ -522,6 +607,63 @@
         destination_capabilities: capabilities.iter().map(|((ofe, tile), capability)| { let destination=DestinationCaptureV1 { ofe_id:ofe.as_str().to_owned(), tile_id:tile.as_str().to_owned() }; match capability { crate::snow_stage3_open_boundary::SealedStage3TileBoundaryForcingV1::V11CanopyCovered(value) => DestinationCapabilityCaptureV1::Covered { destination, rho_air_kg_m3:value.rho_air_kg_m3, cp_air_j_kg_k:value.cp_air_j_kg_k, effective_canopy_cover:value.effective_canopy_cover }, crate::snow_stage3_open_boundary::SealedStage3TileBoundaryForcingV1::OpenSnow(_) => DestinationCapabilityCaptureV1::Open { destination } } }).collect(),
         open_snow_destinations: open_requests.iter().map(|(ofe,tile)| DestinationCaptureV1 { ofe_id:ofe.as_str().to_owned(), tile_id:tile.as_str().to_owned() }).collect(),
     })
+}
+
+fn capture_snow_free_prepared_support(
+    support: &crate::DirectSnowStage3V11PreparedSupport,
+) -> Result<serde_json::Value, String> {
+    let (active, forcing, snow_free, hard_boundaries, identities, represented, open_requests) =
+        support.diagnostic_snow_free_capture_parts_v1()?;
+    let active = active
+        .iter()
+        .map(|(lane, value)| (*lane, active_snow_capture(value)))
+        .collect::<BTreeMap<_, _>>();
+    let forcing = forcing
+        .iter()
+        .map(|(lane, value)| {
+            (
+                *lane,
+                SupportForcingCaptureV1 {
+                    hourly: SnowHourlyCaptureV1 {
+                        values: [
+                            value.forcing.active_precipitation_m,
+                            value.forcing.rain_m,
+                            value.forcing.snowfall_m,
+                            value.forcing.radiation_mj_m2,
+                            value.forcing.air_temperature_c,
+                            value.forcing.cloud_fraction,
+                            value.forcing.rain_fraction,
+                            value.forcing.snow_fraction,
+                        ],
+                        phase_model: value.forcing.phase_model.id().into(),
+                        hydrometeor_temperature_c: value.forcing.hydrometeor_temperature_c,
+                    },
+                    duration_seconds: value.duration_seconds,
+                },
+            )
+        })
+        .collect::<BTreeMap<_, _>>();
+    let identities = identities
+        .iter()
+        .map(|(lane, values)| {
+            serde_json::to_vec(values)
+                .map(|bytes| (*lane, bytes))
+                .map_err(|error| error.to_string())
+        })
+        .collect::<Result<BTreeMap<_, _>, _>>()?;
+    Ok(serde_json::json!({
+        "support": support.support(),
+        "active_snow_by_lane": active,
+        "support_forcing_by_lane": forcing,
+        "snow_free_interval": snow_free,
+        "covered_projection": null,
+        "hard_boundaries": hard_boundaries,
+        "identities_by_lane": identities,
+        "destination_capabilities": [],
+        "destination_capability_count": represented.len(),
+        "open_snow_destinations": [],
+        "open_snow_destination_count": open_requests.len(),
+    }))
 }
 
 fn restore_covered_interval(
@@ -1231,7 +1373,64 @@
         && support.end_ns().get() == B01_CHILD_END_NS
 }
 
+#[cfg(test)]
+#[test]
+fn b01_pre_child_selector_requires_every_exact_coordinate() {
+    let target = TimeSupport::new(
+        openwepp_coupled_time::ModelTimeNs::new(B01_CHILD_START_NS),
+        openwepp_coupled_time::ModelTimeNs::new(B01_CHILD_END_NS),
+    )
+    .expect("target support");
+    assert!(is_target(B01_DAY_INDEX, B01_INTERVAL_INDEX, target));
+    assert!(!is_target(B01_DAY_INDEX - 1, B01_INTERVAL_INDEX, target));
+    assert!(!is_target(B01_DAY_INDEX, B01_INTERVAL_INDEX - 1, target));
+    assert!(!is_target(
+        B01_DAY_INDEX,
+        B01_INTERVAL_INDEX,
+        TimeSupport::new(
+            openwepp_coupled_time::ModelTimeNs::new(B01_CHILD_START_NS - 1),
+            openwepp_coupled_time::ModelTimeNs::new(B01_CHILD_END_NS),
+        )
+        .expect("wrong-start support"),
+    ));
+    assert!(!is_target(
+        B01_DAY_INDEX,
+        B01_INTERVAL_INDEX,
+        TimeSupport::new(
+            openwepp_coupled_time::ModelTimeNs::new(B01_CHILD_START_NS),
+            openwepp_coupled_time::ModelTimeNs::new(B01_CHILD_END_NS + 1),
+        )
+        .expect("wrong-end support"),
+    ));
+}
+
+#[derive(Clone, Copy)]
+pub(crate) struct SnowFreePreChildHistoryV1<'a> {
+    pub owner_joins: &'a [crate::snow_stage3_v11_attachment::Stage3CoupledSubslabReceiptV1],
+    pub event_groups: &'a [crate::snow_stage3_v11_attachment::Stage3V11TerminalEventGroupV1],
+    pub current_stage3: &'a BTreeMap<u32, crate::DirectSnowStage3PersistentState>,
+    pub current_snow_enthalpy_material_owner: Option<
+        &'a crate::snow_stage3_v11_snow_enthalpy_carry::AuthenticatedCoveredSnowMaterialOwnerV1,
+    >,
+    pub pending_terminal_parcels:
+        &'a BTreeMap<openwepp_coupled_time::Digest32, crate::DirectSnowStage3V11TerminalParcel>,
+}
+
+#[derive(Clone, Copy)]
+pub(crate) enum PreChildCapturePhaseV1<'a> {
+    Covered,
+    SnowFree {
+        history: SnowFreePreChildHistoryV1<'a>,
+        validated_native_inactive_wb14_prefix:
+            Option<&'a crate::direct_runtime::ValidatedNativeInactiveWb14PrefixV1>,
+        ending_snow_owner_bytes: &'a [u8],
+        deferred_native_v2_soil_custody:
+            Option<&'a crate::v9_real_consumer_shadow::DeferredNativeV2SoilCustodyV1>,
+    },
+}
+
 pub(crate) struct PreChildContext<'a> {
+    pub phase: PreChildCapturePhaseV1<'a>,
     pub context: &'a DirectSnowStage3V11StaticContext,
     pub parent: &'a V11ParentTransaction,
     pub consumer: &'a DirectV10RealConsumerShadow,
@@ -1256,8 +1455,243 @@
     pub interval: usize,
 }
 
+/// Producer-local test oracle inputs, populated before the recorder context is
+/// constructed so a recorder wiring mistake cannot define its own expectation.
+#[cfg(test)]
+pub(crate) struct PreChildProducerSnapshotInputsV1<'a> {
+    pub phase: PreChildCapturePhaseV1<'a>,
+    pub context: &'a DirectSnowStage3V11StaticContext,
+    pub parent: &'a V11ParentTransaction,
+    pub consumer: &'a DirectV10RealConsumerShadow,
+    pub clock: &'a CoupledClockStateV1,
+    pub prepared_support: Option<&'a crate::DirectSnowStage3V11PreparedSupport>,
+    pub beginning_stage3: &'a BTreeMap<u32, crate::DirectSnowStage3PersistentState>,
+    pub beginning_snow_enthalpy_material_owner: Option<
+        &'a crate::snow_stage3_v11_snow_enthalpy_carry::AuthenticatedCoveredSnowMaterialOwnerV1,
+    >,
+    pub support: TimeSupport,
+    pub reduction: &'a ConstraintReductionReceiptV1,
+    pub ledger: &'a LedgerEntryV1,
+    pub receipt: &'a AcceptedSlabReceiptV1,
+    pub binding: &'a crate::direct_runtime::DirectWb14CoupledChildBindingV1,
+    pub pending_terminal_parcels:
+        &'a BTreeMap<openwepp_coupled_time::Digest32, crate::DirectSnowStage3V11TerminalParcel>,
+    pub forcing_receipt: openwepp_coupled_time::Digest32,
+}
+
+fn capture_soil_trial_v2(
+    trial: &openwepp_land_surface_energy::SoilThermalTrialStateV2,
+) -> serde_json::Value {
+    serde_json::json!({
+        "transaction_id": trial.transaction_id(),
+        "predecessor_transaction_id": trial.predecessor_transaction_id(),
+        "support_start_ns": trial.support_start_ns(),
+        "support_end_ns": trial.support_end_ns(),
+        "beginning_state_sha256": trial.beginning_state_sha256(),
+        "accepted_predecessor_receipt_chain_sha256": trial.accepted_predecessor_receipt_chain_sha256(),
+        "unpublished_predecessor_trial_sha256": trial.unpublished_predecessor_trial_sha256(),
+        "numerical_coordinate_set_sha256": trial.numerical_coordinate_set_sha256(),
+        "numerical_coordinate_authority_sha256": trial.numerical_coordinate_authority_sha256(),
+        "ending_state": trial.ending_state(),
+        "layer_credits": trial.layer_credits(),
+        "unpublished_trial_sha256": trial.unpublished_trial_sha256(),
+    })
+}
+
+fn capture_deferred_native_v2_soil_custody(
+    custody: Option<&crate::v9_real_consumer_shadow::DeferredNativeV2SoilCustodyV1>,
+) -> Result<Option<serde_json::Value>, String> {
+    custody
+        .map(|custody| {
+            let candidate = custody
+                .candidate()
+                .v2()
+                .map_err(|error| error.to_string())?;
+            let continuation = custody.continuation().map(|continuation| {
+                serde_json::json!({
+                    "original_beginning_owner": continuation.original_prepared().beginning_owner(),
+                    "physical_trial": capture_soil_trial_v2(continuation.physical_trial()),
+                    "accumulated_operands": continuation.accumulated_operands(),
+                    "ordered_layer_credit_chain": continuation.diagnostic_layer_credit_chain(),
+                })
+            });
+            Ok(serde_json::json!({
+                "posture": "reconstructible_operands_not_installable_capability",
+                "candidate": capture_soil_trial_v2(candidate),
+                "continuation": continuation,
+            }))
+        })
+        .transpose()
+}
+
+/// Immutable configuration and identity authority recovered from the pinned
+/// original seed.  This is deliberately private to the diagnostic reader:
+/// it does not create a restart wire or an alternative production bootstrap.
+#[cfg(all(test, feature = "persisted-restart-v1"))]
+#[derive(Clone, Debug)]
+struct PinnedSeedConfigurationV1 {
+    vegetation: openwepp_vegetation::VegetationConfiguration,
+    lse: openwepp_land_surface_energy::LandSurfaceEnergyConfiguration,
+    calendar: openwepp_coupled_time::Digest32,
+    controller_policy: openwepp_coupled_time::Digest32,
+    run_identity: openwepp_coupled_time::Digest32,
+    topology_identity: openwepp_coupled_time::Digest32,
+}
+
+#[cfg(all(test, feature = "persisted-restart-v1"))]
+fn extract_pinned_seed_configuration_v1(
+    seed_path: &std::path::Path,
+) -> Result<PinnedSeedConfigurationV1, String> {
+    let seed: serde_json::Value =
+        serde_json::from_slice(&std::fs::read(seed_path).map_err(|error| error.to_string())?)
+            .map_err(|error| error.to_string())?;
+    let required = |key: &str| {
+        seed.get(key)
+            .cloned()
+            .ok_or_else(|| format!("pinned seed missing {key}"))
+    };
+    let checkpoint = required("checkpoint")?;
+    let checkpoint_field = |key: &str| {
+        checkpoint
+            .get(key)
+            .cloned()
+            .ok_or_else(|| format!("pinned seed checkpoint missing {key}"))
+    };
+    Ok(PinnedSeedConfigurationV1 {
+        vegetation: serde_json::from_value(required("vegetation_configuration")?)
+            .map_err(|error| error.to_string())?,
+        lse: serde_json::from_value(required("lse_configuration")?)
+            .map_err(|error| error.to_string())?,
+        calendar: serde_json::from_value(required("calendar_receipt")?)
+            .map_err(|error| error.to_string())?,
+        controller_policy: serde_json::from_value(required("controller_policy")?)
+            .map_err(|error| error.to_string())?,
+        run_identity: serde_json::from_value(checkpoint_field("run_identity_sha256")?)
+            .map_err(|error| error.to_string())?,
+        topology_identity: serde_json::from_value(checkpoint_field("topology_sha256")?)
+            .map_err(|error| error.to_string())?,
+    })
+}
+
+fn capture_direct_run_constructor_inputs(
+    inputs: &crate::DirectRunConstructorInputs,
+) -> serde_json::Value {
+    let lanes = inputs
+        .lanes
+        .iter()
+        .map(|lane| {
+            serde_json::json!({
+                "lane_id": lane.lane_id, "upstream_lane_id": lane.upstream_lane_id,
+                "downstream_lane_id": lane.downstream_lane_id,
+                "upstream_area_ratio": lane.upstream_area_ratio, "area_m2": lane.area_m2,
+                "runoff_publication_q_scale": lane.runoff_publication_q_scale,
+                "runoff_publication_qofe_scale": lane.runoff_publication_qofe_scale,
+                "runoff_publication_efflen_m": lane.runoff_publication_efflen_m,
+                "runoff_publication_cumulative_length_m": lane.runoff_publication_cumulative_length_m,
+                "runoff_publication_ofe_length_m": lane.runoff_publication_ofe_length_m,
+                "water": [lane.water.soil_water_m, lane.water.infiltration_m, lane.water.runoff_m, lane.water.evapotranspiration_m, lane.water.drainage_m, lane.water.lateral_flow_m],
+                "transfer": { "surface_carry_m": lane.transfer.surface_carry_m, "surface_hourly_weights": lane.transfer.surface_hourly_weights, "lateral_carry_m": lane.transfer.lateral_carry_m, "upstream_flow_m": lane.transfer.upstream_flow_m, "subsurface_input_m": lane.transfer.subsurface_input_m },
+                "publication": lane.publication, "subsurface_layers": lane.subsurface_layers,
+                "evapotranspiration_stage_state": lane.evapotranspiration_stage_state,
+                "plant_growth_state": lane.plant_growth_state, "plant_water_stress": lane.plant_water_stress,
+                "winter_snow": crate::DirectSnowRuntimeCarry::from(lane.winter_column.snow.clone()),
+                "winter_frost": crate::DirectFrostRuntimeCarry::from(lane.winter_column.frost.clone()),
+                "snow_runtime_carry": lane.snow_runtime_carry, "frost_runtime_carry": lane.frost_runtime_carry,
+                "day_inputs": lane.day_inputs,
+            })
+        })
+        .collect::<Vec<_>>();
+    serde_json::json!({
+        "identity": { "run_id": inputs.identity.run_id, "hillslope_id": inputs.identity.hillslope_id, "lane_count": inputs.identity.lane_count, "day_count": inputs.identity.day_count },
+        "phase_plan_ranks": inputs.phase_plan.phases().iter().map(|phase| phase.rank()).collect::<Vec<_>>(),
+        "lanes": lanes,
+    })
+}
+
+struct CapturedPreChildPhaseV1 {
+    kind: &'static str,
+    phase_name: &'static str,
+    constraint_source_owner_id: &'static str,
+    prepared_support: Option<serde_json::Value>,
+    diagnostics: serde_json::Value,
+}
+
+fn capture_pre_child_phase(
+    phase: PreChildCapturePhaseV1<'_>,
+    prepared_support: Option<&crate::DirectSnowStage3V11PreparedSupport>,
+    parent_support: TimeSupport,
+    surface_configuration: &[u8],
+) -> Result<CapturedPreChildPhaseV1, String> {
+    match phase {
+        PreChildCapturePhaseV1::Covered => Ok(CapturedPreChildPhaseV1 {
+            kind: "b01_wb14_pre_child_current_context_v1",
+            phase_name: "covered",
+            constraint_source_owner_id: "v11-snow-covered-real-consumer",
+            prepared_support: prepared_support
+                .map(capture_prepared_support)
+                .transpose()?
+                .map(serde_json::to_value)
+                .transpose()
+                .map_err(|error| error.to_string())?,
+            diagnostics: serde_json::Value::Null,
+        }),
+        PreChildCapturePhaseV1::SnowFree {
+            history,
+            validated_native_inactive_wb14_prefix,
+            ending_snow_owner_bytes,
+            deferred_native_v2_soil_custody,
+        } => {
+            let prepared_support =
+                prepared_support.ok_or("snow-free capture missing transformed prepared support")?;
+            let deferred_native_v2_soil_custody =
+                capture_deferred_native_v2_soil_custody(deferred_native_v2_soil_custody)?;
+            let owner_joins =
+                crate::snow_stage3_v11_attachment::diagnostic_coupled_subslab_receipts_value_v2(
+                    history.owner_joins,
+                )?;
+            Ok(CapturedPreChildPhaseV1 {
+                kind: "b01_wb14_snow_free_pre_child_current_context_v1",
+                phase_name: "snow_free",
+                constraint_source_owner_id: "v11-real-consumer",
+                prepared_support: Some(capture_snow_free_prepared_support(prepared_support)?),
+                diagnostics: serde_json::json!({
+                    "ordered_owner_joins": owner_joins,
+                    "ordered_owner_join_support_liquid_custody_v2": history.owner_joins.iter().map(|receipt| receipt.support_liquid_custody_v2()).collect::<Vec<_>>(),
+                    "ordered_event_groups": history.event_groups,
+                    "ordered_event_group_terminal_liquid_custody_v2": history.event_groups.iter().map(|group| group.terminal_receiver_custody_v2()).collect::<Vec<_>>(),
+                    "prefix_validation_parent_support": parent_support,
+                    "prefix_validation_surface_configuration_canonical_json": surface_configuration,
+                    "validated_native_inactive_wb14_prefix": validated_native_inactive_wb14_prefix,
+                    "current_stage3": history.current_stage3,
+                    "current_snow_enthalpy_material_owner": history.current_snow_enthalpy_material_owner,
+                    "pending_terminal_parcels": history.pending_terminal_parcels,
+                    "ending_snow_owner_canonical_bytes": ending_snow_owner_bytes,
+                    "deferred_native_v2_soil_custody": deferred_native_v2_soil_custody,
+                    "history_posture": "actual_ordered_prefix_inputs_and_result; no fabricated receipts or installable capability",
+                }),
+            })
+        }
+    }
+}
+
+fn capture_coupled_clock_restart(clock: &CoupledClockStateV1) -> Result<Vec<u8>, String> {
+    CoupledTimeRestartV2::new(
+        digest_bytes(CLOCK_MODEL_DOMAIN),
+        digest_bytes(CLOCK_AUTHORITY_DOMAIN),
+        clock.clone(),
+        DiagnosticReductionV1::new(CLOCK_REDUCTION_ID.into(), "dimensionless".into())
+            .map_err(|error| error.to_string())?,
+        None,
+        Vec::new(),
+    )
+    .map_err(|error| error.to_string())?
+    .to_canonical_json()
+    .map_err(|error| error.to_string())
+}
+
 fn capture_row(input: &PreChildContext<'_>) -> Result<serde_json::Value, String> {
     let PreChildContext {
+        phase,
         context,
         parent,
         consumer,
@@ -1275,18 +1709,7 @@
         day,
         interval,
     } = input;
-    let restart = CoupledTimeRestartV2::new(
-        digest_bytes(CLOCK_MODEL_DOMAIN),
-        digest_bytes(CLOCK_AUTHORITY_DOMAIN),
-        (*clock).clone(),
-        DiagnosticReductionV1::new(CLOCK_REDUCTION_ID.into(), "dimensionless".into())
-            .map_err(|e| e.to_string())?,
-        None,
-        Vec::new(),
-    )
-    .map_err(|e| e.to_string())?
-    .to_canonical_json()
-    .map_err(|e| e.to_string())?;
+    let restart = capture_coupled_clock_restart(clock)?;
     let owners = consumer
         .canonical_v11_parent_owner_state_bytes()
         .map_err(|e| e.to_string())?;
@@ -1309,52 +1732,54 @@
     let native_soil = consumer
         .diagnostic_native_soil_v2()
         .map_err(|error| error.to_string())?;
+    let surface_configuration = context
+        .surface_liquid_configuration
+        .canonical_bytes()
+        .map_err(|error| error.to_string())?;
+    let native_hydrology_constructor_inputs = consumer
+        .diagnostic_hydrology_constructor_inputs_v1()
+        .ok_or("captured native hydrology constructor pin missing")?;
     let hydrology_frame_dynamic = capture_direct_run_frame_dynamic(
         consumer.restart_authority_hydrology_frame(),
-        consumer
-            .diagnostic_hydrology_constructor_inputs_v1()
-            .ok_or("captured native hydrology constructor pin missing")?
-            .as_ref(),
+        native_hydrology_constructor_inputs.as_ref(),
         &context.surface_liquid_configuration,
     )?;
     let native_consumer_constructor_operands = consumer
         .diagnostic_native_consumer_constructor_operands_v1()
         .map_err(|error| error.to_string())?;
-    let prepared_support = prepared_support
-        .map(capture_prepared_support)
-        .transpose()?
-        .map(serde_json::to_value)
-        .transpose()
-        .map_err(|e| e.to_string())?;
+    let phase = capture_pre_child_phase(
+        *phase,
+        *prepared_support,
+        clock.parent_support(),
+        &surface_configuration,
+    )?;
     let beginning_stage3 = serde_json::to_vec(beginning_stage3).map_err(|e| e.to_string())?;
     if let Some(owner) = beginning_snow_enthalpy_material_owner {
         owner.validate().map_err(|error| error.to_string())?;
     }
     Ok(serde_json::json!({
-        "kind":"b01_wb14_pre_child_current_context_v1", "day_index":day, "interval_index":interval,
+        "kind":phase.kind, "capture_phase":phase.phase_name, "day_index":day, "interval_index":interval,
         "support":support, "parent_support":clock.parent_support(), "accepted_until":clock.accepted_until(),
         "parent_checkpoint_canonical_json":parent_checkpoint, "complete_owner_canonical_bytes":owners,
         "clock_complete_owner_canonical_bytes":clock_owners,
         "coupled_clock_restart_canonical_json":restart, "coupled_clock_restart_model_sha256":digest_bytes(CLOCK_MODEL_DOMAIN), "coupled_clock_restart_authority_sha256":digest_bytes(CLOCK_AUTHORITY_DOMAIN), "coupled_clock_restart_policy_sha256":context.controller_policy,
         "vegetation_configuration_canonical_json":vegetation, "lse_configuration_canonical_json":lse,
+        "surface_liquid_configuration_canonical_json":surface_configuration,
         "constraint_reduction_canonical_json":reduction, "complete_owner_ledger_canonical_json":ledger, "provisional_receipt_canonical_json":receipt,
-        "provisional_constructor_inputs":{"constraint_source_owner_id":"v11-snow-covered-real-consumer","constraint_class":"hard_boundary","forcing_receipt":forcing_receipt,"calendar_receipt":context.calendar_receipt,"controller_policy":context.controller_policy,"ledger_flux_id":"complete-owner-custody","ledger_units":"canonical-owner-state","ledger_child_support_start_ns":support.start_ns().get(),"ledger_child_support_end_ns":support.end_ns().get()},
+        "provisional_constructor_inputs":{"constraint_source_owner_id":phase.constraint_source_owner_id,"constraint_class":"hard_boundary","forcing_receipt":forcing_receipt,"calendar_receipt":context.calendar_receipt,"controller_policy":context.controller_policy,"ledger_flux_id":"complete-owner-custody","ledger_units":"canonical-owner-state","ledger_child_support_start_ns":support.start_ns().get(),"ledger_child_support_end_ns":support.end_ns().get()},
         "coupled_binding":{"proposed_upper_bound_s_bits":binding.proposed_upper_bound_s_bits,"coupled_parent_transaction_sha256":binding.coupled_parent_transaction_sha256,"accepted_slab_sha256":binding.accepted_slab_sha256,"parent_beginning_complete_owner_set_sha256":binding.parent_beginning_complete_owner_set_sha256,"parent_support_start_ns":binding.parent_support_start_ns,"parent_support_end_ns":binding.parent_support_end_ns,"child_support_start_ns":binding.child_support_start_ns,"child_support_end_ns":binding.child_support_end_ns},
         "provider_cursor_canonical_json":provider, "terminal_parcels_produced_unconsumed_canonical_json":terminal_custody, "static_run_identity":context.run_identity, "static_topology_identity":context.topology_identity,
-        "prepared_support_constructor_operands":prepared_support,
+        "prepared_support_constructor_operands":phase.prepared_support,
         "native_lane_map_constructor_operands":native_lane_maps,
         "native_soil_v2_constructor_operands":native_soil,
         "native_hydrology_frame_dynamic_constructor_operands":hydrology_frame_dynamic,
+        "native_hydrology_constructor_inputs":capture_direct_run_constructor_inputs(native_hydrology_constructor_inputs.as_ref()),
         "native_consumer_constructor_operands":native_consumer_constructor_operands,
         "beginning_stage3_canonical_json":beginning_stage3,
         "beginning_snow_enthalpy_material_owner":beginning_snow_enthalpy_material_owner,
+        "phase_diagnostics":phase.diagnostics,
         "capture_posture":"before physical ingress; diagnostic no-publication wrapper"
     }))
-}
-
-fn capture(input: &PreChildContext<'_>) -> Result<(), String> {
-    crate::snow_accuracy_observer::record(capture_row(input)?);
-    Ok(())
 }
 
 /// Record the actual provider-bound day once, before its supports enter the
@@ -1470,18 +1895,669 @@
     capture_prepared_day_row(day, attachment)
 }
 
+#[cfg(test)]
+#[derive(Clone, Copy, Debug, Eq, PartialEq)]
+pub(crate) enum PreChildRecorderTestModeV1 {
+    Disabled,
+    ProductionNonTargetObserverEnabled,
+    EnabledCovered,
+    ForcedCoveredCaptureFailure,
+    EnabledSnowFree,
+    ForcedSnowFreeCaptureFailure,
+    ForcedSnowFreeWrongPhaseFailure,
+    ForcedSnowFreeRequiredOmissionFailure,
+}
+
+#[cfg(test)]
+#[derive(Clone, Debug, Default, PartialEq)]
+pub(crate) struct PreChildRecorderTestAuditV1 {
+    pub producer_calls: usize,
+    pub live_inputs: Vec<serde_json::Value>,
+    pub recorded_rows: Vec<serde_json::Value>,
+}
+
+#[cfg(test)]
+struct PreChildRecorderTestStateV1 {
+    mode: PreChildRecorderTestModeV1,
+    producer_recorded: bool,
+    selected_once: bool,
+    audit: PreChildRecorderTestAuditV1,
+}
+
+#[cfg(test)]
+std::thread_local! {
+    static PRE_CHILD_RECORDER_TEST_STATE_V1: std::cell::RefCell<Option<PreChildRecorderTestStateV1>> = const { std::cell::RefCell::new(None) };
+}
+
+#[cfg(test)]
+pub(crate) fn begin_pre_child_recorder_test_audit_v1(mode: PreChildRecorderTestModeV1) {
+    PRE_CHILD_RECORDER_TEST_STATE_V1.with(|slot| {
+        *slot.borrow_mut() = Some(PreChildRecorderTestStateV1 {
+            mode,
+            producer_recorded: false,
+            selected_once: false,
+            audit: PreChildRecorderTestAuditV1::default(),
+        });
+    });
+}
+
+#[cfg(test)]
+pub(crate) fn pre_child_recorder_test_observer_active_v1() -> bool {
+    PRE_CHILD_RECORDER_TEST_STATE_V1.with(|slot| slot.borrow().is_some())
+}
+
+#[cfg(test)]
+pub(crate) fn take_pre_child_recorder_test_audit_v1() -> PreChildRecorderTestAuditV1 {
+    PRE_CHILD_RECORDER_TEST_STATE_V1
+        .with(|slot| slot.borrow_mut().take())
+        .map_or_else(PreChildRecorderTestAuditV1::default, |state| state.audit)
+}
+
+#[cfg(test)]
+fn test_recorder_selection_v1(
+    phase: PreChildCapturePhaseV1<'_>,
+) -> Option<PreChildRecorderTestModeV1> {
+    PRE_CHILD_RECORDER_TEST_STATE_V1.with(|slot| {
+        let mut slot = slot.borrow_mut();
+        let state = slot.as_mut()?;
+        let phase_matches = matches!(
+            (state.mode, phase),
+            (
+                PreChildRecorderTestModeV1::EnabledCovered
+                    | PreChildRecorderTestModeV1::ForcedCoveredCaptureFailure,
+                PreChildCapturePhaseV1::Covered,
+            ) | (
+                PreChildRecorderTestModeV1::EnabledSnowFree
+                    | PreChildRecorderTestModeV1::ForcedSnowFreeCaptureFailure
+                    | PreChildRecorderTestModeV1::ForcedSnowFreeWrongPhaseFailure
+                    | PreChildRecorderTestModeV1::ForcedSnowFreeRequiredOmissionFailure,
+                PreChildCapturePhaseV1::SnowFree { .. }
+            )
+        );
+        if !phase_matches || state.selected_once {
+            return None;
+        }
+        state.selected_once = true;
+        Some(state.mode)
+    })
+}
+
+#[cfg(not(test))]
+fn test_recorder_selection_v1(_: PreChildCapturePhaseV1<'_>) -> Option<()> {
+    None
+}
+
+#[cfg(test)]
+fn record_test_recorder_result_v1(_input: &PreChildContext<'_>, row: &serde_json::Value) {
+    PRE_CHILD_RECORDER_TEST_STATE_V1.with(|slot| {
+        if let Some(state) = slot.borrow_mut().as_mut() {
+            state.audit.recorded_rows.push(row.clone());
+        }
+    });
+}
+
+#[cfg(test)]
+fn capture_test_direct_run_constructor_inputs_raw(
+    inputs: &crate::DirectRunConstructorInputs,
+) -> serde_json::Value {
+    let lanes = inputs
+        .lanes
+        .iter()
+        .map(|lane| {
+            serde_json::json!({
+                "lane_id": lane.lane_id, "upstream_lane_id": lane.upstream_lane_id,
+                "downstream_lane_id": lane.downstream_lane_id,
+                "upstream_area_ratio": lane.upstream_area_ratio, "area_m2": lane.area_m2,
+                "runoff_publication_q_scale": lane.runoff_publication_q_scale,
+                "runoff_publication_qofe_scale": lane.runoff_publication_qofe_scale,
+                "runoff_publication_efflen_m": lane.runoff_publication_efflen_m,
+                "runoff_publication_cumulative_length_m": lane.runoff_publication_cumulative_length_m,
+                "runoff_publication_ofe_length_m": lane.runoff_publication_ofe_length_m,
+                "water": [lane.water.soil_water_m, lane.water.infiltration_m, lane.water.runoff_m, lane.water.evapotranspiration_m, lane.water.drainage_m, lane.water.lateral_flow_m],
+                "transfer": { "surface_carry_m": lane.transfer.surface_carry_m, "surface_hourly_weights": lane.transfer.surface_hourly_weights, "lateral_carry_m": lane.transfer.lateral_carry_m, "upstream_flow_m": lane.transfer.upstream_flow_m, "subsurface_input_m": lane.transfer.subsurface_input_m },
+                "publication": lane.publication, "subsurface_layers": lane.subsurface_layers,
+                "evapotranspiration_stage_state": lane.evapotranspiration_stage_state,
+                "plant_growth_state": lane.plant_growth_state, "plant_water_stress": lane.plant_water_stress,
+                "winter_snow": crate::DirectSnowRuntimeCarry::from(lane.winter_column.snow.clone()),
+                "winter_frost": crate::DirectFrostRuntimeCarry::from(lane.winter_column.frost.clone()),
+                "snow_runtime_carry": lane.snow_runtime_carry, "frost_runtime_carry": lane.frost_runtime_carry,
+                "day_inputs": lane.day_inputs,
+            })
+        })
+        .collect::<Vec<_>>();
+    serde_json::json!({
+        "identity": { "run_id": inputs.identity.run_id, "hillslope_id": inputs.identity.hillslope_id, "lane_count": inputs.identity.lane_count, "day_count": inputs.identity.day_count },
+        "phase_plan_ranks": inputs.phase_plan.phases().iter().map(|phase| phase.rank()).collect::<Vec<_>>(),
+        "lanes": lanes,
+    })
+}
+
+#[cfg(test)]
+fn capture_test_direct_run_frame_dynamic_raw(
+    frame: &crate::DirectRunFrame,
+    surface_configuration: &crate::DirectSurfaceLiquidConfiguration,
+) -> Result<serde_json::Value, String> {
+    let transfer_operands = |value: crate::DirectRunTransferDownstreamOperands| {
+        serde_json::json!({
+            "lane_count": value.lane_count,
+            "outlet_lane_id": value.outlet_lane_id,
+            "values": [value.total_outgoing_surface_m, value.total_outgoing_lateral_m,
+                value.total_received_surface_m, value.total_received_lateral_m,
+                value.total_net_transfer_m],
+        })
+    };
+    let lanes = frame
+        .lanes
+        .iter()
+        .map(|lane| serde_json::json!({
+            "water": [lane.water.soil_water_m, lane.water.infiltration_m, lane.water.runoff_m,
+                lane.water.evapotranspiration_m, lane.water.drainage_m, lane.water.lateral_flow_m],
+            "subsurface_layers": lane.subsurface_layers,
+            "transfer_surface_carry_m": lane.transfer.surface_carry_m,
+            "transfer_surface_hourly_weights": lane.transfer.surface_hourly_weights,
+            "transfer_lateral_carry_m": lane.transfer.lateral_carry_m,
+            "transfer_scalars": [lane.transfer.upstream_flow_m, lane.transfer.subsurface_input_m],
+            "publication": [lane.publication.runoff_m, lane.publication.infiltration_m,
+                lane.publication.evapotranspiration_m, lane.publication.drainage_m,
+                lane.publication.lateral_flow_m],
+        }))
+        .collect::<Vec<_>>();
+    let lane_transfer_ledger = frame
+        .lane_transfer_ledger
+        .iter()
+        .map(|entry| {
+            serde_json::json!({
+                "lane_id": entry.lane_id,
+                "upstream_lane_id": entry.upstream_lane_id,
+                "downstream_lane_id": entry.downstream_lane_id,
+                "values": [entry.upstream_area_ratio, entry.area_m2, entry.outgoing_surface_m,
+                    entry.outgoing_lateral_m, entry.received_surface_m, entry.received_lateral_m,
+                    entry.net_transfer_m],
+            })
+        })
+        .collect::<Vec<_>>();
+    let groundwater = match frame.groundwater.authority {
+        crate::DirectGroundwaterAuthority::Disabled => serde_json::json!("Disabled"),
+        crate::DirectGroundwaterAuthority::LinearReservoir {
+            initial_storage_depth_m,
+            baseflow_coeff_per_day,
+            deep_seepage_coeff_per_day,
+            baseflow_threshold_area_ha,
+        } => serde_json::json!({"LinearReservoir": {
+            "authority": [initial_storage_depth_m, baseflow_coeff_per_day,
+                deep_seepage_coeff_per_day, baseflow_threshold_area_ha],
+            "state": [frame.groundwater.storage_m3, frame.groundwater.previous_baseflow_m3,
+                frame.groundwater.previous_deep_seepage_m3],
+            "initialized_area_m2": frame.groundwater.initialized_area_m2,
+        }}),
+    };
+    let surface_liquid_canonical_bytes = frame
+        .surface_liquid_shadow
+        .as_deref()
+        .map(|state| {
+            state
+                .canonical_bytes(surface_configuration)
+                .map_err(|error| error.to_string())
+        })
+        .transpose()?;
+    Ok(serde_json::json!({
+        "publication": [frame.publication.runoff_m, frame.publication.infiltration_m,
+            frame.publication.evapotranspiration_m, frame.publication.drainage_m,
+            frame.publication.lateral_flow_m],
+        "lanes": lanes,
+        "lane_transfer_ledger": lane_transfer_ledger,
+        "lane_transfer_downstream_operands": transfer_operands(
+            frame.lane_transfer_downstream_operands),
+        "lane_transfer_shadow_projection": frame.lane_transfer_shadow_projection.map(|value| {
+            serde_json::json!({
+                "lane_count": value.lane_count,
+                "outlet_lane_id": value.outlet_lane_id,
+                "values": [value.total_outgoing_surface_m, value.total_outgoing_lateral_m,
+                    value.total_received_surface_m, value.total_received_lateral_m,
+                    value.total_net_transfer_m],
+            })
+        }),
+        "groundwater": groundwater,
+        "surface_liquid_canonical_bytes": surface_liquid_canonical_bytes,
+    }))
+}
+
+#[cfg(test)]
+fn capture_test_active_snow_raw(
+    value: &crate::hydrology::DirectActiveSnowPartitionInputs,
+) -> ActiveSnowPartitionCaptureV1 {
+    ActiveSnowPartitionCaptureV1 {
+        scalars: [
+            value.hyetograph_rainfall_m,
+            value.rst_c,
+            value.newsnw_kg_m3,
+            value.ssd_kg_m3,
+            value.runtime_swe_m,
+            value.runtime_depth_m,
+            value.runtime_density_kg_m3,
+            value.runtime_settle_day_count,
+            value.liquid_water_retained_m,
+            value.tmax_c,
+            value.tmin_c,
+            value.canopy_cover_fraction,
+            value.wind_m_s,
+            value.dewpoint_c,
+            value.coe_boundary_depth_m,
+            value.coe_boundary_density_kg_m3,
+            value.coe_boundary_settle_day_count,
+        ],
+        snow_melt_model: value.snow_melt_model.id().into(),
+        snow_density_model: value.snow_density_model.id().into(),
+        liquid_routing_model: value.stage3_liquid_routing_model.id().into(),
+        surface_energy: SurfaceEnergyCaptureV1 {
+            longwave: value.surface_energy_options.longwave_model.id().into(),
+            sublimation: value.surface_energy_options.sublimation_model.id().into(),
+            values: [
+                value.surface_energy_options.daily_solar_radiation_mj_m2,
+                value
+                    .surface_energy_options
+                    .daily_extraterrestrial_radiation_mj_m2,
+                value.surface_energy_options.atmospheric_pressure_pa,
+                value
+                    .surface_energy_options
+                    .turbulent_geometry
+                    .air_temperature_height_m,
+                value
+                    .surface_energy_options
+                    .turbulent_geometry
+                    .vapor_pressure_height_m,
+                value
+                    .surface_energy_options
+                    .turbulent_geometry
+                    .wind_speed_height_m,
+                value
+                    .surface_energy_options
+                    .turbulent_geometry
+                    .aerodynamic_roughness_length_m,
+            ],
+            daylight: value.surface_energy_options.daylight,
+            complete_carrier_shadow: value.surface_energy_options.complete_carrier_shadow,
+        },
+        sturm_climate_class: value.sturm_climate_class.map(|x| x.id().into()),
+        sturm_day_of_year: value.sturm_day_of_year,
+        snow_albedo_model: value.snow_albedo_model.map(|x| x.id().into()),
+        snow_albedo_state: value.snow_albedo_state.map(|x| SnowAlbedoCaptureV1 {
+            model: x.model.id().into(),
+            albedo: x.albedo,
+            accumulated_positive_temperature_c_day: x.accumulated_positive_temperature_c_day,
+        }),
+        snow_layers: value.snow_layers.clone(),
+        underlying_surface_albedo: value.underlying_surface_albedo,
+        hourly: value
+            .hourly
+            .iter()
+            .map(|x| SnowHourlyCaptureV1 {
+                values: [
+                    x.active_precipitation_m,
+                    x.rain_m,
+                    x.snowfall_m,
+                    x.radiation_mj_m2,
+                    x.air_temperature_c,
+                    x.cloud_fraction,
+                    x.rain_fraction,
+                    x.snow_fraction,
+                ],
+                phase_model: x.phase_model.id().into(),
+                hydrometeor_temperature_c: x.hydrometeor_temperature_c,
+            })
+            .collect(),
+    }
+}
+
+#[cfg(test)]
+fn capture_test_prepared_support_raw(
+    support: &crate::DirectSnowStage3V11PreparedSupport,
+) -> Result<PreparedSupportCaptureV1, String> {
+    let (
+        active,
+        forcing,
+        snow_free,
+        covered,
+        hard_boundaries,
+        identities,
+        capabilities,
+        open_requests,
+    ) = support.diagnostic_capture_parts_v1()?;
+    Ok(PreparedSupportCaptureV1 {
+        support: support.support(),
+        active_snow_by_lane: active
+            .iter()
+            .map(|(lane, value)| (*lane, capture_test_active_snow_raw(value)))
+            .collect(),
+        support_forcing_by_lane: forcing.iter().map(|(lane, value)| (*lane, SupportForcingCaptureV1 { hourly: SnowHourlyCaptureV1 { values:[value.forcing.active_precipitation_m,value.forcing.rain_m,value.forcing.snowfall_m,value.forcing.radiation_mj_m2,value.forcing.air_temperature_c,value.forcing.cloud_fraction,value.forcing.rain_fraction,value.forcing.snow_fraction],phase_model:value.forcing.phase_model.id().into(),hydrometeor_temperature_c:value.forcing.hydrometeor_temperature_c }, duration_seconds:value.duration_seconds })).collect(),
+        snow_free_interval: snow_free.clone(),
+        covered_interval: CoveredIntervalCaptureV1 { source: crate::v9_real_consumer_shadow::DirectV9ShadowIntervalInput { lse_forcing:covered.lse_forcing.clone(), vegetation_forcing:covered.vegetation_forcing.clone(), wb14_parameters:covered.wb14_parameters.clone() } },
+        hard_boundaries: hard_boundaries.to_vec(),
+        identities_by_lane: identities.iter().map(|(lane, values)| serde_json::to_vec(values).map(|bytes| (*lane, bytes))).collect::<Result<_,_>>().map_err(|error| error.to_string())?,
+        destination_capabilities: capabilities.iter().map(|((ofe, tile), capability)| { let destination=DestinationCaptureV1 { ofe_id:ofe.as_str().to_owned(), tile_id:tile.as_str().to_owned() }; match capability { crate::snow_stage3_open_boundary::SealedStage3TileBoundaryForcingV1::V11CanopyCovered(value) => DestinationCapabilityCaptureV1::Covered { destination, rho_air_kg_m3:value.rho_air_kg_m3, cp_air_j_kg_k:value.cp_air_j_kg_k, effective_canopy_cover:value.effective_canopy_cover }, crate::snow_stage3_open_boundary::SealedStage3TileBoundaryForcingV1::OpenSnow(_) => DestinationCapabilityCaptureV1::Open { destination } } }).collect(),
+        open_snow_destinations: open_requests.iter().map(|(ofe,tile)| DestinationCaptureV1 { ofe_id:ofe.as_str().to_owned(), tile_id:tile.as_str().to_owned() }).collect(),
+    })
+}
+
+#[cfg(test)]
+fn capture_test_snow_free_prepared_support_raw(
+    support: &crate::DirectSnowStage3V11PreparedSupport,
+) -> Result<serde_json::Value, String> {
+    let (active, forcing, snow_free, hard_boundaries, identities, represented, open_requests) =
+        support.diagnostic_snow_free_capture_parts_v1()?;
+    let active = active
+        .iter()
+        .map(|(lane, value)| (*lane, capture_test_active_snow_raw(value)))
+        .collect::<BTreeMap<_, _>>();
+    let forcing = forcing
+        .iter()
+        .map(|(lane, value)| {
+            (
+                *lane,
+                SupportForcingCaptureV1 {
+                    hourly: SnowHourlyCaptureV1 {
+                        values: [
+                            value.forcing.active_precipitation_m,
+                            value.forcing.rain_m,
+                            value.forcing.snowfall_m,
+                            value.forcing.radiation_mj_m2,
+                            value.forcing.air_temperature_c,
+                            value.forcing.cloud_fraction,
+                            value.forcing.rain_fraction,
+                            value.forcing.snow_fraction,
+                        ],
+                        phase_model: value.forcing.phase_model.id().into(),
+                        hydrometeor_temperature_c: value.forcing.hydrometeor_temperature_c,
+                    },
+                    duration_seconds: value.duration_seconds,
+                },
+            )
+        })
+        .collect::<BTreeMap<_, _>>();
+    let identities = identities
+        .iter()
+        .map(|(lane, values)| {
+            serde_json::to_vec(values)
+                .map(|bytes| (*lane, bytes))
+                .map_err(|error| error.to_string())
+        })
+        .collect::<Result<BTreeMap<_, _>, _>>()?;
+    Ok(serde_json::json!({
+        "support": support.support(),
+        "active_snow_by_lane": active,
+        "support_forcing_by_lane": forcing,
+        "snow_free_interval": snow_free,
+        "covered_projection": null,
+        "hard_boundaries": hard_boundaries,
+        "identities_by_lane": identities,
+        "destination_capabilities": [],
+        "destination_capability_count": represented.len(),
+        "open_snow_destinations": [],
+        "open_snow_destination_count": open_requests.len(),
+    }))
+}
+
+#[cfg(test)]
+fn capture_test_soil_trial_raw(
+    trial: &openwepp_land_surface_energy::SoilThermalTrialStateV2,
+) -> serde_json::Value {
+    serde_json::json!({
+        "transaction_id": trial.transaction_id(), "predecessor_transaction_id": trial.predecessor_transaction_id(),
+        "support_start_ns": trial.support_start_ns(), "support_end_ns": trial.support_end_ns(),
+        "beginning_state_sha256": trial.beginning_state_sha256(),
+        "accepted_predecessor_receipt_chain_sha256": trial.accepted_predecessor_receipt_chain_sha256(),
+        "unpublished_predecessor_trial_sha256": trial.unpublished_predecessor_trial_sha256(),
+        "numerical_coordinate_set_sha256": trial.numerical_coordinate_set_sha256(),
+        "numerical_coordinate_authority_sha256": trial.numerical_coordinate_authority_sha256(),
+        "ending_state": trial.ending_state(), "layer_credits": trial.layer_credits(),
+        "unpublished_trial_sha256": trial.unpublished_trial_sha256(),
+    })
+}
+
+#[cfg(test)]
+fn capture_test_ordered_owner_joins(
+    receipts: &[crate::snow_stage3_v11_attachment::Stage3CoupledSubslabReceiptV1],
+) -> serde_json::Value {
+    let rows = receipts
+        .iter()
+        .map(|receipt| {
+            let destination_receipts = receipt
+                .destination_receipts
+                .iter()
+                .map(|((ofe_id, tile_id), boundary_receipt)| {
+                    serde_json::json!({
+                        "ofe_id": ofe_id,
+                        "tile_id": tile_id,
+                        "receipt": boundary_receipt,
+                    })
+                })
+                .collect::<Vec<_>>();
+            let mut row = serde_json::json!({
+                "parent_support": receipt.parent_support,
+                "support": receipt.support,
+                "selected_upper_bound_s_bits": receipt.selected_upper_bound_s_bits,
+                "accepted_slab_sha256": receipt.accepted_slab_sha256,
+                "wb14_replay_trial_sha256": receipt.wb14_replay_trial_sha256,
+                "wb14_replay_beginning_owner_set_sha256": receipt.wb14_replay_beginning_owner_set_sha256,
+                "wb14_child_receipt_set_sha256": receipt.wb14_child_receipt_set_sha256,
+                "wb14_parent_receipt_set_sha256": receipt.wb14_parent_receipt_set_sha256,
+                "wb14_ofe_topology": receipt.wb14_ofe_topology,
+                "wb14_child_replay_bytes": receipt.wb14_child_replay_bytes,
+                "wb14_parent_replay_bytes": receipt.wb14_parent_replay_bytes,
+                "destination_receipts": destination_receipts,
+                "lane_receipts": receipt.lane_receipts,
+                "physical_outcome_ledger_set_sha256": receipt.physical_outcome_ledger_set_sha256,
+                "terminal_events": receipt.terminal_events,
+                "post_support_liquid_receiver_event": receipt.post_support_liquid_receiver_event,
+                "post_support_liquid_output_set_sha256": receipt.post_support_liquid_output_set_sha256,
+                "post_support_liquid_mass_kg_m2_bits": receipt.post_support_liquid_mass_kg_m2_bits,
+                "post_support_liquid_enthalpy_j_m2_bits": receipt.post_support_liquid_enthalpy_j_m2_bits,
+                "post_support_liquid_surface_beginning_state": receipt.post_support_liquid_surface_beginning_state,
+                "post_support_liquid_surface_ending_state": receipt.post_support_liquid_surface_ending_state,
+                "owner_join": receipt.owner_join,
+                "receipt_sha256": receipt.receipt_sha256,
+            });
+            if let Some(custody) = &receipt.post_support_native_custody {
+                row.as_object_mut()
+                    .expect("test owner-join row object")
+                    .insert(
+                        "post_support_native_custody".to_owned(),
+                        serde_json::to_value(custody)
+                            .expect("test live post-support native custody serialization"),
+                    );
+            }
+            row
+        })
+        .collect();
+    serde_json::Value::Array(rows)
+}
+
+#[cfg(test)]
+fn capture_test_phase_live(
+    input: &PreChildProducerSnapshotInputsV1<'_>,
+) -> (&'static str, serde_json::Value) {
+    match input.phase {
+        PreChildCapturePhaseV1::Covered => ("covered", serde_json::Value::Null),
+        PreChildCapturePhaseV1::SnowFree {
+            history,
+            validated_native_inactive_wb14_prefix,
+            ending_snow_owner_bytes,
+            deferred_native_v2_soil_custody,
+        } => (
+            "snow_free",
+            serde_json::json!({
+                "ordered_owner_joins": capture_test_ordered_owner_joins(history.owner_joins),
+                "ordered_owner_join_support_liquid_custody_v2": history.owner_joins.iter().map(|receipt| receipt.support_liquid_custody_v2()).collect::<Vec<_>>(),
+                "ordered_event_groups": history.event_groups,
+                "ordered_event_group_terminal_liquid_custody_v2": history.event_groups.iter().map(|group| group.terminal_receiver_custody_v2()).collect::<Vec<_>>(),
+                "validated_native_inactive_wb14_prefix": validated_native_inactive_wb14_prefix,
+                "current_stage3": history.current_stage3,
+                "current_snow_enthalpy_material_owner": history.current_snow_enthalpy_material_owner,
+                "pending_terminal_parcels": history.pending_terminal_parcels,
+                "ending_snow_owner_canonical_bytes": ending_snow_owner_bytes,
+                "deferred_native_v2_soil_custody": deferred_native_v2_soil_custody.map(|custody| {
+                    let candidate = custody.candidate().v2().expect("test live deferred soil candidate");
+                    let continuation = custody.continuation().map(|continuation| serde_json::json!({
+                        "original_beginning_owner": continuation.original_prepared().beginning_owner(),
+                        "physical_trial": capture_test_soil_trial_raw(continuation.physical_trial()),
+                        "accumulated_operands": continuation.accumulated_operands(),
+                        "ordered_layer_credit_chain": continuation.diagnostic_layer_credit_chain(),
+                    }));
+                    serde_json::json!({
+                        "posture": "reconstructible_operands_not_installable_capability",
+                        "candidate": capture_test_soil_trial_raw(candidate),
+                        "continuation": continuation,
+                    })
+                }),
+            }),
+        ),
+    }
+}
+
+#[cfg(test)]
+fn capture_test_producer_live_row(
+    input: &PreChildProducerSnapshotInputsV1<'_>,
+) -> serde_json::Value {
+    let prepared_support = input.prepared_support.map(|prepared| match input.phase {
+        PreChildCapturePhaseV1::Covered => serde_json::to_value(
+            capture_test_prepared_support_raw(prepared)
+                .expect("test live covered support operands"),
+        )
+        .expect("test live covered support serialization"),
+        PreChildCapturePhaseV1::SnowFree { .. } => {
+            capture_test_snow_free_prepared_support_raw(prepared)
+                .expect("test live snow-free support operands")
+        }
+    });
+    let (phase, phase_live) = capture_test_phase_live(input);
+    serde_json::json!({
+        "capture_phase": phase, "support": input.support,
+        "receipt_canonical_json": serde_json::to_vec(input.receipt).expect("test live receipt serialization"),
+        "binding": { "proposed_upper_bound_s_bits": input.binding.proposed_upper_bound_s_bits, "coupled_parent_transaction_sha256": input.binding.coupled_parent_transaction_sha256, "accepted_slab_sha256": input.binding.accepted_slab_sha256, "parent_beginning_complete_owner_set_sha256": input.binding.parent_beginning_complete_owner_set_sha256, "parent_support_start_ns": input.binding.parent_support_start_ns, "parent_support_end_ns": input.binding.parent_support_end_ns, "child_support_start_ns": input.binding.child_support_start_ns, "child_support_end_ns": input.binding.child_support_end_ns },
+        "prepared_support_constructor_operands": prepared_support,
+        "parent_checkpoint_canonical_json": serde_json::to_vec(&input.parent.checkpoint()).expect("test live parent checkpoint serialization"),
+        "clock_complete_owner_canonical_bytes": input.clock.owners().iter().map(|owner| (owner.owner_id().to_owned(), owner.state_bytes().to_vec())).collect::<BTreeMap<_, _>>(),
+        "complete_owner_canonical_bytes": input.consumer.canonical_v11_parent_owner_state_bytes().expect("test live complete owner bytes"),
+        "native_consumer_constructor_operands": input.consumer.diagnostic_native_consumer_constructor_operands_v1().expect("test live native consumer operands"),
+        "parent_support": input.clock.parent_support(), "accepted_until": input.clock.accepted_until(),
+        "beginning_stage3_canonical_json": serde_json::to_vec(input.beginning_stage3).expect("test live stage3 serialization"),
+        "beginning_snow_enthalpy_material_owner": input.beginning_snow_enthalpy_material_owner,
+        "pending_terminal_parcels": input.pending_terminal_parcels,
+        "terminal_parcels_produced_unconsumed_canonical_json": serde_json::to_vec(input.pending_terminal_parcels).expect("test live terminal parcel serialization"),
+        "constraint_reduction_canonical_json": serde_json::to_vec(input.reduction).expect("test live reduction serialization"),
+        "complete_owner_ledger_canonical_json": serde_json::to_vec(input.ledger).expect("test live ledger serialization"),
+        "forcing_receipt": input.forcing_receipt,
+        "surface_liquid_configuration_canonical_json": input.context.surface_liquid_configuration.canonical_bytes().expect("test live surface configuration canonical bytes"),
+        "vegetation_configuration_canonical_json": serde_json::to_vec(&input.context.vegetation_configuration).expect("test live vegetation configuration serialization"),
+        "lse_configuration_canonical_json": serde_json::to_vec(input.consumer.lse_configuration()).expect("test live LSE configuration serialization"),
+        "provider_cursor_canonical_json": serde_json::to_vec(input.consumer.provider_cursor()).expect("test live provider cursor serialization"),
+        "native_lane_map_constructor_operands": input.consumer.diagnostic_native_lane_maps_v1(),
+        "native_soil_v2_constructor_operands": input.consumer.diagnostic_native_soil_v2().expect("test live native soil diagnostics"),
+        "native_hydrology_frame_dynamic_constructor_operands": capture_test_direct_run_frame_dynamic_raw(input.consumer.restart_authority_hydrology_frame(), &input.context.surface_liquid_configuration).expect("test live hydrology frame dynamic operands"),
+        "native_hydrology_constructor_inputs": capture_test_direct_run_constructor_inputs_raw(input.consumer.diagnostic_hydrology_constructor_inputs_v1().expect("test live hydrology constructor pin").as_ref()),
+        "phase_live": phase_live,
+    })
+}
+
+#[cfg(test)]
+pub(crate) fn record_pre_child_producer_live_inputs_v1(
+    input: &PreChildProducerSnapshotInputsV1<'_>,
+) {
+    PRE_CHILD_RECORDER_TEST_STATE_V1.with(|slot| {
+        let mut slot = slot.borrow_mut();
+        let Some(state) = slot.as_mut() else {
+            return;
+        };
+        state.audit.producer_calls += 1;
+        let selected = matches!(
+            (state.mode, input.phase),
+            (
+                PreChildRecorderTestModeV1::EnabledCovered
+                    | PreChildRecorderTestModeV1::ForcedCoveredCaptureFailure,
+                PreChildCapturePhaseV1::Covered
+            ) | (
+                PreChildRecorderTestModeV1::EnabledSnowFree
+                    | PreChildRecorderTestModeV1::ForcedSnowFreeCaptureFailure
+                    | PreChildRecorderTestModeV1::ForcedSnowFreeWrongPhaseFailure
+                    | PreChildRecorderTestModeV1::ForcedSnowFreeRequiredOmissionFailure,
+                PreChildCapturePhaseV1::SnowFree { .. }
+            )
+        );
+        if selected && !state.producer_recorded {
+            state.producer_recorded = true;
+            state
+                .audit
+                .live_inputs
+                .push(capture_test_producer_live_row(input));
+        }
+    });
+}
+
+#[cfg(not(test))]
+fn record_test_recorder_result_v1(_: &PreChildContext<'_>, _: &serde_json::Value) {}
+
 pub(crate) fn record_pre_child_context(input: PreChildContext<'_>) {
-    if crate::snow_accuracy_observer::physical_global_enabled()
-        && is_target(input.day, input.interval, input.support)
-    {
-        if let Err(detail) = capture(&input) {
-            crate::snow_accuracy_observer::record(
-                serde_json::json!({"kind":"b01_wb14_pre_child_current_context_capture_error_v1","detail":detail,"day_index":input.day,"interval_index":input.interval,"support":input.support}),
-            );
-        }
-    }
-}
-
+    #[cfg(test)]
+    let mut input = input;
+    #[cfg(test)]
+    let test_physical_observer_enabled = PRE_CHILD_RECORDER_TEST_STATE_V1.with(|slot| {
+        slot.borrow().as_ref().is_some_and(|state| {
+            state.mode == PreChildRecorderTestModeV1::ProductionNonTargetObserverEnabled
+        })
+    });
+    #[cfg(not(test))]
+    let test_physical_observer_enabled = false;
+    let production_selected = (crate::snow_accuracy_observer::physical_global_enabled()
+        || test_physical_observer_enabled)
+        && is_target(input.day, input.interval, input.support);
+    let test_mode = test_recorder_selection_v1(input.phase);
+    if !production_selected && test_mode.is_none() {
+        return;
+    }
+    let reported_phase = match input.phase {
+        PreChildCapturePhaseV1::Covered => "covered",
+        PreChildCapturePhaseV1::SnowFree { .. } => "snow_free",
+    };
+    #[cfg(test)]
+    match test_mode {
+        Some(PreChildRecorderTestModeV1::ForcedSnowFreeWrongPhaseFailure) => {
+            input.phase = PreChildCapturePhaseV1::Covered;
+        }
+        Some(PreChildRecorderTestModeV1::ForcedSnowFreeRequiredOmissionFailure) => {
+            input.prepared_support = None;
+        }
+        _ => {}
+    }
+    #[cfg(test)]
+    let forced_failure = matches!(
+        test_mode,
+        Some(
+            PreChildRecorderTestModeV1::ForcedCoveredCaptureFailure
+                | PreChildRecorderTestModeV1::ForcedSnowFreeCaptureFailure
+        )
+    );
+    #[cfg(not(test))]
+    let forced_failure = false;
+    let captured = if forced_failure {
+        Err("forced capture-only recorder failure".to_owned())
+    } else {
+        capture_row(&input)
+    };
+    let row = match captured {
+        Ok(row) => row,
+        Err(detail) => serde_json::json!({
+            "kind":"b01_wb14_pre_child_current_context_capture_error_v1",
+            "capture_phase":reported_phase,
+            "detail":detail,"day_index":input.day,"interval_index":input.interval,"support":input.support
+        }),
+    };
+    record_test_recorder_result_v1(&input, &row);
+    if crate::snow_accuracy_observer::physical_global_enabled() {
+        crate::snow_accuracy_observer::record(row);
+    }
+}
+
+#[cfg(test)]
 pub(crate) fn restore_coupled_clock(
     bytes: &[u8],
     model: openwepp_coupled_time::Digest32,
@@ -1494,6 +2570,7 @@
 
 /// Restores the enclosing parent through its existing configured BGC scope.
 /// The configuration is supplied independently by the real runner fixture.
+#[cfg(test)]
 pub(crate) fn restore_parent_with_configured_bgc_scope(
     bytes: &[u8],
     vegetation: &openwepp_vegetation::VegetationConfigurationV11,
@@ -1512,6 +2589,7 @@
 
 /// Replays the existing provisional constructor on a clone only. Callers retain
 /// the beginning clock and can compare it byte-for-byte after this returns.
+#[cfg(test)]
 pub(crate) fn reconstruct_provisional_on_clone(
     clock: &CoupledClockStateV1,
     support: TimeSupport,
@@ -1526,15 +2604,72 @@
     ),
     String,
 > {
+    reconstruct_provisional_on_clone_for_constraint_owner(
+        clock,
+        support,
+        context,
+        forcing_receipt,
+        "v11-snow-covered-real-consumer",
+    )
+}
+
+/// Reconstruct a provisional receipt for the recorded phase only.  The source
+/// owner is part of the receipt preimage, so a covered recorder receipt cannot
+/// be used to validate the snow-free native restoration (or conversely).
+#[cfg(test)]
+fn reconstruct_provisional_on_clone_for_constraint_owner(
+    clock: &CoupledClockStateV1,
+    support: TimeSupport,
+    context: &DirectSnowStage3V11StaticContext,
+    forcing_receipt: openwepp_coupled_time::Digest32,
+    constraint_source_owner_id: &str,
+) -> Result<
+    (
+        CoupledClockStateV1,
+        ConstraintReductionReceiptV1,
+        LedgerEntryV1,
+        AcceptedSlabReceiptV1,
+    ),
+    String,
+> {
+    reconstruct_provisional_on_clone_with_authorities(
+        clock,
+        support,
+        context.calendar_receipt,
+        context.controller_policy,
+        forcing_receipt,
+        constraint_source_owner_id,
+    )
+}
+
+/// Diagnostic-only minimal authority form for a captured provisional receipt.
+/// It deliberately has no physical configuration or consumer dependency.
+#[cfg(test)]
+fn reconstruct_provisional_on_clone_with_authorities(
+    clock: &CoupledClockStateV1,
+    support: TimeSupport,
+    calendar_receipt: openwepp_coupled_time::Digest32,
+    controller_policy: openwepp_coupled_time::Digest32,
+    forcing_receipt: openwepp_coupled_time::Digest32,
+    constraint_source_owner_id: &str,
+) -> Result<
+    (
+        CoupledClockStateV1,
+        ConstraintReductionReceiptV1,
+        LedgerEntryV1,
+        AcceptedSlabReceiptV1,
+    ),
+    String,
+> {
     let parent = clock.parent_transaction_id();
     let constraint = StepConstraintV1::new(
         parent,
         support.start_ns(),
         support.end_ns(),
-        "v11-snow-covered-real-consumer".into(),
+        constraint_source_owner_id.into(),
         ConstraintClass::HardBoundary,
-        context.controller_policy,
-        context.calendar_receipt,
+        controller_policy,
+        calendar_receipt,
         forcing_receipt,
     )
     .map_err(|e| e.to_string())?;
@@ -1578,11 +2713,60 @@
 /// Consume the captured diagnostic row in a fresh process. It performs only
 /// canonical restoration and provisional construction; no consumer, ingress,
 /// WB14, or constitutive operation is called here.
+#[cfg(test)]
 pub(crate) fn import_and_reconstruct_pre_child_context(
     row: &serde_json::Value,
     context: &DirectSnowStage3V11StaticContext,
     consumer: &DirectV10RealConsumerShadow,
 ) -> Result<(), String> {
+    let inputs = restore_pre_child_provisional_inputs(row, context)?;
+    let before = inputs
+        .restored
+        .to_canonical_json()
+        .map_err(|e| e.to_string())?;
+    let (_, reduction, ledger, receipt) = reconstruct_provisional_on_clone_for_constraint_owner(
+        &inputs.beginning_clock,
+        inputs.support,
+        context,
+        inputs.forcing,
+        inputs.expected_owner,
+    )?;
+    if inputs
+        .restored
+        .to_canonical_json()
+        .map_err(|e| e.to_string())?
+        != before
+    {
+        return Err("provisional clone mutated restored beginning clock".into());
+    }
+    validate_pre_child_provisional_evidence(row, &reduction, &ledger, &receipt)?;
+    validate_pre_child_binding_and_owners(
+        row,
+        &inputs.parent,
+        &inputs.beginning_clock,
+        inputs.support,
+        inputs.parent_support,
+        &receipt,
+        consumer,
+    )
+}
+
+#[cfg(test)]
+struct PreChildProvisionalInputsV1 {
+    restored: CoupledTimeRestartV2,
+    beginning_clock: CoupledClockStateV1,
+    parent: V11ParentTransaction,
+    support: TimeSupport,
+    parent_support: TimeSupport,
+    expected_owner: &'static str,
+    forcing: openwepp_coupled_time::Digest32,
+}
+
+#[cfg(test)]
+fn restore_pre_child_provisional_inputs(
+    row: &serde_json::Value,
+    context: &DirectSnowStage3V11StaticContext,
+) -> Result<PreChildProvisionalInputsV1, String> {
     let get = |name: &str| {
         row.get(name)
             .ok_or_else(|| format!("capture missing {name}"))
@@ -1645,8 +2829,16 @@
         return Err("captured clock/support parent join".into());
     }
     let constructor = get("provisional_constructor_inputs")?;
-    if constructor.get("constraint_source_owner_id")
-        != Some(&serde_json::json!("v11-snow-covered-real-consumer"))
+    let snow_free =
+        get("kind")? == &serde_json::json!("b01_wb14_snow_free_pre_child_current_context_v1");
+    let expected_phase = if snow_free { "snow_free" } else { "covered" };
+    let expected_owner = if snow_free {
+        "v11-real-consumer"
+    } else {
+        "v11-snow-covered-real-consumer"
+    };
+    if get("capture_phase")? != &serde_json::json!(expected_phase)
+        || constructor.get("constraint_source_owner_id") != Some(&serde_json::json!(expected_owner))
         || constructor.get("constraint_class") != Some(&serde_json::json!("hard_boundary"))
         || constructor.get("calendar_receipt") != Some(&serde_json::json!(context.calendar_receipt))
         || constructor.get("controller_policy")
@@ -1665,12 +2857,28 @@
             .clone(),
     )
     .map_err(|e| e.to_string())?;
-    let before = restored.to_canonical_json().map_err(|e| e.to_string())?;
-    let (_, reduction, ledger, receipt) =
-        reconstruct_provisional_on_clone(&beginning_clock, support, context, forcing)?;
-    if restored.to_canonical_json().map_err(|e| e.to_string())? != before {
-        return Err("provisional clone mutated restored beginning clock".into());
-    }
+    Ok(PreChildProvisionalInputsV1 {
+        restored,
+        beginning_clock,
+        parent,
+        support,
+        parent_support,
+        expected_owner,
+        forcing,
+    })
+}
+
+#[cfg(test)]
+fn validate_pre_child_provisional_evidence(
+    row: &serde_json::Value,
+    reduction: &ConstraintReductionReceiptV1,
+    ledger: &LedgerEntryV1,
+    receipt: &AcceptedSlabReceiptV1,
+) -> Result<(), String> {
+    let get = |name: &str| {
+        row.get(name)
+            .ok_or_else(|| format!("capture missing {name}"))
+    };
     let expected: Vec<u8> =
         serde_json::from_value(get("provisional_receipt_canonical_json")?.clone())
             .map_err(|e| e.to_string())?;
@@ -1689,6 +2897,23 @@
     if serde_json::to_vec(&receipt).map_err(|e| e.to_string())? != expected {
         return Err("provisional receipt durable coordinates".into());
     }
+    Ok(())
+}
+
+#[cfg(test)]
+fn validate_pre_child_binding_and_owners(
+    row: &serde_json::Value,
+    parent: &V11ParentTransaction,
+    beginning_clock: &CoupledClockStateV1,
+    support: TimeSupport,
+    parent_support: TimeSupport,
+    receipt: &AcceptedSlabReceiptV1,
+    consumer: &DirectV10RealConsumerShadow,
+) -> Result<(), String> {
+    let get = |name: &str| {
+        row.get(name)
+            .ok_or_else(|| format!("capture missing {name}"))
+    };
     let beginning_owner_digest =
         complete_owner_set_digest(beginning_clock.owners()).map_err(|e| e.to_string())?;
     let binding = get("coupled_binding")?;
@@ -1741,28 +2966,11 @@
     Ok(())
 }
 
-/// File-consumer entrypoint for the provider-bound part of a capture.  The
-/// caller supplies the independently restored provider day; the row supplies
-/// only recorded constructor operands.  This seam deliberately returns the
-/// sealed support so a fresh-process probe owns the reconstructed context.
-pub(crate) fn restore_captured_prepared_support_from_provider(
-    row: &serde_json::Value,
-    provider: &crate::runtime_inputs::PreparedSnowFreeGsiDayV1,
-) -> Result<crate::DirectSnowStage3V11PreparedSupport, String> {
-    let captured: PreparedSupportCaptureV1 = serde_json::from_value(
-        row.get("prepared_support_constructor_operands")
-            .ok_or("capture missing prepared-support constructor operands")?
-            .clone(),
-    )
-    .map_err(|error| error.to_string())?;
-    let support = restore_prepared_support(captured, provider)?;
-    Ok(support)
-}
-
 /// Owned, provider-bound portion of the file consumer.  It takes only capture
 /// rows and the independently pinned static authority; callers cannot smuggle
 /// a live provider or prepared support across the diagnostic boundary.
 #[cfg(feature = "persisted-restart-v1")]
+#[cfg(test)]
 pub(crate) struct RestoredProviderBoundContextV1 {
     /// The complete, authenticated 48-support day.  Keeping this capability
     /// is essential: a selected child alone cannot carry the provider cursor
@@ -1784,21 +2992,22 @@
 }
 
 #[cfg(feature = "persisted-restart-v1")]
+#[cfg(test)]
 pub(crate) struct AuthenticatedArchivedDayV1 {
     pub entry: crate::Stage3CommittedDayArchiveEntryV1,
-    pub canonical_record: Vec<u8>,
     pub parent_receipt: crate::DirectSnowStage3V11ParentReceipt,
     pub publication: crate::v9_real_consumer_shadow::RestoredStage3PublicationDayEvidenceV1,
-    pub qualification_delta: crate::SnowStage3V11QualificationDayDeltaV1,
 }
 
 #[cfg(feature = "persisted-restart-v1")]
+#[cfg(test)]
 struct CommittedDayFrameReaderV1<'a> {
     bytes: &'a [u8],
     cursor: usize,
 }
 
 #[cfg(feature = "persisted-restart-v1")]
+#[cfg(test)]
 impl<'a> CommittedDayFrameReaderV1<'a> {
     fn take(&mut self, length: usize) -> Result<&'a [u8], String> {
         let end = self
@@ -1832,26 +3041,82 @@
 }
 
 #[cfg(feature = "persisted-restart-v1")]
+#[cfg(test)]
 fn authenticate_archived_day_v1(
     entry: crate::Stage3CommittedDayArchiveEntryV1,
-    canonical_record: Vec<u8>,
+    canonical_record: &[u8],
     context: &DirectSnowStage3V11StaticContext,
+    pinned_lse_configuration: &openwepp_land_surface_energy::LandSurfaceEnergyConfiguration,
+) -> Result<AuthenticatedArchivedDayV1, String> {
+    authenticate_archived_day_with_configuration_v1(
+        entry,
+        canonical_record,
+        context.run_identity,
+        context.topology_identity,
+        &context.vegetation_configuration,
+        pinned_lse_configuration,
+    )
+}
+
+#[cfg(all(test, feature = "persisted-restart-v1"))]
+fn validate_archive_first_positive_join(
+    parent_receipt: &crate::DirectSnowStage3V11ParentReceipt,
+    publication: &crate::v9_real_consumer_shadow::RestoredStage3PublicationDayEvidenceV1,
+) -> Result<(), String> {
+    let mut positive = parent_receipt
+        .coupled_subslabs
+        .iter()
+        .map(|receipt| {
+            (
+                receipt.support,
+                receipt.owner_join.beginning_complete_owner_set_sha256,
+            )
+        })
+        .chain(
+            parent_receipt
+                .snow_free_successor_receipts
+                .iter()
+                .map(|receipt| (receipt.support, receipt.beginning_complete_owner_set_sha256)),
+        )
+        .collect::<Vec<_>>();
+    positive.sort_by_key(|(support, _)| support.start_ns());
+    let (first_support, first_beginning) = positive
+        .first()
+        .ok_or("archive parent positive support missing")?;
+    if positive
+        .get(1)
+        .is_some_and(|(support, _)| support.start_ns() == first_support.start_ns())
+        || *first_support != publication.first_positive_support
+        || *first_beginning != publication.first_positive_beginning_owner
+    {
+        return Err("archive parent first-positive beginning/publication join".into());
+    }
+    Ok(())
+}
+
+#[cfg(all(test, feature = "persisted-restart-v1"))]
+fn authenticate_archived_day_with_configuration_v1(
+    entry: crate::Stage3CommittedDayArchiveEntryV1,
+    canonical_record: &[u8],
+    run_identity: openwepp_coupled_time::Digest32,
+    topology_identity: openwepp_coupled_time::Digest32,
+    vegetation_configuration: &openwepp_vegetation::VegetationConfigurationV11,
     pinned_lse_configuration: &openwepp_land_surface_energy::LandSurfaceEnergyConfiguration,
 ) -> Result<AuthenticatedArchivedDayV1, String> {
     entry.validate().map_err(|error| error.to_string())?;
     if u64::try_from(canonical_record.len()).ok() != Some(entry.canonical_uncompressed_len)
-        || digest_bytes(&canonical_record) != entry.content_sha256
+        || digest_bytes(canonical_record) != entry.content_sha256
     {
         return Err("captured canonical archive record content identity".into());
     }
     let mut reader = CommittedDayFrameReaderV1 {
-        bytes: &canonical_record,
+        bytes: canonical_record,
         cursor: 0,
     };
     reader.exact(b"OPENWEPP_STAGE3_COMMITTED_DAY_EVIDENCE_V1\0")?;
     reader.exact(&entry.schema_version.to_be_bytes())?;
-    reader.exact(context.run_identity.as_bytes())?;
-    reader.exact(context.topology_identity.as_bytes())?;
+    reader.exact(run_identity.as_bytes())?;
+    reader.exact(topology_identity.as_bytes())?;
     reader.exact(
         &u64::try_from(entry.day_index)
             .map_err(|_| "archive day width")?
@@ -1865,7 +3130,7 @@
     let parent_receipt = crate::snow_stage3_v11_attachment::
         diagnostic_restore_parent_receipt_archive_with_configured_scope_v3(
             parent_bytes,
-            &context.vegetation_configuration,
+            vegetation_configuration,
             pinned_lse_configuration,
         )
         .map_err(|error| error.to_string())?;
@@ -1878,6 +3143,7 @@
             publication_bytes,
         )
         .map_err(|error| error.to_string())?;
+    validate_archive_first_positive_join(&parent_receipt, &publication)?;
     reader.exact(entry.committed_publication_receipt_sha256.as_bytes())?;
     reader.exact(publication.ordered_support_receipt_set_sha256.as_bytes())?;
     reader.exact(entry.beginning_owner_set_sha256.as_bytes())?;
@@ -1916,10 +3182,8 @@
     }
     Ok(AuthenticatedArchivedDayV1 {
         entry,
-        canonical_record,
         parent_receipt,
         publication,
-        qualification_delta,
     })
 }
 
@@ -1928,6 +3192,7 @@
 /// DTO: neither implements serde and each must be admitted through its
 /// private native constructor rather than reconstructed by a generic wire
 /// deserializer.
+#[cfg(test)]
 pub(crate) struct RestoredCapturedPreChildContextV1 {
     pub parent: V11ParentTransaction,
     pub clock: CoupledClockStateV1,
@@ -1942,10 +3207,11 @@
     pub binding: serde_json::Value,
 }
 
-fn restore_captured_pre_child_context(
+#[cfg(test)]
+fn restore_captured_clock_parent(
     row: &serde_json::Value,
     context: &DirectSnowStage3V11StaticContext,
-) -> Result<RestoredCapturedPreChildContextV1, String> {
+) -> Result<(CoupledClockStateV1, V11ParentTransaction), String> {
     let get = |name: &str| {
         row.get(name)
             .ok_or_else(|| format!("capture missing {name}"))
@@ -1990,6 +3256,19 @@
     parent
         .restore_evaluator_cache_from_accepted_slabs(clock.accepted_slab_receipts())
         .map_err(|error| error.to_string())?;
+    Ok((clock, parent))
+}
+
+#[cfg(test)]
+fn restore_captured_pre_child_context(
+    row: &serde_json::Value,
+    context: &DirectSnowStage3V11StaticContext,
+) -> Result<RestoredCapturedPreChildContextV1, String> {
+    let get = |name: &str| {
+        row.get(name)
+            .ok_or_else(|| format!("capture missing {name}"))
+    };
+    let (clock, parent) = restore_captured_clock_parent(row, context)?;
     let beginning_stage3_bytes: Vec<u8> =
         serde_json::from_value(get("beginning_stage3_canonical_json")?.clone())
             .map_err(|error| error.to_string())?;
@@ -2044,8 +3323,24 @@
             .clone(),
     )
     .map_err(|error| error.to_string())?;
-    let (_, reduction, ledger, receipt) =
-        reconstruct_provisional_on_clone(&clock, support, context, forcing)?;
+    let expected_owner = match row.get("kind") {
+        Some(kind)
+            if kind == &serde_json::json!("b01_wb14_snow_free_pre_child_current_context_v1") =>
+        {
+            "v11-real-consumer"
+        }
+        Some(kind) if kind == &serde_json::json!("b01_wb14_pre_child_current_context_v1") => {
+            "v11-snow-covered-real-consumer"
+        }
+        _ => return Err("captured pre-child phase kind".into()),
+    };
+    let (_, reduction, ledger, receipt) = reconstruct_provisional_on_clone_for_constraint_owner(
+        &clock,
+        support,
+        context,
+        forcing,
+        expected_owner,
+    )?;
     if serde_json::to_vec(&reduction).map_err(|error| error.to_string())? != reduction_bytes
         || serde_json::to_vec(&ledger).map_err(|error| error.to_string())? != ledger_bytes
         || serde_json::to_vec(&receipt).map_err(|error| error.to_string())? != receipt_bytes
@@ -2065,7 +3360,113 @@
     })
 }
 
+/// A native inactive-prefix proof authenticates its supplied receipt graph but
+/// cannot establish that the graph came from this restored clock.  Bind every
+/// recorded join and terminal event back to the accepted history before the
+/// proof is compared with the recorded result.
+#[cfg(all(test, feature = "persisted-restart-v1"))]
+fn validate_snow_free_prefix_membership_v1(
+    row: &serde_json::Value,
+    clock: &CoupledClockStateV1,
+    surface_configuration: &crate::DirectSurfaceLiquidConfiguration,
+) -> Result<(), String> {
+    let diagnostics = row
+        .get("phase_diagnostics")
+        .ok_or("snow-free phase diagnostics")?;
+    let joins =
+        crate::snow_stage3_v11_attachment::diagnostic_restore_coupled_subslab_receipts_value_v2(
+            diagnostics
+                .get("ordered_owner_joins")
+                .ok_or("snow-free ordered owner joins")?
+                .clone(),
+        )?;
+    let groups: Vec<crate::snow_stage3_v11_attachment::Stage3V11TerminalEventGroupV1> =
+        serde_json::from_value(
+            diagnostics
+                .get("ordered_event_groups")
+                .ok_or("snow-free ordered event groups")?
+                .clone(),
+        )
+        .map_err(|error| error.to_string())?;
+    let parent_support: TimeSupport = serde_json::from_value(
+        diagnostics
+            .get("prefix_validation_parent_support")
+            .ok_or("snow-free prefix parent support")?
+            .clone(),
+    )
+    .map_err(|error| error.to_string())?;
+    if joins.len() != 8 || groups.len() != 1 {
+        return Err("snow-free prefix cardinality".into());
+    }
+    for join in &joins {
+        let matches = clock
+            .accepted_slab_receipts()
+            .iter()
+            .filter(|slab| {
+                if slab.support() != join.owner_join.support
+                    || slab.slab_id().digest() != join.owner_join.accepted_slab_sha256
+                    || slab.parent_transaction_id().digest()
+                        != join.owner_join.parent_transaction_sha256
+                {
+                    return false;
+                }
+                let value = serde_json::to_value(slab).ok();
+                value
+                    .as_ref()
+                    .and_then(|value| value.get("begin_owner_set"))
+                    == Some(&serde_json::json!(
+                        join.owner_join.beginning_complete_owner_set_sha256
+                    ))
+                    && value.as_ref().and_then(|value| value.get("end_owner_set"))
+                        == Some(&serde_json::json!(
+                            join.owner_join.ending_complete_owner_set_sha256
+                        ))
+            })
+            .count();
+        if matches != 1 {
+            return Err("snow-free prefix accepted-slab membership".into());
+        }
+    }
+    for group in &groups {
+        let accepted = group
+            .accepted_event_receipt
+            .as_ref()
+            .ok_or("snow-free terminal accepted event")?;
+        let matches = clock
+            .accepted_event_receipts()
+            .iter()
+            .filter(|event| {
+                event.id() == accepted.id()
+                    && event.tick() == accepted.tick()
+                    && event.ordinal() == accepted.ordinal()
+                    && event.parent_transaction_id() == accepted.parent_transaction_id()
+                    && event.beginning_owner_set_digest() == accepted.beginning_owner_set_digest()
+                    && event.ending_owner_set_digest() == accepted.ending_owner_set_digest()
+            })
+            .count();
+        if matches != 1 {
+            return Err("snow-free prefix accepted-event membership".into());
+        }
+    }
+    let validated = crate::direct_runtime::validate_native_inactive_wb14_prefix_v1(
+        &joins,
+        &groups,
+        parent_support,
+        surface_configuration,
+    )
+    .map_err(|error| error.to_string())?;
+    if serde_json::to_value(validated).map_err(|error| error.to_string())?
+        != *diagnostics
+            .get("validated_native_inactive_wb14_prefix")
+            .ok_or("snow-free recorded prefix proof")?
+    {
+        return Err("snow-free recomputed prefix proof comparison".into());
+    }
+    Ok(())
+}
+
 #[cfg(feature = "persisted-restart-v1")]
+#[cfg(test)]
 pub(crate) fn restore_provider_bound_context_from_rows(
     rows: &[serde_json::Value],
     context: &DirectSnowStage3V11StaticContext,
@@ -2074,10 +3475,61 @@
     pinned_frozen_litter_lse_configuration: &openwepp_land_surface_energy::LandSurfaceEnergyConfiguration,
     pinned_hydrology_constructor_inputs: crate::DirectRunConstructorInputs,
 ) -> Result<RestoredProviderBoundContextV1, String> {
+    let (target, day) = select_provider_capture_rows(rows, context)?;
+    let prepared = restore_provider_prepared_parts(target, day, pinned_provider_configuration)?;
+    let parent_support = prepared
+        .restored_day
+        .supports()
+        .get(B01_INTERVAL_INDEX)
+        .ok_or("captured parent support index missing")?;
+    let (captured, forcing_receipt) = restore_provider_current_child(
+        target,
+        context,
+        &prepared.provider,
+        &prepared.prepared_support,
+        parent_support,
+        prepared.target_support,
+    )?;
+    let consumer = restore_provider_native_consumer(
+        target,
+        day,
+        context,
+        &captured,
+        ReaderNativePinsV1 {
+            provider: pinned_provider_configuration,
+            lse: pinned_lse_configuration,
+            frozen_lse: pinned_frozen_litter_lse_configuration,
+            hydrology: pinned_hydrology_constructor_inputs,
+        },
+    )?;
+    let snow_enthalpy_material_resident = restore_provider_snow_material(day, &captured)?;
+    let (archived_receipt_prefix, authenticated_archived_days) =
+        restore_provider_archive_context(rows, day, context, pinned_lse_configuration)?;
+    Ok(RestoredProviderBoundContextV1 {
+        prepared_day: prepared.restored_day,
+        prepared_support: prepared.prepared_support,
+        forcing_receipt,
+        consumer,
+        archived_receipt_prefix,
+        snow_enthalpy_material_resident,
+        authenticated_archived_days,
+        captured,
+    })
+}
+
+#[cfg(all(test, feature = "persisted-restart-v1"))]
+fn select_provider_capture_rows<'a>(
+    rows: &'a [serde_json::Value],
+    context: &DirectSnowStage3V11StaticContext,
+) -> Result<(&'a serde_json::Value, &'a serde_json::Value), String> {
     let target = rows
         .iter()
         .filter(|row| {
-            row.get("kind") == Some(&serde_json::json!("b01_wb14_pre_child_current_context_v1"))
+            (row.get("kind") == Some(&serde_json::json!("b01_wb14_pre_child_current_context_v1"))
+                || row.get("kind")
+                    == Some(&serde_json::json!(
+                        "b01_wb14_snow_free_pre_child_current_context_v1"
+                    )))
                 && row.get("day_index") == Some(&serde_json::json!(B01_DAY_INDEX))
                 && row.get("interval_index") == Some(&serde_json::json!(B01_INTERVAL_INDEX))
                 && row.get("static_run_identity") == Some(&serde_json::json!(context.run_identity))
@@ -2104,6 +3556,22 @@
     let [day] = day.as_slice() else {
         return Err("captured prepared-day row missing or duplicate".into());
     };
+    Ok((target, day))
+}
+
+#[cfg(all(test, feature = "persisted-restart-v1"))]
+struct ProviderPreparedPartsV1 {
+    provider: crate::runtime_inputs::PreparedSnowFreeGsiDayV1,
+    restored_day: crate::ValidatedPreparedStage3V11DayV1,
+    prepared_support: crate::DirectSnowStage3V11PreparedSupport,
+    target_support: TimeSupport,
+}
+#[cfg(all(test, feature = "persisted-restart-v1"))]
+fn restore_provider_prepared_parts(
+    target: &serde_json::Value,
+    day: &serde_json::Value,
+    pinned_provider_configuration: &crate::runtime_inputs::SnowFreeHalfHourStaticConfiguration,
+) -> Result<ProviderPreparedPartsV1, String> {
     let provider: ProviderCaptureV1 = serde_json::from_value(
         day.get("provider_constructor_operands")
             .ok_or("captured provider operands missing")?
@@ -2168,6 +3636,29 @@
     {
         return Err("captured child/parent support projection join".into());
     }
+    Ok(ProviderPreparedPartsV1 {
+        provider,
+        restored_day,
+        prepared_support,
+        target_support,
+    })
+}
+
+#[cfg(all(test, feature = "persisted-restart-v1"))]
+fn restore_provider_current_child(
+    target: &serde_json::Value,
+    context: &DirectSnowStage3V11StaticContext,
+    provider: &crate::runtime_inputs::PreparedSnowFreeGsiDayV1,
+    prepared_support: &crate::DirectSnowStage3V11PreparedSupport,
+    parent_support: &crate::DirectSnowStage3V11PreparedSupport,
+    target_support: TimeSupport,
+) -> Result<
+    (
+        RestoredCapturedPreChildContextV1,
+        openwepp_coupled_time::Digest32,
+    ),
+    String,
+> {
     let beginning_stage3_bytes: Vec<u8> = serde_json::from_value(
         target
             .get("beginning_stage3_canonical_json")
@@ -2206,7 +3697,7 @@
         .map_err(|error| error.to_string())?
         .retain_active_snow_lanes(&active_lanes)
         .map_err(|error| error.to_string())?;
-    if projected_child != prepared_support {
+    if &projected_child != prepared_support {
         return Err("captured child canonical adaptive projection".into());
     }
     let forcing_receipt = crate::snow_stage3_v11_attachment::canonical_parent_forcing_digest(
@@ -2225,6 +3716,24 @@
     {
         return Err("captured provider-derived forcing receipt".into());
     }
+    Ok((captured, forcing_receipt))
+}
+
+#[cfg(all(test, feature = "persisted-restart-v1"))]
+struct ReaderNativePinsV1<'a> {
+    provider: &'a crate::runtime_inputs::SnowFreeHalfHourStaticConfiguration,
+    lse: &'a openwepp_land_surface_energy::LandSurfaceEnergyConfiguration,
+    frozen_lse: &'a openwepp_land_surface_energy::LandSurfaceEnergyConfiguration,
+    hydrology: crate::DirectRunConstructorInputs,
+}
+#[cfg(all(test, feature = "persisted-restart-v1"))]
+fn restore_provider_native_consumer(
+    target: &serde_json::Value,
+    day: &serde_json::Value,
+    context: &DirectSnowStage3V11StaticContext,
+    captured: &RestoredCapturedPreChildContextV1,
+    pins: ReaderNativePinsV1<'_>,
+) -> Result<DirectV10RealConsumerShadow, String> {
     let hydrology_capture: DirectRunFrameDynamicCaptureV1 = serde_json::from_value(
         target
             .get("native_hydrology_frame_dynamic_constructor_operands")
@@ -2234,7 +3743,7 @@
     .map_err(|error| error.to_string())?;
     let hydrology_frame = restore_direct_run_frame_dynamic(
         hydrology_capture,
-        pinned_hydrology_constructor_inputs.clone(),
+        pins.hydrology.clone(),
         &context.surface_liquid_configuration,
     )?;
     let target_soil = serde_json::from_value(
@@ -2267,13 +3776,22 @@
         &captured.clock,
         hydrology_frame.clone(),
         &context.surface_liquid_configuration,
-        pinned_lse_configuration,
-        pinned_frozen_litter_lse_configuration,
-        pinned_provider_configuration,
-        std::sync::Arc::new(pinned_hydrology_constructor_inputs),
+        pins.lse,
+        pins.frozen_lse,
+        pins.provider,
+        std::sync::Arc::new(pins.hydrology),
     )
     .map_err(|error| error.to_string())?;
     import_and_reconstruct_pre_child_context(target, context, &consumer)?;
+    Ok(consumer)
+}
+
+#[cfg(all(test, feature = "persisted-restart-v1"))]
+fn restore_provider_snow_material(
+    day: &serde_json::Value,
+    captured: &RestoredCapturedPreChildContextV1,
+) -> Result<crate::snow_stage3_v11_attachment::SnowStage3V11SnowEnthalpyMaterialResidentV1, String>
+{
     let day_material_residents: crate::snow_stage3_v11_attachment::
         SnowStage3V11SnowEnthalpyMaterialResidentSetV1 =
         serde_json::from_value(
@@ -2320,6 +3838,27 @@
     {
         return Err("captured snow-enthalpy current/base material owner join".into());
     }
+    Ok(
+        crate::snow_stage3_v11_attachment::SnowStage3V11SnowEnthalpyMaterialResidentV1 {
+            current_owner: current_snow_owner,
+            accepted_owner_chronology: snow_chronology,
+        },
+    )
+}
+
+#[cfg(all(test, feature = "persisted-restart-v1"))]
+fn restore_provider_archive_context(
+    rows: &[serde_json::Value],
+    day: &serde_json::Value,
+    context: &DirectSnowStage3V11StaticContext,
+    pinned_lse_configuration: &openwepp_land_surface_energy::LandSurfaceEnergyConfiguration,
+) -> Result<
+    (
+        crate::Stage3ArchivedReceiptPrefixV1,
+        Vec<AuthenticatedArchivedDayV1>,
+    ),
+    String,
+> {
     let archived_next_parent_sequence = serde_json::from_value(
         day.get("committed_next_parent_sequence")
             .ok_or("captured next parent sequence missing")?
@@ -2335,82 +3874,8 @@
             .clone(),
     )
     .map_err(|error| error.to_string())?;
-    let archived_rows = rows
-        .iter()
-        .filter(|row| {
-            row.get("kind")
-                == Some(&serde_json::json!(
-                    "b01_wb14_committed_day_archive_record_v1"
-                ))
-                && row.get("static_run_identity") == Some(&serde_json::json!(context.run_identity))
-                && row.get("static_topology_identity")
-                    == Some(&serde_json::json!(context.topology_identity))
-        })
-        .collect::<Vec<_>>();
-    if archived_rows.len() != B01_DAY_INDEX {
-        return Err("captured committed-day archive record cardinality".into());
-    }
-    let mut archive_manifest = crate::Stage3CommittedDayArchiveManifestV1::empty(
-        context.run_identity,
-        context.topology_identity,
-    )
-    .map_err(|error| error.to_string())?;
-    let mut authenticated_archived_days: Vec<AuthenticatedArchivedDayV1> =
-        Vec::with_capacity(B01_DAY_INDEX);
-    for expected_day in 0..B01_DAY_INDEX {
-        let matches = archived_rows
-            .iter()
-            .filter(|row| row.get("day_index") == Some(&serde_json::json!(expected_day)))
-            .collect::<Vec<_>>();
-        let [row] = matches.as_slice() else {
-            return Err("captured committed-day archive record missing or duplicate".into());
-        };
-        let entry: crate::Stage3CommittedDayArchiveEntryV1 = serde_json::from_value(
-            row.get("archive_entry")
-                .ok_or("captured archive entry missing")?
-                .clone(),
-        )
-        .map_err(|error| error.to_string())?;
-        let canonical_record: Vec<u8> = serde_json::from_value(
-            row.get("canonical_record")
-                .ok_or("captured canonical archive record missing")?
-                .clone(),
-        )
-        .map_err(|error| error.to_string())?;
-        let authenticated = authenticate_archived_day_v1(
-            entry.clone(),
-            canonical_record,
-            context,
-            pinned_lse_configuration,
-        )?;
-        if let Some(previous) = authenticated_archived_days.last() {
-            if authenticated.publication.prefix_support_count
-                != previous.publication.cumulative_support_count
-                || authenticated.publication.prefix_event_count
-                    != previous.publication.cumulative_event_count
-                || authenticated.publication.prefix_authority_sha256
-                    != previous.publication.cumulative_authority_sha256
-                || authenticated.publication.prefix_ending_owner_set_sha256
-                    != Some(previous.publication.ending_owner_set_sha256)
-            {
-                return Err("captured archived publication prefix chronology".into());
-            }
-        } else if authenticated.publication.prefix_support_count != 0
-            || authenticated.publication.prefix_event_count != 0
-            || authenticated.publication.prefix_authority_sha256
-                != digest_bytes(b"OPENWEPP_ACCEPTED_PUBLICATION_INCREMENTAL_AUTHORITY_V1\0")
-            || authenticated
-                .publication
-                .prefix_ending_owner_set_sha256
-                .is_some()
-        {
-            return Err("captured archived publication genesis prefix".into());
-        }
-        archive_manifest
-            .append(entry)
-            .map_err(|error| error.to_string())?;
-        authenticated_archived_days.push(authenticated);
-    }
+    let (archive_manifest, authenticated_archived_days) =
+        restore_provider_archived_rows(rows, context, pinned_lse_configuration)?;
     let committed_clock_restart_bytes: Vec<u8> = serde_json::from_value(
         day.get("committed_clock_restart_canonical_json")
             .ok_or("captured committed clock restart missing")?
@@ -2443,24 +3908,111 @@
     {
         return Err("captured archive prefix/day-start owner-clock join".into());
     }
-    let snow_enthalpy_material_resident =
-        crate::snow_stage3_v11_attachment::SnowStage3V11SnowEnthalpyMaterialResidentV1 {
-            current_owner: current_snow_owner,
-            accepted_owner_chronology: snow_chronology,
+    Ok((archived_receipt_prefix, authenticated_archived_days))
+}
+
+#[cfg(all(test, feature = "persisted-restart-v1"))]
+fn restore_provider_archived_rows(
+    rows: &[serde_json::Value],
+    context: &DirectSnowStage3V11StaticContext,
+    pinned_lse_configuration: &openwepp_land_surface_energy::LandSurfaceEnergyConfiguration,
+) -> Result<
+    (
+        crate::Stage3CommittedDayArchiveManifestV1,
+        Vec<AuthenticatedArchivedDayV1>,
+    ),
+    String,
+> {
+    let archived_rows = rows
+        .iter()
+        .filter(|row| {
+            row.get("kind")
+                == Some(&serde_json::json!(
+                    "b01_wb14_committed_day_archive_record_v1"
+                ))
+                && row.get("static_run_identity") == Some(&serde_json::json!(context.run_identity))
+                && row.get("static_topology_identity")
+                    == Some(&serde_json::json!(context.topology_identity))
+        })
+        .collect::<Vec<_>>();
+    if archived_rows.len() != B01_DAY_INDEX {
+        return Err("captured committed-day archive record cardinality".into());
+    }
+    let mut archive_manifest = crate::Stage3CommittedDayArchiveManifestV1::empty(
+        context.run_identity,
+        context.topology_identity,
+    )
+    .map_err(|error| error.to_string())?;
+    let mut authenticated_archived_days: Vec<AuthenticatedArchivedDayV1> =
+        Vec::with_capacity(B01_DAY_INDEX);
+    for expected_day in 0..B01_DAY_INDEX {
+        let matches = archived_rows
+            .iter()
+            .filter(|row| row.get("day_index") == Some(&serde_json::json!(expected_day)))
+            .collect::<Vec<_>>();
+        let [row] = matches.as_slice() else {
+            return Err("captured committed-day archive record missing or duplicate".into());
         };
-    Ok(RestoredProviderBoundContextV1 {
-        prepared_day: restored_day,
-        prepared_support,
-        forcing_receipt,
-        consumer,
-        archived_receipt_prefix,
-        snow_enthalpy_material_resident,
-        authenticated_archived_days,
-        captured,
-    })
+        let entry: crate::Stage3CommittedDayArchiveEntryV1 = serde_json::from_value(
+            row.get("archive_entry")
+                .ok_or("captured archive entry missing")?
+                .clone(),
+        )
+        .map_err(|error| error.to_string())?;
+        let canonical_record: Vec<u8> = serde_json::from_value(
+            row.get("canonical_record")
+                .ok_or("captured canonical archive record missing")?
+                .clone(),
+        )
+        .map_err(|error| error.to_string())?;
+        let authenticated = authenticate_archived_day_v1(
+            entry.clone(),
+            &canonical_record,
+            context,
+            pinned_lse_configuration,
+        )?;
+        validate_provider_publication_prefix(authenticated_archived_days.last(), &authenticated)?;
+        archive_manifest
+            .append(entry)
+            .map_err(|error| error.to_string())?;
+        authenticated_archived_days.push(authenticated);
+    }
+    Ok((archive_manifest, authenticated_archived_days))
+}
+
+#[cfg(all(test, feature = "persisted-restart-v1"))]
+fn validate_provider_publication_prefix(
+    previous: Option<&AuthenticatedArchivedDayV1>,
+    authenticated: &AuthenticatedArchivedDayV1,
+) -> Result<(), String> {
+    if let Some(previous) = previous {
+        if authenticated.publication.prefix_support_count
+            != previous.publication.cumulative_support_count
+            || authenticated.publication.prefix_event_count
+                != previous.publication.cumulative_event_count
+            || authenticated.publication.prefix_authority_sha256
+                != previous.publication.cumulative_authority_sha256
+            || authenticated.publication.prefix_ending_owner_set_sha256
+                != Some(previous.publication.ending_owner_set_sha256)
+        {
+            return Err("captured archived publication prefix chronology".into());
+        }
+    } else if authenticated.publication.prefix_support_count != 0
+        || authenticated.publication.prefix_event_count != 0
+        || authenticated.publication.prefix_authority_sha256
+            != digest_bytes(b"OPENWEPP_ACCEPTED_PUBLICATION_INCREMENTAL_AUTHORITY_V1\0")
+        || authenticated
+            .publication
+            .prefix_ending_owner_set_sha256
+            .is_some()
+    {
+        return Err("captured archived publication genesis prefix".into());
+    }
+    Ok(())
 }
 
 #[cfg(feature = "persisted-restart-v1")]
+#[cfg(test)]
 pub(crate) fn restore_provider_bound_context_from_file(
     path: &std::path::Path,
     context: &DirectSnowStage3V11StaticContext,
@@ -2495,3 +4047,1879 @@
         pinned_hydrology_constructor_inputs,
     )
 }
+
+/// Decode one member-backed exported row without rebuilding the original
+/// observation stream.  The exporter retains map-key events so duplicate keys
+/// are refused instead of silently becoming last-key-wins JSON.  Callers name
+/// the large members they deliberately do not need; this keeps chronology and
+/// parent validation usable before native-consumer decoding.
+#[cfg(test)]
+fn attach_member_backed_value_v1(
+    stack: &mut [serde_json::Value],
+    keys: &mut [Option<String>],
+    root: &mut Option<serde_json::Value>,
+    value: serde_json::Value,
+) -> Result<(), String> {
+    let Some(parent) = stack.last_mut() else {
+        if root.replace(value).is_some() {
+            return Err("member-backed row has multiple roots".into());
+        }
+        return Ok(());
+    };
+    match parent {
+        serde_json::Value::Array(values) => values.push(value),
+        serde_json::Value::Object(values) => {
+            let key = keys
+                .last_mut()
+                .and_then(Option::take)
+                .ok_or("member-backed object value lacks key")?;
+            if values.insert(key, value).is_some() {
+                return Err("member-backed row duplicate object member".into());
+            }
+        }
+        _ => return Err("member-backed row scalar container".into()),
+    }
+    Ok(())
+}
+
+#[cfg(test)]
+fn validate_member_backed_external_end_v1(
+    item: &serde_json::Value,
+    pending_external: &mut Option<(String, usize, openwepp_coupled_time::Digest32)>,
+) -> Result<(), String> {
+    let (file, bytes, digest) = pending_external
+        .take()
+        .ok_or("member-backed unexpected external JSON end")?;
+    let expected_file = item
+        .get("file")
+        .and_then(serde_json::Value::as_str)
+        .ok_or("member-backed external end file")?;
+    let expected_bytes = item
+        .get("bytes")
+        .and_then(serde_json::Value::as_u64)
+        .and_then(|value| usize::try_from(value).ok())
+        .ok_or("member-backed external end bytes")?;
+    let expected_digest: openwepp_coupled_time::Digest32 = serde_json::from_value(
+        item.get("sha256")
+            .ok_or("member-backed external end digest")?
+            .clone(),
+    )
+    .map_err(|error| error.to_string())?;
+    if file != expected_file || bytes != expected_bytes || digest != expected_digest {
+        return Err("member-backed external payload identity".into());
+    }
+    Ok(())
+}
+
+#[cfg(test)]
+fn decode_member_backed_scalar_event_v1(
+    item: &serde_json::Value,
+    event: &str,
+) -> Result<serde_json::Value, String> {
+    Ok(match event {
+        "start_map" => serde_json::Value::Object(serde_json::Map::new()),
+        "start_array" => serde_json::Value::Array(Vec::new()),
+        "string" | "boolean" => item.get("value").cloned().ok_or("member-backed scalar")?,
+        "null" => serde_json::Value::Null,
+        "number" => {
+            let lexeme = item
+                .get("value")
+                .and_then(|value| value.get("number_lexeme"))
+                .and_then(serde_json::Value::as_str)
+                .ok_or("member-backed number lexeme")?;
+            serde_json::from_str(lexeme).map_err(|error| error.to_string())?
+        }
+        _ => return Err("member-backed unsupported event".into()),
+    })
+}
+
+#[cfg(test)]
+fn decode_member_backed_row_v1(
+    export_root: &std::path::Path,
+    event_path: &std::path::Path,
+    omitted_members: &BTreeSet<&str>,
+) -> Result<serde_json::Value, String> {
+    let raw = std::fs::File::open(event_path).map_err(|error| error.to_string())?;
+    let reader = std::io::BufReader::new(raw);
+    let mut stack = Vec::<serde_json::Value>::new();
+    let mut keys = Vec::<Option<String>>::new();
+    let mut root = None;
+    let mut pending_external = None::<(String, usize, openwepp_coupled_time::Digest32)>;
+    let mut skipped_external = false;
+    for line in std::io::BufRead::lines(reader) {
+        let item: serde_json::Value =
+            serde_json::from_str(&line.map_err(|error| error.to_string())?)
+                .map_err(|error| error.to_string())?;
+        let event = item
+            .get("event")
+            .and_then(serde_json::Value::as_str)
+            .ok_or("member-backed event kind")?;
+        if event == "map_key" {
+            let key = item
+                .get("value")
+                .and_then(serde_json::Value::as_str)
+                .ok_or("member-backed map key")?;
+            if !matches!(stack.last(), Some(serde_json::Value::Object(_))) {
+                return Err("member-backed map key outside object".into());
+            }
+            *keys.last_mut().ok_or("member-backed key stack")? = Some(key.into());
+            continue;
+        }
+        if event == "external_json_start" {
+            let member = item
+                .get("path")
+                .and_then(serde_json::Value::as_array)
+                .and_then(|path| path.first())
+                .and_then(|entry| entry.get("member"))
+                .and_then(serde_json::Value::as_str)
+                .ok_or("member-backed external member path")?;
+            if omitted_members.contains(member) {
+                skipped_external = true;
+                continue;
+            }
+            let file = item
+                .get("file")
+                .and_then(serde_json::Value::as_str)
+                .ok_or("member-backed external file")?;
+            let payload =
+                std::fs::read(export_root.join(file)).map_err(|error| error.to_string())?;
+            let value = serde_json::from_slice(&payload).map_err(|error| error.to_string())?;
+            pending_external = Some((file.into(), payload.len(), digest_bytes(&payload)));
+            attach_member_backed_value_v1(&mut stack, &mut keys, &mut root, value)?;
+            continue;
+        }
+        if event == "external_json_end" {
+            if skipped_external {
+                skipped_external = false;
+                continue;
+            }
+            validate_member_backed_external_end_v1(&item, &mut pending_external)?;
+            continue;
+        }
+        if event == "external_byte_array_start" || event == "external_byte_array_end" {
+            continue;
+        }
+        if event == "end_map" || event == "end_array" {
+            let value = stack
+                .pop()
+                .ok_or("member-backed unexpected container end")?;
+            keys.pop().ok_or("member-backed key stack end")?;
+            attach_member_backed_value_v1(&mut stack, &mut keys, &mut root, value)?;
+            continue;
+        }
+        let value = decode_member_backed_scalar_event_v1(&item, event)?;
+        if event == "start_map" || event == "start_array" {
+            stack.push(value);
+            keys.push(None);
+        } else {
+            attach_member_backed_value_v1(&mut stack, &mut keys, &mut root, value)?;
+        }
+    }
+    if !stack.is_empty() {
+        return Err("member-backed truncated event stream".into());
+    }
+    if pending_external.is_some() || skipped_external {
+        return Err("member-backed missing external JSON end".into());
+    }
+    root.ok_or("member-backed row missing root".into())
+}
+
+#[cfg(feature = "persisted-restart-v1")]
+#[cfg(test)]
+pub(crate) struct RestoredSnowFreeClockParentV1 {
+    pub clock: CoupledClockStateV1,
+    pub parent: V11ParentTransaction,
+    recorded_row: serde_json::Value,
+}
+
+/// Small, independent first component for a member-backed export.  It opens
+/// neither the native consumer operand nor archive records and is therefore
+/// suitable for reporting a parent/clock refusal before any large decode.
+#[cfg(feature = "persisted-restart-v1")]
+#[cfg(test)]
+pub(crate) fn restore_snow_free_clock_parent_from_member_export(
+    export_root: &std::path::Path,
+) -> Result<RestoredSnowFreeClockParentV1, String> {
+    let summary: serde_json::Value = serde_json::from_slice(
+        &std::fs::read(export_root.join("summary.json")).map_err(|error| error.to_string())?,
+    )
+    .map_err(|error| error.to_string())?;
+    if summary.get("status") != Some(&serde_json::json!("COMPLETE")) {
+        return Err("member-backed export is not complete".into());
+    }
+    let rows = summary
+        .get("rows")
+        .and_then(serde_json::Value::as_array)
+        .ok_or("member-backed export rows")?;
+    let matches = rows
+        .iter()
+        .filter(|entry| {
+            entry
+                .get("kinds")
+                .and_then(serde_json::Value::as_array)
+                .is_some_and(|kinds| {
+                    kinds
+                        .iter()
+                        .any(|kind| kind == "b01_wb14_snow_free_pre_child_current_context_v1")
+                })
+        })
+        .collect::<Vec<_>>();
+    let [target] = matches.as_slice() else {
+        return Err("member-backed snow-free target missing or duplicate".into());
+    };
+    let event_file = target
+        .get("event_file")
+        .and_then(serde_json::Value::as_str)
+        .ok_or("member-backed target event path")?;
+    let event_bytes =
+        std::fs::read(export_root.join(event_file)).map_err(|error| error.to_string())?;
+    let expected_event_digest: openwepp_coupled_time::Digest32 = serde_json::from_value(
+        target
+            .get("event_file_sha256")
+            .ok_or("member-backed target event digest")?
+            .clone(),
+    )
+    .map_err(|error| error.to_string())?;
+    if target
+        .get("event_file_bytes")
+        .and_then(serde_json::Value::as_u64)
+        .and_then(|value| usize::try_from(value).ok())
+        != Some(event_bytes.len())
+        || digest_bytes(&event_bytes) != expected_event_digest
+    {
+        return Err("member-backed target event identity".into());
+    }
+    let omitted = BTreeSet::from(["native_consumer_constructor_operands"]);
+    let row = decode_member_backed_row_v1(export_root, &export_root.join(event_file), &omitted)?;
+    if row.get("kind")
+        != Some(&serde_json::json!(
+            "b01_wb14_snow_free_pre_child_current_context_v1"
+        ))
+        || row.get("capture_phase") != Some(&serde_json::json!("snow_free"))
+    {
+        return Err("member-backed snow-free target phase".into());
+    }
+    let get = |name| {
+        row.get(name)
+            .ok_or_else(|| format!("capture missing {name}"))
+    };
+    let model = serde_json::from_value(get("coupled_clock_restart_model_sha256")?.clone())
+        .map_err(|error| error.to_string())?;
+    let authority = serde_json::from_value(get("coupled_clock_restart_authority_sha256")?.clone())
+        .map_err(|error| error.to_string())?;
+    let policy = serde_json::from_value(get("coupled_clock_restart_policy_sha256")?.clone())
+        .map_err(|error| error.to_string())?;
+    let clock_bytes: Vec<u8> =
+        serde_json::from_value(get("coupled_clock_restart_canonical_json")?.clone())
+            .map_err(|error| error.to_string())?;
+    let clock = restore_coupled_clock(&clock_bytes, model, authority, policy)?
+        .clock()
+        .clone();
+    let vegetation_bytes: Vec<u8> =
+        serde_json::from_value(get("vegetation_configuration_canonical_json")?.clone())
+            .map_err(|error| error.to_string())?;
+    let vegetation =
+        serde_json::from_slice(&vegetation_bytes).map_err(|error| error.to_string())?;
+    let lse_bytes: Vec<u8> =
+        serde_json::from_value(get("lse_configuration_canonical_json")?.clone())
+            .map_err(|error| error.to_string())?;
+    let lse = serde_json::from_slice(&lse_bytes).map_err(|error| error.to_string())?;
+    let parent_bytes: Vec<u8> =
+        serde_json::from_value(get("parent_checkpoint_canonical_json")?.clone())
+            .map_err(|error| error.to_string())?;
+    let parent = restore_parent_with_configured_bgc_scope(&parent_bytes, &vegetation, &lse)?;
+    if parent.parent_transaction_id() != clock.parent_transaction_id() {
+        return Err("member-backed restored parent/clock identity".into());
+    }
+    Ok(RestoredSnowFreeClockParentV1 {
+        clock,
+        parent,
+        recorded_row: row,
+    })
+}
+
+/// Localize a `ChildIdentity` refusal without changing the canonical prefix
+/// validator or turning any failed prerequisite into acceptance.
+#[cfg(all(test, feature = "persisted-restart-v1"))]
+fn diagnose_snow_free_prefix_receipt_gates_v1(
+    row: &serde_json::Value,
+    surface_configuration: &crate::DirectSurfaceLiquidConfiguration,
+) -> Result<(), String> {
+    let diagnostics = row
+        .get("phase_diagnostics")
+        .ok_or("snow-free phase diagnostics")?;
+    let receipts =
+        crate::snow_stage3_v11_attachment::diagnostic_restore_coupled_subslab_receipts_value_v2(
+            diagnostics
+                .get("ordered_owner_joins")
+                .ok_or("snow-free ordered owner joins")?
+                .clone(),
+        )?;
+    for (index, receipt) in receipts.iter().enumerate() {
+        for ((ofe_id, tile_id), boundary) in &receipt.destination_receipts {
+            boundary.validate().map_err(|error| {
+                format!("gate boundary.validate[{index},{ofe_id},{tile_id:?}]: {error}")
+            })?;
+        }
+        for (lane_id, lane) in &receipt.lane_receipts {
+            lane.validate()
+                .map_err(|error| format!("gate lane.validate[{index},{lane_id}]: {error}"))?;
+        }
+        receipt
+            .validate()
+            .map_err(|error| format!("gate receipt.validate[{index}]: {error}"))?;
+        if receipt.wb14_ofe_topology != surface_configuration.ofe_topology {
+            return Err(format!("gate WB14 topology[{index}]"));
+        }
+        crate::direct_runtime::stage3_covered_native_inactive_child_custody_binding(
+            &receipt.wb14_child_replay_bytes,
+            &surface_configuration.ofe_topology,
+        )
+        .map_err(|error| format!("gate WB14 child binding[{index}]: {error}"))?
+        .ok_or_else(|| format!("gate WB14 child binding absent[{index}]"))?;
+    }
+    Ok(())
+}
+
+#[cfg(all(test, feature = "persisted-restart-v1"))]
+pub(crate) mod member_backed_restore_tests {
+    use super::*;
+
+    pub(crate) struct ProbeAudit(
+        Option<openwepp_land_surface_energy::solver_mechanism_audit::Session>,
+    );
+
+    impl ProbeAudit {
+        pub(crate) fn begin() -> Self {
+            crate::direct_runtime::reset_direct_runtime_audit_counters();
+            Self(Some(
+                openwepp_land_surface_energy::solver_mechanism_audit::begin_mechanism_audit(
+                    false, 0,
+                )
+                .expect("begin diagnostic mechanism audit"),
+            ))
+        }
+    }
+
+    impl Drop for ProbeAudit {
+        fn drop(&mut self) {
+            let mechanism = self.0.take().expect("audit session").finish();
+            let direct = crate::direct_runtime::direct_runtime_audit_snapshot();
+            let native =
+                crate::v9_real_consumer_shadow::take_covered_native_physical_path_audit_v1();
+            println!("probe-mechanism-audit: {mechanism:?}");
+            println!("probe-direct-audit: {direct:?}");
+            println!("probe-native-physics-audit: {native:?}");
+            if !std::thread::panicking() {
+                let mechanism = mechanism.expect("complete diagnostic mechanism audit");
+                assert_eq!(mechanism.maps.starts, 0);
+                assert_eq!(mechanism.solves.starts, 0);
+                assert_eq!(mechanism.sweeps.starts, 0);
+                assert_eq!(mechanism.probes.starts, 0);
+                assert_eq!(mechanism.evaluations.starts, 0);
+                assert_eq!(mechanism.leaf_calls, 0);
+                assert_eq!(direct.day_frame_commits, 0);
+                assert_eq!(direct.publication_capture_runs, 0);
+                assert_eq!(direct.direct_compute_operations, 0);
+                assert_eq!(direct.phase_span_runs, 0);
+                assert_eq!(native.snow_free_litter_physics_call_count, 0);
+                assert_eq!(native.snow_free_surface_physics_call_count, 0);
+                assert_eq!(native.snow_free_wb14_physics_call_count, 0);
+            }
+        }
+    }
+
+    #[test]
+    fn recorded_archive_manifest_rejects_semantic_order_and_namespace_changes() {
+        let export = std::path::PathBuf::from(
+            std::env::var_os("OPENWEPP_B01_MEMBER_EXPORT").expect("export"),
+        );
+        let seed = std::env::var_os("OPENWEPP_B01_PINNED_SEED").expect("seed");
+        let pin = extract_pinned_seed_configuration_v1(std::path::Path::new(&seed)).expect("pin");
+        let _native_audit =
+            crate::v9_real_consumer_shadow::begin_covered_native_physical_path_audit_v1();
+        let _probe_audit = ProbeAudit::begin();
+        let summary: serde_json::Value =
+            serde_json::from_slice(&std::fs::read(export.join("summary.json")).expect("summary"))
+                .expect("summary JSON");
+        let mut entries = Vec::new();
+        for entry in summary["rows"].as_array().expect("rows") {
+            if entry["kinds"] != serde_json::json!(["b01_wb14_committed_day_archive_record_v1"]) {
+                continue;
+            }
+            let row = decode_member_backed_row_v1(
+                &export,
+                &export.join(entry["event_file"].as_str().expect("event file")),
+                &BTreeSet::new(),
+            )
+            .expect("archive row");
+            let archive: crate::Stage3CommittedDayArchiveEntryV1 =
+                serde_json::from_value(row["archive_entry"].clone()).expect("entry");
+            archive
+                .validate()
+                .expect("each original entry is independently sealed");
+            entries.push(archive);
+        }
+        assert_eq!(entries.len(), 4);
+        let empty = crate::Stage3CommittedDayArchiveManifestV1::empty(
+            pin.run_identity,
+            pin.topology_identity,
+        )
+        .expect("empty manifest");
+        let mut positive = empty.clone();
+        for entry in &entries {
+            positive.append(entry.clone()).expect("recorded order");
+        }
+        positive.validate().expect("complete canonical manifest");
+        let day = decode_member_backed_row_v1(
+            &export,
+            &export.join("rows/108033.events.jsonl"),
+            &BTreeSet::new(),
+        )
+        .expect("day");
+        let prefix: crate::Stage3ArchivedReceiptPrefixV1 =
+            serde_json::from_value(day["archived_receipt_prefix"].clone()).expect("prefix");
+        assert_eq!(
+            positive.ordered_day_chain_sha256,
+            prefix.ordered_day_chain_sha256
+        );
+        assert_eq!(
+            positive.archive_content_root_sha256,
+            prefix.archive_content_root_sha256
+        );
+        assert_eq!(positive.committed_day_count, prefix.archived_day_count);
+        for (name, sequence) in [
+            ("omitted first", vec![1]),
+            ("duplicate", vec![0, 0]),
+            ("reordered", vec![0, 2, 1]),
+            ("omitted middle", vec![0, 1, 3]),
+        ] {
+            let mut candidate = empty.clone();
+            let result = sequence.into_iter().try_for_each(|index| {
+                candidate
+                    .append(entries[index].clone())
+                    .map_err(|error| error.to_string())
+            });
+            println!("manifest poison {name}: {result:?}");
+            assert!(
+                result.as_ref().is_err_and(
+                    |error| error.contains("archive manifest append order or prior root")
+                )
+            );
+            candidate
+                .validate()
+                .expect("failed append preserves admitted prefix");
+        }
+        check_foreign_manifest_namespaces(&pin, &entries[0]);
+    }
+
+    fn check_foreign_manifest_namespaces(
+        pin: &PinnedSeedConfigurationV1,
+        entry: &crate::Stage3CommittedDayArchiveEntryV1,
+    ) {
+        for (name, run, topology) in [
+            (
+                "run namespace",
+                digest_bytes(b"foreign-run"),
+                pin.topology_identity,
+            ),
+            (
+                "topology namespace",
+                pin.run_identity,
+                digest_bytes(b"foreign-topology"),
+            ),
+        ] {
+            let mut foreign = crate::Stage3CommittedDayArchiveManifestV1::empty(run, topology)
+                .expect("valid foreign namespace");
+            let result = foreign.append(entry.clone());
+            println!("manifest poison {name}: {result:?}");
+            assert!(matches!(
+                result,
+                Err(crate::DirectSnowStage3V11AttachmentError::Identity(
+                    "archive manifest append order or prior root"
+                ))
+            ));
+        }
+    }
+
+    #[test]
+    fn archive_first_positive_beginning_rejects_pre_event_owner_substitution() {
+        let export = std::path::PathBuf::from(
+            std::env::var_os("OPENWEPP_B01_MEMBER_EXPORT").expect("export"),
+        );
+        let seed = std::env::var_os("OPENWEPP_B01_PINNED_SEED").expect("seed");
+        let pin = extract_pinned_seed_configuration_v1(std::path::Path::new(&seed)).expect("pin");
+        let _native_audit =
+            crate::v9_real_consumer_shadow::begin_covered_native_physical_path_audit_v1();
+        let _probe_audit = ProbeAudit::begin();
+        openwepp_land_surface_energy::snow_accuracy_policy::Policy::B01
+            .install()
+            .expect("policy");
+        let target = restore_snow_free_clock_parent_from_member_export(&export).expect("target");
+        let bytes: Vec<u8> = serde_json::from_value(
+            target.recorded_row["vegetation_configuration_canonical_json"].clone(),
+        )
+        .expect("vegetation bytes");
+        let vegetation: openwepp_vegetation::VegetationConfigurationV11 =
+            serde_json::from_slice(&bytes).expect("vegetation");
+        assert_eq!(vegetation.imported_v10, pin.vegetation);
+        let row = decode_member_backed_row_v1(
+            &export,
+            &export.join("rows/27431.events.jsonl"),
+            &BTreeSet::new(),
+        )
+        .expect("archive row");
+        let entry = serde_json::from_value(row["archive_entry"].clone()).expect("entry");
+        let raw =
+            std::fs::read(export.join("rows/27431.canonical_record.bin")).expect("actual archive");
+        let restored = authenticate_archived_day_with_configuration_v1(
+            entry,
+            &raw,
+            pin.run_identity,
+            pin.topology_identity,
+            &vegetation,
+            &pin.lse,
+        )
+        .expect("fully authenticated archive zero");
+        validate_archive_first_positive_join(&restored.parent_receipt, &restored.publication)
+            .expect("positive cross-object join");
+        assert_ne!(
+            restored.entry.beginning_owner_set_sha256,
+            restored.publication.first_positive_beginning_owner,
+            "day zero has an admitted event before its first support"
+        );
+        let mut substituted = restored.publication.clone();
+        substituted.first_positive_beginning_owner = restored.entry.beginning_owner_set_sha256;
+        let result = validate_archive_first_positive_join(&restored.parent_receipt, &substituted);
+        println!("archive beginning semantic substitution: {result:?}");
+        assert_eq!(
+            result.as_ref().map_err(String::as_str),
+            Err("archive parent first-positive beginning/publication join")
+        );
+    }
+
+    #[test]
+    fn provider_authority_scalars_reject_semantic_substitutions_without_installation() {
+        let export = std::path::PathBuf::from(
+            std::env::var_os("OPENWEPP_B01_MEMBER_EXPORT").expect("export"),
+        );
+        let _native_audit =
+            crate::v9_real_consumer_shadow::begin_covered_native_physical_path_audit_v1();
+        let _probe_audit = ProbeAudit::begin();
+        openwepp_land_surface_energy::snow_accuracy_policy::Policy::B01
+            .install()
+            .expect("policy");
+        let day = decode_member_backed_row_v1(
+            &export,
+            &export.join("rows/108033.events.jsonl"),
+            &BTreeSet::new(),
+        )
+        .expect("day");
+        let prefix: crate::Stage3ArchivedReceiptPrefixV1 =
+            serde_json::from_value(day["archived_receipt_prefix"].clone()).expect("prefix");
+        prefix.validate().expect("prefix validates");
+        let target = restore_snow_free_clock_parent_from_member_export(&export).expect("target");
+        let interval: u128 = serde_json::from_value(target.recorded_row["interval_index"].clone())
+            .expect("interval");
+        let position = prefix
+            .next_parent_sequence
+            .checked_add(interval)
+            .expect("derived position");
+        let expected_gsi =
+            day["provider_constructor_operands"]["gsi_receipt"]["receipt_sha256"].clone();
+        // This is a scalar-join fixture, never a native object or installable capability.
+        let mut operands = serde_json::json!({"accepted_interval_count": position, "provider_gsi_receipt_sha256": expected_gsi});
+        assert_eq!(
+            member_restoration::native_authority_joins(&operands, position, &expected_gsi)
+                .expect("well-typed scalar fixture"),
+            (true, true)
+        );
+        operands["accepted_interval_count"] = serde_json::json!(position + 1);
+        assert_eq!(
+            member_restoration::native_authority_joins(&operands, position, &expected_gsi)
+                .expect("well-typed position poison"),
+            (false, true)
+        );
+        operands["accepted_interval_count"] = serde_json::json!(position);
+        operands["provider_gsi_receipt_sha256"] =
+            serde_json::json!(digest_bytes(b"foreign-gsi-receipt"));
+        assert_eq!(
+            member_restoration::native_authority_joins(&operands, position, &expected_gsi)
+                .expect("well-typed GSI poison"),
+            (true, false)
+        );
+        println!(
+            "provider scalar join controls: matching, shifted position and foreign GSI checked; no native installation"
+        );
+    }
+
+    #[test]
+    fn current_phase_members_validate_without_physics() {
+        let export = std::env::var_os("OPENWEPP_B01_MEMBER_EXPORT").expect("bound member export");
+        let export = std::path::Path::new(&export);
+        let _native_audit =
+            crate::v9_real_consumer_shadow::begin_covered_native_physical_path_audit_v1();
+        let _probe_audit = ProbeAudit::begin();
+        openwepp_land_surface_energy::snow_accuracy_policy::Policy::B01
+            .install()
+            .expect("policy");
+        let target =
+            restore_snow_free_clock_parent_from_member_export(export).expect("clock parent");
+        let day = decode_member_backed_row_v1(
+            export,
+            &export.join("rows/108033.events.jsonl"),
+            &BTreeSet::new(),
+        )
+        .expect("prepared day");
+        let base: u128 = serde_json::from_value(day["committed_next_parent_sequence"].clone())
+            .expect("sequence");
+        let interval: u128 = serde_json::from_value(target.recorded_row["interval_index"].clone())
+            .expect("interval");
+        member_restoration::validate_current_phase(
+            &target.recorded_row,
+            &target.clock,
+            base + interval,
+        )
+        .expect("phase current/deferred joins");
+    }
+
+    #[test]
+    fn current_phase_semantic_substitutions_refuse_without_physics() {
+        let export = std::env::var_os("OPENWEPP_B01_MEMBER_EXPORT").expect("bound member export");
+        let export = std::path::Path::new(&export);
+        let _native_audit =
+            crate::v9_real_consumer_shadow::begin_covered_native_physical_path_audit_v1();
+        let _probe_audit = ProbeAudit::begin();
+        openwepp_land_surface_energy::snow_accuracy_policy::Policy::B01
+            .install()
+            .expect("policy");
+        let target =
+            restore_snow_free_clock_parent_from_member_export(export).expect("clock parent");
+        let day = decode_member_backed_row_v1(
+            export,
+            &export.join("rows/108033.events.jsonl"),
+            &BTreeSet::new(),
+        )
+        .expect("prepared day");
+        let base: u128 = serde_json::from_value(day["committed_next_parent_sequence"].clone())
+            .expect("sequence");
+        let interval: u128 = serde_json::from_value(target.recorded_row["interval_index"].clone())
+            .expect("interval");
+        let position = base + interval;
+        let original = &target.recorded_row;
+        member_restoration::validate_current_phase(original, &target.clock, position)
+            .expect("positive phase control");
+        let parcel = original["phase_diagnostics"]["pending_terminal_parcels"]
+            .as_object()
+            .expect("parcel map")
+            .keys()
+            .next()
+            .expect("parcel");
+        let cases = [
+            ("current position", "/phase_diagnostics/current_stage3/1/next_interval_index".to_owned(), serde_json::json!(position+1), "current Stage3 accepted position"),
+            ("current state", "/phase_diagnostics/current_stage3/1/cumulative_melt_kg_m2".to_owned(), serde_json::json!(14.0), "current Stage3 ending snow projection"),
+            ("parcel custody", format!("/phase_diagnostics/pending_terminal_parcels/{parcel}/mass_kg_m2_tile_ground"), serde_json::json!(2.0), "current terminal parcel custody posture"),
+            ("trial transaction", "/phase_diagnostics/deferred_native_v2_soil_custody/candidate/transaction_id".to_owned(), serde_json::json!(5775), "deferred trial transaction/support"),
+            ("trial predecessor", "/phase_diagnostics/deferred_native_v2_soil_custody/candidate/predecessor_transaction_id".to_owned(), serde_json::json!(5772), "deferred unpublished trial seal"),
+            ("trial custody variant", "/phase_diagnostics/deferred_native_v2_soil_custody/candidate/accepted_predecessor_receipt_chain_sha256".to_owned(), serde_json::json!("1".repeat(64)), "deferred trial predecessor custody"),
+            ("trial exponential credit", "/phase_diagnostics/deferred_native_v2_soil_custody/candidate/layer_credits/0/ending_temperature_k".to_owned(), serde_json::json!(1e-7), "deferred unpublished trial seal"),
+            ("deferred trial seal", "/phase_diagnostics/deferred_native_v2_soil_custody/candidate/unpublished_trial_sha256".to_owned(), serde_json::json!("0".repeat(64)), "deferred unpublished trial seal"),
+            ("committed/current substitution", "/phase_diagnostics/deferred_native_v2_soil_custody/continuation/original_beginning_owner".to_owned(), day["native_committed_bootstrap_soil_v2_constructor_operands"]["owner"].clone(), "deferred/current resident separation and support join"),
+            ("ordered credit state", "/phase_diagnostics/deferred_native_v2_soil_custody/continuation/ordered_layer_credit_chain/0/0/ending_temperature_k".to_owned(), serde_json::json!(271.0), "deferred ordered layer beginning"),
+            ("accumulated custody", "/phase_diagnostics/deferred_native_v2_soil_custody/continuation/accumulated_operands/0/energy_j_m2_ofe_ground".to_owned(), serde_json::json!(1.0), "deferred canonical accumulated operand chain"),
+        ];
+        for (name, pointer, replacement, expected) in cases {
+            let mut poisoned = original.clone();
+            *poisoned
+                .pointer_mut(&pointer)
+                .expect("existing semantic field") = replacement;
+            let result =
+                member_restoration::validate_current_phase(&poisoned, &target.clock, position);
+            println!("phase poison {name}: {result:?}");
+            assert_eq!(result.as_ref().map_err(String::as_str), Err(expected));
+        }
+        let bytes: Vec<u8> =
+            serde_json::from_value(original["surface_liquid_configuration_canonical_json"].clone())
+                .expect("surface bytes");
+        let surface =
+            crate::DirectSurfaceLiquidConfiguration::from_canonical_bytes(&bytes).expect("surface");
+        validate_snow_free_prefix_membership_v1(original, &target.clock, &surface)
+            .expect("positive prefix");
+        for (name, pointer, replacement, expected) in [
+            (
+                "prefix proof",
+                "/phase_diagnostics/validated_native_inactive_wb14_prefix/prefix_end_ns",
+                serde_json::json!(1),
+                "snow-free recomputed prefix proof comparison",
+            ),
+            (
+                "event membership",
+                "/phase_diagnostics/ordered_event_groups/0/accepted_event_receipt/ordinal",
+                serde_json::json!(1),
+                "snow-free prefix accepted-event membership",
+            ),
+        ] {
+            let mut poisoned = original.clone();
+            *poisoned
+                .pointer_mut(pointer)
+                .expect("existing prefix field") = replacement;
+            let result =
+                validate_snow_free_prefix_membership_v1(&poisoned, &target.clock, &surface);
+            println!("prefix poison {name}: {result:?}");
+            assert_eq!(result.as_ref().map_err(String::as_str), Err(expected));
+        }
+    }
+
+    #[test]
+    fn composed_member_native_restoration_from_pinned_seed() {
+        let export = std::env::var_os("OPENWEPP_B01_MEMBER_EXPORT").expect("bound member export");
+        let seed = std::env::var_os("OPENWEPP_B01_PINNED_SEED").expect("bound pinned seed");
+        let _native_audit =
+            crate::v9_real_consumer_shadow::begin_covered_native_physical_path_audit_v1();
+        let _probe_audit = ProbeAudit::begin();
+        openwepp_land_surface_energy::snow_accuracy_policy::Policy::B01
+            .install()
+            .expect("policy");
+        let result =
+            member_restoration::restore(std::path::Path::new(&export), std::path::Path::new(&seed));
+        println!("composed-native-result: {result:?}");
+        result.expect("composed native restoration");
+    }
+
+    #[test]
+    fn composed_member_native_refuses_original_authority_mismatches() {
+        let export = std::env::var_os("OPENWEPP_B01_MEMBER_EXPORT").expect("bound member export");
+        let seed = std::env::var_os("OPENWEPP_B01_PINNED_SEED").expect("bound pinned seed");
+        let _native_audit =
+            crate::v9_real_consumer_shadow::begin_covered_native_physical_path_audit_v1();
+        let _probe_audit = ProbeAudit::begin();
+        openwepp_land_surface_energy::snow_accuracy_policy::Policy::B01
+            .install()
+            .expect("policy");
+        let result =
+            member_restoration::restore(std::path::Path::new(&export), std::path::Path::new(&seed));
+        println!("composed-native-result: {result:?}");
+        assert_eq!(
+            result.as_ref().map_err(String::as_str),
+            Err(
+                "reader position/GSI joins refused (outer=214, captured=0, gsi_matches=false); canonical native constructor refusal: V9 real-consumer identity failure: native soil proof accepted support or exact bootstrap"
+            )
+        );
+        println!(
+            "EXPECTED_REFUSAL: original position/GSI and canonical accepted-soil support remain inadmissible"
+        );
+    }
+
+    #[test]
+    #[ignore = "requires owner-authorized pinned B01 corpus/provider computation"]
+    fn deferred_credit_capture_represents_no_per_credit_clock_provenance() {
+        let export = std::env::var_os("OPENWEPP_B01_MEMBER_EXPORT").expect("bound member export");
+        let restored =
+            restore_snow_free_clock_parent_from_member_export(std::path::Path::new(&export))
+                .expect("member clock and parent");
+        let continuation = &restored.recorded_row["phase_diagnostics"]["deferred_native_v2_soil_custody"]
+            ["continuation"];
+        let credits = continuation["ordered_layer_credit_chain"]
+            .as_array()
+            .expect("recorded ordered layer credit groups");
+        assert!(!credits.is_empty(), "retained deferred credit groups");
+        for group in credits {
+            let group = group.as_array().expect("recorded credit group");
+            assert!(!group.is_empty(), "recorded credit-group operands");
+            for credit in group {
+                for absent_key in [
+                    "child_support",
+                    "support_start_ns",
+                    "support_end_ns",
+                    "child_transaction_id",
+                    "accepted_event_ordinal",
+                ] {
+                    assert!(
+                        credit.get(absent_key).is_none(),
+                        "serialized deferred credit unexpectedly supplies {absent_key}"
+                    );
+                }
+            }
+        }
+        assert!(
+            continuation.get("accumulated_operands").is_some(),
+            "this is a provenance absence, not a claim that deferred custody is absent"
+        );
+    }
+
+    /// Test-only raw-record correspondence validator for the B01 deferred
+    /// custody specimen.  It is deliberately JSON/carry/state validation: it
+    /// neither calls a physical map nor advances, installs, or promotes a
+    /// native soil child.
+    fn validate_b01_retained_soil_trial_correspondence_v1(
+        export: &std::path::Path,
+        primitives: &serde_json::Value,
+        subslabs: &serde_json::Value,
+    ) -> Result<(), String> {
+        let records = primitives["records"]
+            .as_array()
+            .ok_or("retained primitive records")?;
+        let restored = restore_snow_free_clock_parent_from_member_export(export)?;
+        let phase = &restored.recorded_row["phase_diagnostics"];
+        let continuation = &phase["deferred_native_v2_soil_custody"]["continuation"];
+        let chain = continuation["ordered_layer_credit_chain"]
+            .as_array()
+            .ok_or("retained ordered credit chain")?;
+        if chain.len() != 12 || records.len() != 24 {
+            return Err("retained primitive/credit group cardinality".into());
+        }
+
+        // The observer captured every selected primitive twice.  Duplicates
+        // are permitted only when all semantic fields agree; capture metadata
+        // itself is intentionally not correspondence authority.
+        let mut unique = BTreeMap::<String, serde_json::Value>::new();
+        for extracted in records {
+            let mut semantic = extracted["record"].clone();
+            semantic
+                .as_object_mut()
+                .ok_or("primitive semantic record")?
+                .remove("capture");
+            let receipt = semantic["receipt"]["receipt_sha256"]
+                .as_str()
+                .ok_or("primitive receipt identity")?
+                .to_owned();
+            if let Some(prior) = unique.insert(receipt, semantic.clone()) {
+                if prior != semantic {
+                    return Err("conflicting duplicate primitive record".into());
+                }
+            }
+        }
+        if unique.len() != chain.len() {
+            return Err("primitive credit group coverage".into());
+        }
+        for primitive in unique.values() {
+            terminal_trial_receipt_from_raw_v1(&primitive["receipt"])?
+                .validate()
+                .map_err(|_| "raw trial receipt seal")?;
+        }
+
+        let slab_records = subslabs["records"]
+            .as_array()
+            .ok_or("retained subslab primitive records")?;
+        if slab_records.len() != 8 {
+            return Err("retained subslab primitive cardinality".into());
+        }
+        let mut slabs = BTreeMap::<String, &serde_json::Value>::new();
+        for extracted in slab_records {
+            let record = &extracted["record"];
+            let Some(receipt) =
+                record["adaptive_terminal_snow_soil_trial_receipts"]["1"]["receipt_sha256"]
+                    .as_str()
+            else {
+                continue;
+            };
+            let receipt = receipt.to_owned();
+            if slabs.insert(receipt, record).is_some() {
+                return Err("duplicate subslab trial receipt".into());
+            }
+        }
+        if slabs.len() != 7 {
+            return Err("subslab trial receipt cardinality".into());
+        }
+
+        let mut raw_by_support = BTreeMap::<(u128, u128), String>::new();
+        let mut raw_heat = 0.0;
+        for (receipt_id, primitive) in &unique {
+            let receipt = &primitive["receipt"];
+            let start = receipt["support"]["start_ns"]
+                .as_str()
+                .ok_or("primitive support start")?
+                .parse::<u128>()
+                .map_err(|_| "primitive support start integer")?;
+            let end = receipt["support"]["end_ns"]
+                .as_str()
+                .ok_or("primitive support end")?
+                .parse::<u128>()
+                .map_err(|_| "primitive support end integer")?;
+            if raw_by_support
+                .insert((start, end), receipt_id.clone())
+                .is_some()
+            {
+                return Err("duplicate raw receipt support".into());
+            }
+            raw_heat += receipt["soil_heat_j_m2"]
+                .as_f64()
+                .ok_or("primitive soil heat")?;
+        }
+        if !raw_heat.is_finite() {
+            return Err("raw trial heat total".into());
+        }
+
+        let mut prior_end = None;
+        let mut child_width = None;
+        let mut supports = Vec::with_capacity(chain.len());
+        for credit_group in chain {
+            let layers = credit_group.as_array().ok_or("credit group layers")?;
+            let matching = unique
+                .iter()
+                .filter(|(_, primitive)| {
+                    let receipt = &primitive["receipt"];
+                    layers
+                        .iter()
+                        .filter(|layer| {
+                            layer["accepted_operands"]
+                                .as_array()
+                                .is_some_and(|operands| {
+                                    operands.iter().any(|operand| {
+                                        operand["source_kind"] == "top_boundary"
+                                            && operand["source_owner_id"] == "snow"
+                                            && operand["debit_credit_identity_sha256"]
+                                                == receipt["receipt_sha256"]
+                                            && operand["ofe_id"] == receipt["ofe_id"]
+                                            && operand["energy_j_m2_ofe_ground"]
+                                                == receipt["soil_heat_j_m2"]
+                                    })
+                                })
+                        })
+                        .count()
+                        == 1
+                })
+                .collect::<Vec<_>>();
+            if matching.len() != 1 {
+                return Err("raw receipt to one credit-layer correspondence".into());
+            }
+            let (receipt_id, primitive) = matching[0];
+            let receipt = &primitive["receipt"];
+            if primitive["kind"] != "soil_trial_primitive"
+                || primitive["disposition"] != "trial_only_select_by_committed_final_receipt"
+                || receipt["lane_id"] != 1
+                || receipt["ofe_id"] != "ofe-1"
+                || receipt["snow_heat_j_m2"].as_f64()
+                    != receipt["soil_heat_j_m2"].as_f64().map(|value| -value)
+            {
+                return Err("primitive receipt identity or heat custody".into());
+            }
+            let start = receipt["support"]["start_ns"]
+                .as_str()
+                .ok_or("primitive support start")?
+                .parse::<u128>()
+                .map_err(|_| "primitive support start integer")?;
+            let end = receipt["support"]["end_ns"]
+                .as_str()
+                .ok_or("primitive support end")?
+                .parse::<u128>()
+                .map_err(|_| "primitive support end integer")?;
+            let width = end.checked_sub(start).ok_or("primitive support ordering")?;
+            if width < openwepp_land_surface_energy::MINIMUM_SUPPORT_NS
+                || child_width.is_some_and(|prior| prior != width)
+                || prior_end.is_some_and(|prior| prior != start)
+            {
+                return Err("primitive support contiguous minimum-width coverage".into());
+            }
+            child_width = Some(width);
+            prior_end = Some(end);
+            supports.push((start, end));
+            let layer = layers
+                .iter()
+                .find(|layer| {
+                    layer["accepted_operands"]
+                        .as_array()
+                        .is_some_and(|operands| {
+                            operands.iter().any(|operand| {
+                                operand["debit_credit_identity_sha256"] == receipt["receipt_sha256"]
+                            })
+                        })
+                })
+                .ok_or("raw receipt layer lookup")?;
+            let beginning = &primitive["beginning_soil"]["v2"];
+            let ending = &primitive["ending_soil"]["v2"];
+            if layer["layer_id"] != beginning["layer_id"]
+                || layer["layer_id"] != ending["layer_id"]
+                || layer["beginning_enthalpy_hi_j_m2_ofe_ground"]
+                    != beginning["enthalpy_hi_j_m2_ofe_ground"]
+                || layer["beginning_enthalpy_carry"] != beginning["enthalpy_carry"]
+                || layer["beginning_temperature_k"] != beginning["temperature_k"]
+                || layer["ending_enthalpy_hi_j_m2_ofe_ground"]
+                    != ending["enthalpy_hi_j_m2_ofe_ground"]
+                || layer["ending_enthalpy_carry"] != ending["enthalpy_carry"]
+                || layer["ending_temperature_k"] != ending["temperature_k"]
+            {
+                return Err("primitive exact carry/state correspondence".into());
+            }
+            let slab = slabs
+                .get(receipt_id)
+                .copied()
+                .or_else(|| {
+                    slab_records.iter().map(|v| &v["record"]).find(|slab| {
+                        let slab_start = slab["support"]["start_ns"]
+                            .as_str()
+                            .and_then(|v| v.parse::<u128>().ok());
+                        let slab_end = slab["support"]["end_ns"]
+                            .as_str()
+                            .and_then(|v| v.parse::<u128>().ok());
+                        slab_start
+                            .zip(slab_end)
+                            .is_some_and(|(slab_start, slab_end)| {
+                                start >= slab_start && end <= slab_end
+                            })
+                    })
+                })
+                .ok_or("raw receipt missing subslab support")?;
+            let trial = &slab["adaptive_terminal_snow_soil_trial_receipts"]["1"];
+            if !trial.is_null() && trial != receipt {
+                return Err("raw receipt/subslab identity correspondence".into());
+            }
+        }
+        if prior_end != Some(385_920_000_000_000) {
+            return Err("primitive terminal support end".into());
+        }
+        let phase_joins = phase["ordered_owner_joins"]
+            .as_array()
+            .ok_or("phase ordered owner joins")?;
+        if phase_joins.len() != slab_records.len() {
+            return Err("phase/subslab cardinality".into());
+        }
+        let mut consumed_children = Vec::new();
+        for (index, slab) in slab_records
+            .iter()
+            .map(|value| &value["record"])
+            .enumerate()
+        {
+            let trial_value = &slab["adaptive_terminal_snow_soil_trial_receipts"]["1"];
+            let support: openwepp_coupled_time::TimeSupport =
+                serde_json::from_value(slab["support"].clone()).map_err(|_| "subslab support")?;
+            let accepted = restored
+                .clock
+                .accepted_slab_receipts()
+                .iter()
+                .filter(|accepted| {
+                    accepted.support() == support
+                        && serde_json::to_value(accepted.slab_id().digest())
+                            .ok()
+                            .as_ref()
+                            == Some(&slab["accepted_slab_sha256"])
+                })
+                .count();
+            let phase_join = &phase_joins[index];
+            let accepted_value = restored
+                .clock
+                .accepted_slab_receipts()
+                .iter()
+                .find(|accepted| {
+                    accepted.support() == support
+                        && serde_json::to_value(accepted.slab_id().digest())
+                            .ok()
+                            .as_ref()
+                            == Some(&slab["accepted_slab_sha256"])
+                })
+                .map(serde_json::to_value)
+                .transpose()
+                .map_err(|_| "accepted slab JSON")?
+                .ok_or("accepted subslab lookup")?;
+            if accepted != 1
+                || slab["owner_join"]["accepted_slab_sha256"] != slab["accepted_slab_sha256"]
+                || slab["accepted_slab_sha256"] != phase_join["accepted_slab_sha256"]
+                || slab["support"] != phase_join["support"]
+                || slab["owner_join"] != phase_join["owner_join"]
+                || slab["owner_join"]["parent_transaction_sha256"]
+                    != phase_join["owner_join"]["parent_transaction_sha256"]
+                || slab["parent_transaction_id"] != accepted_value["parent_transaction_id"]
+                || slab["parent_transaction_id"]
+                    != serde_json::to_value(restored.clock.parent_transaction_id().digest())
+                        .map_err(|_| "clock parent transaction JSON")?
+                || accepted_value["begin_owner_set"]
+                    != slab["owner_join"]["beginning_complete_owner_set_sha256"]
+                || accepted_value["end_owner_set"]
+                    != slab["owner_join"]["ending_complete_owner_set_sha256"]
+            {
+                return Err("subslab phase/accepted clock owner join".into());
+            }
+            let mut owned_fields = Vec::new();
+            if !trial_value.is_null() {
+                let trial = terminal_trial_receipt_from_raw_v1(trial_value)?;
+                trial.validate().map_err(|_| "raw trial receipt seal")?;
+                let selected = supports
+                    .iter()
+                    .enumerate()
+                    .filter(|(_, child)| {
+                        **child == (support.start_ns().get(), support.end_ns().get())
+                    })
+                    .collect::<Vec<_>>();
+                if selected.len() != 1 {
+                    return Err("trial raw child support coverage".into());
+                }
+                consumed_children.push(selected[0].0);
+                owned_fields.push((
+                    "adaptive_terminal_snow_soil_trial_receipt",
+                    trial.receipt_sha256,
+                ));
+            }
+            if let Some(final_value) = slab["terminal_snow_soil_heat_receipts"]["1"].as_object() {
+                let final_receipt =
+                    terminal_receipt_from_raw_v1(&serde_json::Value::Object(final_value.clone()))?;
+                final_receipt
+                    .validate()
+                    .map_err(|_| "raw terminal receipt seal")?;
+                if final_receipt.snow_heat_j_m2.to_bits() != (-427.759_947_125_143_3_f64).to_bits()
+                    || final_receipt.receipt_sha256
+                        != serde_json::from_value(serde_json::json!(
+                            "6e92cc24a0d1c96c4d83e287dcab3ee9bd45ad80700936e991b2855fc66f05b2"
+                        ))
+                        .map_err(|_| "raw terminal receipt pin")?
+                {
+                    return Err("raw terminal event receipt identity".into());
+                }
+                let selected = supports
+                    .iter()
+                    .enumerate()
+                    .filter(|(_, (start, end))| {
+                        *start >= support.start_ns().get() && *end <= support.end_ns().get()
+                    })
+                    .collect::<Vec<_>>();
+                if selected.len() != 5
+                    || selected
+                        .first()
+                        .is_none_or(|(_, (start, _))| *start != support.start_ns().get())
+                    || selected
+                        .last()
+                        .is_none_or(|(_, (_, end))| *end != support.end_ns().get())
+                {
+                    return Err("terminal raw child support coverage".into());
+                }
+                let last_receipt = raw_by_support
+                    .get(selected.last().expect("selected raw child").1)
+                    .ok_or("terminal limiting raw receipt")?;
+                if serde_json::to_value(final_receipt.limiting_boundary_receipt_sha256)
+                    .map_err(|_| "terminal limiting receipt JSON")?
+                    != serde_json::json!(last_receipt)
+                {
+                    return Err("terminal limiting raw receipt join".into());
+                }
+                let snow_heat: f64 = selected.iter().try_fold(0.0_f64, |sum, (_, support)| {
+                    let receipt = raw_by_support
+                        .get(support)
+                        .ok_or("terminal raw child lookup")?;
+                    let value = unique
+                        .get(receipt)
+                        .ok_or("terminal raw receipt lookup")?["receipt"]["snow_heat_j_m2"]
+                        .as_f64()
+                        .ok_or("terminal raw child heat")?;
+                    Ok::<f64, String>(sum + value)
+                })?;
+                let event = phase["ordered_event_groups"]
+                    .as_array()
+                    .and_then(|groups| groups.first())
+                    .ok_or("terminal event group")?;
+                let candidate = event["candidates"]
+                    .as_array()
+                    .and_then(|candidates| candidates.first())
+                    .ok_or("terminal event candidate")?;
+                if restored.clock.accepted_event_receipts().len() != 1 {
+                    return Err("terminal accepted event cardinality".into());
+                }
+                let clock_event =
+                    serde_json::to_value(&restored.clock.accepted_event_receipts()[0])
+                        .map_err(|_| "accepted event JSON")?;
+                if final_receipt.support != support
+                    || final_receipt.snow_heat_j_m2.to_bits() != snow_heat.to_bits()
+                    || candidate["support"] != slab["support"]
+                    || candidate["terminal_snow_soil_trial_receipt_sha256"]
+                        != serde_json::to_value(final_receipt.limiting_boundary_receipt_sha256)
+                            .map_err(|_| "terminal candidate receipt JSON")?
+                    || candidate["terminal_state_sha256"]
+                        != serde_json::to_value(final_receipt.ending_dormant_snow_owner_sha256)
+                            .map_err(|_| "terminal candidate state JSON")?
+                    || candidate["event"]["accepted_trials"] != 5
+                    || candidate["event"]["snow_soil_heat_energy_j_m2"]
+                        != serde_json::json!(snow_heat)
+                    || event["accepted_event_receipt"]["begin_owner_set"]
+                        != accepted_value["end_owner_set"]
+                    || event["accepted_event_receipt"]["receipt_id"] != clock_event["receipt_id"]
+                    || event["receipt_sha256"] != clock_event["event_context_digest"]
+                {
+                    return Err("terminal event/candidate raw correspondence".into());
+                }
+                consumed_children.extend(selected.into_iter().map(|(index, _)| index));
+                owned_fields.push((
+                    "terminal_snow_soil_heat_receipt",
+                    final_receipt.receipt_sha256,
+                ));
+            }
+            let fields = owned_fields
+                .iter()
+                .map(|(tag, digest)| openwepp_coupled_time::FramedField {
+                    tag,
+                    value: digest.as_bytes(),
+                })
+                .collect::<Vec<_>>();
+            let set = openwepp_coupled_time::framed_sha256(
+                "covered-terminal-snow-soil-heat-receipt-set-v1",
+                &fields,
+            )
+            .map_err(|_| "terminal receipt framing")?;
+            if serde_json::to_value(set).map_err(|_| "terminal receipt framing JSON")?
+                != slab["owner_join"]["terminal_snow_soil_heat_receipt_set_sha256"]
+            {
+                return Err("subslab terminal receipt set join".into());
+            }
+        }
+        if consumed_children != (7..12).collect::<Vec<_>>() {
+            return Err("complete raw child/subslab coverage".into());
+        }
+        let candidate = &phase["deferred_native_v2_soil_custody"]["candidate"];
+        if candidate["support_start_ns"].as_u64() != Some(385_860_000_000_000)
+            || candidate["support_end_ns"].as_u64() != Some(385_920_000_000_000)
+            || candidate["unpublished_predecessor_trial_sha256"] == serde_json::Value::Null
+            || candidate["accepted_predecessor_receipt_chain_sha256"] != serde_json::Value::Null
+        {
+            return Err("captured final unpublished trial lineage".into());
+        }
+        let original: openwepp_land_surface_energy::SoilThermalOwnerEnvelopeV2 =
+            serde_json::from_value(continuation["original_beginning_owner"].clone())
+                .map_err(|_| "deferred original owner")?;
+        let typed_chain: Vec<Vec<openwepp_land_surface_energy::SoilThermalLayerEnergyCreditV2>> =
+            serde_json::from_value(continuation["ordered_layer_credit_chain"].clone())
+                .map_err(|_| "deferred typed credit chain")?;
+        let seals = member_restoration::replay_deferred_unpublished_trial_seals_v1(
+            &original,
+            &typed_chain,
+            &supports,
+        )?;
+        if seals.len() != 12
+            || serde_json::to_value(seals.last().ok_or("deferred final seal")?)
+                .map_err(|_| "deferred final seal JSON")?
+                != candidate["unpublished_trial_sha256"]
+            || serde_json::to_value(
+                seals
+                    .get(seals.len() - 2)
+                    .ok_or("deferred penultimate seal")?,
+            )
+            .map_err(|_| "deferred penultimate seal JSON")?
+                != candidate["unpublished_predecessor_trial_sha256"]
+        {
+            return Err("deferred reconstructed unpublished seal chain".into());
+        }
+        let day = decode_member_backed_row_v1(
+            export,
+            &export.join("rows/108033.events.jsonl"),
+            &BTreeSet::new(),
+        )?;
+        let base: u128 = serde_json::from_value(day["committed_next_parent_sequence"].clone())
+            .map_err(|_| "committed parent sequence")?;
+        let interval: u128 =
+            serde_json::from_value(restored.recorded_row["interval_index"].clone())
+                .map_err(|_| "captured interval index")?;
+        member_restoration::validate_current_phase(
+            &restored.recorded_row,
+            &restored.clock,
+            base.checked_add(interval)
+                .ok_or("current phase position overflow")?,
+        )?;
+        Ok(())
+    }
+
+    fn terminal_trial_receipt_from_raw_v1(
+        value: &serde_json::Value,
+    ) -> Result<crate::v9_real_consumer_shadow::TerminalSnowSoilTrialReceiptV1, String> {
+        if !value["experimental_bulk"].is_null() {
+            return Err("raw trial experimental bulk".into());
+        }
+        Ok(
+            crate::v9_real_consumer_shadow::TerminalSnowSoilTrialReceiptV1 {
+                experimental_bulk: None,
+                support: serde_json::from_value(value["support"].clone())
+                    .map_err(|_| "raw trial support")?,
+                lane_id: serde_json::from_value(value["lane_id"].clone())
+                    .map_err(|_| "raw trial lane")?,
+                ofe_id: serde_json::from_value(value["ofe_id"].clone())
+                    .map_err(|_| "raw trial ofe")?,
+                canonical_source_sha256: serde_json::from_value(
+                    value["canonical_source_sha256"].clone(),
+                )
+                .map_err(|_| "raw trial source")?,
+                beginning_snow_temperature_k: value["beginning_snow_temperature_k"]
+                    .as_f64()
+                    .ok_or("raw trial beginning snow")?,
+                ending_snow_temperature_k: value["ending_snow_temperature_k"]
+                    .as_f64()
+                    .ok_or("raw trial ending snow")?,
+                beginning_soil_temperature_k: value["beginning_soil_temperature_k"]
+                    .as_f64()
+                    .ok_or("raw trial beginning soil")?,
+                ending_soil_temperature_k: value["ending_soil_temperature_k"]
+                    .as_f64()
+                    .ok_or("raw trial ending soil")?,
+                snow_heat_j_m2: value["snow_heat_j_m2"]
+                    .as_f64()
+                    .ok_or("raw trial snow heat")?,
+                soil_heat_j_m2: value["soil_heat_j_m2"]
+                    .as_f64()
+                    .ok_or("raw trial soil heat")?,
+                ending_soil_candidate_sha256: serde_json::from_value(
+                    value["ending_soil_candidate_sha256"].clone(),
+                )
+                .map_err(|_| "raw trial ending candidate")?,
+                receipt_sha256: serde_json::from_value(value["receipt_sha256"].clone())
+                    .map_err(|_| "raw trial receipt")?,
+            },
+        )
+    }
+
+    fn terminal_receipt_from_raw_v1(
+        value: &serde_json::Value,
+    ) -> Result<crate::v9_real_consumer_shadow::TerminalSnowSoilHeatReceiptV1, String> {
+        Ok(
+            crate::v9_real_consumer_shadow::TerminalSnowSoilHeatReceiptV1 {
+                support: serde_json::from_value(value["support"].clone())
+                    .map_err(|_| "raw terminal support")?,
+                lane_id: serde_json::from_value(value["lane_id"].clone())
+                    .map_err(|_| "raw terminal lane")?,
+                ofe_id: serde_json::from_value(value["ofe_id"].clone())
+                    .map_err(|_| "raw terminal ofe")?,
+                beginning_snow_owner_sha256: serde_json::from_value(
+                    value["beginning_snow_owner_sha256"].clone(),
+                )
+                .map_err(|_| "raw terminal beginning")?,
+                ending_dormant_snow_owner_sha256: serde_json::from_value(
+                    value["ending_dormant_snow_owner_sha256"].clone(),
+                )
+                .map_err(|_| "raw terminal dormant")?,
+                ending_soil_owner_sha256: serde_json::from_value(
+                    value["ending_soil_owner_sha256"].clone(),
+                )
+                .map_err(|_| "raw terminal soil")?,
+                limiting_boundary_receipt_sha256: serde_json::from_value(
+                    value["limiting_boundary_receipt_sha256"].clone(),
+                )
+                .map_err(|_| "raw terminal boundary")?,
+                snow_heat_j_m2: value["snow_heat_j_m2"]
+                    .as_f64()
+                    .ok_or("raw terminal snow heat")?,
+                soil_heat_j_m2: value["soil_heat_j_m2"]
+                    .as_f64()
+                    .ok_or("raw terminal soil heat")?,
+                receipt_sha256: serde_json::from_value(value["receipt_sha256"].clone())
+                    .map_err(|_| "raw terminal receipt")?,
+            },
+        )
+    }
+
+    #[test]
+    #[ignore = "requires owner-authorized pinned B01 corpus/provider computation"]
+    fn deferred_context_raw_primitives_authenticate_credit_correspondence_before_admission() {
+        let export = std::env::var_os("OPENWEPP_B01_MEMBER_EXPORT").expect("bound member export");
+        let primitives = std::env::var_os("OPENWEPP_B01_RETAINED_SOIL_TRIAL_PRIMITIVES")
+            .expect("bound retained soil primitive artifact");
+        let subslabs = std::env::var_os("OPENWEPP_B01_RETAINED_SUBSLAB_PRIMITIVES")
+            .expect("bound retained subslab primitive artifact");
+        let _native_audit =
+            crate::v9_real_consumer_shadow::begin_covered_native_physical_path_audit_v1();
+        let _probe_audit = ProbeAudit::begin();
+        openwepp_land_surface_energy::snow_accuracy_policy::Policy::B01
+            .install()
+            .expect("policy");
+        let primitives: serde_json::Value = serde_json::from_slice(
+            &std::fs::read(primitives).expect("retained primitive artifact bytes"),
+        )
+        .expect("retained primitive artifact JSON");
+        let subslabs: serde_json::Value = serde_json::from_slice(
+            &std::fs::read(subslabs).expect("retained subslab primitive artifact bytes"),
+        )
+        .expect("retained subslab primitive artifact JSON");
+        assert_eq!(
+            primitives["source_sha256"],
+            "1555efe1b592081a3dd67639d71a18e4e67f518e24b93a95ddfe18de2023d87c",
+            "raw primitive source pin"
+        );
+        validate_b01_retained_soil_trial_correspondence_v1(
+            std::path::Path::new(&export),
+            &primitives,
+            &subslabs,
+        )
+        .expect("complete raw receipt/credit/carry correspondence");
+    }
+
+    #[test]
+    #[ignore = "requires owner-authorized pinned B01 corpus/provider computation"]
+    fn deferred_context_raw_correspondence_refuses_semantic_poison() {
+        let export = std::env::var_os("OPENWEPP_B01_MEMBER_EXPORT").expect("bound member export");
+        let primitives: serde_json::Value = serde_json::from_slice(
+            &std::fs::read(
+                std::env::var_os("OPENWEPP_B01_RETAINED_SOIL_TRIAL_PRIMITIVES")
+                    .expect("raw primitives"),
+            )
+            .expect("raw primitive bytes"),
+        )
+        .expect("raw primitive JSON");
+        let subslabs: serde_json::Value = serde_json::from_slice(
+            &std::fs::read(
+                std::env::var_os("OPENWEPP_B01_RETAINED_SUBSLAB_PRIMITIVES").expect("raw subslabs"),
+            )
+            .expect("raw subslab bytes"),
+        )
+        .expect("raw subslab JSON");
+        let export = std::path::Path::new(&export);
+        let mut missing = primitives.clone();
+        missing["records"].as_array_mut().expect("records").pop();
+        assert_eq!(
+            validate_b01_retained_soil_trial_correspondence_v1(export, &missing, &subslabs)
+                .as_ref()
+                .map_err(String::as_str),
+            Err("retained primitive/credit group cardinality")
+        );
+        let mut foreign = primitives.clone();
+        for record in foreign["records"].as_array_mut().expect("records") {
+            record["record"]["receipt"]["canonical_source_sha256"] =
+                serde_json::json!("00".repeat(32));
+        }
+        assert_eq!(
+            validate_b01_retained_soil_trial_correspondence_v1(export, &foreign, &subslabs)
+                .as_ref()
+                .map_err(String::as_str),
+            Err("raw trial receipt seal")
+        );
+        let mut support = primitives.clone();
+        for record in support["records"]
+            .as_array_mut()
+            .expect("records")
+            .iter_mut()
+            .take(2)
+        {
+            record["record"]["receipt"]["support"]["end_ns"] = serde_json::json!("385260000000001");
+        }
+        assert_eq!(
+            validate_b01_retained_soil_trial_correspondence_v1(export, &support, &subslabs)
+                .as_ref()
+                .map_err(String::as_str),
+            Err("raw trial receipt seal")
+        );
+        let mut carry = primitives.clone();
+        for record in carry["records"]
+            .as_array_mut()
+            .expect("records")
+            .iter_mut()
+            .take(2)
+        {
+            record["record"]["ending_soil"]["v2"]["enthalpy_carry"]["coefficient_hex"] =
+                serde_json::json!("1");
+        }
+        assert_eq!(
+            validate_b01_retained_soil_trial_correspondence_v1(export, &carry, &subslabs)
+                .as_ref()
+                .map_err(String::as_str),
+            Err("primitive exact carry/state correspondence")
+        );
+        let mut event = subslabs.clone();
+        event["records"]
+            .as_array_mut()
+            .expect("subslabs")
+            .last_mut()
+            .expect("final subslab")["record"]["terminal_snow_soil_heat_receipts"]["1"]["receipt_sha256"] =
+            serde_json::json!("00".repeat(32));
+        assert_eq!(
+            validate_b01_retained_soil_trial_correspondence_v1(export, &primitives, &event)
+                .as_ref()
+                .map_err(String::as_str),
+            Err("raw terminal receipt seal")
+        );
+        let mut reordered = primitives.clone();
+        reordered["records"]
+            .as_array_mut()
+            .expect("records")
+            .reverse();
+        validate_b01_retained_soil_trial_correspondence_v1(export, &reordered, &subslabs)
+            .expect("raw record order is not correspondence authority");
+    }
+
+    /// Contract-derived RED: all currently retained component evidence is
+    /// authenticated first, then the real ordinary restoration edge is asked
+    /// for the future private-composite outcome.  Until that separately gated
+    /// constructor exists, this must fail at the ordinary native admission
+    /// guard; it is never a passing acceptance test for the current runtime.
+    #[test]
+    #[ignore = "requires owner-authorized pinned B01 corpus/provider computation"]
+    fn deferred_context_complete_bundle_is_red_at_private_composition() {
+        let export = std::env::var_os("OPENWEPP_B01_MEMBER_EXPORT").expect("bound member export");
+        let seed = std::env::var_os("OPENWEPP_B01_PINNED_SEED").expect("bound pinned seed");
+        let primitives = std::env::var_os("OPENWEPP_B01_RETAINED_SOIL_TRIAL_PRIMITIVES")
+            .expect("bound retained soil primitive artifact");
+        let subslabs = std::env::var_os("OPENWEPP_B01_RETAINED_SUBSLAB_PRIMITIVES")
+            .expect("bound retained subslab primitive artifact");
+        let _native_audit =
+            crate::v9_real_consumer_shadow::begin_covered_native_physical_path_audit_v1();
+        let _probe_audit = ProbeAudit::begin();
+        openwepp_land_surface_energy::snow_accuracy_policy::Policy::B01
+            .install()
+            .expect("policy");
+        let primitives: serde_json::Value = serde_json::from_slice(
+            &std::fs::read(primitives).expect("retained primitive artifact bytes"),
+        )
+        .expect("retained primitive artifact JSON");
+        let subslabs: serde_json::Value = serde_json::from_slice(
+            &std::fs::read(subslabs).expect("retained subslab primitive artifact bytes"),
+        )
+        .expect("retained subslab primitive artifact JSON");
+        validate_b01_retained_soil_trial_correspondence_v1(
+            std::path::Path::new(&export),
+            &primitives,
+            &subslabs,
+        )
+        .expect("complete component bundle before ordinary-edge RED");
+        member_restoration::restore(std::path::Path::new(&export), std::path::Path::new(&seed))
+            .expect("RED: future private composite admission is not implemented at ordinary edge");
+    }
+
+    #[test]
+    fn member_constructor_inverse_roundtrips_actual_operands() {
+        let path = std::env::var_os("OPENWEPP_B01_MEMBER_EXPORT").expect("bound member export");
+        let _native_audit =
+            crate::v9_real_consumer_shadow::begin_covered_native_physical_path_audit_v1();
+        let _probe_audit = ProbeAudit::begin();
+        openwepp_land_surface_energy::snow_accuracy_policy::Policy::B01
+            .install()
+            .expect("policy");
+        let restored =
+            restore_snow_free_clock_parent_from_member_export(std::path::Path::new(&path))
+                .expect("member clock and parent");
+        let raw = &restored.recorded_row["native_hydrology_constructor_inputs"];
+        let typed = constructor_inverse::restore(raw).expect("field-complete constructor inverse");
+        assert_eq!(capture_direct_run_constructor_inputs(&typed), *raw);
+        let seed = std::env::var_os("OPENWEPP_B01_PINNED_SEED").expect("seed");
+        let seed: serde_json::Value =
+            serde_json::from_slice(&std::fs::read(seed).expect("seed bytes")).expect("seed JSON");
+        member_restoration::pin_hydrology(&seed, &typed)
+            .expect("independent immutable hydrology authority");
+        for (pointer, expected) in [
+            (
+                "/checkpoint/phase/committed/scientific/direct_hydrology/phase_plan_sha256",
+                "seed phase plan digest",
+            ),
+            (
+                "/checkpoint/phase/committed/scientific/direct_hydrology/lanes/0/day_inputs_sha256",
+                "seed lane immutable day inputs digest",
+            ),
+        ] {
+            let mut substituted_seed = seed.clone();
+            *substituted_seed
+                .pointer_mut(pointer)
+                .expect("pinned digest field") =
+                serde_json::json!(digest_bytes(b"foreign-immutable-operand"));
+            assert_eq!(
+                member_restoration::pin_hydrology(&substituted_seed, &typed)
+                    .as_ref()
+                    .map_err(String::as_str),
+                Err(expected)
+            );
+        }
+        let mut poisoned = typed.clone();
+        poisoned.lanes[0].area_m2 += 1.0;
+        assert_eq!(
+            member_restoration::pin_hydrology(&seed, &poisoned)
+                .as_ref()
+                .map_err(String::as_str),
+            Err("seed immutable lane area_m2")
+        );
+        member_restoration::restore_pinned_surface(&seed, &restored.recorded_row, &restored.clock)
+            .expect("seed surface and prefix configuration join");
+        let mut wrong_surface = restored.recorded_row.clone();
+        wrong_surface["phase_diagnostics"]["prefix_validation_surface_configuration_canonical_json"] =
+            serde_json::json!([]);
+        assert_eq!(
+            member_restoration::restore_pinned_surface(&seed, &wrong_surface, &restored.clock)
+                .err()
+                .as_deref(),
+            Some("prefix surface configuration authority join")
+        );
+
+        println!(
+            "constructor inverse: {} lanes; exact capture roundtrip",
+            typed.lanes.len()
+        );
+    }
+
+    #[test]
+    fn snow_free_member_export_restores_clock_and_parent_without_native_decode() {
+        let path = std::env::var_os("OPENWEPP_B01_MEMBER_EXPORT")
+            .expect("bound member export; run through the event-restoration recorder");
+        let _native_audit =
+            crate::v9_real_consumer_shadow::begin_covered_native_physical_path_audit_v1();
+        let _probe_audit = ProbeAudit::begin();
+        openwepp_land_surface_energy::snow_accuracy_policy::Policy::B01
+            .install()
+            .expect("pinned B01 policy context");
+        let restored =
+            restore_snow_free_clock_parent_from_member_export(std::path::Path::new(&path))
+                .expect("exact member-backed snow-free clock and parent restoration");
+        assert_eq!(restored.clock.accepted_until().get(), B01_CHILD_START_NS);
+        assert_eq!(restored.clock.accepted_slab_receipts().len(), 8);
+        assert_eq!(restored.clock.accepted_event_receipts().len(), 1);
+        assert_eq!(
+            restored.parent.parent_transaction_id(),
+            restored.clock.parent_transaction_id()
+        );
+        let surface_bytes: Vec<u8> = serde_json::from_value(
+            restored.recorded_row["surface_liquid_configuration_canonical_json"].clone(),
+        )
+        .expect("captured surface configuration bytes");
+        let surface =
+            crate::direct_runtime::DirectSurfaceLiquidConfiguration::from_canonical_bytes(
+                &surface_bytes,
+            )
+            .expect("captured surface configuration");
+        diagnose_snow_free_prefix_receipt_gates_v1(&restored.recorded_row, &surface)
+            .expect("native prefix receipt-gate localization");
+        validate_snow_free_prefix_membership_v1(&restored.recorded_row, &restored.clock, &surface)
+            .expect("clock-bound native inactive-prefix validation");
+    }
+
+    #[test]
+    fn snow_free_member_export_reconstructs_provisional_on_clone_from_pinned_seed() {
+        let export = std::env::var_os("OPENWEPP_B01_MEMBER_EXPORT")
+            .expect("bound member export; run through the event-restoration recorder");
+        let seed = std::env::var_os("OPENWEPP_B01_PINNED_SEED")
+            .expect("bound pinned seed; run through the event-restoration recorder");
+        let _native_audit =
+            crate::v9_real_consumer_shadow::begin_covered_native_physical_path_audit_v1();
+        let _probe_audit = ProbeAudit::begin();
+        openwepp_land_surface_energy::snow_accuracy_policy::Policy::B01
+            .install()
+            .expect("pinned B01 policy context");
+        let restored =
+            restore_snow_free_clock_parent_from_member_export(std::path::Path::new(&export))
+                .expect("member-backed clock and parent restoration");
+        let seed = extract_pinned_seed_configuration_v1(std::path::Path::new(&seed))
+            .expect("pinned seed configuration and identities");
+        let calendar = seed.calendar;
+        let policy = seed.controller_policy;
+        let row = &restored.recorded_row;
+        let constructor = &row["provisional_constructor_inputs"];
+        assert_eq!(row["capture_phase"], "snow_free");
+        assert_eq!(
+            constructor["constraint_source_owner_id"],
+            "v11-real-consumer"
+        );
+        assert_eq!(constructor["calendar_receipt"], serde_json::json!(calendar));
+        assert_eq!(constructor["controller_policy"], serde_json::json!(policy));
+        let support: TimeSupport =
+            serde_json::from_value(row["support"].clone()).expect("captured support");
+        let forcing = serde_json::from_value(constructor["forcing_receipt"].clone())
+            .expect("captured forcing receipt");
+        let before =
+            capture_coupled_clock_restart(&restored.clock).expect("clock bytes before clone");
+        let (clone, reduction, ledger, receipt) =
+            reconstruct_provisional_on_clone_with_authorities(
+                &restored.clock,
+                support,
+                calendar,
+                policy,
+                forcing,
+                "v11-real-consumer",
+            )
+            .expect("phase-correct provisional clone");
+        assert_eq!(
+            capture_coupled_clock_restart(&restored.clock).expect("clock bytes after clone"),
+            before
+        );
+        assert!(clone.accepted_until() > restored.clock.accepted_until());
+        let expected_reduction: Vec<u8> =
+            serde_json::from_value(row["constraint_reduction_canonical_json"].clone())
+                .expect("recorded reduction bytes");
+        let expected_ledger: Vec<u8> =
+            serde_json::from_value(row["complete_owner_ledger_canonical_json"].clone())
+                .expect("recorded ledger bytes");
+        let expected_receipt: Vec<u8> =
+            serde_json::from_value(row["provisional_receipt_canonical_json"].clone())
+                .expect("recorded provisional receipt bytes");
+        assert_eq!(
+            serde_json::to_vec(&reduction).expect("reduction bytes"),
+            expected_reduction
+        );
+        assert_eq!(
+            serde_json::to_vec(&ledger).expect("ledger bytes"),
+            expected_ledger
+        );
+        assert_eq!(
+            serde_json::to_vec(&receipt).expect("receipt bytes"),
+            expected_receipt
+        );
+    }
+
+    #[test]
+    fn archive_records_authenticate_sequentially_from_pinned_seed() {
+        let export = std::env::var_os("OPENWEPP_B01_MEMBER_EXPORT")
+            .expect("bound member export; run through the event-restoration recorder");
+        let seed_path = std::env::var_os("OPENWEPP_B01_PINNED_SEED")
+            .expect("bound pinned seed; run through the event-restoration recorder");
+        let _native_audit =
+            crate::v9_real_consumer_shadow::begin_covered_native_physical_path_audit_v1();
+        let _probe_audit = ProbeAudit::begin();
+        openwepp_land_surface_energy::snow_accuracy_policy::Policy::B01
+            .install()
+            .expect("pinned B01 policy context");
+        let export = std::path::PathBuf::from(export);
+        let target =
+            restore_snow_free_clock_parent_from_member_export(&export).expect("target row");
+        let vegetation_bytes: Vec<u8> = serde_json::from_value(
+            target.recorded_row["vegetation_configuration_canonical_json"].clone(),
+        )
+        .expect("V11 configuration bytes");
+        let vegetation: openwepp_vegetation::VegetationConfigurationV11 =
+            serde_json::from_slice(&vegetation_bytes).expect("V11 configuration");
+        let seed = extract_pinned_seed_configuration_v1(std::path::Path::new(&seed_path))
+            .expect("pinned seed configuration and identities");
+        assert_eq!(vegetation.imported_v10, seed.vegetation);
+        let lse = seed.lse;
+        let run = seed.run_identity;
+        let topology = seed.topology_identity;
+        let summary: serde_json::Value = serde_json::from_slice(
+            &std::fs::read(export.join("summary.json")).expect("summary read"),
+        )
+        .expect("summary JSON");
+        let archive_ordinals = [27_431_u64, 54_078, 79_222, 107_936];
+        let selected = summary["rows"]
+            .as_array()
+            .expect("summary rows")
+            .iter()
+            .filter_map(|row| row["ordinal"].as_u64())
+            .filter(|ordinal| archive_ordinals.contains(ordinal))
+            .collect::<Vec<_>>();
+        assert_eq!(selected.len(), B01_DAY_INDEX);
+        let mut days = Vec::new();
+        for ordinal in selected {
+            let path = export.join("rows").join(format!("{ordinal}.events.jsonl"));
+            let row =
+                decode_member_backed_row_v1(&export, &path, &BTreeSet::new()).expect("member row");
+            if row["kind"] != "b01_wb14_committed_day_archive_record_v1" {
+                continue;
+            }
+            let entry: crate::Stage3CommittedDayArchiveEntryV1 =
+                serde_json::from_value(row["archive_entry"].clone()).expect("archive entry");
+            assert_eq!(row["capture"]["ordinal"].as_u64(), Some(ordinal));
+            let bytes = std::fs::read(
+                export
+                    .join("rows")
+                    .join(format!("{ordinal}.canonical_record.bin")),
+            )
+            .expect("raw archive member");
+            assert_eq!(
+                u64::try_from(bytes.len()).ok(),
+                Some(entry.canonical_uncompressed_len)
+            );
+            assert_eq!(digest_bytes(&bytes), entry.content_sha256);
+            let day = row["day_index"].as_u64().expect("day index");
+            let result = authenticate_archived_day_with_configuration_v1(
+                entry,
+                &bytes,
+                run,
+                topology,
+                &vegetation,
+                &lse,
+            );
+            println!(
+                "archive-day-{day}: {}",
+                if result.is_ok() { "PASS" } else { "REFUSAL" }
+            );
+            days.push((day, result.err()));
+        }
+        days.sort_by_key(|(day, _)| *day);
+        assert_eq!(days.len(), B01_DAY_INDEX);
+        assert!(
+            days.iter().all(|(_, error)| error.is_none()),
+            "archive canonical refusals: {days:?}"
+        );
+    }
+
+    #[test]
+    fn prepared_day_member_restores_provider_and_forcing_component() {
+        let export = std::path::PathBuf::from(
+            std::env::var_os("OPENWEPP_B01_MEMBER_EXPORT").expect("bound member export"),
+        );
+        let _native_audit =
+            crate::v9_real_consumer_shadow::begin_covered_native_physical_path_audit_v1();
+        let _probe_audit = ProbeAudit::begin();
+        openwepp_land_surface_energy::snow_accuracy_policy::Policy::B01
+            .install()
+            .expect("pinned B01 policy context");
+        let day = decode_member_backed_row_v1(
+            &export,
+            &export.join("rows/108033.events.jsonl"),
+            &BTreeSet::new(),
+        )
+        .expect("bound prepared day member");
+        assert_eq!(day["kind"], "b01_wb14_prepared_day_current_context_v1");
+        assert_eq!(day["day_index"], B01_DAY_INDEX);
+        let seed: serde_json::Value = serde_json::from_slice(
+            &std::fs::read(std::env::var_os("OPENWEPP_B01_PINNED_SEED").expect("pinned seed path"))
+                .expect("pinned seed read"),
+        )
+        .expect("pinned seed JSON");
+        assert_eq!(
+            day["static_run_identity"],
+            seed["checkpoint"]["run_identity_sha256"]
+        );
+        assert_eq!(
+            day["static_topology_identity"],
+            seed["checkpoint"]["topology_sha256"]
+        );
+        let capture: ProviderCaptureV1 =
+            serde_json::from_value(day["provider_constructor_operands"].clone())
+                .expect("provider operands");
+        // The configuration is bound to this immutable member, not independent
+        // seed configuration authority. This test qualifies only the component.
+        let c = &capture.static_configuration;
+        let configuration = crate::runtime_inputs::SnowFreeHalfHourStaticConfiguration {
+            run_id: c.run_id.clone(),
+            co2_pa: c.co2_pa,
+            reference_height_m: c.reference_height_m,
+            gsi_owner_configuration_sha256: c.gsi_owner_configuration_sha256.clone(),
+            destinations: c
+                .destinations
+                .iter()
+                .map(|d| crate::runtime_inputs::SnowFreeHalfHourDestination {
+                    ofe_id: d.ofe_id.clone(),
+                    tile_id: d.tile_id.clone(),
+                    wb14_configuration_sha256: d.wb14_configuration_sha256.clone(),
+                })
+                .collect(),
+        };
+        configuration
+            .validate()
+            .expect("provider configuration validation");
+        let provider =
+            restore_provider_day(capture, &configuration).expect("native provider restore");
+        let captures: Vec<PreparedSupportCaptureV1> =
+            serde_json::from_value(day["ordered_support_constructor_operands"].clone())
+                .expect("ordered support operands");
+        assert_eq!(captures.len(), 48);
+        let supports = captures
+            .into_iter()
+            .map(|c| restore_prepared_support(c, &provider))
+            .collect::<Result<Vec<_>, _>>()
+            .expect("native support restore");
+        let prepared = crate::PreparedStage3V11DayV1::bind_production_provider_day(
+            &provider,
+            B01_DAY_INDEX,
+            supports,
+        )
+        .expect("native complete prepared day binding");
+        let restored =
+            restore_snow_free_clock_parent_from_member_export(&export).expect("target member");
+        let row = &restored.recorded_row;
+        assert_eq!(row["static_run_identity"], day["static_run_identity"]);
+        assert_eq!(
+            row["static_topology_identity"],
+            day["static_topology_identity"]
+        );
+        let parent = &prepared.supports()[B01_INTERVAL_INDEX];
+        assert_eq!(serde_json::json!(parent.support()), row["parent_support"]);
+        let forcing = crate::snow_stage3_v11_attachment::canonical_parent_forcing_digest(
+            B01_DAY_INDEX,
+            B01_INTERVAL_INDEX,
+            provider.gsi_receipt_digest().expect("GSI receipt digest"),
+            parent,
+        )
+        .expect("native parent forcing identity");
+        assert_eq!(
+            serde_json::json!(forcing),
+            row["provisional_constructor_inputs"]["forcing_receipt"]
+        );
+        println!(
+            "provider component: 48 supports, day 4 interval 22 parent and captured forcing match"
+        );
+    }
+}
--- a/crates/openwepp-hillslope-orchestrator/src/snow_stage3_v11_member_restoration.rs
+++ b/crates/openwepp-hillslope-orchestrator/src/snow_stage3_v11_member_restoration.rs
@@ -0,0 +1,1139 @@
+#![cfg(test)]
+//! File-only composition of the retained diagnostic members. No process step.
+use super::{
+    B01_DAY_INDEX, B01_INTERVAL_INDEX, CLOCK_AUTHORITY_DOMAIN, CLOCK_MODEL_DOMAIN,
+    CoupledClockStateV1, DirectV10RealConsumerShadow, PreparedSupportCaptureV1, TimeSupport,
+    authenticate_archived_day_with_configuration_v1, capture_direct_run_constructor_inputs,
+    capture_snow_free_prepared_support, complete_owner_set_digest, constructor_inverse,
+    decode_member_backed_row_v1, digest_bytes, extract_pinned_seed_configuration_v1,
+    reconstruct_provisional_on_clone_with_authorities, restore_coupled_clock,
+    restore_direct_run_frame_dynamic, restore_prepared_support, restore_provider_day,
+    restore_snow_free_clock_parent_from_member_export, validate_snow_free_prefix_membership_v1,
+};
+use serde::{Serialize, de::DeserializeOwned};
+use serde_json::Value;
+use std::{
+    collections::{BTreeMap, BTreeSet},
+    path::Path,
+};
+
+fn required<'a>(value: &'a Value, name: &str) -> Result<&'a Value, String> {
+    value.get(name).ok_or_else(|| format!("missing {name}"))
+}
+fn decode<T: DeserializeOwned>(value: &Value) -> Result<T, String> {
+    serde_json::from_value(value.clone()).map_err(|error| error.to_string())
+}
+fn hex_float(value: &Value) -> Result<f64, String> {
+    let text = value.as_str().ok_or("seed hex float string")?;
+    let hex = text
+        .strip_prefix("0x")
+        .filter(|s| s.len() == 16)
+        .ok_or("seed hex float width")?;
+    let value = f64::from_bits(u64::from_str_radix(hex, 16).map_err(|error| error.to_string())?);
+    if !value.is_finite() {
+        return Err("seed nonfinite float".into());
+    }
+    Ok(value)
+}
+fn pinned_provider(
+    seed: &Value,
+) -> Result<crate::runtime_inputs::SnowFreeHalfHourStaticConfiguration, String> {
+    let value = &seed["checkpoint"]["phase"]["committed"]["static_forcing_configuration"];
+    let destinations = required(value, "destinations")?
+        .as_array()
+        .ok_or("seed destinations")?
+        .iter()
+        .map(|d| {
+            Ok(crate::runtime_inputs::SnowFreeHalfHourDestination {
+                ofe_id: decode(required(d, "ofe_id")?)?,
+                tile_id: decode(required(d, "tile_id")?)?,
+                wb14_configuration_sha256: decode(required(d, "wb14_configuration_sha256")?)?,
+            })
+        })
+        .collect::<Result<Vec<_>, String>>()?;
+    let result = crate::runtime_inputs::SnowFreeHalfHourStaticConfiguration {
+        run_id: decode(required(value, "run_id")?)?,
+        co2_pa: hex_float(required(value, "co2_pa")?)?,
+        reference_height_m: hex_float(required(value, "reference_height_m")?)?,
+        gsi_owner_configuration_sha256: decode(required(value, "gsi_owner_configuration_sha256")?)?,
+        destinations,
+    };
+    result.validate().map_err(|error| error.to_string())?;
+    if value["configuration_sha256"] != result.configuration_sha256() {
+        return Err("seed provider configuration digest".into());
+    }
+    Ok(result)
+}
+// Same private operand projection as persisted-restart-v1/hydrology_restart.rs:
+// float bits are strings before serializing the (domain, sorted Value) tuple.
+fn operand_digest(domain: &str, value: &impl Serialize) -> Result<Value, String> {
+    fn hexify(value: &mut Value) {
+        match value {
+            Value::Number(n) if n.is_f64() => {
+                if let Some(f) = n.as_f64() {
+                    *value = Value::String(format!("0x{:016x}", f.to_bits()));
+                }
+            }
+            Value::Array(a) => a.iter_mut().for_each(hexify),
+            Value::Object(o) => o.values_mut().for_each(hexify),
+            _ => {}
+        }
+    }
+    let mut projected = serde_json::to_value(value).map_err(|error| error.to_string())?;
+    hexify(&mut projected);
+    serde_json::to_value(digest_bytes(
+        &serde_json::to_vec(&(domain, projected)).map_err(|error| error.to_string())?,
+    ))
+    .map_err(|error| error.to_string())
+}
+pub(super) fn pin_hydrology(
+    seed: &Value,
+    inputs: &crate::DirectRunConstructorInputs,
+) -> Result<(), String> {
+    let committed = &seed["checkpoint"]["phase"]["committed"]["scientific"]["direct_hydrology"];
+    let captured = capture_direct_run_constructor_inputs(inputs);
+    for name in ["run_id", "hillslope_id", "lane_count", "day_count"] {
+        if required(committed, name)? != required(&captured["identity"], name)? {
+            return Err(format!("seed hydrology {name}"));
+        }
+    }
+    let phases = inputs
+        .phase_plan
+        .phases()
+        .iter()
+        .map(|phase| match phase {
+            crate::DirectPhaseKind::Normalization => "normalization",
+            crate::DirectPhaseKind::StorageBounds => "storage_bounds",
+            crate::DirectPhaseKind::DecompositionTransition => "decomposition_transition",
+            crate::DirectPhaseKind::ResiduePartitionTransition => "residue_partition_transition",
+            crate::DirectPhaseKind::AnnualGrowthTransition => "annual_growth_transition",
+            crate::DirectPhaseKind::PerennialGrowthTransition => "perennial_growth_transition",
+            crate::DirectPhaseKind::PercolationDeepSeepage => "percolation_deep_seepage",
+            crate::DirectPhaseKind::Evapotranspiration => "evapotranspiration",
+            crate::DirectPhaseKind::Drainage => "drainage",
+            crate::DirectPhaseKind::LateralTransfer => "lateral_transfer",
+            crate::DirectPhaseKind::PlantRootUptake => "plant_root_uptake",
+            crate::DirectPhaseKind::RunoffReconciliation => "runoff_reconciliation",
+            crate::DirectPhaseKind::StorageReconciliation => "storage_reconciliation",
+            crate::DirectPhaseKind::ClosureDiagnostics => "closure_diagnostics",
+        })
+        .collect::<Vec<_>>();
+    if operand_digest("DirectPhasePlanV1", &phases)? != committed["phase_plan_sha256"] {
+        return Err("seed phase plan digest".into());
+    }
+    let lanes = required(committed, "lanes")?
+        .as_array()
+        .ok_or("seed lanes")?;
+    if lanes.len() != inputs.lanes.len() {
+        return Err("seed lane cardinality".into());
+    }
+    for (lane, pinned) in inputs.lanes.iter().zip(lanes) {
+        pin_lane_geometry(lane, pinned)?;
+        if pinned["lane_id"] != lane.lane_id
+            || operand_digest("DirectDayConstructorInputsV1", &lane.day_inputs)?
+                != pinned["day_inputs_sha256"]
+        {
+            return Err("seed lane immutable day inputs digest".into());
+        }
+    }
+    Ok(())
+}
+fn pin_lane_geometry(
+    lane: &crate::DirectLaneConstructorInputs,
+    pinned: &Value,
+) -> Result<(), String> {
+    for (name, actual) in [
+        ("upstream_area_ratio", lane.upstream_area_ratio),
+        ("area_m2", lane.area_m2),
+        (
+            "runoff_publication_q_scale",
+            lane.runoff_publication_q_scale,
+        ),
+        (
+            "runoff_publication_qofe_scale",
+            lane.runoff_publication_qofe_scale,
+        ),
+        (
+            "runoff_publication_efflen_m",
+            lane.runoff_publication_efflen_m,
+        ),
+        (
+            "runoff_publication_cumulative_length_m",
+            lane.runoff_publication_cumulative_length_m,
+        ),
+        (
+            "runoff_publication_ofe_length_m",
+            lane.runoff_publication_ofe_length_m,
+        ),
+    ] {
+        if actual.to_bits() != hex_float(required(pinned, name)?)?.to_bits() {
+            return Err(format!("seed immutable lane {name}"));
+        }
+    }
+    if pinned["upstream_lane_id"] != lane.upstream_lane_id
+        || pinned["downstream_lane_id"] != lane.downstream_lane_id
+    {
+        return Err("seed immutable lane topology".into());
+    }
+    Ok(())
+}
+
+pub(super) fn native_authority_joins(
+    native: &Value,
+    position: u128,
+    expected_gsi: &Value,
+) -> Result<(bool, bool), String> {
+    let captured_position: u128 = decode(required(native, "accepted_interval_count")?)?;
+    let captured_gsi: openwepp_coupled_time::Digest32 =
+        decode(required(native, "provider_gsi_receipt_sha256")?)?;
+    let expected_gsi: openwepp_coupled_time::Digest32 = decode(expected_gsi)?;
+    Ok((captured_position == position, captured_gsi == expected_gsi))
+}
+
+fn load_row(export: &Path, entry: &Value, omitted: &BTreeSet<&str>) -> Result<Value, String> {
+    let path = required(entry, "event_file")?
+        .as_str()
+        .ok_or("member event path")?;
+    let bytes = std::fs::read(export.join(path)).map_err(|error| error.to_string())?;
+    if serde_json::to_value(digest_bytes(&bytes)).map_err(|error| error.to_string())?
+        != entry["event_file_sha256"]
+        || entry["event_file_bytes"].as_u64() != u64::try_from(bytes.len()).ok()
+    {
+        return Err("member event manifest identity".into());
+    }
+    decode_member_backed_row_v1(export, &export.join(path), omitted)
+}
+fn kind(entry: &Value, name: &str) -> bool {
+    entry["kinds"] == serde_json::json!([name])
+}
+
+#[derive(Serialize)]
+struct SnowProjection<'a> {
+    schema: &'static str,
+    lanes: Vec<(&'a u32, &'a crate::DirectSnowStage3PersistentState)>,
+}
+
+fn restore_authenticated_archive_manifest(
+    export: &Path,
+    entries: &[Value],
+    pin: &super::PinnedSeedConfigurationV1,
+    vegetation: &openwepp_vegetation::VegetationConfigurationV11,
+) -> Result<crate::Stage3CommittedDayArchiveManifestV1, String> {
+    let mut manifest =
+        crate::Stage3CommittedDayArchiveManifestV1::empty(pin.run_identity, pin.topology_identity)
+            .map_err(|error| error.to_string())?;
+    let mut previous = None;
+    let archives = entries
+        .iter()
+        .filter(|r| kind(r, "b01_wb14_committed_day_archive_record_v1"))
+        .collect::<Vec<_>>();
+    if archives.len() != B01_DAY_INDEX {
+        return Err("archive member cardinality".into());
+    }
+    for (index, item) in archives.iter().enumerate() {
+        let row = load_row(export, item, &BTreeSet::from(["canonical_record"]))?;
+        let entry: crate::Stage3CommittedDayArchiveEntryV1 =
+            decode(required(&row, "archive_entry")?)?;
+        if entry.day_index != index || row["day_index"] != index {
+            return Err("archive recorded order".into());
+        }
+        let ordinal = required(item, "ordinal")?
+            .as_u64()
+            .ok_or("archive ordinal")?;
+        let bytes = std::fs::read(export.join(format!("rows/{ordinal}.canonical_record.bin")))
+            .map_err(|error| error.to_string())?;
+        let authenticated = authenticate_archived_day_with_configuration_v1(
+            entry.clone(),
+            &bytes,
+            pin.run_identity,
+            pin.topology_identity,
+            vegetation,
+            &pin.lse,
+        )?;
+        let publication = &authenticated.publication;
+        if let Some((support_count, event_count, authority, ending)) = previous {
+            if publication.prefix_support_count != support_count
+                || publication.prefix_event_count != event_count
+                || publication.prefix_authority_sha256 != authority
+                || publication.prefix_ending_owner_set_sha256 != Some(ending)
+                || entry.beginning_owner_set_sha256 != ending
+            {
+                return Err("archive full publication prefix join".into());
+            }
+        } else if publication.prefix_support_count != 0
+            || publication.prefix_event_count != 0
+            || publication.prefix_authority_sha256
+                != digest_bytes(b"OPENWEPP_ACCEPTED_PUBLICATION_INCREMENTAL_AUTHORITY_V1\0")
+            || publication.prefix_ending_owner_set_sha256.is_some()
+        {
+            return Err("archive genesis publication prefix".into());
+        }
+        previous = Some((
+            publication.cumulative_support_count,
+            publication.cumulative_event_count,
+            publication.cumulative_authority_sha256,
+            publication.ending_owner_set_sha256,
+        ));
+        manifest.append(entry).map_err(|error| error.to_string())?;
+        println!(
+            "composed archive day {index}: canonical authentication and ordered manifest PASS"
+        );
+        // The canonical blob and restored day are dropped before reading the next.
+    }
+    Ok(manifest)
+}
+
+struct NativeRestorationContext<'a> {
+    export: &'a Path,
+    seed: &'a Value,
+    pin: &'a super::PinnedSeedConfigurationV1,
+    provider_configuration: &'a crate::runtime_inputs::SnowFreeHalfHourStaticConfiguration,
+    day: &'a Value,
+    prefix: &'a crate::Stage3ArchivedReceiptPrefixV1,
+    clock_parent: &'a super::RestoredSnowFreeClockParentV1,
+}
+
+pub(super) fn restore_pinned_surface(
+    seed: &Value,
+    target: &Value,
+    clock: &CoupledClockStateV1,
+) -> Result<crate::DirectSurfaceLiquidConfiguration, String> {
+    let surface_bytes: Vec<u8> = decode(required(
+        target,
+        "surface_liquid_configuration_canonical_json",
+    )?)?;
+    let surface = crate::DirectSurfaceLiquidConfiguration::from_canonical_bytes(&surface_bytes)
+        .map_err(|error| error.to_string())?;
+    if seed["checkpoint"]["phase"]["committed"]["surface_liquid_configuration"]["configuration_sha256"]
+        != surface.configuration_sha256
+    {
+        return Err("seed surface configuration digest".into());
+    }
+    let phase = required(target, "phase_diagnostics")?;
+    let prefix_surface: Vec<u8> = decode(required(
+        phase,
+        "prefix_validation_surface_configuration_canonical_json",
+    )?)?;
+    if prefix_surface != surface_bytes {
+        return Err("prefix surface configuration authority join".into());
+    }
+    validate_snow_free_prefix_membership_v1(target, clock, &surface)?;
+    Ok(surface)
+}
+
+fn restore_bound_provider(
+    day: &Value,
+    provider_configuration: &crate::runtime_inputs::SnowFreeHalfHourStaticConfiguration,
+) -> Result<
+    (
+        crate::runtime_inputs::PreparedSnowFreeGsiDayV1,
+        crate::ValidatedPreparedStage3V11DayV1,
+    ),
+    String,
+> {
+    let provider = restore_provider_day(
+        decode(required(day, "provider_constructor_operands")?)?,
+        provider_configuration,
+    )?;
+    let captures: Vec<PreparedSupportCaptureV1> =
+        decode(required(day, "ordered_support_constructor_operands")?)?;
+    let supports = captures
+        .into_iter()
+        .map(|c| restore_prepared_support(c, &provider))
+        .collect::<Result<Vec<_>, _>>()?;
+    let prepared_day = crate::PreparedStage3V11DayV1::bind_production_provider_day(
+        &provider,
+        B01_DAY_INDEX,
+        supports,
+    )
+    .map_err(|error| error.to_string())?;
+    Ok((provider, prepared_day))
+}
+
+fn validate_selected_support(
+    target: &Value,
+    parent: &crate::DirectSnowStage3V11PreparedSupport,
+    clock: &CoupledClockStateV1,
+) -> Result<(), String> {
+    let parent_support: TimeSupport = decode(required(target, "parent_support")?)?;
+    let support: TimeSupport = decode(required(target, "support")?)?;
+    let pending = decode(required(
+        required(target, "phase_diagnostics")?,
+        "pending_terminal_parcels",
+    )?)?;
+    let ordinal =
+        u32::try_from(clock.accepted_slab_receipts().len()).map_err(|error| error.to_string())?;
+    let prepared = parent.diagnostic_snow_free_successor_v1(support, ordinal, &pending)?;
+    if capture_snow_free_prepared_support(&prepared)?
+        != *required(target, "prepared_support_constructor_operands")?
+    {
+        return Err("snow-free successor provider projection".into());
+    }
+    if parent.support() != parent_support
+        || prepared.support() != support
+        || support.start_ns().get() != clock.accepted_until().get()
+        || support.start_ns() < parent_support.start_ns()
+        || support.end_ns() > parent_support.end_ns()
+    {
+        return Err("provider selected support/clock position".into());
+    }
+    Ok(())
+}
+
+fn validate_provisional(
+    target: &Value,
+    parent: &crate::DirectSnowStage3V11PreparedSupport,
+    provider: &crate::runtime_inputs::PreparedSnowFreeGsiDayV1,
+    clock: &CoupledClockStateV1,
+    pin: &super::PinnedSeedConfigurationV1,
+) -> Result<(), String> {
+    let support: TimeSupport = decode(required(target, "support")?)?;
+    let forcing = crate::snow_stage3_v11_attachment::canonical_parent_forcing_digest(
+        B01_DAY_INDEX,
+        B01_INTERVAL_INDEX,
+        provider
+            .gsi_receipt_digest()
+            .map_err(|error| error.to_string())?,
+        parent,
+    )
+    .map_err(|error| error.to_string())?;
+    if target["provisional_constructor_inputs"]["forcing_receipt"] != serde_json::json!(forcing) {
+        return Err("native provider forcing join".into());
+    }
+    let (_, reduction, ledger, receipt) = reconstruct_provisional_on_clone_with_authorities(
+        clock,
+        support,
+        pin.calendar,
+        pin.controller_policy,
+        forcing,
+        "v11-real-consumer",
+    )?;
+    for (name, actual) in [
+        (
+            "constraint_reduction_canonical_json",
+            serde_json::to_vec(&reduction),
+        ),
+        (
+            "complete_owner_ledger_canonical_json",
+            serde_json::to_vec(&ledger),
+        ),
+        (
+            "provisional_receipt_canonical_json",
+            serde_json::to_vec(&receipt),
+        ),
+    ] {
+        let expected: Vec<u8> = decode(required(target, name)?)?;
+        if actual.map_err(|error| error.to_string())? != expected {
+            return Err(format!("native provisional {name}"));
+        }
+    }
+    Ok(())
+}
+
+fn restore_native_context(
+    entries: &[Value],
+    context: &NativeRestorationContext<'_>,
+) -> Result<(), String> {
+    let export = context.export;
+    let seed = context.seed;
+    let pin = context.pin;
+    let provider_configuration = context.provider_configuration;
+    let day = context.day;
+    let prefix = context.prefix;
+    let clock_parent = context.clock_parent;
+    let target_entries = entries
+        .iter()
+        .filter(|r| kind(r, "b01_wb14_snow_free_pre_child_current_context_v1"))
+        .collect::<Vec<_>>();
+    let [target_entry] = target_entries.as_slice() else {
+        return Err("native target missing or duplicate".into());
+    };
+    let target = load_row(export, target_entry, &BTreeSet::new())?;
+    println!("composed actual native member decoded");
+    let inputs =
+        constructor_inverse::restore(required(&target, "native_hydrology_constructor_inputs")?)?;
+    pin_hydrology(seed, &inputs)?;
+    let surface = restore_pinned_surface(seed, &target, &clock_parent.clock)?;
+    let (provider, prepared_day) = restore_bound_provider(day, provider_configuration)?;
+    let parent = prepared_day
+        .supports()
+        .get(B01_INTERVAL_INDEX)
+        .ok_or("selected parent missing")?;
+    validate_selected_support(&target, parent, &clock_parent.clock)?;
+    let native = required(&target, "native_consumer_constructor_operands")?;
+    let position = prefix
+        .next_parent_sequence
+        .checked_add(u128::try_from(B01_INTERVAL_INDEX).map_err(|error| error.to_string())?)
+        .ok_or("position overflow")?;
+    if native["next_day_index"] != B01_DAY_INDEX
+        || target["day_index"] != B01_DAY_INDEX
+        || target["interval_index"] != B01_INTERVAL_INDEX
+    {
+        return Err("native outer day/interval join".into());
+    }
+    let expected_gsi = serde_json::json!(
+        provider
+            .gsi_receipt_digest()
+            .map_err(|error| error.to_string())?
+    );
+    let (position_matches, gsi_matches) = native_authority_joins(native, position, &expected_gsi)?;
+    println!(
+        "native position validation: outer={position}; captured={}; matched={position_matches}; no scalar installation",
+        native["accepted_interval_count"]
+    );
+    println!(
+        "native GSI validation: provider={expected_gsi}; captured={}; matched={gsi_matches}; no receipt installation",
+        native["provider_gsi_receipt_sha256"]
+    );
+    validate_current_phase(&target, &clock_parent.clock, position)?;
+    validate_provisional(&target, parent, &provider, &clock_parent.clock, pin)?;
+    restore_native_result(
+        &target,
+        context,
+        inputs,
+        &surface,
+        position,
+        position_matches,
+        gsi_matches,
+    )
+}
+
+fn restore_native_result(
+    target: &Value,
+    context: &NativeRestorationContext<'_>,
+    inputs: crate::DirectRunConstructorInputs,
+    surface: &crate::DirectSurfaceLiquidConfiguration,
+    position: u128,
+    position_matches: bool,
+    gsi_matches: bool,
+) -> Result<(), String> {
+    let native = required(target, "native_consumer_constructor_operands")?;
+    let pin = context.pin;
+    let day = context.day;
+    let clock_parent = context.clock_parent;
+    let provider_configuration = context.provider_configuration;
+    let frame = restore_direct_run_frame_dynamic(
+        decode(required(
+            target,
+            "native_hydrology_frame_dynamic_constructor_operands",
+        )?)?,
+        inputs.clone(),
+        surface,
+    )?;
+    println!(
+        "composed immutable operands, current/deferred phase and provisional checks PASS; invoking canonical native constructor with original unadmitted position/GSI operands"
+    );
+    let native_result = DirectV10RealConsumerShadow::diagnostic_restore_native_consumer_v1(
+        native,
+        decode(required(target, "native_soil_v2_constructor_operands")?)?,
+        decode(required(
+            day,
+            "native_committed_bootstrap_soil_v2_constructor_operands",
+        )?)?,
+        decode(required(target, "native_lane_map_constructor_operands")?)?,
+        &clock_parent.clock,
+        frame,
+        surface,
+        &pin.lse,
+        &pin.lse,
+        provider_configuration,
+        std::sync::Arc::new(inputs),
+    )
+    .map_err(|error| format!("canonical native constructor refusal: {error}"));
+    println!(
+        "canonical native constructor: {}",
+        match &native_result {
+            Ok(_) => "accepted original operands".to_owned(),
+            Err(error) => error.clone(),
+        }
+    );
+    let consumer = match (native_result, position_matches && gsi_matches) {
+        (Err(error), false) => {
+            return Err(format!(
+                "reader position/GSI joins refused (outer={position}, captured={}, gsi_matches={gsi_matches}); {error}",
+                native["accepted_interval_count"]
+            ));
+        }
+        (Err(error), true) => return Err(error),
+        (Ok(_), false) => {
+            return Err(format!(
+                "reader position/GSI joins refused (outer={position}, captured={}, gsi_matches={gsi_matches}); canonical native constructor accepted original operands",
+                native["accepted_interval_count"]
+            ));
+        }
+        (Ok(consumer), true) => consumer,
+    };
+    let expected: BTreeMap<String, Vec<u8>> =
+        decode(required(target, "complete_owner_canonical_bytes")?)?;
+    if consumer
+        .canonical_v11_parent_owner_state_bytes()
+        .map_err(|error| error.to_string())?
+        != expected
+    {
+        return Err("native six-owner projection".into());
+    }
+    Ok(())
+}
+
+pub(super) fn restore(export: &Path, seed_path: &Path) -> Result<(), String> {
+    let seed: Value =
+        serde_json::from_slice(&std::fs::read(seed_path).map_err(|error| error.to_string())?)
+            .map_err(|error| error.to_string())?;
+    let pin = extract_pinned_seed_configuration_v1(seed_path)?;
+    let provider_configuration = pinned_provider(&seed)?;
+    let summary: Value = serde_json::from_slice(
+        &std::fs::read(export.join("summary.json")).map_err(|error| error.to_string())?,
+    )
+    .map_err(|error| error.to_string())?;
+    if summary["status"] != "COMPLETE" {
+        return Err("member export incomplete".into());
+    }
+    let entries = required(&summary, "rows")?
+        .as_array()
+        .ok_or("member rows")?;
+    let day_entries = entries
+        .iter()
+        .filter(|r| kind(r, "b01_wb14_prepared_day_current_context_v1"))
+        .collect::<Vec<_>>();
+    let [day_entry] = day_entries.as_slice() else {
+        return Err("prepared day missing or duplicate".into());
+    };
+    let day = load_row(export, day_entry, &BTreeSet::new())?;
+    if day["day_index"] != B01_DAY_INDEX
+        || day["static_run_identity"] != serde_json::json!(pin.run_identity)
+        || day["static_topology_identity"] != serde_json::json!(pin.topology_identity)
+    {
+        return Err("prepared day seed identity".into());
+    }
+    let clock_parent = restore_snow_free_clock_parent_from_member_export(export)?;
+    let vegetation_bytes: Vec<u8> = decode(required(
+        &clock_parent.recorded_row,
+        "vegetation_configuration_canonical_json",
+    )?)?;
+    let vegetation: openwepp_vegetation::VegetationConfigurationV11 =
+        serde_json::from_slice(&vegetation_bytes).map_err(|error| error.to_string())?;
+    if vegetation.imported_v10 != pin.vegetation {
+        return Err("seed vegetation configuration".into());
+    }
+    vegetation.validate().map_err(|error| error.to_string())?;
+    let manifest = restore_authenticated_archive_manifest(export, entries, &pin, &vegetation)?;
+    let prefix: crate::Stage3ArchivedReceiptPrefixV1 =
+        decode(required(&day, "archived_receipt_prefix")?)?;
+    prefix.validate().map_err(|error| error.to_string())?;
+    let committed_bytes: Vec<u8> =
+        decode(required(&day, "committed_clock_restart_canonical_json")?)?;
+    let committed = restore_coupled_clock(
+        &committed_bytes,
+        digest_bytes(CLOCK_MODEL_DOMAIN),
+        digest_bytes(CLOCK_AUTHORITY_DOMAIN),
+        pin.controller_policy,
+    )?
+    .clock()
+    .clone();
+    if prefix.run_identity != pin.run_identity
+        || prefix.topology_identity != pin.topology_identity
+        || prefix.archived_day_count != manifest.committed_day_count
+        || prefix.ordered_day_chain_sha256 != manifest.ordered_day_chain_sha256
+        || prefix.archive_content_root_sha256 != manifest.archive_content_root_sha256
+        || prefix.accepted_until_ns != committed.accepted_until().get()
+        || prefix.ending_owner_set_sha256
+            != Some(
+                complete_owner_set_digest(committed.owners()).map_err(|error| error.to_string())?,
+            )
+        || day["committed_next_parent_sequence"] != serde_json::json!(prefix.next_parent_sequence)
+        || day["committed_receipt_tail_count"] != 0
+    {
+        return Err("archive prepared-day prefix join".into());
+    }
+    println!("composed archive/provider prefix PASS");
+    restore_native_context(
+        entries,
+        &NativeRestorationContext {
+            export,
+            seed: &seed,
+            pin: &pin,
+            provider_configuration: &provider_configuration,
+            day: &day,
+            prefix: &prefix,
+            clock_parent: &clock_parent,
+        },
+    )?;
+    Ok(())
+}
+
+pub(super) fn validate_current_phase(
+    target: &Value,
+    clock: &CoupledClockStateV1,
+    position: u128,
+) -> Result<(), String> {
+    let phase = required(target, "phase_diagnostics")?;
+    let current: BTreeMap<u32, crate::DirectSnowStage3PersistentState> =
+        decode(required(phase, "current_stage3")?)?;
+    if current.is_empty()
+        || current
+            .values()
+            .any(|state| u128::from(state.next_interval_index) != position)
+    {
+        return Err("current Stage3 accepted position".into());
+    }
+    let material: Option<
+        crate::snow_stage3_v11_snow_enthalpy_carry::AuthenticatedCoveredSnowMaterialOwnerV1,
+    > = decode(required(phase, "current_snow_enthalpy_material_owner")?)?;
+    if let Some(owner) = material {
+        owner.validate().map_err(|error| error.to_string())?;
+        if owner.base_material_owner() != &current {
+            return Err("current snow material/base state".into());
+        }
+    }
+    let ending_bytes: Vec<u8> = decode(required(phase, "ending_snow_owner_canonical_bytes")?)?;
+    let projected = serde_json::to_vec(&SnowProjection {
+        schema: "OPENWEPP_STAGE3_CANONICAL_SNOW_OWNER_V1",
+        lanes: current.iter().collect(),
+    })
+    .map_err(|error| error.to_string())?;
+    if projected != ending_bytes {
+        return Err("current Stage3 ending snow projection".into());
+    }
+    validate_current_snow_custody(target, phase, clock, &current)?;
+    let expected_clock: BTreeMap<String, Vec<u8>> =
+        decode(required(target, "clock_complete_owner_canonical_bytes")?)?;
+    let actual_clock = clock
+        .owners()
+        .iter()
+        .map(|o| (o.owner_id().to_owned(), o.state_bytes().to_vec()))
+        .collect::<BTreeMap<_, _>>();
+    if expected_clock != actual_clock {
+        return Err("current seven-owner clock projection".into());
+    }
+    validate_deferred_custody(target, clock)?;
+    Ok(())
+}
+
+fn validate_current_snow_custody(
+    target: &Value,
+    phase: &Value,
+    clock: &CoupledClockStateV1,
+    current: &BTreeMap<u32, crate::DirectSnowStage3PersistentState>,
+) -> Result<(), String> {
+    let parcels: BTreeMap<
+        openwepp_coupled_time::Digest32,
+        crate::DirectSnowStage3V11TerminalParcel,
+    > = decode(required(phase, "pending_terminal_parcels")?)?;
+    let captured_bytes: Vec<u8> = decode(required(
+        target,
+        "terminal_parcels_produced_unconsumed_canonical_json",
+    )?)?;
+    let captured_parcels: BTreeMap<
+        openwepp_coupled_time::Digest32,
+        crate::DirectSnowStage3V11TerminalParcel,
+    > = serde_json::from_slice(&captured_bytes).map_err(|error| error.to_string())?;
+    if parcels != captured_parcels
+        || parcels.iter().any(|(digest, parcel)| {
+            parcel.posture != crate::DirectSnowStage3V11TerminalParcelPosture::ProducedUnconsumed
+                || parcel.parent_transaction_id != clock.parent_transaction_id().digest()
+                || parcel.support.start_ns() < clock.parent_support().start_ns()
+                || parcel.support.end_ns() > clock.accepted_until()
+                || crate::snow_owner_v4::canonical_terminal_parcel_digest(parcel)
+                    .map_or(true, |actual| {
+                        actual != *digest || actual != parcel.parcel_digest
+                    })
+        })
+    {
+        return Err("current terminal parcel custody posture".into());
+    }
+    let joins =
+        crate::snow_stage3_v11_attachment::diagnostic_restore_coupled_subslab_receipts_value_v2(
+            required(phase, "ordered_owner_joins")?.clone(),
+        )?;
+    let last = joins.last().ok_or("current snow prefix tail")?;
+    let lanes = last
+        .lane_receipts
+        .iter()
+        .map(|(lane, receipt)| (*lane, receipt.receipt_sha256))
+        .collect();
+    let tiles = last
+        .destination_receipts
+        .iter()
+        .map(|((ofe, tile), receipt)| {
+            (
+                (ofe.as_str().to_owned(), tile.as_str().to_owned()),
+                *receipt.receipt_sha256(),
+            )
+        })
+        .collect();
+    let expected = crate::snow_owner_v4::canonical_stage3_snow_owner_v4_bytes(
+        current, &parcels, &lanes, &tiles,
+    )
+    .map_err(|error| error.0.to_owned())?;
+    let snow = clock
+        .owners()
+        .iter()
+        .find(|owner| owner.owner_id() == "snow")
+        .ok_or("clock snow owner")?;
+    if expected != snow.state_bytes() {
+        return Err("current Stage3 clock snow owner projection".into());
+    }
+    Ok(())
+}
+
+#[derive(Serialize)]
+#[serde(rename_all = "snake_case")]
+enum TrialPredecessor {
+    AcceptedReceiptChain(openwepp_land_surface_energy::Sha256Digest),
+    UnpublishedTrial(openwepp_land_surface_energy::Sha256Digest),
+    NumericalCoordinateProjection {
+        authority_sha256: openwepp_land_surface_energy::Sha256Digest,
+        accepted_receipt_chain_sha256: openwepp_land_surface_energy::Sha256Digest,
+        coordinate_set_sha256: openwepp_land_surface_energy::Sha256Digest,
+    },
+}
+// Representation inverse only: no numerical evolution or install capability.
+fn validate_trial(
+    value: &Value,
+) -> Result<openwepp_land_surface_energy::SoilThermalOwnedStateV2, String> {
+    use openwepp_kernel_contract::TransactionId;
+    use openwepp_land_surface_energy::{
+        Sha256Digest, SoilThermalLayerEnergyCreditV2, SoilThermalOwnedStateV2,
+    };
+    let accepted: Option<Sha256Digest> = decode(required(
+        value,
+        "accepted_predecessor_receipt_chain_sha256",
+    )?)?;
+    let unpublished: Option<Sha256Digest> =
+        decode(required(value, "unpublished_predecessor_trial_sha256")?)?;
+    let authority: Option<Sha256Digest> =
+        decode(required(value, "numerical_coordinate_authority_sha256")?)?;
+    let coordinate: Option<Sha256Digest> =
+        decode(required(value, "numerical_coordinate_set_sha256")?)?;
+    let custody = match (accepted, unpublished, authority, coordinate) {
+        (Some(a), None, None, None) => TrialPredecessor::AcceptedReceiptChain(a),
+        (None, Some(u), None, None) => TrialPredecessor::UnpublishedTrial(u),
+        (Some(a), None, Some(authority), Some(coordinate)) => {
+            TrialPredecessor::NumericalCoordinateProjection {
+                authority_sha256: authority,
+                accepted_receipt_chain_sha256: a,
+                coordinate_set_sha256: coordinate,
+            }
+        }
+        _ => return Err("deferred trial predecessor custody".into()),
+    };
+    let transaction: TransactionId = decode(required(value, "transaction_id")?)?;
+    let predecessor: Option<TransactionId> =
+        decode(required(value, "predecessor_transaction_id")?)?;
+    let start: u128 = decode(required(value, "support_start_ns")?)?;
+    let end: u128 = decode(required(value, "support_end_ns")?)?;
+    let beginning: Sha256Digest = decode(required(value, "beginning_state_sha256")?)?;
+    let ending: SoilThermalOwnedStateV2 = decode(required(value, "ending_state")?)?;
+    let credits: Vec<SoilThermalLayerEnergyCreditV2> = decode(required(value, "layer_credits")?)?;
+    ending.validate().map_err(|error| error.to_string())?;
+    if start >= end || ending.last_accepted_transaction_id != Some(transaction) {
+        return Err("deferred trial transaction/support".into());
+    }
+    let bytes = serde_json::to_vec(&(
+        "OPENWEPP_SOIL_THERMAL_UNPUBLISHED_TRIAL_V2",
+        transaction,
+        predecessor,
+        start,
+        end,
+        beginning,
+        custody,
+        &ending,
+        credits,
+    ))
+    .map_err(|error| error.to_string())?;
+    if serde_json::to_value(digest_bytes(&python_exponents(&bytes)))
+        .map_err(|error| error.to_string())?
+        != value["unpublished_trial_sha256"]
+    {
+        return Err("deferred unpublished trial seal".into());
+    }
+    Ok(ending)
+}
+// Exact digest representation rule from land_surface_energy::identity.
+fn python_exponents(bytes: &[u8]) -> Vec<u8> {
+    let mut output = Vec::with_capacity(bytes.len());
+    let (mut quoted, mut escaped, mut index) = (false, false, 0);
+    while index < bytes.len() {
+        let byte = bytes[index];
+        if quoted {
+            output.push(byte);
+            if escaped {
+                escaped = false;
+            } else if byte == b'\\' {
+                escaped = true;
+            } else if byte == b'"' {
+                quoted = false;
+            }
+            index += 1;
+            continue;
+        }
+        if byte == b'"' {
+            quoted = true;
+            output.push(byte);
+            index += 1;
+            continue;
+        }
+        if byte == b'e'
+            && index + 3 < bytes.len()
+            && matches!(bytes[index + 1], b'+' | b'-')
+            && bytes[index + 2].is_ascii_digit()
+            && !bytes[index + 3].is_ascii_digit()
+        {
+            output.extend_from_slice(&[byte, bytes[index + 1], b'0', bytes[index + 2]]);
+            index += 3;
+            continue;
+        }
+        output.push(byte);
+        index += 1;
+    }
+    output
+}
+fn replay_deferred_ordered_state_chain(
+    original: &openwepp_land_surface_energy::SoilThermalOwnerEnvelopeV2,
+    chain: &[Vec<openwepp_land_surface_energy::SoilThermalLayerEnergyCreditV2>],
+    trial: &Value,
+) -> Result<openwepp_land_surface_energy::SoilThermalOwnedStateV2, String> {
+    let mut state = original.state.clone();
+    for (index, credits) in chain.iter().enumerate() {
+        if index + 1 == chain.len()
+            && serde_json::to_value(&state.state_sha256).map_err(|error| error.to_string())?
+                != trial["beginning_state_sha256"]
+        {
+            return Err("deferred final trial beginning state".into());
+        }
+        let layer_count = state
+            .ofes
+            .iter()
+            .map(|ofe| ofe.ordered_layers.len())
+            .sum::<usize>();
+        if credits.len() != layer_count {
+            return Err("deferred layer chain cardinality".into());
+        }
+        for ((ofe_id, layer), credit) in state
+            .ofes
+            .iter_mut()
+            .flat_map(|ofe| {
+                ofe.ordered_layers
+                    .iter_mut()
+                    .map(|layer| (&ofe.ofe_id, layer))
+            })
+            .zip(credits)
+        {
+            if ofe_id != &credit.ofe_id
+                || layer.layer_id != credit.layer_id
+                || layer.enthalpy_hi_j_m2_ofe_ground.to_bits()
+                    != credit.beginning_enthalpy_hi_j_m2_ofe_ground.to_bits()
+                || layer.enthalpy_carry != credit.beginning_enthalpy_carry
+                || layer.temperature_k.to_bits() != credit.beginning_temperature_k.to_bits()
+            {
+                return Err("deferred ordered layer beginning".into());
+            }
+            layer.enthalpy_hi_j_m2_ofe_ground = credit.ending_enthalpy_hi_j_m2_ofe_ground;
+            layer.enthalpy_carry = credit.ending_enthalpy_carry.clone();
+            layer.temperature_k = credit.ending_temperature_k;
+            layer.last_accepted_transaction_id = Some(openwepp_kernel_contract::TransactionId(
+                original
+                    .transaction_id
+                    .0
+                    .checked_add(u128::try_from(index).map_err(|error| error.to_string())?)
+                    .ok_or("deferred transaction overflow")?,
+            ));
+        }
+        state.last_accepted_transaction_id = Some(openwepp_kernel_contract::TransactionId(
+            original
+                .transaction_id
+                .0
+                .checked_add(u128::try_from(index).map_err(|error| error.to_string())?)
+                .ok_or("deferred transaction overflow")?,
+        ));
+        state.reseal().map_err(|error| error.to_string())?;
+        state.validate().map_err(|error| error.to_string())?;
+    }
+    Ok(state)
+}
+
+/// Test-only inverse of the private unpublished-trial seal chain.  This
+/// reconstructs typed state and canonical tuple bytes only; it never invokes
+/// a physical map, an accepted-child advance, or an owner installation.
+#[cfg(test)]
+pub(super) fn replay_deferred_unpublished_trial_seals_v1(
+    original: &openwepp_land_surface_energy::SoilThermalOwnerEnvelopeV2,
+    chain: &[Vec<openwepp_land_surface_energy::SoilThermalLayerEnergyCreditV2>],
+    supports: &[(u128, u128)],
+) -> Result<Vec<openwepp_land_surface_energy::Sha256Digest>, String> {
+    use openwepp_kernel_contract::TransactionId;
+    use openwepp_land_surface_energy::Sha256Digest;
+
+    if chain.is_empty() || chain.len() != supports.len() {
+        return Err("deferred seal replay cardinality".into());
+    }
+    let mut state = original.state.clone();
+    let mut seals: Vec<Sha256Digest> = Vec::with_capacity(chain.len());
+    for (index, (credits, (start, end))) in chain.iter().zip(supports).enumerate() {
+        if start >= end || end - start < openwepp_land_surface_energy::MINIMUM_SUPPORT_NS {
+            return Err("deferred seal replay support".into());
+        }
+        if index > 0 && supports[index - 1].1 != *start {
+            return Err("deferred seal replay support continuity".into());
+        }
+        let layer_count = state
+            .ofes
+            .iter()
+            .map(|ofe| ofe.ordered_layers.len())
+            .sum::<usize>();
+        if credits.len() != layer_count {
+            return Err("deferred seal replay layer cardinality".into());
+        }
+        let beginning = state.state_sha256.clone();
+        let transaction = TransactionId(
+            original
+                .transaction_id
+                .0
+                .checked_add(u128::try_from(index).map_err(|error| error.to_string())?)
+                .ok_or("deferred seal replay transaction overflow")?,
+        );
+        for ((ofe_id, layer), credit) in state
+            .ofes
+            .iter_mut()
+            .flat_map(|ofe| {
+                ofe.ordered_layers
+                    .iter_mut()
+                    .map(|layer| (&ofe.ofe_id, layer))
+            })
+            .zip(credits)
+        {
+            if ofe_id != &credit.ofe_id
+                || layer.layer_id != credit.layer_id
+                || layer.enthalpy_hi_j_m2_ofe_ground.to_bits()
+                    != credit.beginning_enthalpy_hi_j_m2_ofe_ground.to_bits()
+                || layer.enthalpy_carry != credit.beginning_enthalpy_carry
+                || layer.temperature_k.to_bits() != credit.beginning_temperature_k.to_bits()
+            {
+                return Err("deferred seal replay ordered layer beginning".into());
+            }
+            layer.enthalpy_hi_j_m2_ofe_ground = credit.ending_enthalpy_hi_j_m2_ofe_ground;
+            layer.enthalpy_carry = credit.ending_enthalpy_carry.clone();
+            layer.temperature_k = credit.ending_temperature_k;
+            layer.last_accepted_transaction_id = Some(transaction);
+        }
+        state.last_accepted_transaction_id = Some(transaction);
+        state.reseal().map_err(|error| error.to_string())?;
+        state.validate().map_err(|error| error.to_string())?;
+        let predecessor = if index == 0 {
+            TrialPredecessor::AcceptedReceiptChain(original.receipt_chain_sha256.clone())
+        } else {
+            TrialPredecessor::UnpublishedTrial(seals[index - 1].clone())
+        };
+        let previous_transaction = if index == 0 {
+            original.expected_predecessor_transaction_id
+        } else {
+            Some(TransactionId(
+                transaction
+                    .0
+                    .checked_sub(1)
+                    .ok_or("deferred seal replay predecessor underflow")?,
+            ))
+        };
+        let bytes = serde_json::to_vec(&(
+            "OPENWEPP_SOIL_THERMAL_UNPUBLISHED_TRIAL_V2",
+            transaction,
+            previous_transaction,
+            *start,
+            *end,
+            beginning,
+            predecessor,
+            &state,
+            credits,
+        ))
+        .map_err(|error| error.to_string())?;
+        let seal: Sha256Digest = serde_json::from_value(
+            serde_json::to_value(digest_bytes(&python_exponents(&bytes)))
+                .map_err(|error| error.to_string())?,
+        )
+        .map_err(|error| error.to_string())?;
+        seals.push(seal);
+    }
+    Ok(seals)
+}
+
+fn validate_deferred_custody(target: &Value, clock: &CoupledClockStateV1) -> Result<(), String> {
+    let custody = required(
+        &target["phase_diagnostics"],
+        "deferred_native_v2_soil_custody",
+    )?;
+    if custody["posture"] != "reconstructible_operands_not_installable_capability" {
+        return Err("deferred custody posture".into());
+    }
+    let trial = required(custody, "candidate")?;
+    let ending = validate_trial(trial)?;
+    let continuation = required(custody, "continuation")?;
+    if required(continuation, "physical_trial")? != trial {
+        return Err("deferred candidate/physical trial join".into());
+    }
+    let original: openwepp_land_surface_energy::SoilThermalOwnerEnvelopeV2 =
+        decode(required(continuation, "original_beginning_owner")?)?;
+    original.validate().map_err(|error| error.to_string())?;
+    let resident: crate::v9_real_consumer_shadow::DirectV10SoilThermalResidentV2 =
+        decode(required(target, "native_soil_v2_constructor_operands")?)?;
+    if original.state != resident.owner().state
+        || original.receipt_chain_sha256 != resident.owner().receipt_chain_sha256
+        || trial["support_end_ns"] != serde_json::json!(clock.accepted_until().get())
+        || ending == resident.owner().state
+    {
+        return Err("deferred/current resident separation and support join".into());
+    }
+    let chain: Vec<Vec<openwepp_land_surface_energy::SoilThermalLayerEnergyCreditV2>> =
+        decode(required(continuation, "ordered_layer_credit_chain")?)?;
+    let transaction: openwepp_kernel_contract::TransactionId =
+        decode(required(trial, "transaction_id")?)?;
+    let predecessor: Option<openwepp_kernel_contract::TransactionId> =
+        decode(required(trial, "predecessor_transaction_id")?)?;
+    let expected_last = original
+        .transaction_id
+        .0
+        .checked_add(
+            u128::try_from(chain.len().checked_sub(1).ok_or("empty deferred chain")?)
+                .map_err(|error| error.to_string())?,
+        )
+        .ok_or("deferred transaction overflow")?;
+    if transaction.0 != expected_last
+        || predecessor.map(|id| id.0) != expected_last.checked_sub(1)
+        || original.expected_predecessor_transaction_id != Some(resident.owner().transaction_id)
+        || original.transaction_id.0
+            != resident
+                .owner()
+                .transaction_id
+                .0
+                .checked_add(1)
+                .ok_or("resident transaction overflow")?
+    {
+        return Err("deferred ordered transaction lineage".into());
+    }
+    let state = replay_deferred_ordered_state_chain(&original, &chain, trial)?;
+    if state != ending {
+        return Err("deferred complete ordered ending state".into());
+    }
+    let last = chain.last().ok_or("deferred credit chain missing")?;
+    if serde_json::to_value(last).map_err(|error| error.to_string())? != trial["layer_credits"] {
+        return Err("deferred credit chain tail".into());
+    }
+    let operands: Vec<openwepp_land_surface_energy::SoilThermalAcceptedEnergyOperandV2> =
+        decode(required(continuation, "accumulated_operands")?)?;
+    let mut flattened = chain
+        .iter()
+        .flat_map(|layers| {
+            layers
+                .iter()
+                .flat_map(|layer| layer.accepted_operands.clone())
+        })
+        .collect::<Vec<_>>();
+    crate::v9_real_consumer_shadow::diagnostic_canonicalize_soil_operand_chain_v1(
+        &original,
+        &mut flattened,
+    )
+    .map_err(|error| error.to_string())?;
+    if operands != flattened {
+        return Err("deferred canonical accumulated operand chain".into());
+    }
+    Ok(())
+}
--- a/crates/openwepp-hillslope-orchestrator/src/snow_stage3_v11_real_parent_execution.rs
+++ b/crates/openwepp-hillslope-orchestrator/src/snow_stage3_v11_real_parent_execution.rs
@@ -14,6 +14,7 @@
     forcing_receipt: Digest32,
     ending_snow_owner_bytes: Vec<u8>,
     native_inactive_wb14_prefix: Option<crate::direct_runtime::ValidatedNativeInactiveWb14PrefixV1>,
+    recorder_history: crate::snow_stage3_v11_current_context_capture::SnowFreePreChildHistoryV1<'_>,
     deferred_native_v2_soil_custody: Option<
         crate::v9_real_consumer_shadow::DeferredNativeV2SoilCustodyV1,
     >,
@@ -113,22 +114,78 @@
     let provisional_receipt = accept_slab(&mut provisional_clock, provisional_slab)?;
     drop(provisional_custody_cost);
     let provisional_parent = beginning_parent.clone();
+    let provisional_binding = crate::direct_runtime::DirectWb14CoupledChildBindingV1 {
+        proposed_upper_bound_s_bits: support.duration_s_bits(),
+        coupled_parent_transaction_sha256: *parent_id.digest().as_bytes(),
+        accepted_slab_sha256: *provisional_receipt.slab_id().digest().as_bytes(),
+        parent_beginning_complete_owner_set_sha256: *ledger_digest.as_bytes(),
+        parent_support_start_ns: beginning_clock.parent_support().start_ns().get(),
+        parent_support_end_ns: beginning_clock.parent_support().end_ns().get(),
+        child_support_start_ns: support.start_ns().get(),
+        child_support_end_ns: support.end_ns().get(),
+    };
+    #[cfg(test)]
+    crate::snow_stage3_v11_current_context_capture::record_pre_child_producer_live_inputs_v1(
+        &crate::snow_stage3_v11_current_context_capture::PreChildProducerSnapshotInputsV1 {
+            phase:
+                crate::snow_stage3_v11_current_context_capture::PreChildCapturePhaseV1::SnowFree {
+                    history: recorder_history,
+                    validated_native_inactive_wb14_prefix: native_inactive_wb14_prefix.as_ref(),
+                    ending_snow_owner_bytes: &ending_snow_owner_bytes,
+                    deferred_native_v2_soil_custody: deferred_native_v2_soil_custody.as_ref(),
+                },
+            context,
+            parent: beginning_parent,
+            consumer: beginning_consumer,
+            clock: beginning_clock,
+            prepared_support: Some(prepared),
+            beginning_stage3: recorder_history.current_stage3,
+            beginning_snow_enthalpy_material_owner: recorder_history
+                .current_snow_enthalpy_material_owner,
+            support,
+            reduction: &reduction,
+            ledger: &ledger,
+            receipt: &provisional_receipt,
+            binding: &provisional_binding,
+            pending_terminal_parcels: recorder_history.pending_terminal_parcels,
+            forcing_receipt,
+        },
+    );
+    crate::snow_stage3_v11_current_context_capture::record_pre_child_context(
+        crate::snow_stage3_v11_current_context_capture::PreChildContext {
+            phase:
+                crate::snow_stage3_v11_current_context_capture::PreChildCapturePhaseV1::SnowFree {
+                    history: recorder_history,
+                    validated_native_inactive_wb14_prefix: native_inactive_wb14_prefix.as_ref(),
+                    ending_snow_owner_bytes: &ending_snow_owner_bytes,
+                    deferred_native_v2_soil_custody: deferred_native_v2_soil_custody.as_ref(),
+                },
+            context,
+            parent: beginning_parent,
+            consumer: beginning_consumer,
+            clock: beginning_clock,
+            prepared_support: Some(prepared),
+            beginning_stage3: recorder_history.current_stage3,
+            beginning_snow_enthalpy_material_owner: recorder_history
+                .current_snow_enthalpy_material_owner,
+            support,
+            reduction: &reduction,
+            ledger: &ledger,
+            receipt: &provisional_receipt,
+            binding: &provisional_binding,
+            pending_terminal_parcels: recorder_history.pending_terminal_parcels,
+            forcing_receipt,
+            day: day_index,
+            interval: interval_index,
+        },
+    );
     let provisional_stack = DirectV11RealConsumerStack::new_parent_child_with_ending_snow_owner(
         beginning_consumer,
         &prepared.v11_interval,
         day_index,
         interval_index,
         support.end_ns() == beginning_clock.parent_support().end_ns(),
-        crate::direct_runtime::DirectWb14CoupledChildBindingV1 {
-            proposed_upper_bound_s_bits: support.duration_s_bits(),
-            coupled_parent_transaction_sha256: *parent_id.digest().as_bytes(),
-            accepted_slab_sha256: *provisional_receipt.slab_id().digest().as_bytes(),
-            parent_beginning_complete_owner_set_sha256: *ledger_digest.as_bytes(),
-            parent_support_start_ns: beginning_clock.parent_support().start_ns().get(),
-            parent_support_end_ns: beginning_clock.parent_support().end_ns().get(),
-            child_support_start_ns: support.start_ns().get(),
-            child_support_end_ns: support.end_ns().get(),
-        },
+        provisional_binding,
         ending_snow_owner_bytes.clone(),
     );
     let provisional_stack = match native_inactive_wb14_prefix {
--- a/crates/openwepp-hillslope-orchestrator/src/snow_stage3_v11_restart.rs
+++ b/crates/openwepp-hillslope-orchestrator/src/snow_stage3_v11_restart.rs
@@ -1553,6 +1553,32 @@
     }
 }
 
+/// Private diagnostic projection through the canonical restart representation.
+/// The production receipt stores destination receipts under typed tuple keys,
+/// which JSON objects cannot represent directly.
+pub(crate) fn diagnostic_coupled_subslab_receipts_value_v2(
+    receipts: &[Stage3CoupledSubslabReceiptV1],
+) -> Result<serde_json::Value, String> {
+    serde_json::to_value(Stage3CoupledSubslabReceiptsWireRefV2(receipts))
+        .map_err(|error| error.to_string())
+}
+
+/// Restores the private diagnostic projection emitted for captured current
+/// context. Tuple-keyed destination receipts are intentionally represented as
+/// ordered rows in JSON, so this must share the canonical restart wire rather
+/// than deserialize the production receipt directly.
+#[cfg(all(test, feature = "persisted-restart-v1"))]
+pub(crate) fn diagnostic_restore_coupled_subslab_receipts_value_v2(
+    value: serde_json::Value,
+) -> Result<Vec<Stage3CoupledSubslabReceiptV1>, String> {
+    let rows: Vec<Stage3CoupledSubslabReceiptWireV2> =
+        serde_json::from_value(value).map_err(|error| error.to_string())?;
+    rows.into_iter()
+        .map(Stage3CoupledSubslabReceiptWireV2::restore)
+        .collect::<Result<Vec<_>, _>>()
+        .map_err(|error| error.to_string())
+}
+
 #[derive(Serialize)]
 struct Stage3V11ParentReceiptRestartWireRefV2<'a> {
     day_index: usize,
--- a/crates/openwepp-hillslope-orchestrator/src/snow_stage3_v11_terminal_execution.rs
+++ b/crates/openwepp-hillslope-orchestrator/src/snow_stage3_v11_terminal_execution.rs
@@ -2349,8 +2349,29 @@
         child_support_start_ns: support.start_ns().get() as u128,
         child_support_end_ns: support.end_ns().get() as u128,
     };
+    #[cfg(test)]
+    crate::snow_stage3_v11_current_context_capture::record_pre_child_producer_live_inputs_v1(
+        &crate::snow_stage3_v11_current_context_capture::PreChildProducerSnapshotInputsV1 {
+            phase: crate::snow_stage3_v11_current_context_capture::PreChildCapturePhaseV1::Covered,
+            context,
+            parent: beginning_parent,
+            consumer: beginning_consumer,
+            clock: beginning_clock,
+            prepared_support: Some(prepared),
+            beginning_stage3: &beginning_stage3,
+            beginning_snow_enthalpy_material_owner,
+            support,
+            reduction: &reduction,
+            ledger: &ledger,
+            receipt: &provisional_receipt,
+            binding: &provisional_binding,
+            forcing_receipt,
+            pending_terminal_parcels,
+        },
+    );
     crate::snow_stage3_v11_current_context_capture::record_pre_child_context(
         crate::snow_stage3_v11_current_context_capture::PreChildContext {
+            phase: crate::snow_stage3_v11_current_context_capture::PreChildCapturePhaseV1::Covered,
             context,
             parent: beginning_parent,
             consumer: beginning_consumer,
--- a/crates/openwepp-hillslope-orchestrator/src/v11_covered/physical_outcome_ledger.rs
+++ b/crates/openwepp-hillslope-orchestrator/src/v11_covered/physical_outcome_ledger.rs
@@ -118,8 +118,7 @@
 /// Unpublished receipt for one adaptive/root trial. This is deliberately not
 /// the final dormant-endpoint receipt: rejected/full alternatives remain
 /// evidence and cannot claim an accepted terminal owner transition.
-#[derive(Clone, Debug, PartialEq)]
-#[derive(serde::Serialize)]
+#[derive(Clone, Debug, PartialEq, serde::Serialize)]
 pub(crate) struct TerminalSnowSoilTrialReceiptV1 {
     pub experimental_bulk: Option<crate::snow_accuracy_bulk::Receipt>,
     pub support: TimeSupport,
@@ -490,8 +489,7 @@
 /// Event-integrated lower-boundary custody for a lane whose accepted ending
 /// snow owner is dormant.  Unlike the persistent receipt, this wire has no
 /// ending snow temperature: dormancy is proved by the ending owner identity.
-#[derive(Clone, Debug, PartialEq)]
-#[derive(serde::Serialize)]
+#[derive(Clone, Debug, PartialEq, serde::Serialize)]
 pub(crate) struct TerminalSnowSoilHeatReceiptV1 {
     pub support: TimeSupport,
     pub lane_id: u32,
@@ -663,8 +661,7 @@
     pub source_receipts_sha256: [Digest32; 6],
 }
 
-#[derive(Clone, Debug, PartialEq)]
-#[derive(serde::Serialize)]
+#[derive(Clone, Debug, PartialEq, serde::Serialize)]
 pub(crate) struct Stage3LanePhysicalOutcomeLedgerV1 {
     pub support: TimeSupport,
     pub lane_id: u32,
--- a/crates/openwepp-hillslope-orchestrator/src/v9_real_consumer_shadow.rs
+++ b/crates/openwepp-hillslope-orchestrator/src/v9_real_consumer_shadow.rs
@@ -104,13 +104,17 @@
     validate_precipitation_phase_parcel_set,
 };
 use crate::vegetation_real_hydrology_shadow::{
-    RealHydrologyLaneLayerMap, RealHydrologyOfeLaneId, RealHydrologyShadowAdapter,
-    RealHydrologyShadowError,
+    RealHydrologyLaneLayerMap, RealHydrologyShadowAdapter, RealHydrologyShadowError,
 };
 use crate::{
-    DirectDayFrame, DirectOfeWb14Parameters, DirectPublicationDayInput, DirectRunConstructorInputs,
-    DirectRunFrame, DirectSurfaceLiquidConfiguration,
+    DirectDayFrame, DirectOfeWb14Parameters, DirectPublicationDayInput, DirectRunFrame,
+    DirectSurfaceLiquidConfiguration,
 };
+
+#[cfg(test)]
+use crate::DirectRunConstructorInputs;
+#[cfg(test)]
+use crate::vegetation_real_hydrology_shadow::RealHydrologyOfeLaneId;
 
 #[path = "canonical_owner_bytes.rs"]
 mod canonical_owner_bytes;
@@ -130,6 +134,7 @@
 #[cfg(test)]
 pub(crate) use v11_covered::audit_covered_carrier_support;
 pub(crate) use v11_covered::normalize_v11_staged_parent_lineage;
+pub(crate) use v11_covered::physical_outcome_ledger::TerminalSnowSoilHeatReceiptV1;
 pub(crate) use v11_covered::physical_outcome_ledger::TerminalSnowSoilTrialReceiptV1;
 pub(crate) use v11_covered::physical_outcome_ledger::ledger_set_digest as stage3_physical_outcome_ledger_set_digest;
 pub use v11_covered::physical_outcome_ledger::{
@@ -352,6 +357,35 @@
 
 include!("v9_real_consumer_shadow_forcing.rs");
 include!("v9_real_consumer_shadow/v10_soil_thermal_v2.rs");
+
+#[cfg(feature = "persisted-restart-v1")]
+#[cfg(test)]
+fn diagnostic_native_field<T: serde::de::DeserializeOwned>(
+    captured: &serde_json::Value,
+    name: &str,
+) -> Result<T, DirectV10RealConsumerError> {
+    let value = captured
+        .get(name)
+        .cloned()
+        .ok_or(DirectV10RealConsumerError::Runtime(
+            DirectV9RealConsumerError::Identity("diagnostic native constructor operand"),
+        ))?;
+    serde_json::from_value(value).map_err(|error| {
+        DirectV10RealConsumerError::Runtime(DirectV9RealConsumerError::Serialization(
+            error.to_string(),
+        ))
+    })
+}
+
+#[cfg(feature = "persisted-restart-v1")]
+#[cfg(test)]
+pub(crate) fn diagnostic_canonicalize_soil_operand_chain_v1(
+    topology: &openwepp_land_surface_energy::SoilThermalOwnerEnvelopeV2,
+    operands: &mut [openwepp_land_surface_energy::SoilThermalAcceptedEnergyOperandV2],
+) -> Result<(), DirectV9RealConsumerError> {
+    reordinal_and_canonicalize_v2_operands(topology, operands)
+}
+
 include!("v9_real_consumer_shadow/native_soil_restart_admission.rs");
 #[derive(Clone, Debug, PartialEq)]
 pub struct DirectV9RealConsumerShadow {
@@ -573,6 +607,7 @@
         feature = "persisted-restart-v1"
     ))]
     #[allow(clippy::too_many_arguments)]
+    #[cfg(test)]
     pub(crate) fn diagnostic_restore_native_consumer_v1(
         captured: &serde_json::Value,
         target_soil: DirectV10SoilThermalResidentV2,
@@ -586,37 +621,13 @@
         pinned_provider_static_configuration: &SnowFreeHalfHourStaticConfiguration,
         pinned_hydrology_constructor_inputs: std::sync::Arc<DirectRunConstructorInputs>,
     ) -> Result<Self, DirectV10RealConsumerError> {
-        let get = |name| {
-            captured.get(name).cloned().ok_or_else(|| {
-                DirectV10RealConsumerError::Runtime(DirectV9RealConsumerError::Identity(
-                    "diagnostic native constructor operand",
-                ))
-            })
-        };
-        let vegetation_configuration = serde_json::from_value(get("vegetation_configuration")?)
-            .map_err(|e| {
-                DirectV10RealConsumerError::Runtime(DirectV9RealConsumerError::Serialization(
-                    e.to_string(),
-                ))
-            })?;
+        let vegetation_configuration =
+            diagnostic_native_field(captured, "vegetation_configuration")?;
         let vegetation_state: V10CoupledOwnedState =
-            serde_json::from_value(get("vegetation_state")?).map_err(|e| {
-                DirectV10RealConsumerError::Runtime(DirectV9RealConsumerError::Serialization(
-                    e.to_string(),
-                ))
-            })?;
-        let vegetation_owner_id =
-            serde_json::from_value(get("vegetation_owner_id")?).map_err(|e| {
-                DirectV10RealConsumerError::Runtime(DirectV9RealConsumerError::Serialization(
-                    e.to_string(),
-                ))
-            })?;
+            diagnostic_native_field(captured, "vegetation_state")?;
+        let vegetation_owner_id = diagnostic_native_field(captured, "vegetation_owner_id")?;
         let captured_lse_configuration: LandSurfaceEnergyConfiguration =
-            serde_json::from_value(get("lse_configuration")?).map_err(|e| {
-                DirectV10RealConsumerError::Runtime(DirectV9RealConsumerError::Serialization(
-                    e.to_string(),
-                ))
-            })?;
+            diagnostic_native_field(captured, "lse_configuration")?;
         if &captured_lse_configuration != pinned_lse_configuration {
             return Err(DirectV9RealConsumerError::Identity(
                 "diagnostic main LSE configuration/pinned authority join",
@@ -624,83 +635,29 @@
             .into());
         }
         let lse_configuration = pinned_lse_configuration.clone();
-        let lse_state: LandSurfaceEnergyV2State = serde_json::from_value(get("lse_state")?)
-            .map_err(|e| {
-                DirectV10RealConsumerError::Runtime(DirectV9RealConsumerError::Serialization(
-                    e.to_string(),
-                ))
-            })?;
-        let surface_configuration_canonical_json: Vec<u8> =
-            serde_json::from_value(get("surface_configuration_canonical_json")?).map_err(|e| {
-                DirectV10RealConsumerError::Runtime(DirectV9RealConsumerError::Serialization(
-                    e.to_string(),
-                ))
-            })?;
-        let captured_surface_configuration =
-            DirectSurfaceLiquidConfiguration::from_canonical_bytes(
-                &surface_configuration_canonical_json,
-            )?;
-        if captured_surface_configuration.canonical_bytes()?
-            != pinned_surface_configuration.canonical_bytes()?
-        {
-            return Err(DirectV9RealConsumerError::Identity(
-                "diagnostic surface configuration/pinned authority join",
-            )
-            .into());
-        }
+        let lse_state: LandSurfaceEnergyV2State = diagnostic_native_field(captured, "lse_state")?;
+        Self::diagnostic_validate_surface_configuration_v1(captured, pinned_surface_configuration)?;
         let surface_configuration = pinned_surface_configuration.clone();
-        let biogeochemistry = serde_json::from_value(get("biogeochemistry")?).map_err(|e| {
-            DirectV10RealConsumerError::Runtime(DirectV9RealConsumerError::Serialization(
-                e.to_string(),
-            ))
-        })?;
-        let next_day_index = serde_json::from_value(get("next_day_index")?).map_err(|e| {
-            DirectV10RealConsumerError::Runtime(DirectV9RealConsumerError::Serialization(
-                e.to_string(),
-            ))
-        })?;
-        let gsi_owner_configuration = serde_json::from_value(get("gsi_owner_configuration")?)
-            .map_err(|e| {
-                DirectV10RealConsumerError::Runtime(DirectV9RealConsumerError::Serialization(
-                    e.to_string(),
-                ))
-            })?;
+        let biogeochemistry = diagnostic_native_field(captured, "biogeochemistry")?;
+        let next_day_index = diagnostic_native_field(captured, "next_day_index")?;
+        let gsi_owner_configuration = diagnostic_native_field(captured, "gsi_owner_configuration")?;
         let gsi_owner_state: crate::runtime_inputs::DirectGsiOwnerStateV1 =
-            serde_json::from_value(get("gsi_owner_state")?).map_err(|e| {
-                DirectV10RealConsumerError::Runtime(DirectV9RealConsumerError::Serialization(
-                    e.to_string(),
-                ))
-            })?;
+            diagnostic_native_field(captured, "gsi_owner_state")?;
         let gsi_state =
             crate::runtime_inputs::restart_authority_restore_gsi_state(&gsi_owner_state)?;
         let provider_static_configuration: crate::snow_stage3_v11_current_context_capture::
-            ProviderStaticConfigurationCaptureV1 = serde_json::from_value(get(
-            "provider_static_configuration",
-        )?)
-        .map_err(|e| {
-            DirectV10RealConsumerError::Runtime(DirectV9RealConsumerError::Serialization(
-                e.to_string(),
-            ))
-        })?;
+            ProviderStaticConfigurationCaptureV1 = diagnostic_native_field(captured, "provider_static_configuration")?;
         provider_static_configuration.validate_against(pinned_provider_static_configuration)?;
         let provider_static_configuration = pinned_provider_static_configuration.clone();
         let provider_cursor_canonical_json: Vec<u8> =
-            serde_json::from_value(get("provider_cursor_canonical_json")?).map_err(|e| {
-                DirectV10RealConsumerError::Runtime(DirectV9RealConsumerError::Serialization(
-                    e.to_string(),
-                ))
-            })?;
+            diagnostic_native_field(captured, "provider_cursor_canonical_json")?;
         let provider_cursor = SnowFreeHalfHourProviderCursor::restore_json(
             &provider_cursor_canonical_json,
             &provider_static_configuration,
             next_day_index,
         )?;
         let root_zone_hydraulic_configuration =
-            serde_json::from_value(get("root_zone_hydraulic_configuration")?).map_err(|e| {
-                DirectV10RealConsumerError::Runtime(DirectV9RealConsumerError::Serialization(
-                    e.to_string(),
-                ))
-            })?;
+            diagnostic_native_field(captured, "root_zone_hydraulic_configuration")?;
         let proof = NativeSoilRestartAdmissionV1::authenticate(
             &target_soil,
             &committed_bootstrap,
@@ -737,117 +694,155 @@
             clock,
             NativeSoilRestartPositionV1::InProgressSupportCurrent,
         )?;
-        let history: Vec<u8> = serde_json::from_value(get("accepted_publication_rotation")?)
-            .map_err(|error| {
-                DirectV10RealConsumerError::Runtime(DirectV9RealConsumerError::Serialization(
-                    error.to_string(),
-                ))
-            })?;
+        let history: Vec<u8> = diagnostic_native_field(captured, "accepted_publication_rotation")?;
         restored
             .restart_authority_restore_accepted_publication_rotation_canonical_bytes_v3(&history)?;
-        let wb14: Option<Vec<u8>> =
-            serde_json::from_value(get("wb14_parent")?).map_err(|error| {
-                DirectV10RealConsumerError::Runtime(DirectV9RealConsumerError::Serialization(
-                    error.to_string(),
-                ))
-            })?;
+        let wb14: Option<Vec<u8>> = diagnostic_native_field(captured, "wb14_parent")?;
         restored.restart_authority_restore_native_wb14_parent_canonical_bytes(
             wb14.as_deref(),
             &proof,
             clock,
             NativeSoilRestartPositionV1::InProgressSupportCurrent,
         )?;
+        restored.diagnostic_restore_captured_residents_v1(
+            captured,
+            pinned_frozen_litter_lse_configuration,
+            pinned_surface_configuration,
+        )?;
+        restored.diagnostic_install_hydrology_constructor_inputs_v1(
+            pinned_hydrology_constructor_inputs,
+        );
+        Ok(restored)
+    }
+    #[cfg(all(test, feature = "persisted-restart-v1"))]
+    fn diagnostic_validate_surface_configuration_v1(
+        captured: &serde_json::Value,
+        pinned_surface_configuration: &DirectSurfaceLiquidConfiguration,
+    ) -> Result<(), DirectV10RealConsumerError> {
+        let surface_configuration_canonical_json: Vec<u8> =
+            diagnostic_native_field(captured, "surface_configuration_canonical_json")?;
+        let captured_surface_configuration =
+            DirectSurfaceLiquidConfiguration::from_canonical_bytes(
+                &surface_configuration_canonical_json,
+            )?;
+        if captured_surface_configuration.canonical_bytes()?
+            != pinned_surface_configuration.canonical_bytes()?
+        {
+            return Err(DirectV9RealConsumerError::Identity(
+                "diagnostic surface configuration/pinned authority join",
+            )
+            .into());
+        }
+        Ok(())
+    }
+
+    #[cfg(all(test, feature = "persisted-restart-v1"))]
+    fn diagnostic_restore_captured_residents_v1(
+        &mut self,
+        captured: &serde_json::Value,
+        pinned_frozen_litter_lse_configuration: &LandSurfaceEnergyConfiguration,
+        pinned_surface_configuration: &DirectSurfaceLiquidConfiguration,
+    ) -> Result<(), DirectV10RealConsumerError> {
         let frozen_litter_residents: Option<DiagnosticFrozenLitterResidentsV1> =
-            serde_json::from_value(get("frozen_litter_residents")?).map_err(|error| {
-                DirectV10RealConsumerError::Runtime(DirectV9RealConsumerError::Serialization(
-                    error.to_string(),
-                ))
-            })?;
-        let frozen = frozen_litter_residents.ok_or_else(|| {
-            DirectV10RealConsumerError::Runtime(DirectV9RealConsumerError::Identity(
+            diagnostic_native_field(captured, "frozen_litter_residents")?;
+        let frozen = frozen_litter_residents.ok_or(DirectV10RealConsumerError::Runtime(
+            DirectV9RealConsumerError::Identity(
                 "diagnostic native target requires frozen-litter V3/V4 residents",
-            ))
-        })?;
-        {
-            if &frozen.lse_configuration != pinned_frozen_litter_lse_configuration {
-                return Err(DirectV9RealConsumerError::Identity(
-                    "diagnostic frozen-litter LSE configuration/pinned authority join",
-                )
-                .into());
-            }
-            let surface_parent = DirectSurfaceLiquidConfiguration::from_canonical_bytes(
-                &frozen.surface_parent_configuration_canonical_bytes,
-            )?;
-            if surface_parent.canonical_bytes()?
-                != pinned_surface_configuration.canonical_bytes()?
-            {
-                return Err(DirectV9RealConsumerError::Identity(
-                    "diagnostic frozen-litter surface parent/pinned authority join",
-                )
-                .into());
-            }
-            let surface_model = crate::direct_runtime::SurfaceLiquidOwnerModelDefinitionV2::
-                restart_authority_from_canonical_bytes(
-                    &frozen.surface_model_definition_canonical_bytes,
-                )?;
-            let surface_configuration =
-                crate::direct_runtime::SurfaceLiquidConfigurationV2::from_canonical_bytes(
-                    surface_parent,
-                    surface_model,
-                    &frozen.surface_configuration_canonical_bytes,
-                )?;
-            let surface_owner =
-                crate::direct_runtime::SurfaceLiquidOwnerEnvelopeV2::from_canonical_bytes(
-                    surface_configuration.parent(),
-                    Some(&surface_configuration),
-                    &frozen.surface_owner_canonical_bytes,
-                )?;
-            let mut physical = FrozenLitterV3Resident::try_new(
-                pinned_frozen_litter_lse_configuration.clone(),
-                frozen.lse_state,
-                surface_configuration,
-                surface_owner,
-            )?;
-            physical.restore_restart_authority(
-                &frozen.physical_publication_history,
-                frozen.physical_wb14_parent.as_deref(),
-                &frozen.physical_predecessor_receipt_chain_sha256,
-            )?;
-            let exact_owner = crate::LseSurfaceEnthalpyOwnerEnvelopeV1::from_canonical_bytes(
-                &frozen.exact_surface_owner_canonical_bytes,
-            )?;
-            let exact = FrozenLitterV4Resident::try_restore(
-                &physical,
-                exact_owner,
-                &frozen.exact_publication_history,
-                &frozen.exact_publication_beginning_lse_v3_state_sha256,
-            )?;
-            restored.install_restored_frozen_litter_v4_residents(physical, exact)?;
-        }
-        let accepted_interval_count: u64 = serde_json::from_value(get("accepted_interval_count")?)
-            .map_err(|error| {
-                DirectV10RealConsumerError::Runtime(DirectV9RealConsumerError::Serialization(
-                    error.to_string(),
-                ))
-            })?;
+            ),
+        ))?;
+        self.diagnostic_restore_frozen_litter_residents_v1(
+            frozen,
+            pinned_frozen_litter_lse_configuration,
+            pinned_surface_configuration,
+        )?;
+        let accepted_interval_count: u64 =
+            diagnostic_native_field(captured, "accepted_interval_count")?;
         let provider_gsi_receipt_sha256: String =
-            serde_json::from_value(get("provider_gsi_receipt_sha256")?).map_err(|error| {
-                DirectV10RealConsumerError::Runtime(DirectV9RealConsumerError::Serialization(
-                    error.to_string(),
-                ))
-            })?;
+            diagnostic_native_field(captured, "provider_gsi_receipt_sha256")?;
         if provider_gsi_receipt_sha256.len() != 64 {
             return Err(DirectV9RealConsumerError::Identity(
                 "diagnostic provider GSI receipt width",
             )
             .into());
         }
-        restored.inner.accepted_interval_count = accepted_interval_count;
-        restored.inner.provider_gsi_receipt_sha256 = provider_gsi_receipt_sha256;
-        restored.diagnostic_install_hydrology_constructor_inputs_v1(
-            pinned_hydrology_constructor_inputs,
-        );
-        Ok(restored)
+        // This diagnostic must not authenticate scheduler position by assignment.
+        // The file composer separately verifies outer position and retains any
+        // original nested mismatch. This constructor never installs that scalar.
+        if self.inner.accepted_interval_count != accepted_interval_count {
+            return Err(DirectV9RealConsumerError::Identity(
+                "diagnostic captured scheduler position differs from constructor",
+            )
+            .into());
+        }
+        if self.inner.provider_gsi_receipt_sha256 != provider_gsi_receipt_sha256 {
+            return Err(DirectV9RealConsumerError::Identity(
+                "diagnostic captured GSI receipt differs from constructor",
+            )
+            .into());
+        }
+        Ok(())
+    }
+
+    #[cfg(test)]
+    fn diagnostic_restore_frozen_litter_residents_v1(
+        &mut self,
+        frozen: DiagnosticFrozenLitterResidentsV1,
+        pinned_frozen_litter_lse_configuration: &LandSurfaceEnergyConfiguration,
+        pinned_surface_configuration: &DirectSurfaceLiquidConfiguration,
+    ) -> Result<(), DirectV10RealConsumerError> {
+        if &frozen.lse_configuration != pinned_frozen_litter_lse_configuration {
+            return Err(DirectV9RealConsumerError::Identity(
+                "diagnostic frozen-litter LSE configuration/pinned authority join",
+            )
+            .into());
+        }
+        let surface_parent = DirectSurfaceLiquidConfiguration::from_canonical_bytes(
+            &frozen.surface_parent_configuration_canonical_bytes,
+        )?;
+        if surface_parent.canonical_bytes()? != pinned_surface_configuration.canonical_bytes()? {
+            return Err(DirectV9RealConsumerError::Identity(
+                "diagnostic frozen-litter surface parent/pinned authority join",
+            )
+            .into());
+        }
+        let surface_model = crate::direct_runtime::SurfaceLiquidOwnerModelDefinitionV2::
+            restart_authority_from_canonical_bytes(
+                &frozen.surface_model_definition_canonical_bytes,
+            )?;
+        let surface_configuration =
+            crate::direct_runtime::SurfaceLiquidConfigurationV2::from_canonical_bytes(
+                surface_parent,
+                surface_model,
+                &frozen.surface_configuration_canonical_bytes,
+            )?;
+        let surface_owner =
+            crate::direct_runtime::SurfaceLiquidOwnerEnvelopeV2::from_canonical_bytes(
+                surface_configuration.parent(),
+                Some(&surface_configuration),
+                &frozen.surface_owner_canonical_bytes,
+            )?;
+        let mut physical = FrozenLitterV3Resident::try_new(
+            pinned_frozen_litter_lse_configuration.clone(),
+            frozen.lse_state,
+            surface_configuration,
+            surface_owner,
+        )?;
+        physical.restore_restart_authority(
+            &frozen.physical_publication_history,
+            frozen.physical_wb14_parent.as_deref(),
+            &frozen.physical_predecessor_receipt_chain_sha256,
+        )?;
+        let exact_owner = crate::LseSurfaceEnthalpyOwnerEnvelopeV1::from_canonical_bytes(
+            &frozen.exact_surface_owner_canonical_bytes,
+        )?;
+        let exact = FrozenLitterV4Resident::try_restore(
+            &physical,
+            exact_owner,
+            &frozen.exact_publication_history,
+            &frozen.exact_publication_beginning_lse_v3_state_sha256,
+        )?;
+        self.install_restored_frozen_litter_v4_residents(physical, exact)?;
+        Ok(())
     }
 
     pub(crate) fn diagnostic_native_lane_maps_v1(&self) -> Vec<DiagnosticNativeLaneMapV1> {
@@ -868,6 +863,7 @@
         self.soil_thermal_v2().cloned()
     }
 
+    #[cfg(test)]
     pub(crate) fn diagnostic_restore_native_lane_maps_v1(
         maps: Vec<DiagnosticNativeLaneMapV1>,
     ) -> Result<Vec<RealHydrologyLaneLayerMap>, DirectV10RealConsumerError> {
--- a/crates/openwepp-hillslope-orchestrator/src/v9_real_consumer_shadow/composite_lse_owner.rs
+++ b/crates/openwepp-hillslope-orchestrator/src/v9_real_consumer_shadow/composite_lse_owner.rs
@@ -1,23 +1,34 @@
 //! Scientific encoding/validation of the shared policy14 component transport.
-use openwepp_land_surface_energy::{LandSurfaceEnergyState, LandSurfaceEnergyV3State};
 use crate::{DirectSurfaceLiquidError as Error, LseSurfaceEnthalpyOwnerEnvelopeV1};
 pub(crate) use openwepp_coupled_time::Policy14LseOwnerTransport as CompositeLseOwnerV1;
+use openwepp_land_surface_energy::{LandSurfaceEnergyState, LandSurfaceEnergyV3State};
 
-pub(crate) fn composite_lse_bytes(authority: openwepp_coupled_time::Policy14RuntimeAuthority, active: &LandSurfaceEnergyState,
-    native: &LandSurfaceEnergyV3State, exact: &LseSurfaceEnthalpyOwnerEnvelopeV1,
+pub(crate) fn composite_lse_bytes(
+    authority: openwepp_coupled_time::Policy14RuntimeAuthority,
+    active: &LandSurfaceEnergyState,
+    native: &LandSurfaceEnergyV3State,
+    exact: &LseSurfaceEnthalpyOwnerEnvelopeV1,
 ) -> Result<Vec<u8>, Error> {
-    active.validate_schema().map_err(|_| Error::Identity("composite active LSE schema"))?;
-    let active_bytes = serde_json::to_vec(active).map_err(|_| Error::Schema("composite active encoding"))?;
-    let native_bytes = serde_json::to_vec(native).map_err(|_| Error::Schema("composite native encoding"))?;
-    let exact_bytes = exact.canonical_bytes().map_err(|_| Error::Identity("composite exact encoding"))?;
+    active
+        .validate_schema()
+        .map_err(|_| Error::Identity("composite active LSE schema"))?;
+    let active_bytes =
+        serde_json::to_vec(active).map_err(|_| Error::Schema("composite active encoding"))?;
+    let native_bytes =
+        serde_json::to_vec(native).map_err(|_| Error::Schema("composite native encoding"))?;
+    let exact_bytes = exact
+        .canonical_bytes()
+        .map_err(|_| Error::Identity("composite exact encoding"))?;
     CompositeLseOwnerV1::new(authority, active_bytes, native_bytes, exact_bytes)
-        .and_then(|value| value.canonical_bytes()).map_err(|_| Error::Identity("composite component transport"))
+        .and_then(|value| value.canonical_bytes())
+        .map_err(|_| Error::Identity("composite component transport"))
 }
 
-pub(crate) fn actual_runtime_authority(value: openwepp_land_surface_energy::CoveredColumnAuthority)
-    -> openwepp_coupled_time::Policy14RuntimeAuthority {
+pub(crate) fn actual_runtime_authority(
+    value: openwepp_land_surface_energy::CoveredColumnAuthority,
+) -> openwepp_coupled_time::Policy14RuntimeAuthority {
+    use openwepp_coupled_time::Policy14RuntimeAuthority as Wire;
     use openwepp_land_surface_energy::CoveredColumnAuthority as Actual;
-    use openwepp_coupled_time::Policy14RuntimeAuthority as Wire;
     match value {
         Actual::HistoricalV8 => Wire::HistoricalV8,
         Actual::V10NonpositiveAssimilation => Wire::V10NonpositiveAssimilation,
--- a/crates/openwepp-hillslope-orchestrator/src/v9_real_consumer_shadow/direct_v9_real_consumer_shadow_impl.rs
+++ b/crates/openwepp-hillslope-orchestrator/src/v9_real_consumer_shadow/direct_v9_real_consumer_shadow_impl.rs
@@ -203,6 +203,22 @@
             crate::snow_accuracy_observer::physical_enabled().then(|| {
                 std::sync::Arc::new(hydrology_frame.diagnostic_constructor_inputs_snapshot_v1())
             });
+        #[cfg(all(
+            test,
+            any(
+                feature = "restart-authority-evidence",
+                feature = "persisted-restart-v1"
+            )
+        ))]
+        let hydrology_constructor_inputs = hydrology_constructor_inputs.or_else(|| {
+            crate::snow_stage3_v11_current_context_capture::
+                pre_child_recorder_test_observer_active_v1()
+                .then(|| {
+                    std::sync::Arc::new(
+                        hydrology_frame.diagnostic_constructor_inputs_snapshot_v1(),
+                    )
+                })
+        });
         let value = Self {
             authority,
             provider_gsi_receipt_sha256,
--- a/crates/openwepp-hillslope-orchestrator/src/v9_real_consumer_shadow/frozen_litter_v3_adoption.rs
+++ b/crates/openwepp-hillslope-orchestrator/src/v9_real_consumer_shadow/frozen_litter_v3_adoption.rs
@@ -17,7 +17,9 @@
     LandSurfaceEnergyConfiguration, LandSurfaceEnergyV3State, SurfaceConfiguration,
 };
 
-use super::frozen_litter_v3_publication_retention::{FrozenLitterV3PublicationSupportV1, FrozenLitterHistoryEntryV1, FrozenLitterHistoryIdentityV1};
+use super::frozen_litter_v3_publication_retention::{
+    FrozenLitterHistoryEntryV1, FrozenLitterHistoryIdentityV1, FrozenLitterV3PublicationSupportV1,
+};
 use super::{DirectV10RealConsumerError, DirectV10RealConsumerShadow};
 
 #[cfg(test)]
@@ -114,10 +116,7 @@
 }
 
 impl ValidatedFrozenLitterV3ResidentRevisionV1 {
-    fn append_publication_chain(
-        chain: Digest32,
-        support: &FrozenLitterHistoryEntryV1,
-    ) -> Digest32 {
+    fn append_publication_chain(chain: Digest32, support: &FrozenLitterHistoryEntryV1) -> Digest32 {
         digest_bytes(
             &[
                 b"OPENWEPP_FROZEN_LITTER_V3_VALIDATED_HISTORY_V1\0".as_slice(),
@@ -326,16 +325,28 @@
 
     #[cfg(feature = "persisted-restart-v1")]
     pub fn restart_authority_validate_native_adoption(
-        &self, lse_sha256: &str, surface_sha256: &str,
+        &self,
+        lse_sha256: &str,
+        surface_sha256: &str,
     ) -> Result<(), crate::DirectSurfaceLiquidError> {
-        let (beginning_lse, beginning_surface) = if let Some(first) = self.accepted_publications.first() {
+        let (beginning_lse, beginning_surface) = if let Some(first) =
+            self.accepted_publications.first()
+        {
             let identity = first.identity(&self.surface_configuration, &self.lse_configuration)?;
-            (identity.predecessor_receipt_chain_sha256, identity.beginning_surface_owner_sha256)
+            (
+                identity.predecessor_receipt_chain_sha256,
+                identity.beginning_surface_owner_sha256,
+            )
         } else {
-            (self.lse_state.0.state_sha256.to_string(), self.surface_owner.envelope_sha256().to_owned())
+            (
+                self.lse_state.0.state_sha256.to_string(),
+                self.surface_owner.envelope_sha256().to_owned(),
+            )
         };
         if beginning_lse != lse_sha256 || beginning_surface != surface_sha256 {
-            return Err(crate::DirectSurfaceLiquidError::Identity("native restart external adoption anchor"));
+            return Err(crate::DirectSurfaceLiquidError::Identity(
+                "native restart external adoption anchor",
+            ));
         }
         Ok(())
     }
@@ -349,7 +360,9 @@
     ) -> Result<Vec<Vec<u8>>, crate::DirectSurfaceLiquidError> {
         self.accepted_publications
             .iter()
-            .map(|support| support.canonical_bytes(&self.surface_configuration, &self.lse_configuration))
+            .map(|support| {
+                support.canonical_bytes(&self.surface_configuration, &self.lse_configuration)
+            })
             .collect()
     }
 
@@ -453,20 +466,24 @@
                 #[cfg(test)]
                 FROZEN_LITTER_V3_FULL_HISTORY_VALIDATIONS.with(|count| count.set(count.get() + 1));
                 FrozenLitterHistoryEntryV1::from_canonical_bytes(
-                    &self.surface_configuration, &self.lse_configuration,
+                    &self.surface_configuration,
+                    &self.lse_configuration,
                     value,
                 )
             })
             .collect::<Result<Vec<_>, _>>()?;
-        let native_history = super::frozen_litter_v3_publication_retention::validate_native_history_v1(
-            &restored,&self.surface_configuration,&self.lse_configuration)?;
+        let native_history =
+            super::frozen_litter_v3_publication_retention::validate_native_history_v1(
+                &restored,
+                &self.surface_configuration,
+                &self.lse_configuration,
+            )?;
         let projections = restored
             .iter()
             .map(|support| support.identity(&self.surface_configuration, &self.lse_configuration))
             .collect::<Result<Vec<_>, _>>()?;
         if projections.windows(2).any(|pair| {
-            pair[1].predecessor_receipt_chain_sha256
-                != pair[0].receipt_chain_sha256
+            pair[1].predecessor_receipt_chain_sha256 != pair[0].receipt_chain_sha256
                 || pair[1].beginning_surface_owner_sha256 != pair[0].ending_surface_owner_sha256
         }) || projections.last().is_some_and(|projection| {
             projection.receipt_chain_sha256 != self.predecessor_receipt_chain_sha256
@@ -514,20 +531,24 @@
                 #[cfg(test)]
                 FROZEN_LITTER_V3_FULL_HISTORY_VALIDATIONS.with(|count| count.set(count.get() + 1));
                 FrozenLitterHistoryEntryV1::from_canonical_bytes(
-                    &self.surface_configuration, &self.lse_configuration,
+                    &self.surface_configuration,
+                    &self.lse_configuration,
                     bytes,
                 )
             })
             .collect::<Result<Vec<_>, _>>()?;
-        let native_history = super::frozen_litter_v3_publication_retention::validate_native_history_v1(
-            &restored_publications,&self.surface_configuration,&self.lse_configuration)?;
+        let native_history =
+            super::frozen_litter_v3_publication_retention::validate_native_history_v1(
+                &restored_publications,
+                &self.surface_configuration,
+                &self.lse_configuration,
+            )?;
         let restored_projections = restored_publications
             .iter()
             .map(|support| support.identity(&self.surface_configuration, &self.lse_configuration))
             .collect::<Result<Vec<_>, _>>()?;
         if restored_projections.windows(2).any(|pair| {
-            pair[1].predecessor_receipt_chain_sha256
-                != pair[0].receipt_chain_sha256
+            pair[1].predecessor_receipt_chain_sha256 != pair[0].receipt_chain_sha256
                 || pair[1].beginning_surface_owner_sha256 != pair[0].ending_surface_owner_sha256
         }) {
             return Err(crate::DirectSurfaceLiquidError::Identity(
@@ -619,7 +640,10 @@
             &candidate.litter_phase_receipts,
         )?);
         let mut native_history = self.validated_revision.native_history.clone();
-        native_history.advance(&support, FrozenLitterHistoryIdentityV1::from_phase(&candidate.complete_owner_projection))?;
+        native_history.advance(
+            &support,
+            FrozenLitterHistoryIdentityV1::from_phase(&candidate.complete_owner_projection),
+        )?;
         let identity = candidate.complete_owner_projection.identity();
         let validated_revision = ValidatedFrozenLitterV3ResidentRevisionV1 {
             lse_configuration_sha256: self.lse_configuration.configuration_sha256.to_string(),
@@ -659,37 +683,73 @@
     }
 
     pub(super) fn accept_native_snow_publication(
-        &mut self, record:super::native_snow_liquid_publication::NativeSnowLiquidPublicationV1,
-        expected_outputs:&[super::Stage3AcceptedSnowLiquidOutputV1],
-    )->Result<(),crate::DirectSurfaceLiquidError> {
+        &mut self,
+        record: super::native_snow_liquid_publication::NativeSnowLiquidPublicationV1,
+        expected_outputs: &[super::Stage3AcceptedSnowLiquidOutputV1],
+    ) -> Result<(), crate::DirectSurfaceLiquidError> {
         use crate::DirectSurfaceLiquidError as Error;
-        self.validated_revision.validate_same_revision(self)
-            .map_err(|_|Error::Identity("native drain beginning resident revision"))?;
-        record.validate(&self.surface_configuration,&self.lse_configuration)?;
-        if record.outputs!=expected_outputs || record.beginning_lse!=self.lse_state
-            || record.beginning_surface_bytes!=self.surface_owner.canonical_bytes(
-                self.surface_configuration.parent(),Some(&self.surface_configuration))?
-            || record.identity.predecessor_physical_receipt_chain_sha256!=self.predecessor_receipt_chain_sha256 {
-            return Err(Error::Identity("native drain authentic donor/recipient predecessor"));
-        }
-        let ending_surface=SurfaceLiquidOwnerEnvelopeV2::from_canonical_bytes(
-            self.surface_configuration.parent(),Some(&self.surface_configuration),&record.ending_surface_bytes)?;
-        let ending_parent = self.wb14_parent.as_ref().map(|parent|
-            parent.with_native_custody_candidate(&self.surface_configuration, &self.surface_owner, &ending_surface)
-        ).transpose()?;
-        let ending_lse=record.ending_lse.clone();
-        let entry=FrozenLitterHistoryEntryV1::Custody(Box::new(record));
-        let identity=entry.identity(&self.surface_configuration,&self.lse_configuration)?;
-        let mut history=self.accepted_publications.as_ref().clone();
+        self.validated_revision
+            .validate_same_revision(self)
+            .map_err(|_| Error::Identity("native drain beginning resident revision"))?;
+        record.validate(&self.surface_configuration, &self.lse_configuration)?;
+        if record.outputs != expected_outputs
+            || record.beginning_lse != self.lse_state
+            || record.beginning_surface_bytes
+                != self.surface_owner.canonical_bytes(
+                    self.surface_configuration.parent(),
+                    Some(&self.surface_configuration),
+                )?
+            || record.identity.predecessor_physical_receipt_chain_sha256
+                != self.predecessor_receipt_chain_sha256
+        {
+            return Err(Error::Identity(
+                "native drain authentic donor/recipient predecessor",
+            ));
+        }
+        let ending_surface = SurfaceLiquidOwnerEnvelopeV2::from_canonical_bytes(
+            self.surface_configuration.parent(),
+            Some(&self.surface_configuration),
+            &record.ending_surface_bytes,
+        )?;
+        let ending_parent = self
+            .wb14_parent
+            .as_ref()
+            .map(|parent| {
+                parent.with_native_custody_candidate(
+                    &self.surface_configuration,
+                    &self.surface_owner,
+                    &ending_surface,
+                )
+            })
+            .transpose()?;
+        let ending_lse = record.ending_lse.clone();
+        let entry = FrozenLitterHistoryEntryV1::Custody(Box::new(record));
+        let identity = entry.identity(&self.surface_configuration, &self.lse_configuration)?;
+        let mut history = self.accepted_publications.as_ref().clone();
         history.push(entry);
         let mut native_history = self.validated_revision.native_history.clone();
-        native_history.advance(history.last().ok_or(Error::Identity("native append missing tail"))?, identity.clone())?;
-        let revision=ValidatedFrozenLitterV3ResidentRevisionV1::from_validated_parts(
-            &self.lse_configuration,&ending_lse,&self.surface_configuration,&ending_surface,
-            &identity.receipt_chain_sha256,&history,Some(&identity),native_history);
-        self.lse_state=ending_lse;self.surface_owner=ending_surface;self.wb14_parent=ending_parent;
-        self.predecessor_receipt_chain_sha256=identity.receipt_chain_sha256;
-        self.accepted_publications=Arc::new(history);self.validated_revision=revision;
+        native_history.advance(
+            history
+                .last()
+                .ok_or(Error::Identity("native append missing tail"))?,
+            identity.clone(),
+        )?;
+        let revision = ValidatedFrozenLitterV3ResidentRevisionV1::from_validated_parts(
+            &self.lse_configuration,
+            &ending_lse,
+            &self.surface_configuration,
+            &ending_surface,
+            &identity.receipt_chain_sha256,
+            &history,
+            Some(&identity),
+            native_history,
+        );
+        self.lse_state = ending_lse;
+        self.surface_owner = ending_surface;
+        self.wb14_parent = ending_parent;
+        self.predecessor_receipt_chain_sha256 = identity.receipt_chain_sha256;
+        self.accepted_publications = Arc::new(history);
+        self.validated_revision = revision;
         Ok(())
     }
 
--- a/crates/openwepp-hillslope-orchestrator/src/v9_real_consumer_shadow/frozen_litter_v3_publication_retention.rs
+++ b/crates/openwepp-hillslope-orchestrator/src/v9_real_consumer_shadow/frozen_litter_v3_publication_retention.rs
@@ -365,63 +365,99 @@
 
 #[derive(Clone, Debug, PartialEq, Eq)]
 pub(crate) struct FrozenLitterHistoryIdentityV1 {
-    pub transaction_id:openwepp_kernel_contract::TransactionId,
-    pub predecessor_transaction_id:Option<openwepp_kernel_contract::TransactionId>,
-    pub parent_support_start_ns:u128,
-    pub parent_support_end_ns:u128,
-    pub support_start_ns:u128,
-    pub support_end_ns:u128,
-    pub beginning_surface_owner_sha256:String,
-    pub ending_surface_owner_sha256:String,
-    pub predecessor_receipt_chain_sha256:String,
-    pub receipt_chain_sha256:String,
+    pub transaction_id: openwepp_kernel_contract::TransactionId,
+    pub predecessor_transaction_id: Option<openwepp_kernel_contract::TransactionId>,
+    pub parent_support_start_ns: u128,
+    pub parent_support_end_ns: u128,
+    pub support_start_ns: u128,
+    pub support_end_ns: u128,
+    pub beginning_surface_owner_sha256: String,
+    pub ending_surface_owner_sha256: String,
+    pub predecessor_receipt_chain_sha256: String,
+    pub receipt_chain_sha256: String,
 }
 impl FrozenLitterHistoryIdentityV1 {
-    pub(crate) fn from_phase(projection:&crate::SurfaceLiquidCompleteOwnerProjectionV3)->Self {
-        let id=projection.identity();
-        Self {transaction_id:id.transaction_id,predecessor_transaction_id:id.predecessor_transaction_id,
-            parent_support_start_ns:id.parent_support_start_ns,parent_support_end_ns:id.parent_support_end_ns,
-            support_start_ns:id.support_start_ns,support_end_ns:id.support_end_ns,
-            beginning_surface_owner_sha256:id.beginning_surface_owner_sha256.clone(),
-            ending_surface_owner_sha256:projection.envelope_sha256().into(),
-            predecessor_receipt_chain_sha256:id.predecessor_receipt_chain_sha256.clone(),
-            receipt_chain_sha256:id.receipt_chain_sha256.clone()}
+    pub(crate) fn from_phase(projection: &crate::SurfaceLiquidCompleteOwnerProjectionV3) -> Self {
+        let id = projection.identity();
+        Self {
+            transaction_id: id.transaction_id,
+            predecessor_transaction_id: id.predecessor_transaction_id,
+            parent_support_start_ns: id.parent_support_start_ns,
+            parent_support_end_ns: id.parent_support_end_ns,
+            support_start_ns: id.support_start_ns,
+            support_end_ns: id.support_end_ns,
+            beginning_surface_owner_sha256: id.beginning_surface_owner_sha256.clone(),
+            ending_surface_owner_sha256: projection.envelope_sha256().into(),
+            predecessor_receipt_chain_sha256: id.predecessor_receipt_chain_sha256.clone(),
+            receipt_chain_sha256: id.receipt_chain_sha256.clone(),
+        }
     }
 }
 impl FrozenLitterHistoryEntryV1 {
-    pub(crate) fn publication_sha256(&self)->&Digest32 {
-        match self {Self::Phase(value)=>value.publication_sha256(),Self::Custody(value)=>&value.publication_sha256}
-    }
-    pub(crate) fn identity(&self,configuration:&crate::SurfaceLiquidConfigurationV2,
-        lse_configuration:&openwepp_land_surface_energy::LandSurfaceEnergyConfiguration)
-        ->Result<FrozenLitterHistoryIdentityV1,crate::DirectSurfaceLiquidError> {
+    pub(crate) fn publication_sha256(&self) -> &Digest32 {
         match self {
-            Self::Phase(value)=>Ok(FrozenLitterHistoryIdentityV1::from_phase(&value.complete_owner_projection(configuration)?)),
-            Self::Custody(value)=>{
-                value.validate(configuration,lse_configuration)?;
-                let id=&value.identity;
-                Ok(FrozenLitterHistoryIdentityV1 {transaction_id:id.transaction_id,
-                    predecessor_transaction_id:id.predecessor_transaction_id,
-                    parent_support_start_ns:id.parent_start_ns,parent_support_end_ns:id.parent_end_ns,
-                    support_start_ns:id.support_start_ns,support_end_ns:id.support_end_ns,
-                    beginning_surface_owner_sha256:value.beginning_exact.frozen_surface_owner_v2_sha256.to_string(),
-                    ending_surface_owner_sha256:value.ending_exact.frozen_surface_owner_v2_sha256.to_string(),
-                    predecessor_receipt_chain_sha256:id.predecessor_physical_receipt_chain_sha256.clone(),
-                    receipt_chain_sha256:value.receipt_sha256.as_bytes().iter().map(|byte|format!("{byte:02x}")).collect()})
+            Self::Phase(value) => value.publication_sha256(),
+            Self::Custody(value) => &value.publication_sha256,
+        }
+    }
+    pub(crate) fn identity(
+        &self,
+        configuration: &crate::SurfaceLiquidConfigurationV2,
+        lse_configuration: &openwepp_land_surface_energy::LandSurfaceEnergyConfiguration,
+    ) -> Result<FrozenLitterHistoryIdentityV1, crate::DirectSurfaceLiquidError> {
+        match self {
+            Self::Phase(value) => Ok(FrozenLitterHistoryIdentityV1::from_phase(
+                &value.complete_owner_projection(configuration)?,
+            )),
+            Self::Custody(value) => {
+                value.validate(configuration, lse_configuration)?;
+                let id = &value.identity;
+                Ok(FrozenLitterHistoryIdentityV1 {
+                    transaction_id: id.transaction_id,
+                    predecessor_transaction_id: id.predecessor_transaction_id,
+                    parent_support_start_ns: id.parent_start_ns,
+                    parent_support_end_ns: id.parent_end_ns,
+                    support_start_ns: id.support_start_ns,
+                    support_end_ns: id.support_end_ns,
+                    beginning_surface_owner_sha256: value
+                        .beginning_exact
+                        .frozen_surface_owner_v2_sha256
+                        .to_string(),
+                    ending_surface_owner_sha256: value
+                        .ending_exact
+                        .frozen_surface_owner_v2_sha256
+                        .to_string(),
+                    predecessor_receipt_chain_sha256: id
+                        .predecessor_physical_receipt_chain_sha256
+                        .clone(),
+                    receipt_chain_sha256: value
+                        .receipt_sha256
+                        .as_bytes()
+                        .iter()
+                        .map(|byte| format!("{byte:02x}"))
+                        .collect(),
+                })
             }
         }
     }
-    pub(crate) fn canonical_bytes(&self,configuration:&crate::SurfaceLiquidConfigurationV2,
-        lse_configuration:&openwepp_land_surface_energy::LandSurfaceEnergyConfiguration)
-        ->Result<Vec<u8>,crate::DirectSurfaceLiquidError> {
-        match self {Self::Phase(value)=>value.canonical_bytes(configuration),
-            Self::Custody(value)=>value.canonical_bytes(configuration,lse_configuration)}
-    }
-    pub(crate) fn from_canonical_bytes(configuration:&crate::SurfaceLiquidConfigurationV2,
-        lse_configuration:&openwepp_land_surface_energy::LandSurfaceEnergyConfiguration,bytes:&[u8])
-        ->Result<Self,crate::DirectSurfaceLiquidError> {
-        let probe:serde_json::Value=serde_json::from_slice(bytes)
-            .map_err(|_|crate::DirectSurfaceLiquidError::Schema("native history schema discriminator"))?;
+    pub(crate) fn canonical_bytes(
+        &self,
+        configuration: &crate::SurfaceLiquidConfigurationV2,
+        lse_configuration: &openwepp_land_surface_energy::LandSurfaceEnergyConfiguration,
+    ) -> Result<Vec<u8>, crate::DirectSurfaceLiquidError> {
+        match self {
+            Self::Phase(value) => value.canonical_bytes(configuration),
+            Self::Custody(value) => value.canonical_bytes(configuration, lse_configuration),
+        }
+    }
+    pub(crate) fn from_canonical_bytes(
+        configuration: &crate::SurfaceLiquidConfigurationV2,
+        lse_configuration: &openwepp_land_surface_energy::LandSurfaceEnergyConfiguration,
+        bytes: &[u8],
+    ) -> Result<Self, crate::DirectSurfaceLiquidError> {
+        let probe: serde_json::Value = serde_json::from_slice(bytes).map_err(|_| {
+            crate::DirectSurfaceLiquidError::Schema("native history schema discriminator")
+        })?;
         match probe.get("schema").and_then(serde_json::Value::as_str) {
             Some(FROZEN_LITTER_V3_PUBLICATION_SUPPORT_SCHEMA)=>Ok(Self::Phase(
                 FrozenLitterV3PublicationSupportV1::from_canonical_bytes(configuration,bytes)?)),
@@ -445,76 +481,136 @@
     dirty_parent_digest: Option<Digest32>,
 }
 impl NativeHistoryCursorV1 {
-    pub(crate) fn dirty_parent(&self) -> Option<(openwepp_kernel_contract::TransactionId, u128, u128, Digest32)> {
-        self.dirty_parent.zip(self.dirty_parent_digest).map(|((tx, start, end), digest)| (tx, start, end, digest))
-    }
-    pub(crate) fn advance(&mut self, entry: &FrozenLitterHistoryEntryV1,
-        current: FrozenLitterHistoryIdentityV1) -> Result<(), crate::DirectSurfaceLiquidError> {
+    pub(crate) fn dirty_parent(
+        &self,
+    ) -> Option<(
+        openwepp_kernel_contract::TransactionId,
+        u128,
+        u128,
+        Digest32,
+    )> {
+        self.dirty_parent
+            .zip(self.dirty_parent_digest)
+            .map(|((tx, start, end), digest)| (tx, start, end, digest))
+    }
+    pub(crate) fn advance(
+        &mut self,
+        entry: &FrozenLitterHistoryEntryV1,
+        current: FrozenLitterHistoryIdentityV1,
+    ) -> Result<(), crate::DirectSurfaceLiquidError> {
         use super::native_snow_liquid_publication::NativeSnowCustodyKindV1;
         use crate::DirectSurfaceLiquidError as Error;
-        let parent=(current.transaction_id,current.parent_support_start_ns,current.parent_support_end_ns);
-        if let Some((previous,previous_is_phase))=&self.prior {
-            if current.predecessor_receipt_chain_sha256!=previous.receipt_chain_sha256
-                || current.beginning_surface_owner_sha256!=previous.ending_surface_owner_sha256 {
+        let parent = (
+            current.transaction_id,
+            current.parent_support_start_ns,
+            current.parent_support_end_ns,
+        );
+        if let Some((previous, previous_is_phase)) = &self.prior {
+            if current.predecessor_receipt_chain_sha256 != previous.receipt_chain_sha256
+                || current.beginning_surface_owner_sha256 != previous.ending_surface_owner_sha256
+            {
                 return Err(Error::Identity("native history physical predecessor chain"));
             }
-            let same_support=current.support_start_ns==previous.support_start_ns
-                && current.support_end_ns==previous.support_end_ns;
-            if current.support_start_ns<previous.support_end_ns
-                && !(same_support && *previous_is_phase && matches!(entry,FrozenLitterHistoryEntryV1::Custody(_))) {
-                return Err(Error::Identity("native history overlapping or reordered origin support"));
+            let same_support = current.support_start_ns == previous.support_start_ns
+                && current.support_end_ns == previous.support_end_ns;
+            if current.support_start_ns < previous.support_end_ns
+                && !(same_support
+                    && *previous_is_phase
+                    && matches!(entry, FrozenLitterHistoryEntryV1::Custody(_)))
+            {
+                return Err(Error::Identity(
+                    "native history overlapping or reordered origin support",
+                ));
             }
         }
-        if self.dirty_parent.is_some_and(|dirty|dirty!=parent) {
-            return Err(Error::Identity("native history omitted dirty parent finalization"));
+        if self.dirty_parent.is_some_and(|dirty| dirty != parent) {
+            return Err(Error::Identity(
+                "native history omitted dirty parent finalization",
+            ));
         }
         match entry {
-            FrozenLitterHistoryEntryV1::Phase(_)=>{
-                if current.support_end_ns==current.parent_support_end_ns {self.dirty_parent=None;}
+            FrozenLitterHistoryEntryV1::Phase(_) => {
+                if current.support_end_ns == current.parent_support_end_ns {
+                    self.dirty_parent = None;
+                }
             }
-            FrozenLitterHistoryEntryV1::Custody(record)=>{
-                if self.dirty_parent_digest.is_some_and(|digest| digest != record.identity.parent_transaction_sha256) {
-                    return Err(Error::Identity("native history foreign dirty parent digest"));
+            FrozenLitterHistoryEntryV1::Custody(record) => {
+                if self
+                    .dirty_parent_digest
+                    .is_some_and(|digest| digest != record.identity.parent_transaction_sha256)
+                {
+                    return Err(Error::Identity(
+                        "native history foreign dirty parent digest",
+                    ));
                 }
                 match record.identity.kind {
-                    NativeSnowCustodyKindV1::Drain=>{
+                    NativeSnowCustodyKindV1::Drain => {
                         for output in &record.outputs {
                             if !self.consumed.insert(output.receipt_sha256) {
-                                return Err(Error::Identity("native history replayed donor output"));
+                                return Err(Error::Identity(
+                                    "native history replayed donor output",
+                                ));
                             }
                         }
-                        if record.beginning_lse.0.last_accepted_transaction_id==Some(current.transaction_id)
-                            && self.prior.as_ref().is_none_or(|(previous,is_phase)| !*is_phase
-                                || previous.transaction_id!=current.transaction_id
-                                || previous.parent_support_start_ns!=current.parent_support_start_ns
-                                || previous.parent_support_end_ns!=current.parent_support_end_ns
-                                || previous.support_end_ns!=current.support_end_ns
-                                || previous.support_end_ns!=previous.parent_support_end_ns) {
-                            return Err(Error::Identity("native history foreign existing final marker"));
+                        if record.beginning_lse.0.last_accepted_transaction_id
+                            == Some(current.transaction_id)
+                            && self.prior.as_ref().is_none_or(|(previous, is_phase)| {
+                                !*is_phase
+                                    || previous.transaction_id != current.transaction_id
+                                    || previous.parent_support_start_ns
+                                        != current.parent_support_start_ns
+                                    || previous.parent_support_end_ns
+                                        != current.parent_support_end_ns
+                                    || previous.support_end_ns != current.support_end_ns
+                                    || previous.support_end_ns != previous.parent_support_end_ns
+                            })
+                        {
+                            return Err(Error::Identity(
+                                "native history foreign existing final marker",
+                            ));
                         }
-                        self.dirty_parent=if current.support_end_ns<current.parent_support_end_ns {Some(parent)} else {None};
-                        self.dirty_parent_digest = self.dirty_parent.map(|_| record.identity.parent_transaction_sha256);
+                        self.dirty_parent =
+                            if current.support_end_ns < current.parent_support_end_ns {
+                                Some(parent)
+                            } else {
+                                None
+                            };
+                        self.dirty_parent_digest = self
+                            .dirty_parent
+                            .map(|_| record.identity.parent_transaction_sha256);
                     }
-                    NativeSnowCustodyKindV1::ParentFinalization=>{
-                        if self.dirty_parent_digest != Some(record.identity.parent_transaction_sha256) {
+                    NativeSnowCustodyKindV1::ParentFinalization => {
+                        if self.dirty_parent_digest
+                            != Some(record.identity.parent_transaction_sha256)
+                        {
                             return Err(Error::Identity("native history dirty parent digest"));
                         }
-                        if self.dirty_parent!=Some(parent) || current.support_end_ns!=current.parent_support_end_ns {
-                            return Err(Error::Identity("native history unearned parent finalization"));
+                        if self.dirty_parent != Some(parent)
+                            || current.support_end_ns != current.parent_support_end_ns
+                        {
+                            return Err(Error::Identity(
+                                "native history unearned parent finalization",
+                            ));
                         }
-                        self.dirty_parent=None;
+                        self.dirty_parent = None;
                     }
                 }
             }
         }
-        if self.dirty_parent.is_none() { self.dirty_parent_digest = None; }
-        self.prior = Some((current, matches!(entry, FrozenLitterHistoryEntryV1::Phase(_))));
+        if self.dirty_parent.is_none() {
+            self.dirty_parent_digest = None;
+        }
+        self.prior = Some((
+            current,
+            matches!(entry, FrozenLitterHistoryEntryV1::Phase(_)),
+        ));
         Ok(())
     }
 }
 
 pub(crate) fn validate_native_history_v1(
-    entries: &[FrozenLitterHistoryEntryV1], configuration: &crate::SurfaceLiquidConfigurationV2,
+    entries: &[FrozenLitterHistoryEntryV1],
+    configuration: &crate::SurfaceLiquidConfigurationV2,
     lse_configuration: &openwepp_land_surface_energy::LandSurfaceEnergyConfiguration,
 ) -> Result<NativeHistoryCursorV1, crate::DirectSurfaceLiquidError> {
     let mut cursor = NativeHistoryCursorV1::default();
--- a/crates/openwepp-hillslope-orchestrator/src/v9_real_consumer_shadow/native_snow_liquid_custody.rs
+++ b/crates/openwepp-hillslope-orchestrator/src/v9_real_consumer_shadow/native_snow_liquid_custody.rs
@@ -1,17 +1,23 @@
 //! Isolated policy13 native positive-origin snow-liquid custody.
 //! Numerical candidate construction is separate from accepted history.
 
+use crate::direct_runtime::{
+    DirectZeroDurationSnowLiquidInputV1, PositiveSupportSnowLiquidAllocationV1,
+    allocate_positive_support_snow_liquid_v1,
+};
+use crate::{
+    DirectSurfaceLiquidError, LseSurfaceEnthalpyOwnerEnvelopeV1, SurfaceLiquidConfigurationV2,
+    SurfaceLiquidOwnedStateV2, SurfaceLiquidOwnerEnvelopeV2,
+};
 use openwepp_kernel_contract::TransactionId;
 use openwepp_land_surface_energy::{
-    ExactDyadicEnthalpy, LandSurfaceEnergyConfiguration, LandSurfaceEnergyV3State,
-    Sha256Digest, SurfaceConfiguration, BeginningLitterPhaseState, LitterPhaseConfiguration,
+    BeginningLitterPhaseState, ExactDyadicEnthalpy, LandSurfaceEnergyConfiguration,
+    LandSurfaceEnergyV3State, LitterPhaseConfiguration, Sha256Digest, SurfaceConfiguration,
+    physics::{
+        LITTER_ICE_HEAT_CAPACITY_J_KG_K, REFERENCE_TEMPERATURE_K, WATER_HEAT_CAPACITY_J_KG_K,
+    },
     validate_beginning_litter_state,
-    physics::{WATER_HEAT_CAPACITY_J_KG_K, LITTER_ICE_HEAT_CAPACITY_J_KG_K, REFERENCE_TEMPERATURE_K},
 };
-use crate::{DirectSurfaceLiquidError, SurfaceLiquidConfigurationV2, SurfaceLiquidOwnerEnvelopeV2,
-    SurfaceLiquidOwnedStateV2, LseSurfaceEnthalpyOwnerEnvelopeV1};
-use crate::direct_runtime::{DirectZeroDurationSnowLiquidInputV1,
-    PositiveSupportSnowLiquidAllocationV1, allocate_positive_support_snow_liquid_v1};
 
 /// Unpublished arithmetic candidate. This type has no wire or acceptance API;
 /// its exact owner still has the predecessor chain. A donor-bound publication
@@ -35,38 +41,70 @@
     ending_marker: Option<TransactionId>,
 ) -> Result<UnpublishedNativeSnowLiquidCandidateV1, DirectSurfaceLiquidError> {
     if ending_marker != beginning_lse.0.last_accepted_transaction_id {
-        return Err(DirectSurfaceLiquidError::Identity("native custody cannot advance physical marker"));
+        return Err(DirectSurfaceLiquidError::Identity(
+            "native custody cannot advance physical marker",
+        ));
     }
-    beginning_lse.validate(lse_configuration)
+    beginning_lse
+        .validate(lse_configuration)
         .map_err(|_| DirectSurfaceLiquidError::Identity("native drain beginning LSE"))?;
-    beginning_exact.validate_frozen_parent_join(lse_configuration, beginning_lse,
-        configuration, beginning_surface)
+    beginning_exact
+        .validate_frozen_parent_join(
+            lse_configuration,
+            beginning_lse,
+            configuration,
+            beginning_surface,
+        )
         .map_err(|_| DirectSurfaceLiquidError::Identity("native drain exact beginning join"))?;
-    let state = beginning_surface.v2_state()
+    let state = beginning_surface
+        .v2_state()
         .ok_or(DirectSurfaceLiquidError::Identity("native drain V2 owner"))?;
-    let allocation = allocate_positive_support_snow_liquid_v1(configuration.parent(),
-        &state.records().iter().map(|record| record.liquid_kg_m2_tile).collect::<Vec<_>>(),
-        inputs, transaction)?;
+    let allocation = allocate_positive_support_snow_liquid_v1(
+        configuration.parent(),
+        &state
+            .records()
+            .iter()
+            .map(|record| record.liquid_kg_m2_tile)
+            .collect::<Vec<_>>(),
+        inputs,
+        transaction,
+    )?;
     let mut records = state.records().to_vec();
     let mut exact = beginning_exact.clone();
     let mut lse = beginning_lse.clone();
     for (record, liquid) in records.iter_mut().zip(&allocation.ending_liquid) {
         // Exact accumulation consumes primitive retained receipt credits; the
         // legacy tile aggregate has already rounded and is not an exact operand.
-        let energy = allocation.receipts.iter().filter(|receipt|
-            receipt.disposition == crate::DirectZeroDurationSnowLiquidDispositionV1::RetainedSurface
-                && receipt.recipient_ofe_id == record.key.ofe_id
-                && receipt.recipient_tile_id.as_ref() == Some(&record.key.tile_id))
-            .map(|receipt| receipt.credited_enthalpy_j_m2_recipient_tile_ground
-                .ok_or(DirectSurfaceLiquidError::Identity("native drain retained energy operand")))
+        let energy = allocation
+            .receipts
+            .iter()
+            .filter(|receipt| {
+                receipt.disposition
+                    == crate::DirectZeroDurationSnowLiquidDispositionV1::RetainedSurface
+                    && receipt.recipient_ofe_id == record.key.ofe_id
+                    && receipt.recipient_tile_id.as_ref() == Some(&record.key.tile_id)
+            })
+            .map(|receipt| {
+                receipt.credited_enthalpy_j_m2_recipient_tile_ground.ok_or(
+                    DirectSurfaceLiquidError::Identity("native drain retained energy operand"),
+                )
+            })
             .collect::<Result<Vec<_>, _>>()?;
-        let exact_record = exact.records.iter_mut().find(|value| value.surface_key == record.key)
-            .ok_or(DirectSurfaceLiquidError::Identity("native drain exact tile"))?;
+        let exact_record = exact
+            .records
+            .iter_mut()
+            .find(|value| value.surface_key == record.key)
+            .ok_or(DirectSurfaceLiquidError::Identity(
+                "native drain exact tile",
+            ))?;
         if energy.iter().any(|value| *value != 0.0) {
             let (high, carry) = ExactDyadicEnthalpy::exact_sum_binary64(
-                exact_record.enthalpy_hi_j_m2_tile, &exact_record.enthalpy_carry, &energy)
-                .and_then(|value| value.rounded_high_and_remainder())
-                .map_err(|_| DirectSurfaceLiquidError::Domain("native drain exact retained energy"))?;
+                exact_record.enthalpy_hi_j_m2_tile,
+                &exact_record.enthalpy_carry,
+                &energy,
+            )
+            .and_then(|value| value.rounded_high_and_remainder())
+            .map_err(|_| DirectSurfaceLiquidError::Domain("native drain exact retained energy"))?;
             exact_record.enthalpy_hi_j_m2_tile = high;
             exact_record.enthalpy_carry = carry;
         }
@@ -76,62 +114,117 @@
         record.surface_enthalpy_j_m2_tile = exact_record.enthalpy_hi_j_m2_tile;
         record.last_accepted_transaction_id = ending_marker;
         exact_record.last_accepted_transaction_id = ending_marker;
-        if !physical_changed { continue; }
-        let tile = lse.0.tiles.iter_mut().find(|tile|
-            tile.ofe_id == record.key.ofe_id && tile.tile_id == record.key.tile_id)
+        if !physical_changed {
+            continue;
+        }
+        let tile = lse
+            .0
+            .tiles
+            .iter_mut()
+            .find(|tile| tile.ofe_id == record.key.ofe_id && tile.tile_id == record.key.tile_id)
             .ok_or(DirectSurfaceLiquidError::Identity("native drain LSE tile"))?;
-        let configured = lse_configuration.ofes.iter().find(|ofe| ofe.ofe_id == record.key.ofe_id)
-            .and_then(|ofe| ofe.tiles.iter().find(|tile| tile.tile_id == record.key.tile_id))
-            .ok_or(DirectSurfaceLiquidError::Identity("native drain LSE configuration tile"))?;
+        let configured = lse_configuration
+            .ofes
+            .iter()
+            .find(|ofe| ofe.ofe_id == record.key.ofe_id)
+            .and_then(|ofe| {
+                ofe.tiles
+                    .iter()
+                    .find(|tile| tile.tile_id == record.key.tile_id)
+            })
+            .ok_or(DirectSurfaceLiquidError::Identity(
+                "native drain LSE configuration tile",
+            ))?;
         let dry_capacity = match configured.surface {
-            SurfaceConfiguration::ForestLitter { thickness_m, dry_density_kg_m3,
-                dry_specific_heat_j_kg_k, .. } => thickness_m * dry_density_kg_m3 * dry_specific_heat_j_kg_k,
-            SurfaceConfiguration::BareMineralSoil { dry_areal_heat_capacity_j_m2_k, .. }
-                => dry_areal_heat_capacity_j_m2_k,
+            SurfaceConfiguration::ForestLitter {
+                thickness_m,
+                dry_density_kg_m3,
+                dry_specific_heat_j_kg_k,
+                ..
+            } => thickness_m * dry_density_kg_m3 * dry_specific_heat_j_kg_k,
+            SurfaceConfiguration::BareMineralSoil {
+                dry_areal_heat_capacity_j_m2_k,
+                ..
+            } => dry_areal_heat_capacity_j_m2_k,
         };
-        let capacity = dry_capacity + WATER_HEAT_CAPACITY_J_KG_K * record.liquid_kg_m2_tile
+        let capacity = dry_capacity
+            + WATER_HEAT_CAPACITY_J_KG_K * record.liquid_kg_m2_tile
             + LITTER_ICE_HEAT_CAPACITY_J_KG_K * record.litter_ice_kg_m2_tile;
         if !capacity.is_finite() || capacity <= 0.0 {
-            return Err(DirectSurfaceLiquidError::Domain("native drain heat capacity"));
+            return Err(DirectSurfaceLiquidError::Domain(
+                "native drain heat capacity",
+            ));
         }
         tile.surface_enthalpy_j_m2_tile_ground = record.surface_enthalpy_j_m2_tile;
-        tile.surface_temperature_warm_start_k = REFERENCE_TEMPERATURE_K
-            + record.surface_enthalpy_j_m2_tile / capacity;
+        tile.surface_temperature_warm_start_k =
+            REFERENCE_TEMPERATURE_K + record.surface_enthalpy_j_m2_tile / capacity;
         if !tile.surface_temperature_warm_start_k.is_finite()
-            || !(200.0..=350.0).contains(&tile.surface_temperature_warm_start_k) {
-            return Err(DirectSurfaceLiquidError::Domain("native drain dependent temperature"));
-        }
-        if let SurfaceConfiguration::ForestLitter {thickness_m, liquid_capacity_kg_m2_tile_ground, ..}
-            = configured.surface {
-            let surface_configured = configuration.records().iter().find(|value| value.key == record.key)
-                .ok_or(DirectSurfaceLiquidError::Identity("native drain litter configuration"))?;
-            validate_beginning_litter_state(LitterPhaseConfiguration {
-                litter_depth_m: thickness_m, dry_heat_capacity_j_m2_k: dry_capacity,
-                liquid_capacity_kg_m2_tile: liquid_capacity_kg_m2_tile_ground,
-                ice_capacity_kg_m2_tile: surface_configured.litter_ice_capacity_kg_m2_tile
-                    .ok_or(DirectSurfaceLiquidError::Identity("native drain litter ice capacity"))?,
-            }, BeginningLitterPhaseState {liquid_kg_m2_tile: record.liquid_kg_m2_tile,
-                ice_kg_m2_tile: record.litter_ice_kg_m2_tile,
-                sensible_energy_j_m2_tile: record.surface_enthalpy_j_m2_tile,
-                temperature_k: tile.surface_temperature_warm_start_k})
-                .map_err(|_| DirectSurfaceLiquidError::Closure("native drain litter temperature coordinate"))?;
+            || !(200.0..=350.0).contains(&tile.surface_temperature_warm_start_k)
+        {
+            return Err(DirectSurfaceLiquidError::Domain(
+                "native drain dependent temperature",
+            ));
+        }
+        if let SurfaceConfiguration::ForestLitter {
+            thickness_m,
+            liquid_capacity_kg_m2_tile_ground,
+            ..
+        } = configured.surface
+        {
+            let surface_configured = configuration
+                .records()
+                .iter()
+                .find(|value| value.key == record.key)
+                .ok_or(DirectSurfaceLiquidError::Identity(
+                    "native drain litter configuration",
+                ))?;
+            validate_beginning_litter_state(
+                LitterPhaseConfiguration {
+                    litter_depth_m: thickness_m,
+                    dry_heat_capacity_j_m2_k: dry_capacity,
+                    liquid_capacity_kg_m2_tile: liquid_capacity_kg_m2_tile_ground,
+                    ice_capacity_kg_m2_tile: surface_configured
+                        .litter_ice_capacity_kg_m2_tile
+                        .ok_or(DirectSurfaceLiquidError::Identity(
+                            "native drain litter ice capacity",
+                        ))?,
+                },
+                BeginningLitterPhaseState {
+                    liquid_kg_m2_tile: record.liquid_kg_m2_tile,
+                    ice_kg_m2_tile: record.litter_ice_kg_m2_tile,
+                    sensible_energy_j_m2_tile: record.surface_enthalpy_j_m2_tile,
+                    temperature_k: tile.surface_temperature_warm_start_k,
+                },
+            )
+            .map_err(|_| {
+                DirectSurfaceLiquidError::Closure("native drain litter temperature coordinate")
+            })?;
         }
     }
     let continuations = state.continuations().to_vec();
-    let surface = SurfaceLiquidOwnerEnvelopeV2::wrap_v2(configuration,
-        SurfaceLiquidOwnedStateV2::try_new(configuration, records, continuations)?)?;
+    let surface = SurfaceLiquidOwnerEnvelopeV2::wrap_v2(
+        configuration,
+        SurfaceLiquidOwnedStateV2::try_new(configuration, records, continuations)?,
+    )?;
     lse.0.last_accepted_transaction_id = ending_marker;
-    lse.0.state_sha256 = lse.canonical_sha256()
+    lse.0.state_sha256 = lse
+        .canonical_sha256()
         .map_err(|_| DirectSurfaceLiquidError::Schema("native drain LSE seal"))?;
     lse.validate(lse_configuration)
         .map_err(|_| DirectSurfaceLiquidError::Identity("native drain ending LSE"))?;
     exact.frozen_lse_v3_state_sha256 = lse.0.state_sha256.clone();
     exact.frozen_surface_owner_v2_sha256 = Sha256Digest::try_new(surface.envelope_sha256())
         .map_err(|_| DirectSurfaceLiquidError::Schema("native drain surface digest"))?;
-    exact.state_sha256 = exact.recomputed_state_sha256()
+    exact.state_sha256 = exact
+        .recomputed_state_sha256()
         .map_err(|_| DirectSurfaceLiquidError::Schema("native drain exact state seal"))?;
-    exact.validate_frozen_parent_join(lse_configuration, &lse, configuration, &surface)
+    exact
+        .validate_frozen_parent_join(lse_configuration, &lse, configuration, &surface)
         .map_err(|_| DirectSurfaceLiquidError::Identity("native drain ending exact join"))?;
-    Ok(UnpublishedNativeSnowLiquidCandidateV1 {surface_owner:surface, lse_state:lse,
-        exact_owner:exact, allocation})
+    Ok(UnpublishedNativeSnowLiquidCandidateV1 {
+        surface_owner: surface,
+        lse_state: lse,
+        exact_owner: exact,
+        allocation,
+    })
 }
--- a/crates/openwepp-hillslope-orchestrator/src/v9_real_consumer_shadow/native_snow_liquid_publication.rs
+++ b/crates/openwepp-hillslope-orchestrator/src/v9_real_consumer_shadow/native_snow_liquid_publication.rs
@@ -1,38 +1,48 @@
 //! Policy14 donor-bound native custody publication, distinct from phase work.
-use std::collections::BTreeSet;
+use super::{
+    Stage3AcceptedSnowLiquidOutputV1,
+    native_snow_liquid_custody::prepare_native_snow_liquid_candidate_v1,
+};
+use crate::direct_runtime::DirectZeroDurationSnowLiquidInputV1;
+use crate::{
+    DirectSurfaceLiquidError as Error, DirectZeroDurationSnowLiquidReceiptV1,
+    LseSurfaceEnthalpyOwnerEnvelopeV1, SurfaceLiquidConfigurationV2, SurfaceLiquidOwnerEnvelopeV2,
+};
 use openwepp_coupled_time::{Digest32, FramedField, digest_bytes, framed_sha256};
 use openwepp_kernel_contract::TransactionId;
-use openwepp_land_surface_energy::{LandSurfaceEnergyConfiguration, LandSurfaceEnergyState, LandSurfaceEnergyV3State, Sha256Digest};
+use openwepp_land_surface_energy::{
+    LandSurfaceEnergyConfiguration, LandSurfaceEnergyState, LandSurfaceEnergyV3State, Sha256Digest,
+};
 use serde::{Deserialize, Serialize};
-use crate::{DirectSurfaceLiquidError as Error, SurfaceLiquidConfigurationV2,
-    SurfaceLiquidOwnerEnvelopeV2, LseSurfaceEnthalpyOwnerEnvelopeV1,
-    DirectZeroDurationSnowLiquidReceiptV1};
-use crate::direct_runtime::DirectZeroDurationSnowLiquidInputV1;
-use super::{Stage3AcceptedSnowLiquidOutputV1, native_snow_liquid_custody::prepare_native_snow_liquid_candidate_v1};
-
-pub(crate) const NATIVE_SNOW_CUSTODY_SCHEMA: &str = "OPENWEPP_B01_NATIVE_SNOW_LIQUID_CUSTODY_V2_POLICY14";
+use std::collections::BTreeSet;
+
+pub(crate) const NATIVE_SNOW_CUSTODY_SCHEMA: &str =
+    "OPENWEPP_B01_NATIVE_SNOW_LIQUID_CUSTODY_V2_POLICY14";
 
 #[derive(Clone, Copy, Debug, PartialEq, Eq, Serialize, Deserialize)]
-#[serde(rename_all="snake_case")]
-pub(crate) enum NativeSnowCustodyKindV1 { Drain, ParentFinalization }
+#[serde(rename_all = "snake_case")]
+pub(crate) enum NativeSnowCustodyKindV1 {
+    Drain,
+    ParentFinalization,
+}
 
 #[derive(Clone, Debug, PartialEq, Serialize, Deserialize)]
 #[serde(deny_unknown_fields)]
 pub(crate) struct NativeSnowCustodyIdentityV1 {
-    pub policy_id:u8,
-    pub kind:NativeSnowCustodyKindV1,
-    pub parent_transaction_sha256:Digest32,
-    pub transaction_id:TransactionId,
-    pub predecessor_transaction_id:Option<TransactionId>,
-    pub parent_start_ns:u128,
-    pub parent_end_ns:u128,
-    pub support_start_ns:u128,
-    pub support_end_ns:u128,
-    pub receiver_ordinal:u32,
-    pub donor_ending_complete_owner_sha256:Digest32,
-    pub receiver_context_sha256:Digest32,
-    pub output_set_sha256:Digest32,
-    pub predecessor_physical_receipt_chain_sha256:String,
+    pub policy_id: u8,
+    pub kind: NativeSnowCustodyKindV1,
+    pub parent_transaction_sha256: Digest32,
+    pub transaction_id: TransactionId,
+    pub predecessor_transaction_id: Option<TransactionId>,
+    pub parent_start_ns: u128,
+    pub parent_end_ns: u128,
+    pub support_start_ns: u128,
+    pub support_end_ns: u128,
+    pub receiver_ordinal: u32,
+    pub donor_ending_complete_owner_sha256: Digest32,
+    pub receiver_context_sha256: Digest32,
+    pub output_set_sha256: Digest32,
+    pub predecessor_physical_receipt_chain_sha256: String,
 }
 
 /// A complete physical/exact transition record. Its positive support is donor
@@ -40,34 +50,46 @@
 #[derive(Clone, Debug, PartialEq, Serialize, Deserialize)]
 #[serde(deny_unknown_fields)]
 pub(crate) struct NativeSnowLiquidPublicationV1 {
-    schema:String,
+    schema: String,
     pub runtime_authority: openwepp_coupled_time::Policy14RuntimeAuthority,
-    pub identity:NativeSnowCustodyIdentityV1,
-    lse_configuration_sha256:Sha256Digest,
-    surface_configuration_sha256:String,
-    pub active_covered_lse:LandSurfaceEnergyState,
-    pub beginning_lse:LandSurfaceEnergyV3State,
-    pub ending_lse:LandSurfaceEnergyV3State,
-    pub beginning_surface_bytes:Vec<u8>,
-    pub ending_surface_bytes:Vec<u8>,
+    pub identity: NativeSnowCustodyIdentityV1,
+    lse_configuration_sha256: Sha256Digest,
+    surface_configuration_sha256: String,
+    pub active_covered_lse: LandSurfaceEnergyState,
+    pub beginning_lse: LandSurfaceEnergyV3State,
+    pub ending_lse: LandSurfaceEnergyV3State,
+    pub beginning_surface_bytes: Vec<u8>,
+    pub ending_surface_bytes: Vec<u8>,
     // Authenticated native V2 projections for consumers without V2 configuration.
     pub beginning_liquid_records: Vec<(crate::DirectSurfaceLiquidStoreKey, f64)>,
     pub ending_liquid_records: Vec<(crate::DirectSurfaceLiquidStoreKey, f64)>,
-    pub beginning_exact:LseSurfaceEnthalpyOwnerEnvelopeV1,
-    pub ending_exact:LseSurfaceEnthalpyOwnerEnvelopeV1,
-    pub outputs:Vec<Stage3AcceptedSnowLiquidOutputV1>,
-    pub allocation_receipts:Vec<DirectZeroDurationSnowLiquidReceiptV1>,
-    pub receipt_sha256:Digest32,
-    pub publication_sha256:Digest32,
-}
-
-fn wire_digest(value:Digest32)->Result<Sha256Digest,Error> {
-    Sha256Digest::try_new(value.as_bytes().iter().map(|byte|format!("{byte:02x}")).collect::<String>())
-        .map_err(|_|Error::Schema("native snow custody digest"))
-}
-fn surface(config:&SurfaceLiquidConfigurationV2,bytes:&[u8])->Result<SurfaceLiquidOwnerEnvelopeV2,Error> {
-    let owner=SurfaceLiquidOwnerEnvelopeV2::from_canonical_bytes(config.parent(),Some(config),bytes)?;
-    if owner.v2_state().is_none() {return Err(Error::Identity("native snow custody cannot downgrade"));}
+    pub beginning_exact: LseSurfaceEnthalpyOwnerEnvelopeV1,
+    pub ending_exact: LseSurfaceEnthalpyOwnerEnvelopeV1,
+    pub outputs: Vec<Stage3AcceptedSnowLiquidOutputV1>,
+    pub allocation_receipts: Vec<DirectZeroDurationSnowLiquidReceiptV1>,
+    pub receipt_sha256: Digest32,
+    pub publication_sha256: Digest32,
+}
+
+fn wire_digest(value: Digest32) -> Result<Sha256Digest, Error> {
+    Sha256Digest::try_new(
+        value
+            .as_bytes()
+            .iter()
+            .map(|byte| format!("{byte:02x}"))
+            .collect::<String>(),
+    )
+    .map_err(|_| Error::Schema("native snow custody digest"))
+}
+fn surface(
+    config: &SurfaceLiquidConfigurationV2,
+    bytes: &[u8],
+) -> Result<SurfaceLiquidOwnerEnvelopeV2, Error> {
+    let owner =
+        SurfaceLiquidOwnerEnvelopeV2::from_canonical_bytes(config.parent(), Some(config), bytes)?;
+    if owner.v2_state().is_none() {
+        return Err(Error::Identity("native snow custody cannot downgrade"));
+    }
     Ok(owner)
 }
 
@@ -82,64 +104,132 @@
     let mut sorted = std::collections::BTreeMap::new();
     let mut lanes = BTreeSet::new();
     for output in outputs {
-        output.validate().map_err(|_| Error::Identity("positive support donor seal"))?;
+        output
+            .validate()
+            .map_err(|_| Error::Identity("positive support donor seal"))?;
         if output.support.start_ns().get() != support_start_ns
             || output.support.end_ns().get() != support_end_ns
             || output.mass_kg_m2_ofe_ground <= 0.0
             || !lanes.insert(output.lane_id)
-            || sorted.insert(*output.receipt_sha256.as_bytes(), output).is_some()
-        { return Err(Error::Identity("positive support donor set domain")); }
-    }
-    if outputs.is_empty() { return Err(Error::Identity("positive support empty donor set")); }
+            || sorted
+                .insert(*output.receipt_sha256.as_bytes(), output)
+                .is_some()
+        {
+            return Err(Error::Identity("positive support donor set domain"));
+        }
+    }
+    if outputs.is_empty() {
+        return Err(Error::Identity("positive support empty donor set"));
+    }
     let mut mass = 0.0;
     let mut energy = 0.0;
     for output in sorted.values() {
-        mass = crate::direct_runtime::checked_surface_liquid_add(mass, output.mass_kg_m2_ofe_ground)
-            .ok_or(Error::Domain("positive support mass sum"))?;
-        energy = crate::direct_runtime::checked_surface_liquid_add(energy, output.sensible_enthalpy_j_m2_ofe_ground)
-            .ok_or(Error::Domain("positive support energy sum"))?;
-    }
-    let fields = outputs.iter().map(|output| FramedField {
-        tag: "snow_liquid_output", value: output.receipt_sha256.as_bytes().as_slice(),
-    }).collect::<Vec<_>>();
+        mass =
+            crate::direct_runtime::checked_surface_liquid_add(mass, output.mass_kg_m2_ofe_ground)
+                .ok_or(Error::Domain("positive support mass sum"))?;
+        energy = crate::direct_runtime::checked_surface_liquid_add(
+            energy,
+            output.sensible_enthalpy_j_m2_ofe_ground,
+        )
+        .ok_or(Error::Domain("positive support energy sum"))?;
+    }
+    let fields = outputs
+        .iter()
+        .map(|output| FramedField {
+            tag: "snow_liquid_output",
+            value: output.receipt_sha256.as_bytes().as_slice(),
+        })
+        .collect::<Vec<_>>();
     let set = framed_sha256("stage3-v11-positive-support-liquid-output-set", &fields)
         .map_err(|_| Error::Schema("positive support output set"))?;
     Ok((set, mass, energy))
 }
 
 impl NativeSnowCustodyIdentityV1 {
-    pub(crate) fn expected_receiver_context(&self,outputs:&[Stage3AcceptedSnowLiquidOutputV1])->Result<Digest32,Error> {
+    pub(crate) fn expected_receiver_context(
+        &self,
+        outputs: &[Stage3AcceptedSnowLiquidOutputV1],
+    ) -> Result<Digest32, Error> {
         let (mass, energy) = if self.kind == NativeSnowCustodyKindV1::Drain {
-            let (set, mass, energy) = positive_support_output_summary_v1(outputs, self.support_start_ns, self.support_end_ns)?;
-            if set != self.output_set_sha256 { return Err(Error::Identity("native donor output set")); }
+            let (set, mass, energy) = positive_support_output_summary_v1(
+                outputs,
+                self.support_start_ns,
+                self.support_end_ns,
+            )?;
+            if set != self.output_set_sha256 {
+                return Err(Error::Identity("native donor output set"));
+            }
             (mass, energy)
-        } else { (0.0, 0.0) };
-        framed_sha256(if self.kind==NativeSnowCustodyKindV1::Drain {
-            "stage3-v11-positive-support-liquid-receiver"
-        } else {"stage3-v11-native-drain-parent-finalization"}, &[
-            FramedField{tag:"parent_transaction",value:self.parent_transaction_sha256.as_bytes()},
-            FramedField{tag:"support_start",value:&self.support_start_ns.to_be_bytes()},
-            FramedField{tag:"support_end",value:&self.support_end_ns.to_be_bytes()},
-            FramedField{tag:"support_ending_owner",value:self.donor_ending_complete_owner_sha256.as_bytes()},
-            FramedField{tag:"output_set",value:self.output_set_sha256.as_bytes()},
-            FramedField{tag:"mass_kg_m2",value:&mass.to_bits().to_be_bytes()},
-            FramedField{tag:"enthalpy_j_m2",value:&energy.to_bits().to_be_bytes()},
-        ]).map_err(|_|Error::Schema("native custody receiver context"))
-    }
-    fn ending_marker(&self, beginning:Option<TransactionId>)->Result<Option<TransactionId>,Error> {
-        if self.policy_id!=14 || self.transaction_id.0==0
-            || self.parent_transaction_sha256==Digest32::zero()
-            || self.donor_ending_complete_owner_sha256==Digest32::zero()
-            || self.receiver_context_sha256==Digest32::zero()
-            || self.parent_start_ns>=self.parent_end_ns
-            || self.support_start_ns<self.parent_start_ns
-            || self.support_start_ns>=self.support_end_ns || self.support_end_ns>self.parent_end_ns
-            || self.predecessor_physical_receipt_chain_sha256.len()!=64
-            || !self.predecessor_physical_receipt_chain_sha256.bytes().all(|b|b.is_ascii_hexdigit())
-            || self.predecessor_transaction_id.is_some_and(|p|p.0>self.transaction_id.0)
-        {return Err(Error::Identity("native snow custody policy/parent/support"));}
+        } else {
+            (0.0, 0.0)
+        };
+        framed_sha256(
+            if self.kind == NativeSnowCustodyKindV1::Drain {
+                "stage3-v11-positive-support-liquid-receiver"
+            } else {
+                "stage3-v11-native-drain-parent-finalization"
+            },
+            &[
+                FramedField {
+                    tag: "parent_transaction",
+                    value: self.parent_transaction_sha256.as_bytes(),
+                },
+                FramedField {
+                    tag: "support_start",
+                    value: &self.support_start_ns.to_be_bytes(),
+                },
+                FramedField {
+                    tag: "support_end",
+                    value: &self.support_end_ns.to_be_bytes(),
+                },
+                FramedField {
+                    tag: "support_ending_owner",
+                    value: self.donor_ending_complete_owner_sha256.as_bytes(),
+                },
+                FramedField {
+                    tag: "output_set",
+                    value: self.output_set_sha256.as_bytes(),
+                },
+                FramedField {
+                    tag: "mass_kg_m2",
+                    value: &mass.to_bits().to_be_bytes(),
+                },
+                FramedField {
+                    tag: "enthalpy_j_m2",
+                    value: &energy.to_bits().to_be_bytes(),
+                },
+            ],
+        )
+        .map_err(|_| Error::Schema("native custody receiver context"))
+    }
+    fn ending_marker(
+        &self,
+        beginning: Option<TransactionId>,
+    ) -> Result<Option<TransactionId>, Error> {
+        if self.policy_id != 14
+            || self.transaction_id.0 == 0
+            || self.parent_transaction_sha256 == Digest32::zero()
+            || self.donor_ending_complete_owner_sha256 == Digest32::zero()
+            || self.receiver_context_sha256 == Digest32::zero()
+            || self.parent_start_ns >= self.parent_end_ns
+            || self.support_start_ns < self.parent_start_ns
+            || self.support_start_ns >= self.support_end_ns
+            || self.support_end_ns > self.parent_end_ns
+            || self.predecessor_physical_receipt_chain_sha256.len() != 64
+            || !self
+                .predecessor_physical_receipt_chain_sha256
+                .bytes()
+                .all(|b| b.is_ascii_hexdigit())
+            || self
+                .predecessor_transaction_id
+                .is_some_and(|p| p.0 > self.transaction_id.0)
+        {
+            return Err(Error::Identity("native snow custody policy/parent/support"));
+        }
         if beginning != self.predecessor_transaction_id
-            || (self.kind==NativeSnowCustodyKindV1::ParentFinalization && self.support_end_ns!=self.parent_end_ns) {
+            || (self.kind == NativeSnowCustodyKindV1::ParentFinalization
+                && self.support_end_ns != self.parent_end_ns)
+        {
             return Err(Error::Identity("native custody retained physical lineage"));
         }
         Ok(beginning)
@@ -147,142 +237,297 @@
 }
 
 impl NativeSnowLiquidPublicationV1 {
-    fn inputs(&self,configuration:&SurfaceLiquidConfigurationV2)->Result<Vec<DirectZeroDurationSnowLiquidInputV1>,Error> {
-        let identity=&self.identity;
-        if identity.receiver_context_sha256!=identity.expected_receiver_context(&self.outputs)? {
-            return Err(Error::Identity("native custody receiver context substitution"));
-        }
-        if identity.kind==NativeSnowCustodyKindV1::ParentFinalization {
-            if !self.outputs.is_empty() || identity.output_set_sha256!=Digest32::zero() {
-                return Err(Error::Identity("native parent finalization has no donor output"));
+    fn inputs(
+        &self,
+        configuration: &SurfaceLiquidConfigurationV2,
+    ) -> Result<Vec<DirectZeroDurationSnowLiquidInputV1>, Error> {
+        let identity = &self.identity;
+        if identity.receiver_context_sha256 != identity.expected_receiver_context(&self.outputs)? {
+            return Err(Error::Identity(
+                "native custody receiver context substitution",
+            ));
+        }
+        if identity.kind == NativeSnowCustodyKindV1::ParentFinalization {
+            if !self.outputs.is_empty() || identity.output_set_sha256 != Digest32::zero() {
+                return Err(Error::Identity(
+                    "native parent finalization has no donor output",
+                ));
             }
             return Ok(Vec::new());
         }
-        if self.outputs.is_empty() {return Err(Error::Identity("native drain missing outputs"));}
-        let mut seen=BTreeSet::new();
-        let mut inputs=Vec::new();
+        if self.outputs.is_empty() {
+            return Err(Error::Identity("native drain missing outputs"));
+        }
+        let mut seen = BTreeSet::new();
+        let mut inputs = Vec::new();
         for output in &self.outputs {
-            output.validate().map_err(|_|Error::Identity("native drain donor output seal"))?;
-            if output.support.start_ns().get()!=identity.support_start_ns
-                || output.support.end_ns().get()!=identity.support_end_ns
-                || output.mass_kg_m2_ofe_ground<=0.0 || !seen.insert(output.lane_id)
-                || !configuration.parent().ofe_bindings.iter().any(|binding|
-                    binding.production_lane_id==output.lane_id && binding.ofe_id==output.ofe_id)
-            {return Err(Error::Identity("native drain donor lane/support"));}
+            output
+                .validate()
+                .map_err(|_| Error::Identity("native drain donor output seal"))?;
+            if output.support.start_ns().get() != identity.support_start_ns
+                || output.support.end_ns().get() != identity.support_end_ns
+                || output.mass_kg_m2_ofe_ground <= 0.0
+                || !seen.insert(output.lane_id)
+                || !configuration.parent().ofe_bindings.iter().any(|binding| {
+                    binding.production_lane_id == output.lane_id && binding.ofe_id == output.ofe_id
+                })
+            {
+                return Err(Error::Identity("native drain donor lane/support"));
+            }
             for destination in &output.destinations {
                 inputs.push(DirectZeroDurationSnowLiquidInputV1 {
-                    output_receipt_sha256:*output.receipt_sha256.as_bytes(),
-                    output_set_sha256:*identity.output_set_sha256.as_bytes(),
-                    predecessor_owner_set_sha256:*identity.donor_ending_complete_owner_sha256.as_bytes(),
-                    receiver_context_sha256:*identity.receiver_context_sha256.as_bytes(),
-                    support_start_ns:identity.support_start_ns, support_end_ns:identity.support_end_ns,
-                    receiver_ordinal:identity.receiver_ordinal, ofe_id:destination.ofe_id.clone(),
-                    tile_id:destination.tile_id.clone(), tile_fraction:destination.tile_fraction,
-                    mass_kg_m2_tile_ground:destination.mass_kg_m2_tile_ground,
-                    sensible_enthalpy_j_m2_tile_ground:destination.sensible_enthalpy_j_m2_tile_ground,
+                    output_receipt_sha256: *output.receipt_sha256.as_bytes(),
+                    output_set_sha256: *identity.output_set_sha256.as_bytes(),
+                    predecessor_owner_set_sha256: *identity
+                        .donor_ending_complete_owner_sha256
+                        .as_bytes(),
+                    receiver_context_sha256: *identity.receiver_context_sha256.as_bytes(),
+                    support_start_ns: identity.support_start_ns,
+                    support_end_ns: identity.support_end_ns,
+                    receiver_ordinal: identity.receiver_ordinal,
+                    ofe_id: destination.ofe_id.clone(),
+                    tile_id: destination.tile_id.clone(),
+                    tile_fraction: destination.tile_fraction,
+                    mass_kg_m2_tile_ground: destination.mass_kg_m2_tile_ground,
+                    sensible_enthalpy_j_m2_tile_ground: destination
+                        .sensible_enthalpy_j_m2_tile_ground,
                 });
             }
         }
-        let fields=self.outputs.iter().map(|output|FramedField {tag:"snow_liquid_output",
-            value:output.receipt_sha256.as_bytes().as_slice()}).collect::<Vec<_>>();
-        let output_set=framed_sha256("stage3-v11-positive-support-liquid-output-set",&fields)
-            .map_err(|_|Error::Schema("native drain output-set seal"))?;
-        if output_set!=identity.output_set_sha256 {return Err(Error::Identity("native drain output-set substitution"));}
+        let fields = self
+            .outputs
+            .iter()
+            .map(|output| FramedField {
+                tag: "snow_liquid_output",
+                value: output.receipt_sha256.as_bytes().as_slice(),
+            })
+            .collect::<Vec<_>>();
+        let output_set = framed_sha256("stage3-v11-positive-support-liquid-output-set", &fields)
+            .map_err(|_| Error::Schema("native drain output-set seal"))?;
+        if output_set != identity.output_set_sha256 {
+            return Err(Error::Identity("native drain output-set substitution"));
+        }
         Ok(inputs)
     }
 
-    fn reconstruct(&self,configuration:&SurfaceLiquidConfigurationV2,lse_configuration:&LandSurfaceEnergyConfiguration)
-        ->Result<(SurfaceLiquidOwnerEnvelopeV2,LandSurfaceEnergyV3State,LseSurfaceEnthalpyOwnerEnvelopeV1,Vec<DirectZeroDurationSnowLiquidReceiptV1>),Error> {
-        let beginning=surface(configuration,&self.beginning_surface_bytes)?;
-        self.beginning_exact.validate_frozen_parent_join(lse_configuration,&self.beginning_lse,configuration,&beginning)
-            .map_err(|_|Error::Identity("native custody beginning exact/physical join"))?;
-        let marker=self.beginning_lse.0.last_accepted_transaction_id;
-        if beginning.v2_state().ok_or(Error::Identity("native custody V2 beginning"))?.records().iter()
-            .any(|record|record.last_accepted_transaction_id!=marker)
-            || self.beginning_exact.records().iter().any(|record|record.last_accepted_transaction_id!=marker)
-        {return Err(Error::Identity("native custody mixed beginning markers"));}
-        let ending_marker=self.identity.ending_marker(marker)?;
-        let inputs=self.inputs(configuration)?;
-        if self.identity.kind==NativeSnowCustodyKindV1::Drain {
-            let candidate=prepare_native_snow_liquid_candidate_v1(configuration,lse_configuration,
-                &self.beginning_lse,&beginning,&self.beginning_exact,&inputs,self.identity.transaction_id,ending_marker)?;
-            return Ok((candidate.surface_owner,candidate.lse_state,candidate.exact_owner,candidate.allocation.receipts));
+    fn reconstruct(
+        &self,
+        configuration: &SurfaceLiquidConfigurationV2,
+        lse_configuration: &LandSurfaceEnergyConfiguration,
+    ) -> Result<
+        (
+            SurfaceLiquidOwnerEnvelopeV2,
+            LandSurfaceEnergyV3State,
+            LseSurfaceEnthalpyOwnerEnvelopeV1,
+            Vec<DirectZeroDurationSnowLiquidReceiptV1>,
+        ),
+        Error,
+    > {
+        let beginning = surface(configuration, &self.beginning_surface_bytes)?;
+        self.beginning_exact
+            .validate_frozen_parent_join(
+                lse_configuration,
+                &self.beginning_lse,
+                configuration,
+                &beginning,
+            )
+            .map_err(|_| Error::Identity("native custody beginning exact/physical join"))?;
+        let marker = self.beginning_lse.0.last_accepted_transaction_id;
+        if beginning
+            .v2_state()
+            .ok_or(Error::Identity("native custody V2 beginning"))?
+            .records()
+            .iter()
+            .any(|record| record.last_accepted_transaction_id != marker)
+            || self
+                .beginning_exact
+                .records()
+                .iter()
+                .any(|record| record.last_accepted_transaction_id != marker)
+        {
+            return Err(Error::Identity("native custody mixed beginning markers"));
+        }
+        let ending_marker = self.identity.ending_marker(marker)?;
+        let inputs = self.inputs(configuration)?;
+        if self.identity.kind == NativeSnowCustodyKindV1::Drain {
+            let candidate = prepare_native_snow_liquid_candidate_v1(
+                configuration,
+                lse_configuration,
+                &self.beginning_lse,
+                &beginning,
+                &self.beginning_exact,
+                &inputs,
+                self.identity.transaction_id,
+                ending_marker,
+            )?;
+            return Ok((
+                candidate.surface_owner,
+                candidate.lse_state,
+                candidate.exact_owner,
+                candidate.allocation.receipts,
+            ));
         }
         // The history validator separately proves an earlier accepted partial
         // drain made this parent dirty. No physics or allocation occurs here.
-        Ok((beginning,self.beginning_lse.clone(),self.beginning_exact.clone(),Vec::new()))
-    }
-
-    fn recomputed_receipt(&self)->Result<Digest32,Error> {
-        let mut value=self.clone(); value.receipt_sha256=Digest32::zero(); value.publication_sha256=Digest32::zero();
-        value.ending_exact.receipt_chain_sha256=wire_digest(Digest32::zero())?;
+        Ok((
+            beginning,
+            self.beginning_lse.clone(),
+            self.beginning_exact.clone(),
+            Vec::new(),
+        ))
+    }
+
+    fn recomputed_receipt(&self) -> Result<Digest32, Error> {
+        let mut value = self.clone();
+        value.receipt_sha256 = Digest32::zero();
+        value.publication_sha256 = Digest32::zero();
+        value.ending_exact.receipt_chain_sha256 = wire_digest(Digest32::zero())?;
         // State digest excludes the receipt chain by existing exact-owner law.
-        Ok(digest_bytes(&serde_json::to_vec(&value).map_err(|_|Error::Schema("native custody receipt preimage"))?))
-    }
-    fn recomputed_publication(&self)->Result<Digest32,Error> {
-        let mut value=self.clone(); value.publication_sha256=Digest32::zero();
-        Ok(digest_bytes(&serde_json::to_vec(&value).map_err(|_|Error::Schema("native custody publication preimage"))?))
-    }
-    pub(crate) fn validate(&self,configuration:&SurfaceLiquidConfigurationV2,lse_configuration:&LandSurfaceEnergyConfiguration)->Result<(),Error> {
-        if self.schema!=NATIVE_SNOW_CUSTODY_SCHEMA
-            || self.surface_configuration_sha256!=configuration.configuration_sha256()
-            || self.lse_configuration_sha256!=lse_configuration.configuration_sha256
-            || self.receipt_sha256==Digest32::zero() || self.receipt_sha256!=self.recomputed_receipt()?
-            || self.publication_sha256==Digest32::zero() || self.publication_sha256!=self.recomputed_publication()?
-        {return Err(Error::Identity("native custody publication seal/configuration"));}
+        Ok(digest_bytes(&serde_json::to_vec(&value).map_err(|_| {
+            Error::Schema("native custody receipt preimage")
+        })?))
+    }
+    fn recomputed_publication(&self) -> Result<Digest32, Error> {
+        let mut value = self.clone();
+        value.publication_sha256 = Digest32::zero();
+        Ok(digest_bytes(&serde_json::to_vec(&value).map_err(|_| {
+            Error::Schema("native custody publication preimage")
+        })?))
+    }
+    pub(crate) fn validate(
+        &self,
+        configuration: &SurfaceLiquidConfigurationV2,
+        lse_configuration: &LandSurfaceEnergyConfiguration,
+    ) -> Result<(), Error> {
+        if self.schema != NATIVE_SNOW_CUSTODY_SCHEMA
+            || self.surface_configuration_sha256 != configuration.configuration_sha256()
+            || self.lse_configuration_sha256 != lse_configuration.configuration_sha256
+            || self.receipt_sha256 == Digest32::zero()
+            || self.receipt_sha256 != self.recomputed_receipt()?
+            || self.publication_sha256 == Digest32::zero()
+            || self.publication_sha256 != self.recomputed_publication()?
+        {
+            return Err(Error::Identity(
+                "native custody publication seal/configuration",
+            ));
+        }
         self.lse_owner_bytes(false)?;
         self.lse_owner_bytes(true)?;
-        let (expected_surface,expected_lse,mut expected_exact,receipts)=self.reconstruct(configuration,lse_configuration)?;
+        let (expected_surface, expected_lse, mut expected_exact, receipts) =
+            self.reconstruct(configuration, lse_configuration)?;
         let beginning_surface = surface(configuration, &self.beginning_surface_bytes)?;
         let liquid_records = |owner: &SurfaceLiquidOwnerEnvelopeV2| -> Result<Vec<_>, Error> {
-            Ok(owner.v2_state().ok_or(Error::Identity("native liquid projection V2"))?.records()
-                .iter().map(|record| (record.key.clone(), record.liquid_kg_m2_tile)).collect())
+            Ok(owner
+                .v2_state()
+                .ok_or(Error::Identity("native liquid projection V2"))?
+                .records()
+                .iter()
+                .map(|record| (record.key.clone(), record.liquid_kg_m2_tile))
+                .collect())
         };
         if liquid_records(&beginning_surface)? != self.beginning_liquid_records
-            || liquid_records(&expected_surface)? != self.ending_liquid_records {
+            || liquid_records(&expected_surface)? != self.ending_liquid_records
+        {
             return Err(Error::Identity("native liquid projection physical join"));
         }
-        expected_exact.receipt_chain_sha256=wire_digest(self.receipt_sha256)?;
-        if expected_surface.canonical_bytes(configuration.parent(),Some(configuration))?!=self.ending_surface_bytes
-            || expected_lse!=self.ending_lse || expected_exact!=self.ending_exact || receipts!=self.allocation_receipts
-        {return Err(Error::Closure("native custody independently replayed ending"));}
-        self.ending_exact.validate_frozen_parent_join(lse_configuration,&self.ending_lse,configuration,&expected_surface)
-            .map_err(|_|Error::Identity("native custody ending owner join"))?;
+        expected_exact.receipt_chain_sha256 = wire_digest(self.receipt_sha256)?;
+        if expected_surface.canonical_bytes(configuration.parent(), Some(configuration))?
+            != self.ending_surface_bytes
+            || expected_lse != self.ending_lse
+            || expected_exact != self.ending_exact
+            || receipts != self.allocation_receipts
+        {
+            return Err(Error::Closure(
+                "native custody independently replayed ending",
+            ));
+        }
+        self.ending_exact
+            .validate_frozen_parent_join(
+                lse_configuration,
+                &self.ending_lse,
+                configuration,
+                &expected_surface,
+            )
+            .map_err(|_| Error::Identity("native custody ending owner join"))?;
         Ok(())
     }
     #[allow(clippy::too_many_arguments)]
-    pub(crate) fn try_new(configuration:&SurfaceLiquidConfigurationV2,lse_configuration:&LandSurfaceEnergyConfiguration,
-        identity:NativeSnowCustodyIdentityV1,runtime_authority:openwepp_coupled_time::Policy14RuntimeAuthority,active_covered_lse:LandSurfaceEnergyState,beginning_lse:LandSurfaceEnergyV3State,
-        beginning_surface:&SurfaceLiquidOwnerEnvelopeV2,beginning_exact:LseSurfaceEnthalpyOwnerEnvelopeV1,
-        outputs:Vec<Stage3AcceptedSnowLiquidOutputV1>)->Result<Self,Error> {
-        let bytes=beginning_surface.canonical_bytes(configuration.parent(),Some(configuration))?;
-        let mut value=Self {schema:NATIVE_SNOW_CUSTODY_SCHEMA.into(),identity,runtime_authority,active_covered_lse,
-            lse_configuration_sha256:lse_configuration.configuration_sha256.clone(),
-            surface_configuration_sha256:configuration.configuration_sha256().into(),
-            ending_lse:beginning_lse.clone(),beginning_lse,ending_surface_bytes:bytes.clone(),beginning_surface_bytes:bytes,
-            beginning_liquid_records: beginning_surface.v2_state().ok_or(Error::Identity("native beginning liquid V2"))?
-                .records().iter().map(|record| (record.key.clone(), record.liquid_kg_m2_tile)).collect(),
+    pub(crate) fn try_new(
+        configuration: &SurfaceLiquidConfigurationV2,
+        lse_configuration: &LandSurfaceEnergyConfiguration,
+        identity: NativeSnowCustodyIdentityV1,
+        runtime_authority: openwepp_coupled_time::Policy14RuntimeAuthority,
+        active_covered_lse: LandSurfaceEnergyState,
+        beginning_lse: LandSurfaceEnergyV3State,
+        beginning_surface: &SurfaceLiquidOwnerEnvelopeV2,
+        beginning_exact: LseSurfaceEnthalpyOwnerEnvelopeV1,
+        outputs: Vec<Stage3AcceptedSnowLiquidOutputV1>,
+    ) -> Result<Self, Error> {
+        let bytes =
+            beginning_surface.canonical_bytes(configuration.parent(), Some(configuration))?;
+        let mut value = Self {
+            schema: NATIVE_SNOW_CUSTODY_SCHEMA.into(),
+            identity,
+            runtime_authority,
+            active_covered_lse,
+            lse_configuration_sha256: lse_configuration.configuration_sha256.clone(),
+            surface_configuration_sha256: configuration.configuration_sha256().into(),
+            ending_lse: beginning_lse.clone(),
+            beginning_lse,
+            ending_surface_bytes: bytes.clone(),
+            beginning_surface_bytes: bytes,
+            beginning_liquid_records: beginning_surface
+                .v2_state()
+                .ok_or(Error::Identity("native beginning liquid V2"))?
+                .records()
+                .iter()
+                .map(|record| (record.key.clone(), record.liquid_kg_m2_tile))
+                .collect(),
             ending_liquid_records: Vec::new(),
-            ending_exact:beginning_exact.clone(),beginning_exact,outputs,allocation_receipts:Vec::new(),
-            receipt_sha256:Digest32::zero(),publication_sha256:Digest32::zero()};
-        let (ending,lse,exact,receipts)=value.reconstruct(configuration,lse_configuration)?;
-        value.ending_liquid_records = ending.v2_state().ok_or(Error::Identity("native ending liquid V2"))?
-            .records().iter().map(|record| (record.key.clone(), record.liquid_kg_m2_tile)).collect();
-        value.ending_surface_bytes=ending.canonical_bytes(configuration.parent(),Some(configuration))?;
-        value.ending_lse=lse;value.ending_exact=exact;value.allocation_receipts=receipts;
-        value.receipt_sha256=value.recomputed_receipt()?;
-        value.ending_exact.receipt_chain_sha256=wire_digest(value.receipt_sha256)?;
-        value.publication_sha256=value.recomputed_publication()?;
-        value.validate(configuration,lse_configuration)?;
+            ending_exact: beginning_exact.clone(),
+            beginning_exact,
+            outputs,
+            allocation_receipts: Vec::new(),
+            receipt_sha256: Digest32::zero(),
+            publication_sha256: Digest32::zero(),
+        };
+        let (ending, lse, exact, receipts) = value.reconstruct(configuration, lse_configuration)?;
+        value.ending_liquid_records = ending
+            .v2_state()
+            .ok_or(Error::Identity("native ending liquid V2"))?
+            .records()
+            .iter()
+            .map(|record| (record.key.clone(), record.liquid_kg_m2_tile))
+            .collect();
+        value.ending_surface_bytes =
+            ending.canonical_bytes(configuration.parent(), Some(configuration))?;
+        value.ending_lse = lse;
+        value.ending_exact = exact;
+        value.allocation_receipts = receipts;
+        value.receipt_sha256 = value.recomputed_receipt()?;
+        value.ending_exact.receipt_chain_sha256 = wire_digest(value.receipt_sha256)?;
+        value.publication_sha256 = value.recomputed_publication()?;
+        value.validate(configuration, lse_configuration)?;
         Ok(value)
     }
-    pub(crate) fn canonical_bytes(&self,configuration:&SurfaceLiquidConfigurationV2,lse_configuration:&LandSurfaceEnergyConfiguration)->Result<Vec<u8>,Error> {
-        self.validate(configuration,lse_configuration)?;
-        serde_json::to_vec(self).map_err(|_|Error::Schema("native custody serialization"))
-    }
-    pub(crate) fn from_canonical_bytes(configuration:&SurfaceLiquidConfigurationV2,lse_configuration:&LandSurfaceEnergyConfiguration,bytes:&[u8])->Result<Self,Error> {
-        let value:Self=serde_json::from_slice(bytes).map_err(|_|Error::Schema("native custody parsing"))?;
-        if value.canonical_bytes(configuration,lse_configuration)?!=bytes {return Err(Error::Schema("native custody noncanonical bytes"));}
+    pub(crate) fn canonical_bytes(
+        &self,
+        configuration: &SurfaceLiquidConfigurationV2,
+        lse_configuration: &LandSurfaceEnergyConfiguration,
+    ) -> Result<Vec<u8>, Error> {
+        self.validate(configuration, lse_configuration)?;
+        serde_json::to_vec(self).map_err(|_| Error::Schema("native custody serialization"))
+    }
+    pub(crate) fn from_canonical_bytes(
+        configuration: &SurfaceLiquidConfigurationV2,
+        lse_configuration: &LandSurfaceEnergyConfiguration,
+        bytes: &[u8],
+    ) -> Result<Self, Error> {
+        let value: Self =
+            serde_json::from_slice(bytes).map_err(|_| Error::Schema("native custody parsing"))?;
+        if value.canonical_bytes(configuration, lse_configuration)? != bytes {
+            return Err(Error::Schema("native custody noncanonical bytes"));
+        }
         Ok(value)
     }
 }
@@ -299,46 +544,93 @@
         donor_ending: Digest32,
         ordinal: u32,
         terminal_lanes: &BTreeSet<u32>,
-    ) -> Result<Option<(Self, NativeSnowLiquidPublicationV1)>, super::DirectV11RealConsumerError> {
+    ) -> Result<Option<(Self, NativeSnowLiquidPublicationV1)>, super::DirectV11RealConsumerError>
+    {
         use super::DirectV11RealConsumerError as ConsumerError;
-        let physical = self.frozen_litter_v3.as_ref()
+        let physical = self
+            .frozen_litter_v3
+            .as_ref()
             .ok_or(ConsumerError::Identity("native drain physical resident"))?;
-        let exact = self.frozen_litter_v4.as_ref()
+        let exact = self
+            .frozen_litter_v4
+            .as_ref()
             .ok_or(ConsumerError::Identity("native drain exact resident"))?;
-        let outputs = self.accepted_snow_liquid_outputs_for_support(support)?
-            .into_iter().filter(|output| !terminal_lanes.contains(&output.lane_id())).collect::<Vec<_>>();
+        let outputs = self
+            .accepted_snow_liquid_outputs_for_support(support)?
+            .into_iter()
+            .filter(|output| !terminal_lanes.contains(&output.lane_id()))
+            .collect::<Vec<_>>();
         let transaction_id = TransactionId(self.vegetation_state.0.last_transaction_id);
-        let dirty = physical.native_dirty_parent() == Some((transaction_id,
-            parent_support.start_ns().get(), parent_support.end_ns().get(), parent_digest));
+        let dirty = physical.native_dirty_parent()
+            == Some((
+                transaction_id,
+                parent_support.start_ns().get(),
+                parent_support.end_ns().get(),
+                parent_digest,
+            ));
         if outputs.is_empty() && !(dirty && support.end_ns() == parent_support.end_ns()) {
             return Ok(None);
         }
-        let kind = if outputs.is_empty() { NativeSnowCustodyKindV1::ParentFinalization }
-            else { NativeSnowCustodyKindV1::Drain };
-        let output_set = if outputs.is_empty() { Digest32::zero() } else {
-            positive_support_output_summary_v1(&outputs, support.start_ns().get(), support.end_ns().get())
-                .map_err(ConsumerError::SurfaceLiquidReplay)?.0
+        let kind = if outputs.is_empty() {
+            NativeSnowCustodyKindV1::ParentFinalization
+        } else {
+            NativeSnowCustodyKindV1::Drain
+        };
+        let output_set = if outputs.is_empty() {
+            Digest32::zero()
+        } else {
+            positive_support_output_summary_v1(
+                &outputs,
+                support.start_ns().get(),
+                support.end_ns().get(),
+            )
+            .map_err(ConsumerError::SurfaceLiquidReplay)?
+            .0
         };
         let mut identity = NativeSnowCustodyIdentityV1 {
-            policy_id: 14, kind, parent_transaction_sha256: parent_digest, transaction_id,
+            policy_id: 14,
+            kind,
+            parent_transaction_sha256: parent_digest,
+            transaction_id,
             predecessor_transaction_id: physical.lse_state().0.last_accepted_transaction_id,
-            parent_start_ns: parent_support.start_ns().get(), parent_end_ns: parent_support.end_ns().get(),
-            support_start_ns: support.start_ns().get(), support_end_ns: support.end_ns().get(),
-            receiver_ordinal: ordinal, donor_ending_complete_owner_sha256: donor_ending,
-            receiver_context_sha256: Digest32::zero(), output_set_sha256: output_set,
-            predecessor_physical_receipt_chain_sha256: physical.predecessor_receipt_chain_sha256().into(),
+            parent_start_ns: parent_support.start_ns().get(),
+            parent_end_ns: parent_support.end_ns().get(),
+            support_start_ns: support.start_ns().get(),
+            support_end_ns: support.end_ns().get(),
+            receiver_ordinal: ordinal,
+            donor_ending_complete_owner_sha256: donor_ending,
+            receiver_context_sha256: Digest32::zero(),
+            output_set_sha256: output_set,
+            predecessor_physical_receipt_chain_sha256: physical
+                .predecessor_receipt_chain_sha256()
+                .into(),
         };
-        identity.receiver_context_sha256 = identity.expected_receiver_context(&outputs)
+        identity.receiver_context_sha256 = identity
+            .expected_receiver_context(&outputs)
             .map_err(ConsumerError::SurfaceLiquidReplay)?;
-        let record = NativeSnowLiquidPublicationV1::try_new(physical.surface_configuration(),
-            physical.lse_configuration(), identity, super::composite_lse_owner::actual_runtime_authority(self.inner.authority), self.inner.lse_state.clone(), physical.lse_state().clone(), physical.surface_owner(),
-            exact.exact_surface_owner().clone(), outputs.clone()).map_err(ConsumerError::SurfaceLiquidReplay)?;
+        let record = NativeSnowLiquidPublicationV1::try_new(
+            physical.surface_configuration(),
+            physical.lse_configuration(),
+            identity,
+            super::composite_lse_owner::actual_runtime_authority(self.inner.authority),
+            self.inner.lse_state.clone(),
+            physical.lse_state().clone(),
+            physical.surface_owner(),
+            exact.exact_surface_owner().clone(),
+            outputs.clone(),
+        )
+        .map_err(ConsumerError::SurfaceLiquidReplay)?;
         let mut candidate = self.clone();
-        let candidate_physical = candidate.frozen_litter_v3.as_mut()
+        let candidate_physical = candidate
+            .frozen_litter_v3
+            .as_mut()
             .ok_or(ConsumerError::Identity("native drain candidate physical"))?;
-        candidate_physical.accept_native_snow_publication(record.clone(), &outputs)
+        candidate_physical
+            .accept_native_snow_publication(record.clone(), &outputs)
             .map_err(ConsumerError::SurfaceLiquidReplay)?;
-        candidate.frozen_litter_v4.as_mut()
+        candidate
+            .frozen_litter_v4
+            .as_mut()
             .ok_or(ConsumerError::Identity("native drain candidate exact"))?
             .accept_native_snow_publication(candidate_physical, &record)?;
         Ok(Some((candidate, record)))
@@ -349,31 +641,49 @@
     /// Transport integrity only. Full configuration-bound semantic replay is
     /// mandatory in the native resident acceptance/restore path.
     pub(crate) fn from_transport_bytes(bytes: &[u8]) -> Result<Self, Error> {
-        let value: Self = serde_json::from_slice(bytes).map_err(|_| Error::Schema("native transport parsing"))?;
-        if serde_json::to_vec(&value).map_err(|_| Error::Schema("native transport encoding"))? != bytes
+        let value: Self =
+            serde_json::from_slice(bytes).map_err(|_| Error::Schema("native transport parsing"))?;
+        if serde_json::to_vec(&value).map_err(|_| Error::Schema("native transport encoding"))?
+            != bytes
             || value.schema != NATIVE_SNOW_CUSTODY_SCHEMA
             || value.receipt_sha256 == Digest32::zero()
             || value.receipt_sha256 != value.recomputed_receipt()?
             || value.publication_sha256 == Digest32::zero()
             || value.publication_sha256 != value.recomputed_publication()?
-            || value.identity.receiver_context_sha256 != value.identity.expected_receiver_context(&value.outputs)?
-        { return Err(Error::Identity("native transport seal")); }
-        value.identity.ending_marker(value.beginning_lse.0.last_accepted_transaction_id)?;
+            || value.identity.receiver_context_sha256
+                != value.identity.expected_receiver_context(&value.outputs)?
+        {
+            return Err(Error::Identity("native transport seal"));
+        }
+        value
+            .identity
+            .ending_marker(value.beginning_lse.0.last_accepted_transaction_id)?;
         value.lse_owner_bytes(false)?;
         value.lse_owner_bytes(true)?;
         if value.identity.kind == NativeSnowCustodyKindV1::ParentFinalization
-            && (!value.outputs.is_empty() || !value.allocation_receipts.is_empty()
+            && (!value.outputs.is_empty()
+                || !value.allocation_receipts.is_empty()
                 || value.identity.output_set_sha256 != Digest32::zero())
-        { return Err(Error::Identity("native finalization transport operands")); }
+        {
+            return Err(Error::Identity("native finalization transport operands"));
+        }
         Ok(value)
     }
 }
 
 impl NativeSnowLiquidPublicationV1 {
     pub(crate) fn lse_owner_bytes(&self, ending: bool) -> Result<Vec<u8>, Error> {
-        let (lse, exact) = if ending { (&self.ending_lse, &self.ending_exact) }
-            else { (&self.beginning_lse, &self.beginning_exact) };
-        super::composite_lse_owner::composite_lse_bytes(self.runtime_authority, &self.active_covered_lse, lse, exact)
+        let (lse, exact) = if ending {
+            (&self.ending_lse, &self.ending_exact)
+        } else {
+            (&self.beginning_lse, &self.beginning_exact)
+        };
+        super::composite_lse_owner::composite_lse_bytes(
+            self.runtime_authority,
+            &self.active_covered_lse,
+            lse,
+            exact,
+        )
     }
 }
 
@@ -387,35 +697,66 @@
         use super::DirectV11RealConsumerError as ConsumerError;
         use super::frozen_litter_v3_publication_retention::FrozenLitterHistoryEntryV1;
         let Some(physical) = self.frozen_litter_v3.as_ref() else {
-            if subslabs.iter().any(|receipt| receipt.post_support_native_custody.is_some()) {
-                return Err(ConsumerError::Identity("native packet without native owner"));
+            if subslabs
+                .iter()
+                .any(|receipt| receipt.post_support_native_custody.is_some())
+            {
+                return Err(ConsumerError::Identity(
+                    "native packet without native owner",
+                ));
             }
             return Ok(());
         };
         if let Some((tx, start, end, digest)) = physical.native_dirty_parent() {
-            if !allow_open_parent || tx.0 != self.vegetation_state.0.last_transaction_id
+            if !allow_open_parent
+                || tx.0 != self.vegetation_state.0.last_transaction_id
                 || start != clock.parent_support().start_ns().get()
                 || end != clock.parent_support().end_ns().get()
                 || digest != clock.parent_transaction_id().digest()
                 || clock.accepted_until() <= clock.parent_support().start_ns()
-                || clock.accepted_until() >= clock.parent_support().end_ns() {
-                return Err(ConsumerError::Identity("native dirty parent enclosing posture"));
+                || clock.accepted_until() >= clock.parent_support().end_ns()
+            {
+                return Err(ConsumerError::Identity(
+                    "native dirty parent enclosing posture",
+                ));
             }
         }
         if self.frozen_litter_v4.is_some() {
-            let surface = physical.surface_owner().canonical_bytes(physical.surface_configuration().parent(), Some(physical.surface_configuration()))
+            let surface = physical
+                .surface_owner()
+                .canonical_bytes(
+                    physical.surface_configuration().parent(),
+                    Some(physical.surface_configuration()),
+                )
                 .map_err(ConsumerError::SurfaceLiquidReplay)?;
             let lse = self.canonical_v11_lse_owner_bytes()?;
-            if clock.owners().iter().find(|owner| owner.owner_id() == "surface_liquid").is_none_or(|owner| owner.state_bytes() != surface)
-                || clock.owners().iter().find(|owner| owner.owner_id() == "land_surface_energy").is_none_or(|owner| owner.state_bytes() != lse) {
-                return Err(ConsumerError::Identity("native enclosing clock physical owner bytes"));
+            if clock
+                .owners()
+                .iter()
+                .find(|owner| owner.owner_id() == "surface_liquid")
+                .is_none_or(|owner| owner.state_bytes() != surface)
+                || clock
+                    .owners()
+                    .iter()
+                    .find(|owner| owner.owner_id() == "land_surface_energy")
+                    .is_none_or(|owner| owner.state_bytes() != lse)
+            {
+                return Err(ConsumerError::Identity(
+                    "native enclosing clock physical owner bytes",
+                ));
             }
         }
         let mut seen = BTreeSet::new();
         for subslab in subslabs {
-            let Some(packet) = &subslab.post_support_native_custody else { continue; };
-            subslab.validate().map_err(|_| ConsumerError::Identity("native boundary subslab seal"))?;
-            let record = packet.publication().map_err(|_| ConsumerError::Identity("native boundary packet"))?;
+            let Some(packet) = &subslab.post_support_native_custody else {
+                continue;
+            };
+            subslab
+                .validate()
+                .map_err(|_| ConsumerError::Identity("native boundary subslab seal"))?;
+            let record = packet
+                .publication()
+                .map_err(|_| ConsumerError::Identity("native boundary packet"))?;
             // The immutable resident history was configuration-validated at
             // acceptance or full durable reconstruction. Exact membership,
             // not a transport self-seal, authenticates its derived projections.
@@ -424,19 +765,30 @@
                     matches!(entry, FrozenLitterHistoryEntryV1::Custody(accepted) if accepted.as_ref() == &record)) {
                 return Err(ConsumerError::Identity("native packet validated history join"));
             }
-            let outputs = self.accepted_snow_liquid_outputs_for_retained_support(subslab.support)?
-                .into_iter().filter(|output| !subslab.terminal_events.contains_key(&output.lane_id())).collect::<Vec<_>>();
+            let outputs = self
+                .accepted_snow_liquid_outputs_for_retained_support(subslab.support)?
+                .into_iter()
+                .filter(|output| !subslab.terminal_events.contains_key(&output.lane_id()))
+                .collect::<Vec<_>>();
             if outputs != record.outputs {
-                return Err(ConsumerError::Identity("native boundary complete donor set"));
-            }
-        }
-        if let (Some(start), Some(end)) = (subslabs.iter().map(|s| s.support.start_ns().get()).min(),
-            subslabs.iter().map(|s| s.support.end_ns().get()).max()) {
+                return Err(ConsumerError::Identity(
+                    "native boundary complete donor set",
+                ));
+            }
+        }
+        if let (Some(start), Some(end)) = (
+            subslabs.iter().map(|s| s.support.start_ns().get()).min(),
+            subslabs.iter().map(|s| s.support.end_ns().get()).max(),
+        ) {
             for entry in physical.accepted_history_entries() {
                 if let FrozenLitterHistoryEntryV1::Custody(record) = entry {
-                    if record.identity.support_start_ns >= start && record.identity.support_end_ns <= end
-                        && !seen.contains(&record.publication_sha256) {
-                        return Err(ConsumerError::Identity("native boundary omitted custody packet"));
+                    if record.identity.support_start_ns >= start
+                        && record.identity.support_end_ns <= end
+                        && !seen.contains(&record.publication_sha256)
+                    {
+                        return Err(ConsumerError::Identity(
+                            "native boundary omitted custody packet",
+                        ));
                     }
                 }
             }
--- a/crates/openwepp-hillslope-orchestrator/src/v9_real_consumer_shadow/snow_free_physical_reuse.rs
+++ b/crates/openwepp-hillslope-orchestrator/src/v9_real_consumer_shadow/snow_free_physical_reuse.rs
@@ -577,8 +577,11 @@
             "missing staged LSE owner",
         ))?
         .state_bytes;
-    let (support_configuration, support_beginning) =
-        super::v11_support_lse_beginning(beginning, staged_lse_bytes, super::V11SupportLseRole::NativeSnowFree)?;
+    let (support_configuration, support_beginning) = super::v11_support_lse_beginning(
+        beginning,
+        staged_lse_bytes,
+        super::V11SupportLseRole::NativeSnowFree,
+    )?;
     let support_receipt = super::LseSupportAdmissibilityReceiptV1::admit(
         support_configuration,
         support_beginning,
--- a/crates/openwepp-hillslope-orchestrator/src/v9_real_consumer_shadow/v10_soil_thermal_v2_unpublished_continuation.rs
+++ b/crates/openwepp-hillslope-orchestrator/src/v9_real_consumer_shadow/v10_soil_thermal_v2_unpublished_continuation.rs
@@ -159,9 +159,11 @@
                 DirectV9RealConsumerError::OwnerClosure("V2 chained continuation unpublished trial")
             },
         )?;
-        prior.compose_accepted_outer_candidate(configuration).map_err(|_| {
-            DirectV9RealConsumerError::OwnerClosure("V2 chained continuation prior replay")
-        })?;
+        prior
+            .compose_accepted_outer_candidate(configuration)
+            .map_err(|_| {
+                DirectV9RealConsumerError::OwnerClosure("V2 chained continuation prior replay")
+            })?;
         Ok(Self {
             original_prepared: original_prepared.clone(),
             retained_trial: trial.clone(),
@@ -500,6 +502,14 @@
         &self.accumulated_operands
     }
 
+    /// Read-only diagnostic projection of the complete ordered credit chain.
+    /// This exposes reconstructible operands, never an install authority.
+    pub(crate) fn diagnostic_layer_credit_chain(
+        &self,
+    ) -> &[Vec<openwepp_land_surface_energy::SoilThermalLayerEnergyCreditV2>] {
+        &self.layer_credit_chain
+    }
+
     pub fn compose_accepted_outer_candidate(
         &self,
         configuration: &LandSurfaceEnergyConfiguration,
@@ -516,7 +526,12 @@
             && self
                 .physical_trial
                 .accepted_predecessor_receipt_chain_sha256()
-                == Some(&self.original_prepared.beginning_owner().receipt_chain_sha256)
+                == Some(
+                    &self
+                        .original_prepared
+                        .beginning_owner()
+                        .receipt_chain_sha256,
+                )
             && self.layer_credit_chain.len() == 1
             && self.layer_credit_chain.first().map(Vec::as_slice)
                 == Some(self.physical_trial.layer_credits())
--- a/crates/openwepp-hillslope-orchestrator/src/v9_real_consumer_shadow/v3_publication_retention_tests.rs
+++ b/crates/openwepp-hillslope-orchestrator/src/v9_real_consumer_shadow/v3_publication_retention_tests.rs
@@ -1561,14 +1561,20 @@
 }
 
 fn native_drain_record_for_history_test(
-    physical:&FrozenLitterV3Resident,exact:&FrozenLitterV4Resident,start:u128,end:u128,finalize:bool,
-)->super::native_snow_liquid_publication::NativeSnowLiquidPublicationV1 {
-    use super::native_snow_liquid_publication::{NativeSnowLiquidPublicationV1,NativeSnowCustodyIdentityV1,NativeSnowCustodyKindV1};
-    use openwepp_coupled_time::{TimeSupport,ModelTimeNs,framed_sha256,FramedField};
-    let mut outputs=Vec::new();
+    physical: &FrozenLitterV3Resident,
+    exact: &FrozenLitterV4Resident,
+    start: u128,
+    end: u128,
+    finalize: bool,
+) -> super::native_snow_liquid_publication::NativeSnowLiquidPublicationV1 {
+    use super::native_snow_liquid_publication::{
+        NativeSnowCustodyIdentityV1, NativeSnowCustodyKindV1, NativeSnowLiquidPublicationV1,
+    };
+    use openwepp_coupled_time::{FramedField, ModelTimeNs, TimeSupport, framed_sha256};
+    let mut outputs = Vec::new();
     if !finalize {
-        let config=physical.surface_configuration().parent();
-        let binding=&config.ofe_bindings[0];
+        let config = physical.surface_configuration().parent();
+        let binding = &config.ofe_bindings[0];
         let mut output=super::Stage3AcceptedSnowLiquidOutputV1 {
             support:TimeSupport::new(ModelTimeNs::new(start),ModelTimeNs::new(end)).unwrap(),
             lane_id:binding.production_lane_id,ofe_id:binding.ofe_id.clone(),
@@ -1582,82 +1588,209 @@
             refreeze_kg_m2_ofe_ground:0.0,physical_ledger_receipt_sha256:Digest32::from_bytes([30;32]),
             receipt_sha256:Digest32::zero(),
         };
-        output.receipt_sha256=digest_bytes(&serde_json::to_vec(&output).unwrap());
-        output.validate().unwrap();outputs.push(output);
+        output.receipt_sha256 = digest_bytes(&serde_json::to_vec(&output).unwrap());
+        output.validate().unwrap();
+        outputs.push(output);
     }
-    let output_set=if finalize {Digest32::zero()} else {
-        framed_sha256("stage3-v11-positive-support-liquid-output-set",&outputs.iter().map(|output|
-            FramedField{tag:"snow_liquid_output",value:output.receipt_sha256.as_bytes().as_slice()}).collect::<Vec<_>>()).unwrap()
+    let output_set = if finalize {
+        Digest32::zero()
+    } else {
+        framed_sha256(
+            "stage3-v11-positive-support-liquid-output-set",
+            &outputs
+                .iter()
+                .map(|output| FramedField {
+                    tag: "snow_liquid_output",
+                    value: output.receipt_sha256.as_bytes().as_slice(),
+                })
+                .collect::<Vec<_>>(),
+        )
+        .unwrap()
     };
-    let mut id=NativeSnowCustodyIdentityV1 {policy_id:14,
-        kind:if finalize {NativeSnowCustodyKindV1::ParentFinalization} else {NativeSnowCustodyKindV1::Drain},
-        parent_transaction_sha256:Digest32::from_bytes([40;32]),transaction_id:TransactionId(1),
-        predecessor_transaction_id:None,parent_start_ns:0,parent_end_ns:1_800_000_000_000,
-        support_start_ns:start,support_end_ns:end,receiver_ordinal:0,
-        donor_ending_complete_owner_sha256:Digest32::from_bytes([41;32]),receiver_context_sha256:Digest32::zero(),
-        output_set_sha256:output_set,predecessor_physical_receipt_chain_sha256:physical.predecessor_receipt_chain_sha256().into()};
-    id.receiver_context_sha256=id.expected_receiver_context(&outputs).unwrap();
-    NativeSnowLiquidPublicationV1::try_new(physical.surface_configuration(),physical.lse_configuration(),id, openwepp_coupled_time::Policy14RuntimeAuthority::V11SnowCovered, crate::land_surface_energy_shadow::endpoint_fixture().lse_state,
-        physical.lse_state().clone(),physical.surface_owner(),exact.exact_surface_owner().clone(),outputs).unwrap()
+    let mut id = NativeSnowCustodyIdentityV1 {
+        policy_id: 14,
+        kind: if finalize {
+            NativeSnowCustodyKindV1::ParentFinalization
+        } else {
+            NativeSnowCustodyKindV1::Drain
+        },
+        parent_transaction_sha256: Digest32::from_bytes([40; 32]),
+        transaction_id: TransactionId(1),
+        predecessor_transaction_id: None,
+        parent_start_ns: 0,
+        parent_end_ns: 1_800_000_000_000,
+        support_start_ns: start,
+        support_end_ns: end,
+        receiver_ordinal: 0,
+        donor_ending_complete_owner_sha256: Digest32::from_bytes([41; 32]),
+        receiver_context_sha256: Digest32::zero(),
+        output_set_sha256: output_set,
+        predecessor_physical_receipt_chain_sha256: physical
+            .predecessor_receipt_chain_sha256()
+            .into(),
+    };
+    id.receiver_context_sha256 = id.expected_receiver_context(&outputs).unwrap();
+    NativeSnowLiquidPublicationV1::try_new(
+        physical.surface_configuration(),
+        physical.lse_configuration(),
+        id,
+        openwepp_coupled_time::Policy14RuntimeAuthority::V11SnowCovered,
+        crate::land_surface_energy_shadow::endpoint_fixture().lse_state,
+        physical.lse_state().clone(),
+        physical.surface_owner(),
+        exact.exact_surface_owner().clone(),
+        outputs,
+    )
+    .unwrap()
 }
 
 #[test]
 fn native_drain_histories_retain_partial_state_finalize_dry_tail_and_reject_replay() {
-    let (mut physical,mut exact)=v4_resident_fixture();
-    let adoption=physical.lse_state().0.state_sha256.clone();
-    let record=native_drain_record_for_history_test(&physical,&exact,0,60_000_000_000,false);
-    let first=record.clone();
-    physical.accept_native_snow_publication(record.clone(),&record.outputs).unwrap();
-    exact.accept_native_snow_publication(&physical,&record).unwrap();
-    assert_eq!(physical.lse_state().0.last_accepted_transaction_id,None);
-    let previous=physical.clone();
-    assert!(physical.accept_native_snow_publication(first.clone(),&first.outputs).is_err());
-    assert_eq!(physical,previous);
-    let finalization=native_drain_record_for_history_test(&physical,&exact,1_740_000_000_000,1_800_000_000_000,true);
+    let (mut physical, mut exact) = v4_resident_fixture();
+    let adoption = physical.lse_state().0.state_sha256.clone();
+    let record = native_drain_record_for_history_test(&physical, &exact, 0, 60_000_000_000, false);
+    let first = record.clone();
+    physical
+        .accept_native_snow_publication(record.clone(), &record.outputs)
+        .unwrap();
+    exact
+        .accept_native_snow_publication(&physical, &record)
+        .unwrap();
+    assert_eq!(physical.lse_state().0.last_accepted_transaction_id, None);
+    let previous = physical.clone();
+    assert!(
+        physical
+            .accept_native_snow_publication(first.clone(), &first.outputs)
+            .is_err()
+    );
+    assert_eq!(physical, previous);
+    let finalization = native_drain_record_for_history_test(
+        &physical,
+        &exact,
+        1_740_000_000_000,
+        1_800_000_000_000,
+        true,
+    );
     let physical_surface_before = physical.surface_owner().clone();
     let physical_lse_before = physical.lse_state().clone();
     let exact_before = exact.exact_surface_owner().clone();
-    let liquids=physical.surface_owner().v2_state().unwrap().records().iter()
-        .map(|record|(record.liquid_kg_m2_tile,record.litter_ice_kg_m2_tile,record.surface_enthalpy_j_m2_tile)).collect::<Vec<_>>();
-    physical.accept_native_snow_publication(finalization.clone(),&[]).unwrap();
-    exact.accept_native_snow_publication(&physical,&finalization).unwrap();
-    assert_eq!(physical.lse_state().0.last_accepted_transaction_id,None);
-    assert_eq!(liquids,physical.surface_owner().v2_state().unwrap().records().iter()
-        .map(|record|(record.liquid_kg_m2_tile,record.litter_ice_kg_m2_tile,record.surface_enthalpy_j_m2_tile)).collect::<Vec<_>>());
+    let liquids = physical
+        .surface_owner()
+        .v2_state()
+        .unwrap()
+        .records()
+        .iter()
+        .map(|record| {
+            (
+                record.liquid_kg_m2_tile,
+                record.litter_ice_kg_m2_tile,
+                record.surface_enthalpy_j_m2_tile,
+            )
+        })
+        .collect::<Vec<_>>();
+    physical
+        .accept_native_snow_publication(finalization.clone(), &[])
+        .unwrap();
+    exact
+        .accept_native_snow_publication(&physical, &finalization)
+        .unwrap();
+    assert_eq!(physical.lse_state().0.last_accepted_transaction_id, None);
+    assert_eq!(
+        liquids,
+        physical
+            .surface_owner()
+            .v2_state()
+            .unwrap()
+            .records()
+            .iter()
+            .map(|record| (
+                record.liquid_kg_m2_tile,
+                record.litter_ice_kg_m2_tile,
+                record.surface_enthalpy_j_m2_tile
+            ))
+            .collect::<Vec<_>>()
+    );
     assert_eq!(physical.surface_owner(), &physical_surface_before);
     assert_eq!(physical.lse_state(), &physical_lse_before);
     let mut expected_exact = exact_before;
     expected_exact.receipt_chain_sha256 = exact.exact_surface_owner().receipt_chain_sha256.clone();
     assert_eq!(exact.exact_surface_owner(), &expected_exact);
-    let bytes=physical.accepted_publication_supports_canonical_bytes().unwrap();
-    let mut restored=FrozenLitterV3Resident::try_new(physical.lse_configuration().clone(),physical.lse_state().clone(),
-        physical.surface_configuration().clone(),physical.surface_owner().clone()).unwrap();
-    restored.restore_restart_authority(&bytes,None,physical.predecessor_receipt_chain_sha256()).unwrap();
-    assert_eq!(restored,physical);
-    let restored_exact=FrozenLitterV4Resident::try_restore(&restored,exact.exact_surface_owner().clone(),
-        exact.accepted_publication_supports_canonical_bytes(),&adoption).unwrap();
-    assert_eq!(restored_exact,exact);
-    let before=restored.clone();
-    assert!(restored.restore_restart_authority(&[bytes[1].clone()],None,
-        physical.predecessor_receipt_chain_sha256()).is_err());
-    assert_eq!(restored,before);
-    assert!(restored.restore_restart_authority(&[bytes[1].clone(),bytes[0].clone()],None,
-        physical.predecessor_receipt_chain_sha256()).is_err());
-    assert!(FrozenLitterV4Resident::try_restore(&restored,exact.exact_surface_owner().clone(),
-        &[bytes[0].clone()],&adoption).is_err());
+    let bytes = physical
+        .accepted_publication_supports_canonical_bytes()
+        .unwrap();
+    let mut restored = FrozenLitterV3Resident::try_new(
+        physical.lse_configuration().clone(),
+        physical.lse_state().clone(),
+        physical.surface_configuration().clone(),
+        physical.surface_owner().clone(),
+    )
+    .unwrap();
+    restored
+        .restore_restart_authority(&bytes, None, physical.predecessor_receipt_chain_sha256())
+        .unwrap();
+    assert_eq!(restored, physical);
+    let restored_exact = FrozenLitterV4Resident::try_restore(
+        &restored,
+        exact.exact_surface_owner().clone(),
+        exact.accepted_publication_supports_canonical_bytes(),
+        &adoption,
+    )
+    .unwrap();
+    assert_eq!(restored_exact, exact);
+    let before = restored.clone();
+    assert!(
+        restored
+            .restore_restart_authority(
+                &[bytes[1].clone()],
+                None,
+                physical.predecessor_receipt_chain_sha256()
+            )
+            .is_err()
+    );
+    assert_eq!(restored, before);
+    assert!(
+        restored
+            .restore_restart_authority(
+                &[bytes[1].clone(), bytes[0].clone()],
+                None,
+                physical.predecessor_receipt_chain_sha256()
+            )
+            .is_err()
+    );
+    assert!(
+        FrozenLitterV4Resident::try_restore(
+            &restored,
+            exact.exact_surface_owner().clone(),
+            &[bytes[0].clone()],
+            &adoption
+        )
+        .is_err()
+    );
 }
 
 #[test]
 fn native_donor_summary_keeps_vector_seal_and_receipt_sorted_arithmetic() {
     use super::native_snow_liquid_publication::positive_support_output_summary_v1;
     let (physical, exact) = v4_resident_fixture();
-    let template = native_drain_record_for_history_test(&physical, &exact, 0, 60_000_000_000, false).outputs[0].clone();
+    let template =
+        native_drain_record_for_history_test(&physical, &exact, 0, 60_000_000_000, false).outputs
+            [0]
+        .clone();
     let mut outputs = Vec::new();
-    for (index, mass) in [1.0, 2.0_f64.powi(-53), 2.0_f64.powi(-53)].into_iter().enumerate() {
+    for (index, mass) in [1.0, 2.0_f64.powi(-53), 2.0_f64.powi(-53)]
+        .into_iter()
+        .enumerate()
+    {
         let mut output = template.clone();
         output.lane_id = index as u32 + 1;
-        let (destinations, mass, energy) = super::v11_covered::physical_outcome_ledger::seal_destination_liquid_outcomes_v1(
-            &output.ofe_id, [(&output.destinations[0].tile_id, 1.0)], mass, mass * 4218.0).unwrap();
+        let (destinations, mass, energy) =
+            super::v11_covered::physical_outcome_ledger::seal_destination_liquid_outcomes_v1(
+                &output.ofe_id,
+                [(&output.destinations[0].tile_id, 1.0)],
+                mass,
+                mass * 4218.0,
+            )
+            .unwrap();
         output.destinations = destinations;
         output.mass_kg_m2_ofe_ground = mass;
         output.sensible_enthalpy_j_m2_ofe_ground = energy;
@@ -1665,12 +1798,18 @@
         output.validate().unwrap();
         outputs.push(output);
     }
-    let summarize = |values: &[super::Stage3AcceptedSnowLiquidOutputV1]| positive_support_output_summary_v1(values, 0, 60_000_000_000).unwrap();
+    let summarize = |values: &[super::Stage3AcceptedSnowLiquidOutputV1]| {
+        positive_support_output_summary_v1(values, 0, 60_000_000_000).unwrap()
+    };
     let original = summarize(&outputs);
     let mut sorted = outputs.clone();
     sorted.sort_by_key(|output| *output.receipt_sha256.as_bytes());
-    let expected_mass = sorted.iter().fold(0.0_f64, |sum, output| sum + output.mass_kg_m2_ofe_ground);
-    let expected_energy = sorted.iter().fold(0.0_f64, |sum, output| sum + output.sensible_enthalpy_j_m2_ofe_ground);
+    let expected_mass = sorted
+        .iter()
+        .fold(0.0_f64, |sum, output| sum + output.mass_kg_m2_ofe_ground);
+    let expected_energy = sorted.iter().fold(0.0_f64, |sum, output| {
+        sum + output.sensible_enthalpy_j_m2_ofe_ground
+    });
     assert_eq!(original.1.to_bits(), expected_mass.to_bits());
     assert_eq!(original.2.to_bits(), expected_energy.to_bits());
     outputs.reverse();
@@ -1679,12 +1818,23 @@
     assert_eq!(original.1.to_bits(), reversed.1.to_bits());
     assert_eq!(original.2.to_bits(), reversed.2.to_bits());
     assert_ne!(original.0, summarize(&outputs[..2]).0);
-    assert!(positive_support_output_summary_v1(&[outputs[0].clone(), outputs[0].clone()], 0, 60_000_000_000).is_err());
+    assert!(
+        positive_support_output_summary_v1(
+            &[outputs[0].clone(), outputs[0].clone()],
+            0,
+            60_000_000_000
+        )
+        .is_err()
+    );
     assert!(positive_support_output_summary_v1(&outputs, 60_000_000_000, 120_000_000_000).is_err());
     // A different vector sum is deliberately present; shared summary order,
     // not accidental equality for uniform lanes, establishes the context.
-    let large_first = [1.0_f64, 2.0_f64.powi(-53), 2.0_f64.powi(-53)].into_iter().fold(0.0, |a,b| a+b);
-    let large_last = [2.0_f64.powi(-53), 2.0_f64.powi(-53), 1.0_f64].into_iter().fold(0.0, |a,b| a+b);
+    let large_first = [1.0_f64, 2.0_f64.powi(-53), 2.0_f64.powi(-53)]
+        .into_iter()
+        .fold(0.0, |a, b| a + b);
+    let large_last = [2.0_f64.powi(-53), 2.0_f64.powi(-53), 1.0_f64]
+        .into_iter()
+        .fold(0.0, |a, b| a + b);
     assert_ne!(large_first.to_bits(), large_last.to_bits());
 }
 
@@ -1737,7 +1887,8 @@
             wb14_parent: physical.wb14_parent(),
             finalize_wb14_parent_interval: false,
             coupled_binding: DirectWb14CoupledChildBindingV1 {
-                parent_support_start_ns: 0, parent_support_end_ns: RUNTIME_SUPPORT_NS,
+                parent_support_start_ns: 0,
+                parent_support_end_ns: RUNTIME_SUPPORT_NS,
                 ..runtime_binding(support_start_ns, support_end_ns)
             },
             soil_beginning: FrozenLitterV3SoilBeginningV1::PublishableOwner {
@@ -1752,40 +1903,96 @@
 
 #[test]
 fn native_dirty_parent_survives_actual_partial_phase_and_rejects_foreign_drain() {
-    use super::native_snow_liquid_publication::{NativeSnowLiquidPublicationV1, NativeSnowCustodyKindV1};
-    use super::frozen_litter_v3_publication_retention::{FrozenLitterHistoryEntryV1, validate_native_history_v1};
+    use super::frozen_litter_v3_publication_retention::{
+        FrozenLitterHistoryEntryV1, validate_native_history_v1,
+    };
+    use super::native_snow_liquid_publication::{
+        NativeSnowCustodyKindV1, NativeSnowLiquidPublicationV1,
+    };
     let (mut physical, mut exact) = v4_resident_fixture();
-    physical.restore_restart_authority(&[], None, &digest('b')).unwrap();
-    let first_phase = execute_runtime_v4_partial_candidate(&physical, &exact, TRANSACTION, 0, 60_000_000_000);
+    physical
+        .restore_restart_authority(&[], None, &digest('b'))
+        .unwrap();
+    let first_phase =
+        execute_runtime_v4_partial_candidate(&physical, &exact, TRANSACTION, 0, 60_000_000_000);
     let before = physical.clone();
-    physical.accept_runtime_candidate(&first_phase.physical).unwrap();
-    exact.accept_runtime_candidate(&before, &first_phase).unwrap();
-    let make_drain = |physical: &FrozenLitterV3Resident, exact: &FrozenLitterV4Resident, start, end, parent| {
-        let template = native_drain_record_for_history_test(physical, exact, start, end, false);
-        let mut id = template.identity;
-        id.transaction_id = TRANSACTION;
-        id.parent_transaction_sha256 = parent;
-        id.receiver_context_sha256 = id.expected_receiver_context(&template.outputs).unwrap();
-        NativeSnowLiquidPublicationV1::try_new(physical.surface_configuration(), physical.lse_configuration(),
-            id, openwepp_coupled_time::Policy14RuntimeAuthority::V11SnowCovered, crate::land_surface_energy_shadow::endpoint_fixture().lse_state, physical.lse_state().clone(), physical.surface_owner(), exact.exact_surface_owner().clone(), template.outputs).unwrap()
-    };
+    physical
+        .accept_runtime_candidate(&first_phase.physical)
+        .unwrap();
+    exact
+        .accept_runtime_candidate(&before, &first_phase)
+        .unwrap();
+    let make_drain =
+        |physical: &FrozenLitterV3Resident, exact: &FrozenLitterV4Resident, start, end, parent| {
+            let template = native_drain_record_for_history_test(physical, exact, start, end, false);
+            let mut id = template.identity;
+            id.transaction_id = TRANSACTION;
+            id.parent_transaction_sha256 = parent;
+            id.receiver_context_sha256 = id.expected_receiver_context(&template.outputs).unwrap();
+            NativeSnowLiquidPublicationV1::try_new(
+                physical.surface_configuration(),
+                physical.lse_configuration(),
+                id,
+                openwepp_coupled_time::Policy14RuntimeAuthority::V11SnowCovered,
+                crate::land_surface_energy_shadow::endpoint_fixture().lse_state,
+                physical.lse_state().clone(),
+                physical.surface_owner(),
+                exact.exact_surface_owner().clone(),
+                template.outputs,
+            )
+            .unwrap()
+        };
     let parent = Digest32::from_bytes([1; 32]);
     let drain = make_drain(&physical, &exact, 0, 60_000_000_000, parent);
-    physical.accept_native_snow_publication(drain.clone(), &drain.outputs).unwrap();
-    exact.accept_native_snow_publication(&physical, &drain).unwrap();
+    physical
+        .accept_native_snow_publication(drain.clone(), &drain.outputs)
+        .unwrap();
+    exact
+        .accept_native_snow_publication(&physical, &drain)
+        .unwrap();
     assert!(physical.native_dirty_parent().is_some());
-    let second_phase = execute_runtime_v4_partial_candidate(&physical, &exact, TRANSACTION, 60_000_000_000, 120_000_000_000);
+    let second_phase = execute_runtime_v4_partial_candidate(
+        &physical,
+        &exact,
+        TRANSACTION,
+        60_000_000_000,
+        120_000_000_000,
+    );
     let before = physical.clone();
-    physical.accept_runtime_candidate(&second_phase.physical).unwrap();
-    exact.accept_runtime_candidate(&before, &second_phase).unwrap();
-    assert_eq!(physical.native_dirty_parent(), Some((TRANSACTION, 0, RUNTIME_SUPPORT_NS, parent)));
-    let foreign = make_drain(&physical, &exact, 120_000_000_000, 180_000_000_000, Digest32::from_bytes([9; 32]));
+    physical
+        .accept_runtime_candidate(&second_phase.physical)
+        .unwrap();
+    exact
+        .accept_runtime_candidate(&before, &second_phase)
+        .unwrap();
+    assert_eq!(
+        physical.native_dirty_parent(),
+        Some((TRANSACTION, 0, RUNTIME_SUPPORT_NS, parent))
+    );
+    let foreign = make_drain(
+        &physical,
+        &exact,
+        120_000_000_000,
+        180_000_000_000,
+        Digest32::from_bytes([9; 32]),
+    );
     let before = physical.clone();
-    assert!(physical.accept_native_snow_publication(foreign.clone(), &foreign.outputs).is_err());
+    assert!(
+        physical
+            .accept_native_snow_publication(foreign.clone(), &foreign.outputs)
+            .is_err()
+    );
     assert_eq!(physical, before);
     let mut history = physical.accepted_history_entries().to_vec();
     history.push(FrozenLitterHistoryEntryV1::Custody(Box::new(foreign)));
-    assert!(validate_native_history_v1(&history, physical.surface_configuration(), physical.lse_configuration()).is_err());
+    assert!(
+        validate_native_history_v1(
+            &history,
+            physical.surface_configuration(),
+            physical.lse_configuration()
+        )
+        .is_err()
+    );
     // Custody-only endpoint tests the dirty-history obligation independently
     // of completing the staged physical WB14 parent in this local fixture.
     let mut id = drain.identity.clone();
@@ -1793,35 +2000,69 @@
     id.support_start_ns = RUNTIME_SUPPORT_NS - 60_000_000_000;
     id.support_end_ns = RUNTIME_SUPPORT_NS;
     id.output_set_sha256 = Digest32::zero();
-    id.predecessor_physical_receipt_chain_sha256 = physical.predecessor_receipt_chain_sha256().into();
+    id.predecessor_physical_receipt_chain_sha256 =
+        physical.predecessor_receipt_chain_sha256().into();
     id.receiver_context_sha256 = id.expected_receiver_context(&[]).unwrap();
-    let finalization = NativeSnowLiquidPublicationV1::try_new(physical.surface_configuration(), physical.lse_configuration(),
-        id, openwepp_coupled_time::Policy14RuntimeAuthority::V11SnowCovered, crate::land_surface_energy_shadow::endpoint_fixture().lse_state, physical.lse_state().clone(), physical.surface_owner(), exact.exact_surface_owner().clone(), vec![]).unwrap();
-    physical.accept_native_snow_publication(finalization.clone(), &[]).unwrap();
-    exact.accept_native_snow_publication(&physical, &finalization).unwrap();
+    let finalization = NativeSnowLiquidPublicationV1::try_new(
+        physical.surface_configuration(),
+        physical.lse_configuration(),
+        id,
+        openwepp_coupled_time::Policy14RuntimeAuthority::V11SnowCovered,
+        crate::land_surface_energy_shadow::endpoint_fixture().lse_state,
+        physical.lse_state().clone(),
+        physical.surface_owner(),
+        exact.exact_surface_owner().clone(),
+        vec![],
+    )
+    .unwrap();
+    physical
+        .accept_native_snow_publication(finalization.clone(), &[])
+        .unwrap();
+    exact
+        .accept_native_snow_publication(&physical, &finalization)
+        .unwrap();
     assert_eq!(physical.native_dirty_parent(), None);
 }
 
 #[test]
 fn native_composite_support_role_rejects_half_owner_and_stale_active() {
-    use super::{v11_support_lse_beginning, V11SupportLseRole, CoveredColumnAuthority};
-    openwepp_land_surface_energy::snow_accuracy_policy::Policy::B01.install().unwrap();
+    use super::{CoveredColumnAuthority, V11SupportLseRole, v11_support_lse_beginning};
+    openwepp_land_surface_energy::snow_accuracy_policy::Policy::B01
+        .install()
+        .unwrap();
     let (mut shadow, _) = super::tests::v10_shadow_fixture();
     let (physical, exact) = native_v4_resident_pair_fixture(&shadow);
-    shadow.install_frozen_litter_v4_resident(physical, exact).unwrap();
+    shadow
+        .install_frozen_litter_v4_resident(physical, exact)
+        .unwrap();
     shadow.inner.authority = CoveredColumnAuthority::V11SnowCovered;
     shadow.inner.lse_state.tiles[0].surface_enthalpy_j_m2_tile_ground += 2.0;
     shadow.inner.lse_state.tiles[0].surface_temperature_warm_start_k += 0.01;
     shadow.inner.lse_state.state_sha256 = shadow.inner.lse_state.canonical_sha256().unwrap();
     let bytes = shadow.canonical_v11_lse_owner_bytes().unwrap();
-    let (covered_config, covered) = v11_support_lse_beginning(&shadow, &bytes, V11SupportLseRole::Covered).unwrap();
-    let (native_config, native) = v11_support_lse_beginning(&shadow, &bytes, V11SupportLseRole::NativeSnowFree).unwrap();
+    let (covered_config, covered) =
+        v11_support_lse_beginning(&shadow, &bytes, V11SupportLseRole::Covered).unwrap();
+    let (native_config, native) =
+        v11_support_lse_beginning(&shadow, &bytes, V11SupportLseRole::NativeSnowFree).unwrap();
     assert_eq!(covered, &shadow.inner.lse_state);
     assert_eq!(covered_config, &shadow.inner.lse_configuration);
-    assert_eq!(native, &shadow.frozen_litter_v3.as_ref().unwrap().lse_state().0);
-    assert_eq!(native_config, shadow.frozen_litter_v3.as_ref().unwrap().lse_configuration());
+    assert_eq!(
+        native,
+        &shadow.frozen_litter_v3.as_ref().unwrap().lse_state().0
+    );
+    assert_eq!(
+        native_config,
+        shadow
+            .frozen_litter_v3
+            .as_ref()
+            .unwrap()
+            .lse_configuration()
+    );
     assert_ne!(covered.configuration_sha256, native.configuration_sha256);
-    assert_ne!(covered.tiles[0].surface_enthalpy_j_m2_tile_ground, native.tiles[0].surface_enthalpy_j_m2_tile_ground);
+    assert_ne!(
+        covered.tiles[0].surface_enthalpy_j_m2_tile_ground,
+        native.tiles[0].surface_enthalpy_j_m2_tile_ground
+    );
     let unchanged = shadow.clone();
     let mut stale = shadow.clone();
     stale.inner.lse_state.tiles[0].surface_enthalpy_j_m2_tile_ground += 1.0;
@@ -1831,81 +2072,158 @@
     foreign.inner.lse_state.configuration_sha256 = Sha256Digest::try_new("f".repeat(64)).unwrap();
     foreign.inner.lse_state.state_sha256 = foreign.inner.lse_state.canonical_sha256().unwrap();
     assert!(foreign.canonical_v11_lse_owner_bytes().is_err());
-    let mut half = shadow.clone(); half.frozen_litter_v4 = None;
+    let mut half = shadow.clone();
+    half.frozen_litter_v4 = None;
     assert!(half.canonical_v11_lse_owner_bytes().is_err());
     let single = shadow.canonical_native_lse_owner_bytes().unwrap();
-    assert!(v11_support_lse_beginning(&shadow, &single, V11SupportLseRole::NativeSnowFree).is_err());
+    assert!(
+        v11_support_lse_beginning(&shadow, &single, V11SupportLseRole::NativeSnowFree).is_err()
+    );
     assert_eq!(shadow, unchanged);
 }
-
 
 #[test]
 fn native_restart_full_exact_adoption_rejects_resealed_initial_carry() {
+    use openwepp_land_surface_energy::ExactDyadicEnthalpy;
     use sha2::{Digest as _, Sha256};
-    use openwepp_land_surface_energy::ExactDyadicEnthalpy;
     let (physical, exact) = v4_resident_fixture();
     let authentic = exact.exact_surface_owner().clone();
-    exact.restart_authority_validate_native_exact_adoption(&physical, &authentic).unwrap();
+    exact
+        .restart_authority_validate_native_exact_adoption(&physical, &authentic)
+        .unwrap();
     let mut forged = authentic.clone();
-    forged.records[0].enthalpy_carry = ExactDyadicEnthalpy::exact_product_binary64(
-        &[2.0_f64.powi(-1000), 2.0_f64.powi(-100)]).unwrap();
+    forged.records[0].enthalpy_carry =
+        ExactDyadicEnthalpy::exact_product_binary64(&[2.0_f64.powi(-1000), 2.0_f64.powi(-100)])
+            .unwrap();
     let mut preimage = forged.clone();
     preimage.state_sha256 = Sha256Digest::try_new("0".repeat(64)).unwrap();
     preimage.receipt_chain_sha256 = Sha256Digest::try_new("0".repeat(64)).unwrap();
-    forged.state_sha256 = Sha256Digest::try_new(format!("{:x}", Sha256::digest(serde_json::to_vec(&preimage).unwrap()))).unwrap();
-    assert_eq!(forged.frozen_lse_v3_state_sha256, authentic.frozen_lse_v3_state_sha256);
-    assert_eq!(forged.frozen_surface_owner_v2_sha256, authentic.frozen_surface_owner_v2_sha256);
+    forged.state_sha256 = Sha256Digest::try_new(format!(
+        "{:x}",
+        Sha256::digest(serde_json::to_vec(&preimage).unwrap())
+    ))
+    .unwrap();
+    assert_eq!(
+        forged.frozen_lse_v3_state_sha256,
+        authentic.frozen_lse_v3_state_sha256
+    );
+    assert_eq!(
+        forged.frozen_surface_owner_v2_sha256,
+        authentic.frozen_surface_owner_v2_sha256
+    );
     let mut forged_exact = FrozenLitterV4Resident::try_new(&physical, forged).unwrap();
-    assert!(forged_exact.restart_authority_validate_native_exact_adoption(&physical, &authentic).is_err());
+    assert!(
+        forged_exact
+            .restart_authority_validate_native_exact_adoption(&physical, &authentic)
+            .is_err()
+    );
     let mut after = physical.clone();
-    let drain = native_drain_record_for_history_test(&after, &forged_exact, 0, 60_000_000_000, false);
-    after.accept_native_snow_publication(drain.clone(), &drain.outputs).unwrap();
-    forged_exact.accept_native_snow_publication(&after, &drain).unwrap();
-    let reconstructed = FrozenLitterV4Resident::try_restore(&after, forged_exact.exact_surface_owner().clone(),
-        forged_exact.accepted_publication_supports_canonical_bytes(), &authentic.frozen_lse_v3_state_sha256).unwrap();
-    assert!(reconstructed.restart_authority_validate_native_exact_adoption(&after, &authentic).is_err());
+    let drain =
+        native_drain_record_for_history_test(&after, &forged_exact, 0, 60_000_000_000, false);
+    after
+        .accept_native_snow_publication(drain.clone(), &drain.outputs)
+        .unwrap();
+    forged_exact
+        .accept_native_snow_publication(&after, &drain)
+        .unwrap();
+    let reconstructed = FrozenLitterV4Resident::try_restore(
+        &after,
+        forged_exact.exact_surface_owner().clone(),
+        forged_exact.accepted_publication_supports_canonical_bytes(),
+        &authentic.frozen_lse_v3_state_sha256,
+    )
+    .unwrap();
+    assert!(
+        reconstructed
+            .restart_authority_validate_native_exact_adoption(&after, &authentic)
+            .is_err()
+    );
     // Authentic phase-first history is admitted against the same external full owner.
     let mut phase_physical = physical.clone();
     let mut phase_exact = exact.clone();
-    let candidate = execute_runtime_v4_partial_candidate(&phase_physical, &phase_exact, TransactionId(703), 0, 60_000_000_000);
+    let candidate = execute_runtime_v4_partial_candidate(
+        &phase_physical,
+        &phase_exact,
+        TransactionId(703),
+        0,
+        60_000_000_000,
+    );
     let before = phase_physical.clone();
-    phase_physical.accept_runtime_candidate(&candidate.physical).unwrap();
-    phase_exact.accept_runtime_candidate(&before, &candidate).unwrap();
-    phase_exact.restart_authority_validate_native_exact_adoption(&phase_physical, &authentic).unwrap();
-    let mut foreign = authentic.clone(); foreign.owner_id = ResourceOwnerId::try_new("foreign-adoption").unwrap();
-    assert!(phase_exact.restart_authority_validate_native_exact_adoption(&phase_physical, &foreign).is_err());
+    phase_physical
+        .accept_runtime_candidate(&candidate.physical)
+        .unwrap();
+    phase_exact
+        .accept_runtime_candidate(&before, &candidate)
+        .unwrap();
+    phase_exact
+        .restart_authority_validate_native_exact_adoption(&phase_physical, &authentic)
+        .unwrap();
+    let mut foreign = authentic.clone();
+    foreign.owner_id = ResourceOwnerId::try_new("foreign-adoption").unwrap();
+    assert!(
+        phase_exact
+            .restart_authority_validate_native_exact_adoption(&phase_physical, &foreign)
+            .is_err()
+    );
     assert_eq!(exact.exact_surface_owner(), &authentic);
 }
 
 #[test]
 #[cfg(feature = "persisted-restart-v1")]
 fn native_routing_projection_preserves_clock_and_rejects_foreign_install_atomically() {
-    openwepp_land_surface_energy::snow_accuracy_policy::Policy::B01.install().unwrap();
+    openwepp_land_surface_energy::snow_accuracy_policy::Policy::B01
+        .install()
+        .unwrap();
     let (mut shadow, _) = super::tests::v10_shadow_fixture();
-    let lane = crate::DirectLanedActiveLaneConfig { slplen_m: 1.0, width_m: 1.0,
-        mean_gradient: 0.01, skin_friction_coefficient_ko: 500.0, form_drag_coefficient: 0.0,
-        roughness_element_height_m: 0.0, roughness_concentration: 0.0,
-        vegetation_drag_coefficient: 0.2, canopy_height_m: Some(0.2) };
+    let lane = crate::DirectLanedActiveLaneConfig {
+        slplen_m: 1.0,
+        width_m: 1.0,
+        mean_gradient: 0.01,
+        skin_friction_coefficient_ko: 500.0,
+        form_drag_coefficient: 0.0,
+        roughness_element_height_m: 0.0,
+        roughness_concentration: 0.0,
+        vegetation_drag_coefficient: 0.2,
+        canopy_height_m: Some(0.2),
+    };
     let config = Some(Box::new(crate::DirectLanedActiveConfig {
         lanes: vec![lane; shadow.hydrology_frame().identity.lane_count],
-        mesh_policy: crate::DirectLanedActiveMeshPolicy::production_default(), max_dt_s: 60.0,
-        trace_enabled: false, trace_detail_filter: None, step_trace_enabled: false }));
+        mesh_policy: crate::DirectLanedActiveMeshPolicy::production_default(),
+        max_dt_s: 60.0,
+        trace_enabled: false,
+        trace_detail_filter: None,
+        step_trace_enabled: false,
+    }));
     let summary = Some(Box::new(crate::DirectLanedActiveRunSummary {
-        max_dt_s: 60.0, days_seen: 3, days_routed: 2, total_source_m3: 1.25,
-        total_end_window_storage_m3: 0.125, total_tail_fold_m3: 0.25,
-        initial_groundwater_storage_m3: Some(7.0), terminal_groundwater_storage_m3: Some(6.0),
-        trace_records: Some(vec![]), ..Default::default() }));
+        max_dt_s: 60.0,
+        days_seen: 3,
+        days_routed: 2,
+        total_source_m3: 1.25,
+        total_end_window_storage_m3: 0.125,
+        total_tail_fold_m3: 0.25,
+        initial_groundwater_storage_m3: Some(7.0),
+        terminal_groundwater_storage_m3: Some(6.0),
+        trace_records: Some(vec![]),
+        ..Default::default()
+    }));
     shadow.inner.hydrology_frame.laned_active = config.clone();
     shadow.inner.hydrology_frame.laned_active_summary = summary.clone();
     let bytes = shadow.canonical_owner_state_bytes().unwrap();
     let mut base = shadow.restart_authority_native_without_routing().unwrap();
     assert_eq!(base.canonical_owner_state_bytes().unwrap(), bytes);
     let unchanged = base.clone();
-    assert!(base.restart_authority_install_native_routing(config.clone(), summary.clone(), &None).is_err());
+    assert!(
+        base.restart_authority_install_native_routing(config.clone(), summary.clone(), &None)
+            .is_err()
+    );
     assert_eq!(base, unchanged);
-    base.restart_authority_install_native_routing(config.clone(), summary.clone(), &config).unwrap();
+    base.restart_authority_install_native_routing(config.clone(), summary.clone(), &config)
+        .unwrap();
     assert_eq!(base, shadow);
-    assert!(base.restart_authority_install_native_routing(config.clone(), summary, &config).is_err());
+    assert!(
+        base.restart_authority_install_native_routing(config.clone(), summary, &config)
+            .is_err()
+    );
     assert_eq!(base, shadow);
 }
 
@@ -1916,65 +2234,122 @@
 #[test]
 fn native_restart_rejects_valid_initial_owner_reset_under_evolved_phase_history() {
     let (mut physical, mut exact) = v4_resident_fixture();
-    physical.restore_restart_authority(&[], None, &digest('b')).unwrap();
+    physical
+        .restore_restart_authority(&[], None, &digest('b'))
+        .unwrap();
     let initial_physical = physical.clone();
     let initial_exact = exact.clone();
     let adoption = physical.lse_state().0.state_sha256.clone();
-    assert!(initial_physical.surface_owner().v2_state().unwrap().continuations()
-        .iter().all(|c| c.next_interval_index == 0 && c.last_accepted_transaction_id.is_none()));
+    assert!(
+        initial_physical
+            .surface_owner()
+            .v2_state()
+            .unwrap()
+            .continuations()
+            .iter()
+            .all(|c| c.next_interval_index == 0 && c.last_accepted_transaction_id.is_none())
+    );
     let first = execute_runtime_v4_candidate(&physical, &exact, TRANSACTION, 0, RUNTIME_SUPPORT_NS);
     physical.accept_runtime_candidate(&first.physical).unwrap();
-    exact.accept_runtime_candidate(&initial_physical, &first).unwrap();
-    assert!(physical.surface_owner().v2_state().unwrap().continuations()
-        .iter().all(|c| c.next_interval_index == 1 && c.last_accepted_transaction_id.is_some()));
-    let physical_history = physical.accepted_publication_supports_canonical_bytes().unwrap();
+    exact
+        .accept_runtime_candidate(&initial_physical, &first)
+        .unwrap();
+    assert!(
+        physical
+            .surface_owner()
+            .v2_state()
+            .unwrap()
+            .continuations()
+            .iter()
+            .all(|c| c.next_interval_index == 1 && c.last_accepted_transaction_id.is_some())
+    );
+    let physical_history = physical
+        .accepted_publication_supports_canonical_bytes()
+        .unwrap();
     let exact_history = exact.accepted_publication_supports_canonical_bytes();
     let physical_before = v3_resident_canonical_bytes(&physical);
     let exact_before = exact.canonical_inactive_projection_v1().unwrap();
-    FrozenLitterV4Resident::try_restore(&physical, exact.exact_surface_owner().clone(),
-        exact_history, &adoption).unwrap(); // authentic positive control
+    FrozenLitterV4Resident::try_restore(
+        &physical,
+        exact.exact_surface_owner().clone(),
+        exact_history,
+        &adoption,
+    )
+    .unwrap(); // authentic positive control
     let mut reset = FrozenLitterV3Resident::try_new(
-        initial_physical.lse_configuration().clone(), initial_physical.lse_state().clone(),
-        initial_physical.surface_configuration().clone(), initial_physical.surface_owner().clone()).unwrap();
+        initial_physical.lse_configuration().clone(),
+        initial_physical.lse_state().clone(),
+        initial_physical.surface_configuration().clone(),
+        initial_physical.surface_owner().clone(),
+    )
+    .unwrap();
     // V3 source currently checks history chain but V4 pairs the actual final owner.
-    let parsed = reset.restore_restart_authority(&physical_history, None,
-        physical.predecessor_receipt_chain_sha256());
+    let parsed = reset.restore_restart_authority(
+        &physical_history,
+        None,
+        physical.predecessor_receipt_chain_sha256(),
+    );
     if parsed.is_ok() {
-        let rejected = FrozenLitterV4Resident::try_restore(&reset,
-            initial_exact.exact_surface_owner().clone(), exact_history, &adoption);
-        assert!(rejected.is_err(), "valid initial owners cannot replace the ending of evolved history");
+        let rejected = FrozenLitterV4Resident::try_restore(
+            &reset,
+            initial_exact.exact_surface_owner().clone(),
+            exact_history,
+            &adoption,
+        );
+        assert!(
+            rejected.is_err(),
+            "valid initial owners cannot replace the ending of evolved history"
+        );
     }
     assert_eq!(v3_resident_canonical_bytes(&physical), physical_before);
-    assert_eq!(exact.canonical_inactive_projection_v1().unwrap(), exact_before);
+    assert_eq!(
+        exact.canonical_inactive_projection_v1().unwrap(),
+        exact_before
+    );
 }
 
 #[cfg(feature = "persisted-restart-v1")]
 #[test]
 fn native_clock_mode_restore_preserves_exact_owner_and_refuses_foreign_inputs_atomically() {
-    use super::{v11_support_lse_beginning, V11SupportLseRole, CoveredColumnAuthority};
+    use super::{CoveredColumnAuthority, V11SupportLseRole, v11_support_lse_beginning};
     use openwepp_coupled_time::{Policy14LseOwnerTransport, Policy14RuntimeAuthority};
-    openwepp_land_surface_energy::snow_accuracy_policy::Policy::B01.install().unwrap();
+    openwepp_land_surface_energy::snow_accuracy_policy::Policy::B01
+        .install()
+        .unwrap();
     let (mut original, _) = super::tests::v10_shadow_fixture();
     let (physical, exact) = native_v4_resident_pair_fixture(&original);
-    original.install_frozen_litter_v4_resident(physical, exact).unwrap();
+    original
+        .install_frozen_litter_v4_resident(physical, exact)
+        .unwrap();
     original.inner.authority = CoveredColumnAuthority::V11SnowCovered;
     let expected = original.canonical_v11_lse_owner_bytes().unwrap();
     let mut restored = original.clone();
     restored.inner.authority = CoveredColumnAuthority::V10NonpositiveAssimilation;
     assert_ne!(restored.canonical_v11_lse_owner_bytes().unwrap(), expected);
     assert!(v11_support_lse_beginning(&restored, &expected, V11SupportLseRole::Covered).is_err());
-    restored.restart_authority_install_clock_runtime_mode(&expected).unwrap();
+    restored
+        .restart_authority_install_clock_runtime_mode(&expected)
+        .unwrap();
     assert_eq!(restored, original);
     assert_eq!(restored.canonical_v11_lse_owner_bytes().unwrap(), expected);
     for index in 0..4 {
-        let mut wire = serde_json::to_value(Policy14LseOwnerTransport::from_canonical_bytes(&expected).unwrap()).unwrap();
+        let mut wire = serde_json::to_value(
+            Policy14LseOwnerTransport::from_canonical_bytes(&expected).unwrap(),
+        )
+        .unwrap();
         match index {
-            0 => { wire.as_object_mut().unwrap().remove("runtime_authority"); }
+            0 => {
+                wire.as_object_mut().unwrap().remove("runtime_authority");
+            }
             1 => wire["runtime_authority"] = serde_json::json!("unknown"),
             2 => wire["runtime_authority"] = serde_json::json!("HistoricalV8"),
             _ => wire["active_covered_runtime_lse_v1"] = serde_json::json!([0]),
         }
-        assert!(restored.restart_authority_install_clock_runtime_mode(&serde_json::to_vec(&wire).unwrap()).is_err());
+        assert!(
+            restored
+                .restart_authority_install_clock_runtime_mode(&serde_json::to_vec(&wire).unwrap())
+                .is_err()
+        );
         assert_eq!(restored, original);
     }
     let mut mode_swap = Policy14LseOwnerTransport::from_canonical_bytes(&expected).unwrap();
--- a/crates/openwepp-hillslope-orchestrator/src/v9_real_consumer_shadow_publication_retention.rs
+++ b/crates/openwepp-hillslope-orchestrator/src/v9_real_consumer_shadow_publication_retention.rs
@@ -1729,6 +1729,8 @@
 ))]
 #[derive(Clone, Debug, PartialEq)]
 pub(crate) struct RestoredStage3PublicationDayEvidenceV1 {
+    pub(crate) first_positive_support: TimeSupport,
+    pub(crate) first_positive_beginning_owner: Digest32,
     pub(crate) canonical_bytes: Vec<u8>,
     pub(crate) day_index: usize,
     pub(crate) prefix_support_count: usize,
@@ -1787,9 +1789,6 @@
         .ok_or(DirectV11RealConsumerError::Identity(
             "archived publication day beginning",
         ))?;
-    let last = supports.last().ok_or(DirectV11RealConsumerError::Identity(
-        "archived publication day ending",
-    ))?;
     let beginning_owner_set_sha256 = wire
         .event_handoffs
         .first()
@@ -1803,9 +1802,11 @@
         );
     let mut restored = AcceptedPublicationHistoryV1::default();
     restored.replace(supports.clone(), &wire.event_handoffs)?;
+    // Accepted zero-duration event handoffs may follow the last positive-duration
+    // support at its endpoint. `replace` validates and replays every event, so
+    // its full-history tail is the effective archive ending owner.
     if restored.validate_cached_tail_against_full_scan()? != Some(wire.ending_owner_set_sha256)
         || beginning_owner_set_sha256 == Digest32::zero()
-        || last.ending_complete_owner_set_sha256 != wire.ending_owner_set_sha256
     {
         return Err(DirectV11RealConsumerError::Identity(
             "archived publication day chronology",
@@ -1822,6 +1823,8 @@
         ordered.extend_from_slice(event.id().digest().as_bytes());
     }
     Ok(RestoredStage3PublicationDayEvidenceV1 {
+        first_positive_support: first.support,
+        first_positive_beginning_owner: first.beginning_complete_owner_set_sha256,
         canonical_bytes: bytes.to_vec(),
         day_index: wire.day_index,
         prefix_support_count: wire.prefix_support_count,
@@ -2089,6 +2092,550 @@
 mod accepted_publication_chronology_tests {
     use super::*;
 
+    #[cfg(feature = "persisted-restart-v1")]
+    const HEADER: &[u8] = b"OPENWEPP_STAGE3_COMMITTED_DAY_EVIDENCE_V1\0";
+
+    #[cfg(feature = "persisted-restart-v1")]
+    fn archived_publication_wire_v1(
+        ordinal: u64,
+    ) -> Option<Stage3AcceptedPublicationDayEvidenceWireV1> {
+        let export = std::env::var_os("OPENWEPP_B01_MEMBER_EXPORT")?;
+        let record = std::fs::read(
+            std::path::Path::new(&export)
+                .join("rows")
+                .join(format!("{ordinal}.canonical_record.bin")),
+        )
+        .expect("immutable archived day record");
+        let mut cursor = HEADER.len() + 2 + 32 + 32 + 8 + 32;
+        let mut take_blob = || {
+            let length: [u8; 8] = record[cursor..cursor + 8]
+                .try_into()
+                .expect("archive blob length");
+            cursor += 8;
+            let end = cursor
+                .checked_add(usize::try_from(u64::from_be_bytes(length)).expect("archive size"))
+                .expect("archive blob end");
+            let value = &record[cursor..end];
+            cursor = end;
+            value
+        };
+        let _parent = take_blob();
+        let publication = take_blob();
+        Some(serde_json::from_slice(publication).expect("canonical publication wire"))
+    }
+
+    #[cfg(feature = "persisted-restart-v1")]
+    fn canonical_event_field_substitution(
+        wire: &[u8],
+        event: &AcceptedEventReceiptV1,
+        field: &str,
+        replacement: &str,
+    ) -> Vec<u8> {
+        // This retains the typed wire's field order and changes one semantic
+        // event field only. Rebuilding through `Value` would exercise the
+        // restorer's canonical-byte guard instead of its event guard.
+        let event_value = serde_json::to_value(event).expect("event wire value");
+        let receipt_id = event_value["receipt_id"]
+            .as_str()
+            .expect("event receipt id");
+        let prior = event_value[field].as_str().expect("event field value");
+        let mut text = String::from_utf8(wire.to_vec()).expect("canonical UTF-8 wire");
+        let receipt_marker = format!(r#""receipt_id":"{receipt_id}""#);
+        let receipt_offset = text.find(&receipt_marker).expect("unique event receipt");
+        assert_eq!(text.matches(&receipt_marker).count(), 1);
+        let event_start = text[..receipt_offset]
+            .rfind('{')
+            .expect("event object start");
+        let event_end = text[receipt_offset..]
+            .find('}')
+            .map(|offset| receipt_offset + offset)
+            .expect("event object end");
+        let field_marker = format!(r#""{field}":"{prior}""#);
+        let field_offset = text[event_start..event_end]
+            .find(&field_marker)
+            .map(|offset| event_start + offset)
+            .expect("event field in selected receipt");
+        assert_eq!(
+            text[event_start..event_end].matches(&field_marker).count(),
+            1
+        );
+        text.replace_range(
+            field_offset..field_offset + field_marker.len(),
+            &format!(r#""{field}":"{replacement}""#),
+        );
+        text.into_bytes()
+    }
+
+    #[cfg(feature = "persisted-restart-v1")]
+    fn resealed_event_field_substitution(
+        wire: &[u8],
+        event: &AcceptedEventReceiptV1,
+        field: &str,
+        replacement: &str,
+    ) -> Vec<u8> {
+        let mut text = String::from_utf8(canonical_event_field_substitution(
+            wire,
+            event,
+            field,
+            replacement,
+        ))
+        .expect("canonical UTF-8 resealed wire");
+        let event_value = serde_json::to_value(event).expect("event wire value");
+        let receipt_id = event_value["receipt_id"]
+            .as_str()
+            .expect("event receipt id");
+        let receipt_marker = format!(r#""receipt_id":"{receipt_id}""#);
+        let receipt_offset = text.find(&receipt_marker).expect("selected event receipt");
+        let event_start = text[..receipt_offset]
+            .rfind('{')
+            .expect("event object start");
+        let event_end = text[receipt_offset..]
+            .find('}')
+            .map(|offset| receipt_offset + offset)
+            .expect("event object end");
+        let value: serde_json::Value = serde_json::from_str(&text[event_start..=event_end])
+            .expect("selected canonical event object");
+        let event_id = substituted_event_id(&value);
+        let (end_clock, receipt_id) = substituted_event_receipt(&value, event_id);
+        let replacement = |digest: Digest32| {
+            serde_json::to_value(digest)
+                .expect("canonical digest JSON")
+                .as_str()
+                .expect("canonical digest text")
+                .to_owned()
+        };
+        for (name, old, new) in [
+            (
+                "event_id",
+                value["event_id"].as_str().expect("old event id"),
+                replacement(event_id),
+            ),
+            (
+                "end_clock",
+                value["end_clock"].as_str().expect("old end clock"),
+                replacement(end_clock),
+            ),
+            (
+                "receipt_id",
+                value["receipt_id"].as_str().expect("old receipt id"),
+                replacement(receipt_id),
+            ),
+        ] {
+            let old_marker = format!(r#""{name}":"{old}""#);
+            let offset = text[event_start..event_end]
+                .find(&old_marker)
+                .map(|offset| event_start + offset)
+                .expect("selected event identity field");
+            text.replace_range(
+                offset..offset + old_marker.len(),
+                &format!(r#""{name}":"{new}""#),
+            );
+        }
+        text.into_bytes()
+    }
+
+    #[cfg(feature = "persisted-restart-v1")]
+    fn substituted_event_id(value: &serde_json::Value) -> Digest32 {
+        let digest = |name: &str| {
+            serde_json::from_value::<Digest32>(value[name].clone()).expect("selected event digest")
+        };
+        let parent: ParentTransactionId =
+            serde_json::from_value(value["parent_transaction_id"].clone())
+                .expect("selected event parent");
+        let tick = value["tick"]
+            .as_str()
+            .expect("selected event tick")
+            .parse::<u128>()
+            .expect("selected event tick width");
+        let ordinal = u32::try_from(value["ordinal"].as_u64().expect("selected event ordinal"))
+            .expect("selected event ordinal width");
+        let class = match value["class"].as_str().expect("selected event class") {
+            "OwnershipTransfer" => "OwnershipTransfer",
+            "BoundaryModeTransition" => "BoundaryModeTransition",
+            "RegimeTransition" => "RegimeTransition",
+            "ScheduledBoundary" => "ScheduledBoundary",
+            "DiagnosticMarker" => "DiagnosticMarker",
+            _ => panic!("selected event class is canonical"),
+        };
+        let source = value["source_owner_id"]
+            .as_str()
+            .expect("selected event source");
+        let context = digest("event_context_digest");
+        let tick_bytes = tick.to_be_bytes();
+        let ordinal_bytes = ordinal.to_be_bytes();
+        framed_sha256(
+            "event",
+            &[
+                FramedField {
+                    tag: "parent_transaction_id",
+                    value: parent.digest().as_bytes(),
+                },
+                FramedField {
+                    tag: "tick_ns",
+                    value: &tick_bytes,
+                },
+                FramedField {
+                    tag: "event_class",
+                    value: class.as_bytes(),
+                },
+                FramedField {
+                    tag: "event_ordinal",
+                    value: &ordinal_bytes,
+                },
+                FramedField {
+                    tag: "source_owner_id",
+                    value: source.as_bytes(),
+                },
+                FramedField {
+                    tag: "event_context",
+                    value: context.as_bytes(),
+                },
+            ],
+        )
+        .expect("canonical event id")
+    }
+
+    #[cfg(feature = "persisted-restart-v1")]
+    fn substituted_event_receipt(
+        value: &serde_json::Value,
+        event_id: Digest32,
+    ) -> (Digest32, Digest32) {
+        let digest = |name: &str| {
+            serde_json::from_value::<Digest32>(value[name].clone()).expect("selected event digest")
+        };
+        let parent: ParentTransactionId =
+            serde_json::from_value(value["parent_transaction_id"].clone())
+                .expect("selected event parent");
+        let tick = value["tick"]
+            .as_str()
+            .expect("selected event tick")
+            .parse::<u128>()
+            .expect("selected event tick width");
+        let ordinal = u32::try_from(value["ordinal"].as_u64().expect("selected event ordinal"))
+            .expect("selected event ordinal width");
+        let tick_bytes = tick.to_be_bytes();
+        let ordinal_bytes = ordinal.to_be_bytes();
+        let context = digest("event_context_digest");
+        let begin_clock = digest("begin_clock");
+        let end_owner = digest("end_owner_set");
+        let end_clock = digest_bytes(
+            &[
+                &begin_clock.as_bytes()[..],
+                &event_id.as_bytes()[..],
+                &end_owner.as_bytes()[..],
+            ]
+            .concat(),
+        );
+        let begin_owner = digest("begin_owner_set");
+        let ledger = digest("ledger_digest");
+        let receipt_id = framed_sha256(
+            "event-receipt-v2",
+            &[
+                FramedField {
+                    tag: "parent_transaction_id",
+                    value: parent.digest().as_bytes(),
+                },
+                FramedField {
+                    tag: "event_id",
+                    value: event_id.as_bytes(),
+                },
+                FramedField {
+                    tag: "tick_ns",
+                    value: &tick_bytes,
+                },
+                FramedField {
+                    tag: "ordinal",
+                    value: &ordinal_bytes,
+                },
+                FramedField {
+                    tag: "begin_clock",
+                    value: begin_clock.as_bytes(),
+                },
+                FramedField {
+                    tag: "end_clock",
+                    value: end_clock.as_bytes(),
+                },
+                FramedField {
+                    tag: "begin_owner_set",
+                    value: begin_owner.as_bytes(),
+                },
+                FramedField {
+                    tag: "end_owner_set",
+                    value: end_owner.as_bytes(),
+                },
+                FramedField {
+                    tag: "event_context",
+                    value: context.as_bytes(),
+                },
+                FramedField {
+                    tag: "ledger_digest",
+                    value: ledger.as_bytes(),
+                },
+            ],
+        )
+        .expect("canonical event receipt id");
+        (end_clock, receipt_id)
+    }
+
+    #[cfg(feature = "persisted-restart-v1")]
+    fn assert_resealed_event_is_valid(wire: &[u8], event_index: usize) {
+        let wire: Stage3AcceptedPublicationDayEvidenceWireV1 =
+            serde_json::from_slice(wire).expect("resealed canonical typed wire");
+        wire.event_handoffs[event_index]
+            .validate()
+            .expect("resealed event receipt validates before chronology replay");
+    }
+
+    #[cfg(feature = "persisted-restart-v1")]
+    fn assert_archive_identity(
+        result: &Result<RestoredStage3PublicationDayEvidenceV1, DirectV11RealConsumerError>,
+        expected: &'static str,
+    ) {
+        match result {
+            Err(DirectV11RealConsumerError::Identity(actual)) => {
+                println!("archive refusal: {actual}");
+                assert_eq!(*actual, expected);
+            }
+            Err(error) => panic!("expected archive identity {expected}; got {error}"),
+            Ok(_) => panic!("expected archive identity {expected}; archive accepted"),
+        }
+    }
+
+    #[test]
+    #[cfg(feature = "persisted-restart-v1")]
+    fn archived_restorer_uses_replayed_terminal_event_tail_and_rejects_wire_poisons() {
+        let _native_audit =
+            crate::v9_real_consumer_shadow::begin_covered_native_physical_path_audit_v1();
+        let _probe_audit = crate::snow_stage3_v11_current_context_capture::member_backed_restore_tests::ProbeAudit::begin();
+        let one_event_wire = archived_publication_wire_v1(27_431)
+            .expect("bound immutable one-terminal-event archive");
+        assert_eq!(one_event_wire.event_handoffs.len(), 49);
+        let one = serde_json::to_vec(&one_event_wire).expect("canonical one-event-tail wire");
+        let one_restored = restart_authority_restore_archived_publication_day_v1(&one)
+            .expect("replayed one terminal event is the archive ending");
+        assert_ne!(
+            one_event_wire
+                .supports
+                .last()
+                .expect("day has supports")
+                .ending_complete_owner_set_sha256,
+            one_restored.ending_owner_set_sha256,
+        );
+
+        let multiple_event_wire =
+            archived_publication_wire_v1(54_078).expect("immutable multi-event archived day");
+        let multiple =
+            serde_json::to_vec(&multiple_event_wire).expect("canonical multiple-event wire");
+        let restored = restart_authority_restore_archived_publication_day_v1(&multiple)
+            .expect("replayed same-tick terminal events are the archive ending");
+        let multiple_wire: Stage3AcceptedPublicationDayEvidenceWireV1 =
+            serde_json::from_slice(&multiple).expect("sealed multiple-event wire");
+        assert_eq!(
+            serde_json::to_vec(&multiple_wire).expect("recanonicalized multiple-event wire"),
+            multiple,
+            "fixture must reach semantic guards through canonical serialization",
+        );
+        assert!(multiple_wire.event_handoffs.len() > one_event_wire.event_handoffs.len());
+        let last_support = multiple_wire.supports.last().expect("day has supports");
+        assert_ne!(
+            last_support.ending_complete_owner_set_sha256,
+            restored.ending_owner_set_sha256,
+        );
+
+        check_no_terminal_event(&one_event_wire);
+        check_terminal_event_substitutions(&multiple_wire, &multiple);
+        check_structural_archive_poisons(&multiple_wire);
+        let mut noncanonical = one;
+        noncanonical.push(b'\n');
+        assert_archive_identity(
+            &restart_authority_restore_archived_publication_day_v1(&noncanonical),
+            "archived publication day wire",
+        );
+    }
+
+    #[cfg(feature = "persisted-restart-v1")]
+    fn check_no_terminal_event(one_event_wire: &Stage3AcceptedPublicationDayEvidenceWireV1) {
+        // Day-zero has no sealed prefix. Rebuild the no-terminal fixture's
+        // complete authority root through the canonical history owner, while
+        // retaining all internal events including its event at tick zero.
+        let mut no_terminal = one_event_wire.clone();
+        let no_terminal_last = no_terminal
+            .supports
+            .last()
+            .expect("day-zero last support")
+            .clone();
+        let original_count = no_terminal.event_handoffs.len();
+        no_terminal
+            .event_handoffs
+            .retain(|event| event.tick() != no_terminal_last.support.end_ns());
+        let removed = original_count - no_terminal.event_handoffs.len();
+        assert_eq!(removed, 1, "day-zero has one terminal event");
+        no_terminal.cumulative_event_count -= removed;
+        no_terminal.ending_owner_set_sha256 = no_terminal_last.ending_complete_owner_set_sha256;
+        let mut no_terminal_history = AcceptedPublicationHistoryV1::default();
+        no_terminal_history
+            .replace(
+                no_terminal
+                    .supports
+                    .iter()
+                    .cloned()
+                    .map(Stage3AcceptedPublicationSupportV1::try_from_wire)
+                    .collect::<Result<Vec<_>, _>>()
+                    .expect("canonical no-terminal supports"),
+                &no_terminal.event_handoffs,
+            )
+            .expect("canonical no-terminal history");
+        no_terminal.cumulative_authority_sha256 = no_terminal_history
+            .inner
+            .tail_authority
+            .aggregate_authority_sha256;
+        let no_terminal = serde_json::to_vec(&no_terminal).expect("canonical no-terminal wire");
+        assert_eq!(
+            restart_authority_restore_archived_publication_day_v1(&no_terminal)
+                .expect("no terminal event preserves final support endpoint")
+                .ending_owner_set_sha256,
+            no_terminal_last.ending_complete_owner_set_sha256,
+        );
+    }
+
+    #[cfg(feature = "persisted-restart-v1")]
+    fn check_terminal_event_substitutions(
+        multiple_wire: &Stage3AcceptedPublicationDayEvidenceWireV1,
+        multiple: &[u8],
+    ) {
+        let final_tick = multiple_wire
+            .supports
+            .last()
+            .expect("last support")
+            .support
+            .end_ns();
+        let terminal_indexes = multiple_wire
+            .event_handoffs
+            .iter()
+            .enumerate()
+            .filter_map(|(index, event)| (event.tick() == final_tick).then_some(index))
+            .collect::<Vec<_>>();
+        let [first_terminal, second_terminal] = terminal_indexes.as_slice() else {
+            panic!("exactly two terminal events");
+        };
+        let first_event = &multiple_wire.event_handoffs[*first_terminal];
+        let second_event = &multiple_wire.event_handoffs[*second_terminal];
+        let mut reordered = multiple_wire.clone();
+        reordered
+            .event_handoffs
+            .swap(*first_terminal, *second_terminal);
+        assert_archive_identity(
+            &restart_authority_restore_archived_publication_day_v1(
+                &serde_json::to_vec(&reordered).expect("canonical reordered wire"),
+            ),
+            "accepted publication incremental event chronology",
+        );
+        let foreign = multiple_wire
+            .event_handoffs
+            .iter()
+            .find(|event| event.parent_transaction_id() != first_event.parent_transaction_id())
+            .expect("foreign parent");
+        let foreign_value = serde_json::to_value(foreign).expect("foreign event value");
+        let second_value = serde_json::to_value(second_event).expect("second event value");
+        let fields = [
+            (
+                "tick",
+                (final_tick.get() + 1).to_string(),
+                "accepted publication incremental restore chronology",
+                "accepted publication incremental restore chronology",
+            ),
+            (
+                "parent_transaction_id",
+                foreign_value["parent_transaction_id"]
+                    .as_str()
+                    .expect("parent")
+                    .to_owned(),
+                "accepted publication incremental event seal",
+                "accepted publication incremental event chronology",
+            ),
+            (
+                "begin_owner_set",
+                second_value["begin_owner_set"]
+                    .as_str()
+                    .expect("beginning owner")
+                    .to_owned(),
+                "accepted publication incremental event seal",
+                "accepted publication incremental event chronology",
+            ),
+        ];
+        for resealed in [false, true] {
+            for (field, replacement, raw_expected, sealed_expected) in &fields {
+                let wire = if resealed {
+                    resealed_event_field_substitution(multiple, first_event, field, replacement)
+                } else {
+                    canonical_event_field_substitution(multiple, first_event, field, replacement)
+                };
+                if resealed {
+                    assert_resealed_event_is_valid(&wire, *first_terminal);
+                }
+                println!("terminal event substitution: {field}; independently sealed={resealed}");
+                assert_archive_identity(
+                    &restart_authority_restore_archived_publication_day_v1(&wire),
+                    if resealed {
+                        sealed_expected
+                    } else {
+                        raw_expected
+                    },
+                );
+            }
+        }
+    }
+
+    #[cfg(feature = "persisted-restart-v1")]
+    fn check_structural_archive_poisons(
+        multiple_wire: &Stage3AcceptedPublicationDayEvidenceWireV1,
+    ) {
+        let mut omitted = multiple_wire.clone();
+        omitted.event_handoffs.remove(0);
+        omitted.cumulative_event_count -= 1;
+        assert_archive_identity(
+            &restart_authority_restore_archived_publication_day_v1(
+                &serde_json::to_vec(&omitted).expect("canonical omitted-event wire"),
+            ),
+            "accepted publication incremental owner handoff",
+        );
+        let mut duplicated = multiple_wire.clone();
+        duplicated
+            .event_handoffs
+            .push(duplicated.event_handoffs[0].clone());
+        duplicated.cumulative_event_count += 1;
+        assert_archive_identity(
+            &restart_authority_restore_archived_publication_day_v1(
+                &serde_json::to_vec(&duplicated).expect("canonical duplicated-event wire"),
+            ),
+            "accepted publication incremental restore chronology",
+        );
+        let mut wrong_final = multiple_wire.clone();
+        wrong_final.ending_owner_set_sha256 = Digest32::zero();
+        assert_archive_identity(
+            &restart_authority_restore_archived_publication_day_v1(
+                &serde_json::to_vec(&wrong_final).expect("canonical wrong-final-owner wire"),
+            ),
+            "archived publication day chronology",
+        );
+        let mut wrong_schema = multiple_wire.clone();
+        wrong_schema.schema_version = 2;
+        assert_archive_identity(
+            &restart_authority_restore_archived_publication_day_v1(
+                &serde_json::to_vec(&wrong_schema).expect("canonical wrong-schema wire"),
+            ),
+            "archived publication day wire",
+        );
+        let mut wrong_count = multiple_wire.clone();
+        wrong_count.cumulative_event_count += 1;
+        assert_archive_identity(
+            &restart_authority_restore_archived_publication_day_v1(
+                &serde_json::to_vec(&wrong_count).expect("canonical wrong-count wire"),
+            ),
+            "archived publication day wire",
+        );
+    }
+
     fn support(
         interval_index: usize,
         parent: u8,
@@ -2178,6 +2725,52 @@
             )
             .is_err(),
             "genesis event ordinal substitution",
+        );
+    }
+
+    #[test]
+    fn terminal_event_tail_is_the_effective_ending_and_chronology_poisons_reject() {
+        // SC-COUPLEDTIME-001 sections 6, 8, and 9 admit a zero-duration
+        // handoff at a positive support endpoint.  The replayed event tail,
+        // rather than that support's local ending owner, is therefore the
+        // effective archive endpoint.
+        let supports = [support(22, 7, 0, 10, 1, 2)];
+        assert_eq!(
+            validate_accepted_publication_chronology_v1(&supports, &[])
+                .expect("no terminal event preserves support endpoint"),
+            Some(Digest32::from_bytes([2; 32])),
+        );
+        let one = [event(7, 10, 0, 2, 3)];
+        assert_eq!(
+            validate_accepted_publication_chronology_v1(&supports, &one)
+                .expect("one terminal event advances archive endpoint"),
+            Some(Digest32::from_bytes([3; 32])),
+        );
+        let multiple = [event(7, 10, 0, 2, 3), event(7, 10, 1, 3, 4)];
+        assert_eq!(
+            validate_accepted_publication_chronology_v1(&supports, &multiple)
+                .expect("multiple terminal events advance archive endpoint in order"),
+            Some(Digest32::from_bytes([4; 32])),
+        );
+        assert!(
+            validate_accepted_publication_chronology_v1(&supports, &[event(7, 10, 1, 2, 3)],)
+                .is_err(),
+            "wrong terminal ordinal",
+        );
+        assert!(
+            validate_accepted_publication_chronology_v1(&supports, &[event(7, 11, 0, 2, 3)],)
+                .is_err(),
+            "wrong terminal tick",
+        );
+        assert!(
+            validate_accepted_publication_chronology_v1(&supports, &[event(8, 10, 0, 2, 3)],)
+                .is_err(),
+            "foreign terminal parent",
+        );
+        assert!(
+            validate_accepted_publication_chronology_v1(&supports, &[multiple[1], multiple[0]])
+                .is_err(),
+            "reordered terminal events",
         );
     }
 
--- a/crates/openwepp-hillslope-orchestrator/src/v9_real_consumer_shadow_tests.rs
+++ b/crates/openwepp-hillslope-orchestrator/src/v9_real_consumer_shadow_tests.rs
@@ -1688,6 +1688,14 @@
         );
     }
 
+    fn assert_no_snow_free_physics_calls(
+        audit: &crate::v9_real_consumer_shadow::CoveredNativePhysicalPathAuditV1,
+    ) {
+        assert_eq!(audit.snow_free_litter_physics_call_count, 0);
+        assert_eq!(audit.snow_free_surface_physics_call_count, 0);
+        assert_eq!(audit.snow_free_wb14_physics_call_count, 0);
+    }
+
     #[test]
     fn b01_current_context_importer_restores_fixture_without_physics() {
         // The capture records native V2 custody.  Build it through the
@@ -1776,6 +1784,8 @@
         let beginning_stage3 = BTreeMap::new();
         let row = crate::snow_stage3_v11_current_context_capture::capture_row_for_test(
             &crate::snow_stage3_v11_current_context_capture::PreChildContext {
+                phase:
+                    crate::snow_stage3_v11_current_context_capture::PreChildCapturePhaseV1::Covered,
                 context: &context,
                 parent: &parent,
                 consumer: &shadow,
@@ -1802,9 +1812,7 @@
         )
         .expect("composed context import");
         let audit = crate::v9_real_consumer_shadow::take_covered_native_physical_path_audit_v1();
-        assert_eq!(audit.snow_free_litter_physics_call_count, 0);
-        assert_eq!(audit.snow_free_surface_physics_call_count, 0);
-        assert_eq!(audit.snow_free_wb14_physics_call_count, 0);
+        assert_no_snow_free_physics_calls(&audit);
         let mut missing_lse = row.clone();
         missing_lse
             .as_object_mut()
--- a/crates/openwepp-hillslope-orchestrator/src/v9_real_consumer_shadow_wb14_tests.rs
+++ b/crates/openwepp-hillslope-orchestrator/src/v9_real_consumer_shadow_wb14_tests.rs
@@ -24,23 +24,222 @@
     REAL_NATIVE_INACTIVE_PREFIX_V1.with(|fixture| fixture.borrow_mut().take())
 }
 
+pub(super) struct CompleteWb14CadenceFixtureResultV1 {
+    pub fixture_inputs: serde_json::Value,
+    pub beginning_parent_checkpoint_canonical_json: Vec<u8>,
+    pub beginning_complete_owner_canonical_bytes: BTreeMap<String, Vec<u8>>,
+    pub beginning_clock_canonical_json: Vec<u8>,
+    pub beginning_stage3_owner_canonical_bytes: Vec<u8>,
+    pub beginning_surface_configuration_canonical_bytes: Vec<u8>,
+    pub beginning_stage3_lane_count: usize,
+    pub beginning_native_v3_resident_present: bool,
+    pub beginning_native_v4_resident_present: bool,
+    pub ending_parent_checkpoint_canonical_json: Vec<u8>,
+    pub finalized_accepted_segment_count: usize,
+    pub finalized_parent_canonical_projection: serde_json::Value,
+    pub ending_complete_owner_canonical_bytes: BTreeMap<String, Vec<u8>>,
+    pub ending_clock_canonical_json: Vec<u8>,
+    pub ending_clock_accepted_until: ModelTimeNs,
+    pub ending_stage3_owner_canonical_bytes: Vec<u8>,
+    pub subslabs: Vec<crate::snow_stage3_v11_attachment::Stage3CoupledSubslabReceiptV1>,
+    pub subslab_liquid_custody_canonical_json: Vec<Option<Vec<u8>>>,
+    pub event_groups: Vec<crate::snow_stage3_v11_attachment::Stage3V11TerminalEventGroupV1>,
+    pub event_group_terminal_custody_canonical_json: Vec<Option<Vec<u8>>>,
+    pub terminal_parcels: Vec<crate::snow_stage3_v11_attachment::DirectSnowStage3V11TerminalParcel>,
+    pub snow_free_successor_receipts:
+        Vec<crate::snow_stage3_v11_attachment::Stage3SnowFreeSuccessorReceiptV1>,
+}
+
+fn v11_parent_candidate_canonical_projection(candidate: &V11ParentCandidate) -> serde_json::Value {
+    serde_json::json!({
+        "parent_transaction_id": candidate.parent_transaction_id,
+        "beginning_state_sha256": candidate.beginning_state_sha256,
+        "ending_state": candidate.ending_state,
+        "accepted_segments": candidate.accepted_segments.iter().map(|segment| {
+            serde_json::json!({
+                "accepted_slab_receipt": segment.accepted_slab_receipt,
+                "lse_support_receipt": segment.lse_support_receipt,
+                "beginning_state_sha256": segment.beginning_state_sha256,
+                "ending_state": segment.ending_state,
+                "resource_debits": segment.resource_debits,
+                "admitted_resource_fluxes": segment.admitted_resource_fluxes,
+                "shared_resource_transitions": segment.shared_resource_transitions,
+                "complete_owner_candidates": segment.complete_owner_candidates,
+                "material_transfers": segment.material_transfers,
+                "ending_resource_owners": segment.ending_resource_owners,
+            })
+        }).collect::<Vec<_>>(),
+        "accepted_segment_checkpoints": candidate.accepted_segment_checkpoints,
+        "cumulative_debits": candidate.cumulative_debits.iter().map(
+            |((owner_id, resource_key), value)| {
+                serde_json::json!({
+                    "owner_id": owner_id,
+                    "resource_key": resource_key,
+                    "value_bits": value.to_bits(),
+                })
+            }
+        ).collect::<Vec<_>>(),
+        "material_transfers": candidate.material_transfers,
+        "beginning_complete_owners": candidate.beginning_complete_owners,
+        "ending_complete_owners": candidate.ending_complete_owners,
+    })
+}
+
+impl CompleteWb14CadenceFixtureResultV1 {
+    pub(super) fn canonical_comparison_payload(&self) -> serde_json::Value {
+        serde_json::json!({
+            "fixture_inputs": self.fixture_inputs,
+            "beginning_parent_checkpoint_canonical_json": self.beginning_parent_checkpoint_canonical_json,
+            "beginning_complete_owner_canonical_bytes": self.beginning_complete_owner_canonical_bytes,
+            "beginning_clock_canonical_json": self.beginning_clock_canonical_json,
+            "beginning_stage3_owner_canonical_bytes": self.beginning_stage3_owner_canonical_bytes,
+            "beginning_surface_configuration_canonical_bytes": self.beginning_surface_configuration_canonical_bytes,
+            "beginning_stage3_lane_count": self.beginning_stage3_lane_count,
+            "beginning_native_v3_resident_present": self.beginning_native_v3_resident_present,
+            "beginning_native_v4_resident_present": self.beginning_native_v4_resident_present,
+            "ending_parent_checkpoint_canonical_json": self.ending_parent_checkpoint_canonical_json,
+            "finalized_accepted_segment_count": self.finalized_accepted_segment_count,
+            "finalized_parent_canonical_projection": self.finalized_parent_canonical_projection,
+            "ending_complete_owner_canonical_bytes": self.ending_complete_owner_canonical_bytes,
+            "ending_clock_canonical_json": self.ending_clock_canonical_json,
+            "ending_clock_accepted_until": self.ending_clock_accepted_until,
+            "ending_stage3_owner_canonical_bytes": self.ending_stage3_owner_canonical_bytes,
+            "subslabs": self.subslabs.iter().map(|receipt| {
+                let mut core = receipt.clone();
+                core.destination_receipts.clear();
+                serde_json::json!({
+                    "core_without_destination_receipts": core,
+                    "ordered_destination_receipts": receipt.destination_receipts.iter().map(
+                        |((ofe_id, tile_id), boundary)| {
+                            serde_json::json!({
+                                "ofe_id": ofe_id,
+                                "tile_id": tile_id,
+                                "boundary_receipt": boundary,
+                            })
+                        }
+                    ).collect::<Vec<_>>(),
+                })
+            }).collect::<Vec<_>>(),
+            "subslab_liquid_custody_canonical_json": self.subslab_liquid_custody_canonical_json,
+            "event_groups": self.event_groups,
+            "event_group_terminal_custody_canonical_json": self.event_group_terminal_custody_canonical_json,
+            "terminal_parcels": self.terminal_parcels,
+            "snow_free_successor_receipts": self.snow_free_successor_receipts,
+        })
+    }
+}
+
+fn assert_legacy_single_lane_terminal_fixture(result: &CompleteWb14CadenceFixtureResultV1) {
+    // The parent returns retained custody evidence, not pending custody. One terminal event occurs at 540 seconds and its produced parcel is consumed by the immediately following receiver support.
+    assert_eq!(result.event_groups.len(), 1);
+    let group = &result.event_groups[0];
+    assert_eq!(group.tick, ModelTimeNs::new(540_000_000_000));
+    let accepted_event = group
+        .accepted_event_receipt
+        .as_ref()
+        .expect("accepted terminal event evidence");
+    assert_eq!(accepted_event.tick(), group.tick);
+    assert_eq!(result.terminal_parcels.len(), 1);
+    let parcel = &result.terminal_parcels[0];
+    assert_eq!(
+        parcel.posture,
+        crate::snow_stage3_v11_attachment::DirectSnowStage3V11TerminalParcelPosture::Consumed
+    );
+    assert!(
+        group
+            .produced_unconsumed_parcel_digests
+            .contains(&parcel.parcel_digest)
+    );
+}
+
+#[derive(Clone, Copy)]
+enum CompleteWb14CadenceFixtureFlagV1 {
+    Disabled,
+    Enabled,
+}
+
+impl CompleteWb14CadenceFixtureFlagV1 {
+    fn enabled(self) -> bool {
+        matches!(self, Self::Enabled)
+    }
+}
+
+impl From<bool> for CompleteWb14CadenceFixtureFlagV1 {
+    fn from(value: bool) -> Self {
+        if value { Self::Enabled } else { Self::Disabled }
+    }
+}
+
+#[derive(Clone, Copy)]
+struct CompleteWb14CadenceFixtureRequestV1 {
+    runtime_swe_m: f64,
+    initial_cold_delta_k: f64,
+    include_child_17: CompleteWb14CadenceFixtureFlagV1,
+    hard_boundary_ns: Option<u128>,
+    expect_dynamic_proposal: CompleteWb14CadenceFixtureFlagV1,
+    second_lane_swe_m: Option<f64>,
+    terminal_event: CompleteWb14CadenceFixtureFlagV1,
+    capture_terminal_failure: CompleteWb14CadenceFixtureFlagV1,
+    solid_reappearance: CompleteWb14CadenceFixtureFlagV1,
+    production_only: CompleteWb14CadenceFixtureFlagV1,
+}
+
+fn exercise_complete_wb14_cadence(
+    runtime_swe_m: f64,
+    initial_cold_delta_k: f64,
+    include_child_17: impl Into<CompleteWb14CadenceFixtureFlagV1>,
+    hard_boundary_ns: Option<u128>,
+    expect_dynamic_proposal: impl Into<CompleteWb14CadenceFixtureFlagV1>,
+    second_lane_swe_m: Option<f64>,
+    terminal_event: impl Into<CompleteWb14CadenceFixtureFlagV1>,
+    capture_terminal_failure: impl Into<CompleteWb14CadenceFixtureFlagV1>,
+    solid_reappearance: impl Into<CompleteWb14CadenceFixtureFlagV1>,
+    production_only: impl Into<CompleteWb14CadenceFixtureFlagV1>,
+) {
+    if let Some(result) =
+        execute_complete_wb14_cadence_fixture(CompleteWb14CadenceFixtureRequestV1 {
+            runtime_swe_m,
+            initial_cold_delta_k,
+            include_child_17: include_child_17.into(),
+            hard_boundary_ns,
+            expect_dynamic_proposal: expect_dynamic_proposal.into(),
+            second_lane_swe_m,
+            terminal_event: terminal_event.into(),
+            capture_terminal_failure: capture_terminal_failure.into(),
+            solid_reappearance: solid_reappearance.into(),
+            production_only: production_only.into(),
+        })
+    {
+        assert_legacy_single_lane_terminal_fixture(&result);
+    }
+}
+
 #[allow(
     clippy::cast_possible_truncation,
     clippy::cast_sign_loss,
     clippy::too_many_lines
 )]
-fn exercise_complete_wb14_cadence(
-    runtime_swe_m: f64,
-    initial_cold_delta_k: f64,
-    include_child_17: bool,
-    hard_boundary_ns: Option<u128>,
-    expect_dynamic_proposal: bool,
-    second_lane_swe_m: Option<f64>,
-    terminal_event: bool,
-    capture_terminal_failure: bool,
-    solid_reappearance: bool,
-    production_only: bool,
-) {
+fn execute_complete_wb14_cadence_fixture(
+    request: CompleteWb14CadenceFixtureRequestV1,
+) -> Option<CompleteWb14CadenceFixtureResultV1> {
+    let CompleteWb14CadenceFixtureRequestV1 {
+        runtime_swe_m,
+        initial_cold_delta_k,
+        include_child_17,
+        hard_boundary_ns,
+        expect_dynamic_proposal,
+        second_lane_swe_m,
+        terminal_event,
+        capture_terminal_failure,
+        solid_reappearance,
+        production_only,
+    } = request;
+    let include_child_17 = include_child_17.enabled();
+    let expect_dynamic_proposal = expect_dynamic_proposal.enabled();
+    let terminal_event = terminal_event.enabled();
+    let capture_terminal_failure = capture_terminal_failure.enabled();
+    let solid_reappearance = solid_reappearance.enabled();
+    let production_only = production_only.enabled();
     let equilibrium_fixture = adaptive_production_path_coverage::equilibrium_fixture_requested();
     let parity_case = adaptive_production_path_coverage::canonical_parity_case_requested();
     let positive_covered_rain = runtime_swe_m.to_bits() == 0.005_f64.to_bits()
@@ -409,22 +608,42 @@
         let resident = shadow
             .frozen_litter_v3_resident()
             .expect("native parity V3 resident");
-        let lse_bytes = &owners.get("land_surface_energy")
-            .expect("native parity LSE owner").state_bytes;
-        let native_lse_bytes = if openwepp_land_surface_energy::snow_accuracy_policy::Policy::current().identity() == Some(14) {
-            let transport = openwepp_coupled_time::Policy14LseOwnerTransport::from_canonical_bytes(lse_bytes)
-                .expect("canonical B01 composite LSE transport");
-            assert_eq!(transport.active_covered_runtime_lse_v1,
-                serde_json::to_vec(&shadow.inner.lse_state).expect("active LSE bytes"));
-            assert_eq!(transport.native_litter_lse_v3,
-                serde_json::to_vec(resident.lse_state()).expect("native LSE bytes"));
-            assert_eq!(transport.native_exact_surface_enthalpy_v4,
-                shadow.frozen_litter_v4_resident().expect("native exact resident")
-                    .exact_surface_owner().canonical_bytes().expect("native exact owner bytes"));
-            transport.native_litter_lse_v3
-        } else { lse_bytes.clone() };
-        let lse_owner: serde_json::Value = serde_json::from_slice(&native_lse_bytes)
-            .expect("native parity LSE component JSON");
+        let lse_bytes = &owners
+            .get("land_surface_energy")
+            .expect("native parity LSE owner")
+            .state_bytes;
+        let native_lse_bytes =
+            if openwepp_land_surface_energy::snow_accuracy_policy::Policy::current().identity()
+                == Some(14)
+            {
+                let transport =
+                    openwepp_coupled_time::Policy14LseOwnerTransport::from_canonical_bytes(
+                        lse_bytes,
+                    )
+                    .expect("canonical B01 composite LSE transport");
+                assert_eq!(
+                    transport.active_covered_runtime_lse_v1,
+                    serde_json::to_vec(&shadow.inner.lse_state).expect("active LSE bytes")
+                );
+                assert_eq!(
+                    transport.native_litter_lse_v3,
+                    serde_json::to_vec(resident.lse_state()).expect("native LSE bytes")
+                );
+                assert_eq!(
+                    transport.native_exact_surface_enthalpy_v4,
+                    shadow
+                        .frozen_litter_v4_resident()
+                        .expect("native exact resident")
+                        .exact_surface_owner()
+                        .canonical_bytes()
+                        .expect("native exact owner bytes")
+                );
+                transport.native_litter_lse_v3
+            } else {
+                lse_bytes.clone()
+            };
+        let lse_owner: serde_json::Value =
+            serde_json::from_slice(&native_lse_bytes).expect("native parity LSE component JSON");
         assert_eq!(
             lse_owner
                 .get("configuration_sha256")
@@ -1426,7 +1645,7 @@
             assert_eq!(shadow, rollback_consumer);
             assert_eq!(beginning_clock, rollback_clock);
             assert_eq!(stage3_beginning, rollback_stage3);
-            return;
+            return None;
         }
         fn is_refinement_source(error: &DirectSnowStage3V11AttachmentError) -> bool {
             match error {
@@ -1465,7 +1684,7 @@
             assert_eq!(shadow, rollback_consumer);
             assert_eq!(beginning_clock, rollback_clock);
             assert_eq!(stage3_beginning, rollback_stage3);
-            return;
+            return None;
         }
         assert!(
             matches!(
@@ -1576,11 +1795,9 @@
         assert!(evidence.coupling_iterations.iter().all(|iteration| {
             let request = &iteration.hook.request;
             let comparison_shape = if request.coupling_iteration == 0 {
-                iteration.hook.comparisons.is_none()
-                    && request.ending_snow_hint.is_none()
+                iteration.hook.comparisons.is_none() && request.ending_snow_hint.is_none()
             } else {
-                iteration.hook.comparisons.is_some()
-                    && request.ending_snow_hint.is_some()
+                iteration.hook.comparisons.is_some() && request.ending_snow_hint.is_some()
             };
             comparison_shape
                 && evidence
@@ -1590,8 +1807,7 @@
                         call.request.support == request.support
                             && call.request.role == request.role
                             && call.request.attempt_ordinal == request.attempt_ordinal
-                            && call.request.coupling_iteration
-                                == request.coupling_iteration
+                            && call.request.coupling_iteration == request.coupling_iteration
                             && call.request.lane_id == request.lane_id
                             && call.request.beginning_joint.receipt_sha256()
                                 == request.beginning_joint.receipt_sha256()
@@ -1997,7 +2213,7 @@
         assert_eq!(shadow, rollback_consumer);
         assert_eq!(beginning_clock, rollback_clock);
         assert_eq!(stage3_beginning, rollback_stage3);
-        return;
+        return None;
     }
     let execution = execute_covered_real_v11_parent(
         &context,
@@ -2014,8 +2230,8 @@
     );
     if crate::v9_real_consumer_shadow::v11_covered::canonical_covered_parity_poison_active_for_test(
     ) {
-        let active_poison = crate::v9_real_consumer_shadow::v11_covered::
-            canonical_covered_parity_poison_v1();
+        let active_poison =
+            crate::v9_real_consumer_shadow::v11_covered::canonical_covered_parity_poison_v1();
         if let Err(error) = &execution {
             crate::v9_real_consumer_shadow::v11_covered::
                 record_canonical_covered_outer_attachment_rejection_for_test(error);
@@ -2054,10 +2270,10 @@
             .is_ok_and(|bytes| bytes == rollback_stage3_bytes);
         crate::v9_real_consumer_shadow::v11_covered::
             record_canonical_covered_parity_rollback_for_test(rollback);
-        return;
+        return None;
     }
     let (
-        _,
+        ending_parent,
         ending_consumer,
         ending_clock,
         finalized_parent,
@@ -2097,33 +2313,99 @@
         }
     });
     if terminal_event {
-        if second_lane_swe_m.is_none() {
-            // The parent returns retained custody evidence, not pending
-            // custody. One terminal event occurs at 540 seconds and its
-            // produced parcel is consumed by the immediately following
-            // receiver support.
-            assert_eq!(event_groups.len(), 1);
-            let group = &event_groups[0];
-            assert_eq!(group.tick, ModelTimeNs::new(540_000_000_000));
-            let accepted_event = group
-                .accepted_event_receipt
-                .as_ref()
-                .expect("accepted terminal event evidence");
-            assert_eq!(accepted_event.tick(), group.tick);
-            assert_eq!(terminal_parcels.len(), 1);
-            let parcel = &terminal_parcels[0];
-            assert_eq!(
-                parcel.posture,
-                crate::snow_stage3_v11_attachment::DirectSnowStage3V11TerminalParcelPosture::Consumed,
-            );
-            assert!(
+        if second_lane_swe_m.is_some() {
+            return None;
+        }
+        let snow_free_successor_receipts =
+            crate::snow_stage3_v11_attachment::take_snow_free_successor_fixture_audit_v1();
+        let subslab_liquid_custody_canonical_json = subslabs
+            .iter()
+            .map(|receipt| {
+                receipt.support_liquid_custody_v2().map(|custody| {
+                    serde_json::to_vec(custody).expect("support-liquid custody bytes")
+                })
+            })
+            .collect();
+        let event_group_terminal_custody_canonical_json = event_groups
+            .iter()
+            .map(|group| {
                 group
-                    .produced_unconsumed_parcel_digests
-                    .contains(&parcel.parcel_digest)
-            );
-        }
-        return;
-    }
+                    .terminal_receiver_custody_v2()
+                    .map(|custody| serde_json::to_vec(custody).expect("terminal custody bytes"))
+            })
+            .collect();
+        let parity_profile = match parity_case {
+            Some(crate::v9_real_consumer_shadow::v11_covered::CanonicalCoveredPhysicalParityCaseV1::Ordinary) => "ordinary",
+            Some(crate::v9_real_consumer_shadow::v11_covered::CanonicalCoveredPhysicalParityCaseV1::NativeFrozen) => "native-frozen",
+            Some(crate::v9_real_consumer_shadow::v11_covered::CanonicalCoveredPhysicalParityCaseV1::NativeMixedPhase) => "native-mixed-phase",
+            Some(crate::v9_real_consumer_shadow::v11_covered::CanonicalCoveredPhysicalParityCaseV1::NativeThawRefreeze) => "native-thaw-refreeze",
+            Some(crate::v9_real_consumer_shadow::v11_covered::CanonicalCoveredPhysicalParityCaseV1::NativeWetCanopy) => "native-wet-canopy",
+            Some(crate::v9_real_consumer_shadow::v11_covered::CanonicalCoveredPhysicalParityCaseV1::NativeMultiOfe) => "native-multi-ofe",
+            None => "default",
+        };
+        return Some(CompleteWb14CadenceFixtureResultV1 {
+            fixture_inputs: serde_json::json!({
+                "runtime_swe_m_bits": runtime_swe_m.to_bits(),
+                "initial_cold_delta_k_bits": initial_cold_delta_k.to_bits(),
+                "include_child_17": include_child_17,
+                "hard_boundary_ns": hard_boundary_ns,
+                "expect_dynamic_proposal": expect_dynamic_proposal,
+                "second_lane_swe_m_bits": second_lane_swe_m.map(f64::to_bits),
+                "terminal_event": terminal_event,
+                "capture_terminal_failure": capture_terminal_failure,
+                "solid_reappearance": solid_reappearance,
+                "production_only": production_only,
+                "parity_profile": parity_profile,
+                "parent_support": support,
+            }),
+            beginning_parent_checkpoint_canonical_json: serde_json::to_vec(&parent.checkpoint())
+                .expect("beginning parent checkpoint bytes"),
+            beginning_complete_owner_canonical_bytes: shadow
+                .canonical_v11_parent_owner_state_bytes()
+                .expect("beginning complete owner bytes"),
+            beginning_clock_canonical_json: serde_json::to_vec(&beginning_clock)
+                .expect("beginning clock bytes"),
+            beginning_stage3_owner_canonical_bytes:
+                crate::v9_real_consumer_shadow::v11_covered::canonical_stage3_snow_owner_bytes_v11(
+                    &stage3_beginning_by_lane,
+                )
+                .expect("beginning Stage3 owner bytes"),
+            beginning_surface_configuration_canonical_bytes: shadow
+                .inner
+                .surface_configuration
+                .canonical_bytes()
+                .expect("beginning surface configuration bytes"),
+            beginning_stage3_lane_count: stage3_beginning_by_lane.len(),
+            beginning_native_v3_resident_present: shadow.frozen_litter_v3.is_some(),
+            beginning_native_v4_resident_present: shadow.frozen_litter_v4.is_some(),
+            ending_parent_checkpoint_canonical_json: serde_json::to_vec(
+                &ending_parent.checkpoint(),
+            )
+            .expect("ending parent checkpoint bytes"),
+            finalized_accepted_segment_count: finalized_parent.accepted_segments.len(),
+            finalized_parent_canonical_projection: v11_parent_candidate_canonical_projection(
+                &finalized_parent,
+            ),
+            ending_complete_owner_canonical_bytes: ending_consumer
+                .canonical_v11_parent_owner_state_bytes()
+                .expect("ending complete owner bytes"),
+            ending_clock_canonical_json: serde_json::to_vec(&ending_clock)
+                .expect("ending clock bytes"),
+            ending_clock_accepted_until: ending_clock.accepted_until(),
+            ending_stage3_owner_canonical_bytes:
+                crate::v9_real_consumer_shadow::v11_covered::canonical_stage3_snow_owner_bytes_v11(
+                    &ending_stage3,
+                )
+                .expect("ending Stage3 owner bytes"),
+            subslabs,
+            subslab_liquid_custody_canonical_json,
+            event_groups,
+            event_group_terminal_custody_canonical_json,
+            terminal_parcels,
+            snow_free_successor_receipts,
+        });
+    }
+
     assert_eq!(ending_clock.accepted_until(), support.end_ns());
     assert!(!subslabs.is_empty());
     assert!(
@@ -2251,10 +2533,10 @@
         // placeholder and does not execute the canonical zero-duration
         // reappearance event. The adaptive assertions above are the real owner
         // path for this fixture.
-        return;
+        return None;
     }
     if production_only {
-        return;
+        return None;
     }
     let lane_receipt = executor
         .stack
@@ -2276,7 +2558,7 @@
     }
 
     if second_lane_swe_m.is_some() {
-        return;
+        return None;
     }
 
     let mut open_shadow = shadow.clone();
@@ -2675,6 +2957,7 @@
         execute_direct_v11_segment(&migrated.configuration, &parent, &slab, &mut poisoned).is_err()
     );
     assert!(poisoned.stack.take_staged_ending().is_none());
+    None
 }
 
 include!("v9_real_consumer_shadow_wb14_routing_tests.rs");
--- a/crates/openwepp-land-surface-energy/src/covered_liquid.rs
+++ b/crates/openwepp-land-surface-energy/src/covered_liquid.rs
@@ -74,7 +74,8 @@
         }
         if amounts.iter().any(|value| *value < 0.0)
             || !(0.0..=1.0).contains(&self.wet_fraction)
-            || (self.transparent_passage.is_none() && self.wet_surface_temperature_k < REFERENCE_TEMPERATURE_K)
+            || (self.transparent_passage.is_none()
+                && self.wet_surface_temperature_k < REFERENCE_TEMPERATURE_K)
             || self.wet_surface_specific_enthalpy_j_kg.to_bits()
                 != (WATER_HEAT_CAPACITY_J_KG_K
                     * (self.wet_surface_temperature_k - REFERENCE_TEMPERATURE_K))
@@ -84,7 +85,9 @@
                 "covered occupancy liquid ledger",
             ));
         }
-        if let Some(proof)=self.transparent_passage { proof.validate_ledger(&self)?; }
+        if let Some(proof) = self.transparent_passage {
+            proof.validate_ledger(&self)?;
+        }
         let residual = self.beginning_store_kg_m2_tile
             + self.incident_rain_kg_m2_tile
             + self.condensation_kg_m2_tile
@@ -184,27 +187,47 @@
     wet_surface_temperature_k: f64,
     pass: CoveredLiquidPass,
 ) -> Result<CoveredOccupancyLiquidLedger, LandSurfaceEnergyError> {
-    finalize_covered_liquid_common(preparation,signed_vapor_amount,wet_surface_temperature_k,pass,None)
+    finalize_covered_liquid_common(
+        preparation,
+        signed_vapor_amount,
+        wet_surface_temperature_k,
+        pass,
+        None,
+    )
 }
 
 pub(crate) fn finalize_covered_liquid_for_column(
-    column:&crate::CoveredColumnInputs,occupancy:&CoveredOccupancyInputs,
-    preparation:CoveredLiquidPreparation,signed_vapor_amount:f64,numerical_wet_temperature_k:f64,pass:CoveredLiquidPass,
-)->Result<CoveredOccupancyLiquidLedger,LandSurfaceEnergyError> {
-    let proof=column.passage_proof(occupancy)?;
-    if proof.is_some() && (preparation.capacity!=0.0 || preparation.preliminary_store!=0.0 || signed_vapor_amount!=0.0) {
-        return Err(LandSurfaceEnergyError::StateLineage("transparent passage interception/store/vapor"));
-    }
-    let temperature=proof.map_or(numerical_wet_temperature_k,|p|p.release_temperature_k());
-    let ledger=finalize_covered_liquid_common(preparation,signed_vapor_amount,temperature,pass,proof)?;
-    column.validate_passage_ledger(occupancy,&ledger)?;
+    column: &crate::CoveredColumnInputs,
+    occupancy: &CoveredOccupancyInputs,
+    preparation: CoveredLiquidPreparation,
+    signed_vapor_amount: f64,
+    numerical_wet_temperature_k: f64,
+    pass: CoveredLiquidPass,
+) -> Result<CoveredOccupancyLiquidLedger, LandSurfaceEnergyError> {
+    let proof = column.passage_proof(occupancy)?;
+    if proof.is_some()
+        && (preparation.capacity != 0.0
+            || preparation.preliminary_store != 0.0
+            || signed_vapor_amount != 0.0)
+    {
+        return Err(LandSurfaceEnergyError::StateLineage(
+            "transparent passage interception/store/vapor",
+        ));
+    }
+    let temperature = proof.map_or(numerical_wet_temperature_k, |p| p.release_temperature_k());
+    let ledger =
+        finalize_covered_liquid_common(preparation, signed_vapor_amount, temperature, pass, proof)?;
+    column.validate_passage_ledger(occupancy, &ledger)?;
     Ok(ledger)
 }
 
 fn finalize_covered_liquid_common(
-    preparation:CoveredLiquidPreparation,signed_vapor_amount:f64,wet_surface_temperature_k:f64,pass:CoveredLiquidPass,
-    transparent_passage:Option<crate::TransparentCanopyPassageProofV1>,
-)->Result<CoveredOccupancyLiquidLedger,LandSurfaceEnergyError> {
+    preparation: CoveredLiquidPreparation,
+    signed_vapor_amount: f64,
+    wet_surface_temperature_k: f64,
+    pass: CoveredLiquidPass,
+    transparent_passage: Option<crate::TransparentCanopyPassageProofV1>,
+) -> Result<CoveredOccupancyLiquidLedger, LandSurfaceEnergyError> {
     if !signed_vapor_amount.is_finite() || !wet_surface_temperature_k.is_finite() {
         return Err(LandSurfaceEnergyError::NonFinite(
             "covered E04 finalization",
--- a/crates/openwepp-land-surface-energy/src/forcing.rs
+++ b/crates/openwepp-land-surface-energy/src/forcing.rs
@@ -145,16 +145,24 @@
         self.validate_material(expected_transaction, false)
     }
 
-    pub(crate) fn validate_policy14_transparent_material(&self)->Result<(),LandSurfaceEnergyError> {
-        if crate::snow_accuracy_policy::Policy::current().identity()!=Some(14) {
-            return Err(LandSurfaceEnergyError::StateLineage("transparent material policy"));
+    pub(crate) fn validate_policy14_transparent_material(
+        &self,
+    ) -> Result<(), LandSurfaceEnergyError> {
+        if crate::snow_accuracy_policy::Policy::current().identity() != Some(14) {
+            return Err(LandSurfaceEnergyError::StateLineage(
+                "transparent material policy",
+            ));
         }
         // This material boundary is bound at the actual policy14 covered tile
         // projection. Ordinary forcing validation retains its snow refusals.
         self.validate_material(self.transaction_id, true)
     }
 
-    fn validate_material(&self,expected_transaction:TransactionId,policy14_material:bool)->Result<(),LandSurfaceEnergyError> {
+    fn validate_material(
+        &self,
+        expected_transaction: TransactionId,
+        policy14_material: bool,
+    ) -> Result<(), LandSurfaceEnergyError> {
         if self.transaction_id.0 == 0 || self.transaction_id != expected_transaction {
             return Err(LandSurfaceEnergyError::Identity {
                 field: "forcing.transaction_id",
--- a/crates/openwepp-land-surface-energy/src/solver.rs
+++ b/crates/openwepp-land-surface-energy/src/solver.rs
@@ -17,9 +17,7 @@
     clippy::too_many_lines
 )]
 
-use crate::covered_liquid::{
-    CoveredLiquidPass, CoveredLiquidPreparation, prepare_covered_liquid,
-};
+use crate::covered_liquid::{CoveredLiquidPass, CoveredLiquidPreparation, prepare_covered_liquid};
 use crate::covered_output::{CoveredColumnEvaluation, CoveredOccupancyEvaluation};
 use crate::numerics::{
     MAX_BACKTRACKING_HALVINGS, MAX_NEWTON_ITERATIONS, is_strict_residual_decrease,
--- a/crates/openwepp-land-surface-energy/src/solver_litter_phase.rs
+++ b/crates/openwepp-land-surface-energy/src/solver_litter_phase.rs
@@ -226,10 +226,8 @@
             let index = 10 * occupancy_index + 6 + component_index;
             jacobian[index].fill(0.0);
             jacobian[index][index] = 1.0;
-            let target = column.inactive_component_anchor_k(
-                component_index,
-                canopy_temperature_k,
-            ) / coordinate_scales[index];
+            let target = column.inactive_component_anchor_k(component_index, canopy_temperature_k)
+                / coordinate_scales[index];
             right_hand_side[index] = target - scaled_trial[index];
         }
     }
--- a/crates/openwepp-land-surface-energy/src/solver_tests.rs
+++ b/crates/openwepp-land-surface-energy/src/solver_tests.rs
@@ -1161,7 +1161,7 @@
         .expect("Stage-3 optical receipt");
     let stage3_common = 10 * stage3_column.occupancies.len();
     stage3_column.stage3_lower_boundary = Some(Stage3SnowCoveredLowerBoundary {
-                native_snow_exchange: None,
+        native_snow_exchange: None,
         snow_temperature_k: full_trial[stage3_common + 2],
         latent_heat_j_kg: stage3_column.latent_heat_j_kg,
         sensible_to_canopy_air_w_m2: 0.0,
--- a/crates/openwepp-land-surface-energy/src/transaction.rs
+++ b/crates/openwepp-land-surface-energy/src/transaction.rs
@@ -618,7 +618,7 @@
         .iter()
         .zip(&accepted.evaluation.occupancies)
     {
-        beginning.validate_passage_ledger(input,&evaluation.liquid)?;
+        beginning.validate_passage_ledger(input, &evaluation.liquid)?;
         let runtime = identities
             .iter()
             .find_map(|((solver_occupancy, _), runtime)| {
@@ -2188,14 +2188,18 @@
     // advection and soil coupling remain separate Stage-3 ledger terms and
     // are not silently borrowed from the snow-free ground projection.
     let latent_heat_j_kg = boundary.latent_heat_j_kg;
-    if (boundary.native_snow_exchange.is_none() || boundary.final_canopy_boundary_receipt_sha256.is_some())
-        && ground_vapor != boundary.vapor_to_canopy_air_kg_m2_s {
+    if (boundary.native_snow_exchange.is_none()
+        || boundary.final_canopy_boundary_receipt_sha256.is_some())
+        && ground_vapor != boundary.vapor_to_canopy_air_kg_m2_s
+    {
         return Err(LandSurfaceEnergyError::StateLineage(
             "Stage-3 lower-boundary vapor custody",
         ));
     }
-    if (boundary.native_snow_exchange.is_none() || boundary.final_canopy_boundary_receipt_sha256.is_some())
-        && ground_sensible != boundary.sensible_to_canopy_air_w_m2 {
+    if (boundary.native_snow_exchange.is_none()
+        || boundary.final_canopy_boundary_receipt_sha256.is_some())
+        && ground_sensible != boundary.sensible_to_canopy_air_w_m2
+    {
         return Err(LandSurfaceEnergyError::StateLineage(
             "Stage-3 lower-boundary sensible custody",
         ));
@@ -2372,7 +2376,9 @@
         .zip(&final_value.evaluation.occupancies)
         .enumerate()
     {
-        phase.beginning.validate_passage_ledger(input,&evaluation.liquid)?;
+        phase
+            .beginning
+            .validate_passage_ledger(input, &evaluation.liquid)?;
         let block = &final_value.solution[index * 10..(index + 1) * 10];
         let runtime_identity = phase
             .root_identities
@@ -2442,7 +2448,9 @@
             shade_leaf_temperature_k: evaluation.component_temperatures_k[1],
             wet_surface_temperature_k: if evaluation.liquid.transparent_passage.is_some() {
                 evaluation.component_temperatures_k[2]
-            } else { evaluation.liquid.wet_surface_temperature_k },
+            } else {
+                evaluation.liquid.wet_surface_temperature_k
+            },
             dry_stem_temperature_k: evaluation.component_temperatures_k[3],
             sun_ci_pa: evaluation.ci_pa[0],
             shade_ci_pa: evaluation.ci_pa[1],
@@ -2970,10 +2978,14 @@
 
 impl OpenPotentialPhase {
     /// Read-only experimental physical-input observation.
-    pub fn accuracy_observation_beginning(&self) -> &OpenSurfaceProblem { &self.beginning }
+    pub fn accuracy_observation_beginning(&self) -> &OpenSurfaceProblem {
+        &self.beginning
+    }
 }
 
 impl CoveredPotentialPhase {
     /// Read-only experimental physical-input observation.
-    pub fn accuracy_observation_beginning(&self) -> &CoveredColumnInputs { &self.beginning }
-}
+    pub fn accuracy_observation_beginning(&self) -> &CoveredColumnInputs {
+        &self.beginning
+    }
+}
--- a/crates/openwepp-land-surface-energy/src/transaction_v3_bridge.rs
+++ b/crates/openwepp-land-surface-energy/src/transaction_v3_bridge.rs
@@ -67,7 +67,7 @@
         .iter()
         .zip(&accepted.evaluation.predecessor.occupancies)
     {
-        beginning.validate_passage_ledger(input,&evaluation.liquid)?;
+        beginning.validate_passage_ledger(input, &evaluation.liquid)?;
         let runtime = root_identities
             .iter()
             .find_map(|((solver_occupancy, _), runtime)| {
--- a/crates/openwepp-land-surface-energy/src/transaction_v3_open.rs
+++ b/crates/openwepp-land-surface-energy/src/transaction_v3_open.rs
@@ -356,5 +356,7 @@
 
 impl V3OpenPotentialPhase {
     /// Read-only experimental physical-input observation.
-    pub fn accuracy_observation_beginning(&self) -> &OpenSurfaceProblem { &self.beginning }
-}
+    pub fn accuracy_observation_beginning(&self) -> &OpenSurfaceProblem {
+        &self.beginning
+    }
+}
--- a/crates/openwepp-land-surface-energy/src/transaction_v3_tests.rs
+++ b/crates/openwepp-land-surface-energy/src/transaction_v3_tests.rs
@@ -507,63 +507,132 @@
 
 #[test]
 fn native_snow_shared_node_evaluates_constitutive_fluxes_independent_of_guess() {
-    use crate::{NativeSnowExchangeV1, Stage3SnowCoveredLowerBoundary,
-        Stage3SnowOpticalBoundaryReceiptV1, Stage3SnowOpticalBoundaryReceiptInputs};
+    use crate::{
+        NativeSnowExchangeV1, Stage3SnowCoveredLowerBoundary,
+        Stage3SnowOpticalBoundaryReceiptInputs, Stage3SnowOpticalBoundaryReceiptV1,
+    };
     crate::snow_accuracy_policy::Policy::B01.install().unwrap();
     let mut c = column();
     c.authority = CoveredColumnAuthority::V11SnowCovered;
-    let optical = Stage3SnowOpticalBoundaryReceiptV1::try_new(Stage3SnowOpticalBoundaryReceiptInputs {
-        ofe_id: OfeId::try_new("ofe-1").unwrap(), tile_id: TileId::try_new("forest").unwrap(),
-        terminal_w_m2_tile: c.ground.terminal_shortwave_w_m2_tile,
-        absorbed_w_m2_tile: crate::partition_ground_shortwave(c.ground.terminal_shortwave_w_m2_tile,
-            c.ground.surface_vis_albedo, c.ground.surface_nir_albedo).unwrap().absorbed,
-        reflected_w_m2_tile: crate::partition_ground_shortwave(c.ground.terminal_shortwave_w_m2_tile,
-            c.ground.surface_vis_albedo, c.ground.surface_nir_albedo).unwrap().reflected,
-        snow_vis_albedo: c.ground.surface_vis_albedo, snow_nir_albedo: c.ground.surface_nir_albedo,
-        stage3_albedo_state_sha256: digest('a'), forcing_receipt_sha256: digest('b'),
-    }).unwrap();
-    let mut exchange = NativeSnowExchangeV1 { policy_id: 14, ofe_id: optical.ofe_id.clone(),
-        tile_id: optical.tile_id.clone(), start_ns: 0, end_ns: 1_800_000_000_000,
-        snow_temperature_k: 273.15, snow_saturation_specific_humidity: 0.004,
-        pressure_pa: c.pressure_pa, heat_conductance_m_s: 0.025, vapor_conductance_m_s: 0.02,
-        forcing_sha256: digest('b'), exposure_sha256: digest('c'), payload_sha256: digest('d') };
+    let optical =
+        Stage3SnowOpticalBoundaryReceiptV1::try_new(Stage3SnowOpticalBoundaryReceiptInputs {
+            ofe_id: OfeId::try_new("ofe-1").unwrap(),
+            tile_id: TileId::try_new("forest").unwrap(),
+            terminal_w_m2_tile: c.ground.terminal_shortwave_w_m2_tile,
+            absorbed_w_m2_tile: crate::partition_ground_shortwave(
+                c.ground.terminal_shortwave_w_m2_tile,
+                c.ground.surface_vis_albedo,
+                c.ground.surface_nir_albedo,
+            )
+            .unwrap()
+            .absorbed,
+            reflected_w_m2_tile: crate::partition_ground_shortwave(
+                c.ground.terminal_shortwave_w_m2_tile,
+                c.ground.surface_vis_albedo,
+                c.ground.surface_nir_albedo,
+            )
+            .unwrap()
+            .reflected,
+            snow_vis_albedo: c.ground.surface_vis_albedo,
+            snow_nir_albedo: c.ground.surface_nir_albedo,
+            stage3_albedo_state_sha256: digest('a'),
+            forcing_receipt_sha256: digest('b'),
+        })
+        .unwrap();
+    let mut exchange = NativeSnowExchangeV1 {
+        policy_id: 14,
+        ofe_id: optical.ofe_id.clone(),
+        tile_id: optical.tile_id.clone(),
+        start_ns: 0,
+        end_ns: 1_800_000_000_000,
+        snow_temperature_k: 273.15,
+        snow_saturation_specific_humidity: 0.004,
+        pressure_pa: c.pressure_pa,
+        heat_conductance_m_s: 0.025,
+        vapor_conductance_m_s: 0.02,
+        forcing_sha256: digest('b'),
+        exposure_sha256: digest('c'),
+        payload_sha256: digest('d'),
+    };
     exchange.payload_sha256 = exchange.reconstructed_sha256().unwrap();
     c.stage3_lower_boundary = Some(Stage3SnowCoveredLowerBoundary {
-        native_snow_exchange: Some(exchange.clone()), snow_temperature_k: 273.15,
-        latent_heat_j_kg: 2_834_000.0, sensible_to_canopy_air_w_m2: 777.0,
-        vapor_to_canopy_air_kg_m2_s: 0.0001, net_longwave_w_m2: 0.0,
-        shortwave_absorbed_w_m2: optical.absorbed_w_m2_tile.total(), precipitation_advection_w_m2: 0.0,
-        carrier_receipt_id: digest('e'), snow_vis_albedo: optical.snow_vis_albedo,
-        snow_nir_albedo: optical.snow_nir_albedo, stage3_albedo_state_sha256: digest('a'),
-        forcing_receipt_sha256: digest('b'), optical_receipt_sha256: Some(optical.receipt_sha256.clone()),
-        reciprocal_longwave_receipt_sha256: None, final_canopy_boundary_receipt_sha256: None,
+        native_snow_exchange: Some(exchange.clone()),
+        snow_temperature_k: 273.15,
+        latent_heat_j_kg: 2_834_000.0,
+        sensible_to_canopy_air_w_m2: 777.0,
+        vapor_to_canopy_air_kg_m2_s: 0.0001,
+        net_longwave_w_m2: 0.0,
+        shortwave_absorbed_w_m2: optical.absorbed_w_m2_tile.total(),
+        precipitation_advection_w_m2: 0.0,
+        carrier_receipt_id: digest('e'),
+        snow_vis_albedo: optical.snow_vis_albedo,
+        snow_nir_albedo: optical.snow_nir_albedo,
+        stage3_albedo_state_sha256: digest('a'),
+        forcing_receipt_sha256: digest('b'),
+        optical_receipt_sha256: Some(optical.receipt_sha256.clone()),
+        reciprocal_longwave_receipt_sha256: None,
+        final_canopy_boundary_receipt_sha256: None,
     });
     c.stage3_optical = Some(optical);
-    for (t,q) in [(270.0,0.002),(280.0,0.006),(273.15,0.004)] {
-        let mut x = initial_trial(); x[10]=t; x[11]=q; x[12]=273.15;
-        let actual = crate::evaluate_covered_column(&c,&x,None,None).unwrap();
+    for (t, q) in [(270.0, 0.002), (280.0, 0.006), (273.15, 0.004)] {
+        let mut x = initial_trial();
+        x[10] = t;
+        x[11] = q;
+        x[12] = 273.15;
+        let actual = crate::evaluate_covered_column(&c, &x, None, None).unwrap();
         // Independent operands and explicit units: kg/m3 * J/kg/K * m/s * K.
-        let rho = c.pressure_pa / (287.05*t);
-        let expected_h = rho*1004.64*0.025*(273.15-t);
-        let expected_v = rho*0.02*(0.004-q);
-        assert_eq!(actual.ground_sensible_to_canopy_air_w_m2,expected_h);
-        assert_eq!(actual.lower_boundary_vapor_to_canopy_air_kg_m2_s,expected_v);
-        let mut perturbed = c.clone(); let b=perturbed.stage3_lower_boundary.as_mut().unwrap();
-        b.sensible_to_canopy_air_w_m2=-999.0; b.vapor_to_canopy_air_kg_m2_s=-0.0002;
-        assert_eq!(actual,crate::evaluate_covered_column(&perturbed,&x,None,None).unwrap());
-    }
-    let mut initial = initial_trial(); initial[12] = 273.15;
-    let solved = crate::solver::solve_covered_column(&c, None, initial.clone()).expect("native coupled nonlinear solution");
+        let rho = c.pressure_pa / (287.05 * t);
+        let expected_h = rho * 1004.64 * 0.025 * (273.15 - t);
+        let expected_v = rho * 0.02 * (0.004 - q);
+        assert_eq!(actual.ground_sensible_to_canopy_air_w_m2, expected_h);
+        assert_eq!(
+            actual.lower_boundary_vapor_to_canopy_air_kg_m2_s,
+            expected_v
+        );
+        let mut perturbed = c.clone();
+        let b = perturbed.stage3_lower_boundary.as_mut().unwrap();
+        b.sensible_to_canopy_air_w_m2 = -999.0;
+        b.vapor_to_canopy_air_kg_m2_s = -0.0002;
+        assert_eq!(
+            actual,
+            crate::evaluate_covered_column(&perturbed, &x, None, None).unwrap()
+        );
+    }
+    let mut initial = initial_trial();
+    initial[12] = 273.15;
+    let solved = crate::solver::solve_covered_column(&c, None, initial.clone())
+        .expect("native coupled nonlinear solution");
     let mut different_guess = c.clone();
-    different_guess.stage3_lower_boundary.as_mut().unwrap().sensible_to_canopy_air_w_m2 = -999.0;
-    different_guess.stage3_lower_boundary.as_mut().unwrap().vapor_to_canopy_air_kg_m2_s = -0.0002;
-    assert_eq!(solved, crate::solver::solve_covered_column(&different_guess, None, initial).unwrap());
-    let mut bad=c.clone();bad.stage3_lower_boundary.as_mut().unwrap().native_snow_exchange=None;
-    assert!(crate::evaluate_covered_column(&bad,&initial_trial(),None,None).is_err());
-    let mut bad=c.clone();bad.stage3_lower_boundary.as_mut().unwrap().native_snow_exchange.as_mut().unwrap().heat_conductance_m_s *=2.0;
-    assert!(crate::evaluate_covered_column(&bad,&initial_trial(),None,None).is_err());
-}
-
+    different_guess
+        .stage3_lower_boundary
+        .as_mut()
+        .unwrap()
+        .sensible_to_canopy_air_w_m2 = -999.0;
+    different_guess
+        .stage3_lower_boundary
+        .as_mut()
+        .unwrap()
+        .vapor_to_canopy_air_kg_m2_s = -0.0002;
+    assert_eq!(
+        solved,
+        crate::solver::solve_covered_column(&different_guess, None, initial).unwrap()
+    );
+    let mut bad = c.clone();
+    bad.stage3_lower_boundary
+        .as_mut()
+        .unwrap()
+        .native_snow_exchange = None;
+    assert!(crate::evaluate_covered_column(&bad, &initial_trial(), None, None).is_err());
+    let mut bad = c.clone();
+    bad.stage3_lower_boundary
+        .as_mut()
+        .unwrap()
+        .native_snow_exchange
+        .as_mut()
+        .unwrap()
+        .heat_conductance_m_s *= 2.0;
+    assert!(crate::evaluate_covered_column(&bad, &initial_trial(), None, None).is_err());
+}
 
 #[test]
 #[ignore = "isolated policy14 process and prospective transparent rain identity"]
@@ -572,161 +641,342 @@
     let mut c = column();
     c.top_rain_kg_m2_tile = 2.0;
     let o = &mut c.occupancies[0];
-    o.lai=0.0; o.sai=0.0;
-    o.sun.leaf_area_m2_m2_tile=0.0; o.shade.leaf_area_m2_m2_tile=0.0;
-    o.stem_area_m2_m2_tile=0.0; o.beginning_canopy_liquid_kg_m2_tile=0.0;
-    o.sun.absorbed_shortwave_w_m2_tile=0.0; o.shade.absorbed_shortwave_w_m2_tile=0.0;
-    o.stem_absorbed_shortwave_w_m2_tile=0.0;
-    let f=transparent_test_forcing(&c,vec![(2.0,280.0)]);
-    let id=identity(c.interval_s);
-    c.bind_transparent_liquid(&f,&id).unwrap();
+    o.lai = 0.0;
+    o.sai = 0.0;
+    o.sun.leaf_area_m2_m2_tile = 0.0;
+    o.shade.leaf_area_m2_m2_tile = 0.0;
+    o.stem_area_m2_m2_tile = 0.0;
+    o.beginning_canopy_liquid_kg_m2_tile = 0.0;
+    o.sun.absorbed_shortwave_w_m2_tile = 0.0;
+    o.shade.absorbed_shortwave_w_m2_tile = 0.0;
+    o.stem_absorbed_shortwave_w_m2_tile = 0.0;
+    let f = transparent_test_forcing(&c, vec![(2.0, 280.0)]);
+    let id = identity(c.interval_s);
+    c.bind_transparent_liquid(&f, &id).unwrap();
     // Explicit independently prescribed incoming rain: 2 kg/m² at280 K.
-    let mut x=initial_trial(); x[8]=300.0;
-    let r=crate::evaluate_covered_occupancy_block(&c,&c.occupancies[0],&x[..10],300.0,0.01,[0.0;4]).unwrap();
-    assert_eq!(r[8],0.0,"transparent numerical coordinate retains canonical anchor; passage temperature is separate");
-}
-
-
-fn transparent_test_forcing(c:&CoveredColumnInputs,parcels:Vec<(f64,f64)>)->crate::LandSurfaceForcing {
-    let id=identity(c.interval_s);
-    let mut f=crate::LandSurfaceForcing {forcing_sha256:digest('a'),transaction_id:id.transaction_id,
-        interval_s:c.interval_s,air_temperature_k:c.air_temperature_k,
-        air_specific_humidity_kg_kg:c.air_specific_humidity_kg_kg,air_pressure_pa:c.pressure_pa,
-        reference_wind_m_s:c.reference_wind_m_s,neutral_stability:true,
-        snow_present_at_beginning:false,snow_present_at_end:false,snow_terminal_payload_present:false,
-        direct_vis_w_m2:0.0,diffuse_vis_w_m2:0.0,direct_nir_w_m2:0.0,diffuse_nir_w_m2:0.0,
-        atmospheric_downward_longwave_w_m2:c.atmospheric_downward_longwave_w_m2,
-        precipitation_parcels:parcels.into_iter().enumerate().map(|(i,(m,t))|crate::LiquidParcel {
-            parcel_kind:crate::LiquidParcelKind::Precipitation,parcel_id:crate::ParcelId::try_new(format!("rain-{i}")).unwrap(),
-            source_owner_id:owner("climate"),source_ofe_id:id.ofe_id.clone(),source_tile_id:id.tile_id.clone(),
-            destination_ofe_id:id.ofe_id.clone(),destination_tile_id:id.tile_id.clone(),start_s:0.0,end_s:c.interval_s,
-            amount_kg_m2_destination_tile_ground:m,temperature_provider:crate::LiquidTemperatureProvider::HarderPomeroyHourly,
-            temperature_k:(m>0.0).then_some(t),specific_liquid_enthalpy_j_kg:(m>0.0).then_some(4218.0*(t-273.15)),
-            source_state_sha256:Some(digest('b')),
-        }).collect(),runon_parcels:vec![]};
-    f.forcing_sha256=f.canonical_sha256().unwrap(); f
+    let mut x = initial_trial();
+    x[8] = 300.0;
+    let r = crate::evaluate_covered_occupancy_block(
+        &c,
+        &c.occupancies[0],
+        &x[..10],
+        300.0,
+        0.01,
+        [0.0; 4],
+    )
+    .unwrap();
+    assert_eq!(
+        r[8], 0.0,
+        "transparent numerical coordinate retains canonical anchor; passage temperature is separate"
+    );
+}
+
+fn transparent_test_forcing(
+    c: &CoveredColumnInputs,
+    parcels: Vec<(f64, f64)>,
+) -> crate::LandSurfaceForcing {
+    let id = identity(c.interval_s);
+    let mut f = crate::LandSurfaceForcing {
+        forcing_sha256: digest('a'),
+        transaction_id: id.transaction_id,
+        interval_s: c.interval_s,
+        air_temperature_k: c.air_temperature_k,
+        air_specific_humidity_kg_kg: c.air_specific_humidity_kg_kg,
+        air_pressure_pa: c.pressure_pa,
+        reference_wind_m_s: c.reference_wind_m_s,
+        neutral_stability: true,
+        snow_present_at_beginning: false,
+        snow_present_at_end: false,
+        snow_terminal_payload_present: false,
+        direct_vis_w_m2: 0.0,
+        diffuse_vis_w_m2: 0.0,
+        direct_nir_w_m2: 0.0,
+        diffuse_nir_w_m2: 0.0,
+        atmospheric_downward_longwave_w_m2: c.atmospheric_downward_longwave_w_m2,
+        precipitation_parcels: parcels
+            .into_iter()
+            .enumerate()
+            .map(|(i, (m, t))| crate::LiquidParcel {
+                parcel_kind: crate::LiquidParcelKind::Precipitation,
+                parcel_id: crate::ParcelId::try_new(format!("rain-{i}")).unwrap(),
+                source_owner_id: owner("climate"),
+                source_ofe_id: id.ofe_id.clone(),
+                source_tile_id: id.tile_id.clone(),
+                destination_ofe_id: id.ofe_id.clone(),
+                destination_tile_id: id.tile_id.clone(),
+                start_s: 0.0,
+                end_s: c.interval_s,
+                amount_kg_m2_destination_tile_ground: m,
+                temperature_provider: crate::LiquidTemperatureProvider::HarderPomeroyHourly,
+                temperature_k: (m > 0.0).then_some(t),
+                specific_liquid_enthalpy_j_kg: (m > 0.0).then_some(4218.0 * (t - 273.15)),
+                source_state_sha256: Some(digest('b')),
+            })
+            .collect(),
+        runon_parcels: vec![],
+    };
+    f.forcing_sha256 = f.canonical_sha256().unwrap();
+    f
 }
 
 #[test]
 #[ignore = "isolated policy14 transparent source and two-rank full solve"]
 fn transparent_liquid_source_poisons_two_ranks_and_solver_parity() {
     crate::snow_accuracy_policy::Policy::B01.install().unwrap();
-    let mut c=column();
-    c.authority=CoveredColumnAuthority::V10NonpositiveAssimilation;
-    c.top_rain_kg_m2_tile=3.0;
-    let mut o=c.occupancies[0].clone();
-    o.lai=0.0;o.sai=0.0;o.sun.leaf_area_m2_m2_tile=0.0;o.shade.leaf_area_m2_m2_tile=0.0;
-    o.stem_area_m2_m2_tile=0.0;o.beginning_canopy_liquid_kg_m2_tile=0.0;
-    o.sun.absorbed_shortwave_w_m2_tile=0.0;o.shade.absorbed_shortwave_w_m2_tile=0.0;
-    o.stem_absorbed_shortwave_w_m2_tile=0.0;o.stemflow_fraction=0.2;
-    c.occupancies=vec![o.clone(),o]; c.occupancies[1].occupancy_id="canopy-rank-1".into();
-    c.occupancies[1].stemflow_fraction=0.3;
-    let ground=crate::partition_ground_shortwave(c.ground.terminal_shortwave_w_m2_tile,c.ground.surface_vis_albedo,c.ground.surface_nir_albedo).unwrap();
-    c.shortwave.incident_w_m2_tile=c.ground.terminal_shortwave_w_m2_tile;
-    c.shortwave.top_reflected_w_m2_tile=ground.reflected;
-    c.shortwave.ground_absorbed_by_incident_w_m2_tile=ground.absorbed;
-    c.shortwave.occupancies=c.occupancies.iter().map(|o|CoveredOccupancyShortwaveInputs{
-        occupancy_id:o.occupancy_id.clone(),sun_leaf_absorbed_w_m2_tile:BandDirectionalFluxes::default(),
-        shade_leaf_absorbed_w_m2_tile:BandDirectionalFluxes::default(),stem_absorbed_w_m2_tile:BandDirectionalFluxes::default()}).collect();
-    let source=transparent_test_forcing(&c,vec![(1.0,278.15),(2.0,288.15)]);
-    let id=identity(c.interval_s);
+    let mut c = column();
+    c.authority = CoveredColumnAuthority::V10NonpositiveAssimilation;
+    c.top_rain_kg_m2_tile = 3.0;
+    let mut o = c.occupancies[0].clone();
+    o.lai = 0.0;
+    o.sai = 0.0;
+    o.sun.leaf_area_m2_m2_tile = 0.0;
+    o.shade.leaf_area_m2_m2_tile = 0.0;
+    o.stem_area_m2_m2_tile = 0.0;
+    o.beginning_canopy_liquid_kg_m2_tile = 0.0;
+    o.sun.absorbed_shortwave_w_m2_tile = 0.0;
+    o.shade.absorbed_shortwave_w_m2_tile = 0.0;
+    o.stem_absorbed_shortwave_w_m2_tile = 0.0;
+    o.stemflow_fraction = 0.2;
+    c.occupancies = vec![o.clone(), o];
+    c.occupancies[1].occupancy_id = "canopy-rank-1".into();
+    c.occupancies[1].stemflow_fraction = 0.3;
+    let ground = crate::partition_ground_shortwave(
+        c.ground.terminal_shortwave_w_m2_tile,
+        c.ground.surface_vis_albedo,
+        c.ground.surface_nir_albedo,
+    )
+    .unwrap();
+    c.shortwave.incident_w_m2_tile = c.ground.terminal_shortwave_w_m2_tile;
+    c.shortwave.top_reflected_w_m2_tile = ground.reflected;
+    c.shortwave.ground_absorbed_by_incident_w_m2_tile = ground.absorbed;
+    c.shortwave.occupancies = c
+        .occupancies
+        .iter()
+        .map(|o| CoveredOccupancyShortwaveInputs {
+            occupancy_id: o.occupancy_id.clone(),
+            sun_leaf_absorbed_w_m2_tile: BandDirectionalFluxes::default(),
+            shade_leaf_absorbed_w_m2_tile: BandDirectionalFluxes::default(),
+            stem_absorbed_w_m2_tile: BandDirectionalFluxes::default(),
+        })
+        .collect();
+    let source = transparent_test_forcing(&c, vec![(1.0, 278.15), (2.0, 288.15)]);
+    let id = identity(c.interval_s);
     assert!(c.validate_transparent_liquid(Some(&id)).is_err());
-    c.bind_transparent_liquid(&source,&id).unwrap();
+    c.bind_transparent_liquid(&source, &id).unwrap();
     c.validate_transparent_liquid(Some(&id)).unwrap();
-    let boundary=c.transparent_liquid_boundary.as_ref().unwrap();
-    let expected_t=273.15+(1.0*5.0+2.0*15.0)/3.0;
-    assert_eq!(c.inactive_component_anchor_k(2,310.0),310.0);
-    let mut foreign=source.clone();foreign.precipitation_parcels[0].source_state_sha256=Some(digest('c'));
-    foreign.forcing_sha256=foreign.canonical_sha256().unwrap();
-    assert!(boundary.validate_against_actual_forcing(&foreign,&id.ofe_id,&id.tile_id,c.tile_fraction).is_err());
-    let mut foreign=transparent_test_forcing(&c,vec![(1.0,280.15),(2.0,287.15)]);
+    let boundary = c.transparent_liquid_boundary.as_ref().unwrap();
+    let expected_t = 273.15 + (1.0 * 5.0 + 2.0 * 15.0) / 3.0;
+    assert_eq!(c.inactive_component_anchor_k(2, 310.0), 310.0);
+    let mut foreign = source.clone();
+    foreign.precipitation_parcels[0].source_state_sha256 = Some(digest('c'));
+    foreign.forcing_sha256 = foreign.canonical_sha256().unwrap();
+    assert!(
+        boundary
+            .validate_against_actual_forcing(&foreign, &id.ofe_id, &id.tile_id, c.tile_fraction)
+            .is_err()
+    );
+    let mut foreign = transparent_test_forcing(&c, vec![(1.0, 280.15), (2.0, 287.15)]);
     // Same mass and same mixture temperature still has a different actual source.
-    assert!(boundary.validate_against_actual_forcing(&foreign,&id.ofe_id,&id.tile_id,c.tile_fraction).is_err());
+    assert!(
+        boundary
+            .validate_against_actual_forcing(&foreign, &id.ofe_id, &id.tile_id, c.tile_fraction)
+            .is_err()
+    );
     for poison in 0..9 {
-        foreign=source.clone();
+        foreign = source.clone();
         match poison {
-            0=>foreign.precipitation_parcels[0].temperature_k=None,
-            1=>foreign.precipitation_parcels[0].specific_liquid_enthalpy_j_kg=None,
-            2=>foreign.precipitation_parcels[0].source_state_sha256=None,
-            3=>foreign.precipitation_parcels[0].end_s=c.interval_s+60.0,
-            4=>foreign.precipitation_parcels.push(foreign.precipitation_parcels[0].clone()),
-            5=>foreign.precipitation_parcels[0].temperature_provider=crate::LiquidTemperatureProvider::AcceptedUpstreamOutletParcel,
-            6=>foreign.precipitation_parcels[0].specific_liquid_enthalpy_j_kg=Some(42.0),
-            7=>foreign.precipitation_parcels[0].amount_kg_m2_destination_tile_ground=0.0,
-            _=>foreign.precipitation_parcels[0].amount_kg_m2_destination_tile_ground=f64::from_bits(1),
+            0 => foreign.precipitation_parcels[0].temperature_k = None,
+            1 => foreign.precipitation_parcels[0].specific_liquid_enthalpy_j_kg = None,
+            2 => foreign.precipitation_parcels[0].source_state_sha256 = None,
+            3 => foreign.precipitation_parcels[0].end_s = c.interval_s + 60.0,
+            4 => foreign
+                .precipitation_parcels
+                .push(foreign.precipitation_parcels[0].clone()),
+            5 => {
+                foreign.precipitation_parcels[0].temperature_provider =
+                    crate::LiquidTemperatureProvider::AcceptedUpstreamOutletParcel
+            }
+            6 => foreign.precipitation_parcels[0].specific_liquid_enthalpy_j_kg = Some(42.0),
+            7 => foreign.precipitation_parcels[0].amount_kg_m2_destination_tile_ground = 0.0,
+            _ => {
+                foreign.precipitation_parcels[0].amount_kg_m2_destination_tile_ground =
+                    f64::from_bits(1)
+            }
         }
-        foreign.forcing_sha256=foreign.canonical_sha256().unwrap();
-        assert!(crate::TransparentCanopyLiquidBoundaryV1::try_new(&foreign,&id.ofe_id,&id.tile_id,c.tile_fraction).is_err(),"poison{poison}");
+        foreign.forcing_sha256 = foreign.canonical_sha256().unwrap();
+        assert!(
+            crate::TransparentCanopyLiquidBoundaryV1::try_new(
+                &foreign,
+                &id.ofe_id,
+                &id.tile_id,
+                c.tile_fraction
+            )
+            .is_err(),
+            "poison{poison}"
+        );
     }
     for poison in 0..5 {
-        let mut bad=c.clone();
-        match poison {0=>bad.top_rain_kg_m2_tile=4.0,1=>bad.interval_s+=60.0,
-            2=>bad.tile_fraction=0.5,3=>bad.occupancies[1].lai=1.0,
-            _=>bad.occupancies[1].beginning_canopy_liquid_kg_m2_tile=0.1}
-        assert!(bad.validate_transparent_liquid(Some(&id)).is_err(),"column poison{poison}");
-    }
-    let mut wrong=id.clone();wrong.transaction_id=TransactionId(92);
+        let mut bad = c.clone();
+        match poison {
+            0 => bad.top_rain_kg_m2_tile = 4.0,
+            1 => bad.interval_s += 60.0,
+            2 => bad.tile_fraction = 0.5,
+            3 => bad.occupancies[1].lai = 1.0,
+            _ => bad.occupancies[1].beginning_canopy_liquid_kg_m2_tile = 0.1,
+        }
+        assert!(
+            bad.validate_transparent_liquid(Some(&id)).is_err(),
+            "column poison{poison}"
+        );
+    }
+    let mut wrong = id.clone();
+    wrong.transaction_id = TransactionId(92);
     assert!(c.validate_transparent_liquid(Some(&wrong)).is_err());
-    wrong=id.clone();wrong.ofe_id=OfeId::try_new("foreign").unwrap();assert!(c.validate_transparent_liquid(Some(&wrong)).is_err());
-    wrong=id.clone();wrong.tile_id=TileId::try_new("foreign").unwrap();assert!(c.validate_transparent_liquid(Some(&wrong)).is_err());
-    let mut dry=c.clone();dry.top_rain_kg_m2_tile=0.0;dry.transparent_liquid_boundary=None;
-    dry.validate_transparent_liquid(Some(&id)).unwrap();assert_eq!(dry.inactive_component_anchor_k(2,300.0),300.0);
-    let mut x=initial_trial()[..10].to_vec();x.extend_from_slice(&initial_trial()[..10]);x.extend_from_slice(&initial_trial()[10..]);
+    wrong = id.clone();
+    wrong.ofe_id = OfeId::try_new("foreign").unwrap();
+    assert!(c.validate_transparent_liquid(Some(&wrong)).is_err());
+    wrong = id.clone();
+    wrong.tile_id = TileId::try_new("foreign").unwrap();
+    assert!(c.validate_transparent_liquid(Some(&wrong)).is_err());
+    let mut dry = c.clone();
+    dry.top_rain_kg_m2_tile = 0.0;
+    dry.transparent_liquid_boundary = None;
+    dry.validate_transparent_liquid(Some(&id)).unwrap();
+    assert_eq!(dry.inactive_component_anchor_k(2, 300.0), 300.0);
+    let mut x = initial_trial()[..10].to_vec();
+    x.extend_from_slice(&initial_trial()[..10]);
+    x.extend_from_slice(&initial_trial()[10..]);
     // Authentic warm066 zero-area hydraulic beginning, both ranks.
-    for base in [0,10] {x[base..base+4].fill(0.0);x[base+4..base+6].fill(1.0);}
-    x[8]=expected_t;x[18]=expected_t;
-    let detail=crate::evaluate_covered_column(&c,&x,None,None).unwrap();
-    let mut outgoing_h=0.0;let mut outgoing_m=0.0;let mut scale=0.0;
-    for (i,o) in detail.occupancies.iter().enumerate() {
-        let l=&o.liquid;
-        let m=l.stemflow_kg_m2_tile+if i+1==detail.occupancies.len(){l.throughfall_kg_m2_tile+l.initial_drainage_kg_m2_tile+l.second_drainage_kg_m2_tile}else{0.0};
-        outgoing_m+=m;outgoing_h+=m*4218.0*(l.wet_surface_temperature_k-273.15);
-        scale+=m*4218.0*(l.wet_surface_temperature_k.abs()+273.15);
-    }
-    let incoming_h:f64=4218.0*35.0;
-    scale+=1.0*4218.0*(278.15+273.15)+2.0*4218.0*(288.15+273.15)+incoming_h.abs()+outgoing_h.abs();
-    let n=64.0*(2.0+4.0*2.0+8.0);let nu=n*2.0_f64.powi(-53);let bound=nu/(1.0-nu)*scale;
-    assert!((outgoing_h-incoming_h).abs()<=bound);assert!((outgoing_m-3.0).abs()<=16.0*f64::EPSILON);
-    let mut perturbed=x.clone();perturbed[8]+=0.001;
-    let p=crate::evaluate_covered_column(&c,&perturbed,None,None).unwrap();
-    assert!(((p.raw_residuals[8]-detail.raw_residuals[8])/0.001-1.0).abs()<1e-9);
-    perturbed=x.clone();perturbed[20]+=0.001;
-    let p=crate::evaluate_covered_column(&c,&perturbed,None,None).unwrap();assert!(((p.raw_residuals[8]-detail.raw_residuals[8])/0.001+1.0).abs()<1e-9);
-    for guess in [280.0,305.0] {
-        let mut trial=x.clone();trial[8]=guess;trial[18]=guess;trial[20]=guess;
-        let ordinary=crate::solve_covered_column(&c,None,trial.clone()).unwrap();
-        let crate::CoveredColumnSolveOutcome::Accepted(a)=ordinary else{panic!("ordinary solve {ordinary:?}")};
-        assert!((a.solution[8]-a.solution[20].max(273.15)).abs()<1e-10);assert!((a.solution[18]-a.solution[20].max(273.15)).abs()<1e-10);
-        for o in &a.evaluation.occupancies {assert!((o.liquid.wet_surface_temperature_k-expected_t).abs()<1e-10);}
-        let native=crate::solve_v3_phase_free_covered_column(&c,None,&trial,crate::V3LitterResidualContext{
-            configuration:configuration(),beginning:litter_beginning(),finalized_vapor:None}).unwrap();
-        let crate::NormalizedSolveOutcome::Accepted{solution:native_solution,..}=native else {panic!("native solve {native:?}")};
-        assert!((native_solution[8]-native_solution[20].max(273.15)).abs()<1e-10);assert!((native_solution[18]-native_solution[20].max(273.15)).abs()<1e-10);
-    }
-    eprintln!("transparent independent mass={outgoing_m} energy={outgoing_h} expected={incoming_h} allowance={bound}");
+    for base in [0, 10] {
+        x[base..base + 4].fill(0.0);
+        x[base + 4..base + 6].fill(1.0);
+    }
+    x[8] = expected_t;
+    x[18] = expected_t;
+    let detail = crate::evaluate_covered_column(&c, &x, None, None).unwrap();
+    let mut outgoing_h = 0.0;
+    let mut outgoing_m = 0.0;
+    let mut scale = 0.0;
+    for (i, o) in detail.occupancies.iter().enumerate() {
+        let l = &o.liquid;
+        let m = l.stemflow_kg_m2_tile
+            + if i + 1 == detail.occupancies.len() {
+                l.throughfall_kg_m2_tile
+                    + l.initial_drainage_kg_m2_tile
+                    + l.second_drainage_kg_m2_tile
+            } else {
+                0.0
+            };
+        outgoing_m += m;
+        outgoing_h += m * 4218.0 * (l.wet_surface_temperature_k - 273.15);
+        scale += m * 4218.0 * (l.wet_surface_temperature_k.abs() + 273.15);
+    }
+    let incoming_h: f64 = 4218.0 * 35.0;
+    scale += 1.0 * 4218.0 * (278.15 + 273.15)
+        + 2.0 * 4218.0 * (288.15 + 273.15)
+        + incoming_h.abs()
+        + outgoing_h.abs();
+    let n = 64.0 * (2.0 + 4.0 * 2.0 + 8.0);
+    let nu = n * 2.0_f64.powi(-53);
+    let bound = nu / (1.0 - nu) * scale;
+    assert!((outgoing_h - incoming_h).abs() <= bound);
+    assert!((outgoing_m - 3.0).abs() <= 16.0 * f64::EPSILON);
+    let mut perturbed = x.clone();
+    perturbed[8] += 0.001;
+    let p = crate::evaluate_covered_column(&c, &perturbed, None, None).unwrap();
+    assert!(((p.raw_residuals[8] - detail.raw_residuals[8]) / 0.001 - 1.0).abs() < 1e-9);
+    perturbed = x.clone();
+    perturbed[20] += 0.001;
+    let p = crate::evaluate_covered_column(&c, &perturbed, None, None).unwrap();
+    assert!(((p.raw_residuals[8] - detail.raw_residuals[8]) / 0.001 + 1.0).abs() < 1e-9);
+    for guess in [280.0, 305.0] {
+        let mut trial = x.clone();
+        trial[8] = guess;
+        trial[18] = guess;
+        trial[20] = guess;
+        let ordinary = crate::solve_covered_column(&c, None, trial.clone()).unwrap();
+        let crate::CoveredColumnSolveOutcome::Accepted(a) = ordinary else {
+            panic!("ordinary solve {ordinary:?}")
+        };
+        assert!((a.solution[8] - a.solution[20].max(273.15)).abs() < 1e-10);
+        assert!((a.solution[18] - a.solution[20].max(273.15)).abs() < 1e-10);
+        for o in &a.evaluation.occupancies {
+            assert!((o.liquid.wet_surface_temperature_k - expected_t).abs() < 1e-10);
+        }
+        let native = crate::solve_v3_phase_free_covered_column(
+            &c,
+            None,
+            &trial,
+            crate::V3LitterResidualContext {
+                configuration: configuration(),
+                beginning: litter_beginning(),
+                finalized_vapor: None,
+            },
+        )
+        .unwrap();
+        let crate::NormalizedSolveOutcome::Accepted {
+            solution: native_solution,
+            ..
+        } = native
+        else {
+            panic!("native solve {native:?}")
+        };
+        assert!((native_solution[8] - native_solution[20].max(273.15)).abs() < 1e-10);
+        assert!((native_solution[18] - native_solution[20].max(273.15)).abs() < 1e-10);
+    }
+    eprintln!(
+        "transparent independent mass={outgoing_m} energy={outgoing_h} expected={incoming_h} allowance={bound}"
+    );
 }
 
 #[test]
 #[ignore = "isolated policy14 source-bound cold liquid passage"]
 fn transparent_cold_source_is_admitted_without_changing_numerical_anchor() {
     crate::snow_accuracy_policy::Policy::B01.install().unwrap();
-    let mut c=column(); c.top_rain_kg_m2_tile=9.400473273461845e-8;
+    let mut c = column();
+    c.top_rain_kg_m2_tile = 9.400473273461845e-8;
     for o in &mut c.occupancies {
-        o.lai=0.0;o.sai=0.0;o.sun.leaf_area_m2_m2_tile=0.0;o.shade.leaf_area_m2_m2_tile=0.0;
-        o.stem_area_m2_m2_tile=0.0;o.beginning_canopy_liquid_kg_m2_tile=0.0;
-    }
-    let f=transparent_test_forcing(&c,vec![(c.top_rain_kg_m2_tile,265.28179297231515)]);
-    let id=identity(c.interval_s); c.bind_transparent_liquid(&f,&id).unwrap();
-    assert_eq!(c.inactive_component_anchor_k(2,265.0),273.15);
-    assert_eq!(c.inactive_component_anchor_k(2,300.0),300.0);
-    for pass in [crate::CoveredLiquidPass::Potential,crate::CoveredLiquidPass::FixedAuthorizationFinal] {
-        let prep=crate::covered_liquid::prepare_covered_liquid(&c.occupancies[0],c.top_rain_kg_m2_tile).unwrap();
-        let ledger=crate::covered_liquid::finalize_covered_liquid_for_column(&c,&c.occupancies[0],prep,0.0,300.0,pass).unwrap();
-        assert_eq!(ledger.wet_surface_temperature_k,265.28179297231515);
-        c.validate_passage_ledger(&c.occupancies[0],&ledger).unwrap();
-        let mut missing=ledger;missing.transparent_passage=None;
-        assert!(c.validate_passage_ledger(&c.occupancies[0],&missing).is_err());
-        assert!(crate::covered_liquid::finalize_covered_liquid(prep,0.0,265.28179297231515,pass).is_err());
+        o.lai = 0.0;
+        o.sai = 0.0;
+        o.sun.leaf_area_m2_m2_tile = 0.0;
+        o.shade.leaf_area_m2_m2_tile = 0.0;
+        o.stem_area_m2_m2_tile = 0.0;
+        o.beginning_canopy_liquid_kg_m2_tile = 0.0;
+    }
+    let f = transparent_test_forcing(&c, vec![(c.top_rain_kg_m2_tile, 265.28179297231515)]);
+    let id = identity(c.interval_s);
+    c.bind_transparent_liquid(&f, &id).unwrap();
+    assert_eq!(c.inactive_component_anchor_k(2, 265.0), 273.15);
+    assert_eq!(c.inactive_component_anchor_k(2, 300.0), 300.0);
+    for pass in [
+        crate::CoveredLiquidPass::Potential,
+        crate::CoveredLiquidPass::FixedAuthorizationFinal,
+    ] {
+        let prep =
+            crate::covered_liquid::prepare_covered_liquid(&c.occupancies[0], c.top_rain_kg_m2_tile)
+                .unwrap();
+        let ledger = crate::covered_liquid::finalize_covered_liquid_for_column(
+            &c,
+            &c.occupancies[0],
+            prep,
+            0.0,
+            300.0,
+            pass,
+        )
+        .unwrap();
+        assert_eq!(ledger.wet_surface_temperature_k, 265.28179297231515);
+        c.validate_passage_ledger(&c.occupancies[0], &ledger)
+            .unwrap();
+        let mut missing = ledger;
+        missing.transparent_passage = None;
+        assert!(
+            c.validate_passage_ledger(&c.occupancies[0], &missing)
+                .is_err()
+        );
+        assert!(
+            crate::covered_liquid::finalize_covered_liquid(prep, 0.0, 265.28179297231515, pass)
+                .is_err()
+        );
     }
 }
 
@@ -734,23 +984,48 @@
 #[ignore = "isolated policy14 validated native configuration transition"]
 fn transparent_native_configuration_transition_rebinds_actual_source_and_refuses_foreign_join() {
     crate::snow_accuracy_policy::Policy::B01.install().unwrap();
-    let mut c=column();c.top_rain_kg_m2_tile=2.0;
+    let mut c = column();
+    c.top_rain_kg_m2_tile = 2.0;
     for o in &mut c.occupancies {
-        o.lai=0.0;o.sai=0.0;o.sun.leaf_area_m2_m2_tile=0.0;o.shade.leaf_area_m2_m2_tile=0.0;
-        o.stem_area_m2_m2_tile=0.0;o.beginning_canopy_liquid_kg_m2_tile=0.0;
-    }
-    let f=transparent_test_forcing(&c,vec![(2.0,265.28179297231515)]);
-    let old=identity(c.interval_s);c.bind_transparent_liquid(&f,&old).unwrap();
-    let proof=c.passage_proof(&c.occupancies[0]).unwrap();let before=c.clone();
-    let mut native=old.clone();native.configuration_sha256=digest('d');native.beginning_lse_state_sha256=digest('e');
+        o.lai = 0.0;
+        o.sai = 0.0;
+        o.sun.leaf_area_m2_m2_tile = 0.0;
+        o.shade.leaf_area_m2_m2_tile = 0.0;
+        o.stem_area_m2_m2_tile = 0.0;
+        o.beginning_canopy_liquid_kg_m2_tile = 0.0;
+    }
+    let f = transparent_test_forcing(&c, vec![(2.0, 265.28179297231515)]);
+    let old = identity(c.interval_s);
+    c.bind_transparent_liquid(&f, &old).unwrap();
+    let proof = c.passage_proof(&c.occupancies[0]).unwrap();
+    let before = c.clone();
+    let mut native = old.clone();
+    native.configuration_sha256 = digest('d');
+    native.beginning_lse_state_sha256 = digest('e');
     assert!(c.validate_transparent_liquid(Some(&native)).is_err());
-    let mut foreign=f.clone();foreign.precipitation_parcels[0].source_state_sha256=Some(digest('c'));
-    foreign.forcing_sha256=foreign.canonical_sha256().unwrap();
-    assert!(c.rebind_transparent_liquid_native_configuration(&foreign,&old,&native).is_err());assert_eq!(c,before);
-    let mut wrong=native.clone();wrong.transaction_id=TransactionId(old.transaction_id.0+1);
-    assert!(c.rebind_transparent_liquid_native_configuration(&f,&old,&wrong).is_err());assert_eq!(c,before);
-    c.rebind_transparent_liquid_native_configuration(&f,&old,&native).unwrap();
-    c.validate_transparent_liquid(Some(&native)).unwrap();assert!(c.validate_transparent_liquid(Some(&old)).is_err());
-    assert_ne!(proof,c.passage_proof(&c.occupancies[0]).unwrap());
-    c.transparent_liquid_boundary.as_ref().unwrap().validate_against_actual_forcing(&f,&native.ofe_id,&native.tile_id,c.tile_fraction).unwrap();
-}
+    let mut foreign = f.clone();
+    foreign.precipitation_parcels[0].source_state_sha256 = Some(digest('c'));
+    foreign.forcing_sha256 = foreign.canonical_sha256().unwrap();
+    assert!(
+        c.rebind_transparent_liquid_native_configuration(&foreign, &old, &native)
+            .is_err()
+    );
+    assert_eq!(c, before);
+    let mut wrong = native.clone();
+    wrong.transaction_id = TransactionId(old.transaction_id.0 + 1);
+    assert!(
+        c.rebind_transparent_liquid_native_configuration(&f, &old, &wrong)
+            .is_err()
+    );
+    assert_eq!(c, before);
+    c.rebind_transparent_liquid_native_configuration(&f, &old, &native)
+        .unwrap();
+    c.validate_transparent_liquid(Some(&native)).unwrap();
+    assert!(c.validate_transparent_liquid(Some(&old)).is_err());
+    assert_ne!(proof, c.passage_proof(&c.occupancies[0]).unwrap());
+    c.transparent_liquid_boundary
+        .as_ref()
+        .unwrap()
+        .validate_against_actual_forcing(&f, &native.ofe_id, &native.tile_id, c.tile_fraction)
+        .unwrap();
+}
--- a/crates/openwepp-land-surface-energy/src/transparent_canopy.rs
+++ b/crates/openwepp-land-surface-energy/src/transparent_canopy.rs
@@ -1,6 +1,8 @@
 //! Isolated policy14 transparent-column liquid enthalpy identity.
-use crate::{CoveredColumnInputs, LandSurfaceEnergyError, LandSurfaceForcing,
-    LiquidParcelKind, OfeId, RuntimeTileIdentity};
+use crate::{
+    CoveredColumnInputs, LandSurfaceEnergyError, LandSurfaceForcing, LiquidParcelKind, OfeId,
+    RuntimeTileIdentity,
+};
 use openwepp_kernel_contract::TileId;
 use sha2::{Digest, Sha256};
 
@@ -13,7 +15,7 @@
     tile_fraction: f64,
     forcing: LandSurfaceForcing,
     configuration_sha256: Option<crate::Sha256Digest>,
-    source_sha256: [u8;32],
+    source_sha256: [u8; 32],
     incoming_mass_kg_m2_tile: f64,
     incoming_enthalpy_j_m2_tile: f64,
     mixture_temperature_k: f64,
@@ -21,109 +23,194 @@
 
 pub(crate) fn normal(value: f64) -> Result<f64, LandSurfaceEnergyError> {
     if !value.is_finite() || (value != 0.0 && !value.is_normal()) {
-        return Err(LandSurfaceEnergyError::ConstitutiveDomain("transparent liquid arithmetic domain"));
+        return Err(LandSurfaceEnergyError::ConstitutiveDomain(
+            "transparent liquid arithmetic domain",
+        ));
     }
     Ok(value)
 }
-pub(crate) fn product(a:f64,b:f64)->Result<f64,LandSurfaceEnergyError> {
-    normal(a)?; normal(b)?;
-    let result=normal(a*b)?;
-    if a!=0.0 && b!=0.0 && result==0.0 {
-        return Err(LandSurfaceEnergyError::ConstitutiveDomain("transparent liquid product underflow"));
+pub(crate) fn product(a: f64, b: f64) -> Result<f64, LandSurfaceEnergyError> {
+    normal(a)?;
+    normal(b)?;
+    let result = normal(a * b)?;
+    if a != 0.0 && b != 0.0 && result == 0.0 {
+        return Err(LandSurfaceEnergyError::ConstitutiveDomain(
+            "transparent liquid product underflow",
+        ));
     }
     Ok(result)
 }
 
 impl TransparentCanopyLiquidBoundaryV1 {
-    pub fn try_new(forcing:&LandSurfaceForcing,ofe_id:&OfeId,tile_id:&TileId,tile_fraction:f64)
-        ->Result<Self,LandSurfaceEnergyError> {
+    pub fn try_new(
+        forcing: &LandSurfaceForcing,
+        ofe_id: &OfeId,
+        tile_id: &TileId,
+        tile_fraction: f64,
+    ) -> Result<Self, LandSurfaceEnergyError> {
         forcing.validate_policy14_transparent_material()?;
-        if !tile_fraction.is_finite() || tile_fraction<=0.0 || tile_fraction>1.0 {
-            return Err(LandSurfaceEnergyError::StateLineage("transparent liquid tile fraction"));
-        }
-        let mut mass=0.0;
-        let mut enthalpy=0.0;
-        for p in forcing.precipitation_parcels.iter().filter(|p| p.parcel_kind==LiquidParcelKind::Precipitation
-            && &p.destination_ofe_id==ofe_id && &p.destination_tile_id==tile_id) {
-            let m=normal(p.amount_kg_m2_destination_tile_ground)?;
-            mass=normal(mass+m)?;
-            if m>0.0 {
-                let h=p.specific_liquid_enthalpy_j_kg.ok_or(LandSurfaceEnergyError::StateLineage("transparent liquid missing enthalpy"))?;
-                enthalpy=normal(enthalpy+product(m,h)?)?;
+        if !tile_fraction.is_finite() || tile_fraction <= 0.0 || tile_fraction > 1.0 {
+            return Err(LandSurfaceEnergyError::StateLineage(
+                "transparent liquid tile fraction",
+            ));
+        }
+        let mut mass = 0.0;
+        let mut enthalpy = 0.0;
+        for p in forcing.precipitation_parcels.iter().filter(|p| {
+            p.parcel_kind == LiquidParcelKind::Precipitation
+                && &p.destination_ofe_id == ofe_id
+                && &p.destination_tile_id == tile_id
+        }) {
+            let m = normal(p.amount_kg_m2_destination_tile_ground)?;
+            mass = normal(mass + m)?;
+            if m > 0.0 {
+                let h =
+                    p.specific_liquid_enthalpy_j_kg
+                        .ok_or(LandSurfaceEnergyError::StateLineage(
+                            "transparent liquid missing enthalpy",
+                        ))?;
+                enthalpy = normal(enthalpy + product(m, h)?)?;
             }
         }
-        if mass<=0.0 { return Err(LandSurfaceEnergyError::StateLineage("transparent liquid missing positive input")); }
-        let denominator=product(4218.0,mass)?;
-        let delta=normal(enthalpy/denominator)?;
-        if enthalpy!=0.0 && delta==0.0 {
-            return Err(LandSurfaceEnergyError::ConstitutiveDomain("transparent liquid division underflow"));
-        }
-        let temperature=normal(273.15+delta)?;
+        if mass <= 0.0 {
+            return Err(LandSurfaceEnergyError::StateLineage(
+                "transparent liquid missing positive input",
+            ));
+        }
+        let denominator = product(4218.0, mass)?;
+        let delta = normal(enthalpy / denominator)?;
+        if enthalpy != 0.0 && delta == 0.0 {
+            return Err(LandSurfaceEnergyError::ConstitutiveDomain(
+                "transparent liquid division underflow",
+            ));
+        }
+        let temperature = normal(273.15 + delta)?;
         if !(200.0..=350.0).contains(&temperature) {
-            return Err(LandSurfaceEnergyError::UnsupportedDomain("transparent liquid material temperature"));
-        }
-        let source_bytes=serde_json::to_vec(&(forcing,ofe_id,tile_id,tile_fraction))
-            .map_err(|_|LandSurfaceEnergyError::StateLineage("transparent source encoding"))?;
-        let source_sha256=Sha256::digest(source_bytes).into();
-        Ok(Self{policy_id:14,ofe_id:ofe_id.clone(),tile_id:tile_id.clone(),tile_fraction,
-            forcing:forcing.clone(),configuration_sha256:None,source_sha256,incoming_mass_kg_m2_tile:mass,
-            incoming_enthalpy_j_m2_tile:enthalpy,mixture_temperature_k:temperature})
-    }
-    pub fn validate_against_actual_forcing(&self,forcing:&LandSurfaceForcing,ofe:&OfeId,tile:&TileId,fraction:f64)
-        ->Result<(),LandSurfaceEnergyError> {
-        let mut expected=Self::try_new(forcing,ofe,tile,fraction)?;
-        expected.configuration_sha256=self.configuration_sha256.clone();
-        if self != &expected { return Err(LandSurfaceEnergyError::StateLineage("transparent liquid actual source binding")); }
+            return Err(LandSurfaceEnergyError::UnsupportedDomain(
+                "transparent liquid material temperature",
+            ));
+        }
+        let source_bytes = serde_json::to_vec(&(forcing, ofe_id, tile_id, tile_fraction))
+            .map_err(|_| LandSurfaceEnergyError::StateLineage("transparent source encoding"))?;
+        let source_sha256 = Sha256::digest(source_bytes).into();
+        Ok(Self {
+            policy_id: 14,
+            ofe_id: ofe_id.clone(),
+            tile_id: tile_id.clone(),
+            tile_fraction,
+            forcing: forcing.clone(),
+            configuration_sha256: None,
+            source_sha256,
+            incoming_mass_kg_m2_tile: mass,
+            incoming_enthalpy_j_m2_tile: enthalpy,
+            mixture_temperature_k: temperature,
+        })
+    }
+    pub fn validate_against_actual_forcing(
+        &self,
+        forcing: &LandSurfaceForcing,
+        ofe: &OfeId,
+        tile: &TileId,
+        fraction: f64,
+    ) -> Result<(), LandSurfaceEnergyError> {
+        let mut expected = Self::try_new(forcing, ofe, tile, fraction)?;
+        expected.configuration_sha256 = self.configuration_sha256.clone();
+        if self != &expected {
+            return Err(LandSurfaceEnergyError::StateLineage(
+                "transparent liquid actual source binding",
+            ));
+        }
         Ok(())
     }
-    fn validate(&self)->Result<(),LandSurfaceEnergyError> {
-        self.validate_against_actual_forcing(&self.forcing,&self.ofe_id,&self.tile_id,self.tile_fraction)
+    fn validate(&self) -> Result<(), LandSurfaceEnergyError> {
+        self.validate_against_actual_forcing(
+            &self.forcing,
+            &self.ofe_id,
+            &self.tile_id,
+            self.tile_fraction,
+        )
     }
 }
 
 impl CoveredColumnInputs {
-    pub fn is_wholly_transparent_canopy(&self)->bool {
-        !self.occupancies.is_empty() && self.occupancies.iter().all(|o|
-            [o.lai,o.sai,o.sun.leaf_area_m2_m2_tile,o.shade.leaf_area_m2_m2_tile,
-             o.stem_area_m2_m2_tile,o.beginning_canopy_liquid_kg_m2_tile].iter().all(|v| *v==0.0))
-    }
-    pub fn bind_transparent_liquid(&mut self,forcing:&LandSurfaceForcing,identity:&RuntimeTileIdentity)
-        ->Result<(),LandSurfaceEnergyError> {
-        if crate::snow_accuracy_policy::Policy::current().identity()==Some(14)
-            && self.is_wholly_transparent_canopy() && self.top_rain_kg_m2_tile>0.0 {
-            let mut boundary=TransparentCanopyLiquidBoundaryV1::try_new(forcing,&identity.ofe_id,&identity.tile_id,self.tile_fraction)?;
-            boundary.configuration_sha256=Some(identity.configuration_sha256.clone());
-            self.transparent_liquid_boundary=Some(Box::new(boundary));
+    pub fn is_wholly_transparent_canopy(&self) -> bool {
+        !self.occupancies.is_empty()
+            && self.occupancies.iter().all(|o| {
+                [
+                    o.lai,
+                    o.sai,
+                    o.sun.leaf_area_m2_m2_tile,
+                    o.shade.leaf_area_m2_m2_tile,
+                    o.stem_area_m2_m2_tile,
+                    o.beginning_canopy_liquid_kg_m2_tile,
+                ]
+                .iter()
+                .all(|v| *v == 0.0)
+            })
+    }
+    pub fn bind_transparent_liquid(
+        &mut self,
+        forcing: &LandSurfaceForcing,
+        identity: &RuntimeTileIdentity,
+    ) -> Result<(), LandSurfaceEnergyError> {
+        if crate::snow_accuracy_policy::Policy::current().identity() == Some(14)
+            && self.is_wholly_transparent_canopy()
+            && self.top_rain_kg_m2_tile > 0.0
+        {
+            let mut boundary = TransparentCanopyLiquidBoundaryV1::try_new(
+                forcing,
+                &identity.ofe_id,
+                &identity.tile_id,
+                self.tile_fraction,
+            )?;
+            boundary.configuration_sha256 = Some(identity.configuration_sha256.clone());
+            self.transparent_liquid_boundary = Some(Box::new(boundary));
         }
         self.validate_transparent_liquid(Some(identity))
     }
-    pub fn validate_transparent_liquid(&self,identity:Option<&RuntimeTileIdentity>)->Result<(),LandSurfaceEnergyError> {
-        let eligible=crate::snow_accuracy_policy::Policy::current().identity()==Some(14)
-            && self.is_wholly_transparent_canopy() && self.top_rain_kg_m2_tile>0.0;
-        match (&self.transparent_liquid_boundary,eligible) {
-            (None,false)=>Ok(()),
-            (Some(b),true)=>{
+    pub fn validate_transparent_liquid(
+        &self,
+        identity: Option<&RuntimeTileIdentity>,
+    ) -> Result<(), LandSurfaceEnergyError> {
+        let eligible = crate::snow_accuracy_policy::Policy::current().identity() == Some(14)
+            && self.is_wholly_transparent_canopy()
+            && self.top_rain_kg_m2_tile > 0.0;
+        match (&self.transparent_liquid_boundary, eligible) {
+            (None, false) => Ok(()),
+            (Some(b), true) => {
                 b.validate()?;
-                if b.incoming_mass_kg_m2_tile.to_bits()!=self.top_rain_kg_m2_tile.to_bits()
-                    || b.tile_fraction.to_bits()!=self.tile_fraction.to_bits()
-                    || b.forcing.interval_s.to_bits()!=self.interval_s.to_bits() {
-                    return Err(LandSurfaceEnergyError::StateLineage("transparent liquid column mass/support/fraction"));
+                if b.incoming_mass_kg_m2_tile.to_bits() != self.top_rain_kg_m2_tile.to_bits()
+                    || b.tile_fraction.to_bits() != self.tile_fraction.to_bits()
+                    || b.forcing.interval_s.to_bits() != self.interval_s.to_bits()
+                {
+                    return Err(LandSurfaceEnergyError::StateLineage(
+                        "transparent liquid column mass/support/fraction",
+                    ));
                 }
-                if let Some(id)=identity {
-                    if b.configuration_sha256.as_ref()!=Some(&id.configuration_sha256) || b.ofe_id!=id.ofe_id || b.tile_id!=id.tile_id || b.forcing.transaction_id!=id.transaction_id
-                        || b.forcing.interval_s.to_bits()!=id.interval_s.to_bits() || b.tile_fraction.to_bits()!=id.tile_fraction.to_bits() {
-                        return Err(LandSurfaceEnergyError::StateLineage("transparent liquid runtime identity"));
+                if let Some(id) = identity {
+                    if b.configuration_sha256.as_ref() != Some(&id.configuration_sha256)
+                        || b.ofe_id != id.ofe_id
+                        || b.tile_id != id.tile_id
+                        || b.forcing.transaction_id != id.transaction_id
+                        || b.forcing.interval_s.to_bits() != id.interval_s.to_bits()
+                        || b.tile_fraction.to_bits() != id.tile_fraction.to_bits()
+                    {
+                        return Err(LandSurfaceEnergyError::StateLineage(
+                            "transparent liquid runtime identity",
+                        ));
                     }
                 }
                 Ok(())
-            },
-            _=>Err(LandSurfaceEnergyError::StateLineage("transparent liquid boundary policy/eligibility/presence")),
+            }
+            _ => Err(LandSurfaceEnergyError::StateLineage(
+                "transparent liquid boundary policy/eligibility/presence",
+            )),
         }
     }
     // Called only after immutable input admission. This same exact anchor is
     // used by the residual and both ordinary/native inactive Jacobian rows.
-    pub(crate) fn inactive_component_anchor_k(&self,component:usize,canopy:f64)->f64 {
-        crate::solver::inactive_component_temperature_anchor_k(component,canopy)
+    pub(crate) fn inactive_component_anchor_k(&self, component: usize, canopy: f64) -> f64 {
+        crate::solver::inactive_component_temperature_anchor_k(component, canopy)
     }
 }
 
@@ -131,67 +218,131 @@
 #[derive(Clone, Copy, Debug, PartialEq, serde::Serialize, serde::Deserialize)]
 #[serde(deny_unknown_fields)]
 pub struct TransparentCanopyPassageProofV1 {
-    boundary_sha256: [u8;32],
-    occupancy_sha256: [u8;32],
+    boundary_sha256: [u8; 32],
+    occupancy_sha256: [u8; 32],
     rank: u32,
     top_mass_kg_m2_tile: f64,
     top_enthalpy_j_m2_tile: f64,
     raw_mixture_temperature_k: f64,
 }
 impl TransparentCanopyPassageProofV1 {
-    pub(crate) fn release_temperature_k(self)->f64 {
-        crate::covered_liquid::canonical_liquid_reference_temperature_k(self.raw_mixture_temperature_k)
-    }
-    pub(crate) fn validate_ledger(self, l:&crate::CoveredOccupancyLiquidLedger)->Result<(),LandSurfaceEnergyError> {
-        if self.top_mass_kg_m2_tile<=0.0 || !(200.0..=350.0).contains(&self.raw_mixture_temperature_k)
-            || l.wet_surface_temperature_k.to_bits()!=self.release_temperature_k().to_bits()
-            || [l.beginning_store_kg_m2_tile,l.ending_store_kg_m2_tile,l.evaporation_kg_m2_tile,
-                l.condensation_kg_m2_tile,l.initial_drainage_kg_m2_tile,l.second_drainage_kg_m2_tile,l.wet_fraction].iter().any(|x| *x!=0.0) {
-            return Err(LandSurfaceEnergyError::StateLineage("transparent passage ledger posture"));
-        }
-        for m in [self.top_mass_kg_m2_tile,l.incident_rain_kg_m2_tile,l.throughfall_kg_m2_tile,l.stemflow_kg_m2_tile] {
+    pub(crate) fn release_temperature_k(self) -> f64 {
+        crate::covered_liquid::canonical_liquid_reference_temperature_k(
+            self.raw_mixture_temperature_k,
+        )
+    }
+    pub(crate) fn validate_ledger(
+        self,
+        l: &crate::CoveredOccupancyLiquidLedger,
+    ) -> Result<(), LandSurfaceEnergyError> {
+        if self.top_mass_kg_m2_tile <= 0.0
+            || !(200.0..=350.0).contains(&self.raw_mixture_temperature_k)
+            || l.wet_surface_temperature_k.to_bits() != self.release_temperature_k().to_bits()
+            || [
+                l.beginning_store_kg_m2_tile,
+                l.ending_store_kg_m2_tile,
+                l.evaporation_kg_m2_tile,
+                l.condensation_kg_m2_tile,
+                l.initial_drainage_kg_m2_tile,
+                l.second_drainage_kg_m2_tile,
+                l.wet_fraction,
+            ]
+            .iter()
+            .any(|x| *x != 0.0)
+        {
+            return Err(LandSurfaceEnergyError::StateLineage(
+                "transparent passage ledger posture",
+            ));
+        }
+        for m in [
+            self.top_mass_kg_m2_tile,
+            l.incident_rain_kg_m2_tile,
+            l.throughfall_kg_m2_tile,
+            l.stemflow_kg_m2_tile,
+        ] {
             normal(m)?;
-            product(m,l.wet_surface_specific_enthalpy_j_kg)?;
+            product(m, l.wet_surface_specific_enthalpy_j_kg)?;
         }
         normal(self.top_enthalpy_j_m2_tile)?;
         Ok(())
     }
 }
 impl CoveredColumnInputs {
-    pub(crate) fn passage_proof(&self, occupancy:&crate::CoveredOccupancyInputs)
-        ->Result<Option<TransparentCanopyPassageProofV1>,LandSurfaceEnergyError> {
-        let eligible=crate::snow_accuracy_policy::Policy::current().identity()==Some(14)
-            && self.is_wholly_transparent_canopy() && self.top_rain_kg_m2_tile>0.0;
-        if eligible!=self.transparent_liquid_boundary.is_some() {
-            return Err(LandSurfaceEnergyError::StateLineage("transparent passage required proof posture"));
-        }
-        let Some(b)=&self.transparent_liquid_boundary else { return Ok(None); };
+    pub(crate) fn passage_proof(
+        &self,
+        occupancy: &crate::CoveredOccupancyInputs,
+    ) -> Result<Option<TransparentCanopyPassageProofV1>, LandSurfaceEnergyError> {
+        let eligible = crate::snow_accuracy_policy::Policy::current().identity() == Some(14)
+            && self.is_wholly_transparent_canopy()
+            && self.top_rain_kg_m2_tile > 0.0;
+        if eligible != self.transparent_liquid_boundary.is_some() {
+            return Err(LandSurfaceEnergyError::StateLineage(
+                "transparent passage required proof posture",
+            ));
+        }
+        let Some(b) = &self.transparent_liquid_boundary else {
+            return Ok(None);
+        };
         if !self.is_wholly_transparent_canopy() || b.configuration_sha256.is_none() {
-            return Err(LandSurfaceEnergyError::StateLineage("transparent passage beginning eligibility"));
-        }
-        let matches=self.occupancies.iter().enumerate().filter(|(_,o)|o.occupancy_id==occupancy.occupancy_id).collect::<Vec<_>>();
-        if matches.len()!=1 || matches[0].1!=occupancy {
-            return Err(LandSurfaceEnergyError::StateLineage("transparent passage occupancy identity"));
-        }
-        let rank=u32::try_from(matches[0].0).map_err(|_|LandSurfaceEnergyError::StateLineage("transparent passage rank"))?;
-        let configuration=b.configuration_sha256.as_ref().ok_or(LandSurfaceEnergyError::StateLineage("transparent passage configuration"))?;
-        let mut hash=Sha256::new();hash.update(b.source_sha256);hash.update(configuration.as_str().as_bytes());
+            return Err(LandSurfaceEnergyError::StateLineage(
+                "transparent passage beginning eligibility",
+            ));
+        }
+        let matches = self
+            .occupancies
+            .iter()
+            .enumerate()
+            .filter(|(_, o)| o.occupancy_id == occupancy.occupancy_id)
+            .collect::<Vec<_>>();
+        if matches.len() != 1 || matches[0].1 != occupancy {
+            return Err(LandSurfaceEnergyError::StateLineage(
+                "transparent passage occupancy identity",
+            ));
+        }
+        let rank = u32::try_from(matches[0].0)
+            .map_err(|_| LandSurfaceEnergyError::StateLineage("transparent passage rank"))?;
+        let configuration =
+            b.configuration_sha256
+                .as_ref()
+                .ok_or(LandSurfaceEnergyError::StateLineage(
+                    "transparent passage configuration",
+                ))?;
+        let mut hash = Sha256::new();
+        hash.update(b.source_sha256);
+        hash.update(configuration.as_str().as_bytes());
         Ok(Some(TransparentCanopyPassageProofV1 {
-            boundary_sha256:hash.finalize().into(),occupancy_sha256:Sha256::digest(occupancy.occupancy_id.as_bytes()).into(),rank,
-            top_mass_kg_m2_tile:b.incoming_mass_kg_m2_tile,top_enthalpy_j_m2_tile:b.incoming_enthalpy_j_m2_tile,
-            raw_mixture_temperature_k:b.mixture_temperature_k,
+            boundary_sha256: hash.finalize().into(),
+            occupancy_sha256: Sha256::digest(occupancy.occupancy_id.as_bytes()).into(),
+            rank,
+            top_mass_kg_m2_tile: b.incoming_mass_kg_m2_tile,
+            top_enthalpy_j_m2_tile: b.incoming_enthalpy_j_m2_tile,
+            raw_mixture_temperature_k: b.mixture_temperature_k,
         }))
     }
-    pub(crate) fn validate_passage_ledger(&self,occupancy:&crate::CoveredOccupancyInputs,l:&crate::CoveredOccupancyLiquidLedger)
-        ->Result<(),LandSurfaceEnergyError> {
-        if l.transparent_passage!=self.passage_proof(occupancy)? {
-            return Err(LandSurfaceEnergyError::StateLineage("transparent passage actual source/rank proof"));
+    pub(crate) fn validate_passage_ledger(
+        &self,
+        occupancy: &crate::CoveredOccupancyInputs,
+        l: &crate::CoveredOccupancyLiquidLedger,
+    ) -> Result<(), LandSurfaceEnergyError> {
+        if l.transparent_passage != self.passage_proof(occupancy)? {
+            return Err(LandSurfaceEnergyError::StateLineage(
+                "transparent passage actual source/rank proof",
+            ));
         }
         l.validate()?;
         if l.transparent_passage.is_some() {
-            if l.stemflow_kg_m2_tile.to_bits()!=product(occupancy.stemflow_fraction,l.incident_rain_kg_m2_tile)?.to_bits()
-                || l.throughfall_kg_m2_tile.to_bits()!=product(1.0-occupancy.stemflow_fraction,l.incident_rain_kg_m2_tile)?.to_bits() {
-                return Err(LandSurfaceEnergyError::StateLineage("transparent passage unchanged E04 splits"));
+            if l.stemflow_kg_m2_tile.to_bits()
+                != product(occupancy.stemflow_fraction, l.incident_rain_kg_m2_tile)?.to_bits()
+                || l.throughfall_kg_m2_tile.to_bits()
+                    != product(
+                        1.0 - occupancy.stemflow_fraction,
+                        l.incident_rain_kg_m2_tile,
+                    )?
+                    .to_bits()
+            {
+                return Err(LandSurfaceEnergyError::StateLineage(
+                    "transparent passage unchanged E04 splits",
+                ));
             }
         }
         Ok(())
@@ -202,22 +353,32 @@
     /// Trusted adapter transition after validating the actual native config/state.
     /// Every non-configuration/runtime-source identity remains exactly unchanged.
     pub fn rebind_transparent_liquid_native_configuration(
-        &mut self,forcing:&LandSurfaceForcing,predecessor:&RuntimeTileIdentity,native:&RuntimeTileIdentity,
-    )->Result<(),LandSurfaceEnergyError> {
+        &mut self,
+        forcing: &LandSurfaceForcing,
+        predecessor: &RuntimeTileIdentity,
+        native: &RuntimeTileIdentity,
+    ) -> Result<(), LandSurfaceEnergyError> {
         self.validate_transparent_liquid(Some(predecessor))?;
-        if let Some(boundary)=&self.transparent_liquid_boundary {
-            boundary.validate_against_actual_forcing(forcing,&predecessor.ofe_id,&predecessor.tile_id,predecessor.tile_fraction)?;
-        }
-        let mut permitted=predecessor.clone();
-        permitted.configuration_sha256=native.configuration_sha256.clone();
-        permitted.beginning_lse_state_sha256=native.beginning_lse_state_sha256.clone();
-        if permitted!=*native {
-            return Err(LandSurfaceEnergyError::StateLineage("transparent native configuration transition identity"));
-        }
-        let mut candidate=self.clone();
-        candidate.bind_transparent_liquid(forcing,native)?;
+        if let Some(boundary) = &self.transparent_liquid_boundary {
+            boundary.validate_against_actual_forcing(
+                forcing,
+                &predecessor.ofe_id,
+                &predecessor.tile_id,
+                predecessor.tile_fraction,
+            )?;
+        }
+        let mut permitted = predecessor.clone();
+        permitted.configuration_sha256 = native.configuration_sha256.clone();
+        permitted.beginning_lse_state_sha256 = native.beginning_lse_state_sha256.clone();
+        if permitted != *native {
+            return Err(LandSurfaceEnergyError::StateLineage(
+                "transparent native configuration transition identity",
+            ));
+        }
+        let mut candidate = self.clone();
+        candidate.bind_transparent_liquid(forcing, native)?;
         candidate.validate_transparent_liquid(Some(native))?;
-        *self=candidate;
+        *self = candidate;
         Ok(())
     }
 }
--- a/crates/openwepp-persisted-restart-v1/src/checkpoint.rs
+++ b/crates/openwepp-persisted-restart-v1/src/checkpoint.rs
@@ -15,22 +15,27 @@
 use openwepp_hillslope_orchestrator::{
     DirectDayConstructorInputs, DirectPhasePlan, DirectRunFrame, DirectSurfaceLiquidConfiguration,
 };
-use openwepp_land_surface_energy::{
-    LandSurfaceEnergyConfiguration, LandSurfaceEnergyV2State,
-};
+use openwepp_land_surface_energy::{LandSurfaceEnergyConfiguration, LandSurfaceEnergyV2State};
 use openwepp_vegetation::{V10CoupledOwnedState, VegetationConfiguration};
 use serde::{Deserialize, Serialize};
 use thiserror::Error;
 
-use openwepp_hillslope_orchestrator::v9_real_consumer_shadow::{DirectSoilThermalResident, DirectV10SoilThermalResidentV2};
+use openwepp_hillslope_orchestrator::v9_real_consumer_shadow::{
+    DirectSoilThermalResident, DirectV10SoilThermalResidentV2,
+};
 
 /// Explicit admission selected by the enclosing native workflow, never by payload posture.
 #[derive(Clone, Copy, Debug, PartialEq, Eq)]
-pub(crate) enum ScientificAdmission { Ordinary, NativeB01 }
+pub(crate) enum ScientificAdmission {
+    Ordinary,
+    NativeB01,
+}
 impl ScientificAdmission {
     pub(crate) fn validate(self) -> Result<(), RestartAdmissionFailureV1> {
         if self == Self::NativeB01
-            && openwepp_land_surface_energy::snow_accuracy_policy::Policy::current().identity() != Some(14) {
+            && openwepp_land_surface_energy::snow_accuracy_policy::Policy::current().identity()
+                != Some(14)
+        {
             return Err(RestartAdmissionFailureV1::Schema);
         }
         Ok(())
@@ -43,39 +48,81 @@
     pub resident_bytes: Vec<u8>,
 }
 impl NativeSoilResidentRestartV2 {
-    pub(crate) fn restore_resident(&self)->Result<DirectV10SoilThermalResidentV2,RestartAdmissionFailureV1> {
+    pub(crate) fn restore_resident(
+        &self,
+    ) -> Result<DirectV10SoilThermalResidentV2, RestartAdmissionFailureV1> {
         if self.schema != "OPENWEPP_B01_NATIVE_SOIL_RESIDENT_V2_POLICY14" {
             return Err(RestartAdmissionFailureV1::OwnerValidation);
         }
         // Decode the complete native owner outside the phase enum's internally
         // tagged serde Content buffer, which cannot deserialize u128 fields.
         // Canonical bytes and full native semantics remain mandatory.
-        let resident:DirectV10SoilThermalResidentV2 = from_canonical_bytes(&self.resident_bytes)
-            .map_err(|_|RestartAdmissionFailureV1::OwnerValidation)?;
-        DirectSoilThermalResident::V2(resident.clone()).validate()
-            .map_err(|_|RestartAdmissionFailureV1::OwnerValidation)?;
+        let resident: DirectV10SoilThermalResidentV2 =
+            from_canonical_bytes(&self.resident_bytes)
+                .map_err(|_| RestartAdmissionFailureV1::OwnerValidation)?;
+        DirectSoilThermalResident::V2(resident.clone())
+            .validate()
+            .map_err(|_| RestartAdmissionFailureV1::OwnerValidation)?;
         Ok(resident)
     }
 }
 impl ScientificOwnerStateSetV1 {
-    pub(crate) fn validate_soil_posture(&self, admission: ScientificAdmission) -> Result<(), RestartAdmissionFailureV1> {
+    pub(crate) fn validate_soil_posture(
+        &self,
+        admission: ScientificAdmission,
+    ) -> Result<(), RestartAdmissionFailureV1> {
         admission.validate()?;
         match (&self.soil_thermal, &self.native_soil) {
             (Some(_), None) => Ok(()),
-            (None, Some(native)) if admission == ScientificAdmission::NativeB01
-                && native.schema == "OPENWEPP_B01_NATIVE_SOIL_RESIDENT_V2_POLICY14" =>
-                native.restore_resident().map(|_|()),
+            (None, Some(native))
+                if admission == ScientificAdmission::NativeB01
+                    && native.schema == "OPENWEPP_B01_NATIVE_SOIL_RESIDENT_V2_POLICY14" =>
+            {
+                native.restore_resident().map(|_| ())
+            }
             _ => Err(RestartAdmissionFailureV1::OwnerOmission),
         }
     }
-    pub(crate) fn soil_layer_maps(&self, admission: ScientificAdmission) -> Result<Vec<(String, Vec<String>)>, RestartAdmissionFailureV1> {
+    pub(crate) fn soil_layer_maps(
+        &self,
+        admission: ScientificAdmission,
+    ) -> Result<Vec<(String, Vec<String>)>, RestartAdmissionFailureV1> {
         self.validate_soil_posture(admission)?;
         if let Some(v1) = &self.soil_thermal {
-            return Ok(v1.ofes.iter().map(|ofe| (ofe.ofe_id.clone(), ofe.ordered_layers.iter().map(|layer| layer.layer_id.clone()).collect())).collect());
-        }
-        let native = self.native_soil.as_ref().ok_or(RestartAdmissionFailureV1::OwnerOmission)?;
-        Ok(native.restore_resident()?.owner().state.ofes.iter().map(|ofe| (ofe.ofe_id.as_str().to_owned(),
-            ofe.ordered_layers.iter().map(|layer| layer.layer_id.as_str().to_owned()).collect())).collect())
+            return Ok(v1
+                .ofes
+                .iter()
+                .map(|ofe| {
+                    (
+                        ofe.ofe_id.clone(),
+                        ofe.ordered_layers
+                            .iter()
+                            .map(|layer| layer.layer_id.clone())
+                            .collect(),
+                    )
+                })
+                .collect());
+        }
+        let native = self
+            .native_soil
+            .as_ref()
+            .ok_or(RestartAdmissionFailureV1::OwnerOmission)?;
+        Ok(native
+            .restore_resident()?
+            .owner()
+            .state
+            .ofes
+            .iter()
+            .map(|ofe| {
+                (
+                    ofe.ofe_id.as_str().to_owned(),
+                    ofe.ordered_layers
+                        .iter()
+                        .map(|layer| layer.layer_id.as_str().to_owned())
+                        .collect(),
+                )
+            })
+            .collect())
     }
 }
 
@@ -338,7 +385,9 @@
     admit_checkpoint_with_soil(bytes, context, ScientificAdmission::Ordinary)
 }
 pub(crate) fn admit_checkpoint_with_soil(
-    bytes: &[u8], context: &ExpectedRestartStaticContext<'_>, admission: ScientificAdmission,
+    bytes: &[u8],
+    context: &ExpectedRestartStaticContext<'_>,
+    admission: ScientificAdmission,
 ) -> Result<IsolatedRestoredCheckpointV1, RestartAdmissionFailureV1> {
     admission.validate()?;
     let checkpoint: DirectV10RealConsumerCheckpointV1 =
@@ -347,51 +396,54 @@
                 eprintln!("native restart checkpoint canonical admission: {error:?}");
             }
             match error {
-            crate::CanonicalJsonError::Parse(message) if message.contains("duplicate field") => {
-                RestartAdmissionFailureV1::DuplicateField
+                crate::CanonicalJsonError::Parse(message)
+                    if message.contains("duplicate field") =>
+                {
+                    RestartAdmissionFailureV1::DuplicateField
+                }
+                crate::CanonicalJsonError::Typed(message) if message.contains("missing field") => {
+                    if [
+                        "vegetation_v10",
+                        "lse_v2",
+                        "direct_hydrology",
+                        "soil_thermal",
+                        "biogeochemistry",
+                        "gsi_configuration",
+                        "gsi_state",
+                        "static_forcing_configuration",
+                        "provider_cursor",
+                        "surface_liquid_configuration",
+                    ]
+                    .iter()
+                    .any(|field| message.contains(field))
+                    {
+                        RestartAdmissionFailureV1::OwnerOmission
+                    } else {
+                        RestartAdmissionFailureV1::MissingField
+                    }
+                }
+                crate::CanonicalJsonError::Typed(message) if message.contains("unknown field") => {
+                    RestartAdmissionFailureV1::ExtraField
+                }
+                crate::CanonicalJsonError::Typed(message) if message.contains("HexU128") => {
+                    RestartAdmissionFailureV1::TransactionLineage
+                }
+                crate::CanonicalJsonError::Typed(message)
+                    if message.contains("u64") || message.contains("day index") =>
+                {
+                    RestartAdmissionFailureV1::SchedulerPosition
+                }
+                crate::CanonicalJsonError::NoncanonicalBytes
+                    if bytes.iter().any(u8::is_ascii_whitespace) =>
+                {
+                    RestartAdmissionFailureV1::NoncanonicalBytes
+                }
+                crate::CanonicalJsonError::NoncanonicalBytes => {
+                    RestartAdmissionFailureV1::ReorderedField
+                }
+                _ => RestartAdmissionFailureV1::NoncanonicalBytes,
             }
-            crate::CanonicalJsonError::Typed(message) if message.contains("missing field") => {
-                if [
-                    "vegetation_v10",
-                    "lse_v2",
-                    "direct_hydrology",
-                    "soil_thermal",
-                    "biogeochemistry",
-                    "gsi_configuration",
-                    "gsi_state",
-                    "static_forcing_configuration",
-                    "provider_cursor",
-                    "surface_liquid_configuration",
-                ]
-                .iter()
-                .any(|field| message.contains(field))
-                {
-                    RestartAdmissionFailureV1::OwnerOmission
-                } else {
-                    RestartAdmissionFailureV1::MissingField
-                }
-            }
-            crate::CanonicalJsonError::Typed(message) if message.contains("unknown field") => {
-                RestartAdmissionFailureV1::ExtraField
-            }
-            crate::CanonicalJsonError::Typed(message) if message.contains("HexU128") => {
-                RestartAdmissionFailureV1::TransactionLineage
-            }
-            crate::CanonicalJsonError::Typed(message)
-                if message.contains("u64") || message.contains("day index") =>
-            {
-                RestartAdmissionFailureV1::SchedulerPosition
-            }
-            crate::CanonicalJsonError::NoncanonicalBytes
-                if bytes.iter().any(u8::is_ascii_whitespace) =>
-            {
-                RestartAdmissionFailureV1::NoncanonicalBytes
-            }
-            crate::CanonicalJsonError::NoncanonicalBytes => {
-                RestartAdmissionFailureV1::ReorderedField
-            }
-            _ => RestartAdmissionFailureV1::NoncanonicalBytes,
-        }})?;
+        })?;
     if checkpoint.compute_digest()? != checkpoint.payload_sha256 {
         return Err(RestartAdmissionFailureV1::PayloadDigest);
     }
@@ -454,7 +506,8 @@
             {
                 return Err(RestartAdmissionFailureV1::SchedulerPosition);
             }
-            let committed = restore_committed(committed_day_beginning, context, day_index.0, admission)?;
+            let committed =
+                restore_committed(committed_day_beginning, context, day_index.0, admission)?;
             let receipt = accepted_gsi_daily_receipt
                 .restore()
                 .map_err(|_| RestartAdmissionFailureV1::GsiReceipt)?;
@@ -559,7 +612,8 @@
     context: &ExpectedRestartStaticContext<'_>,
     admission: ScientificAdmission,
 ) -> Result<(), RestartAdmissionFailureV1> {
-    let (run, topology) = checkpoint_identities(value, context.root_zone_hydraulic_configuration, admission)?;
+    let (run, topology) =
+        checkpoint_identities(value, context.root_zone_hydraulic_configuration, admission)?;
     if run != checkpoint.run_identity_sha256 {
         return Err(RestartAdmissionFailureV1::RunIdentity);
     }
@@ -574,8 +628,12 @@
     root_zone_hydraulic_configuration: &openwepp_hillslope_orchestrator::v9_real_consumer_shadow::DirectRootZoneHydraulicConfiguration,
     admission: ScientificAdmission,
 ) -> Result<(Sha256Hex, Sha256Hex), RestartAdmissionFailureV1> {
-    crate::projection::checkpoint_identities_with_soil(committed, root_zone_hydraulic_configuration, admission)
-        .map_err(|_| RestartAdmissionFailureV1::TopologyIdentity)
+    crate::projection::checkpoint_identities_with_soil(
+        committed,
+        root_zone_hydraulic_configuration,
+        admission,
+    )
+    .map_err(|_| RestartAdmissionFailureV1::TopologyIdentity)
 }
 
 fn require_staged_surface_position(
@@ -708,7 +766,15 @@
         })
         .collect::<Vec<_>>();
     let soil_maps = value.soil_layer_maps(admission)?;
-    let actual_soil_order = soil_maps.iter().map(|(ofe, layers)| (ofe.as_str(), layers.iter().map(String::as_str).collect::<Vec<_>>())).collect::<Vec<_>>();
+    let actual_soil_order = soil_maps
+        .iter()
+        .map(|(ofe, layers)| {
+            (
+                ofe.as_str(),
+                layers.iter().map(String::as_str).collect::<Vec<_>>(),
+            )
+        })
+        .collect::<Vec<_>>();
     if actual_lse_order != expected_lse_order || actual_soil_order != expected_soil_order {
         return Err(RestartAdmissionFailureV1::CanonicalOrder);
     }
@@ -749,14 +815,22 @@
         .restore(&hydrology_context)
         .map_err(classify_hydrology)?;
     let soil_thermal = match (&value.soil_thermal, &value.native_soil) {
-        (Some(v1), None) => DirectSoilThermalResident::try_new_v1(v1.restore(
-            context.soil_thermal_owner_id, context.soil_thermal_configuration_sha256)
-            .map_err(|_| RestartAdmissionFailureV1::OwnerValidation)?)
+        (Some(v1), None) => DirectSoilThermalResident::try_new_v1(
+            v1.restore(
+                context.soil_thermal_owner_id,
+                context.soil_thermal_configuration_sha256,
+            )
             .map_err(|_| RestartAdmissionFailureV1::OwnerValidation)?,
+        )
+        .map_err(|_| RestartAdmissionFailureV1::OwnerValidation)?,
         (None, Some(native)) if admission == ScientificAdmission::NativeB01 => {
-            let resident=native.restore_resident()?;
-            resident.restart_authority_validate(context.lse_configuration,
-                context.soil_thermal_owner_id, context.soil_thermal_configuration_sha256)
+            let resident = native.restore_resident()?;
+            resident
+                .restart_authority_validate(
+                    context.lse_configuration,
+                    context.soil_thermal_owner_id,
+                    context.soil_thermal_configuration_sha256,
+                )
                 .map_err(|_| RestartAdmissionFailureV1::OwnerValidation)?;
             DirectSoilThermalResident::V2(resident)
         }
@@ -772,7 +846,12 @@
     if direct_hydrology.identity.run_id.to_string() != context.forcing_static_configuration.run_id
         || direct_hydrology.lanes.len() != soil_maps.len()
         || context.lse_configuration.ofes.len() != soil_maps.len()
-        || direct_hydrology.lanes.iter().zip(&soil_maps).any(|(lane, (_, layers))| lane.subsurface_layers.len() != layers.len()) {
+        || direct_hydrology
+            .lanes
+            .iter()
+            .zip(&soil_maps)
+            .any(|(lane, (_, layers))| lane.subsurface_layers.len() != layers.len())
+    {
         return Err(RestartAdmissionFailureV1::TopologyIdentity);
     }
     Ok(RestoredScientificOwnerStateSetV1 {
@@ -816,7 +895,11 @@
         .as_ref()
         .map(crate::HexU128::to_u128);
     // Native soil retains its own authenticated support/transaction chronology.
-    let soil_lineage = value.soil_thermal.as_ref().and_then(|soil| soil.last_accepted_transaction_id.as_ref()).map(crate::HexU128::to_u128);
+    let soil_lineage = value
+        .soil_thermal
+        .as_ref()
+        .and_then(|soil| soil.last_accepted_transaction_id.as_ref())
+        .map(crate::HexU128::to_u128);
     let native_soil = value.native_soil.is_some();
     if required.is_some_and(|value| value != expected)
         || if required.is_some() {
@@ -968,7 +1051,10 @@
     }
     #[test]
     fn native_scientific_soil_posture_keeps_ordinary_bytes_and_refuses_omission() {
-        openwepp_land_surface_energy::snow_accuracy_policy::Policy::parse("B01").unwrap().install().unwrap();
+        openwepp_land_surface_energy::snow_accuracy_policy::Policy::parse("B01")
+            .unwrap()
+            .install()
+            .unwrap();
         let (fixture, _, _, _) = restart_authority_in_progress_checkpoint_fixture(24);
         let original = fixture.owners.committed.scientific.clone();
         #[derive(Serialize)]
@@ -979,18 +1065,32 @@
             soil_thermal: &'a SoilThermalStateRestartV1,
             biogeochemistry: &'a BiogeochemistryStateRestartV1,
         }
-        let legacy = LegacyScientific { vegetation_v10: &original.vegetation_v10,
-            lse_v2: &original.lse_v2, direct_hydrology: &original.direct_hydrology,
-            soil_thermal: original.soil_thermal.as_ref().unwrap(), biogeochemistry: &original.biogeochemistry };
+        let legacy = LegacyScientific {
+            vegetation_v10: &original.vegetation_v10,
+            lse_v2: &original.lse_v2,
+            direct_hydrology: &original.direct_hydrology,
+            soil_thermal: original.soil_thermal.as_ref().unwrap(),
+            biogeochemistry: &original.biogeochemistry,
+        };
         let bytes = to_canonical_bytes(&original).unwrap();
         assert_eq!(bytes, to_canonical_bytes(&legacy).unwrap());
         let replay: ScientificOwnerStateSetV1 = from_canonical_bytes(&bytes).unwrap();
         assert_eq!(replay, original);
-        original.validate_soil_posture(ScientificAdmission::Ordinary).unwrap();
+        original
+            .validate_soil_posture(ScientificAdmission::Ordinary)
+            .unwrap();
         let mut missing = original.clone();
         missing.soil_thermal = None;
-        assert!(missing.validate_soil_posture(ScientificAdmission::Ordinary).is_err());
-        assert!(missing.validate_soil_posture(ScientificAdmission::NativeB01).is_err());
+        assert!(
+            missing
+                .validate_soil_posture(ScientificAdmission::Ordinary)
+                .is_err()
+        );
+        assert!(
+            missing
+                .validate_soil_posture(ScientificAdmission::NativeB01)
+                .is_err()
+        );
         assert_eq!(to_canonical_bytes(&original).unwrap(), bytes);
     }
 
@@ -1259,12 +1359,23 @@
         phase(&mut p).1.lse_v2.state_sha256 = Sha256Hex::try_new("1".repeat(64)).unwrap();
         check(sealed(p), RestartAdmissionFailureV1::LseV2V1Projection);
         let mut p = baseline.clone();
-        phase(&mut p).1.soil_thermal.as_mut().unwrap().restart_payload_sha256 =
+        phase(&mut p)
+            .1
+            .soil_thermal
+            .as_mut()
+            .unwrap()
+            .restart_payload_sha256 = Sha256Hex::try_new("1".repeat(64)).unwrap();
+        check(sealed(p), RestartAdmissionFailureV1::OwnerValidation);
+        let mut p = baseline.clone();
+        phase(&mut p).1.soil_thermal.as_mut().unwrap().state_sha256 =
             Sha256Hex::try_new("1".repeat(64)).unwrap();
-        check(sealed(p), RestartAdmissionFailureV1::OwnerValidation);
-        let mut p = baseline.clone();
-        phase(&mut p).1.soil_thermal.as_mut().unwrap().state_sha256 = Sha256Hex::try_new("1".repeat(64)).unwrap();
-        phase(&mut p).1.soil_thermal.as_mut().unwrap().seal_restart_payload().unwrap();
+        phase(&mut p)
+            .1
+            .soil_thermal
+            .as_mut()
+            .unwrap()
+            .seal_restart_payload()
+            .unwrap();
         check(sealed(p), RestartAdmissionFailureV1::OwnerValidation);
         let mut p = baseline.clone();
         if let DirectV10CheckpointPhaseV1::InProgressDay {
--- a/crates/openwepp-persisted-restart-v1/src/evidence_fixture.rs
+++ b/crates/openwepp-persisted-restart-v1/src/evidence_fixture.rs
@@ -164,12 +164,14 @@
             day_input_digests,
         )
         .unwrap(),
-        soil_thermal: Some(SoilThermalStateRestartV1::project(
-            shadow
-                .restart_authority_soil_thermal()
-                .expect("V1 evidence fixture resident"),
-        )
-        .unwrap()),
+        soil_thermal: Some(
+            SoilThermalStateRestartV1::project(
+                shadow
+                    .restart_authority_soil_thermal()
+                    .expect("V1 evidence fixture resident"),
+            )
+            .unwrap(),
+        ),
         native_soil: None,
         biogeochemistry: BiogeochemistryStateRestartV1::project(
             shadow.restart_authority_biogeochemistry(),
@@ -981,7 +983,17 @@
                     .is_some_and(|tile| tile.as_str() == "lower-open")
         }));
         assert_eq!(fixture.committed.scientific.lse_v2.tiles.len(), 4);
-        assert_eq!(fixture.committed.scientific.soil_thermal.as_ref().unwrap().ofes.len(), 2);
+        assert_eq!(
+            fixture
+                .committed
+                .scientific
+                .soil_thermal
+                .as_ref()
+                .unwrap()
+                .ofes
+                .len(),
+            2
+        );
         assert_eq!(
             fixture
                 .committed
@@ -1265,7 +1277,9 @@
                 .restore(context.lse_configuration)
                 .unwrap();
             staged_scientific
-                .soil_thermal.as_ref().unwrap()
+                .soil_thermal
+                .as_ref()
+                .unwrap()
                 .restore(
                     context.soil_thermal_owner_id,
                     context.soil_thermal_configuration_sha256,
@@ -1317,7 +1331,9 @@
             .owners
             .committed
             .scientific
-            .soil_thermal.as_ref().unwrap()
+            .soil_thermal
+            .as_ref()
+            .unwrap()
             .restore(
                 context.soil_thermal_owner_id,
                 context.soil_thermal_configuration_sha256,
--- a/crates/openwepp-persisted-restart-v1/src/frozen_litter_v3_tests.rs
+++ b/crates/openwepp-persisted-restart-v1/src/frozen_litter_v3_tests.rs
@@ -175,7 +175,8 @@
             .committed
             .scientific
             .soil_thermal
-            .clone().unwrap();
+            .clone()
+            .unwrap();
         parent_v1.state_sha256 = Sha256Hex::try_new(
             support
                 .soil_thermal_owner
@@ -641,7 +642,8 @@
             .committed
             .scientific
             .soil_thermal
-            .clone().unwrap();
+            .clone()
+            .unwrap();
         parent_v1.state_sha256 = wire_digest('4');
         parent_v1
             .seal_restart_payload()
--- a/crates/openwepp-persisted-restart-v1/src/gsi_forcing.rs
+++ b/crates/openwepp-persisted-restart-v1/src/gsi_forcing.rs
@@ -1037,3 +1037,222 @@
         );
     }
 }
+
+#[cfg(test)]
+mod b01_native_provider_prefix_authentication_tests {
+    use super::*;
+    use openwepp_hillslope_orchestrator::runtime_inputs::{
+        SnowFreeHalfHourForcingError, build_hillslope_climate_runtime_request,
+        restart_authority_project_gsi_state, restart_authority_restore_gsi_state,
+    };
+    use openwepp_input_contract::parsers::climate::{ParserMode, parse_climate_file};
+
+    #[test]
+    #[ignore = "requires owner-authorized pinned B01 corpus/provider computation"]
+    fn b01_pinned_bootstrap_provider_prefix_retains_receipts_and_keeps_day4_prepared() {
+        let seed_path = std::env::var_os("OPENWEPP_B01_PINNED_SEED").expect("bound pinned seed");
+        let climate_path =
+            std::env::var_os("OPENWEPP_B01_ORIGINAL_CLIMATE").expect("bound original climate");
+        let expected_path = std::env::var_os("OPENWEPP_B01_GSI_AUTH_EXPECTED_JSON")
+            .expect("bound independently captured GSI expectation");
+        let seed: serde_json::Value =
+            serde_json::from_slice(&std::fs::read(seed_path).expect("pinned seed bytes"))
+                .expect("pinned seed JSON");
+        let committed = &seed["checkpoint"]["phase"]["committed"];
+        let gsi_configuration: DirectGsiOwnerConfigurationRestartV1 =
+            serde_json::from_value(committed["gsi_configuration"].clone())
+                .expect("pinned GSI configuration wire");
+        let gsi_state: DirectGsiOwnerStateRestartV1 =
+            serde_json::from_value(committed["gsi_state"].clone()).expect("pinned GSI state wire");
+        let static_configuration: SnowFreeHalfHourStaticConfigurationRestartV1 =
+            serde_json::from_value(committed["static_forcing_configuration"].clone())
+                .expect("pinned provider configuration wire");
+        let cursor: SnowFreeHalfHourProviderCursorRestartV1 =
+            serde_json::from_value(committed["provider_cursor"].clone())
+                .expect("pinned provider cursor wire");
+        let configuration = gsi_configuration.restore().expect("GSI configuration");
+        let mut gsi = restart_authority_restore_gsi_state(&gsi_state.restore().expect("GSI state"))
+            .expect("native GSI bootstrap");
+        let provider_configuration = static_configuration
+            .restore()
+            .expect("provider configuration");
+        assert_eq!(
+            provider_configuration.gsi_owner_configuration_sha256,
+            configuration.configuration_sha256,
+            "pinned provider and GSI configuration join"
+        );
+        let mut cursor = cursor
+            .restore(&provider_configuration, 0)
+            .expect("provider cursor");
+        let bootstrap_gsi = gsi.clone();
+        let bootstrap_cursor = cursor.clone();
+        let climate = build_hillslope_climate_runtime_request(
+            &parse_climate_file(climate_path, ParserMode::SnowFreeHalfHourProvider)
+                .expect("original climate"),
+        )
+        .expect("climate runtime request");
+
+        let mut committed_preimages = Vec::new();
+        for day_index in 0..4 {
+            let prepared = climate
+                .prepare_snow_free_gsi_day_from_repository(
+                    day_index,
+                    &provider_configuration,
+                    &configuration,
+                    &gsi,
+                    &cursor,
+                )
+                .expect("provider prefix preparation");
+            committed_preimages.push(prepared.gsi_receipt().clone());
+            prepared
+                .commit(&mut gsi, &mut cursor)
+                .expect("provider prefix commit");
+            cursor
+                .validate_for_configuration(&provider_configuration, day_index + 1)
+                .expect("provider prefix cursor");
+        }
+        let gsi_after_day3 = gsi.clone();
+        let cursor_after_day3 = cursor.clone();
+        let prepared_day4 = climate
+            .prepare_snow_free_gsi_day_from_repository(
+                4,
+                &provider_configuration,
+                &configuration,
+                &gsi,
+                &cursor,
+            )
+            .expect("day4 preparation");
+        println!(
+            "B01_PROVIDER_PREFIX_PREIMAGES={} B01_PROVIDER_DAY4_PREPARED_PREIMAGE={}",
+            serde_json::to_string(&committed_preimages).expect("prefix preimages JSON"),
+            serde_json::to_string(prepared_day4.gsi_receipt())
+                .expect("prepared day4 preimage JSON"),
+        );
+        let expected: serde_json::Value =
+            serde_json::from_slice(&std::fs::read(expected_path).expect("GSI expectation bytes"))
+                .expect("GSI expectation JSON");
+        assert_eq!(committed_preimages.len(), 4);
+        assert_eq!(
+            committed_preimages[3].receipt_sha256,
+            expected["captured_gsi_receipt"]
+                .as_str()
+                .expect("expected committed receipt"),
+            "prior committed receipt authentication"
+        );
+        assert_eq!(
+            prepared_day4.gsi_receipt().receipt_sha256,
+            expected["prepared_receipt"]["receipt_sha256"]
+                .as_str()
+                .expect("expected prepared receipt"),
+            "current prepared receipt authentication"
+        );
+        assert_ne!(
+            committed_preimages[3].receipt_sha256,
+            prepared_day4.gsi_receipt().receipt_sha256,
+            "committed and prepared receipt phases must remain distinct"
+        );
+        let projected_gsi = restart_authority_project_gsi_state(&gsi).expect("projected day3 GSI");
+        let expected_history = expected["captured_gsi_state"]["history_oldest_first"]
+            .as_array()
+            .expect("captured GSI history")
+            .iter()
+            .map(|value| {
+                value["exact_decimal"]
+                    .as_str()
+                    .expect("captured exact GSI history value")
+                    .parse::<f64>()
+                    .expect("captured exact GSI history parses")
+            })
+            .collect::<Vec<_>>();
+        let expected_date = &expected["captured_gsi_state"]["last_date"];
+        assert_eq!(
+            projected_gsi.state_sha256,
+            expected["captured_gsi_state"]["state_sha256"]
+                .as_str()
+                .expect("captured GSI state digest"),
+            "generated day3 ending GSI equals captured tuple"
+        );
+        assert_eq!(
+            projected_gsi.history_oldest_first, expected_history,
+            "generated day3 GSI history equals captured tuple"
+        );
+        assert_eq!(
+            projected_gsi
+                .last_date
+                .as_ref()
+                .map(|date| (date.year, date.ordinal_day)),
+            Some((
+                expected_date["year"].as_i64().expect("captured GSI year") as i32,
+                expected_date["ordinal_day"]
+                    .as_u64()
+                    .expect("captured GSI ordinal day") as u16,
+            )),
+            "generated day3 GSI date equals captured tuple"
+        );
+        let cursor_json: serde_json::Value =
+            serde_json::from_slice(&cursor.to_json_bytes().expect("cursor JSON bytes"))
+                .expect("cursor JSON");
+        assert_eq!(
+            cursor_json,
+            expected["provider_cursor"].clone(),
+            "full captured cursor tuple"
+        );
+        assert_eq!(gsi, gsi_after_day3, "preparation does not advance live GSI");
+        assert_eq!(
+            cursor, cursor_after_day3,
+            "preparation does not advance cursor"
+        );
+        let mut wrong_state = bootstrap_gsi;
+        let mut correct_cursor = cursor.clone();
+        let prepared = climate
+            .prepare_snow_free_gsi_day_from_repository(
+                4,
+                &provider_configuration,
+                &configuration,
+                &gsi,
+                &cursor,
+            )
+            .expect("reprepare day4 for state control");
+        assert!(matches!(
+            prepared.commit(&mut wrong_state, &mut correct_cursor),
+            Err(SnowFreeHalfHourForcingError::Identity(
+                "GSI/provider atomic commit beginning"
+            ))
+        ));
+        let mut correct_state = gsi.clone();
+        let mut wrong_cursor = bootstrap_cursor;
+        let prepared = climate
+            .prepare_snow_free_gsi_day_from_repository(
+                4,
+                &provider_configuration,
+                &configuration,
+                &gsi,
+                &cursor,
+            )
+            .expect("reprepare day4 for cursor control");
+        assert!(matches!(
+            prepared.commit(&mut correct_state, &mut wrong_cursor),
+            Err(SnowFreeHalfHourForcingError::Identity(
+                "GSI/provider atomic commit beginning"
+            ))
+        ));
+        let mut wrong_configuration = provider_configuration.clone();
+        wrong_configuration.gsi_owner_configuration_sha256 = "0".repeat(64);
+        assert!(matches!(
+            climate.prepare_snow_free_gsi_day_from_repository(
+                4,
+                &wrong_configuration,
+                &configuration,
+                &gsi,
+                &cursor,
+            ),
+            Err(SnowFreeHalfHourForcingError::Identity(_))
+        ));
+        let mut wrong_phase = prepared_day4.gsi_receipt().clone();
+        wrong_phase.receipt_sha256 = committed_preimages[3].receipt_sha256.clone();
+        assert!(matches!(
+            wrong_phase.validate(),
+            Err(openwepp_hillslope_orchestrator::runtime_inputs::SnowFreeHalfHourForcingError::Identity(_))
+        ), "a committed receipt cannot be substituted for the prepared receipt phase");
+    }
+}
--- a/crates/openwepp-persisted-restart-v1/src/host.rs
+++ b/crates/openwepp-persisted-restart-v1/src/host.rs
@@ -1,5 +1,11 @@
-use openwepp_hillslope_orchestrator::v9_real_consumer_shadow::{NativeSoilRestartAdmissionV1, NativeSoilRestartPositionV1};
-type NativeSoilAdmissionContext<'a> = (&'a NativeSoilRestartAdmissionV1, &'a openwepp_coupled_time::CoupledClockStateV1, NativeSoilRestartPositionV1);
+use openwepp_hillslope_orchestrator::v9_real_consumer_shadow::{
+    NativeSoilRestartAdmissionV1, NativeSoilRestartPositionV1,
+};
+type NativeSoilAdmissionContext<'a> = (
+    &'a NativeSoilRestartAdmissionV1,
+    &'a openwepp_coupled_time::CoupledClockStateV1,
+    NativeSoilRestartPositionV1,
+);
 use crate::checkpoint::ScientificAdmission;
 use openwepp_hillslope_orchestrator::{
     runtime_inputs::{
@@ -80,18 +86,27 @@
         Self::from_isolated_with_soil(restored, context, ScientificAdmission::Ordinary, None)
     }
     pub(crate) fn from_isolated_with_soil(
-        restored: IsolatedRestoredCheckpointV1, context: &ExpectedRestartStaticContext<'_>,
+        restored: IsolatedRestoredCheckpointV1,
+        context: &ExpectedRestartStaticContext<'_>,
         admission: ScientificAdmission,
         native_context: Option<NativeSoilAdmissionContext<'_>>,
     ) -> Result<Self, RestartInstallError> {
-        admission.validate().map_err(|_| RestartInstallError::OwnerValidation("native soil policy"))?;
+        admission
+            .validate()
+            .map_err(|_| RestartInstallError::OwnerValidation("native soil policy"))?;
         match restored {
             IsolatedRestoredCheckpointV1::BetweenDays {
                 next_day_index,
                 accepted_interval_count,
                 committed,
             } => {
-                let mut shadow = build_shadow(committed, next_day_index, context, admission, native_context)?;
+                let mut shadow = build_shadow(
+                    committed,
+                    next_day_index,
+                    context,
+                    admission,
+                    native_context,
+                )?;
                 if accepted_interval_count != 0 {
                     shadow.restart_authority_install_scheduler_position(accepted_interval_count)?;
                 }
@@ -136,7 +151,8 @@
                     provider_cursor,
                     scientific: staged_scientific,
                 };
-                let mut shadow = build_shadow(committed, day_index, context, admission, native_context)?;
+                let mut shadow =
+                    build_shadow(committed, day_index, context, admission, native_context)?;
                 shadow.restart_authority_install_scheduler_position(accepted_interval_count)?;
                 shadow.restart_authority_install_staged_daily_owners(
                     native_ending_gsi,
@@ -279,49 +295,60 @@
         })
         .collect();
     if admission == ScientificAdmission::NativeB01 {
-    let (proof, clock, position) = native_context.ok_or(RestartInstallError::OwnerValidation("native soil admission proof required"))?;
-    Ok(DirectV10RealConsumerShadow::restart_authority_try_new_with_native_soil(
-        context.vegetation_configuration.clone(),
-        vegetation_v10,
-        context.vegetation_owner_id.clone(),
-        context.lse_configuration.clone(),
-        lse_v2,
-        context.surface_liquid_configuration.clone(),
-        layer_maps,
-        soil_thermal,
-        biogeochemistry,
-        direct_hydrology,
-        usize::try_from(next_day_index)
-            .map_err(|_| RestartInstallError::OwnerValidation("next day width"))?,
-        context.gsi_configuration.clone(),
-        restart_authority_restore_gsi_state(&gsi_state)
-            .map_err(DirectV10RealConsumerError::from)?,
-        context.forcing_static_configuration.clone(),
-        provider_cursor,
-        context.root_zone_hydraulic_configuration.clone(),
-        proof, clock, position,
-    )?)
+        let (proof, clock, position) = native_context.ok_or(
+            RestartInstallError::OwnerValidation("native soil admission proof required"),
+        )?;
+        Ok(
+            DirectV10RealConsumerShadow::restart_authority_try_new_with_native_soil(
+                context.vegetation_configuration.clone(),
+                vegetation_v10,
+                context.vegetation_owner_id.clone(),
+                context.lse_configuration.clone(),
+                lse_v2,
+                context.surface_liquid_configuration.clone(),
+                layer_maps,
+                soil_thermal,
+                biogeochemistry,
+                direct_hydrology,
+                usize::try_from(next_day_index)
+                    .map_err(|_| RestartInstallError::OwnerValidation("next day width"))?,
+                context.gsi_configuration.clone(),
+                restart_authority_restore_gsi_state(&gsi_state)
+                    .map_err(DirectV10RealConsumerError::from)?,
+                context.forcing_static_configuration.clone(),
+                provider_cursor,
+                context.root_zone_hydraulic_configuration.clone(),
+                proof,
+                clock,
+                position,
+            )?,
+        )
     } else {
-    Ok(DirectV10RealConsumerShadow::try_new(
-        context.vegetation_configuration.clone(),
-        vegetation_v10,
-        context.vegetation_owner_id.clone(),
-        context.lse_configuration.clone(),
-        lse_v2,
-        context.surface_liquid_configuration.clone(),
-        layer_maps,
-        soil_thermal.v1().map_err(|_| RestartInstallError::OwnerValidation("ordinary host native soil refusal"))?.clone(),
-        biogeochemistry,
-        direct_hydrology,
-        usize::try_from(next_day_index)
-            .map_err(|_| RestartInstallError::OwnerValidation("next day width"))?,
-        context.gsi_configuration.clone(),
-        restart_authority_restore_gsi_state(&gsi_state)
-            .map_err(DirectV10RealConsumerError::from)?,
-        context.forcing_static_configuration.clone(),
-        provider_cursor,
-        context.root_zone_hydraulic_configuration.clone(),
-    )?)
+        Ok(DirectV10RealConsumerShadow::try_new(
+            context.vegetation_configuration.clone(),
+            vegetation_v10,
+            context.vegetation_owner_id.clone(),
+            context.lse_configuration.clone(),
+            lse_v2,
+            context.surface_liquid_configuration.clone(),
+            layer_maps,
+            soil_thermal
+                .v1()
+                .map_err(|_| {
+                    RestartInstallError::OwnerValidation("ordinary host native soil refusal")
+                })?
+                .clone(),
+            biogeochemistry,
+            direct_hydrology,
+            usize::try_from(next_day_index)
+                .map_err(|_| RestartInstallError::OwnerValidation("next day width"))?,
+            context.gsi_configuration.clone(),
+            restart_authority_restore_gsi_state(&gsi_state)
+                .map_err(DirectV10RealConsumerError::from)?,
+            context.forcing_static_configuration.clone(),
+            provider_cursor,
+            context.root_zone_hydraulic_configuration.clone(),
+        )?)
     }
 }
 
--- a/crates/openwepp-persisted-restart-v1/src/hydrology_restart.rs
+++ b/crates/openwepp-persisted-restart-v1/src/hydrology_restart.rs
@@ -338,9 +338,13 @@
     }
 
     pub(crate) fn project_for_native_parent(
-        value: &DirectRunFrame, phase_plan_sha256: Sha256Hex, day_input_digests: &[Sha256Hex],
+        value: &DirectRunFrame,
+        phase_plan_sha256: Sha256Hex,
+        day_input_digests: &[Sha256Hex],
     ) -> Result<Self, HydrologyRestartError> {
-        crate::checkpoint::ScientificAdmission::NativeB01.validate().map_err(nested)?;
+        crate::checkpoint::ScientificAdmission::NativeB01
+            .validate()
+            .map_err(nested)?;
         Self::project_mode(value, phase_plan_sha256, day_input_digests, true, true)
     }
 
@@ -448,7 +452,11 @@
                 .as_deref()
                 .map(|attachment| {
                     if native_soil {
-                        DirectSnowStage3V11AttachmentRestartV2::project_active_base_native(attachment, &phase_plan_sha256, day_input_digests)
+                        DirectSnowStage3V11AttachmentRestartV2::project_active_base_native(
+                            attachment,
+                            &phase_plan_sha256,
+                            day_input_digests,
+                        )
                     } else if for_exact_parent {
                         DirectSnowStage3V11AttachmentRestartV2::project_active_base_v3(
                             attachment,
@@ -1014,24 +1022,23 @@
     }
 }
 
-
 fn validate_stage3_restart_contexts(
     context: &ExpectedDirectHydrologyRestartContext<'_>,
     stage3_context: &crate::ExpectedSnowStage3V11RestartContext<'_>,
 ) -> Result<(), HydrologyRestartError> {
-        let consumer_context = stage3_context.real_consumer_context;
-        if context.phase_plan != consumer_context.phase_plan
-            || context.phase_plan_sha256 != consumer_context.phase_plan_sha256
-            || context.day_inputs != consumer_context.day_inputs
-            || context.day_input_digests != consumer_context.day_input_digests
-            || context.surface_liquid_configuration != consumer_context.surface_liquid_configuration
-            || context.surface_liquid_configuration
-                != &stage3_context.static_context.surface_liquid_configuration
-        {
-            return Err(HydrologyRestartError::Join(
-                "Stage-3/V11 direct and real-consumer restart contexts",
-            ));
-        }
+    let consumer_context = stage3_context.real_consumer_context;
+    if context.phase_plan != consumer_context.phase_plan
+        || context.phase_plan_sha256 != consumer_context.phase_plan_sha256
+        || context.day_inputs != consumer_context.day_inputs
+        || context.day_input_digests != consumer_context.day_input_digests
+        || context.surface_liquid_configuration != consumer_context.surface_liquid_configuration
+        || context.surface_liquid_configuration
+            != &stage3_context.static_context.surface_liquid_configuration
+    {
+        return Err(HydrologyRestartError::Join(
+            "Stage-3/V11 direct and real-consumer restart contexts",
+        ));
+    }
     Ok(())
 }
 
@@ -1055,70 +1062,168 @@
     pub stage3: &'a crate::ExpectedSnowStage3V11RestartContext<'a>,
     pub archive: &'a ExpectedStage3CommittedDayArchiveV3<'a>,
     pub native: &'a crate::ExpectedNativeCustodyContextV1<'a>,
-    pub expected_laned_active: &'a Option<Box<openwepp_hillslope_orchestrator::DirectLanedActiveConfig>>,
+    pub expected_laned_active:
+        &'a Option<Box<openwepp_hillslope_orchestrator::DirectLanedActiveConfig>>,
 }
 
 impl DirectHydrologyNativeCustodyRestartV1 {
     const SCHEMA: &'static str = "OPENWEPP_B01_HYDROLOGY_NATIVE_CUSTODY_RESTART_V1_POLICY14";
-    pub fn project(value: &DirectRunFrame, phase_plan_sha256: Sha256Hex, day_input_digests: &[Sha256Hex],
-        archive: &ExpectedStage3CommittedDayArchiveV3<'_>, native: &crate::ExpectedNativeCustodyContextV1<'_>,
+    pub fn project(
+        value: &DirectRunFrame,
+        phase_plan_sha256: Sha256Hex,
+        day_input_digests: &[Sha256Hex],
+        archive: &ExpectedStage3CommittedDayArchiveV3<'_>,
+        native: &crate::ExpectedNativeCustodyContextV1<'_>,
     ) -> Result<Self, HydrologyRestartError> {
         let mut base = value.clone();
         let laned_active = base.laned_active.take();
         let laned_active_summary = base.laned_active_summary.take();
         if let Some(attachment) = base.snow_stage3_v11_attachment.as_deref() {
-            base.snow_stage3_v11_attachment = Some(Box::new(attachment.restart_authority_native_without_routing().map_err(nested)?));
-        }
-        let parent = DirectHydrologyRestartV1::project_for_native_parent(&base, phase_plan_sha256.clone(), day_input_digests)?;
-        let attachment = value.snow_stage3_v11_attachment.as_deref().ok_or(HydrologyRestartError::Join("native checkpoint Stage3 missing"))?;
-        let native_stage3 = crate::DirectNativeCustodyStage3RestartV1::project(attachment, &phase_plan_sha256,
-            day_input_digests, archive, native).map_err(nested)?;
+            base.snow_stage3_v11_attachment = Some(Box::new(
+                attachment
+                    .restart_authority_native_without_routing()
+                    .map_err(nested)?,
+            ));
+        }
+        let parent = DirectHydrologyRestartV1::project_for_native_parent(
+            &base,
+            phase_plan_sha256.clone(),
+            day_input_digests,
+        )?;
+        let attachment =
+            value
+                .snow_stage3_v11_attachment
+                .as_deref()
+                .ok_or(HydrologyRestartError::Join(
+                    "native checkpoint Stage3 missing",
+                ))?;
+        let native_stage3 = crate::DirectNativeCustodyStage3RestartV1::project(
+            attachment,
+            &phase_plan_sha256,
+            day_input_digests,
+            archive,
+            native,
+        )
+        .map_err(nested)?;
         let bytes = crate::to_canonical_bytes(&parent).map_err(nested)?;
-        let mut result = Self {schema: Self::SCHEMA.into(), policy_id: 14, parent_v1_sha256: restart_sha(&bytes)?,
-            parent_v1_bytes: bytes, native_stage3, laned_active, laned_active_summary, payload_sha256: restart_zero_sha()?};
+        let mut result = Self {
+            schema: Self::SCHEMA.into(),
+            policy_id: 14,
+            parent_v1_sha256: restart_sha(&bytes)?,
+            parent_v1_bytes: bytes,
+            native_stage3,
+            laned_active,
+            laned_active_summary,
+            payload_sha256: restart_zero_sha()?,
+        };
         result.payload_sha256 = result.digest()?;
         Ok(result)
     }
     fn digest(&self) -> Result<Sha256Hex, HydrologyRestartError> {
-        restart_sha(&crate::to_canonical_bytes(&(&self.schema, self.policy_id, &self.parent_v1_bytes,
-            &self.parent_v1_sha256, &self.native_stage3, &self.laned_active, &self.laned_active_summary)).map_err(nested)?)
-    }
-    pub fn restore(&self, context: &ExpectedDirectHydrologyNativeCustodyContextV1<'_>) -> Result<DirectRunFrame, HydrologyRestartError> {
-        if self.schema != Self::SCHEMA || self.policy_id != 14
-            || self.parent_v1_sha256 != restart_sha(&self.parent_v1_bytes)? || self.payload_sha256 != self.digest()? {
-            return Err(HydrologyRestartError::Join("native hydrology schema/policy/payload"));
+        restart_sha(
+            &crate::to_canonical_bytes(&(
+                &self.schema,
+                self.policy_id,
+                &self.parent_v1_bytes,
+                &self.parent_v1_sha256,
+                &self.native_stage3,
+                &self.laned_active,
+                &self.laned_active_summary,
+            ))
+            .map_err(nested)?,
+        )
+    }
+    pub fn restore(
+        &self,
+        context: &ExpectedDirectHydrologyNativeCustodyContextV1<'_>,
+    ) -> Result<DirectRunFrame, HydrologyRestartError> {
+        if self.schema != Self::SCHEMA
+            || self.policy_id != 14
+            || self.parent_v1_sha256 != restart_sha(&self.parent_v1_bytes)?
+            || self.payload_sha256 != self.digest()?
+        {
+            return Err(HydrologyRestartError::Join(
+                "native hydrology schema/policy/payload",
+            ));
         }
         validate_stage3_restart_contexts(context.hydrology, context.stage3)?;
-        let parent: DirectHydrologyRestartV1 = crate::from_canonical_bytes(&self.parent_v1_bytes).map_err(nested)?;
+        let parent: DirectHydrologyRestartV1 =
+            crate::from_canonical_bytes(&self.parent_v1_bytes).map_err(nested)?;
         if crate::to_canonical_bytes(&parent).map_err(nested)? != self.parent_v1_bytes {
-            return Err(HydrologyRestartError::Join("native hydrology canonical parent"));
-        }
-        let active = parent.snow_stage3_v11_attachment.as_deref().ok_or(HydrologyRestartError::Join("native parent Stage3 posture"))?
-            .to_canonical_bytes().map_err(nested)?;
-        require_exact_parent_stage3_join(&active, &self.native_stage3.nested_active_v2_bytes().map_err(nested)?)?;
+            return Err(HydrologyRestartError::Join(
+                "native hydrology canonical parent",
+            ));
+        }
+        let active = parent
+            .snow_stage3_v11_attachment
+            .as_deref()
+            .ok_or(HydrologyRestartError::Join("native parent Stage3 posture"))?
+            .to_canonical_bytes()
+            .map_err(nested)?;
+        require_exact_parent_stage3_join(
+            &active,
+            &self
+                .native_stage3
+                .nested_active_v2_bytes()
+                .map_err(nested)?,
+        )?;
         let mut frame = parent.restore_without_stage3(context.hydrology)?;
-        let attachment = self.native_stage3.restore(context.stage3, context.archive, context.native).map_err(nested)?;
-        if frame.lanes.iter().map(|lane| lane.lane_id).collect::<Vec<_>>() != attachment.static_context.lane_ids {
-            return Err(HydrologyRestartError::Join("native hydrology Stage3 topology"));
-        }
-        if frame.identity != attachment.committed.real_consumer.hydrology_frame().identity {
-            return Err(HydrologyRestartError::Join("native hydrology complete run identity"));
+        let attachment = self
+            .native_stage3
+            .restore(context.stage3, context.archive, context.native)
+            .map_err(nested)?;
+        if frame
+            .lanes
+            .iter()
+            .map(|lane| lane.lane_id)
+            .collect::<Vec<_>>()
+            != attachment.static_context.lane_ids
+        {
+            return Err(HydrologyRestartError::Join(
+                "native hydrology Stage3 topology",
+            ));
+        }
+        if frame.identity
+            != attachment
+                .committed
+                .real_consumer
+                .hydrology_frame()
+                .identity
+        {
+            return Err(HydrologyRestartError::Join(
+                "native hydrology complete run identity",
+            ));
         }
         if &self.laned_active != context.expected_laned_active {
-            return Err(HydrologyRestartError::Join("native active routing external configuration"));
+            return Err(HydrologyRestartError::Join(
+                "native active routing external configuration",
+            ));
         }
         let next_day = attachment.committed.real_consumer.v11_next_day_index();
         match (&self.laned_active, &self.laned_active_summary) {
             (Some(config), summary) => {
                 config.validate(frame.identity.lane_count).map_err(nested)?;
                 if let Some(summary) = summary {
-                    if summary.mesh_policy != config.mesh_policy.summary() || summary.max_dt_s != config.max_dt_s
-                        || summary.days_seen != next_day as u64 || summary.days_routed > summary.days_seen {
-                        return Err(HydrologyRestartError::Join("native active routing summary/configuration/cursor"));
+                    if summary.mesh_policy != config.mesh_policy.summary()
+                        || summary.max_dt_s != config.max_dt_s
+                        || summary.days_seen != next_day as u64
+                        || summary.days_routed > summary.days_seen
+                    {
+                        return Err(HydrologyRestartError::Join(
+                            "native active routing summary/configuration/cursor",
+                        ));
                     }
-                } else if next_day != 0 { return Err(HydrologyRestartError::Join("native active routing summary missing")); }
+                } else if next_day != 0 {
+                    return Err(HydrologyRestartError::Join(
+                        "native active routing summary missing",
+                    ));
+                }
             }
-            (None, Some(_)) => return Err(HydrologyRestartError::Join("native inactive routing summary")),
+            (None, Some(_)) => {
+                return Err(HydrologyRestartError::Join(
+                    "native inactive routing summary",
+                ));
+            }
             (None, None) => {}
         }
         frame.laned_active = self.laned_active.clone();
@@ -1129,9 +1234,16 @@
     pub fn to_canonical_bytes(&self) -> Result<Vec<u8>, HydrologyRestartError> {
         crate::to_canonical_bytes(self).map_err(nested)
     }
-    pub fn from_canonical_bytes(bytes: &[u8], context: &ExpectedDirectHydrologyNativeCustodyContextV1<'_>) -> Result<Self, HydrologyRestartError> {
+    pub fn from_canonical_bytes(
+        bytes: &[u8],
+        context: &ExpectedDirectHydrologyNativeCustodyContextV1<'_>,
+    ) -> Result<Self, HydrologyRestartError> {
         let value: Self = crate::from_canonical_bytes(bytes).map_err(nested)?;
-        if value.to_canonical_bytes()? != bytes { return Err(HydrologyRestartError::Join("native hydrology canonical bytes")); }
+        if value.to_canonical_bytes()? != bytes {
+            return Err(HydrologyRestartError::Join(
+                "native hydrology canonical bytes",
+            ));
+        }
         value.restore(context)?;
         Ok(value)
     }
--- a/crates/openwepp-persisted-restart-v1/src/lib.rs
+++ b/crates/openwepp-persisted-restart-v1/src/lib.rs
@@ -105,9 +105,9 @@
 pub use hydrology_core::{DirectHydrologyCoreError, DirectWaterStateRestartV1};
 pub use hydrology_restart::{
     DIRECT_HYDROLOGY_EXACT_ENTHALPY_RESTART_V2_SCHEMA, DirectHydrologyExactEnthalpyRestartV2,
-    DirectHydrologyRestartV1, DirectLaneRestartV1, DirectRuntimePostureV1,
-    DirectHydrologyNativeCustodyRestartV1, ExpectedDirectHydrologyNativeCustodyContextV1,
-    ExpectedDirectHydrologyExactEnthalpyRestartContextV2, ExpectedDirectHydrologyRestartContext,
+    DirectHydrologyNativeCustodyRestartV1, DirectHydrologyRestartV1, DirectLaneRestartV1,
+    DirectRuntimePostureV1, ExpectedDirectHydrologyExactEnthalpyRestartContextV2,
+    ExpectedDirectHydrologyNativeCustodyContextV1, ExpectedDirectHydrologyRestartContext,
     HydrologyRestartError,
 };
 pub use primitives::{
--- a/crates/openwepp-persisted-restart-v1/src/projection.rs
+++ b/crates/openwepp-persisted-restart-v1/src/projection.rs
@@ -1,4 +1,4 @@
-use crate::checkpoint::{ScientificAdmission, NativeSoilResidentRestartV2};
+use crate::checkpoint::{NativeSoilResidentRestartV2, ScientificAdmission};
 use openwepp_hillslope_orchestrator::{
     runtime_inputs::restart_authority_project_gsi_state,
     v9_real_consumer_shadow::DirectV10RealConsumerShadow,
@@ -56,7 +56,13 @@
     phase_plan_sha256: &Sha256Hex,
     day_input_digests: &[Sha256Hex],
 ) -> Result<ScientificOwnerStateSetV1, &'static str> {
-    project_scientific_owner_state_v1_inner(shadow, phase_plan_sha256, day_input_digests, false, ScientificAdmission::Ordinary)
+    project_scientific_owner_state_v1_inner(
+        shadow,
+        phase_plan_sha256,
+        day_input_digests,
+        false,
+        ScientificAdmission::Ordinary,
+    )
 }
 
 fn project_scientific_owner_state_v1_inner(
@@ -69,18 +75,31 @@
     guard_legacy_projection(frame_contains_v4(shadow), allow_exact_parent)?;
     admission.validate().map_err(|_| "native soil policy")?;
     let (soil_thermal, native_soil) = match shadow.restart_authority_soil_resident() {
-        openwepp_hillslope_orchestrator::v9_real_consumer_shadow::DirectSoilThermalResident::V1(owner) =>
-            (Some(SoilThermalStateRestartV1::project(owner).map_err(|_| "soil projection")?), None),
-        openwepp_hillslope_orchestrator::v9_real_consumer_shadow::DirectSoilThermalResident::V2(resident)
-            if admission == ScientificAdmission::NativeB01 => {
-                resident.restart_authority_validate(shadow.restart_authority_lse_configuration(),
-                    &resident.owner().state.owner_id, &resident.owner().state.configuration_sha256)
-                    .map_err(|_| "native soil projection validation")?;
-                (None, Some(NativeSoilResidentRestartV2 {
+        openwepp_hillslope_orchestrator::v9_real_consumer_shadow::DirectSoilThermalResident::V1(
+            owner,
+        ) => (
+            Some(SoilThermalStateRestartV1::project(owner).map_err(|_| "soil projection")?),
+            None,
+        ),
+        openwepp_hillslope_orchestrator::v9_real_consumer_shadow::DirectSoilThermalResident::V2(
+            resident,
+        ) if admission == ScientificAdmission::NativeB01 => {
+            resident
+                .restart_authority_validate(
+                    shadow.restart_authority_lse_configuration(),
+                    &resident.owner().state.owner_id,
+                    &resident.owner().state.configuration_sha256,
+                )
+                .map_err(|_| "native soil projection validation")?;
+            (
+                None,
+                Some(NativeSoilResidentRestartV2 {
                     schema: "OPENWEPP_B01_NATIVE_SOIL_RESIDENT_V2_POLICY14".into(),
-                    resident_bytes: crate::to_canonical_bytes(resident).map_err(|_| "native soil canonical bytes")?,
-                }))
-            }
+                    resident_bytes: crate::to_canonical_bytes(resident)
+                        .map_err(|_| "native soil canonical bytes")?,
+                }),
+            )
+        }
         _ => return Err("V1 soil resident projection"),
     };
 
@@ -97,7 +116,11 @@
         )
         .map_err(|_| "LSE projection")?,
         direct_hydrology: if admission == ScientificAdmission::NativeB01 {
-            DirectHydrologyRestartV1::project_for_native_parent(shadow.restart_authority_hydrology_frame(), phase_plan_sha256.clone(), day_input_digests)
+            DirectHydrologyRestartV1::project_for_native_parent(
+                shadow.restart_authority_hydrology_frame(),
+                phase_plan_sha256.clone(),
+                day_input_digests,
+            )
         } else if allow_exact_parent {
             DirectHydrologyRestartV1::project_for_exact_parent(
                 shadow.restart_authority_hydrology_frame(),
@@ -146,7 +169,13 @@
     phase_plan_sha256: &Sha256Hex,
     day_input_digests: &[Sha256Hex],
 ) -> Result<ScientificOwnerStateSetV1, &'static str> {
-    project_scientific_owner_state_v1_inner(shadow, phase_plan_sha256, day_input_digests, true, ScientificAdmission::Ordinary)
+    project_scientific_owner_state_v1_inner(
+        shadow,
+        phase_plan_sha256,
+        day_input_digests,
+        true,
+        ScientificAdmission::Ordinary,
+    )
 }
 
 /// Project the complete between-days owner envelope from the actual consumer.
@@ -183,11 +212,19 @@
 }
 
 pub(crate) fn project_complete_owner_state_native(
-    shadow: &DirectV10RealConsumerShadow, phase_plan_sha256: &Sha256Hex,
-    day_input_digests: &[Sha256Hex], expected_next_day_index: usize,
+    shadow: &DirectV10RealConsumerShadow,
+    phase_plan_sha256: &Sha256Hex,
+    day_input_digests: &[Sha256Hex],
+    expected_next_day_index: usize,
 ) -> Result<CompleteCommittedOwnerStateV1, &'static str> {
-    project_complete_owner_state_v1_inner(shadow, phase_plan_sha256, day_input_digests,
-        expected_next_day_index, true, ScientificAdmission::NativeB01)
+    project_complete_owner_state_v1_inner(
+        shadow,
+        phase_plan_sha256,
+        day_input_digests,
+        expected_next_day_index,
+        true,
+        ScientificAdmission::NativeB01,
+    )
 }
 
 fn project_complete_owner_state_v1_inner(
@@ -236,14 +273,21 @@
     committed: &CompleteCommittedOwnerStateV1,
     root_zone_hydraulic_configuration: &openwepp_hillslope_orchestrator::v9_real_consumer_shadow::DirectRootZoneHydraulicConfiguration,
 ) -> Result<(Sha256Hex, Sha256Hex), &'static str> {
-    checkpoint_identities_with_soil(committed, root_zone_hydraulic_configuration, ScientificAdmission::Ordinary)
+    checkpoint_identities_with_soil(
+        committed,
+        root_zone_hydraulic_configuration,
+        ScientificAdmission::Ordinary,
+    )
 }
 pub(crate) fn checkpoint_identities_with_soil(
     committed: &CompleteCommittedOwnerStateV1,
     root_zone_hydraulic_configuration: &openwepp_hillslope_orchestrator::v9_real_consumer_shadow::DirectRootZoneHydraulicConfiguration,
     admission: ScientificAdmission,
 ) -> Result<(Sha256Hex, Sha256Hex), &'static str> {
-    let soil_maps = committed.scientific.soil_layer_maps(admission).map_err(|_| "soil topology posture")?;
+    let soil_maps = committed
+        .scientific
+        .soil_layer_maps(admission)
+        .map_err(|_| "soil topology posture")?;
     let hydrology = &committed.scientific.direct_hydrology;
     let run = Sha256Hex::try_new(
         crate::canonical_sha256(&(
--- a/crates/openwepp-persisted-restart-v1/src/projection_v2.rs
+++ b/crates/openwepp-persisted-restart-v1/src/projection_v2.rs
@@ -118,9 +118,16 @@
     expected_configuration_sha256: &Sha256Digest,
     identity: SoilThermalV2MigrationIdentity,
 ) -> Result<CompleteCommittedOwnerStateV2, RestartProjectionV2Error> {
-    parent.scientific.validate_soil_posture(crate::checkpoint::ScientificAdmission::Ordinary).map_err(|_| RestartProjectionV2Error::V1SoilOwner)?;
+    parent
+        .scientific
+        .validate_soil_posture(crate::checkpoint::ScientificAdmission::Ordinary)
+        .map_err(|_| RestartProjectionV2Error::V1SoilOwner)?;
     let soil_thermal_v2 = bootstrap_soil_thermal_restart_v1_to_v2(
-        parent.scientific.soil_thermal.clone().ok_or(RestartProjectionV2Error::V1SoilOwner)?,
+        parent
+            .scientific
+            .soil_thermal
+            .clone()
+            .ok_or(RestartProjectionV2Error::V1SoilOwner)?,
         expected_owner_id,
         expected_configuration_sha256,
         identity,
@@ -133,7 +140,9 @@
     parent: ScientificOwnerStateSetV1,
     soil_thermal_v2: SoilThermalOwnerStateRestartV2,
 ) -> Result<ScientificOwnerStateSetV2, RestartProjectionV2Error> {
-    parent.validate_soil_posture(crate::checkpoint::ScientificAdmission::Ordinary).map_err(|_| RestartProjectionV2Error::V1SoilOwner)?;
+    parent
+        .validate_soil_posture(crate::checkpoint::ScientificAdmission::Ordinary)
+        .map_err(|_| RestartProjectionV2Error::V1SoilOwner)?;
     Ok(ScientificOwnerStateSetV2 {
         vegetation_v10: parent.vegetation_v10,
         lse_v2: parent.lse_v2,
--- a/crates/openwepp-persisted-restart-v1/src/scientific_owners.rs
+++ b/crates/openwepp-persisted-restart-v1/src/scientific_owners.rs
@@ -25,7 +25,10 @@
 #[derive(Debug, Error, PartialEq, Eq)]
 pub enum ScientificOwnerRestartError {
     #[error("vegetation V10 {operation} validation failed: {detail}")]
-    Vegetation { operation: &'static str, detail: String },
+    Vegetation {
+        operation: &'static str,
+        detail: String,
+    },
     #[error("invalid owner identity: {0}")]
     Identity(&'static str),
     #[error("invalid owner numeric domain: {0}")]
@@ -498,7 +501,10 @@
     ) -> Result<Self, ScientificOwnerRestartError> {
         value
             .validate(configuration)
-            .map_err(|error| ScientificOwnerRestartError::Vegetation {operation:"project", detail:error.to_string()})?;
+            .map_err(|error| ScientificOwnerRestartError::Vegetation {
+                operation: "project",
+                detail: error.to_string(),
+            })?;
         Ok(Self {
             owner_id: owner_id.as_str().to_owned(),
             model_definition_sha256: sha(&value.0.model_definition_sha256)?,
@@ -604,7 +610,10 @@
         });
         value
             .validate(configuration)
-            .map_err(|error| ScientificOwnerRestartError::Vegetation {operation:"restore", detail:error.to_string()})?;
+            .map_err(|error| ScientificOwnerRestartError::Vegetation {
+                operation: "restore",
+                detail: error.to_string(),
+            })?;
         project_v10_runtime_to_v9(configuration, &value).map_err(|_| {
             ScientificOwnerRestartError::Identity("vegetation V10 to V9 projection")
         })?;
--- a/crates/openwepp-persisted-restart-v1/src/snow_stage3_v11.rs
+++ b/crates/openwepp-persisted-restart-v1/src/snow_stage3_v11.rs
@@ -30,8 +30,7 @@
     AcceptedIntervalCount, CompleteCommittedOwnerStateV1, DirectV10CheckpointPhaseV1,
     DirectV10RealConsumerCheckpointV1, DirectV10RestartHost, ExpectedRestartStaticContext, HexU128,
     RestartAdmissionFailureV1, Sha256Hex, VegetationV10StateRestartV1, WireDayIndex,
-    project_complete_owner_state_v1,
-    to_canonical_bytes,
+    project_complete_owner_state_v1, to_canonical_bytes,
 };
 
 const SCHEMA: &str = "OPENWEPP_SNOW_STAGE3_V11_ATTACHMENT_RESTART_V2";
@@ -194,12 +193,33 @@
 }
 
 pub(crate) trait SnowStage3V11ExactResidentRestorerV4 {
-    fn scientific_admission(&self) -> ScientificAdmission { ScientificAdmission::Ordinary }
-    fn native_bootstrap_soil(&self) -> Option<&openwepp_hillslope_orchestrator::v9_real_consumer_shadow::DirectV10SoilThermalResidentV2> { None }
-    fn native_surface_configuration(&self) -> Option<&openwepp_hillslope_orchestrator::SurfaceLiquidConfigurationV2> { None }
-    fn native_wb14_parent_bytes(&self, _position: SnowStage3V11ExactResidentPositionV4) -> Result<Option<Vec<u8>>, SnowStage3V11RestartError> { Ok(None) }
-    fn validated_native_surface_bytes(&self, _position: SnowStage3V11ExactResidentPositionV4)
-        -> Result<Option<Vec<u8>>, SnowStage3V11RestartError> { Ok(None) }
+    fn scientific_admission(&self) -> ScientificAdmission {
+        ScientificAdmission::Ordinary
+    }
+    fn native_bootstrap_soil(
+        &self,
+    ) -> Option<
+        &openwepp_hillslope_orchestrator::v9_real_consumer_shadow::DirectV10SoilThermalResidentV2,
+    > {
+        None
+    }
+    fn native_surface_configuration(
+        &self,
+    ) -> Option<&openwepp_hillslope_orchestrator::SurfaceLiquidConfigurationV2> {
+        None
+    }
+    fn native_wb14_parent_bytes(
+        &self,
+        _position: SnowStage3V11ExactResidentPositionV4,
+    ) -> Result<Option<Vec<u8>>, SnowStage3V11RestartError> {
+        Ok(None)
+    }
+    fn validated_native_surface_bytes(
+        &self,
+        _position: SnowStage3V11ExactResidentPositionV4,
+    ) -> Result<Option<Vec<u8>>, SnowStage3V11RestartError> {
+        Ok(None)
+    }
     fn restore_exact_resident(
         &self,
         position: SnowStage3V11ExactResidentPositionV4,
@@ -213,7 +233,13 @@
         phase_plan_sha256: &Sha256Hex,
         day_input_digests: &[Sha256Hex],
     ) -> Result<Self, SnowStage3V11RestartError> {
-        Self::project_active_base(value, phase_plan_sha256, day_input_digests, false, ScientificAdmission::Ordinary)
+        Self::project_active_base(
+            value,
+            phase_plan_sha256,
+            day_input_digests,
+            false,
+            ScientificAdmission::Ordinary,
+        )
     }
 
     pub(crate) fn project_active_base_v3(
@@ -221,14 +247,27 @@
         phase_plan_sha256: &Sha256Hex,
         day_input_digests: &[Sha256Hex],
     ) -> Result<Self, SnowStage3V11RestartError> {
-        Self::project_active_base(value, phase_plan_sha256, day_input_digests, true, ScientificAdmission::Ordinary)
+        Self::project_active_base(
+            value,
+            phase_plan_sha256,
+            day_input_digests,
+            true,
+            ScientificAdmission::Ordinary,
+        )
     }
 
     pub(crate) fn project_active_base_native(
-        value: &DirectSnowStage3V11ShadowAttachment, phase_plan_sha256: &Sha256Hex,
+        value: &DirectSnowStage3V11ShadowAttachment,
+        phase_plan_sha256: &Sha256Hex,
         day_input_digests: &[Sha256Hex],
     ) -> Result<Self, SnowStage3V11RestartError> {
-        Self::project_active_base(value, phase_plan_sha256, day_input_digests, true, ScientificAdmission::NativeB01)
+        Self::project_active_base(
+            value,
+            phase_plan_sha256,
+            day_input_digests,
+            true,
+            ScientificAdmission::NativeB01,
+        )
     }
 
     fn project_active_base(
@@ -579,29 +618,50 @@
         // The archived wire retains the final checkpoint and candidate posture,
         // not a duplicate candidate blob. Reconstruct that decoded cache through
         // the same final-event/owner checks as the current committed candidate.
-        let archived = archived.map(|receipt| {
-            if receipt.ending_v11_parent_state.checkpoint() != self.committed.v11_parent_checkpoint
-                || receipt.ending_coupled_owner_set_sha256 != complete_owner_set_digest(committed.coupled_clock.owners()).map_err(nested)?
-                || receipt.ending_coupled_accepted_until_ns != committed.coupled_clock.accepted_until()
-                || receipt.ending_next_parent_sequence != committed.next_parent_sequence {
-                return Err(SnowStage3V11RestartError::Identity("archived candidate exact checkpoint/clock endpoint"));
-            }
-            let mut decoded = receipt.clone();
-            match (receipt.ending_last_v11_parent_candidate.as_ref(), committed.last_v11_parent_candidate.as_ref()) {
-                (Some(cached), Some(current)) => {
-                    let rebound = reconstruct_completed_parent_candidate(
-                        &self.committed.v11_parent_checkpoint, cached.clone(),
-                        &committed.coupled_clock, &committed.real_consumer)?;
-                    if !rebound.has_same_checkpoint_authority(current) {
-                        return Err(SnowStage3V11RestartError::Identity("archived reconstructed complete candidate authority"));
+        let archived = archived
+            .map(|receipt| {
+                if receipt.ending_v11_parent_state.checkpoint()
+                    != self.committed.v11_parent_checkpoint
+                    || receipt.ending_coupled_owner_set_sha256
+                        != complete_owner_set_digest(committed.coupled_clock.owners())
+                            .map_err(nested)?
+                    || receipt.ending_coupled_accepted_until_ns
+                        != committed.coupled_clock.accepted_until()
+                    || receipt.ending_next_parent_sequence != committed.next_parent_sequence
+                {
+                    return Err(SnowStage3V11RestartError::Identity(
+                        "archived candidate exact checkpoint/clock endpoint",
+                    ));
+                }
+                let mut decoded = receipt.clone();
+                match (
+                    receipt.ending_last_v11_parent_candidate.as_ref(),
+                    committed.last_v11_parent_candidate.as_ref(),
+                ) {
+                    (Some(cached), Some(current)) => {
+                        let rebound = reconstruct_completed_parent_candidate(
+                            &self.committed.v11_parent_checkpoint,
+                            cached.clone(),
+                            &committed.coupled_clock,
+                            &committed.real_consumer,
+                        )?;
+                        if !rebound.has_same_checkpoint_authority(current) {
+                            return Err(SnowStage3V11RestartError::Identity(
+                                "archived reconstructed complete candidate authority",
+                            ));
+                        }
+                        decoded.ending_last_v11_parent_candidate = Some(rebound);
                     }
-                    decoded.ending_last_v11_parent_candidate = Some(rebound);
+                    (None, None) => {}
+                    _ => {
+                        return Err(SnowStage3V11RestartError::Identity(
+                            "archived candidate posture",
+                        ));
+                    }
                 }
-                (None, None) => {},
-                _ => return Err(SnowStage3V11RestartError::Identity("archived candidate posture")),
-            }
-            Ok(decoded)
-        }).transpose()?;
+                Ok(decoded)
+            })
+            .transpose()?;
         let pending_candidate = self
             .pending_candidate
             .as_deref()
@@ -701,7 +761,12 @@
         .restart_authority_wb14_parent_canonical_bytes()
         .map_err(nested)?;
     let real_consumer = if admission == ScientificAdmission::NativeB01 {
-        crate::projection::project_complete_owner_state_native(consumer, phase_plan_sha256, day_input_digests, consumer_next_day)
+        crate::projection::project_complete_owner_state_native(
+            consumer,
+            phase_plan_sha256,
+            day_input_digests,
+            consumer_next_day,
+        )
     } else if for_v3 {
         project_complete_owner_state_v1_for_exact_parent(
             consumer,
@@ -861,9 +926,14 @@
     let represented_snow = value.stage3_by_lane.iter().any(|(_, state)| {
         openwepp_hillslope_orchestrator::snow_stage3_v11_attachment::restart_authority_stage3_has_represented_ice_v2(state)
     });
-    let native_surface_bytes = exact_restorer.map(|restorer|
-        restorer.validated_native_surface_bytes(exact_position)).transpose()?.flatten();
-    let native_wb14_bytes = exact_restorer.map(|restorer| restorer.native_wb14_parent_bytes(exact_position)).transpose()?.flatten();
+    let native_surface_bytes = exact_restorer
+        .map(|restorer| restorer.validated_native_surface_bytes(exact_position))
+        .transpose()?
+        .flatten();
+    let native_wb14_bytes = exact_restorer
+        .map(|restorer| restorer.native_wb14_parent_bytes(exact_position))
+        .transpose()?
+        .flatten();
     let mut real_consumer = restore_real_consumer(
         value,
         context.real_consumer_context,
@@ -871,42 +941,84 @@
         &coupled_clock,
         for_v3,
         represented_snow,
-        exact_restorer.map_or(ScientificAdmission::Ordinary, |restorer| restorer.scientific_admission()),
+        exact_restorer.map_or(ScientificAdmission::Ordinary, |restorer| {
+            restorer.scientific_admission()
+        }),
         native_surface_bytes.as_deref(),
         exact_position,
         exact_restorer.and_then(|restorer| restorer.native_bootstrap_soil()),
         exact_restorer.and_then(|restorer| restorer.native_surface_configuration()),
         native_wb14_bytes.as_deref(),
     )?;
-    let native_lse_clock_bytes = if exact_restorer.is_some_and(|restorer|
-        restorer.scientific_admission() == ScientificAdmission::NativeB01) {
-        let mut owners = coupled_clock.owners().iter().filter(|owner| owner.owner_id() == "land_surface_energy");
-        let bytes = owners.next().ok_or(SnowStage3V11RestartError::Identity("native unique LSE clock mode absent"))?.state_bytes();
-        if owners.next().is_some() { return Err(SnowStage3V11RestartError::Identity("native duplicate LSE clock mode")); }
-        real_consumer.restart_authority_install_clock_runtime_mode(bytes).map_err(nested)?;
+    let native_lse_clock_bytes = if exact_restorer
+        .is_some_and(|restorer| restorer.scientific_admission() == ScientificAdmission::NativeB01)
+    {
+        let mut owners = coupled_clock
+            .owners()
+            .iter()
+            .filter(|owner| owner.owner_id() == "land_surface_energy");
+        let bytes = owners
+            .next()
+            .ok_or(SnowStage3V11RestartError::Identity(
+                "native unique LSE clock mode absent",
+            ))?
+            .state_bytes();
+        if owners.next().is_some() {
+            return Err(SnowStage3V11RestartError::Identity(
+                "native duplicate LSE clock mode",
+            ));
+        }
+        real_consumer
+            .restart_authority_install_clock_runtime_mode(bytes)
+            .map_err(nested)?;
         Some(bytes)
-    } else { None };
+    } else {
+        None
+    };
     if let Some(restorer) = exact_restorer {
         restorer.restore_exact_resident(exact_position, &mut real_consumer)?;
     }
     if let Some(expected) = native_lse_clock_bytes {
-        if real_consumer.restart_authority_complete_lse_clock_bytes().map_err(nested)?.as_slice() != expected {
-            return Err(SnowStage3V11RestartError::Identity("native exact complete LSE clock mode/owner"));
-        }
-    }
-    if exact_restorer.is_some_and(|restorer| restorer.scientific_admission() == ScientificAdmission::NativeB01) {
-        let actual = real_consumer.restart_authority_complete_surface_clock_bytes().map_err(nested)?;
-        let surfaces = coupled_clock.owners().iter().filter(|owner| owner.owner_id() == "surface_liquid").collect::<Vec<_>>();
+        if real_consumer
+            .restart_authority_complete_lse_clock_bytes()
+            .map_err(nested)?
+            .as_slice()
+            != expected
+        {
+            return Err(SnowStage3V11RestartError::Identity(
+                "native exact complete LSE clock mode/owner",
+            ));
+        }
+    }
+    if exact_restorer
+        .is_some_and(|restorer| restorer.scientific_admission() == ScientificAdmission::NativeB01)
+    {
+        let actual = real_consumer
+            .restart_authority_complete_surface_clock_bytes()
+            .map_err(nested)?;
+        let surfaces = coupled_clock
+            .owners()
+            .iter()
+            .filter(|owner| owner.owner_id() == "surface_liquid")
+            .collect::<Vec<_>>();
         if surfaces.len() != 1 {
-            return Err(SnowStage3V11RestartError::Identity("native restored unique surface clock owner"));
+            return Err(SnowStage3V11RestartError::Identity(
+                "native restored unique surface clock owner",
+            ));
         }
         let matches = match native_surface_clock_view(surfaces[0].state_bytes())? {
             NativeSurfaceClockView::Complete => actual.as_slice() == surfaces[0].state_bytes(),
             // The native installer has already validated and exactly reprojected
             // this position, including these canonical native surface bytes.
-            NativeSurfaceClockView::NativeV2 => native_surface_bytes.as_deref() == Some(surfaces[0].state_bytes()),
+            NativeSurfaceClockView::NativeV2 => {
+                native_surface_bytes.as_deref() == Some(surfaces[0].state_bytes())
+            }
         };
-        if !matches { return Err(SnowStage3V11RestartError::Identity("native restored surface clock view")); }
+        if !matches {
+            return Err(SnowStage3V11RestartError::Identity(
+                "native restored surface clock view",
+            ));
+        }
     }
     // The checkpoint is the authority for the exact dynamic parent state.
     // `initial_state_sha256` is an imported receipt and is intentionally not
@@ -954,8 +1066,11 @@
             "V11 parent/coupled-clock chronology join",
         ));
     }
-    if exact_restorer.is_some_and(|restorer| restorer.scientific_admission() == ScientificAdmission::NativeB01) {
-        v11_parent_state.restore_evaluator_cache_from_accepted_slabs(coupled_clock.accepted_slab_receipts())
+    if exact_restorer
+        .is_some_and(|restorer| restorer.scientific_admission() == ScientificAdmission::NativeB01)
+    {
+        v11_parent_state
+            .restore_evaluator_cache_from_accepted_slabs(coupled_clock.accepted_slab_receipts())
             .map_err(nested)?;
     }
     if let Some(publication_ending) = real_consumer
@@ -1058,11 +1173,19 @@
     admission: ScientificAdmission,
     native_surface_bytes: Option<&[u8]>,
     exact_position: SnowStage3V11ExactResidentPositionV4,
-    bootstrap_soil: Option<&openwepp_hillslope_orchestrator::v9_real_consumer_shadow::DirectV10SoilThermalResidentV2>,
-    native_surface_configuration: Option<&openwepp_hillslope_orchestrator::SurfaceLiquidConfigurationV2>,
+    bootstrap_soil: Option<
+        &openwepp_hillslope_orchestrator::v9_real_consumer_shadow::DirectV10SoilThermalResidentV2,
+    >,
+    native_surface_configuration: Option<
+        &openwepp_hillslope_orchestrator::SurfaceLiquidConfigurationV2,
+    >,
     native_wb14_bytes: Option<&[u8]>,
 ) -> Result<DirectV10RealConsumerShadow, SnowStage3V11RestartError> {
-    value.real_consumer.scientific.validate_soil_posture(admission).map_err(SnowStage3V11RestartError::RealConsumerAdmission)?;
+    value
+        .real_consumer
+        .scientific
+        .validate_soil_posture(admission)
+        .map_err(SnowStage3V11RestartError::RealConsumerAdmission)?;
     let (run, topology) = crate::projection::checkpoint_identities_with_soil(
         &value.real_consumer,
         context.root_zone_hydraulic_configuration,
@@ -1113,44 +1236,120 @@
     // completed coupled day. Authenticate its exact independent custody before
     // withholding it from the private legacy scheduler/lineage admission copy.
     if admission == ScientificAdmission::NativeB01 && !posture.is_in_progress() {
-        let surfaces = coupled_clock.owners().iter().filter(|owner| owner.owner_id() == "surface_liquid")
-            .map(|owner| owner.state_bytes()).collect::<Vec<_>>();
+        let surfaces = coupled_clock
+            .owners()
+            .iter()
+            .filter(|owner| owner.owner_id() == "surface_liquid")
+            .map(|owner| owner.state_bytes())
+            .collect::<Vec<_>>();
         let retained = withhold_native_between_days_surface_v1(
-            &mut admitted_consumer.scientific.direct_hydrology.surface_liquid_owned_state,
-            context.surface_liquid_configuration, &surfaces, wb14_parent.as_deref(), native_surface_bytes,
+            &mut admitted_consumer
+                .scientific
+                .direct_hydrology
+                .surface_liquid_owned_state,
+            context.surface_liquid_configuration,
+            &surfaces,
+            wb14_parent.as_deref(),
+            native_surface_bytes,
         )?;
         if v3_between_days_surface_liquid_bytes.as_deref() != Some(retained.as_slice()) {
-            return Err(SnowStage3V11RestartError::Identity("native between-days retained surface mismatch"));
-        }
-    }
-    use openwepp_hillslope_orchestrator::v9_real_consumer_shadow::{NativeSoilRestartAdmissionV1, NativeSoilRestartPositionV1};
+            return Err(SnowStage3V11RestartError::Identity(
+                "native between-days retained surface mismatch",
+            ));
+        }
+    }
+    use openwepp_hillslope_orchestrator::v9_real_consumer_shadow::{
+        NativeSoilRestartAdmissionV1, NativeSoilRestartPositionV1,
+    };
     let native_position = match exact_position {
         SnowStage3V11ExactResidentPositionV4::Committed => NativeSoilRestartPositionV1::Committed,
-        SnowStage3V11ExactResidentPositionV4::PendingCandidate => NativeSoilRestartPositionV1::PendingCandidate,
-        SnowStage3V11ExactResidentPositionV4::InProgressDayCandidate => NativeSoilRestartPositionV1::InProgressDayCandidate,
-        SnowStage3V11ExactResidentPositionV4::InProgressSupportCurrent => NativeSoilRestartPositionV1::InProgressSupportCurrent,
+        SnowStage3V11ExactResidentPositionV4::PendingCandidate => {
+            NativeSoilRestartPositionV1::PendingCandidate
+        }
+        SnowStage3V11ExactResidentPositionV4::InProgressDayCandidate => {
+            NativeSoilRestartPositionV1::InProgressDayCandidate
+        }
+        SnowStage3V11ExactResidentPositionV4::InProgressSupportCurrent => {
+            NativeSoilRestartPositionV1::InProgressSupportCurrent
+        }
     };
     let mut native_proof = if admission == ScientificAdmission::NativeB01 {
-        let resident = value.real_consumer.scientific.native_soil.as_ref()
-            .ok_or(SnowStage3V11RestartError::Identity("native soil proof missing selected resident"))?
-            .restore_resident().map_err(SnowStage3V11RestartError::RealConsumerAdmission)?;
-        let bootstrap = bootstrap_soil.ok_or(SnowStage3V11RestartError::Identity("native soil proof missing independent bootstrap"))?;
-        let logical_revision = value.real_consumer.scientific.lse_v2.last_accepted_transaction_id.as_ref()
-            .map_or(value.real_consumer.scientific.vegetation_v10.last_transaction_id.to_u128(), HexU128::to_u128);
-        Some(NativeSoilRestartAdmissionV1::authenticate(&resident, bootstrap, context.lse_configuration,
-            context.soil_thermal_owner_id, context.soil_thermal_configuration_sha256,
-            &context.forcing_static_configuration.run_id, coupled_clock,
-            native_position, logical_revision, usize::try_from(value.real_consumer_next_day_index.0)
-                .map_err(|_| SnowStage3V11RestartError::Identity("native soil proof day width"))?,
-            context.surface_liquid_configuration.run_id).map_err(nested)?)
-    } else { None };
-    if exact_accepted_interval_count == 0 && expected_between_days_count != 0
-        && admission == ScientificAdmission::NativeB01 {
-        let proof = native_proof.take().ok_or(SnowStage3V11RestartError::Identity("native inactive scheduler proof absent"))?;
-        let surface = v3_between_days_surface_liquid_bytes.as_deref()
-            .ok_or(SnowStage3V11RestartError::Identity("native inactive scheduler original surface absent"))?;
-        native_proof = Some(proof.bind_inactive_scheduler(exact_accepted_interval_count,
-            context.surface_liquid_configuration,surface,wb14_parent.as_deref(),native_surface_bytes,native_surface_configuration,native_wb14_bytes).map_err(nested)?);
+        let resident = value
+            .real_consumer
+            .scientific
+            .native_soil
+            .as_ref()
+            .ok_or(SnowStage3V11RestartError::Identity(
+                "native soil proof missing selected resident",
+            ))?
+            .restore_resident()
+            .map_err(SnowStage3V11RestartError::RealConsumerAdmission)?;
+        let bootstrap = bootstrap_soil.ok_or(SnowStage3V11RestartError::Identity(
+            "native soil proof missing independent bootstrap",
+        ))?;
+        let logical_revision = value
+            .real_consumer
+            .scientific
+            .lse_v2
+            .last_accepted_transaction_id
+            .as_ref()
+            .map_or(
+                value
+                    .real_consumer
+                    .scientific
+                    .vegetation_v10
+                    .last_transaction_id
+                    .to_u128(),
+                HexU128::to_u128,
+            );
+        Some(
+            NativeSoilRestartAdmissionV1::authenticate(
+                &resident,
+                bootstrap,
+                context.lse_configuration,
+                context.soil_thermal_owner_id,
+                context.soil_thermal_configuration_sha256,
+                &context.forcing_static_configuration.run_id,
+                coupled_clock,
+                native_position,
+                logical_revision,
+                usize::try_from(value.real_consumer_next_day_index.0).map_err(|_| {
+                    SnowStage3V11RestartError::Identity("native soil proof day width")
+                })?,
+                context.surface_liquid_configuration.run_id,
+            )
+            .map_err(nested)?,
+        )
+    } else {
+        None
+    };
+    if exact_accepted_interval_count == 0
+        && expected_between_days_count != 0
+        && admission == ScientificAdmission::NativeB01
+    {
+        let proof = native_proof
+            .take()
+            .ok_or(SnowStage3V11RestartError::Identity(
+                "native inactive scheduler proof absent",
+            ))?;
+        let surface = v3_between_days_surface_liquid_bytes.as_deref().ok_or(
+            SnowStage3V11RestartError::Identity(
+                "native inactive scheduler original surface absent",
+            ),
+        )?;
+        native_proof = Some(
+            proof
+                .bind_inactive_scheduler(
+                    exact_accepted_interval_count,
+                    context.surface_liquid_configuration,
+                    surface,
+                    wb14_parent.as_deref(),
+                    native_surface_bytes,
+                    native_surface_configuration,
+                    native_wb14_bytes,
+                )
+                .map_err(nested)?,
+        );
     }
     let logical_finalization = normalize_parent_finalization_for_v1_admission(
         &mut admitted_consumer,
@@ -1231,9 +1430,15 @@
     })?;
     let restored = crate::checkpoint::admit_checkpoint_with_soil(&bytes, context, admission)
         .map_err(SnowStage3V11RestartError::RealConsumerAdmission)?;
-    let host = DirectV10RestartHost::from_isolated_with_soil(restored, context, admission,
-        native_proof.as_ref().map(|proof| (proof, coupled_clock, native_position)))
-        .map_err(|error| nested_phase("real-consumer checkpoint host reconstruction", error))?;
+    let host = DirectV10RestartHost::from_isolated_with_soil(
+        restored,
+        context,
+        admission,
+        native_proof
+            .as_ref()
+            .map(|proof| (proof, coupled_clock, native_position)),
+    )
+    .map_err(|error| nested_phase("real-consumer checkpoint host reconstruction", error))?;
     let mut shadow = host.shadow().clone();
     if shadow.restart_authority_accepted_interval_count() != admitted_interval_count.get() {
         return Err(SnowStage3V11RestartError::Identity(
@@ -1246,8 +1451,14 @@
         .0
         .last_accepted_transaction_id
         .map(|value| value.0);
-    let native_soil = matches!(shadow.restart_authority_soil_resident(), openwepp_hillslope_orchestrator::v9_real_consumer_shadow::DirectSoilThermalResident::V2(_));
-    let admitted_soil = shadow.restart_authority_soil_resident().last_accepted_transaction_id().map(|value| value.0);
+    let native_soil = matches!(
+        shadow.restart_authority_soil_resident(),
+        openwepp_hillslope_orchestrator::v9_real_consumer_shadow::DirectSoilThermalResident::V2(_)
+    );
+    let admitted_soil = shadow
+        .restart_authority_soil_resident()
+        .last_accepted_transaction_id()
+        .map(|value| value.0);
     let admitted_biogeochemistry = shadow
         .restart_authority_biogeochemistry()
         .last_transaction_id;
@@ -1267,18 +1478,33 @@
     }
     if let Some(surface_liquid_bytes) = exact_surface_liquid_bytes.as_deref() {
         if admission == ScientificAdmission::NativeB01 {
-            shadow.restart_authority_restore_native_surface_liquid_canonical_bytes(surface_liquid_bytes)
+            shadow.restart_authority_restore_native_surface_liquid_canonical_bytes(
+                surface_liquid_bytes,
+            )
         } else {
             shadow.restart_authority_restore_surface_liquid_canonical_bytes(surface_liquid_bytes)
-        }.map_err(|error| nested_phase("surface-liquid owner restore", error))?;
+        }
+        .map_err(|error| nested_phase("surface-liquid owner restore", error))?;
         if exact_accepted_interval_count != expected_between_days_count {
             if exact_accepted_interval_count == 0 && admission == ScientificAdmission::NativeB01 {
-                let proof = native_proof.as_ref().ok_or(SnowStage3V11RestartError::Identity("native inactive scheduler restoring proof absent"))?;
-                shadow.restart_authority_install_native_inactive_scheduler_position(proof,coupled_clock,native_position)
-                    .map_err(|error| nested_phase("native inactive scheduler position restore",error))?;
+                let proof = native_proof
+                    .as_ref()
+                    .ok_or(SnowStage3V11RestartError::Identity(
+                        "native inactive scheduler restoring proof absent",
+                    ))?;
+                shadow
+                    .restart_authority_install_native_inactive_scheduler_position(
+                        proof,
+                        coupled_clock,
+                        native_position,
+                    )
+                    .map_err(|error| {
+                        nested_phase("native inactive scheduler position restore", error)
+                    })?;
             } else {
-                shadow.restart_authority_install_scheduler_position(exact_accepted_interval_count)
-                    .map_err(|error| nested_phase("scheduler position restore",error))?;
+                shadow
+                    .restart_authority_install_scheduler_position(exact_accepted_interval_count)
+                    .map_err(|error| nested_phase("scheduler position restore", error))?;
             }
         }
     }
@@ -1304,10 +1530,16 @@
             .map_err(nested)?;
     }
     if let Some(proof) = native_proof.as_ref() {
-        shadow.restart_authority_restore_native_wb14_parent_canonical_bytes(wb14_parent.as_deref(), proof, coupled_clock, native_position)
+        shadow.restart_authority_restore_native_wb14_parent_canonical_bytes(
+            wb14_parent.as_deref(),
+            proof,
+            coupled_clock,
+            native_position,
+        )
     } else {
         shadow.restart_authority_restore_wb14_parent_canonical_bytes(wb14_parent.as_deref())
-    }.map_err(|error| nested_phase("WB14 parent restore", error))?;
+    }
+    .map_err(|error| nested_phase("WB14 parent restore", error))?;
     let accepted_publication_supports = decode_blob(
         &value.accepted_publication_supports_canonical_base64,
         &value.accepted_publication_supports_sha256,
@@ -1327,10 +1559,20 @@
     }
     if let Some((vegetation, biogeochemistry)) = logical_finalization {
         if let Some(proof) = native_proof.as_ref() {
-            shadow.restart_authority_restore_native_parent_finalization_logical_owners(vegetation, biogeochemistry, proof, coupled_clock, native_position)
+            shadow.restart_authority_restore_native_parent_finalization_logical_owners(
+                vegetation,
+                biogeochemistry,
+                proof,
+                coupled_clock,
+                native_position,
+            )
         } else {
-            shadow.restart_authority_restore_parent_finalization_logical_owners(vegetation, biogeochemistry)
-        }.map_err(|error| nested_phase("parent-finalization logical owners", error))?;
+            shadow.restart_authority_restore_parent_finalization_logical_owners(
+                vegetation,
+                biogeochemistry,
+            )
+        }
+        .map_err(|error| nested_phase("parent-finalization logical owners", error))?;
     }
     Ok(shadow)
 }
@@ -1342,41 +1584,73 @@
     wb14_parent_bytes: Option<&[u8]>,
     native_surface_bytes: Option<&[u8]>,
 ) -> Result<Vec<u8>, SnowStage3V11RestartError> {
-    let surface = admitted_surface.as_deref().ok_or(SnowStage3V11RestartError::Identity(
-        "native between-days exact surface missing"))?;
-    let exact = surface.restore_with_configuration(configuration).map_err(nested)?
-        .canonical_bytes(configuration).map_err(nested)?;
+    let surface = admitted_surface
+        .as_deref()
+        .ok_or(SnowStage3V11RestartError::Identity(
+            "native between-days exact surface missing",
+        ))?;
+    let exact = surface
+        .restore_with_configuration(configuration)
+        .map_err(nested)?
+        .canonical_bytes(configuration)
+        .map_err(nested)?;
     if native_surface_bytes.is_none() {
-        return Err(SnowStage3V11RestartError::Identity("native between-days supplement missing"));
-    }
-    if clock_surface_bytes.len() != 1 || !surface_clock_components_match(
-        clock_surface_bytes[0], &exact, wb14_parent_bytes, ScientificAdmission::NativeB01, native_surface_bytes,
-    )? {
-        return Err(SnowStage3V11RestartError::Identity("native between-days surface clock mismatch"));
+        return Err(SnowStage3V11RestartError::Identity(
+            "native between-days supplement missing",
+        ));
+    }
+    if clock_surface_bytes.len() != 1
+        || !surface_clock_components_match(
+            clock_surface_bytes[0],
+            &exact,
+            wb14_parent_bytes,
+            ScientificAdmission::NativeB01,
+            native_surface_bytes,
+        )?
+    {
+        return Err(SnowStage3V11RestartError::Identity(
+            "native between-days surface clock mismatch",
+        ));
     }
     *admitted_surface = None;
     Ok(exact)
 }
 
 #[derive(Clone, Copy, PartialEq, Eq)]
-enum NativeSurfaceClockView { Complete, NativeV2 }
-fn native_surface_clock_view(bytes: &[u8]) -> Result<NativeSurfaceClockView, SnowStage3V11RestartError> {
+enum NativeSurfaceClockView {
+    Complete,
+    NativeV2,
+}
+fn native_surface_clock_view(
+    bytes: &[u8],
+) -> Result<NativeSurfaceClockView, SnowStage3V11RestartError> {
     let value: serde_json::Value = serde_json::from_slice(bytes).map_err(nested)?;
-    match (value.get("schema").and_then(serde_json::Value::as_str),
-           value.get("version").and_then(serde_json::Value::as_str)) {
-        (Some("OPENWEPP_SURFACE_LIQUID_COMPLETE_OWNER_PROJECTION_V2"), None) => Ok(NativeSurfaceClockView::Complete),
+    match (
+        value.get("schema").and_then(serde_json::Value::as_str),
+        value.get("version").and_then(serde_json::Value::as_str),
+    ) {
+        (Some("OPENWEPP_SURFACE_LIQUID_COMPLETE_OWNER_PROJECTION_V2"), None) => {
+            Ok(NativeSurfaceClockView::Complete)
+        }
         (None, Some("v2")) => Ok(NativeSurfaceClockView::NativeV2),
-        _ => Err(SnowStage3V11RestartError::Identity("native clock surface view schema")),
+        _ => Err(SnowStage3V11RestartError::Identity(
+            "native clock surface view schema",
+        )),
     }
 }
 
 // Private admission proof compares both original components. Exact canonical
 // full-owner bytes are checked against the restored consumer before return.
 fn surface_clock_components_match(
-    clock_bytes: &[u8], surface_bytes: &[u8], wb14_bytes: Option<&[u8]>, admission: ScientificAdmission,
+    clock_bytes: &[u8],
+    surface_bytes: &[u8],
+    wb14_bytes: Option<&[u8]>,
+    admission: ScientificAdmission,
     native_surface_bytes: Option<&[u8]>,
 ) -> Result<bool, SnowStage3V11RestartError> {
-    if admission == ScientificAdmission::Ordinary { return Ok(clock_bytes == surface_bytes); }
+    if admission == ScientificAdmission::Ordinary {
+        return Ok(clock_bytes == surface_bytes);
+    }
     if native_surface_clock_view(clock_bytes)? == NativeSurfaceClockView::NativeV2 {
         return Ok(native_surface_bytes == Some(clock_bytes));
     }
@@ -1389,10 +1663,18 @@
     }
     let clock: CompleteSurface = serde_json::from_slice(clock_bytes).map_err(nested)?;
     let surface: serde_json::Value = serde_json::from_slice(surface_bytes).map_err(nested)?;
-    let wb14: serde_json::Value = wb14_bytes.map(serde_json::from_slice).transpose().map_err(nested)?.unwrap_or(serde_json::Value::Null);
-    Ok(clock.schema == "OPENWEPP_SURFACE_LIQUID_COMPLETE_OWNER_PROJECTION_V2"
-        && serde_json::to_vec(&clock.persistent_owner).map_err(nested)? == serde_json::to_vec(&surface).map_err(nested)?
-        && serde_json::to_vec(&clock.wb14_parent_working_state).map_err(nested)? == serde_json::to_vec(&wb14).map_err(nested)?)
+    let wb14: serde_json::Value = wb14_bytes
+        .map(serde_json::from_slice)
+        .transpose()
+        .map_err(nested)?
+        .unwrap_or(serde_json::Value::Null);
+    Ok(
+        clock.schema == "OPENWEPP_SURFACE_LIQUID_COMPLETE_OWNER_PROJECTION_V2"
+            && serde_json::to_vec(&clock.persistent_owner).map_err(nested)?
+                == serde_json::to_vec(&surface).map_err(nested)?
+            && serde_json::to_vec(&clock.wb14_parent_working_state).map_err(nested)?
+                == serde_json::to_vec(&wb14).map_err(nested)?,
+    )
 }
 
 fn normalize_parent_finalization_for_v1_admission(
@@ -1420,9 +1702,15 @@
         .last_accepted_transaction_id
         .as_ref()
         .map(HexU128::to_u128);
-    scientific.validate_soil_posture(admission).map_err(SnowStage3V11RestartError::RealConsumerAdmission)?;
+    scientific
+        .validate_soil_posture(admission)
+        .map_err(SnowStage3V11RestartError::RealConsumerAdmission)?;
     let native_soil = scientific.native_soil.is_some();
-    let soil_transaction = scientific.soil_thermal.as_ref().and_then(|soil| soil.last_accepted_transaction_id.as_ref()).map(HexU128::to_u128);
+    let soil_transaction = scientific
+        .soil_thermal
+        .as_ref()
+        .and_then(|soil| soil.last_accepted_transaction_id.as_ref())
+        .map(HexU128::to_u128);
     let surface_lineages = scientific
         .direct_hydrology
         .surface_liquid_owned_state
@@ -1502,7 +1790,13 @@
             .filter(|owner| owner.owner_id() == surface_owner_id)
             .collect::<Vec<_>>();
         if matching_surface_owners.len() != 1
-            || !surface_clock_components_match(matching_surface_owners[0].state_bytes(), &exact_surface_bytes, wb14_parent_bytes, admission, native_surface_bytes)?
+            || !surface_clock_components_match(
+                matching_surface_owners[0].state_bytes(),
+                &exact_surface_bytes,
+                wb14_parent_bytes,
+                admission,
+                native_surface_bytes,
+            )?
         {
             return Err(SnowStage3V11RestartError::Identity(
                 "receiver surface clock owner",
@@ -1624,7 +1918,13 @@
             .filter(|owner| owner.owner_id() == "surface_liquid")
             .collect::<Vec<_>>();
         if matching_surface_owners.len() != 1
-            || !surface_clock_components_match(matching_surface_owners[0].state_bytes(), &exact_surface_bytes, wb14_parent_bytes, admission, native_surface_bytes)?
+            || !surface_clock_components_match(
+                matching_surface_owners[0].state_bytes(),
+                &exact_surface_bytes,
+                wb14_parent_bytes,
+                admission,
+                native_surface_bytes,
+            )?
         {
             return Err(SnowStage3V11RestartError::Identity(
                 "parent-finalization V3 surface clock owner",
@@ -1656,7 +1956,8 @@
         .biogeochemistry
         .restore()
         .map_err(nested)?;
-    let normalized_vegetation = vegetation_for_predecessor_admission(&finalized_vegetation, predecessor);
+    let normalized_vegetation =
+        vegetation_for_predecessor_admission(&finalized_vegetation, predecessor);
     admitted.scientific.vegetation_v10 = VegetationV10StateRestartV1::project(
         &normalized_vegetation,
         context.vegetation_configuration,
@@ -1737,38 +2038,162 @@
         // installer separately parses real envelopes with configuration.
         let selected = br#"{"version":"v2","state":"selected-position"}"#;
         let other = br#"{"version":"v2","state":"other-position"}"#;
-        assert!(surface_clock_components_match(selected,b"{}",None,ScientificAdmission::NativeB01,Some(selected)).unwrap());
-        assert!(!surface_clock_components_match(selected,b"{}",None,ScientificAdmission::NativeB01,Some(other)).unwrap());
-        assert!(!surface_clock_components_match(selected,b"{}",None,ScientificAdmission::NativeB01,None).unwrap());
-        for invalid in [br#"{"version":"v1"}"#.as_slice(),
+        assert!(
+            surface_clock_components_match(
+                selected,
+                b"{}",
+                None,
+                ScientificAdmission::NativeB01,
+                Some(selected)
+            )
+            .unwrap()
+        );
+        assert!(
+            !surface_clock_components_match(
+                selected,
+                b"{}",
+                None,
+                ScientificAdmission::NativeB01,
+                Some(other)
+            )
+            .unwrap()
+        );
+        assert!(
+            !surface_clock_components_match(
+                selected,
+                b"{}",
+                None,
+                ScientificAdmission::NativeB01,
+                None
+            )
+            .unwrap()
+        );
+        for invalid in [
+            br#"{"version":"v1"}"#.as_slice(),
             br#"{"version":"v3"}"#.as_slice(),
-            br#"{"schema":"OPENWEPP_SURFACE_LIQUID_COMPLETE_OWNER_PROJECTION_V2","version":"v2"}"#.as_slice()] {
-            assert!(surface_clock_components_match(invalid,b"{}",None,ScientificAdmission::NativeB01,Some(invalid)).is_err());
-        }
-        assert!(!surface_clock_components_match(selected,b"{}",None,ScientificAdmission::Ordinary,Some(selected)).unwrap());
+            br#"{"schema":"OPENWEPP_SURFACE_LIQUID_COMPLETE_OWNER_PROJECTION_V2","version":"v2"}"#
+                .as_slice(),
+        ] {
+            assert!(
+                surface_clock_components_match(
+                    invalid,
+                    b"{}",
+                    None,
+                    ScientificAdmission::NativeB01,
+                    Some(invalid)
+                )
+                .is_err()
+            );
+        }
+        assert!(
+            !surface_clock_components_match(
+                selected,
+                b"{}",
+                None,
+                ScientificAdmission::Ordinary,
+                Some(selected)
+            )
+            .unwrap()
+        );
     }
     #[test]
     fn native_surface_clock_requires_both_components_and_closed_schema() {
         let surface = br#"{"liquid":"8000000000000000"}"#;
         let wb14 = br#"{"transfer":-0.0}"#;
         let clock = br#"{"schema":"OPENWEPP_SURFACE_LIQUID_COMPLETE_OWNER_PROJECTION_V2","persistent_owner":{"liquid":"8000000000000000"},"wb14_parent_working_state":{"transfer":-0.0}}"#;
-        assert!(surface_clock_components_match(clock,surface,Some(wb14),ScientificAdmission::NativeB01,None).unwrap());
-        assert!(!surface_clock_components_match(clock,surface,None,ScientificAdmission::NativeB01,None).unwrap());
-        assert!(!surface_clock_components_match(clock,surface,Some(br#"{"transfer":0.0}"#),ScientificAdmission::NativeB01,None).unwrap());
-        assert!(!surface_clock_components_match(clock,b"{}",Some(wb14),ScientificAdmission::NativeB01,None).unwrap());
-        assert!(!surface_clock_components_match(clock,surface,Some(wb14),ScientificAdmission::Ordinary,None).unwrap());
-        assert!(surface_clock_components_match(surface,surface,None,ScientificAdmission::Ordinary,None).unwrap());
+        assert!(
+            surface_clock_components_match(
+                clock,
+                surface,
+                Some(wb14),
+                ScientificAdmission::NativeB01,
+                None
+            )
+            .unwrap()
+        );
+        assert!(
+            !surface_clock_components_match(
+                clock,
+                surface,
+                None,
+                ScientificAdmission::NativeB01,
+                None
+            )
+            .unwrap()
+        );
+        assert!(
+            !surface_clock_components_match(
+                clock,
+                surface,
+                Some(br#"{"transfer":0.0}"#),
+                ScientificAdmission::NativeB01,
+                None
+            )
+            .unwrap()
+        );
+        assert!(
+            !surface_clock_components_match(
+                clock,
+                b"{}",
+                Some(wb14),
+                ScientificAdmission::NativeB01,
+                None
+            )
+            .unwrap()
+        );
+        assert!(
+            !surface_clock_components_match(
+                clock,
+                surface,
+                Some(wb14),
+                ScientificAdmission::Ordinary,
+                None
+            )
+            .unwrap()
+        );
+        assert!(
+            surface_clock_components_match(
+                surface,
+                surface,
+                None,
+                ScientificAdmission::Ordinary,
+                None
+            )
+            .unwrap()
+        );
         for field in ["schema", "persistent_owner", "wb14_parent_working_state"] {
-            let mut v:serde_json::Value=serde_json::from_slice(clock).unwrap();v.as_object_mut().unwrap().remove(field);
-            assert!(surface_clock_components_match(&serde_json::to_vec(&v).unwrap(),surface,Some(wb14),ScientificAdmission::NativeB01,None).is_err());
-        }
-        let mut v:serde_json::Value=serde_json::from_slice(clock).unwrap();v["foreign"]=serde_json::json!(0);
-        assert!(surface_clock_components_match(&serde_json::to_vec(&v).unwrap(),surface,Some(wb14),ScientificAdmission::NativeB01,None).is_err());
-    }
-}
-
-fn vegetation_for_predecessor_admission(value: &openwepp_vegetation::V10CoupledOwnedState,
-    predecessor: u128) -> openwepp_vegetation::V10CoupledOwnedState {
+            let mut v: serde_json::Value = serde_json::from_slice(clock).unwrap();
+            v.as_object_mut().unwrap().remove(field);
+            assert!(
+                surface_clock_components_match(
+                    &serde_json::to_vec(&v).unwrap(),
+                    surface,
+                    Some(wb14),
+                    ScientificAdmission::NativeB01,
+                    None
+                )
+                .is_err()
+            );
+        }
+        let mut v: serde_json::Value = serde_json::from_slice(clock).unwrap();
+        v["foreign"] = serde_json::json!(0);
+        assert!(
+            surface_clock_components_match(
+                &serde_json::to_vec(&v).unwrap(),
+                surface,
+                Some(wb14),
+                ScientificAdmission::NativeB01,
+                None
+            )
+            .is_err()
+        );
+    }
+}
+
+fn vegetation_for_predecessor_admission(
+    value: &openwepp_vegetation::V10CoupledOwnedState,
+    predecessor: u128,
+) -> openwepp_vegetation::V10CoupledOwnedState {
     openwepp_hillslope_orchestrator::v9_real_consumer_shadow::restart_authority_vegetation_for_predecessor_admission(value, predecessor)
 }
 
@@ -1778,36 +2203,61 @@
     #[test]
     #[ignore = "requires authenticated actual068 committed owner and configuration"]
     fn actual_committed_vegetation_private_predecessor_is_valid() {
-        let path = std::path::PathBuf::from(std::env::var_os("OPENWEPP_B01_RESTART_REPLAY_INPUTS").expect("replay input directory"));
+        let path = std::path::PathBuf::from(
+            std::env::var_os("OPENWEPP_B01_RESTART_REPLAY_INPUTS").expect("replay input directory"),
+        );
         let configuration: openwepp_vegetation::VegetationConfiguration = serde_json::from_slice(
-            &std::fs::read(path.join("actual068-vegetation-config.json")).unwrap()).unwrap();
-        let actual: openwepp_vegetation::V10CoupledOwnedState = serde_json::from_slice(
-            &std::fs::read(path.join("actual068-vegetation.json")).unwrap()).unwrap();
-        actual.validate(&configuration).expect("authentic final owner valid");
-        assert_eq!(actual.0.last_transaction_id,88);
-        let actual_bytes=serde_json::to_vec(&actual).unwrap();
-        let normalized=vegetation_for_predecessor_admission(&actual,87);
-        let valid=normalized.validate(&configuration);
+            &std::fs::read(path.join("actual068-vegetation-config.json")).unwrap(),
+        )
+        .unwrap();
+        let actual: openwepp_vegetation::V10CoupledOwnedState =
+            serde_json::from_slice(&std::fs::read(path.join("actual068-vegetation.json")).unwrap())
+                .unwrap();
+        actual
+            .validate(&configuration)
+            .expect("authentic final owner valid");
+        assert_eq!(actual.0.last_transaction_id, 88);
+        let actual_bytes = serde_json::to_vec(&actual).unwrap();
+        let normalized = vegetation_for_predecessor_admission(&actual, 87);
+        let valid = normalized.validate(&configuration);
         println!("actual T88 valid; normalized T87 validation: {valid:?}");
         valid.expect("private admission predecessor retains coherent nested markers");
-        assert_eq!(serde_json::to_vec(&actual).unwrap(),actual_bytes);
-        let initial_admission=vegetation_for_predecessor_admission(&actual,0);
-        assert!(initial_admission.validate(&configuration).is_err(),
-            "evolved owner must not pass as the authenticated initial state");
-        assert!(initial_admission.0.occupancies.values().all(|row| row.last_accepted_transaction_id.is_none()));
-        let mut restored=normalized.clone();
-        restored.0.last_transaction_id=actual.0.last_transaction_id;
-        for (key,stratum) in &mut restored.0.strata {stratum.last_transaction_id=actual.0.strata[key].last_transaction_id;}
-        for (key,occupancy) in &mut restored.0.occupancies {occupancy.last_accepted_transaction_id=actual.0.occupancies[key].last_accepted_transaction_id;}
-        restored.0.state_sha256=restored.0.canonical_sha256();
-        assert_eq!(restored,actual,"no physical state changed");
-        let mut poison=normalized;
-        poison.0.occupancies.values_mut().next().unwrap().last_accepted_transaction_id=Some(88);
-        poison.0.state_sha256=poison.0.canonical_sha256();
+        assert_eq!(serde_json::to_vec(&actual).unwrap(), actual_bytes);
+        let initial_admission = vegetation_for_predecessor_admission(&actual, 0);
+        assert!(
+            initial_admission.validate(&configuration).is_err(),
+            "evolved owner must not pass as the authenticated initial state"
+        );
+        assert!(
+            initial_admission
+                .0
+                .occupancies
+                .values()
+                .all(|row| row.last_accepted_transaction_id.is_none())
+        );
+        let mut restored = normalized.clone();
+        restored.0.last_transaction_id = actual.0.last_transaction_id;
+        for (key, stratum) in &mut restored.0.strata {
+            stratum.last_transaction_id = actual.0.strata[key].last_transaction_id;
+        }
+        for (key, occupancy) in &mut restored.0.occupancies {
+            occupancy.last_accepted_transaction_id =
+                actual.0.occupancies[key].last_accepted_transaction_id;
+        }
+        restored.0.state_sha256 = restored.0.canonical_sha256();
+        assert_eq!(restored, actual, "no physical state changed");
+        let mut poison = normalized;
+        poison
+            .0
+            .occupancies
+            .values_mut()
+            .next()
+            .unwrap()
+            .last_accepted_transaction_id = Some(88);
+        poison.0.state_sha256 = poison.0.canonical_sha256();
         assert!(poison.validate(&configuration).is_err());
     }
 }
-
 
 #[cfg(all(test, feature = "fixtures"))]
 mod native_between_days_surface_tests {
@@ -1815,53 +2265,128 @@
     #[test]
     fn native_unexecuted_surface_withholding_preserves_configured_owner() {
         let fixture = crate::restart_authority_owner_fixture();
-        let configuration = fixture.runtime.shadow.restart_authority_surface_configuration();
-        let liquid = configuration.records.iter().map(|record| (record.key.clone(), 0.0)).collect();
+        let configuration = fixture
+            .runtime
+            .shadow
+            .restart_authority_surface_configuration();
+        let liquid = configuration
+            .records
+            .iter()
+            .map(|record| (record.key.clone(), 0.0))
+            .collect();
         let owner = openwepp_hillslope_orchestrator::DirectSurfaceLiquidOwnedState::new_initial(
-            configuration, &liquid, 0).unwrap();
+            configuration,
+            &liquid,
+            0,
+        )
+        .unwrap();
         let exact = owner.canonical_bytes(configuration).unwrap();
-        assert!(owner.restart_authority_with_admission_lineage(configuration,
-            openwepp_kernel_contract::TransactionId(87)).is_err());
+        assert!(
+            owner
+                .restart_authority_with_admission_lineage(
+                    configuration,
+                    openwepp_kernel_contract::TransactionId(87)
+                )
+                .is_err()
+        );
         let clock = serde_json::to_vec(&serde_json::json!({
             "schema":"OPENWEPP_SURFACE_LIQUID_COMPLETE_OWNER_PROJECTION_V2",
             "persistent_owner":serde_json::from_slice::<serde_json::Value>(&exact).unwrap(),
             "wb14_parent_working_state":null,
-        })).unwrap();
-        let original = Some(Box::new(crate::DirectSurfaceLiquidOwnedStateRestartV1::project(&owner).unwrap()));
+        }))
+        .unwrap();
+        let original = Some(Box::new(
+            crate::DirectSurfaceLiquidOwnedStateRestartV1::project(&owner).unwrap(),
+        ));
         let mut admitted = original.clone();
-        let retained = withhold_native_between_days_surface_v1(&mut admitted, configuration,
-            &[&clock], None, Some(b"validated-native-supplement")).unwrap();
+        let retained = withhold_native_between_days_surface_v1(
+            &mut admitted,
+            configuration,
+            &[&clock],
+            None,
+            Some(b"validated-native-supplement"),
+        )
+        .unwrap();
         assert!(admitted.is_none());
         assert_eq!(retained, exact);
-        assert_eq!(original.as_ref().unwrap().restore_with_configuration(configuration).unwrap()
-            .canonical_bytes(configuration).unwrap(), exact);
+        assert_eq!(
+            original
+                .as_ref()
+                .unwrap()
+                .restore_with_configuration(configuration)
+                .unwrap()
+                .canonical_bytes(configuration)
+                .unwrap(),
+            exact
+        );
         for (clocks, wb14, native) in [
             (vec![clock.as_slice()], None, None),
             (vec![], None, Some(b"native".as_slice())),
-            (vec![clock.as_slice(), clock.as_slice()], None, Some(b"native".as_slice())),
-            (vec![clock.as_slice()], Some(b"{}".as_slice()), Some(b"native".as_slice())),
+            (
+                vec![clock.as_slice(), clock.as_slice()],
+                None,
+                Some(b"native".as_slice()),
+            ),
+            (
+                vec![clock.as_slice()],
+                Some(b"{}".as_slice()),
+                Some(b"native".as_slice()),
+            ),
         ] {
             let mut poisoned = original.clone();
-            assert!(withhold_native_between_days_surface_v1(&mut poisoned, configuration,
-                &clocks, wb14, native).is_err());
+            assert!(
+                withhold_native_between_days_surface_v1(
+                    &mut poisoned,
+                    configuration,
+                    &clocks,
+                    wb14,
+                    native
+                )
+                .is_err()
+            );
             assert_eq!(poisoned, original);
         }
-        let mut wrong:serde_json::Value=serde_json::from_slice(&clock).unwrap();
+        let mut wrong: serde_json::Value = serde_json::from_slice(&clock).unwrap();
         wrong["persistent_owner"]["state_sha256"] = serde_json::json!("foreign");
-        let wrong=serde_json::to_vec(&wrong).unwrap();
-        let mut poisoned=original.clone();
-        assert!(withhold_native_between_days_surface_v1(&mut poisoned, configuration,
-            &[&wrong], None, Some(b"native")).is_err());
+        let wrong = serde_json::to_vec(&wrong).unwrap();
+        let mut poisoned = original.clone();
+        assert!(
+            withhold_native_between_days_surface_v1(
+                &mut poisoned,
+                configuration,
+                &[&wrong],
+                None,
+                Some(b"native")
+            )
+            .is_err()
+        );
         assert_eq!(poisoned, original);
-        let mut missing=None;
-        assert!(withhold_native_between_days_surface_v1(&mut missing, configuration,
-            &[&clock], None, Some(b"native")).is_err());
-        let mut foreign=original.clone();
-        foreign.as_mut().unwrap().configuration_sha256 = Sha256Hex::try_new("f".repeat(64)).unwrap();
-        let before=foreign.clone();
-        assert!(withhold_native_between_days_surface_v1(&mut foreign, configuration,
-            &[&clock], None, Some(b"native")).is_err());
-        assert_eq!(foreign,before);
+        let mut missing = None;
+        assert!(
+            withhold_native_between_days_surface_v1(
+                &mut missing,
+                configuration,
+                &[&clock],
+                None,
+                Some(b"native")
+            )
+            .is_err()
+        );
+        let mut foreign = original.clone();
+        foreign.as_mut().unwrap().configuration_sha256 =
+            Sha256Hex::try_new("f".repeat(64)).unwrap();
+        let before = foreign.clone();
+        assert!(
+            withhold_native_between_days_surface_v1(
+                &mut foreign,
+                configuration,
+                &[&clock],
+                None,
+                Some(b"native")
+            )
+            .is_err()
+        );
+        assert_eq!(foreign, before);
     }
 }
 
--- a/crates/openwepp-persisted-restart-v1/src/snow_stage3_v11_native_custody.rs
+++ b/crates/openwepp-persisted-restart-v1/src/snow_stage3_v11_native_custody.rs
@@ -1,17 +1,24 @@
 //! Isolated policy14 native custody disposition; never an ordinary phase checkpoint.
-use openwepp_hillslope_orchestrator::{LseSurfaceEnthalpyOwnerEnvelopeV1,
-    SurfaceLiquidConfigurationV2, SurfaceLiquidOwnerEnvelopeV2,
+use crate::snow_stage3_v11::{
+    SnowStage3V11ExactResidentPositionV4 as Position, SnowStage3V11ExactResidentRestorerV4,
+};
+use crate::{
+    DirectSnowStage3V11AttachmentRestartV3, ExpectedSnowStage3V11RestartContext,
+    ExpectedStage3CommittedDayArchiveV3, Sha256Hex, SnowStage3V11RestartError as Error,
+    from_canonical_bytes, to_canonical_bytes,
+};
+use openwepp_hillslope_orchestrator::{
+    LseSurfaceEnthalpyOwnerEnvelopeV1, SurfaceLiquidConfigurationV2, SurfaceLiquidOwnerEnvelopeV2,
     snow_stage3_v11_attachment::DirectSnowStage3V11ShadowAttachment,
-    v9_real_consumer_shadow::{DirectV10RealConsumerShadow, FrozenLitterV3Resident, FrozenLitterV4Resident}};
-use openwepp_land_surface_energy::{LandSurfaceEnergyConfiguration, LandSurfaceEnergyState,
-    LandSurfaceEnergyV3State, Sha256Digest};
+    v9_real_consumer_shadow::{
+        DirectV10RealConsumerShadow, FrozenLitterV3Resident, FrozenLitterV4Resident,
+    },
+};
+use openwepp_land_surface_energy::{
+    LandSurfaceEnergyConfiguration, LandSurfaceEnergyState, LandSurfaceEnergyV3State, Sha256Digest,
+};
 use serde::{Deserialize, Serialize};
 use sha2::{Digest as _, Sha256};
-use crate::{DirectSnowStage3V11AttachmentRestartV3, ExpectedSnowStage3V11RestartContext,
-    ExpectedStage3CommittedDayArchiveV3, Sha256Hex, SnowStage3V11RestartError as Error,
-    from_canonical_bytes, to_canonical_bytes};
-use crate::snow_stage3_v11::{SnowStage3V11ExactResidentPositionV4 as Position,
-    SnowStage3V11ExactResidentRestorerV4};
 
 const SCHEMA: &str = "OPENWEPP_B01_NATIVE_CUSTODY_RESTART_V3_POLICY14";
 
@@ -24,11 +31,15 @@
 }
 impl NativeExecutionIdentityV1 {
     fn executing() -> Result<Self, Error> {
-        let source = option_env!("OPENWEPP_B01_SOURCE_SHA256")
-            .ok_or(Error::Identity("native restart executing source identity missing"))?;
-        let build = option_env!("OPENWEPP_B01_BUILD_INPUTS_SHA256")
-            .ok_or(Error::Identity("native restart executing build inputs identity missing"))?;
-        if openwepp_land_surface_energy::snow_accuracy_policy::Policy::current().identity() != Some(14) {
+        let source = option_env!("OPENWEPP_B01_SOURCE_SHA256").ok_or(Error::Identity(
+            "native restart executing source identity missing",
+        ))?;
+        let build = option_env!("OPENWEPP_B01_BUILD_INPUTS_SHA256").ok_or(Error::Identity(
+            "native restart executing build inputs identity missing",
+        ))?;
+        if openwepp_land_surface_energy::snow_accuracy_policy::Policy::current().identity()
+            != Some(14)
+        {
             return Err(Error::Identity("native restart executing policy"));
         }
         Ok(Self { source_sha256: Sha256Hex::try_new(source.to_owned()).map_err(nested)?,
@@ -66,68 +77,159 @@
     adoption_lse_sha256: Sha256Digest,
 }
 
-fn nested(error: impl std::fmt::Display) -> Error { Error::Nested(error.to_string()) }
+fn nested(error: impl std::fmt::Display) -> Error {
+    Error::Nested(error.to_string())
+}
 fn sha(bytes: &[u8]) -> Result<Sha256Hex, Error> {
     Sha256Hex::try_new(format!("{:x}", Sha256::digest(bytes))).map_err(nested)
 }
 
 impl NativeCustodyResidentV1 {
-    fn project(consumer: &DirectV10RealConsumerShadow, context: &ExpectedNativeCustodyContextV1<'_>) -> Result<Self, Error> {
-        consumer.restart_authority_validate_native_composite().map_err(nested)?;
-        let physical = consumer.frozen_litter_v3_resident().ok_or(Error::Identity("native physical resident"))?;
-        let exact = consumer.frozen_litter_v4_resident().ok_or(Error::Identity("native exact resident"))?;
+    fn project(
+        consumer: &DirectV10RealConsumerShadow,
+        context: &ExpectedNativeCustodyContextV1<'_>,
+    ) -> Result<Self, Error> {
+        consumer
+            .restart_authority_validate_native_composite()
+            .map_err(nested)?;
+        let physical = consumer
+            .frozen_litter_v3_resident()
+            .ok_or(Error::Identity("native physical resident"))?;
+        let exact = consumer
+            .frozen_litter_v4_resident()
+            .ok_or(Error::Identity("native exact resident"))?;
         if physical.lse_configuration() != context.lse_configuration
             || physical.surface_configuration() != context.surface_configuration
-            || exact.restart_authority_native_adoption_lse_sha256() != context.adoption_lse_sha256 {
+            || exact.restart_authority_native_adoption_lse_sha256() != context.adoption_lse_sha256
+        {
             return Err(Error::Identity("native external configuration/adoption"));
         }
-        physical.restart_authority_validate_native_adoption(context.adoption_lse_sha256.as_str(), context.adoption_surface_sha256).map_err(nested)?;
-        exact.restart_authority_validate_native_exact_adoption(physical, context.adoption_exact).map_err(nested)?;
+        physical
+            .restart_authority_validate_native_adoption(
+                context.adoption_lse_sha256.as_str(),
+                context.adoption_surface_sha256,
+            )
+            .map_err(nested)?;
+        exact
+            .restart_authority_validate_native_exact_adoption(physical, context.adoption_exact)
+            .map_err(nested)?;
         if &consumer.hydrology_frame().laned_active != context.expected_laned_active {
-            return Err(Error::Identity("native nested routing external configuration"));
+            return Err(Error::Identity(
+                "native nested routing external configuration",
+            ));
         }
         // Projection verifies the same independent expected prefix as restore.
-        consumer.restart_authority_validate_native_provider_receipt(
-            consumer.restart_authority_native_provider_receipt_sha256(), context.provider_expectations).map_err(nested)?;
+        consumer
+            .restart_authority_validate_native_provider_receipt(
+                consumer.restart_authority_native_provider_receipt_sha256(),
+                context.provider_expectations,
+            )
+            .map_err(nested)?;
         Ok(Self {
-            provider_gsi_receipt_sha256: Sha256Hex::try_new(consumer.restart_authority_native_provider_receipt_sha256().to_owned()).map_err(nested)?,
+            provider_gsi_receipt_sha256: Sha256Hex::try_new(
+                consumer
+                    .restart_authority_native_provider_receipt_sha256()
+                    .to_owned(),
+            )
+            .map_err(nested)?,
             laned_active: consumer.hydrology_frame().laned_active.clone(),
             laned_active_summary: consumer.hydrology_frame().laned_active_summary.clone(),
             active_covered_lse: consumer.restart_authority_active_covered_lse().clone(),
             native_lse: physical.lse_state().clone(),
-            native_surface_bytes: physical.surface_owner().canonical_bytes(context.surface_configuration.parent(), Some(context.surface_configuration)).map_err(nested)?,
+            native_surface_bytes: physical
+                .surface_owner()
+                .canonical_bytes(
+                    context.surface_configuration.parent(),
+                    Some(context.surface_configuration),
+                )
+                .map_err(nested)?,
             native_exact: exact.exact_surface_owner().clone(),
-            physical_history: physical.accepted_publication_supports_canonical_bytes().map_err(nested)?,
-            exact_history: exact.accepted_publication_supports_canonical_bytes().to_vec(),
-            wb14_parent_bytes: physical.restart_wb14_parent_working_state_bytes().map_err(nested)?,
+            physical_history: physical
+                .accepted_publication_supports_canonical_bytes()
+                .map_err(nested)?,
+            exact_history: exact
+                .accepted_publication_supports_canonical_bytes()
+                .to_vec(),
+            wb14_parent_bytes: physical
+                .restart_wb14_parent_working_state_bytes()
+                .map_err(nested)?,
             physical_receipt_chain_sha256: physical.restart_authority_native_receipt_chain().into(),
             adoption_lse_sha256: context.adoption_lse_sha256.clone(),
         })
     }
 
-    fn install(&self, consumer: &mut DirectV10RealConsumerShadow, context: &ExpectedNativeCustodyContextV1<'_>) -> Result<(), Error> {
+    fn install(
+        &self,
+        consumer: &mut DirectV10RealConsumerShadow,
+        context: &ExpectedNativeCustodyContextV1<'_>,
+    ) -> Result<(), Error> {
         if self.adoption_lse_sha256 != *context.adoption_lse_sha256
-            || consumer.restart_authority_active_covered_lse() != &self.active_covered_lse {
+            || consumer.restart_authority_active_covered_lse() != &self.active_covered_lse
+        {
             return Err(Error::Identity("native adoption/active base owner"));
         }
-        let owner = SurfaceLiquidOwnerEnvelopeV2::from_canonical_bytes(context.surface_configuration.parent(),
-            Some(context.surface_configuration), &self.native_surface_bytes).map_err(nested)?;
-        if owner.v2_state().is_none() { return Err(Error::Identity("native restart surface downgrade")); }
-        let mut physical = FrozenLitterV3Resident::try_new(context.lse_configuration.clone(), self.native_lse.clone(),
-            context.surface_configuration.clone(), owner).map_err(nested)?;
-        physical.restore_restart_authority(&self.physical_history, self.wb14_parent_bytes.as_deref(),
-            &self.physical_receipt_chain_sha256).map_err(nested)?;
-        physical.restart_authority_validate_native_adoption(context.adoption_lse_sha256.as_str(), context.adoption_surface_sha256).map_err(nested)?;
-        let exact = FrozenLitterV4Resident::try_restore(&physical, self.native_exact.clone(), &self.exact_history,
-            context.adoption_lse_sha256).map_err(nested)?;
-        exact.restart_authority_validate_native_exact_adoption(&physical, context.adoption_exact).map_err(nested)?;
+        let owner = SurfaceLiquidOwnerEnvelopeV2::from_canonical_bytes(
+            context.surface_configuration.parent(),
+            Some(context.surface_configuration),
+            &self.native_surface_bytes,
+        )
+        .map_err(nested)?;
+        if owner.v2_state().is_none() {
+            return Err(Error::Identity("native restart surface downgrade"));
+        }
+        let mut physical = FrozenLitterV3Resident::try_new(
+            context.lse_configuration.clone(),
+            self.native_lse.clone(),
+            context.surface_configuration.clone(),
+            owner,
+        )
+        .map_err(nested)?;
+        physical
+            .restore_restart_authority(
+                &self.physical_history,
+                self.wb14_parent_bytes.as_deref(),
+                &self.physical_receipt_chain_sha256,
+            )
+            .map_err(nested)?;
+        physical
+            .restart_authority_validate_native_adoption(
+                context.adoption_lse_sha256.as_str(),
+                context.adoption_surface_sha256,
+            )
+            .map_err(nested)?;
+        let exact = FrozenLitterV4Resident::try_restore(
+            &physical,
+            self.native_exact.clone(),
+            &self.exact_history,
+            context.adoption_lse_sha256,
+        )
+        .map_err(nested)?;
+        exact
+            .restart_authority_validate_native_exact_adoption(&physical, context.adoption_exact)
+            .map_err(nested)?;
         let mut candidate = consumer.clone();
-        candidate.restart_authority_install_native_provider_receipt(
-            self.provider_gsi_receipt_sha256.as_str(), context.provider_expectations).map_err(nested)?;
-        candidate.install_restored_frozen_litter_v4_residents(physical, exact).map_err(nested)?;
-        candidate.restart_authority_install_native_routing(self.laned_active.clone(), self.laned_active_summary.clone(), context.expected_laned_active).map_err(nested)?;
-        candidate.restart_authority_validate_native_composite().map_err(nested)?;
-        if Self::project(&candidate, context)? != *self { return Err(Error::Identity("native resident reprojection")); }
+        candidate
+            .restart_authority_install_native_provider_receipt(
+                self.provider_gsi_receipt_sha256.as_str(),
+                context.provider_expectations,
+            )
+            .map_err(nested)?;
+        candidate
+            .install_restored_frozen_litter_v4_residents(physical, exact)
+            .map_err(nested)?;
+        candidate
+            .restart_authority_install_native_routing(
+                self.laned_active.clone(),
+                self.laned_active_summary.clone(),
+                context.expected_laned_active,
+            )
+            .map_err(nested)?;
+        candidate
+            .restart_authority_validate_native_composite()
+            .map_err(nested)?;
+        if Self::project(&candidate, context)? != *self {
+            return Err(Error::Identity("native resident reprojection"));
+        }
         *consumer = candidate;
         Ok(())
     }
@@ -142,14 +244,32 @@
     in_progress_support_current: Option<NativeCustodyResidentV1>,
 }
 impl NativeCustodyResidentSetV1 {
-    fn project(value: &DirectSnowStage3V11ShadowAttachment, context: &ExpectedNativeCustodyContextV1<'_>) -> Result<Self, Error> {
+    fn project(
+        value: &DirectSnowStage3V11ShadowAttachment,
+        context: &ExpectedNativeCustodyContextV1<'_>,
+    ) -> Result<Self, Error> {
         let progress = value.restart_authority_in_progress_execution_v2();
         Ok(Self {
             committed: NativeCustodyResidentV1::project(&value.committed.real_consumer, context)?,
-            pending_candidate: value.restart_authority_pending_candidate().map(|v| NativeCustodyResidentV1::project(&v.ending_state.real_consumer, context)).transpose()?,
-            in_progress_day_candidate: progress.map(|v| NativeCustodyResidentV1::project(&v.day_candidate().real_consumer, context)).transpose()?,
-            in_progress_support_current: progress.map(|v| NativeCustodyResidentV1::project(
-                &v.support_current().ok_or(Error::Identity("native in-progress current missing"))?.real_consumer, context)).transpose()?,
+            pending_candidate: value
+                .restart_authority_pending_candidate()
+                .map(|v| NativeCustodyResidentV1::project(&v.ending_state.real_consumer, context))
+                .transpose()?,
+            in_progress_day_candidate: progress
+                .map(|v| {
+                    NativeCustodyResidentV1::project(&v.day_candidate().real_consumer, context)
+                })
+                .transpose()?,
+            in_progress_support_current: progress
+                .map(|v| {
+                    NativeCustodyResidentV1::project(
+                        &v.support_current()
+                            .ok_or(Error::Identity("native in-progress current missing"))?
+                            .real_consumer,
+                        context,
+                    )
+                })
+                .transpose()?,
         })
     }
     fn select(&self, position: Position) -> Result<&NativeCustodyResidentV1, Error> {
@@ -158,7 +278,8 @@
             Position::PendingCandidate => self.pending_candidate.as_ref(),
             Position::InProgressDayCandidate => self.in_progress_day_candidate.as_ref(),
             Position::InProgressSupportCurrent => self.in_progress_support_current.as_ref(),
-        }.ok_or(Error::Identity("native resident checkpoint posture"))
+        }
+        .ok_or(Error::Identity("native resident checkpoint posture"))
     }
 }
 fn validate_native_snow_chronology(
@@ -167,14 +288,19 @@
     for value in std::iter::once(&values.committed)
         .chain(values.pending_candidate.iter())
         .chain(values.in_progress_day_candidate.iter())
-        .chain(values.in_progress_support_current.iter()) {
+        .chain(values.in_progress_support_current.iter())
+    {
         if value.current_owner.is_none() != value.accepted_owner_chronology.is_empty() {
-            return Err(Error::Identity("native snow compound owner/chronology posture"));
+            return Err(Error::Identity(
+                "native snow compound owner/chronology posture",
+            ));
         }
         if let Some(first) = value.accepted_owner_chronology.first() {
             first.validate().map_err(nested)?;
             if first.receipt().predecessor_transaction_id().is_some()
-                || first.receipt().predecessor_receipt_chain_sha256() != openwepp_coupled_time::Digest32::zero() {
+                || first.receipt().predecessor_receipt_chain_sha256()
+                    != openwepp_coupled_time::Digest32::zero()
+            {
                 return Err(Error::Identity("native snow truncated initial chronology"));
             }
         }
@@ -182,27 +308,56 @@
     Ok(())
 }
 
-struct Installer<'a, 'b> { values: &'a NativeCustodyResidentSetV1, context: &'a ExpectedNativeCustodyContextV1<'b> }
+struct Installer<'a, 'b> {
+    values: &'a NativeCustodyResidentSetV1,
+    context: &'a ExpectedNativeCustodyContextV1<'b>,
+}
 impl SnowStage3V11ExactResidentRestorerV4 for Installer<'_, '_> {
-    fn native_bootstrap_soil(&self) -> Option<&openwepp_hillslope_orchestrator::v9_real_consumer_shadow::DirectV10SoilThermalResidentV2> {
+    fn native_bootstrap_soil(
+        &self,
+    ) -> Option<
+        &openwepp_hillslope_orchestrator::v9_real_consumer_shadow::DirectV10SoilThermalResidentV2,
+    > {
         Some(self.context.bootstrap_soil)
     }
 
-    fn native_surface_configuration(&self) -> Option<&SurfaceLiquidConfigurationV2> { Some(self.context.surface_configuration) }
-    fn native_wb14_parent_bytes(&self, position: Position) -> Result<Option<Vec<u8>>, Error> { Ok(self.values.select(position)?.wb14_parent_bytes.clone()) }
-    fn scientific_admission(&self) -> crate::checkpoint::ScientificAdmission { crate::checkpoint::ScientificAdmission::NativeB01 }
+    fn native_surface_configuration(&self) -> Option<&SurfaceLiquidConfigurationV2> {
+        Some(self.context.surface_configuration)
+    }
+    fn native_wb14_parent_bytes(&self, position: Position) -> Result<Option<Vec<u8>>, Error> {
+        Ok(self.values.select(position)?.wb14_parent_bytes.clone())
+    }
+    fn scientific_admission(&self) -> crate::checkpoint::ScientificAdmission {
+        crate::checkpoint::ScientificAdmission::NativeB01
+    }
     fn validated_native_surface_bytes(&self, position: Position) -> Result<Option<Vec<u8>>, Error> {
         let bytes = &self.values.select(position)?.native_surface_bytes;
         let configuration = self.context.surface_configuration;
-        let owner = SurfaceLiquidOwnerEnvelopeV2::from_canonical_bytes(configuration.parent(),
-            Some(configuration), bytes).map_err(nested)?;
-        if owner.v2_state().is_none() { return Err(Error::Identity("native clock surface downgrade")); }
-        let canonical = owner.canonical_bytes(configuration.parent(), Some(configuration)).map_err(nested)?;
-        if canonical != *bytes { return Err(Error::Identity("native clock surface noncanonical")); }
+        let owner = SurfaceLiquidOwnerEnvelopeV2::from_canonical_bytes(
+            configuration.parent(),
+            Some(configuration),
+            bytes,
+        )
+        .map_err(nested)?;
+        if owner.v2_state().is_none() {
+            return Err(Error::Identity("native clock surface downgrade"));
+        }
+        let canonical = owner
+            .canonical_bytes(configuration.parent(), Some(configuration))
+            .map_err(nested)?;
+        if canonical != *bytes {
+            return Err(Error::Identity("native clock surface noncanonical"));
+        }
         Ok(Some(canonical))
     }
-    fn restore_exact_resident(&self, position: Position, consumer: &mut DirectV10RealConsumerShadow) -> Result<(), Error> {
-        self.values.select(position)?.install(consumer, self.context)
+    fn restore_exact_resident(
+        &self,
+        position: Position,
+        consumer: &mut DirectV10RealConsumerShadow,
+    ) -> Result<(), Error> {
+        self.values
+            .select(position)?
+            .install(consumer, self.context)
     }
 }
 
@@ -219,54 +374,123 @@
     payload_sha256: Sha256Hex,
 }
 impl DirectNativeCustodyStage3RestartV1 {
-    pub fn project(value: &DirectSnowStage3V11ShadowAttachment, phase_plan_sha256: &Sha256Hex,
-        day_input_digests: &[Sha256Hex], archive: &ExpectedStage3CommittedDayArchiveV3<'_>,
-        context: &ExpectedNativeCustodyContextV1<'_>) -> Result<Self, Error> {
+    pub fn project(
+        value: &DirectSnowStage3V11ShadowAttachment,
+        phase_plan_sha256: &Sha256Hex,
+        day_input_digests: &[Sha256Hex],
+        archive: &ExpectedStage3CommittedDayArchiveV3<'_>,
+        context: &ExpectedNativeCustodyContextV1<'_>,
+    ) -> Result<Self, Error> {
         let residents = NativeCustodyResidentSetV1::project(value, context)?;
-        let snow_material = value.restart_authority_snow_enthalpy_material_residents_v1().map_err(nested)?;
+        let snow_material = value
+            .restart_authority_snow_enthalpy_material_residents_v1()
+            .map_err(nested)?;
         validate_native_snow_chronology(&snow_material)?;
-        let base = value.restart_authority_native_without_routing().map_err(nested)?;
-        let bytes = DirectSnowStage3V11AttachmentRestartV3::project_native(&base, phase_plan_sha256, day_input_digests, archive)?.to_canonical_bytes()?;
-        let mut result = Self {schema: SCHEMA.into(), policy_id: 14, execution_identity: NativeExecutionIdentityV1::executing()?, nested_sha256: sha(&bytes)?,
-            nested_stage3_v3_bytes: bytes, residents, snow_material, payload_sha256: sha(&[])?};
+        let base = value
+            .restart_authority_native_without_routing()
+            .map_err(nested)?;
+        let bytes = DirectSnowStage3V11AttachmentRestartV3::project_native(
+            &base,
+            phase_plan_sha256,
+            day_input_digests,
+            archive,
+        )?
+        .to_canonical_bytes()?;
+        let mut result = Self {
+            schema: SCHEMA.into(),
+            policy_id: 14,
+            execution_identity: NativeExecutionIdentityV1::executing()?,
+            nested_sha256: sha(&bytes)?,
+            nested_stage3_v3_bytes: bytes,
+            residents,
+            snow_material,
+            payload_sha256: sha(&[])?,
+        };
         result.payload_sha256 = result.digest()?;
         Ok(result)
     }
     fn digest(&self) -> Result<Sha256Hex, Error> {
-        sha(&to_canonical_bytes(&(&self.schema, self.policy_id, &self.execution_identity, &self.nested_stage3_v3_bytes, &self.nested_sha256, &self.residents, &self.snow_material)).map_err(nested)?)
-    }
-    pub fn restore(&self, stage3: &ExpectedSnowStage3V11RestartContext<'_>, archive: &ExpectedStage3CommittedDayArchiveV3<'_>,
-        context: &ExpectedNativeCustodyContextV1<'_>) -> Result<DirectSnowStage3V11ShadowAttachment, Error> {
-        if self.schema != SCHEMA || self.policy_id != 14
+        sha(&to_canonical_bytes(&(
+            &self.schema,
+            self.policy_id,
+            &self.execution_identity,
+            &self.nested_stage3_v3_bytes,
+            &self.nested_sha256,
+            &self.residents,
+            &self.snow_material,
+        ))
+        .map_err(nested)?)
+    }
+    pub fn restore(
+        &self,
+        stage3: &ExpectedSnowStage3V11RestartContext<'_>,
+        archive: &ExpectedStage3CommittedDayArchiveV3<'_>,
+        context: &ExpectedNativeCustodyContextV1<'_>,
+    ) -> Result<DirectSnowStage3V11ShadowAttachment, Error> {
+        if self.schema != SCHEMA
+            || self.policy_id != 14
             || self.execution_identity != NativeExecutionIdentityV1::executing()?
-            || openwepp_land_surface_energy::snow_accuracy_policy::Policy::current().identity() != Some(14)
-            || self.nested_sha256 != sha(&self.nested_stage3_v3_bytes)? || self.payload_sha256 != self.digest()? {
+            || openwepp_land_surface_energy::snow_accuracy_policy::Policy::current().identity()
+                != Some(14)
+            || self.nested_sha256 != sha(&self.nested_stage3_v3_bytes)?
+            || self.payload_sha256 != self.digest()?
+        {
             return Err(Error::Identity("native restart schema/policy/payload"));
         }
         validate_native_snow_chronology(&self.snow_material)?;
-        let installer = Installer {values: &self.residents, context};
-        let mut restored = crate::snow_stage3_v11_v4_exact_enthalpy::restore_native_supplement_base(
-            &self.nested_stage3_v3_bytes, stage3, archive, &installer, true)?;
-        restored.restart_authority_install_snow_enthalpy_material_residents_v1(self.snow_material.clone()).map_err(nested)?;
-        if restored.restart_authority_snow_enthalpy_material_residents_v1().map_err(nested)? != self.snow_material
-            || NativeCustodyResidentSetV1::project(&restored, context)? != self.residents {
+        let installer = Installer {
+            values: &self.residents,
+            context,
+        };
+        let mut restored =
+            crate::snow_stage3_v11_v4_exact_enthalpy::restore_native_supplement_base(
+                &self.nested_stage3_v3_bytes,
+                stage3,
+                archive,
+                &installer,
+                true,
+            )?;
+        restored
+            .restart_authority_install_snow_enthalpy_material_residents_v1(
+                self.snow_material.clone(),
+            )
+            .map_err(nested)?;
+        if restored
+            .restart_authority_snow_enthalpy_material_residents_v1()
+            .map_err(nested)?
+            != self.snow_material
+            || NativeCustodyResidentSetV1::project(&restored, context)? != self.residents
+        {
             return Err(Error::Identity("native restored posture/owners"));
         }
         Ok(restored)
     }
     pub(crate) fn nested_active_v2_bytes(&self) -> Result<Vec<u8>, Error> {
-        if self.schema != SCHEMA || self.policy_id != 14
+        if self.schema != SCHEMA
+            || self.policy_id != 14
             || self.execution_identity != NativeExecutionIdentityV1::executing()?
-            || self.nested_sha256 != sha(&self.nested_stage3_v3_bytes)? || self.payload_sha256 != self.digest()? {
+            || self.nested_sha256 != sha(&self.nested_stage3_v3_bytes)?
+            || self.payload_sha256 != self.digest()?
+        {
             return Err(Error::Identity("native nested active envelope"));
         }
-        crate::snow_stage3_v11_v4_exact_enthalpy::native_supplement_active_v2_bytes(&self.nested_stage3_v3_bytes)
-    }
-    pub fn to_canonical_bytes(&self) -> Result<Vec<u8>, Error> { to_canonical_bytes(self).map_err(nested) }
-    pub fn from_canonical_bytes(bytes: &[u8], stage3: &ExpectedSnowStage3V11RestartContext<'_>,
-        archive: &ExpectedStage3CommittedDayArchiveV3<'_>, context: &ExpectedNativeCustodyContextV1<'_>) -> Result<Self, Error> {
+        crate::snow_stage3_v11_v4_exact_enthalpy::native_supplement_active_v2_bytes(
+            &self.nested_stage3_v3_bytes,
+        )
+    }
+    pub fn to_canonical_bytes(&self) -> Result<Vec<u8>, Error> {
+        to_canonical_bytes(self).map_err(nested)
+    }
+    pub fn from_canonical_bytes(
+        bytes: &[u8],
+        stage3: &ExpectedSnowStage3V11RestartContext<'_>,
+        archive: &ExpectedStage3CommittedDayArchiveV3<'_>,
+        context: &ExpectedNativeCustodyContextV1<'_>,
+    ) -> Result<Self, Error> {
         let value: Self = from_canonical_bytes(bytes).map_err(nested)?;
-        if value.to_canonical_bytes()? != bytes { return Err(Error::Identity("native canonical restart bytes")); }
+        if value.to_canonical_bytes()? != bytes {
+            return Err(Error::Identity("native canonical restart bytes"));
+        }
         value.restore(stage3, archive, context)?;
         Ok(value)
     }
@@ -275,14 +499,23 @@
 #[cfg(test)]
 mod native_snow_posture_tests {
     use super::*;
-    use openwepp_hillslope_orchestrator::snow_stage3_v11_attachment::{SnowStage3V11SnowEnthalpyMaterialResidentV1 as Resident, SnowStage3V11SnowEnthalpyMaterialResidentSetV1 as Set};
+    use openwepp_hillslope_orchestrator::snow_stage3_v11_attachment::{
+        SnowStage3V11SnowEnthalpyMaterialResidentSetV1 as Set,
+        SnowStage3V11SnowEnthalpyMaterialResidentV1 as Resident,
+    };
     #[test]
     fn native_snow_optional_transport_preserves_explicit_empty_positions() {
-        let empty = Resident { current_owner: None, accepted_owner_chronology: Vec::new() };
+        let empty = Resident {
+            current_owner: None,
+            accepted_owner_chronology: Vec::new(),
+        };
         for progress in [false, true] {
-            let value = Set { committed: empty.clone(), pending_candidate: Some(empty.clone()),
+            let value = Set {
+                committed: empty.clone(),
+                pending_candidate: Some(empty.clone()),
                 in_progress_day_candidate: progress.then(|| empty.clone()),
-                in_progress_support_current: progress.then(|| empty.clone()) };
+                in_progress_support_current: progress.then(|| empty.clone()),
+            };
             validate_native_snow_chronology(&value).unwrap();
             let bytes = to_canonical_bytes(&value).unwrap();
             let replay: Set = from_canonical_bytes(&bytes).unwrap();
--- a/crates/openwepp-persisted-restart-v1/src/snow_stage3_v11_v3.rs
+++ b/crates/openwepp-persisted-restart-v1/src/snow_stage3_v11_v3.rs
@@ -92,14 +92,19 @@
         Self::project_with_soil(value, phase_plan_sha256, day_input_digests, archive, false)
     }
     pub(crate) fn project_native(
-        value: &DirectSnowStage3V11ShadowAttachment, phase_plan_sha256: &Sha256Hex,
-        day_input_digests: &[Sha256Hex], archive: &ExpectedStage3CommittedDayArchiveV3<'_>,
+        value: &DirectSnowStage3V11ShadowAttachment,
+        phase_plan_sha256: &Sha256Hex,
+        day_input_digests: &[Sha256Hex],
+        archive: &ExpectedStage3CommittedDayArchiveV3<'_>,
     ) -> Result<Self, SnowStage3V11RestartError> {
         Self::project_with_soil(value, phase_plan_sha256, day_input_digests, archive, true)
     }
     fn project_with_soil(
-        value: &DirectSnowStage3V11ShadowAttachment, phase_plan_sha256: &Sha256Hex,
-        day_input_digests: &[Sha256Hex], archive: &ExpectedStage3CommittedDayArchiveV3<'_>, native: bool,
+        value: &DirectSnowStage3V11ShadowAttachment,
+        phase_plan_sha256: &Sha256Hex,
+        day_input_digests: &[Sha256Hex],
+        archive: &ExpectedStage3CommittedDayArchiveV3<'_>,
+        native: bool,
     ) -> Result<Self, SnowStage3V11RestartError> {
         let archived_receipt_prefix = value
             .restart_authority_archived_receipt_prefix_v3()
@@ -107,10 +112,19 @@
         validate_archive_evidence(&archived_receipt_prefix, archive)?;
         validate_resident_tail_bound(value, &archived_receipt_prefix)?;
         let active = if native {
-            DirectSnowStage3V11AttachmentRestartV2::project_active_base_native(value, phase_plan_sha256, day_input_digests)
+            DirectSnowStage3V11AttachmentRestartV2::project_active_base_native(
+                value,
+                phase_plan_sha256,
+                day_input_digests,
+            )
         } else {
-            DirectSnowStage3V11AttachmentRestartV2::project_active_base_v3(value, phase_plan_sha256, day_input_digests)
-        }?.to_canonical_bytes()?;
+            DirectSnowStage3V11AttachmentRestartV2::project_active_base_v3(
+                value,
+                phase_plan_sha256,
+                day_input_digests,
+            )
+        }?
+        .to_canonical_bytes()?;
         let support_liquid_custody =
             restart_authority_encode_support_liquid_custody_state_v3(value).map_err(nested)?;
         let publication_rotation =
@@ -509,33 +523,56 @@
     prefix: &Stage3ArchivedReceiptPrefixV1,
     archive: &ExpectedStage3CommittedDayArchiveV3<'_>,
     context: &ExpectedSnowStage3V11RestartContext<'_>,
-) -> Result<Option<openwepp_hillslope_orchestrator::snow_stage3_v11_attachment::DirectSnowStage3V11ParentReceipt>, SnowStage3V11RestartError> {
+) -> Result<Option<openwepp_hillslope_orchestrator::snow_stage3_v11_attachment::DirectSnowStage3V11ParentReceipt>, SnowStage3V11RestartError>{
     validate_archive_evidence(prefix, archive)?;
     if archive.manifest.run_identity != context.static_context.run_identity
-        || archive.manifest.topology_identity != context.static_context.topology_identity {
-        return Err(SnowStage3V11RestartError::Identity("archive expected static identity"));
-    }
-    let Some(entry) = archive.manifest.entries.last() else { return Ok(None); };
-    let bytes = archive.reader.read_canonical_uncompressed(entry.content_sha256)
-        .ok_or(SnowStage3V11RestartError::Identity("archive original content missing"))?;
+        || archive.manifest.topology_identity != context.static_context.topology_identity
+    {
+        return Err(SnowStage3V11RestartError::Identity(
+            "archive expected static identity",
+        ));
+    }
+    let Some(entry) = archive.manifest.entries.last() else {
+        return Ok(None);
+    };
+    let bytes = archive
+        .reader
+        .read_canonical_uncompressed(entry.content_sha256)
+        .ok_or(SnowStage3V11RestartError::Identity(
+            "archive original content missing",
+        ))?;
     if digest_bytes(&bytes) != entry.content_sha256
-        || u64::try_from(bytes.len()).ok() != Some(entry.canonical_uncompressed_len) {
-        return Err(SnowStage3V11RestartError::Identity("archive original content changed"));
-    }
-    let mut reader = ArchivedParentFrameReaderV1 { bytes: &bytes, cursor: 0 };
+        || u64::try_from(bytes.len()).ok() != Some(entry.canonical_uncompressed_len)
+    {
+        return Err(SnowStage3V11RestartError::Identity(
+            "archive original content changed",
+        ));
+    }
+    let mut reader = ArchivedParentFrameReaderV1 {
+        bytes: &bytes,
+        cursor: 0,
+    };
     reader.exact(b"OPENWEPP_STAGE3_COMMITTED_DAY_EVIDENCE_V1\0")?;
     reader.exact(&entry.schema_version.to_be_bytes())?;
     reader.exact(archive.manifest.run_identity.as_bytes())?;
     reader.exact(archive.manifest.topology_identity.as_bytes())?;
-    reader.exact(&u64::try_from(entry.day_index).map_err(|_| SnowStage3V11RestartError::Identity("archive day width"))?.to_be_bytes())?;
+    reader.exact(
+        &u64::try_from(entry.day_index)
+            .map_err(|_| SnowStage3V11RestartError::Identity("archive day width"))?
+            .to_be_bytes(),
+    )?;
     reader.exact(entry.previous_archive_content_root_sha256.as_bytes())?;
     let parent = reader.blob()?;
     if digest_bytes(parent) != entry.parent_receipt_sha256 {
-        return Err(SnowStage3V11RestartError::Identity("archive parent wire digest"));
+        return Err(SnowStage3V11RestartError::Identity(
+            "archive parent wire digest",
+        ));
     }
     let publication = reader.blob()?;
     if digest_bytes(publication) != entry.publication_evidence_sha256 {
-        return Err(SnowStage3V11RestartError::Identity("archive publication wire digest"));
+        return Err(SnowStage3V11RestartError::Identity(
+            "archive publication wire digest",
+        ));
     }
     reader.exact(entry.committed_publication_receipt_sha256.as_bytes())?;
     reader.take(32)?; // Ordered support set is already bound by the full content digest.
@@ -545,7 +582,9 @@
     reader.exact(&entry.ending_next_parent_sequence.to_be_bytes())?;
     reader.blob()?; // Qualification delta remains bound by the original frame digest.
     if reader.cursor != bytes.len() {
-        return Err(SnowStage3V11RestartError::Identity("archive trailing frame bytes"));
+        return Err(SnowStage3V11RestartError::Identity(
+            "archive trailing frame bytes",
+        ));
     }
     let receipt = openwepp_hillslope_orchestrator::snow_stage3_v11_attachment::restart_authority_decode_parent_receipt_archive_with_configured_scope_v3(
         parent, &context.static_context.vegetation_configuration,
@@ -553,27 +592,49 @@
     if receipt.day_index != entry.day_index
         || receipt.ending_coupled_owner_set_sha256 != entry.ending_owner_set_sha256
         || receipt.ending_coupled_accepted_until_ns.get() != entry.ending_accepted_until_ns
-        || receipt.ending_next_parent_sequence != entry.ending_next_parent_sequence {
-        return Err(SnowStage3V11RestartError::Identity("archive typed parent endpoint"));
+        || receipt.ending_next_parent_sequence != entry.ending_next_parent_sequence
+    {
+        return Err(SnowStage3V11RestartError::Identity(
+            "archive typed parent endpoint",
+        ));
     }
     Ok(Some(receipt))
 }
 
-struct ArchivedParentFrameReaderV1<'a> { bytes: &'a [u8], cursor: usize }
+struct ArchivedParentFrameReaderV1<'a> {
+    bytes: &'a [u8],
+    cursor: usize,
+}
 impl<'a> ArchivedParentFrameReaderV1<'a> {
     fn take(&mut self, length: usize) -> Result<&'a [u8], SnowStage3V11RestartError> {
-        let end = self.cursor.checked_add(length).ok_or(SnowStage3V11RestartError::Identity("archive framing width"))?;
-        let value = self.bytes.get(self.cursor..end).ok_or(SnowStage3V11RestartError::Identity("archive truncated framing"))?;
+        let end = self
+            .cursor
+            .checked_add(length)
+            .ok_or(SnowStage3V11RestartError::Identity("archive framing width"))?;
+        let value = self
+            .bytes
+            .get(self.cursor..end)
+            .ok_or(SnowStage3V11RestartError::Identity(
+                "archive truncated framing",
+            ))?;
         self.cursor = end;
         Ok(value)
     }
     fn exact(&mut self, expected: &[u8]) -> Result<(), SnowStage3V11RestartError> {
-        if self.take(expected.len())? != expected { return Err(SnowStage3V11RestartError::Identity("archive frame identity")); }
+        if self.take(expected.len())? != expected {
+            return Err(SnowStage3V11RestartError::Identity(
+                "archive frame identity",
+            ));
+        }
         Ok(())
     }
     fn blob(&mut self) -> Result<&'a [u8], SnowStage3V11RestartError> {
-        let raw: [u8; 8] = self.take(8)?.try_into().map_err(|_| SnowStage3V11RestartError::Identity("archive length width"))?;
-        let length = usize::try_from(u64::from_be_bytes(raw)).map_err(|_| SnowStage3V11RestartError::Identity("archive host length width"))?;
+        let raw: [u8; 8] = self
+            .take(8)?
+            .try_into()
+            .map_err(|_| SnowStage3V11RestartError::Identity("archive length width"))?;
+        let length = usize::try_from(u64::from_be_bytes(raw))
+            .map_err(|_| SnowStage3V11RestartError::Identity("archive host length width"))?;
         self.take(length)
     }
 }
--- a/crates/openwepp-persisted-restart-v1/src/snow_stage3_v11_v4_exact_enthalpy.rs
+++ b/crates/openwepp-persisted-restart-v1/src/snow_stage3_v11_v4_exact_enthalpy.rs
@@ -142,7 +142,11 @@
             contexts: &context.exact,
         };
         let restored = restore_native_supplement_base(
-            &self.nested_stage3_v3_bytes, context.stage3, context.archive, &exact_installer, false,
+            &self.nested_stage3_v3_bytes,
+            context.stage3,
+            context.archive,
+            &exact_installer,
+            false,
         )?;
         validate_live_residents(&restored, &self.exact_residents, &context.exact)?;
         Ok(restored)
@@ -539,84 +543,94 @@
     installer: &dyn SnowStage3V11ExactResidentRestorerV4,
     native_routing: bool,
 ) -> Result<DirectSnowStage3V11ShadowAttachment, SnowStage3V11RestartError> {
-        let probe: Stage3V3WireProbe =
-            from_canonical_bytes(nested_bytes).map_err(nested)?;
-        validate_probe_shape(&probe)?;
-        let active = decode_blob(
-            &probe.active_attachment_v2_canonical_base64,
-            &probe.active_attachment_v2_sha256,
-        )?;
-        let active_v2: DirectSnowStage3V11AttachmentRestartV2 =
-            from_canonical_bytes(&active).map_err(nested)?;
-        if active_v2.to_canonical_bytes()? != active
-            || active_v2.static_context_sha256 != probe.static_context_sha256
-        {
-            return Err(SnowStage3V11RestartError::Identity(
-                "V4 nested V3/active-V2 canonical join",
-            ));
-        }
-        let archived = if native_routing {
-            crate::snow_stage3_v11_v3::authenticated_last_parent_receipt_v3(&probe.archived_receipt_prefix, archive, stage3)?
-        } else { None };
-        let mut restored = active_v2.restore_active_base_v4(stage3, installer, archived.as_ref())?;
-        restored
-            .restart_authority_install_archived_receipt_prefix_v3(
-                probe.archived_receipt_prefix.clone(),
-            )
-            .map_err(nested)?;
-        let custody = decode_blob(
-            &probe.support_liquid_custody_canonical_base64,
-            &probe.support_liquid_custody_sha256,
-        )?;
-        restart_authority_restore_support_liquid_custody_state_v3(&mut restored, &custody)
-            .map_err(nested)?;
-        let rotation = decode_blob(
-            &probe.publication_rotation_canonical_base64,
-            &probe.publication_rotation_sha256,
-        )?;
-        restart_authority_restore_publication_rotation_state_v3(&mut restored, &rotation)
-            .map_err(nested)?;
-
-        // Reprojection proves the complete V3 archive/custody/rotation state
-        // and every unchanged nested byte after the exact residents exist.
-        let native_base = if native_routing { Some(restored.restart_authority_native_without_routing().map_err(nested)?) } else { None };
-        let replay = if native_routing {
-            DirectSnowStage3V11AttachmentRestartV3::project_native(
+    let probe: Stage3V3WireProbe = from_canonical_bytes(nested_bytes).map_err(nested)?;
+    validate_probe_shape(&probe)?;
+    let active = decode_blob(
+        &probe.active_attachment_v2_canonical_base64,
+        &probe.active_attachment_v2_sha256,
+    )?;
+    let active_v2: DirectSnowStage3V11AttachmentRestartV2 =
+        from_canonical_bytes(&active).map_err(nested)?;
+    if active_v2.to_canonical_bytes()? != active
+        || active_v2.static_context_sha256 != probe.static_context_sha256
+    {
+        return Err(SnowStage3V11RestartError::Identity(
+            "V4 nested V3/active-V2 canonical join",
+        ));
+    }
+    let archived = if native_routing {
+        crate::snow_stage3_v11_v3::authenticated_last_parent_receipt_v3(
+            &probe.archived_receipt_prefix,
+            archive,
+            stage3,
+        )?
+    } else {
+        None
+    };
+    let mut restored = active_v2.restore_active_base_v4(stage3, installer, archived.as_ref())?;
+    restored
+        .restart_authority_install_archived_receipt_prefix_v3(probe.archived_receipt_prefix.clone())
+        .map_err(nested)?;
+    let custody = decode_blob(
+        &probe.support_liquid_custody_canonical_base64,
+        &probe.support_liquid_custody_sha256,
+    )?;
+    restart_authority_restore_support_liquid_custody_state_v3(&mut restored, &custody)
+        .map_err(nested)?;
+    let rotation = decode_blob(
+        &probe.publication_rotation_canonical_base64,
+        &probe.publication_rotation_sha256,
+    )?;
+    restart_authority_restore_publication_rotation_state_v3(&mut restored, &rotation)
+        .map_err(nested)?;
+
+    // Reprojection proves the complete V3 archive/custody/rotation state
+    // and every unchanged nested byte after the exact residents exist.
+    let native_base = if native_routing {
+        Some(
+            restored
+                .restart_authority_native_without_routing()
+                .map_err(nested)?,
+        )
+    } else {
+        None
+    };
+    let replay = if native_routing {
+        DirectSnowStage3V11AttachmentRestartV3::project_native(
             native_base.as_ref().unwrap_or(&restored),
             stage3.real_consumer_context.phase_plan_sha256,
             stage3.real_consumer_context.day_input_digests,
             archive,
         )
-        } else {
-            DirectSnowStage3V11AttachmentRestartV3::project(
+    } else {
+        DirectSnowStage3V11AttachmentRestartV3::project(
             native_base.as_ref().unwrap_or(&restored),
             stage3.real_consumer_context.phase_plan_sha256,
             stage3.real_consumer_context.day_input_digests,
             archive,
         )
-        }?
-        .to_canonical_bytes()?;
-        if replay != nested_bytes
-            || restart_authority_encode_support_liquid_custody_state_v3(&restored)
-                .map_err(nested)?
-                != custody
-            || restart_authority_encode_publication_rotation_state_v3(&restored).map_err(nested)?
-                != rotation
-        {
-            return Err(SnowStage3V11RestartError::Identity(
-                "V4 exact resident changed nested V3 bytes",
-            ));
-        }
-        Ok(restored)
-}
-
-
-pub(crate) fn native_supplement_active_v2_bytes(nested_bytes: &[u8]) -> Result<Vec<u8>, SnowStage3V11RestartError> {
-        let probe: Stage3V3WireProbe =
-            from_canonical_bytes(nested_bytes).map_err(nested)?;
-        validate_probe_shape(&probe)?;
-        decode_blob(
-            &probe.active_attachment_v2_canonical_base64,
-            &probe.active_attachment_v2_sha256,
-        )
-}
+    }?
+    .to_canonical_bytes()?;
+    if replay != nested_bytes
+        || restart_authority_encode_support_liquid_custody_state_v3(&restored).map_err(nested)?
+            != custody
+        || restart_authority_encode_publication_rotation_state_v3(&restored).map_err(nested)?
+            != rotation
+    {
+        return Err(SnowStage3V11RestartError::Identity(
+            "V4 exact resident changed nested V3 bytes",
+        ));
+    }
+    Ok(restored)
+}
+
+pub(crate) fn native_supplement_active_v2_bytes(
+    nested_bytes: &[u8],
+) -> Result<Vec<u8>, SnowStage3V11RestartError> {
+    let probe: Stage3V3WireProbe = from_canonical_bytes(nested_bytes).map_err(nested)?;
+    validate_probe_shape(&probe)?;
+    decode_blob(
+        &probe.active_attachment_v2_canonical_base64,
+        &probe.active_attachment_v2_sha256,
+    )
+}
--- a/crates/openwepp-persisted-restart-v1/src/snow_stage3_v11_v5_snow_enthalpy.rs
+++ b/crates/openwepp-persisted-restart-v1/src/snow_stage3_v11_v5_snow_enthalpy.rs
@@ -899,5 +899,3 @@
         }
     }
 }
-
-
--- a/crates/openwepp-runner/src/hillslope/native_custody_restart_experiment.rs
+++ b/crates/openwepp-runner/src/hillslope/native_custody_restart_experiment.rs
@@ -1,13 +1,20 @@
 //! Actual-process experimental checkpoint custody; no physical prefix replay.
 use super::*;
 use openwepp_coupled_time::{Digest32, digest_bytes};
-use openwepp_hillslope_orchestrator::{DirectPublicationFrame, Stage3CommittedDayArchiveManifestV1};
-use openwepp_persisted_restart_v1::{DirectHydrologyNativeCustodyRestartV1,
-    ExpectedStage3CommittedDayArchiveV3, Stage3CommittedDayArchiveReaderV3};
+use openwepp_hillslope_orchestrator::{
+    DirectPublicationFrame, Stage3CommittedDayArchiveManifestV1,
+};
+use openwepp_persisted_restart_v1::{
+    DirectHydrologyNativeCustodyRestartV1, ExpectedStage3CommittedDayArchiveV3,
+    Stage3CommittedDayArchiveReaderV3,
+};
 use serde::{Deserialize, Serialize};
 
 fn fail(error: impl std::fmt::Display) -> HillslopeCliError {
-    HillslopeCliError::RuntimeSurfaceFailure {surface: "native_custody_restart_experiment", detail: error.to_string()}
+    HillslopeCliError::RuntimeSurfaceFailure {
+        surface: "native_custody_restart_experiment",
+        detail: error.to_string(),
+    }
 }
 #[derive(Clone, Debug, PartialEq, Serialize, Deserialize)]
 #[serde(deny_unknown_fields)]
@@ -16,7 +23,9 @@
     fn read_canonical_uncompressed(&self, sha: Digest32) -> Option<Vec<u8>> {
         let mut found = self.0.iter().filter(|bytes| digest_bytes(bytes) == sha);
         let value = found.next()?.clone();
-        if found.next().is_some() { return None; }
+        if found.next().is_some() {
+            return None;
+        }
         Some(value)
     }
 }
@@ -36,75 +45,134 @@
 }
 impl Workflow {
     fn digest(&self) -> Result<Digest32, HillslopeCliError> {
-        let mut value = self.clone(); value.payload = Digest32::zero();
+        let mut value = self.clone();
+        value.payload = Digest32::zero();
         Ok(digest_bytes(&serde_json::to_vec(&value).map_err(fail)?))
     }
-    fn validate_prefix(&self, frame: &DirectRunFrame, metadata: &DirectPublicationRunMetadata) -> Result<(), HillslopeCliError> {
-        if self.schema != "OPENWEPP_B01_NATIVE_WORKFLOW_V1_POLICY14" || self.payload != self.digest()?
-            || &self.metadata != metadata || self.next_day > frame.identity.day_count
+    fn validate_prefix(
+        &self,
+        frame: &DirectRunFrame,
+        metadata: &DirectPublicationRunMetadata,
+    ) -> Result<(), HillslopeCliError> {
+        if self.schema != "OPENWEPP_B01_NATIVE_WORKFLOW_V1_POLICY14"
+            || self.payload != self.digest()?
+            || &self.metadata != metadata
+            || self.next_day > frame.identity.day_count
             || self.manifest.committed_day_count != self.next_day
             || self.records.0.len() != self.next_day
-            || self.next_day.checked_mul(frame.identity.lane_count) != Some(self.rows.len()) {
+            || self.next_day.checked_mul(frame.identity.lane_count) != Some(self.rows.len())
+        {
             return Err(fail("workflow schema/seal/metadata/prefix cardinality"));
         }
         self.manifest.validate().map_err(fail)?;
-        for (day, (entry, bytes)) in self.manifest.entries.iter().zip(&self.records.0).enumerate() {
-            if entry.day_index != day || entry.content_sha256 != digest_bytes(bytes)
-                || entry.canonical_uncompressed_len != bytes.len() as u64 {
+        for (day, (entry, bytes)) in self
+            .manifest
+            .entries
+            .iter()
+            .zip(&self.records.0)
+            .enumerate()
+        {
+            if entry.day_index != day
+                || entry.content_sha256 != digest_bytes(bytes)
+                || entry.canonical_uncompressed_len != bytes.len() as u64
+            {
                 return Err(fail("workflow archive chronology/content"));
             }
         }
         for (index, row) in self.rows.iter().enumerate() {
             let lane = &frame.lanes[index % frame.identity.lane_count];
-            if row.day_index != index / frame.identity.lane_count || row.lane_index != index % frame.identity.lane_count
-                || row.lane_id != lane.lane_id || row.run_id != frame.identity.run_id
-                || row.hillslope_id != frame.identity.hillslope_id {
+            if row.day_index != index / frame.identity.lane_count
+                || row.lane_index != index % frame.identity.lane_count
+                || row.lane_id != lane.lane_id
+                || row.run_id != frame.identity.run_id
+                || row.hillslope_id != frame.identity.hillslope_id
+            {
                 return Err(fail("workflow row partition/identity"));
             }
         }
         let mut previous = None;
         for row in &self.subhourly_rows {
-            let index = self.rows.iter().position(|daily| daily.ofe_id == row.ofe_id as u32
-                && daily.calendar.year == row.year && daily.sim_day_index == row.sim_day_index
-                && i32::from(daily.calendar.julian_day) == row.julian)
+            let index = self
+                .rows
+                .iter()
+                .position(|daily| {
+                    daily.ofe_id == row.ofe_id as u32
+                        && daily.calendar.year == row.year
+                        && daily.sim_day_index == row.sim_day_index
+                        && i32::from(daily.calendar.julian_day) == row.julian
+                })
                 .ok_or_else(|| fail("workflow subhourly parent row"))?;
             let key = (index, row.hour_index, row.subinterval_index);
-            if previous.is_some_and(|old| old >= key) || row.wepp_id != DIRECT_WAT_WEPP_ID
-                || !valid_subhourly_clock(row.hour_index, row.subinterval_index) {
+            if previous.is_some_and(|old| old >= key)
+                || row.wepp_id != DIRECT_WAT_WEPP_ID
+                || !valid_subhourly_clock(row.hour_index, row.subinterval_index)
+            {
                 return Err(fail("workflow subhourly chronology/duplicate"));
             }
             previous = Some(key);
         }
         Ok(())
     }
-    pub(super) fn project(frame: &DirectRunFrame, fresh: &DirectRunFrame,
+    pub(super) fn project(
+        frame: &DirectRunFrame,
+        fresh: &DirectRunFrame,
         seed: &snow_stage3_v11_production_seed::DirectSnowStage3V11ProductionSeedV1,
         climate: &HillslopeClimateRuntimeRequest,
-        metadata: &DirectPublicationRunMetadata, rows: &[DirectPublicationDayRow], subhourly_rows: &[SavedSubhourlyRow],
-        manifest: &Stage3CommittedDayArchiveManifestV1, records: &ArchiveRecords,
+        metadata: &DirectPublicationRunMetadata,
+        rows: &[DirectPublicationDayRow],
+        subhourly_rows: &[SavedSubhourlyRow],
+        manifest: &Stage3CommittedDayArchiveManifestV1,
+        records: &ArchiveRecords,
     ) -> Result<Self, HillslopeCliError> {
-        let archive = ExpectedStage3CommittedDayArchiveV3 {manifest, reader: records};
-        let native_frame = seed.with_native_restart_context(fresh, climate, &archive, |context| {
-            DirectHydrologyNativeCustodyRestartV1::project(frame,
-                context.hydrology.phase_plan_sha256.clone(), context.hydrology.day_input_digests,
-                &archive, context.native).map_err(fail)?.to_canonical_bytes().map_err(fail)
-        })?;
+        let archive = ExpectedStage3CommittedDayArchiveV3 {
+            manifest,
+            reader: records,
+        };
+        let native_frame =
+            seed.with_native_restart_context(fresh, climate, &archive, |context| {
+                DirectHydrologyNativeCustodyRestartV1::project(
+                    frame,
+                    context.hydrology.phase_plan_sha256.clone(),
+                    context.hydrology.day_input_digests,
+                    &archive,
+                    context.native,
+                )
+                .map_err(fail)?
+                .to_canonical_bytes()
+                .map_err(fail)
+            })?;
         if openwepp_hillslope_orchestrator::snow_accuracy_observer::physical_global_enabled() {
             let destination = std::env::var_os("OPENWEPP_ACCURACY_OUTPUT")
                 .ok_or_else(|| fail("native frame diagnostic output absent"))?;
             let directory = std::path::Path::new(&destination).join("checkpoint-archive-originals");
-            std::fs::write(directory.join("native-frame-unvalidated-diagnostic.bin"), &native_frame).map_err(fail)?;
+            std::fs::write(
+                directory.join("native-frame-unvalidated-diagnostic.bin"),
+                &native_frame,
+            )
+            .map_err(fail)?;
             let diagnostic = serde_json::json!({"phase":"projected_before_private_restore", "checkpoint_admitted":false,
                 "bytes":native_frame.len(), "sha256":openwepp_coupled_time::digest_bytes(&native_frame),
                 "committed_day_count":manifest.committed_day_count,
                 "source_sha256":option_env!("OPENWEPP_B01_SOURCE_SHA256"),
                 "build_inputs_sha256":option_env!("OPENWEPP_B01_BUILD_INPUTS_SHA256"), "policy_id":14});
-            std::fs::write(directory.join("native-frame-unvalidated-diagnostic.json"), serde_json::to_vec(&diagnostic).map_err(fail)?).map_err(fail)?;
-        }
-        let mut value = Self {schema: "OPENWEPP_B01_NATIVE_WORKFLOW_V1_POLICY14".into(),
-            next_day: manifest.committed_day_count, metadata: metadata.clone(), native_frame,
-            publication: frame.publication.clone(), rows: rows.to_vec(), subhourly_rows: subhourly_rows.to_vec(), manifest: manifest.clone(),
-            records: records.clone(), payload: Digest32::zero()};
+            std::fs::write(
+                directory.join("native-frame-unvalidated-diagnostic.json"),
+                serde_json::to_vec(&diagnostic).map_err(fail)?,
+            )
+            .map_err(fail)?;
+        }
+        let mut value = Self {
+            schema: "OPENWEPP_B01_NATIVE_WORKFLOW_V1_POLICY14".into(),
+            next_day: manifest.committed_day_count,
+            metadata: metadata.clone(),
+            native_frame,
+            publication: frame.publication.clone(),
+            rows: rows.to_vec(),
+            subhourly_rows: subhourly_rows.to_vec(),
+            manifest: manifest.clone(),
+            records: records.clone(),
+            payload: Digest32::zero(),
+        };
         value.payload = value.digest()?;
         value.validate_prefix(frame, metadata)?;
         let restored = value.restore(fresh, seed, climate, metadata)?;
@@ -120,52 +188,98 @@
                         let equal = frame.$field == restored.$field;
                         equalities.insert(stringify!($field), equal);
                         if !equal {
-                            std::fs::write(directory.join(concat!(stringify!($field), "-original.debug")), format!("{:?}",frame.$field)).map_err(fail)?;
-                            std::fs::write(directory.join(concat!(stringify!($field), "-restored.debug")), format!("{:?}",restored.$field)).map_err(fail)?;
+                            std::fs::write(
+                                directory.join(concat!(stringify!($field), "-original.debug")),
+                                format!("{:?}", frame.$field),
+                            )
+                            .map_err(fail)?;
+                            std::fs::write(
+                                directory.join(concat!(stringify!($field), "-restored.debug")),
+                                format!("{:?}", restored.$field),
+                            )
+                            .map_err(fail)?;
                         }
                     }};
                 }
-                capture_field!(identity); capture_field!(lanes); capture_field!(phase_plan);
-                capture_field!(publication); capture_field!(lane_transfer_ledger);
-                capture_field!(lane_transfer_downstream_operands); capture_field!(lane_transfer_shadow_projection);
-                capture_field!(groundwater); capture_field!(surface_liquid_shadow);
-                capture_field!(snow_stage3_v11_attachment); capture_field!(laned_active); capture_field!(laned_active_summary);
-                std::fs::write(directory.join("equalities.json"), serde_json::to_vec(&equalities).map_err(fail)?).map_err(fail)?;
+                capture_field!(identity);
+                capture_field!(lanes);
+                capture_field!(phase_plan);
+                capture_field!(publication);
+                capture_field!(lane_transfer_ledger);
+                capture_field!(lane_transfer_downstream_operands);
+                capture_field!(lane_transfer_shadow_projection);
+                capture_field!(groundwater);
+                capture_field!(surface_liquid_shadow);
+                capture_field!(snow_stage3_v11_attachment);
+                capture_field!(laned_active);
+                capture_field!(laned_active_summary);
+                std::fs::write(
+                    directory.join("equalities.json"),
+                    serde_json::to_vec(&equalities).map_err(fail)?,
+                )
+                .map_err(fail)?;
             }
             return Err(fail("workflow full frame project/restore inequality"));
         }
         Ok(value)
     }
-    pub(super) fn restore(&self, fresh: &DirectRunFrame,
+    pub(super) fn restore(
+        &self,
+        fresh: &DirectRunFrame,
         seed: &snow_stage3_v11_production_seed::DirectSnowStage3V11ProductionSeedV1,
         climate: &HillslopeClimateRuntimeRequest,
         metadata: &DirectPublicationRunMetadata,
     ) -> Result<DirectRunFrame, HillslopeCliError> {
         self.validate_prefix(fresh, metadata)?;
-        let archive = ExpectedStage3CommittedDayArchiveV3 {manifest: &self.manifest, reader: &self.records};
+        let archive = ExpectedStage3CommittedDayArchiveV3 {
+            manifest: &self.manifest,
+            reader: &self.records,
+        };
         let mut frame = seed.with_native_restart_context(fresh, climate, &archive, |context| {
             DirectHydrologyNativeCustodyRestartV1::from_canonical_bytes(&self.native_frame, context)
-                .map_err(fail)?.restore(context).map_err(fail)
+                .map_err(fail)?
+                .restore(context)
+                .map_err(fail)
         })?;
-        if frame.identity != fresh.identity || frame.snow_stage3_v11_archived_receipt_prefix().map_err(fail)?.archived_day_count != self.next_day {
+        if frame.identity != fresh.identity
+            || frame
+                .snow_stage3_v11_archived_receipt_prefix()
+                .map_err(fail)?
+                .archived_day_count
+                != self.next_day
+        {
             return Err(fail("workflow restored identity/cursor"));
         }
-        let values = [&self.publication.runoff_m, &self.publication.infiltration_m,
-            &self.publication.evapotranspiration_m, &self.publication.drainage_m, &self.publication.lateral_flow_m];
-        if values.iter().any(|v| !v.is_finite()) { return Err(fail("workflow publication nonfinite")); }
+        let values = [
+            &self.publication.runoff_m,
+            &self.publication.infiltration_m,
+            &self.publication.evapotranspiration_m,
+            &self.publication.drainage_m,
+            &self.publication.lateral_flow_m,
+        ];
+        if values.iter().any(|v| !v.is_finite()) {
+            return Err(fail("workflow publication nonfinite"));
+        }
         frame.publication = self.publication.clone();
         Ok(frame)
     }
     pub(super) fn write(&self, path: &Path) -> Result<(), HillslopeCliError> {
         use std::io::Write as _;
         let bytes = serde_json::to_vec(self).map_err(fail)?;
-        let mut file = std::fs::OpenOptions::new().write(true).create_new(true).open(path).map_err(fail)?;
-        file.write_all(&bytes).map_err(fail)?; file.sync_all().map_err(fail)
+        let mut file = std::fs::OpenOptions::new()
+            .write(true)
+            .create_new(true)
+            .open(path)
+            .map_err(fail)?;
+        file.write_all(&bytes).map_err(fail)?;
+        file.sync_all().map_err(fail)
     }
     pub(super) fn read(path: &Path) -> Result<Self, HillslopeCliError> {
         let bytes = std::fs::read(path).map_err(fail)?;
         let value: Self = serde_json::from_slice(&bytes).map_err(fail)?;
-        if serde_json::to_vec(&value).map_err(fail)? != bytes { return Err(fail("workflow noncanonical bytes")); }
+        if serde_json::to_vec(&value).map_err(fail)? != bytes {
+            return Err(fail("workflow noncanonical bytes"));
+        }
         Ok(value)
     }
 }
@@ -202,64 +316,88 @@
     pub hourly_closure_residual_mm: f64,
 }
 impl SavedSubhourlyRow {
-    pub(super) fn from_row(row: &HillslopeWatSubhourlyRow) -> Self { Self {
-        wepp_id: row.wepp_id.clone(),
-        ofe_id: row.ofe_id.clone(),
-        year: row.year.clone(),
-        sim_day_index: row.sim_day_index.clone(),
-        julian: row.julian.clone(),
-        event_ordinal: row.event_ordinal.clone(),
-        hour_index: row.hour_index.clone(),
-        subinterval_index: row.subinterval_index.clone(),
-        interval_start_s: row.interval_start_s.clone(),
-        interval_duration_s: row.interval_duration_s.clone(),
-        rainfall_depth_mm: row.rainfall_depth_mm.clone(),
-        additional_supply_depth_mm: row.additional_supply_depth_mm.clone(),
-        raw_green_ampt_infiltration_depth_mm: row.raw_green_ampt_infiltration_depth_mm.clone(),
-        depression_storage_retention_depth_mm: row.depression_storage_retention_depth_mm.clone(),
-        raw_wb14_post_depression_generation_depth_mm: row.raw_wb14_post_depression_generation_depth_mm.clone(),
-        closed_wb14_generation_depth_mm: row.closed_wb14_generation_depth_mm.clone(),
-        saturation_return_depth_mm: row.saturation_return_depth_mm.clone(),
-        closing_surface_generation_depth_mm: row.closing_surface_generation_depth_mm.clone(),
-        closing_surface_generation_intensity_mm_h: row.closing_surface_generation_intensity_mm_h.clone(),
-        hourly_authoritative_runoff_depth_mm: row.hourly_authoritative_runoff_depth_mm.clone(),
-        hourly_mean_generation_intensity_mm_h: row.hourly_mean_generation_intensity_mm_h.clone(),
-        hourly_power_equivalent_generation_intensity_mm_h: row.hourly_power_equivalent_generation_intensity_mm_h.clone(),
-        hourly_power_equivalent_duration_s: row.hourly_power_equivalent_duration_s.clone(),
-        power_exponent: row.power_exponent.clone(),
-        method_code: row.method_code.clone(),
-        source_completeness_code: row.source_completeness_code.clone(),
-        hourly_closure_residual_mm: row.hourly_closure_residual_mm.clone(),
-    }}
-    pub(super) fn row(&self) -> HillslopeWatSubhourlyRow { HillslopeWatSubhourlyRow {
-        wepp_id: self.wepp_id.clone(),
-        ofe_id: self.ofe_id.clone(),
-        year: self.year.clone(),
-        sim_day_index: self.sim_day_index.clone(),
-        julian: self.julian.clone(),
-        event_ordinal: self.event_ordinal.clone(),
-        hour_index: self.hour_index.clone(),
-        subinterval_index: self.subinterval_index.clone(),
-        interval_start_s: self.interval_start_s.clone(),
-        interval_duration_s: self.interval_duration_s.clone(),
-        rainfall_depth_mm: self.rainfall_depth_mm.clone(),
-        additional_supply_depth_mm: self.additional_supply_depth_mm.clone(),
-        raw_green_ampt_infiltration_depth_mm: self.raw_green_ampt_infiltration_depth_mm.clone(),
-        depression_storage_retention_depth_mm: self.depression_storage_retention_depth_mm.clone(),
-        raw_wb14_post_depression_generation_depth_mm: self.raw_wb14_post_depression_generation_depth_mm.clone(),
-        closed_wb14_generation_depth_mm: self.closed_wb14_generation_depth_mm.clone(),
-        saturation_return_depth_mm: self.saturation_return_depth_mm.clone(),
-        closing_surface_generation_depth_mm: self.closing_surface_generation_depth_mm.clone(),
-        closing_surface_generation_intensity_mm_h: self.closing_surface_generation_intensity_mm_h.clone(),
-        hourly_authoritative_runoff_depth_mm: self.hourly_authoritative_runoff_depth_mm.clone(),
-        hourly_mean_generation_intensity_mm_h: self.hourly_mean_generation_intensity_mm_h.clone(),
-        hourly_power_equivalent_generation_intensity_mm_h: self.hourly_power_equivalent_generation_intensity_mm_h.clone(),
-        hourly_power_equivalent_duration_s: self.hourly_power_equivalent_duration_s.clone(),
-        power_exponent: self.power_exponent.clone(),
-        method_code: self.method_code.clone(),
-        source_completeness_code: self.source_completeness_code.clone(),
-        hourly_closure_residual_mm: self.hourly_closure_residual_mm.clone(),
-    }}
+    pub(super) fn from_row(row: &HillslopeWatSubhourlyRow) -> Self {
+        Self {
+            wepp_id: row.wepp_id.clone(),
+            ofe_id: row.ofe_id.clone(),
+            year: row.year.clone(),
+            sim_day_index: row.sim_day_index.clone(),
+            julian: row.julian.clone(),
+            event_ordinal: row.event_ordinal.clone(),
+            hour_index: row.hour_index.clone(),
+            subinterval_index: row.subinterval_index.clone(),
+            interval_start_s: row.interval_start_s.clone(),
+            interval_duration_s: row.interval_duration_s.clone(),
+            rainfall_depth_mm: row.rainfall_depth_mm.clone(),
+            additional_supply_depth_mm: row.additional_supply_depth_mm.clone(),
+            raw_green_ampt_infiltration_depth_mm: row.raw_green_ampt_infiltration_depth_mm.clone(),
+            depression_storage_retention_depth_mm: row
+                .depression_storage_retention_depth_mm
+                .clone(),
+            raw_wb14_post_depression_generation_depth_mm: row
+                .raw_wb14_post_depression_generation_depth_mm
+                .clone(),
+            closed_wb14_generation_depth_mm: row.closed_wb14_generation_depth_mm.clone(),
+            saturation_return_depth_mm: row.saturation_return_depth_mm.clone(),
+            closing_surface_generation_depth_mm: row.closing_surface_generation_depth_mm.clone(),
+            closing_surface_generation_intensity_mm_h: row
+                .closing_surface_generation_intensity_mm_h
+                .clone(),
+            hourly_authoritative_runoff_depth_mm: row.hourly_authoritative_runoff_depth_mm.clone(),
+            hourly_mean_generation_intensity_mm_h: row
+                .hourly_mean_generation_intensity_mm_h
+                .clone(),
+            hourly_power_equivalent_generation_intensity_mm_h: row
+                .hourly_power_equivalent_generation_intensity_mm_h
+                .clone(),
+            hourly_power_equivalent_duration_s: row.hourly_power_equivalent_duration_s.clone(),
+            power_exponent: row.power_exponent.clone(),
+            method_code: row.method_code.clone(),
+            source_completeness_code: row.source_completeness_code.clone(),
+            hourly_closure_residual_mm: row.hourly_closure_residual_mm.clone(),
+        }
+    }
+    pub(super) fn row(&self) -> HillslopeWatSubhourlyRow {
+        HillslopeWatSubhourlyRow {
+            wepp_id: self.wepp_id.clone(),
+            ofe_id: self.ofe_id.clone(),
+            year: self.year.clone(),
+            sim_day_index: self.sim_day_index.clone(),
+            julian: self.julian.clone(),
+            event_ordinal: self.event_ordinal.clone(),
+            hour_index: self.hour_index.clone(),
+            subinterval_index: self.subinterval_index.clone(),
+            interval_start_s: self.interval_start_s.clone(),
+            interval_duration_s: self.interval_duration_s.clone(),
+            rainfall_depth_mm: self.rainfall_depth_mm.clone(),
+            additional_supply_depth_mm: self.additional_supply_depth_mm.clone(),
+            raw_green_ampt_infiltration_depth_mm: self.raw_green_ampt_infiltration_depth_mm.clone(),
+            depression_storage_retention_depth_mm: self
+                .depression_storage_retention_depth_mm
+                .clone(),
+            raw_wb14_post_depression_generation_depth_mm: self
+                .raw_wb14_post_depression_generation_depth_mm
+                .clone(),
+            closed_wb14_generation_depth_mm: self.closed_wb14_generation_depth_mm.clone(),
+            saturation_return_depth_mm: self.saturation_return_depth_mm.clone(),
+            closing_surface_generation_depth_mm: self.closing_surface_generation_depth_mm.clone(),
+            closing_surface_generation_intensity_mm_h: self
+                .closing_surface_generation_intensity_mm_h
+                .clone(),
+            hourly_authoritative_runoff_depth_mm: self.hourly_authoritative_runoff_depth_mm.clone(),
+            hourly_mean_generation_intensity_mm_h: self
+                .hourly_mean_generation_intensity_mm_h
+                .clone(),
+            hourly_power_equivalent_generation_intensity_mm_h: self
+                .hourly_power_equivalent_generation_intensity_mm_h
+                .clone(),
+            hourly_power_equivalent_duration_s: self.hourly_power_equivalent_duration_s.clone(),
+            power_exponent: self.power_exponent.clone(),
+            method_code: self.method_code.clone(),
+            source_completeness_code: self.source_completeness_code.clone(),
+            hourly_closure_residual_mm: self.hourly_closure_residual_mm.clone(),
+        }
+    }
 }
 
 fn valid_subhourly_clock(hour: i32, bin: i32) -> bool {
@@ -267,6 +405,10 @@
 }
 #[test]
 fn native_workflow_subhourly_clock_uses_global_day_bins() {
-    for (hour, bin) in [(0,0),(0,11),(1,12),(23,287)] { assert!(valid_subhourly_clock(hour,bin)); }
-    for (hour, bin) in [(0,12),(1,11),(24,288),(0,-1),(-1,0),(23,276-1)] { assert!(!valid_subhourly_clock(hour,bin)); }
-}
+    for (hour, bin) in [(0, 0), (0, 11), (1, 12), (23, 287)] {
+        assert!(valid_subhourly_clock(hour, bin));
+    }
+    for (hour, bin) in [(0, 12), (1, 11), (24, 288), (0, -1), (-1, 0), (23, 276 - 1)] {
+        assert!(!valid_subhourly_clock(hour, bin));
+    }
+}
--- a/crates/openwepp-runner/src/hillslope/snow_stage3_v11_production_seed.rs
+++ b/crates/openwepp-runner/src/hillslope/snow_stage3_v11_production_seed.rs
@@ -213,10 +213,17 @@
         &self.artifact.support_static_authority
     }
 
-    pub(super) fn destination_is_canopy_covered(&self, ofe_id:&str, tile_id: &str)
-        -> Result<bool,openwepp_hillslope_orchestrator::DirectSnowStage3V11AttachmentError> {
-        configured_destination_is_canopy_covered(&self.artifact.lse_configuration,
-            &self.artifact.vegetation_configuration,ofe_id,tile_id)
+    pub(super) fn destination_is_canopy_covered(
+        &self,
+        ofe_id: &str,
+        tile_id: &str,
+    ) -> Result<bool, openwepp_hillslope_orchestrator::DirectSnowStage3V11AttachmentError> {
+        configured_destination_is_canopy_covered(
+            &self.artifact.lse_configuration,
+            &self.artifact.vegetation_configuration,
+            ofe_id,
+            tile_id,
+        )
     }
 
     fn day_zero_committed(
@@ -284,19 +291,20 @@
             .restore(&artifact.lse_configuration)
             .map_err(nested)?;
         let soil_configuration = &artifact.lse_configuration.soil_thermal_configuration;
-        let soil_v1_wire = scientific.soil_thermal.as_ref().filter(|_| scientific.native_soil.is_none())
+        let soil_v1_wire = scientific
+            .soil_thermal
+            .as_ref()
+            .filter(|_| scientific.native_soil.is_none())
             .ok_or_else(|| nested("ordinary seed requires V1 soil"))?;
-        let soil_thermal_v1 = soil_v1_wire.restore(
+        let soil_thermal_v1 = soil_v1_wire
+            .restore(
                 &soil_configuration.owner_id,
                 &soil_configuration.configuration_sha256,
             )
             .map_err(nested)?;
-        let receipt_chain_sha256 = Sha256Digest::try_new(
-            soil_v1_wire.restart_payload_sha256
-                .as_str()
-                .to_owned(),
-        )
-        .map_err(nested)?;
+        let receipt_chain_sha256 =
+            Sha256Digest::try_new(soil_v1_wire.restart_payload_sha256.as_str().to_owned())
+                .map_err(nested)?;
         let (prepared_soil_thermal, soil_thermal_seals) = bootstrap_soil_thermal_v2(
             &soil_thermal_v1,
             &artifact.lse_configuration,
@@ -471,13 +479,22 @@
 }
 
 #[cfg(feature = "test-fixture-authority")]
-pub(super) fn retain_mixed_stage3_initial_snow(lane_id:u32, snow:&openwepp_hillslope_orchestrator::DirectSnowLaneState) -> Result<(),HillslopeCliError> {
-    if !matches!(lane_id,1|2) { return Err(failure("mixed Stage3 lane identity")); }
+pub(super) fn retain_mixed_stage3_initial_snow(
+    lane_id: u32,
+    snow: &openwepp_hillslope_orchestrator::DirectSnowLaneState,
+) -> Result<(), HillslopeCliError> {
+    if !matches!(lane_id, 1 | 2) {
+        return Err(failure("mixed Stage3 lane identity"));
+    }
     MIXED_STAGE3_INITIAL_SNOW.with(|states| {
-        let mut states=states.borrow_mut();
-        if let Some(previous)=states.get(&lane_id) {
-            if previous != snow { return Err(failure("mixed Stage3 seed/execution source changed")); }
-        } else { states.insert(lane_id,snow.clone()); }
+        let mut states = states.borrow_mut();
+        if let Some(previous) = states.get(&lane_id) {
+            if previous != snow {
+                return Err(failure("mixed Stage3 seed/execution source changed"));
+            }
+        } else {
+            states.insert(lane_id, snow.clone());
+        }
         Ok(())
     })
 }
@@ -1717,9 +1734,14 @@
         .iter()
         .map(|layer| layer.layer_id.as_str())
         .collect::<Vec<_>>();
-    let ordinary_soil = committed.scientific.soil_thermal.as_ref().filter(|_| committed.scientific.native_soil.is_none())
+    let ordinary_soil = committed
+        .scientific
+        .soil_thermal
+        .as_ref()
+        .filter(|_| committed.scientific.native_soil.is_none())
         .ok_or_else(|| failure("ordinary production seed requires V1 soil"))?;
-    let configured_soil_layers = ordinary_soil.ofes
+    let configured_soil_layers = ordinary_soil
+        .ofes
         .first()
         .map(|ofe| {
             ofe.ordered_layers
@@ -2453,89 +2475,208 @@
         fresh: &DirectRunFrame,
         climate: &openwepp_hillslope_orchestrator::runtime_inputs::HillslopeClimateRuntimeRequest,
         archive: &openwepp_persisted_restart_v1::ExpectedStage3CommittedDayArchiveV3<'_>,
-        operation: impl FnOnce(&openwepp_persisted_restart_v1::ExpectedDirectHydrologyNativeCustodyContextV1<'_>) -> Result<T, HillslopeCliError>,
+        operation: impl FnOnce(
+            &openwepp_persisted_restart_v1::ExpectedDirectHydrologyNativeCustodyContextV1<'_>,
+        ) -> Result<T, HillslopeCliError>,
     ) -> Result<T, HillslopeCliError> {
-        use openwepp_persisted_restart_v1::{ExpectedRestartStaticContext, ExpectedSnowStage3V11RestartContext,
-            ExpectedNativeCustodyContextV1, ExpectedDirectHydrologyNativeCustodyContextV1};
+        use openwepp_persisted_restart_v1::{
+            ExpectedDirectHydrologyNativeCustodyContextV1, ExpectedNativeCustodyContextV1,
+            ExpectedRestartStaticContext, ExpectedSnowStage3V11RestartContext,
+        };
         self.validate_envelope()?;
         let committed = self.day_zero_committed()?;
         let scientific = &committed.scientific;
         let artifact = &self.artifact;
-        let surface = committed.surface_liquid_configuration.restore().map_err(nested)?;
+        let surface = committed
+            .surface_liquid_configuration
+            .restore()
+            .map_err(nested)?;
         let gsi = committed.gsi_configuration.restore().map_err(nested)?;
-        let forcing = committed.static_forcing_configuration.restore().map_err(nested)?;
-        let days = fresh.lanes.iter().map(|lane| lane.day_inputs.clone()).collect::<Vec<_>>();
-        let digests = scientific.direct_hydrology.lanes.iter().map(|lane| lane.day_inputs_sha256.clone()).collect::<Vec<_>>();
-        let (run, topology) = checkpoint_identities_v1(committed, &artifact.root_zone_hydraulic_configuration).map_err(nested)?;
+        let forcing = committed
+            .static_forcing_configuration
+            .restore()
+            .map_err(nested)?;
+        let days = fresh
+            .lanes
+            .iter()
+            .map(|lane| lane.day_inputs.clone())
+            .collect::<Vec<_>>();
+        let digests = scientific
+            .direct_hydrology
+            .lanes
+            .iter()
+            .map(|lane| lane.day_inputs_sha256.clone())
+            .collect::<Vec<_>>();
+        let (run, topology) =
+            checkpoint_identities_v1(committed, &artifact.root_zone_hydraulic_configuration)
+                .map_err(nested)?;
         let soil = &artifact.lse_configuration.soil_thermal_configuration;
         let hydrology = ExpectedDirectHydrologyRestartContext {
-            phase_plan: &fresh.phase_plan, phase_plan_sha256: &scientific.direct_hydrology.phase_plan_sha256,
-            day_inputs: &days, day_input_digests: &digests, surface_liquid_configuration: &surface,
+            phase_plan: &fresh.phase_plan,
+            phase_plan_sha256: &scientific.direct_hydrology.phase_plan_sha256,
+            day_inputs: &days,
+            day_input_digests: &digests,
+            surface_liquid_configuration: &surface,
         };
         let consumer = ExpectedRestartStaticContext {
-            run_identity_sha256: &run, topology_sha256: &topology,
-            vegetation_configuration: &artifact.vegetation_configuration, vegetation_owner_id: &artifact.vegetation_owner_id,
-            soil_thermal_owner_id: &soil.owner_id, soil_thermal_configuration_sha256: &soil.configuration_sha256,
-            lse_configuration: &artifact.lse_configuration, surface_liquid_configuration: &surface,
-            gsi_configuration: &gsi, forcing_static_configuration: &forcing,
+            run_identity_sha256: &run,
+            topology_sha256: &topology,
+            vegetation_configuration: &artifact.vegetation_configuration,
+            vegetation_owner_id: &artifact.vegetation_owner_id,
+            soil_thermal_owner_id: &soil.owner_id,
+            soil_thermal_configuration_sha256: &soil.configuration_sha256,
+            lse_configuration: &artifact.lse_configuration,
+            surface_liquid_configuration: &surface,
+            gsi_configuration: &gsi,
+            forcing_static_configuration: &forcing,
             root_zone_hydraulic_configuration: &artifact.root_zone_hydraulic_configuration,
-            phase_plan: &fresh.phase_plan, phase_plan_sha256: &scientific.direct_hydrology.phase_plan_sha256,
-            day_inputs: &days, day_input_digests: &digests,
+            phase_plan: &fresh.phase_plan,
+            phase_plan_sha256: &scientific.direct_hydrology.phase_plan_sha256,
+            day_inputs: &days,
+            day_input_digests: &digests,
         };
-        let attachment = fresh.snow_stage3_v11_attachment.as_deref().ok_or_else(|| failure("native restart fresh attachment missing"))?;
-        let physical = attachment.committed.real_consumer.frozen_litter_v3_resident().ok_or_else(|| failure("native restart fresh physical owner missing"))?;
-        let exact = attachment.committed.real_consumer.frozen_litter_v4_resident().ok_or_else(|| failure("native restart fresh exact owner missing"))?;
+        let attachment = fresh
+            .snow_stage3_v11_attachment
+            .as_deref()
+            .ok_or_else(|| failure("native restart fresh attachment missing"))?;
+        let physical = attachment
+            .committed
+            .real_consumer
+            .frozen_litter_v3_resident()
+            .ok_or_else(|| failure("native restart fresh physical owner missing"))?;
+        let exact = attachment
+            .committed
+            .real_consumer
+            .frozen_litter_v4_resident()
+            .ok_or_else(|| failure("native restart fresh exact owner missing"))?;
         let surface_sha = physical.surface_owner().envelope_sha256();
-        let bootstrap_soil = attachment.committed.real_consumer.soil_thermal_resident().v2().map_err(nested)?;
-        bootstrap_soil.restart_authority_validate(&artifact.lse_configuration, &soil.owner_id, &soil.configuration_sha256).map_err(nested)?;
-        let provider_expectations = attachment.committed.real_consumer
-            .restart_authority_native_provider_prefix(climate, fresh.identity.day_count).map_err(nested)?;
+        let bootstrap_soil = attachment
+            .committed
+            .real_consumer
+            .soil_thermal_resident()
+            .v2()
+            .map_err(nested)?;
+        bootstrap_soil
+            .restart_authority_validate(
+                &artifact.lse_configuration,
+                &soil.owner_id,
+                &soil.configuration_sha256,
+            )
+            .map_err(nested)?;
+        let provider_expectations = attachment
+            .committed
+            .real_consumer
+            .restart_authority_native_provider_prefix(climate, fresh.identity.day_count)
+            .map_err(nested)?;
         let native = ExpectedNativeCustodyContextV1 {
             provider_expectations: &provider_expectations,
             bootstrap_soil,
-            expected_laned_active: &attachment.committed.real_consumer.hydrology_frame().laned_active,
-            lse_configuration: physical.lse_configuration(), surface_configuration: physical.surface_configuration(),
+            expected_laned_active: &attachment
+                .committed
+                .real_consumer
+                .hydrology_frame()
+                .laned_active,
+            lse_configuration: physical.lse_configuration(),
+            surface_configuration: physical.surface_configuration(),
             adoption_lse_sha256: &physical.lse_state().0.state_sha256,
-            adoption_exact: exact.exact_surface_owner(), adoption_surface_sha256: surface_sha,
+            adoption_exact: exact.exact_surface_owner(),
+            adoption_surface_sha256: surface_sha,
         };
-        let stage3 = ExpectedSnowStage3V11RestartContext {static_context: &attachment.static_context, real_consumer_context: &consumer};
-        operation(&ExpectedDirectHydrologyNativeCustodyContextV1 {hydrology: &hydrology, stage3: &stage3, archive, native: &native, expected_laned_active: &fresh.laned_active})
-    }
-}
-
-
-fn configured_destination_is_canopy_covered(lse:&LandSurfaceEnergyConfiguration,
-    vegetation:&VegetationConfiguration,ofe_id:&str,tile_id:&str)
-    ->Result<bool,openwepp_hillslope_orchestrator::DirectSnowStage3V11AttachmentError> {
+        let stage3 = ExpectedSnowStage3V11RestartContext {
+            static_context: &attachment.static_context,
+            real_consumer_context: &consumer,
+        };
+        operation(&ExpectedDirectHydrologyNativeCustodyContextV1 {
+            hydrology: &hydrology,
+            stage3: &stage3,
+            archive,
+            native: &native,
+            expected_laned_active: &fresh.laned_active,
+        })
+    }
+}
+
+fn configured_destination_is_canopy_covered(
+    lse: &LandSurfaceEnergyConfiguration,
+    vegetation: &VegetationConfiguration,
+    ofe_id: &str,
+    tile_id: &str,
+) -> Result<bool, openwepp_hillslope_orchestrator::DirectSnowStage3V11AttachmentError> {
     use openwepp_hillslope_orchestrator::DirectSnowStage3V11AttachmentError as Error;
-    let mut ofes=lse.ofes.iter().filter(|o|o.ofe_id.as_str()==ofe_id);
-    let ofe=ofes.next().ok_or(Error::Identity("canopy capability missing configured OFE"))?;
-    if ofes.next().is_some(){return Err(Error::Identity("canopy capability duplicate configured OFE"));}
-    let mut tiles=ofe.tiles.iter().filter(|t|t.tile_id.as_str()==tile_id);
-    let tile=tiles.next().ok_or(Error::Identity("canopy capability missing physical tile"))?;
-    if tiles.next().is_some(){return Err(Error::Identity("canopy capability duplicate physical tile"));}
-    Ok(vegetation.strata.iter().any(|s|s.tile_ids.iter().any(|v|v==&tile.vegetation_tile_id)))
+    let mut ofes = lse.ofes.iter().filter(|o| o.ofe_id.as_str() == ofe_id);
+    let ofe = ofes
+        .next()
+        .ok_or(Error::Identity("canopy capability missing configured OFE"))?;
+    if ofes.next().is_some() {
+        return Err(Error::Identity(
+            "canopy capability duplicate configured OFE",
+        ));
+    }
+    let mut tiles = ofe.tiles.iter().filter(|t| t.tile_id.as_str() == tile_id);
+    let tile = tiles
+        .next()
+        .ok_or(Error::Identity("canopy capability missing physical tile"))?;
+    if tiles.next().is_some() {
+        return Err(Error::Identity("canopy capability duplicate physical tile"));
+    }
+    Ok(vegetation
+        .strata
+        .iter()
+        .any(|s| s.tile_ids.iter().any(|v| v == &tile.vegetation_tile_id)))
 }
 
 #[test]
 #[ignore = "authentic retained two-OFE fixture configuration"]
 fn mixed_physical_destination_resolves_authoritative_vegetation_tile() {
-    let path=std::env::var_os("OPENWEPP_B01_MIXED_CONFIGURATION").expect("actual mixed configuration path");
-    let value:serde_json::Value=serde_json::from_slice(&std::fs::read(path).unwrap()).unwrap();
-    let lse:LandSurfaceEnergyConfiguration=serde_json::from_value(value["lse"].clone()).unwrap();
-    let veg:VegetationConfiguration=serde_json::from_value(value["vegetation"].clone()).unwrap();
-    for (ofe,tile,expected) in [("ofe-1","fixture-lane-1-tile-1",false),
-        ("ofe-1","fixture-lane-1-tile-2",false),("ofe-2","fixture-lane-2-tile-1",true),
-        ("ofe-2","fixture-lane-2-tile-2",false)] {
-        assert_eq!(configured_destination_is_canopy_covered(&lse,&veg,ofe,tile).unwrap(),expected);
-    }
-    assert!(configured_destination_is_canopy_covered(&lse,&veg,"missing","forest").is_err());
-    assert!(configured_destination_is_canopy_covered(&lse,&veg,"ofe-2","missing").is_err());
-    let mut duplicate=lse.clone();duplicate.ofes.push(duplicate.ofes[1].clone());
-    assert!(configured_destination_is_canopy_covered(&duplicate,&veg,"ofe-2","fixture-lane-2-tile-1").is_err());
-    let mut duplicate=lse.clone();let tile=duplicate.ofes[1].tiles[0].clone();duplicate.ofes[1].tiles.push(tile);
-    assert!(configured_destination_is_canopy_covered(&duplicate,&veg,"ofe-2","fixture-lane-2-tile-1").is_err());
-    let mut aliased=lse.clone();aliased.ofes[0].tiles[0].tile_id=aliased.ofes[1].tiles[0].tile_id.clone();
-    assert!(!configured_destination_is_canopy_covered(&aliased,&veg,"ofe-1","fixture-lane-2-tile-1").unwrap());
-    assert!(configured_destination_is_canopy_covered(&aliased,&veg,"ofe-2","fixture-lane-2-tile-1").unwrap());
-}
+    let path = std::env::var_os("OPENWEPP_B01_MIXED_CONFIGURATION")
+        .expect("actual mixed configuration path");
+    let value: serde_json::Value = serde_json::from_slice(&std::fs::read(path).unwrap()).unwrap();
+    let lse: LandSurfaceEnergyConfiguration = serde_json::from_value(value["lse"].clone()).unwrap();
+    let veg: VegetationConfiguration = serde_json::from_value(value["vegetation"].clone()).unwrap();
+    for (ofe, tile, expected) in [
+        ("ofe-1", "fixture-lane-1-tile-1", false),
+        ("ofe-1", "fixture-lane-1-tile-2", false),
+        ("ofe-2", "fixture-lane-2-tile-1", true),
+        ("ofe-2", "fixture-lane-2-tile-2", false),
+    ] {
+        assert_eq!(
+            configured_destination_is_canopy_covered(&lse, &veg, ofe, tile).unwrap(),
+            expected
+        );
+    }
+    assert!(configured_destination_is_canopy_covered(&lse, &veg, "missing", "forest").is_err());
+    assert!(configured_destination_is_canopy_covered(&lse, &veg, "ofe-2", "missing").is_err());
+    let mut duplicate = lse.clone();
+    duplicate.ofes.push(duplicate.ofes[1].clone());
+    assert!(
+        configured_destination_is_canopy_covered(
+            &duplicate,
+            &veg,
+            "ofe-2",
+            "fixture-lane-2-tile-1"
+        )
+        .is_err()
+    );
+    let mut duplicate = lse.clone();
+    let tile = duplicate.ofes[1].tiles[0].clone();
+    duplicate.ofes[1].tiles.push(tile);
+    assert!(
+        configured_destination_is_canopy_covered(
+            &duplicate,
+            &veg,
+            "ofe-2",
+            "fixture-lane-2-tile-1"
+        )
+        .is_err()
+    );
+    let mut aliased = lse.clone();
+    aliased.ofes[0].tiles[0].tile_id = aliased.ofes[1].tiles[0].tile_id.clone();
+    assert!(
+        !configured_destination_is_canopy_covered(&aliased, &veg, "ofe-1", "fixture-lane-2-tile-1")
+            .unwrap()
+    );
+    assert!(
+        configured_destination_is_canopy_covered(&aliased, &veg, "ofe-2", "fixture-lane-2-tile-1")
+            .unwrap()
+    );
+}
--- a/crates/openwepp-runner/src/hillslope/snow_stage3_v11_production_seed_v2_bootstrap_tests.rs
+++ b/crates/openwepp-runner/src/hillslope/snow_stage3_v11_production_seed_v2_bootstrap_tests.rs
@@ -179,7 +179,9 @@
     let soil_configuration = &seed.artifact.lse_configuration.soil_thermal_configuration;
     let v1_owner = committed
         .scientific
-        .soil_thermal.as_ref().unwrap()
+        .soil_thermal
+        .as_ref()
+        .unwrap()
         .restore(
             &soil_configuration.owner_id,
             &soil_configuration.configuration_sha256,
@@ -188,7 +190,9 @@
     let receipt_chain_sha256 = Sha256Digest::try_new(
         committed
             .scientific
-            .soil_thermal.as_ref().unwrap()
+            .soil_thermal
+            .as_ref()
+            .unwrap()
             .restart_payload_sha256
             .as_str()
             .to_owned(),
@@ -290,7 +294,9 @@
         .day_zero_committed()
         .expect("day-zero V1 owner")
         .scientific
-        .soil_thermal.as_ref().unwrap()
+        .soil_thermal
+        .as_ref()
+        .unwrap()
         .last_accepted_transaction_id
         .as_ref()
         .expect("golden predecessor transaction")
--- a/crates/openwepp-vegetation/src/v11.rs
+++ b/crates/openwepp-vegetation/src/v11.rs
@@ -844,18 +844,22 @@
     /// the complete ordered receipts of the independently restored clock.
     /// This does not advance state or replace any checkpoint authority.
     pub fn restore_evaluator_cache_from_accepted_slabs(
-        &mut self, receipts: &[AcceptedSlabReceiptV1],
+        &mut self,
+        receipts: &[AcceptedSlabReceiptV1],
     ) -> Result<(), V11Error> {
         if receipts.len() != self.accepted_segment_checkpoints.len() {
             return Err(V11Error::RestartCheckpoint);
         }
         let mut reconstructed = Vec::with_capacity(receipts.len());
         for (checkpoint, receipt) in self.accepted_segment_checkpoints.iter().zip(receipts) {
-            if checkpoint.receipt_id != receipt.id() || checkpoint.slab_id != receipt.slab_id()
+            if checkpoint.receipt_id != receipt.id()
+                || checkpoint.slab_id != receipt.slab_id()
                 || checkpoint.parent_transaction_id != receipt.parent_transaction_id()
                 || checkpoint.slab_ordinal != receipt.slab_ordinal()
-                || checkpoint.segment_id != receipt.segment_id() || checkpoint.support != receipt.support()
-                || checkpoint.duration_s_bits != receipt.duration_s_bits() {
+                || checkpoint.segment_id != receipt.segment_id()
+                || checkpoint.support != receipt.support()
+                || checkpoint.duration_s_bits != receipt.duration_s_bits()
+            {
                 return Err(V11Error::RestartCheckpoint);
             }
             let candidate = V11AcceptedSegmentCandidate {
