Replacement correctness initial assessment: /root/capture_importer_correctness (Sol/high). This is the owner-permitted replacement for unavailable /root/capture_correctness_final. It preserves the prior final review findings without claiming same-conversation continuity.

Identity and evidence class

Static: read-only inspection of the active diagnostic source `/workdir/openwepp-experiments/b01-wb14-cadence/current-context-capture-20260913`, the adopted continuation, package state, prior correctness review, canonical provider/prepared-support/owner/producer source, and existing persisted-restart fixture use. No build, test, lint, collector, replay, restore, probe, or model execution ran. At the last bounded inspection the capture module SHA-256 was `dd8197eec9a463b21371a22001e0ddf3cc2089eaff2d5621808b93ffef1d02b3`; the only writer-reported retained delta at that instant was the unverified `vec![ledger.clone()]` ownership correction. This is not the forthcoming stable-cut identity and supplies no validation reuse.

Findings and source mappings, ordered by severity

- HIGH — No out-of-scope canonical provider/prepared-support barrier is established. `crates/openwepp-hillslope-orchestrator/src/runtime_inputs/09_snow_free_half_hour_forcing.rs:650-769` already reconstructs `PreparedSnowFreeGsiDayV1` from the accepted GSI receipt, restored ending GSI state, complete forcing-day receipts, beginning/ending provider cursors, and independently supplied static configuration. It validates GSI/static identity, destination order/cardinality, all 48 intervals, and liquid/solid carry custody. This is the actual restart pattern used at `crates/openwepp-persisted-restart-v1/src/host.rs:103-150`, with positive and poison evidence in `crates/openwepp-persisted-restart-v1/src/evidence_fixture.rs:1335-1471`. The earlier barrier report based only on the absence of a `DirectSnowStage3V11PreparedSupportRestartV2` inverse is therefore unsupported.

- HIGH — Complete prepared-support and forcing authentication remains required on the stable cut. `DirectSnowStage3V11PreparedSupportRestartV2` is intentionally a canonical projection/validator rather than a live inverse: `crates/openwepp-hillslope-orchestrator/src/snow_stage3_v11_restart.rs:542-633`. The source-required sequence is to reconstruct the provider and live support graph from admitted inputs, bind it with `PreparedStage3V11DayV1::bind_production_provider_day` (`snow_stage3_v11_attachment.rs:2286-2470`), and validate the captured target projection with `restart_authority_validate_support_checkpoint_v2`. The real runner path already reconstructs this graph at `crates/openwepp-runner/src/hillslope/05_runner_execution_and_outputs.rs:468-500`, through `direct_runtime/snow_stage3_v11_scheduler.rs:369-413` and the private runner builder at `crates/openwepp-runner/src/hillslope/direct_publication/day_input_and_helpers/00c_day_input_builder_impl.rs:263-502`. Private diagnostic/test fixture-loading support can compose these existing paths without changing a public API, canonical wire schema, or physics. The current terminal capture site sees one support, while the binding constructor requires all 48; minimum outer attachment/runner transport of the validated day or equivalent authentic prepared input is necessary and is inside the adopted prepared-support plumbing allowance. If that composition proves impossible without a public/schema/physics change, return as a newly evidenced scope blocker rather than weakening the join.

- HIGH — The forcing receipt must be recomputed from the reconstructed actual prepared day. Production derives it at `snow_stage3_v11_attachment_runtime.rs:880-885` through `snow_stage3_v11_forcing_digest.rs:1-36`, which incorporates day/index, accepted GSI receipt, support, provider receipt, support identity, and four forcing/configuration projections. A digest copied from the capture row is not authority. The importer must reject a well-formed substituted provider/support whose row digest is self-consistent.

- HIGH — Owner representations require separate canonical joins. `begin_v11_parent_for_support` constructs the new clock and parent from the same ordered beginning owners at `snow_stage3_v11_attachment.rs:2893-2923`. Therefore restored clock owners must equal the restored parent's current `staged_complete_owners` after `V11OwnerEnvelope::to_owner_state`, and parent transaction, accepted cursor, parent support, and accepted prefix must join exactly. By contrast, the consumer's six-owner V11 projection comes from `DirectV10RealConsumerShadow::canonical_v11_parent_owner_state_bytes` at `canonical_owner_bytes.rs:1062-1082`; it is a representation of the actual consumer and excludes snow. It must be compared with the corresponding projection of a canonically restored consumer, including accepted-publication/native custody authority where applicable. Blanket equality between this consumer map and the seven clock-owner bytes is invalid; the retained raw inventory already contains legitimate differing digests for several representations. Missing, extra, reordered/foreign, or substituted owners must be rejected by their own projection joins.

- HIGH — The prior final review's two substantive correctness findings remain open until the stable cut proves otherwise: the importer did not consume/authenticate provider, prepared-support, run/topology, terminal-custody, binding, parent/prefix, or forcing sources, and it accepted arbitrary nonempty consumer-owner values whose keys happened to occur in the clock map. The local E0382 ownership fix addresses only compilation ownership and does not close either finding.

- MEDIUM — Real-producer agreement must be non-tautological. The actual covered producer computes reduction, ledger, provisional receipt, and binding at `snow_stage3_v11_terminal_execution.rs:2302-2351`, and the observation hook receives those independently produced values at `:2352-2368`. The importer helper formerly duplicated the producer's StepConstraint/reduction/ledger/candidate/accept sequence. Preserve comparison against the values obtained from the real producer path; a row manufactured and consumed by the same helper is only a local consistency check. Substantial duplicated constructor logic remains at least medium severity unless centralized or explicitly justified with independent producer comparison.

- MEDIUM — Exact selection and once-only custody remain open. The prior selector only gated day, interval, and child support. Stable code must additionally bind the required parent support `[385200000000000,387000000000000)` ns, run identity, topology identity, and one unambiguous target capture, rejecting wrong parent/run/topology and duplicate/conflicting target observations without changing simulation control flow.

- MEDIUM — Binding and prefix joins have direct canonical sources. The captured binding must agree with the restored parent/clock transaction, provisional accepted-slab digest, complete beginning-owner digest, parent/child supports, and selected duration; the canonical production fields are built at `snow_stage3_v11_terminal_execution.rs:2342-2351`. Parent `accepted_until`, accepted segment checkpoints/zero-duration transitions, and the consumer's accepted-publication history must establish the current prefix rather than merely contain nonempty histories. Existing accepted-history validation is exposed at `v9_real_consumer_shadow_v10_accessors.rs:203-257`; native custody boundary validation is at `v9_real_consumer_shadow/native_snow_liquid_publication.rs:380-445`.

Residual risk and missing tests

- No stable substantive writer cut was available for this assessment. The prior terminal source remains HOLD / NOT ACCEPTED, and every prior finding is retained unless the later same reviewer verifies its fix on the stable cut.
- No focused behavior case has run under the continuation. Required evidence still includes nonzero exact selection; legitimate recorder/importer use of real provider and prepared-support sources; provider, owner, binding, parent/prefix, run/topology, and duplicate poisons that reach their intended checks; disabled/non-target no-serialization; isolated diagnostic failure preserving model E008; and zero constitutive/ingress/WB14 entry.
- No current-source format check, matched orchestrator/runner lint, authentic day-4 bundle, baseline capture, or fresh-process probe exists. Capture and probe allowances remain 0/1 each at this assessment.
- The later review must bind the exact stable source diff and evidence. Hash equality alone cannot close semantic joins or establish authentic producer agreement.

Verdict

HOLD / NOT ACCEPTED pending a stable complete cut and the selected execution evidence. Static inspection finds a viable in-scope canonical composition and no present public-API/schema/physics blocker; it does not approve an implementation that omits any required join.
