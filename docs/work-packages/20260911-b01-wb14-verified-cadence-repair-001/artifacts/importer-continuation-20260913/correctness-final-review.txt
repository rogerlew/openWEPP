Replacement final correctness review: /root/capture_importer_correctness (Sol/high), same conversation as the continuation initial assessment. This is fix review of the affected frozen cut, not a fresh review wave and not continuity with unavailable /root/capture_correctness_final. All findings from the prior final review remain preserved unless explicitly resolved below.

Evidence class: Static review of the frozen 2026-09-14 00:21:57 UTC isolated source at `/workdir/openwepp-experiments/b01-wb14-cadence/current-context-capture-20260913` and supplied raw evidence. Reviewer-ran read-only SHA-256 checks matched all five `terminal-source-recovery.json` capture hashes: `lib.rs` 13cb587e..., `snow_stage3_v11_attachment.rs` 646ef885..., `snow_stage3_v11_current_context_capture.rs` 3496d7eb..., `snow_stage3_v11_terminal_execution.rs` a17bed40..., and `v9_real_consumer_shadow_tests.rs` 3ffce676.... The retained patch SHA-256 is 66c7ab9d.... I ran no build, test, lint, collector, replay, restore, probe, or model execution. Supplied logs show two earlier narrow single-test passes on predecessor cuts, then cycle 8/9 failed at E0382 while compiling the expanded poison test; the final test ownership correction and the much larger provider/support source were not executed on this frozen identity.

Findings, ordered by severity:

- HIGH — The new provider/prepared-support implementation is completely unwired and cannot affect either bundle production or import. `snow_stage3_v11_current_context_capture.rs:25-458` defines capture/restore DTOs, `restore_prepared_support`, provider restoration, and full-day projection validation, but no call site exists. `capture_row` at :484-542 emits neither provider-day nor prepared-day/support fields; `PreChildContext` at :467-482 carries neither; `import_and_reconstruct_pre_child_context` at :665-823 never calls `restore_provider_day`, `restore_prepared_support`, or `validate_prepared_day`. The real hook remains the old one-support call at `snow_stage3_v11_terminal_execution.rs:2352-2368`. Consequently the cut does not restore or validate the actual remaining-parent provider, all 48 prepared supports, `DirectSnowStage3V11PreparedSupportRestartV2`, or the actual forcing source. This leaves B01-CONT-015/016 unmet.

- HIGH — The importer still trusts the row-supplied forcing receipt as its own authority. At `snow_stage3_v11_current_context_capture.rs:745-754` it parses `provisional_constructor_inputs.forcing_receipt` and feeds it directly into the duplicated constructor. It never derives `canonical_parent_forcing_digest` from a restored provider-bound prepared support, so a well-formed substituted forcing/provider payload can remain self-consistent and pass the exercised path.

- HIGH — The composed consumer is not restored from the bundle. `import_and_reconstruct_pre_child_context` accepts a caller-supplied live `DirectV10RealConsumerShadow` at :665-669 and compares captured consumer owners/provider cursor with that caller object at :807-820. The only test passes the same `shadow` used to create the row (`v9_real_consumer_shadow_tests.rs:993-1113`). This is a helper round trip, not a fresh-process file consumer or canonical restoration of accepted history/native owner/provider state, and cannot establish source identity for the authentic day-4 consumer.

- HIGH — Parent/clock/prefix and custody validation remains incomplete. After canonical parent restore, the importer checks only parent transaction identity at :716-718 and row copies of parent support/accepted cursor against the clock at :720-729. It never projects the restored parent's `staged_complete_owners` to the restored clock owner set, never joins the parent checkpoint's accepted cursor/accepted segment and zero-duration prefix to the clock and consumer accepted history, and never consumes `terminal_parcels_produced_unconsumed_canonical_json`. Thus substituted staged owners, foreign prefix history, or altered ProducedUnconsumed parcel custody can pass despite well-formed independent objects.

- HIGH — No final source validation exists. `focused-command-records.json` records two earlier exit-0 one-test runs, then exit 101/E0382 for the poison expansion. The frozen source fixes that test move and adds hundreds of provider/support lines after the passing cuts, but no build or test ran on hash 3496d7eb.../3ffce676.... There is no executable evidence that the frozen source compiles, that its new restoration functions type-check when used, or that any required test executes on it. Hash reconstruction is preservation evidence only.

- MEDIUM — Target selection and duplicate custody remain only partially repaired. `is_target` at `snow_stage3_v11_current_context_capture.rs:460-465` still checks only day, interval, and child support. `record_pre_child_context` at :557-567 has no parent-support, run, topology, or once-only key and no duplicate/conflict guard. Import-time run/topology checks at :694-697 do not prevent recording multiple ambiguous rows or wrong-parent rows. The synthetic test covers only a wrong run mutation; there is no wrong topology/parent or duplicate-target case.

- MEDIUM — The provisional constructor remains substantially duplicated. `reconstruct_provisional_on_clone` at :599-659 mirrors the production StepConstraint/reduction/ledger/candidate/accept sequence at `snow_stage3_v11_terminal_execution.rs:2302-2341`. The only test obtains the row's producer values from that same helper and imports them through the same helper (`v9_real_consumer_shadow_tests.rs:1054-1096`). The real production hook supplies actual values, but no test compares against that producer, and no explicit justification addresses long-term divergence.

- MEDIUM — The newly added private prepared-support reverse mapping is a large second representation with no executed parity proof. `snow_stage3_v11_current_context_capture.rs:25-323` manually flattens and reconstructs model enums and many physical scalar arrays, while `snow_stage3_v11_attachment.rs:963-989` exposes private internals for it. Because the entire path is unused, there is no round-trip proof against the canonical live provider-bound support, no poison coverage, and no evidence that manual field ordering preserves all semantics. This duplicated mapping must not be treated as a completed canonical consumer merely because its DTOs serialize.

Verified fixes and retained status:

- Static only: the original E0382 in `reconstruct_provisional_on_clone` is corrected with `vec![ledger.clone()]`; no compiler evidence exists on the final source.
- Static only: importer run/topology comparison, binding digest/support comparisons, and consumer-value equality against a supplied consumer were added. These narrow checks are valid but do not close the authentic restoration, prefix, custody, forcing, selector, or producer findings above.
- Static only: raw canonical owner bytes and non-propagating observation error handling remain retained from the prior cut. Runtime observation isolation remains unverified.
- The initial assessment correctly rejected the claimed canonical impossibility: existing provider restoration is available and the incomplete implementation is not evidence of an out-of-scope/public-API/schema/physics blocker.

Residual risk and missing tests:

- No authentic full-day provider/prepared reconstruction, target support projection validation, canonical forcing derivation, restored-consumer history/native custody join, parent staged-owner/prefix join, terminal-parcel custody validation, or real-producer comparison ran.
- No disabled/non-target no-serialization case, diagnostic-recording-failure isolation case, exact selector wrong parent/run/topology case, duplicate/conflict case, intended owner/provider/binding poisons, or complete zero constitutive/ingress/WB14 audit exists on the frozen source.
- No final formatting check, matched orchestrator/runner lint, exact build identity, baseline capture, authentic bundle, or separate fresh-process file probe exists. New capture/probe remain 0/1 each; protected witness remains exhausted 1/1.
- The focused final test did not compile on its last execution, and the frozen correction was not rerun. Earlier passes cannot be reused for the expanded final source.

Verdict: HOLD / NOT ACCEPTED. The frozen cut preserves recoverable partial provider/support work and some narrow importer joins, but does not deliver a wired authentic recorder/importer, usable fresh-process bundle, or required evidence. The source mapping remains viable in scope; incompleteness must not be relabeled canonical impossibility.
