# B01-WB14-CURRENT-CONTEXT-CAPTURE — same-checkpoint continuation

DRAFT FOR OWNER ADOPTION. This document does not authorize itself.

## Active checkpoint, source, changes, acceptance, allowances, and stops

**One active checkpoint:** continue B01-WB14-CURRENT-CONTEXT-CAPTURE in
`docs/work-packages/20260911-b01-wb14-verified-cadence-repair-001/package.md`.
Deliver the already-authorized usable pre-child capture and fresh-process,
zero-physics restoration/provisional-construction result. The cadence candidate
stays suspended and NOT ACCEPTED. The witness and test-only regression stay closed.
This is not a new package, a repair authorization, or a reset of prior work.

**Immutable evidence revision:** `633a69713c9c1d59f7686d2cd1c1645a916208bb` in
`rogerlew/openWEPP` ([immutable commit](https://github.com/rogerlew/openWEPP/commit/633a69713c9c1d59f7686d2cd1c1645a916208bb)). Read the current package section and
`artifacts/current-context-capture-20260913/{correctness-final-review.txt,qa-final-review.txt}`.
Preserve original findings and failed cuts; the sections below group their fixes,
not replace their requirements.

**Actual executable source:** the existing diagnostic copy
`/workdir/openwepp-experiments/b01-wb14-cadence/current-context-capture-20260913`.
Its base is the accepted `baseline-red` diagnostic tree, itself immutable
`reconciled-available145-r1` plus the three accepted test patches and two fixtures.
Do not identify this modified copy by the underlying 927-entry map alone.
The current failed capture patch is
`artifacts/current-context-capture-20260913/pre-focused-test.patch`, SHA-256
`da01cf0668f4a0f81bd83ca291000c99cd6086a75fef271e19ac5a17e52abb85`.
`terminal-source-recovery.json` records reconstruction against the actual baseline.
The four terminal capture-file hashes are:

| File under `crates/openwepp-hillslope-orchestrator/src/` | SHA-256 |
| --- | --- |
| `lib.rs` | `13cb587e3fb4964aeab1f678d8237dc12ffa8bb61ce0e00579f8b2650e8742e6` |
| `snow_stage3_v11_current_context_capture.rs` | `eca013724a61f35fa920b62856a28d0be2d993f903c9c18a716fe59b7606e43d` |
| `snow_stage3_v11_terminal_execution.rs` | `a17bed40225a821636bdb207c83d210f39e9281bb9db5858cafc2001ef1da4c4` |
| `v9_real_consumer_shadow_tests.rs` | `308a030b7d8a1a87ff591d6d787a258f8a11c5db2ebd2bc9febbdf66385633f6` |

Verify relevant continuation inputs and preserve drift rather than overwriting it.
Keep the accepted baseline, original captures and rejected cadence patch unchanged.
Do not apply the rejected cadence patch to this capture copy.

**Permitted writes:** finish the existing private observational recorder/importer,
its focused tests and fixture-loading support, and minimum previously authorized
producer/prepared-support capture plumbing; update this package's evidence and
record. Use existing canonical serializers, restore validators, and producer
semantics. Necessary diagnostic transport may reach the already-authorized
terminal/snow-free producer or existing runner test harness. This does not permit
changes to physics, admission, clock acceptance, provider behavior, forcing,
receipt identities, public APIs, canonical wire schemas, dependencies, features,
manifest/lockfile/toolchain, primary runtime, or the protected old checkpoints.
Do not add general capture, validation, or agent-management frameworks.

**Acceptance:** complete the existing recorder/importer requirements and both
independent affected-scope reviews; demonstrate legitimate small-fixture producer
and consumer behavior, targeted negative controls, zero physical entry in import,
observational error isolation, exact selection and source identities, formatting,
and the prospectively adopted matched lint policy. Then perform the previously
reserved baseline capture and fresh-process probe under the unchanged acceptance
and capture protocol. A compile pass, self-consistent helper round trip, or a
nonempty payload is not a usable-fixture result.

**Proposed cycle-only amendment:** raise the shared cumulative failed-correction
ceiling from **7 to 9**. Keep the **180-active-minute ceiling unchanged**. Carry
**137m37s consumed, seven failed corrections, and 42m23s remaining** from the
owner's latest publication report. No additional active time is granted. Charge
reading, implementation, orchestration, verification, reviews and preservation;
exclude only documented pure waits and count concurrent wall time once.
A corrective edit followed by unintended failed verification, including ordinary
compiler/integration failures, consumes a cycle. Return a failed verification to
Astra before any next corrective edit. No delegation or thread change resets this.

**Run allowance unchanged:** baseline capture **0/1** and separate fresh-process
zero-physics probe **0/1**, with no additional invocation or retry. The original
witness remains exhausted **1/1**. The one baseline capture retains the original
B01 / gradual_warm_tail7 / one-OFE / physical / day-zero inputs, unchanged
collector protocol and 3,600-second child timeout. Focused small Rust tests,
necessary builds/listings and selected lint are permitted; they must not conceal
a full day-zero replay. The probe consumes only the NEW bundle and executes no
constitutive, ingress or WB14 physics. No candidate suffix or protected checkpoint
operation is authorized.

**Stops:** the revised cycle ceiling or unchanged time ceiling; the first new
downstream setup/physics/canonical-restore/finalization failure; necessary
source/input corruption or unexplained drift; an out-of-scope required change;
or an explicit owner stop. The already-known importer implementation defects may
be corrected within this envelope. A failed canonical restoration or different
runtime failure is not permission for another repair or changed expectations.
Missing independent acceptance keeps the checkpoint incomplete. At a stop,
preserve and return; no automatic cadence continuation.

## Finish the known correction, not just the compiler line

### 1. Mechanical ownership and actual test selection

In `reconstruct_provisional_on_clone`, retain the ledger needed for the returned
and compared evidence while supplying the owned value required by the candidate.
The compiler's suggested `vec![ledger.clone()]` is a local data-ownership fix;
using it does not mint a receipt twice or authorize cloning a live capability.
Do not alter a production API or construct a second physical transaction merely
to resolve E0382.

The retained focused test is nested:
`v9_real_consumer_shadow::tests::b01_current_context_importer_restores_fixture_without_physics`.
A bare function name combined with libtest `--exact` selects no such test.
Discover/verify the full compiled name, use the actual capture manifest and
retained features/Nix/stack/target, and require nonzero exact selection and
reported execution. Add clearly named focused cases as needed within this same
test surface. Preserve complete commands, exits and case outcomes. Do not claim
an unchanged old executable validates a new source cut.

### 2. Consume and validate the complete captured context — B01-CONT-015

Complete the importer rather than merely expanding the writer's JSON. Restore
actual prepared support and remaining-parent provider/forcing inputs through the
existing canonical fixture path, including
`DirectSnowStage3V11PreparedSupportRestartV2` where the actual source requires it.
Retain and validate static configuration, run/topology identity, provider cursor,
terminal-parcel produced/unconsumed custody, enclosing parent/support, current
clock and accepted history, binding, and the required parent/prefix relationships.
Prove the forcing receipt from the restored actual provider/prepared inputs; do
not use a row-supplied digest as its own authority. An external static context is
acceptable only when its complete relevant identity is joined to the recorded
original input/configuration evidence and restored objects.

The importer currently compares clock-owner copies but checks the separately
captured consumer owner map only for nonemptiness and subset key membership.
Replace that with canonical restoration/projection and all source-required owner
membership/value/identity joins. Reject missing or extra required owners and
well-formed foreign/substituted payloads. Preserve legitimate differences between
consumer, clock, parent and native-owner representations: validate each against
its corresponding canonical projection, not blanket equality of unlike views.
No nonempty byte vector or matching key can substitute for that relationship.

Carry the existing precise target: day4/interval22, child
`[385920000000000,385980000000000)` ns, parent
`[385200000000000,387000000000000)` ns. Bind the intended run and topology from
actual identified inputs. Recording must yield one unambiguous target context;
track/reject duplicate/conflicting target captures at the diagnostic collection
boundary without changing simulation control flow or suppressing evidence.
A selector test must cover wrong parent/run/topology, not just day and child time.

### 3. Establish agreement with the real producer — B01-CONT-016

The current positive test obtains its receipt from
`reconstruct_provisional_on_clone` and then uses that same helper in the importer.
Keep such a round trip only as a local consistency check. It is not the required
comparison with the existing producer.

Use the real canonical producer/fixture path where available. Otherwise retain
the necessary diagnostic reconstruction with explicit source mapping and a
comparison against the actual producer's independently obtained reduction,
ledger, receipt, binding and canonical beginning state. Do not require a broad
production refactor merely to share code. A refactor that changes production
semantics or exceeds observational scope requires return, not silent expansion.
Never run physics in the importer to obtain a supposedly independent oracle.

Small tests must exercise the recorder and consumer with a legitimate existing
context, test the applicable provider/owner/binding poisons, demonstrate absence
of constitutive/ingress/WB14 calls, and compare canonical beginning state before
and after provisional construction on a clone. Preserve the distinction between
synthetic small controls and the later authentic captured day4 bundle. Mutations
must reach their intended check rather than pass by causing an unrelated parser
or checksum error. Do not reinstate a large unrelated negative-test framework.

### 4. Preserve observational behavior and complete the composed consumer

Keep the statically improved raw canonical-byte retention and diagnostic-error
isolation. Test that disabled/non-target recording does not serialize state and
that diagnostic recording failure is retained separately without replacing the
model's own E008 or changing physical acceptance. Do not introduce an early exit,
an admission bypass, a new runtime decision, or a second physics call.

Finish the relevant producer/prepared-support hooks and a real file-consuming
fresh-process entrypoint before spending the capture invocation. Every required
bundle component must have a canonical consumer; attaching an unused provider
blob does not meet this requirement. Future accepted children and finalization
receipts remain outputs of later corrected execution, not capture prerequisites.

## Verification, launch, review and disposition

Use Astra/medium for orchestration and one Terra/medium writer under current
repository instructions. Give that writer the complete finite reviewer inventory,
not an assignment to fix E0382 and improvise afterward. Implementation sequencing
within these bounds belongs to Astra. Do not reopen resolved witness, source
reconciliation, baseline-regression or test-only lint work.

Run focused small behavior checks before expensive capture preparation. Complete
the selected formatting checks and matched lint for both orchestrator and runner.
The baseline combined Clippy attempt did not finish runner lint; retain the
separately selected runner baseline and match its actual command/toolchain and
coverage. Reuse unchanged baseline measurements when valid, compare full relevant
diagnostics on the final capture source, and preserve all Clippy FAIL outcomes.
The prospective no-new-relevant-diagnostics policy cannot waive a compiler error,
incomplete tests, or the importer correctness gaps. No lint suppression or
unrelated cleanup is authorized.

Reuse `/root/capture_correctness_final` (Sol/high) and `/root/incoming_qa`
(Terra/medium), independently of the writer and this adviser. Review only affected
source and evidence; preserve prior findings and accepted portions. On adoption,
one identified replacement per genuinely unavailable required scope is permitted,
with the original findings carried and no invented same-conversation continuity.
A capacity error calls for supported sequencing, not a spawn/reviewer cascade.
No extra review pipeline is required. Reviewer absence cannot turn an unmet
requirement into approval.

The only launch condition is completion of the already-selected pre-run tests,
source/input/build identity and observation-only correctness/QA assessment on the
actual stable capture cut. These reviewers then assess the new execution evidence
within their same scopes; this is not another independent-review wave. Do not
launch solely because the code compiles. Preserve exact binary/source and original
input identities, verify current storage, and freeze stable canonical comparisons
before measurement as required by the adopted capture authorization.

After the one baseline invocation, retain the actual model E008, separate capture
status, complete bundle and original-file hash/row membership. Then run the one
separate file-reading probe, minting a fresh process-local incarnation through
canonical restoration and provisional construction. Do not restore the old
non-wire live revision. No physical suffix is included. A different failure or
incomplete bundle returns immediately with its real evidence and no retry.

Update only the existing package narrative and needed evidence. Carry scoped
commit/push permission on the existing branch, preserve unrelated work and all
failed cuts, verify published small-file bytes and state which payloads stay local.
No repeated archive search, full historical corpus rehash, frozen witness rebuild
or speculative source cleanup is a routine prerequisite.

Return source/patch identities, each selected command/result, current-run bundle
and probe outcome, both attributable dispositions and the carried ledger. The
capture checkpoint is complete only when its full retained acceptance is met.
B01-CONT-005/013/014, the rejected cadence candidate, scientific qualification,
production integration and the wider repair remain HOLD regardless.
