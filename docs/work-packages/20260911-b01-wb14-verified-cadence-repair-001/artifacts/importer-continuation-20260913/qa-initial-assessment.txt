Replacement QA initial assessment: /root/capture_importer_qa (Terra/medium). This is the owner-permitted replacement for unavailable /root/incoming_qa. It preserves the prior QA findings and claims no same-conversation continuity.

Evidence class: Static. I read the adopted continuation, package continuation sections, work-package review procedure, prior correctness and QA final reviews, input/source/lint evidence, and the selected candidate lint records. I ran no build, test, formatter, lint, deny check, model, capture, collector, replay, restore, probe, or source edit. I deliberately did not treat the writer's mutable diagnostic source as an accepted cut.

Findings:

- HIGH — Launch and probe custody are not yet acceptable. The adopted authorization requires the completed exact pre-run tests, stable-cut source/input/build identity, observational correctness and QA assessment, then a freeze of canonical comparisons before the sole capture. Neither a stable source-cut identity nor the required completed reviewer assessment exists yet. Capture remains 0/1 and the separate fresh-process probe remains 0/1. Do not launch either allowance from this initial assessment.

- HIGH — No final exact test-selection record exists. The retained historical command used a bare test name with libtest `--exact`; compilation failed first, so it executed zero test bodies and never reached selection. Both prior reviews correctly identify that this bare selector would select zero cases after compilation. The continuation correctly requires the nested compiled name `v9_real_consumer_shadow::tests::b01_current_context_importer_restores_fixture_without_physics`, but supplies neither a final command/listing that binds that exact name nor a nonzero execution result. The final evidence must preserve actual manifest, Nix/toolchain, features, target, source hash, full nested selector, selected/executed count, and outcomes for the positive and required poison/zero-physics cases.

- HIGH — Required acceptance evidence for formatter and matched lint is still absent. `selected-lint-commands.json` correctly derives (a) the existing combined orchestrator+runner baseline and (b) the separately selected runner baseline with only source, cwd, and target substitutions. It remains NOT RUN. A final candidate comparison must retain complete JSON diagnostics and prove relevant equality by code, primary/secondary spans, source content, and dependency surface; baseline totals (orchestrator lib 1231/lib-test 1536; runner lib 3/lib-test 79) are not attribution evidence. No non-mutating final formatting-check command/result is selected or recorded. `cargo deny check` is conditionally NOT_APPLICABLE only if the stable terminal diff confirms unchanged manifests, lockfile, dependency/toolchain resolution, and policy surface; that reconciliation is not yet available.

- MEDIUM — Continuation input custody is adequate for the listed launch inputs, but does not establish final source or binary custody. `input-continuity.json` reports all 17 recorded runtime-template, Nix, collector, and case hashes matching; it also reports available storage and no old-corpus rehash. The preserved original capture patch/source recovery remains identifiable. The active source is expressly unverified beyond the writer-reported ownership delta, and no final source tree/patch hash, clean-diff reconciliation, executable/binary identity, or frozen original-input recheck tied to a stable cut exists. These are required before measurement, not substitutes for it.

- MEDIUM — The adopted shared ledger is supported by the retained terminal-publication record. `/workdir/openwepp-experiments/b01-wb14-cadence/logs/current-context-capture-remote-verification.json` reports terminal UTC `22:59:46.829565`, cumulative active seconds `8257` (137m37s), zero pure-wait deductions, and seven failed corrections. The adopted 137m37s consumed, 7/9 failed-correction, 42m23s remaining ledger and 00:27:23 UTC deadline are therefore the governing conservative limits.

Non-blocking debt / follow-ups:

- Prior final-review defects remain open until the stable cut proves complete prepared-support/provider/forcing and owner/parent/prefix joins, real-producer agreement, target duplicate/conflict handling, observational error isolation, and file-consuming zero-physics import. A helper-only round trip or successful compilation is insufficient.
- The final terminal-diff reconciliation must reevaluate cargo-deny and authority-suite anti-evasion applicability. The present N/A posture is evidence for the old four-file preserved cut, not a waiver for the new cut.

QA verdict: HOLD / NOT ACCEPTED for launch or probe. The continuation’s source-input ledger and planned two-command lint pairing are coherent as prospective custody, but all acceptance-bearing final-source, exact-selection, formatting, matched-lint-attribution, independent-review, capture, and fresh-process evidence is still missing.

Evidence update — supplied after this initial assessment: `focused-importer-ownership.stdout`, `.stderr`, and `.exit` show the fully qualified nested test selected one case and passed (exit 0) on the writer-reported ownership-only cut SHA-256 `dd8197eec9a463b21371a22001e0ddf3cc2089eaff2d5621808b93ffef1d02b3`. This resolves only the historical bare-selector/zero-selection defect for that narrow ownership test. Sanitized command metadata (argv, cwd, manifest, features, target, stack/toolchain, timestamps, and source identity) is still missing, as are the composed producer/importer behavior, poison, zero-physics, formatter, matched-lint, stable-cut review, capture, and probe results. The HOLD verdict is unchanged.
