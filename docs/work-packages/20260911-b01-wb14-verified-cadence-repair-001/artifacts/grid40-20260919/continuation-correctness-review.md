# Grid40 continuation correctness review

Reviewer: `/root/grid40_correctness` (independent replacement correctness
reviewer; no implementation, authority, adviser, or experiment-author role).

## Findings

No blocking correctness, science-contract, lifecycle, source-continuity, or
recorder finding remains in the reviewed continuation increment.

## Static and ran evidence

**Static:** I reviewed the owner-adopted continuation authorization, the Grid40
terminal package disposition, the canonical Grid40 solve-boundary section, the
exact lifecycle amendment, the one-line recorder deadline diff, the existing
recorder implementation and controls, the immutable terminal correctness GO,
the source/recovery/binary receipts, the original-input bit manifest and strict
baseline comparator, the result row-dimension map, the final QA write-boundary
review, and the default-feature ignored-test build/listing receipt. I did not
author or change Rust, numerical policy, canonical evaluator behavior, source,
configuration, or an experiment arm.

**Ran:** Read-only source snapshot and byte/hash comparisons, deadline
arithmetic, Git diff inspection, JSON inspection, and documentation whitespace
checks. I did not run Rust tests, a solver, a canonical evaluator, or either
original-start arm. The supplied preparation and recorder-control results are
root-run evidence inspected by this reviewer, not commands I ran.

## Continuity and affected-fix verification

The owner's 2026-09-20 amendment prospectively reactivates the same
`EXP-B01-LSE-GRID40-20260919` study once. It retains the prior
INCOMPLETE/BLOCKED disposition and zero-arm history, permits only final QA,
concrete freeze, the still-unspent baseline/treatment pair and their independent
assessment, and expires again at continuation disposition or the fixed hard
limit. It does not change a numerical predicate, input, method, equation,
constant, tolerance, guard, counter limit, process limit, arm allowance, or
broader HOLD. This is consistent with the adopted continuation and does not
retrospectively convert the earlier disposition into completed acceptance.

The carried arithmetic is exact:
`140479.464696 - 135809.983578 = 4669.481118` seconds, and the
`2026-09-20T01:24:30Z` anchor plus that balance gives
`2026-09-20T02:42:19.481118Z`. The recorder changes only its expired deadline
literal to that same timestamp. It retains the 1200-second preservation/review
reserve, 120-second arm cap, atomic single-use claim, no-retry behavior,
resource checks, baseline-before-treatment admission, frozen-pair integrity,
eight-file/per-file/aggregate output controls, and durable refusal evidence.
The isolated continuation recorder receipt reports all five existing process
controls passing; its nonzero children are explicit synthetic cases and no Rust
evaluator ran.

I independently recomputed the live detached source snapshot as 749 entries,
SHA-256 `fae897fe90f6bbadcaf298607c9d23fd986cd778f9dd56195f7a5510cbd5c4be`,
with the complete entry map equal to `root-terminal-t.json`. The original trace
is still `527208c3e5ad07f1744f1bd92c9e4ac17cf7efd9469968a97d7964c5db482dfd`.
The retained and executable-copy binaries are byte-identical at 55,908,800
bytes, SHA-256 `e26cc780e7065211714a37653e55f9038cee3a5a32a6964de7122cd80cf0f272`;
the retained copy remains mode 0444. Custody rechecks all nineteen external
build inputs, five support links and the three recovery patches. The launch
copy's exact listing contains only
`grid40_experimental_tests::grid40_original_start_replay_is_explicit_and_single_arm_only`
for the requested selector; the bound arguments are `--ignored --exact
--test-threads=1` and the build receipt identifies default features.

The canonical run directory was absent at continuation custody inspection, so
no baseline or treatment claim existed. The final QA review independently maps
the reachable T writers to `trace.json`, `trace.partial`,
`trace.partial.next`, `sidecar.json`, and `events.jsonl`; together with stdout,
stderr, and `reserved-2`, these are the required eight child slots. Its static
inspection found no reachable child-process creation or other output
destination. This preserves the already accepted implementation/preparation GO
for unchanged T; no source-identical preparation campaign needs repetition.

## Residual risk and missing result evidence

The concrete `pair-freeze.json` is not part of this immutable continuity
verdict. Before baseline, it still must bind the exact source, launch binary,
input, build receipt/features, selector/arguments, recorder and snapshot tool,
nineteen external inputs, five links, exact environment/writer map, eight output
slots, completed reviews, and executable-copy custody. A bounded independent
inspection of that concrete object remains a launch prerequisite.

No baseline or treatment result exists at this review cut. Baseline still must
match all 95 ordinary records in schema, typed values, order and binary64 bits,
then pass independent sidecar/lifecycle/cost admission before treatment. The
first treatment endpoint still requires branch-aware row classification,
residual/step/domain/candidate assessment, exact lifecycle/counter
reconciliation, and independent primitive reconstruction. Potential requests
must not be reported as finalized fluxes, and represented-snow ground/soil
identity rows must not be reported as energy closure. Strict Clippy remains an
inherited-debt FAIL with matched no-new-diagnostic attribution; no lint PASS,
production qualification, root-existence result, reader/E008 result, or
conservation/restart qualification follows.

## Verdict

**GO for correctness continuity and the narrow lifecycle/recorder amendment on
unchanged T.** No blocking finding remains. This GO reuses the immutable
terminal implementation/preparation assessment and does not preapprove an
absent concrete freeze or any numerical result. Result assessment belongs in
the separately frozen `continuation-correctness-result-review.md`.
