--- /workdir/openwepp-experiments/b01-wb14-cadence/baseline-red/crates/openwepp-hillslope-orchestrator/src/lib.rs	2026-09-11 22:51:03.665813365 -0700
+++ crates/openwepp-hillslope-orchestrator/src/lib.rs	2026-09-13 15:33:37.477564591 -0700
@@ -311,3 +311,4 @@
 
 pub mod snow_accuracy_bulk;
 pub mod snow_accuracy_observer;
+mod snow_stage3_v11_current_context_capture;
--- /workdir/openwepp-experiments/b01-wb14-cadence/baseline-red/crates/openwepp-hillslope-orchestrator/src/snow_stage3_v11_terminal_execution.rs	2026-09-11 22:51:03.679698902 -0700
+++ crates/openwepp-hillslope-orchestrator/src/snow_stage3_v11_terminal_execution.rs	2026-09-13 15:50:54.106793049 -0700
@@ -125,12 +125,16 @@
     prior_support: TimeSupport,
     requested_support: TimeSupport,
     requested: &CoveredTerminalJointTrialStateV1,
-    lane_id: u32, ice: f64, liquid: f64, cold_content: f64,
+    lane_id: u32,
+    ice: f64,
+    liquid: f64,
+    cold_content: f64,
 ) -> Result<(), DirectSnowStage3EvaluationError> {
     let source = initial.authority();
     let authority = prior.authority();
     if authority.source_owner_set_sha256 != source.source_owner_set_sha256
-        || authority.lane_id != source.lane_id || lane_id != source.lane_id
+        || authority.lane_id != source.lane_id
+        || lane_id != source.lane_id
         || authority.source_snow_owner_sha256 != source.source_snow_owner_sha256
         || authority.interval_index != source.interval_index
         || authority.state_support != source.state_support
@@ -139,9 +143,11 @@
         || prior_support.end_ns() != requested_support.start_ns()
         || requested_support.start_ns() >= requested_support.end_ns()
         || requested_support.end_ns() > source.state_support.end_ns()
-        || prior.with_terminal_hydrology_state(lane_id, ice, liquid, cold_content)? != *requested {
+        || prior.with_terminal_hydrology_state(lane_id, ice, liquid, cold_content)? != *requested
+    {
         return Err(DirectSnowStage3EvaluationError::TerminalCustody(
-            "covered probe sealed hydrology successor"));
+            "covered probe sealed hydrology successor",
+        ));
     }
     Ok(())
 }
@@ -581,35 +587,52 @@
             if let Some(exact) = retained.get(&requested.receipt_sha256()) {
                 if exact.candidates().joint() != requested {
                     return Err(DirectSnowStage3EvaluationError::TerminalCustody(
-                        "covered probe exact joint equality"));
+                        "covered probe exact joint equality",
+                    ));
                 }
                 match exact {
-                    CoveredTerminalProviderRetentionV1::Initial(value) =>
-                        CoveredTerminalProviderRetentionV1::Initial(value.clone()),
-                    CoveredTerminalProviderRetentionV1::Phase(value) =>
-                        CoveredTerminalProviderRetentionV1::Phase(value.clone()),
+                    CoveredTerminalProviderRetentionV1::Initial(value) => {
+                        CoveredTerminalProviderRetentionV1::Initial(value.clone())
+                    }
+                    CoveredTerminalProviderRetentionV1::Phase(value) => {
+                        CoveredTerminalProviderRetentionV1::Phase(value.clone())
+                    }
                 }
             } else {
-                let predecessor = requested.authority().accepted_predecessors.last()
-                    .ok_or(DirectSnowStage3EvaluationError::TerminalCustody(
-                        "covered probe missing hydrology predecessor"))?;
+                let predecessor = requested.authority().accepted_predecessors.last().ok_or(
+                    DirectSnowStage3EvaluationError::TerminalCustody(
+                        "covered probe missing hydrology predecessor",
+                    ),
+                )?;
                 let phase = match retained.get(predecessor) {
                     Some(CoveredTerminalProviderRetentionV1::Phase(phase)) => phase,
-                    _ => return Err(DirectSnowStage3EvaluationError::TerminalCustody(
-                        "covered probe absent retained predecessor phase")),
+                    _ => {
+                        return Err(DirectSnowStage3EvaluationError::TerminalCustody(
+                            "covered probe absent retained predecessor phase",
+                        ));
+                    }
                 };
                 let prior = phase.ending_candidates.joint();
                 if prior.receipt_sha256() != *predecessor
                     || prior != &phase.transition.ending_joint
                     || phase.transition.boundary.support
-                        != phase.transition.probe_child_identity.trial_support {
+                        != phase.transition.probe_child_identity.trial_support
+                {
                     return Err(DirectSnowStage3EvaluationError::TerminalCustody(
-                        "covered probe retained predecessor phase identity"));
+                        "covered probe retained predecessor phase identity",
+                    ));
                 }
                 validate_terminal_hydrology_successor_v1(
-                    &initial_joint, prior, phase.transition.boundary.support,
-                    request.support, requested, request.lane_id,
-                    request.ice_kg_m2, request.liquid_kg_m2, request.cold_content_j_m2)?;
+                    &initial_joint,
+                    prior,
+                    phase.transition.boundary.support,
+                    request.support,
+                    requested,
+                    request.lane_id,
+                    request.ice_kg_m2,
+                    request.liquid_kg_m2,
+                    request.cold_content_j_m2,
+                )?;
                 CoveredTerminalProviderRetentionV1::Phase(phase.clone())
             }
         };
@@ -1225,7 +1248,8 @@
             "adaptive preterminal V2 accepted snow-owner digest",
         ),
     ];
-    let receiver_bridge = experimental_bulk_receiver_owner_bridge_v1(endpoint, next_consumer, receipt)?;
+    let receiver_bridge =
+        experimental_bulk_receiver_owner_bridge_v1(endpoint, next_consumer, receipt)?;
     for (owner_id, expected_digest, mismatch) in expected_owner_digests {
         let expected_digest = if let Some((before, after)) = receiver_bridge.get(owner_id) {
             if digest_bytes(before) != expected_digest {
@@ -2325,6 +2349,23 @@
         child_support_start_ns: support.start_ns().get() as u128,
         child_support_end_ns: support.end_ns().get() as u128,
     };
+    crate::snow_stage3_v11_current_context_capture::record_pre_child_context(
+        crate::snow_stage3_v11_current_context_capture::PreChildContext {
+            context,
+            parent: beginning_parent,
+            consumer: beginning_consumer,
+            clock: beginning_clock,
+            support,
+            reduction: &reduction,
+            ledger: &ledger,
+            receipt: &provisional_receipt,
+            binding: &provisional_binding,
+            forcing_receipt,
+            pending_terminal_parcels,
+            day: day_index,
+            interval: interval_index,
+        },
+    );
     let finalize_wb14_parent_interval =
         support.end_ns() == beginning_clock.parent_support().end_ns();
     let mut provisional_executor = prepare_provisional_covered_executor_v1(
@@ -2591,19 +2632,35 @@
     installed_soil
         .validate()
         .map_err(|_| DirectSnowStage3V11AttachmentError::Identity("installed soil owner bytes"))?;
-    let receipt_soil = if let Some(custody)=deferred_native_v2_soil_custody.as_ref() {
-        let beginning_resident=beginning_consumer.soil_thermal_resident();
-        let retained_resident=consumer.soil_thermal_resident();
-        if retained_resident!=beginning_resident || retained_resident.canonical_active_owner_bytes()
-            .map_err(|_|DirectSnowStage3V11AttachmentError::Identity("deferred retained soil canonical bytes"))?
-            !=beginning_resident.canonical_active_owner_bytes()
-            .map_err(|_|DirectSnowStage3V11AttachmentError::Identity("deferred beginning soil canonical bytes"))? {
-            return Err(DirectSnowStage3V11AttachmentError::Identity("deferred resolved soil retained resident changed"));
+    let receipt_soil = if let Some(custody) = deferred_native_v2_soil_custody.as_ref() {
+        let beginning_resident = beginning_consumer.soil_thermal_resident();
+        let retained_resident = consumer.soil_thermal_resident();
+        if retained_resident != beginning_resident
+            || retained_resident
+                .canonical_active_owner_bytes()
+                .map_err(|_| {
+                    DirectSnowStage3V11AttachmentError::Identity(
+                        "deferred retained soil canonical bytes",
+                    )
+                })?
+                != beginning_resident
+                    .canonical_active_owner_bytes()
+                    .map_err(|_| {
+                        DirectSnowStage3V11AttachmentError::Identity(
+                            "deferred beginning soil canonical bytes",
+                        )
+                    })?
+        {
+            return Err(DirectSnowStage3V11AttachmentError::Identity(
+                "deferred resolved soil retained resident changed",
+            ));
         }
         // try_new above authenticated candidate/continuation against this beginning.
         // Physical receipt identity and temperature concern that selected candidate.
         custody.candidate().read_view()
-    } else { installed_soil };
+    } else {
+        installed_soil
+    };
     for (lane_id, receipt) in &snow_soil_heat_receipts {
         let state =
             ending_stage3
@@ -2666,9 +2723,18 @@
         if receipt.soil_candidate_ending_identity_sha256 != receipt_soil_identity {
             if crate::snow_accuracy_observer::physical_global_enabled() {
                 let installed_soil_value = match soil_ofe {
-                    crate::v9_real_consumer_shadow::DirectSoilThermalOfeReadView::V1(value) => serde_json::to_value(value),
-                    crate::v9_real_consumer_shadow::DirectSoilThermalOfeReadView::V2(value) => serde_json::to_value(value),
-                }.map_err(|_| DirectSnowStage3V11AttachmentError::Identity("installed soil diagnostic serialization"))?;
+                    crate::v9_real_consumer_shadow::DirectSoilThermalOfeReadView::V1(value) => {
+                        serde_json::to_value(value)
+                    }
+                    crate::v9_real_consumer_shadow::DirectSoilThermalOfeReadView::V2(value) => {
+                        serde_json::to_value(value)
+                    }
+                }
+                .map_err(|_| {
+                    DirectSnowStage3V11AttachmentError::Identity(
+                        "installed soil diagnostic serialization",
+                    )
+                })?;
                 let deferred_soil = deferred_native_v2_soil_custody.as_ref().map(|custody| {
                     custody.candidate().read_view().ordered_ofes().into_iter().map(|ofe| match ofe {
                         crate::v9_real_consumer_shadow::DirectSoilThermalOfeReadView::V1(value) => serde_json::to_value(value),
@@ -2890,7 +2956,8 @@
     let terminal_lanes = terminal_events.keys().copied().collect::<BTreeSet<_>>();
     let native_receiver = consumer.frozen_litter_v3_resident().is_some()
         && consumer.frozen_litter_v4_resident().is_some()
-        && openwepp_land_surface_energy::snow_accuracy_policy::Policy::current().identity() == Some(14);
+        && openwepp_land_surface_energy::snow_accuracy_policy::Policy::current().identity()
+            == Some(14);
     let mut receiver_excluded_lanes = terminal_lanes.clone();
     let mut bulk_receiver_participates = false;
     if stage3_covered_native_inactive && !native_receiver {
@@ -2943,17 +3010,32 @@
         }
     }
     let post_support_native_custody = if native_receiver {
-        consume_native_positive_support_snow_liquid_v1(context, &mut parent_after_segment,
-            &mut consumer, &mut final_clock, support,
-            owner_join.ending_complete_owner_set_sha256, &terminal_lanes)?
-    } else { None };
-    let post_support_liquid_receiver = if native_receiver || (stage3_covered_native_inactive && !bulk_receiver_participates) {
-        None
+        consume_native_positive_support_snow_liquid_v1(
+            context,
+            &mut parent_after_segment,
+            &mut consumer,
+            &mut final_clock,
+            support,
+            owner_join.ending_complete_owner_set_sha256,
+            &terminal_lanes,
+        )?
     } else {
-        consume_positive_support_snow_liquid_v1(context, &mut parent_after_segment,
-            &mut consumer, &mut final_clock, support,
-            owner_join.ending_complete_owner_set_sha256, &receiver_excluded_lanes)?
+        None
     };
+    let post_support_liquid_receiver =
+        if native_receiver || (stage3_covered_native_inactive && !bulk_receiver_participates) {
+            None
+        } else {
+            consume_positive_support_snow_liquid_v1(
+                context,
+                &mut parent_after_segment,
+                &mut consumer,
+                &mut final_clock,
+                support,
+                owner_join.ending_complete_owner_set_sha256,
+                &receiver_excluded_lanes,
+            )?
+        };
     let wb14_replay_trial_sha256 = terminal_endpoints
         .and_then(|endpoints| endpoints.first())
         .map_or(final_receipt.slab_id().digest(), |endpoint| {
@@ -3020,14 +3102,23 @@
     }
     subslab_receipt.validate_support_liquid_custody_v2()?;
     if crate::snow_accuracy_observer::physical_enabled() {
-        let beginning_soil = beginning_deferred_native_v2_soil_custody
-            .map_or_else(|| beginning_consumer.soil_thermal_resident().read_view(), |value| value.candidate().read_view());
-        let ending_soil = deferred_native_v2_soil_custody.as_ref()
-            .map_or_else(|| consumer.soil_thermal_resident().read_view(), |value| value.candidate().read_view());
-        let soil_json = |view: crate::v9_real_consumer_shadow::DirectSoilThermalReadView<'_>| match view {
-            crate::v9_real_consumer_shadow::DirectSoilThermalReadView::V1(value) => serde_json::json!({"v1":value}),
-            crate::v9_real_consumer_shadow::DirectSoilThermalReadView::V2(value) => serde_json::json!({"v2":value}),
-        };
+        let beginning_soil = beginning_deferred_native_v2_soil_custody.map_or_else(
+            || beginning_consumer.soil_thermal_resident().read_view(),
+            |value| value.candidate().read_view(),
+        );
+        let ending_soil = deferred_native_v2_soil_custody.as_ref().map_or_else(
+            || consumer.soil_thermal_resident().read_view(),
+            |value| value.candidate().read_view(),
+        );
+        let soil_json =
+            |view: crate::v9_real_consumer_shadow::DirectSoilThermalReadView<'_>| match view {
+                crate::v9_real_consumer_shadow::DirectSoilThermalReadView::V1(value) => {
+                    serde_json::json!({"v1":value})
+                }
+                crate::v9_real_consumer_shadow::DirectSoilThermalReadView::V2(value) => {
+                    serde_json::json!({"v2":value})
+                }
+            };
         crate::snow_accuracy_observer::record(serde_json::json!({
             "kind":"candidate_subslab_primitives", "disposition":"candidate_until_committed_day_membership",
             "day_index":day_index,"interval_index":interval_index,
@@ -3105,41 +3196,173 @@
 mod sealed_hydrology_successor_tests {
     use super::*;
     fn support(a: u128, b: u128) -> TimeSupport {
-        TimeSupport::new(openwepp_coupled_time::ModelTimeNs::new(a),
-            openwepp_coupled_time::ModelTimeNs::new(b)).expect("positive support")
+        TimeSupport::new(
+            openwepp_coupled_time::ModelTimeNs::new(a),
+            openwepp_coupled_time::ModelTimeNs::new(b),
+        )
+        .expect("positive support")
     }
     #[test]
     fn sealed_successor_distinguishes_identical_non_snow_candidates_and_refuses_foreign_chain() {
-        let owners = ["vegetation", "snow", "land_surface_energy", "hydrology", "bgc",
-            "soil_thermal", "surface_liquid"].into_iter()
-            .map(|key| (key.to_owned(), key.as_bytes().to_vec())).collect();
-        let initial = CoveredTerminalJointTrialStateV1::try_new(JointTrialAuthorityV1 {
-            source_owner_set_sha256: Digest32::from_bytes([1;32]), lane_id: 1,
-            source_snow_owner_sha256: Digest32::from_bytes([2;32]), interval_index: 3,
-            state_support: support(0,1800), accepted_predecessors: Vec::new(),
-        }, owners).expect("initial joint");
-        let prior = initial.with_terminal_hydrology_state(1, 1.0, 0.1, 0.0).expect("prior");
-        let requested = prior.with_terminal_hydrology_state(1, 0.9, 0.15, 0.0).expect("requested");
-        let check = |source: &CoveredTerminalJointTrialStateV1, previous: &CoveredTerminalJointTrialStateV1,
-            previous_support, next_support, next: &CoveredTerminalJointTrialStateV1, lane, ice| {
-            validate_terminal_hydrology_successor_v1(source, previous, previous_support,
-                next_support, next, lane, ice, 0.15, 0.0)
+        let owners = [
+            "vegetation",
+            "snow",
+            "land_surface_energy",
+            "hydrology",
+            "bgc",
+            "soil_thermal",
+            "surface_liquid",
+        ]
+        .into_iter()
+        .map(|key| (key.to_owned(), key.as_bytes().to_vec()))
+        .collect();
+        let initial = CoveredTerminalJointTrialStateV1::try_new(
+            JointTrialAuthorityV1 {
+                source_owner_set_sha256: Digest32::from_bytes([1; 32]),
+                lane_id: 1,
+                source_snow_owner_sha256: Digest32::from_bytes([2; 32]),
+                interval_index: 3,
+                state_support: support(0, 1800),
+                accepted_predecessors: Vec::new(),
+            },
+            owners,
+        )
+        .expect("initial joint");
+        let prior = initial
+            .with_terminal_hydrology_state(1, 1.0, 0.1, 0.0)
+            .expect("prior");
+        let requested = prior
+            .with_terminal_hydrology_state(1, 0.9, 0.15, 0.0)
+            .expect("requested");
+        let check = |source: &CoveredTerminalJointTrialStateV1,
+                     previous: &CoveredTerminalJointTrialStateV1,
+                     previous_support,
+                     next_support,
+                     next: &CoveredTerminalJointTrialStateV1,
+                     lane,
+                     ice| {
+            validate_terminal_hydrology_successor_v1(
+                source,
+                previous,
+                previous_support,
+                next_support,
+                next,
+                lane,
+                ice,
+                0.15,
+                0.0,
+            )
         };
-        check(&initial,&prior,support(120,180),support(180,240),&requested,1,0.9).expect("exact successor");
-        for key in initial.owner_bytes().keys().filter(|key| key.as_str() != "snow") {
-            assert_eq!(initial.owner_bytes()[key],prior.owner_bytes()[key]);
-        }
-        assert!(check(&initial,&initial,support(120,180),support(180,240),&requested,1,0.9).is_err());
-        assert!(check(&initial,&prior,support(120,179),support(180,240),&requested,1,0.9).is_err());
-        assert!(check(&initial,&prior,support(120,180),support(180,1801),&requested,1,0.9).is_err());
-        assert!(check(&initial,&prior,support(120,180),support(180,240),&requested,2,0.9).is_err());
-        assert!(check(&initial,&prior,support(120,180),support(180,240),&requested,1,0.8).is_err());
+        check(
+            &initial,
+            &prior,
+            support(120, 180),
+            support(180, 240),
+            &requested,
+            1,
+            0.9,
+        )
+        .expect("exact successor");
+        for key in initial
+            .owner_bytes()
+            .keys()
+            .filter(|key| key.as_str() != "snow")
+        {
+            assert_eq!(initial.owner_bytes()[key], prior.owner_bytes()[key]);
+        }
+        assert!(
+            check(
+                &initial,
+                &initial,
+                support(120, 180),
+                support(180, 240),
+                &requested,
+                1,
+                0.9
+            )
+            .is_err()
+        );
+        assert!(
+            check(
+                &initial,
+                &prior,
+                support(120, 179),
+                support(180, 240),
+                &requested,
+                1,
+                0.9
+            )
+            .is_err()
+        );
+        assert!(
+            check(
+                &initial,
+                &prior,
+                support(120, 180),
+                support(180, 1801),
+                &requested,
+                1,
+                0.9
+            )
+            .is_err()
+        );
+        assert!(
+            check(
+                &initial,
+                &prior,
+                support(120, 180),
+                support(180, 240),
+                &requested,
+                2,
+                0.9
+            )
+            .is_err()
+        );
+        assert!(
+            check(
+                &initial,
+                &prior,
+                support(120, 180),
+                support(180, 240),
+                &requested,
+                1,
+                0.8
+            )
+            .is_err()
+        );
         let mut authority = prior.authority().clone();
         authority.accepted_predecessors.clear();
-        let resealed = CoveredTerminalJointTrialStateV1::try_new(authority, prior.owner_bytes().clone()).expect("resealed foreign prefix");
-        assert!(check(&initial,&resealed,support(120,180),support(180,240),&requested,1,0.9).is_err());
-        let mut authority = initial.authority().clone(); authority.interval_index += 1;
-        let foreign = CoveredTerminalJointTrialStateV1::try_new(authority,initial.owner_bytes().clone()).expect("foreign interval");
-        assert!(check(&foreign,&prior,support(120,180),support(180,240),&requested,1,0.9).is_err());
+        let resealed =
+            CoveredTerminalJointTrialStateV1::try_new(authority, prior.owner_bytes().clone())
+                .expect("resealed foreign prefix");
+        assert!(
+            check(
+                &initial,
+                &resealed,
+                support(120, 180),
+                support(180, 240),
+                &requested,
+                1,
+                0.9
+            )
+            .is_err()
+        );
+        let mut authority = initial.authority().clone();
+        authority.interval_index += 1;
+        let foreign =
+            CoveredTerminalJointTrialStateV1::try_new(authority, initial.owner_bytes().clone())
+                .expect("foreign interval");
+        assert!(
+            check(
+                &foreign,
+                &prior,
+                support(120, 180),
+                support(180, 240),
+                &requested,
+                1,
+                0.9
+            )
+            .is_err()
+        );
     }
 }
--- /dev/null	2026-08-16 18:42:56.296710845 -0700
+++ crates/openwepp-hillslope-orchestrator/src/snow_stage3_v11_current_context_capture.rs	2026-09-13 15:50:54.068791060 -0700
@@ -0,0 +1,346 @@
+//! B01 diagnostic pre-child capture. This module never affects a model result.
+
+use crate::{
+    snow_stage3_v11_attachment::DirectSnowStage3V11StaticContext,
+    v9_real_consumer_shadow::DirectV10RealConsumerShadow,
+};
+use openwepp_coupled_time::{
+    AcceptedSlabReceiptV1, ConstraintClass, ConstraintReductionReceiptV1, CoupledClockStateV1,
+    CoupledSlabCandidateV1, CoupledTimeRestartV2, DiagnosticReductionV1, LedgerEntryV1,
+    StepConstraintV1, TimeSupport, accept_slab, complete_owner_set_digest, digest_bytes,
+    reduce_constraints,
+};
+use openwepp_vegetation::V11ParentTransaction;
+use std::collections::BTreeMap;
+
+const CLOCK_MODEL_DOMAIN: &[u8] = b"OPENWEPP_STAGE3_V11_COUPLED_CLOCK_RESTART_MODEL_V1";
+const CLOCK_AUTHORITY_DOMAIN: &[u8] = b"SC-SNOWENERGY-001@22+SC-COUPLEDTIME-001";
+const CLOCK_REDUCTION_ID: &str = "stage3-v11-restart-no-publication";
+const B01_DAY_INDEX: usize = 4;
+const B01_INTERVAL_INDEX: usize = 22;
+const B01_CHILD_START_NS: u128 = 385_920_000_000_000;
+const B01_CHILD_END_NS: u128 = 385_980_000_000_000;
+
+fn is_target(day: usize, interval: usize, support: openwepp_coupled_time::TimeSupport) -> bool {
+    day == B01_DAY_INDEX
+        && interval == B01_INTERVAL_INDEX
+        && support.start_ns().get() == B01_CHILD_START_NS
+        && support.end_ns().get() == B01_CHILD_END_NS
+}
+
+pub(crate) struct PreChildContext<'a> {
+    pub context: &'a DirectSnowStage3V11StaticContext,
+    pub parent: &'a V11ParentTransaction,
+    pub consumer: &'a DirectV10RealConsumerShadow,
+    pub clock: &'a CoupledClockStateV1,
+    pub support: TimeSupport,
+    pub reduction: &'a ConstraintReductionReceiptV1,
+    pub ledger: &'a LedgerEntryV1,
+    pub receipt: &'a AcceptedSlabReceiptV1,
+    pub binding: &'a crate::direct_runtime::DirectWb14CoupledChildBindingV1,
+    pub pending_terminal_parcels:
+        &'a BTreeMap<openwepp_coupled_time::Digest32, crate::DirectSnowStage3V11TerminalParcel>,
+    pub forcing_receipt: openwepp_coupled_time::Digest32,
+    pub day: usize,
+    pub interval: usize,
+}
+
+fn capture_row(input: &PreChildContext<'_>) -> Result<serde_json::Value, String> {
+    let PreChildContext {
+        context,
+        parent,
+        consumer,
+        clock,
+        support,
+        reduction,
+        ledger,
+        receipt,
+        binding,
+        pending_terminal_parcels,
+        forcing_receipt,
+        day,
+        interval,
+    } = input;
+    let restart = CoupledTimeRestartV2::new(
+        digest_bytes(CLOCK_MODEL_DOMAIN),
+        digest_bytes(CLOCK_AUTHORITY_DOMAIN),
+        (*clock).clone(),
+        DiagnosticReductionV1::new(CLOCK_REDUCTION_ID.into(), "dimensionless".into())
+            .map_err(|e| e.to_string())?,
+        None,
+        Vec::new(),
+    )
+    .map_err(|e| e.to_string())?
+    .to_canonical_json()
+    .map_err(|e| e.to_string())?;
+    let owners = consumer
+        .canonical_v11_parent_owner_state_bytes()
+        .map_err(|e| e.to_string())?;
+    let clock_owners = clock
+        .owners()
+        .iter()
+        .map(|owner| (owner.owner_id().to_owned(), owner.state_bytes().to_vec()))
+        .collect::<BTreeMap<_, _>>();
+    let parent_checkpoint = serde_json::to_vec(&parent.checkpoint()).map_err(|e| e.to_string())?;
+    let receipt = serde_json::to_vec(receipt).map_err(|e| e.to_string())?;
+    let reduction = serde_json::to_vec(reduction).map_err(|e| e.to_string())?;
+    let ledger = serde_json::to_vec(ledger).map_err(|e| e.to_string())?;
+    let provider = serde_json::to_vec(consumer.provider_cursor()).map_err(|e| e.to_string())?;
+    let vegetation =
+        serde_json::to_vec(&context.vegetation_configuration).map_err(|e| e.to_string())?;
+    let lse = serde_json::to_vec(consumer.lse_configuration()).map_err(|e| e.to_string())?;
+    let terminal_custody =
+        serde_json::to_vec(pending_terminal_parcels).map_err(|e| e.to_string())?;
+    Ok(serde_json::json!({
+        "kind":"b01_wb14_pre_child_current_context_v1", "day_index":day, "interval_index":interval,
+        "support":support, "parent_support":clock.parent_support(), "accepted_until":clock.accepted_until(),
+        "parent_checkpoint_canonical_json":parent_checkpoint, "complete_owner_canonical_bytes":owners,
+        "clock_complete_owner_canonical_bytes":clock_owners,
+        "coupled_clock_restart_canonical_json":restart, "coupled_clock_restart_model_sha256":digest_bytes(CLOCK_MODEL_DOMAIN), "coupled_clock_restart_authority_sha256":digest_bytes(CLOCK_AUTHORITY_DOMAIN), "coupled_clock_restart_policy_sha256":context.controller_policy,
+        "vegetation_configuration_canonical_json":vegetation, "lse_configuration_canonical_json":lse,
+        "constraint_reduction_canonical_json":reduction, "complete_owner_ledger_canonical_json":ledger, "provisional_receipt_canonical_json":receipt,
+        "provisional_constructor_inputs":{"constraint_source_owner_id":"v11-snow-covered-real-consumer","constraint_class":"hard_boundary","forcing_receipt":forcing_receipt,"calendar_receipt":context.calendar_receipt,"controller_policy":context.controller_policy,"ledger_flux_id":"complete-owner-custody","ledger_units":"canonical-owner-state","ledger_child_support_start_ns":support.start_ns().get(),"ledger_child_support_end_ns":support.end_ns().get()},
+        "coupled_binding":{"proposed_upper_bound_s_bits":binding.proposed_upper_bound_s_bits,"coupled_parent_transaction_sha256":binding.coupled_parent_transaction_sha256,"accepted_slab_sha256":binding.accepted_slab_sha256,"parent_beginning_complete_owner_set_sha256":binding.parent_beginning_complete_owner_set_sha256,"parent_support_start_ns":binding.parent_support_start_ns,"parent_support_end_ns":binding.parent_support_end_ns,"child_support_start_ns":binding.child_support_start_ns,"child_support_end_ns":binding.child_support_end_ns},
+        "provider_cursor_canonical_json":provider, "terminal_parcels_produced_unconsumed_canonical_json":terminal_custody, "static_run_identity":context.run_identity, "static_topology_identity":context.topology_identity,
+        "capture_posture":"before physical ingress; diagnostic no-publication wrapper"
+    }))
+}
+
+fn capture(input: &PreChildContext<'_>) -> Result<(), String> {
+    crate::snow_accuracy_observer::record(capture_row(input)?);
+    Ok(())
+}
+
+#[cfg(test)]
+pub(crate) fn capture_row_for_test(
+    input: &PreChildContext<'_>,
+) -> Result<serde_json::Value, String> {
+    capture_row(input)
+}
+
+pub(crate) fn record_pre_child_context(input: PreChildContext<'_>) {
+    if crate::snow_accuracy_observer::physical_global_enabled()
+        && is_target(input.day, input.interval, input.support)
+    {
+        if let Err(detail) = capture(&input) {
+            crate::snow_accuracy_observer::record(
+                serde_json::json!({"kind":"b01_wb14_pre_child_current_context_capture_error_v1","detail":detail,"day_index":input.day,"interval_index":input.interval,"support":input.support}),
+            );
+        }
+    }
+}
+
+pub(crate) fn restore_coupled_clock(
+    bytes: &[u8],
+    model: openwepp_coupled_time::Digest32,
+    authority: openwepp_coupled_time::Digest32,
+    policy: openwepp_coupled_time::Digest32,
+) -> Result<CoupledTimeRestartV2, String> {
+    CoupledTimeRestartV2::from_canonical_json(bytes, model, authority, policy)
+        .map_err(|e| e.to_string())
+}
+
+/// Restores the enclosing parent through its existing configured BGC scope.
+/// The configuration is supplied independently by the real runner fixture.
+pub(crate) fn restore_parent_with_configured_bgc_scope(
+    bytes: &[u8],
+    vegetation: &openwepp_vegetation::VegetationConfigurationV11,
+    lse: &openwepp_land_surface_energy::LandSurfaceEnergyConfiguration,
+) -> Result<V11ParentTransaction, String> {
+    let checkpoint: openwepp_vegetation::V11ParentTransactionCheckpoint =
+        serde_json::from_slice(bytes).map_err(|error| error.to_string())?;
+    let scope = crate::v9_real_consumer_shadow::restart_authority_v11_bgc_debit_scope_v1(
+        &vegetation.imported_v10,
+        lse,
+    )
+    .map_err(|error| error.to_string())?;
+    V11ParentTransaction::restore_with_bgc_scope(vegetation, checkpoint, Some(&scope))
+        .map_err(|error| error.to_string())
+}
+
+/// Replays the existing provisional constructor on a clone only. Callers retain
+/// the beginning clock and can compare it byte-for-byte after this returns.
+pub(crate) fn reconstruct_provisional_on_clone(
+    clock: &CoupledClockStateV1,
+    support: TimeSupport,
+    context: &DirectSnowStage3V11StaticContext,
+    forcing_receipt: openwepp_coupled_time::Digest32,
+) -> Result<
+    (
+        CoupledClockStateV1,
+        ConstraintReductionReceiptV1,
+        LedgerEntryV1,
+        AcceptedSlabReceiptV1,
+    ),
+    String,
+> {
+    let parent = clock.parent_transaction_id();
+    let constraint = StepConstraintV1::new(
+        parent,
+        support.start_ns(),
+        support.end_ns(),
+        "v11-snow-covered-real-consumer".into(),
+        ConstraintClass::HardBoundary,
+        context.controller_policy,
+        context.calendar_receipt,
+        forcing_receipt,
+    )
+    .map_err(|e| e.to_string())?;
+    let reduction = reduce_constraints(
+        &[constraint],
+        parent,
+        support.start_ns(),
+        support.end_ns(),
+        None,
+    )
+    .map_err(|e| e.to_string())?;
+    let owner_digest = complete_owner_set_digest(clock.owners()).map_err(|e| e.to_string())?;
+    let mut preimage = Vec::new();
+    preimage.extend_from_slice(parent.digest().as_bytes());
+    preimage.extend_from_slice(&support.start_ns().get().to_be_bytes());
+    preimage.extend_from_slice(&support.end_ns().get().to_be_bytes());
+    let ledger = LedgerEntryV1::new(
+        "complete-owner-custody".into(),
+        "canonical-owner-state".into(),
+        owner_digest,
+        owner_digest,
+        digest_bytes(&preimage),
+    )
+    .map_err(|e| e.to_string())?;
+    let candidate = CoupledSlabCandidateV1::new(
+        clock,
+        clock.active_segment_id(),
+        support,
+        &reduction,
+        clock.owners().to_vec(),
+        vec![ledger],
+    )
+    .map_err(|e| e.to_string())?;
+    let mut cloned = clock.clone();
+    let receipt = accept_slab(&mut cloned, candidate).map_err(|e| e.to_string())?;
+    Ok((cloned, reduction, ledger, receipt))
+}
+
+/// Consume the captured diagnostic row in a fresh process. It performs only
+/// canonical restoration and provisional construction; no consumer, ingress,
+/// WB14, or constitutive operation is called here.
+pub(crate) fn import_and_reconstruct_pre_child_context(
+    row: &serde_json::Value,
+    context: &DirectSnowStage3V11StaticContext,
+) -> Result<(), String> {
+    let get = |name: &str| {
+        row.get(name)
+            .ok_or_else(|| format!("capture missing {name}"))
+    };
+    let clock_bytes: Vec<u8> =
+        serde_json::from_value(get("coupled_clock_restart_canonical_json")?.clone())
+            .map_err(|e| e.to_string())?;
+    let model: openwepp_coupled_time::Digest32 =
+        serde_json::from_value(get("coupled_clock_restart_model_sha256")?.clone())
+            .map_err(|e| e.to_string())?;
+    let authority: openwepp_coupled_time::Digest32 =
+        serde_json::from_value(get("coupled_clock_restart_authority_sha256")?.clone())
+            .map_err(|e| e.to_string())?;
+    let policy: openwepp_coupled_time::Digest32 =
+        serde_json::from_value(get("coupled_clock_restart_policy_sha256")?.clone())
+            .map_err(|e| e.to_string())?;
+    if model != digest_bytes(CLOCK_MODEL_DOMAIN)
+        || authority != digest_bytes(CLOCK_AUTHORITY_DOMAIN)
+        || policy != context.controller_policy
+    {
+        return Err("captured clock restart authority join".into());
+    }
+    let restored = restore_coupled_clock(&clock_bytes, model, authority, policy)?;
+    let beginning_clock = restored.clock().clone();
+    if restored.to_canonical_json().map_err(|e| e.to_string())? != clock_bytes {
+        return Err("captured clock canonical round trip".into());
+    }
+    let parent_bytes: Vec<u8> =
+        serde_json::from_value(get("parent_checkpoint_canonical_json")?.clone())
+            .map_err(|e| e.to_string())?;
+    let vegetation_bytes: Vec<u8> =
+        serde_json::from_value(get("vegetation_configuration_canonical_json")?.clone())
+            .map_err(|e| e.to_string())?;
+    let vegetation = serde_json::from_slice(&vegetation_bytes).map_err(|e| e.to_string())?;
+    if vegetation != context.vegetation_configuration {
+        return Err("captured V11 vegetation configuration/context join".into());
+    }
+    let lse_bytes: Vec<u8> =
+        serde_json::from_value(get("lse_configuration_canonical_json")?.clone())
+            .map_err(|e| e.to_string())?;
+    let lse = serde_json::from_slice(&lse_bytes).map_err(|e| e.to_string())?;
+    let parent = restore_parent_with_configured_bgc_scope(&parent_bytes, &vegetation, &lse)?;
+    if parent.parent_transaction_id() != beginning_clock.parent_transaction_id() {
+        return Err("restored parent/clock identity".into());
+    }
+    let support: TimeSupport =
+        serde_json::from_value(get("support")?.clone()).map_err(|e| e.to_string())?;
+    let constructor = get("provisional_constructor_inputs")?;
+    if constructor.get("constraint_source_owner_id")
+        != Some(&serde_json::json!("v11-snow-covered-real-consumer"))
+        || constructor.get("constraint_class") != Some(&serde_json::json!("hard_boundary"))
+        || constructor.get("calendar_receipt") != Some(&serde_json::json!(context.calendar_receipt))
+        || constructor.get("controller_policy")
+            != Some(&serde_json::json!(context.controller_policy))
+        || constructor.get("ledger_child_support_start_ns")
+            != Some(&serde_json::json!(support.start_ns().get()))
+        || constructor.get("ledger_child_support_end_ns")
+            != Some(&serde_json::json!(support.end_ns().get()))
+    {
+        return Err("captured provisional constructor operands".into());
+    }
+    let forcing = serde_json::from_value(
+        constructor
+            .get("forcing_receipt")
+            .ok_or("capture missing forcing receipt")?
+            .clone(),
+    )
+    .map_err(|e| e.to_string())?;
+    let before = restored.to_canonical_json().map_err(|e| e.to_string())?;
+    let (_, reduction, ledger, receipt) =
+        reconstruct_provisional_on_clone(&beginning_clock, support, context, forcing)?;
+    if restored.to_canonical_json().map_err(|e| e.to_string())? != before {
+        return Err("provisional clone mutated restored beginning clock".into());
+    }
+    let expected: Vec<u8> =
+        serde_json::from_value(get("provisional_receipt_canonical_json")?.clone())
+            .map_err(|e| e.to_string())?;
+    let expected_reduction: Vec<u8> =
+        serde_json::from_value(get("constraint_reduction_canonical_json")?.clone())
+            .map_err(|e| e.to_string())?;
+    if serde_json::to_vec(&reduction).map_err(|e| e.to_string())? != expected_reduction {
+        return Err("constraint reduction durable operands".into());
+    }
+    let expected_ledger: Vec<u8> =
+        serde_json::from_value(get("complete_owner_ledger_canonical_json")?.clone())
+            .map_err(|e| e.to_string())?;
+    if serde_json::to_vec(&ledger).map_err(|e| e.to_string())? != expected_ledger {
+        return Err("complete owner ledger durable operands".into());
+    }
+    if serde_json::to_vec(&receipt).map_err(|e| e.to_string())? != expected {
+        return Err("provisional receipt durable coordinates".into());
+    }
+    let expected_clock_owners: BTreeMap<String, Vec<u8>> =
+        serde_json::from_value(get("clock_complete_owner_canonical_bytes")?.clone())
+            .map_err(|e| e.to_string())?;
+    let restored_clock_owners = beginning_clock
+        .owners()
+        .iter()
+        .map(|owner| (owner.owner_id().to_owned(), owner.state_bytes().to_vec()))
+        .collect::<BTreeMap<_, _>>();
+    if restored_clock_owners != expected_clock_owners {
+        return Err("clock complete-owner canonical bytes".into());
+    }
+    let consumer_owners: BTreeMap<String, Vec<u8>> =
+        serde_json::from_value(get("complete_owner_canonical_bytes")?.clone())
+            .map_err(|e| e.to_string())?;
+    if consumer_owners.is_empty()
+        || consumer_owners
+            .keys()
+            .any(|owner| !restored_clock_owners.contains_key(owner))
+        || consumer_owners.values().any(Vec::is_empty)
+    {
+        return Err("consumer complete-owner canonical byte manifest".into());
+    }
+    Ok(())
+}
--- /workdir/openwepp-experiments/b01-wb14-cadence/baseline-red/crates/openwepp-hillslope-orchestrator/src/v9_real_consumer_shadow_tests.rs	2026-09-06 19:55:50.000000000 -0700
+++ crates/openwepp-hillslope-orchestrator/src/v9_real_consumer_shadow_tests.rs	2026-09-13 15:50:54.129794252 -0700
@@ -990,6 +990,124 @@
             .collect()
     }
 
+    #[test]
+    fn b01_current_context_importer_restores_fixture_without_physics() {
+        let (shadow, fixture) = v10_shadow_fixture();
+        let migrated =
+            migrate_v10_runtime_to_v11(&shadow.vegetation_configuration, &shadow.vegetation_state)
+                .expect("V11 migration");
+        let owners = initial_v11_owners(&shadow, &migrated.state);
+        let clock_owners = owners
+            .values()
+            .map(|owner| owner.to_owner_state().expect("clock owner"))
+            .collect::<Vec<_>>();
+        let support = TimeSupport::new(ModelTimeNs::new(0), ModelTimeNs::new(60_000_000_000))
+            .expect("capture support");
+        let owner_digest = complete_owner_set_digest(&clock_owners).expect("owner digest");
+        let authority =
+            ParentAuthorityV1::new(digest(1), digest(2), digest(3), 40, support, owner_digest)
+                .expect("parent authority");
+        let participants = clock_owners
+            .iter()
+            .map(|owner| owner.owner_id().to_owned())
+            .collect::<Vec<_>>();
+        let clock = CoupledClockStateV1::new(
+            authority,
+            clock_owners,
+            "snow-stage3-v11".to_owned(),
+            participants,
+            digest(4),
+            Vec::new(),
+        )
+        .expect("beginning clock");
+        let parent = V11ParentTransaction::new_with_complete_owners(
+            &migrated.configuration,
+            &migrated.state,
+            clock.parent_transaction_id(),
+            ModelTimeNs::new(0),
+            owners,
+        )
+        .expect("V11 parent");
+        let context = DirectSnowStage3V11StaticContext {
+            run_identity: digest(1),
+            topology_identity: digest(9),
+            parent_duration_ns: support.duration_ns(),
+            minimum_support_ns: support.duration_ns(),
+            calendar_receipt: digest(2),
+            controller_policy: digest(4),
+            parent_sequence: 40,
+            lane_ids: vec![1],
+            vegetation_configuration: migrated.configuration,
+            surface_liquid_configuration: shadow.inner.surface_configuration.clone(),
+            wb14_parameters: fixture
+                .lse_configuration
+                .ofes
+                .iter()
+                .map(|ofe| crate::DirectOfeWb14Parameters {
+                    ofe_id: ofe.ofe_id.clone(),
+                    effective_conductivity_m_s: 1.0e-6,
+                    matric_potential_m: 0.1,
+                    infiltration_storage_capacity_m: 0.04,
+                })
+                .collect(),
+        };
+        let (_, reduction, ledger, receipt) =
+            crate::snow_stage3_v11_current_context_capture::reconstruct_provisional_on_clone(
+                &clock,
+                support,
+                &context,
+                digest(3),
+            )
+            .expect("provisional construction");
+        let binding = crate::direct_runtime::DirectWb14CoupledChildBindingV1 {
+            proposed_upper_bound_s_bits: 60.0_f64.to_bits(),
+            coupled_parent_transaction_sha256: *clock.parent_transaction_id().digest().as_bytes(),
+            accepted_slab_sha256: *receipt.slab_id().digest().as_bytes(),
+            parent_beginning_complete_owner_set_sha256: *owner_digest.as_bytes(),
+            parent_support_start_ns: support.start_ns().get(),
+            parent_support_end_ns: support.end_ns().get(),
+            child_support_start_ns: support.start_ns().get(),
+            child_support_end_ns: support.end_ns().get(),
+        };
+        let pending_terminal_parcels = BTreeMap::new();
+        let row = crate::snow_stage3_v11_current_context_capture::capture_row_for_test(
+            &crate::snow_stage3_v11_current_context_capture::PreChildContext {
+                context: &context,
+                parent: &parent,
+                consumer: &shadow,
+                clock: &clock,
+                support,
+                reduction: &reduction,
+                ledger: &ledger,
+                receipt: &receipt,
+                binding: &binding,
+                pending_terminal_parcels: &pending_terminal_parcels,
+                forcing_receipt: digest(3),
+                day: 0,
+                interval: 0,
+            },
+        )
+        .expect("capture row");
+        let _audit_guard =
+            crate::v9_real_consumer_shadow::begin_covered_native_physical_path_audit_v1();
+        crate::snow_stage3_v11_current_context_capture::import_and_reconstruct_pre_child_context(
+            &row, &context,
+        )
+        .expect("composed context import");
+        let audit = crate::v9_real_consumer_shadow::take_covered_native_physical_path_audit_v1();
+        assert_eq!(audit.snow_free_litter_physics_call_count, 0);
+        assert_eq!(audit.snow_free_surface_physics_call_count, 0);
+        assert_eq!(audit.snow_free_wb14_physics_call_count, 0);
+        let mut missing_lse = row;
+        missing_lse
+            .as_object_mut()
+            .expect("capture object")
+            .remove("lse_configuration_canonical_json");
+        assert!(crate::snow_stage3_v11_current_context_capture::
+            import_and_reconstruct_pre_child_context(&missing_lse, &context)
+            .is_err());
+    }
+
     fn segment_interval(
         base: &DirectV9ShadowIntervalInput,
         duration_ns: u128,
