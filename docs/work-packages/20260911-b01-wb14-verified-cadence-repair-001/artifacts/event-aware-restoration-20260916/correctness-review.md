# Event-aware restoration correctness review — terminal status

**Current disposition:** Terminal same-reviewer review is complete at source tree `7c04f52e6cae6327202591b37f0c4e7a5fff3ae34a103255a35c49a3dbdf37ab`. All eleven terminal focused tests passed. The file-only diagnostic reaches the native constructor and reports the exact immutable position, provider, and soil-support refusals with zero physics; overall restoration/admission remains **HOLD**. See [Terminal same-reviewer disposition](#terminal-same-reviewer-disposition--tree-7c04f52e6cae6327202591b37f0c4e7a5fff3ae34a103255a35c49a3dbdf37ab) for A-001–A-005 findings and gate limits. The earlier provisional review is retained below as historical evidence.

# Event-aware restoration correctness review — provisional A-002 cut

Reviewer: `/root/event_correctness`, independent of the writer and adviser. Scope: retained reader at `/workdir/openwepp-experiments/b01-wb14-cadence/native-context-restoration-reader-20260916`, green snapshot tree `40ad40854db26f4a9dd9215b379628de968ceb730fd70f9cf98e61bb15ab3342`, patch `6f81b1f3606720caa932ca0c17466c436a1258b79fa2220de0716d76dc60179e`, as identified by `a002-green-source.json`. This is a provisional review while native composition continues, not terminal acceptance.

Evidence class: **Static and inspected Ran evidence.** I read the retained restorer/test source, SC-COUPLEDTIME-001 §§6/8/9, SC-SURFACELIQUID-001 INV-035/C-025, the prior A-001–A-005 review, and the A-002 red/green receipt and green run record. I ran no build, test, model, archive, or physics command. The writer's green direct-restorer command passed 1/1 in 39.15 seconds; the earlier red binary refused at the named archive chronology predicate. The red source snapshot was not retained before the edit, so its exact source-to-binary provenance remains a stated custody gap, although the result and binary are preserved.

## Findings, ordered by severity

**A-002 — High, direct restorer validation is incomplete.** `v9_real_consumer_shadow_publication_retention.rs:2117–2154` invokes `restart_authority_restore_archived_publication_day_v1` using only `rows/27431.canonical_record.bin` (day 0), which the previous source-bound review reports has one terminal event. The variable `multiple` does not establish a multiple-event wire. Its assertion compares `supports[0]` with the restored endpoint, rather than the last support. Direct semantic negatives cover omission, duplicate append, and wrong final owner only, with generic `is_err()` assertions. The zero/one/multiple-event positives and ordinal/tick/foreign-parent/order negatives at lines 2249–2300 exercise a separate chronology helper, not this archive restorer. The adopted A-002 acceptance requires discriminating direct restorer positives for no terminal event, one terminal event, and multiple same-tick terminal events, and direct wrong-tick, foreign-parent, wrong-beginning, reordered, omitted, duplicated, and wrong-final-owner semantic negatives. Show the reached typed error/message and relevant precedence, not parser failure. Add noncanonical serialization/schema/count controls to prove unchanged guards. Until then, the 1/1 green run supports the narrow day-0 endpoint correction, not full A-002 closure.

**A-002 — Medium, red source provenance is incomplete.** `a002-red-green-receipt.md` identifies a frozen red binary and exact targeted refusal, but `a002-green-source.json` says the red source snapshot was not retained before the predicate edit. The red test result is real inspected execution evidence, but a fully reproducible pre-change source-bound red claim needs explicit qualification or a reconstructed source identity clearly labeled as reconstruction. Do not silently attribute the red binary to the green source tree.

**A-001/A-003 — High, pending final composition.** The previous correctness review's native position/phase/provider and archive beginning/root findings remain open until the stable composed source and file-backed execution are inspected. `snow_stage3_v11_current_context_capture.rs:3001–3017` currently lacks a parent-receipt beginning-owner join. The existing manifest `append` validates order, previous/resulting roots, and adjacent owner continuity, but final member-backed execution must reach it and join terminal roots/counts to the prefix. `v9_real_consumer_shadow.rs:845` still installs captured accepted interval count after native construction; it needs independent day-4/interval-22/position-214, provider/GSI, and Stage3 position evidence. No verdict yet on writer changes in progress.

**A-004/A-005 — Medium, pending executed evidence.** Current successful component counters do not cover separate archive/native success or refusal paths. Require current-source fresh-process counters for actual physics and publication call paths on both outcomes, plus semantic poisons for prefix/event/proof, provider position, deferred/current custody, parcel posture, native owner projection, archive beginning, and ordered roots. A parse refusal cannot stand in for a downstream semantic guard.

## Residual risk and required gates

The corrected predicate at `v9_real_consumer_shadow_publication_retention.rs:1801–1811` retains `replace` and full-history scan, so the removed last-support equality is consistent with SC-COUPLEDTIME-001 §6 terminal zero-duration events. It still needs the direct coverage above, canonical serialization/error-precedence checks, four unchanged-archive re-executions, and full composed evidence before A-002 is accepted. Preserve all four earlier refusals as historical FAILs.

The testing strategy §8 classifies a semantic chronology/restart acceptance change as Critical and requires immediate campaign-strength full correctness regression (`cargo nextest run --workspace --profile full` in the local CI guide). The §6.2 bounded inherited-lint exception does not apply to Critical changes. The owner-authorized complete-target matched-lint method is diagnostic quality evidence, not a strict lint PASS. If the present run allowance prevents the full correctness gate, record it as NOT RUN/BLOCKED for acceptance while continuing the authorized file-only diagnostic. This does not block reporting a precisely attributed checkpoint refusal. The original scientific regression, E008, conservation/restart qualification, and production admission remain separate HOLD/FAIL obligations.

Verdict for this provisional cut: **HOLD — no A-002 or overall checkpoint approval yet.** Reinspect the stable writer cut and the actual executed evidence; verify accepted fixes in this same review.

## Interim composed-reader inspection, 2026-09-16 (still provisional)

Evidence class: **Static and inspected Ran evidence.** This addendum inspected `snow_stage3_v11_member_restoration.rs` at SHA-256 `33d14a70f88f67b4746b57f47f8ed4168beefa464fb6596177e5f65084e6d866` and the parent's second composed-run receipt; I ran no build, test, or model command. The source is under active edit, so these findings require same-reviewer verification against its final hash.

**A-001 — High, actual native position remains an attributed refusal.** In `snow_stage3_v11_member_restoration.rs:177–183`, the reader requires native `accepted_interval_count == prefix.next_parent_sequence + B01_INTERVAL_INDEX`, which is 214 for this record; the immutable native member carries 0, with nested `next_day_index` also 4. The second composed run authenticated all four archives and the manifest, joined the prefix, and decoded the actual 457,913,228-byte native member, then refused at `native accepted position/provider GSI join` before invoking the canonical native constructor. The observed 0/1 outcome is a reader position refusal; it cannot be called a constructor refusal or checkpoint restoration. Preserve the original native fields and return an error on mismatch. A private diagnostic may independently observe the canonical constructor result on those unchanged fields, provided both statuses are reported and any reader mismatch still makes the aggregate fail. Report separate position and provider/GSI operands rather than the combined label. The inspected run reported zero physics, mutation, and publication calls.

**A-003 — High, archive parent-beginning cross-join still missing.** The archive loop at `snow_stage3_v11_member_restoration.rs:115–138` authenticates each canonical day and ordered manifest, and checks publication prefix/root continuity, but does not independently bind the parent receipt's first positive support beginning owner to the already-restored publication's first positive support. The source operands are `parent_receipt.coupled_subslabs[*].owner_join.beginning_complete_owner_set_sha256` and `parent_receipt.snow_free_successor_receipts[*].beginning_complete_owner_set_sha256`, each with its exact `support`. Select the unique earliest positive support across both lists and compare both support and beginning digest to a representation-only projection from canonically restored publication supports. Keep the existing archive entry beginning-to-publication beginning check: day 0 has an initial tick-0 event, so its first positive beginning is **not** the pre-event entry beginning. Days 1–3 have later events and their first positive beginning equals entry beginning. This cross-join is a check of existing evidence, not a new chronology acceptance predicate.

**A-001 — High, deferred sequential custody not yet established.** `snow_stage3_v11_member_restoration.rs:302–323` authenticates the unpublished trial seal, candidate/physical-trial equality, original/current resident, final credit-chain tail, and flattened canonical operands. It does not bind each ordered credit layer group to consecutive owned-state beginnings/endings, predecessor transaction IDs, and the final trial beginning/end. This is a sequential 12-child unpublished trial: actual trial beginning digest `c268e49d…` differs legitimately from original/current state digest `13c7d75f…`, and its accepted predecessor is null while unpublished predecessor is populated. Direct trial-beginning-to-original equality or accepted-predecessor-to-resident receipt-chain equality would reject valid custody. Reconstruct byte-level owned-state projections from the original through the ordered credit groups, with canonical reseal and no physical recalculation; verify each transaction/predecessor/support step and terminal trial fields. Derive expected transaction succession from the original transaction and chain index rather than fixed record IDs.

**A-001 — Medium, duplicated private digest representations require parity evidence.** `snow_stage3_v11_member_restoration.rs:39–54,246–301` mirrors persisted hydrology operand-digest and land-surface-energy unpublished-trial/exponent serialization logic because the canonical APIs do not expose a no-physics restore. This is a material maintenance risk: a later canonical representation change could make this diagnostic silently diverge. The private/no-public-API constraint explains the duplication, but acceptance needs exact known-good seal parity and semantic poison checks for changed exponent, predecessor variant, transaction, and credit ordering against the canonical producer, with source provenance recorded. Prefer centralization if a permitted existing API becomes available.

**Residual:** The current `validate_current_phase` path at lines 206–243 is unreachable on the immutable native count 0 because the earlier position guard returns. Any new diagnostic aggregation must keep the overall fail-closed result and explicitly identify which downstream checks actually ran. A002 expanded tests, A003 parent-beginning fix, deferred-chain fix, A004/A005 coverage, Critical full correctness, and strict lint still need final-source and executed-evidence review. No terminal approval follows from this inspection.

## Composed-fourth soil refusal attribution (interim)

Evidence class: **Static and inspected Ran evidence.** I read `composed-fourth.{json,stdout,stderr}`, the source-defined canonical constructor and admission predicate, and the immutable member files named by the export events. I ran no model or Rust test. The run receipt binds frozen binary SHA-256 `c1ec55817463b86c927a2475f9d6e6bedff3c788c9cd254cfeeb722333ca0d55` to source tree `fec21c099387c159ac11326f2bdb70b0ef0434134d36268d95a2c78c1f830eeb`; it records exit 101, source/binary/inputs unchanged, and a 0/1 targeted result. The test reached the canonical native constructor, which refused `native soil proof accepted support or exact bootstrap`. The separately reported reader joins were outer position 214 versus captured 0 and provider GSI `85ca5300…` versus captured `e1ca5d7e…`; neither operand was installed or normalized. The execution audit records one run-frame construction and zero physical compute, state mutation, commit, and publication calls.

**A-001 — High, canonical soil refusal is attributable to stale accepted support in the actual current resident.** `v9_real_consumer_shadow.rs:713–727` passes the captured pre-child soil resident, day-4 committed bootstrap, restored target clock, and `InProgressSupportCurrent` to `NativeSoilRestartAdmissionV1::authenticate`. The current resident is the actual `rows/123091.23.json` member: `latest_accepted=Some`, owner/credit support end `385200000000000`, transaction 5762. The restored target clock has accepted-until `385920000000000` (also the recorded pre-child `accepted_until`). In `native_soil_restart_admission.rs:50–58`, the exact-bootstrap alternative requires clock zero, next day zero, receipt-free resident/bootstrap, and byte equality; none describes this day-4 record. With that branch false and `latest_accepted=Some`, the failing predicate is specifically `owner.support_end_ns != clock.accepted_until()`. Reaching it establishes that earlier policy, resident/bootstrap validation, unique exact clock soil-owner match, and run identity checks passed. This is an actual constructor admission refusal, not a reader selection of the wrong soil or bootstrap: capture source stores the live consumer's `diagnostic_native_soil_v2()` as target and the committed bootstrap's separate soil member as bootstrap (`snow_stage3_v11_current_context_capture.rs:1770,1863`).

The immutable deferred phase member `rows/123091.26.json` explains the gap without curing it: its custody posture is `reconstructible_operands_not_installable_capability`; its 12 ordered credit groups lead to an unpublished trial ending at `385920000000000` (final trial transaction 5774), while the accepted current resident remains at `385200000000000`. Substituting that unpublished candidate for the accepted resident would bypass the proof's published/accepted-support requirement. The committed bootstrap `rows/108033.9.json` is distinct and ends at `345600000000000` (transaction 5304), so it cannot satisfy the exact initial bootstrap branch. Preserve the refusal and all three independent position/GSI/soil outcomes. The final source/evidence review must still verify the deferred-chain joins and that no later diagnostic path turns this failed admission into a checkpoint success.

## Authority-control and private representation update (interim)

Evidence class: **Static and inspected Ran evidence.** I inspected current member-restoration source, the corresponding persisted hydrology and land-surface-energy canonical implementations, and `authority-controls-fast` / `authority-dto-fresh` receipts. I did not run a test or model. Both receipts bind source tree `c665d97ff92dc14c36c49b02fbf67b22597d57d1661fe47d86cb6d95479ca79c`; source remains under edit. The former exited 101 with 3/4 passing because the DTO test attempted to install an already-installed process-global policy; the latter ran that DTO test in a fresh process and passed 1/1. The combined run therefore remains a failed suite, while the fresh DTO result is valid narrow evidence.

**A-001 — Medium, same-process test isolation must be reconciled.** `snow_stage3_v11_current_context_capture.rs:4590–4605` unconditionally installs the global B01 snow policy in the DTO roundtrip test. When included with another policy-installing test in `authority-controls-fast`, it panics `policy already installed` before its assertions. The separate fresh-process pass validates that test's actual roundtrip and seed/surface checks, but does not convert the 3/4 combined result into PASS. Either make the test safely composable under the canonical policy API or keep a deliberate fresh-process invocation in the required evidence and label the combined run FAIL. No physics ran in either receipt.

**A-001 — Medium residual, private representation duplication now has bounded parity evidence.** `snow_stage3_v11_member_restoration.rs:67–87` has the same recursive f64-bit-string, `(domain, Value)` serialization, and SHA-256 projection as `openwepp-persisted-restart-v1/src/hydrology_restart.rs:37–59`; persisted `to_canonical_bytes` is `serde_json::to_vec`. `snow_stage3_v11_member_restoration.rs:793–892` mirrors the canonical unpublished-trial tuple and exponent transformation in `openwepp-land-surface-energy/src/identity.rs:92–142`. The real deferred-trial positive path passes its recorded seal, including actual exponential float representations in `rows/123091.26.json`; transaction, predecessor, custody-variant, exponent-credit, and seal substitutions each refused with the expected source-defined message in the phase-control run. This satisfies bounded actual-record seal parity for the file-only diagnostic. The duplicated algorithms still merit an explicit private/no-public-API justification and future synchronization responsibility; these tests do not make the implementations shared.

**A-001 — Low, immutable hydrology pin negative coverage is narrower than the implementation.** The fresh DTO test roundtrips actual constructor inputs and passes `pin_hydrology` against the pinned seed, then changes lane area and observes `seed immutable lane area_m2`. The source also checks phase-plan digest, each lane's immutable day-input digest, topology, and geometry (`snow_stage3_v11_member_restoration.rs:89–178`), but the inspected controls do not directly poison phase plan or day inputs. Add one semantic negative for each digest if claiming exercised coverage of those independent checks; the successful actual seed join and static canonical parity already support the observed checkpoint path.

**A-003 — Prior beginning finding provisionally addressed.** `snow_stage3_v11_current_context_capture.rs:2991–3024` now compares the unique earliest positive parent receipt support and beginning digest to the canonically restored publication projection. Its day-0 control checks the real tick-zero-event case and rejects substituting the pre-event entry beginning for the first-positive beginning. Final-source and bound execution review is still needed before closing A-003.

## Terminal-source review in progress (audited-source tree `06a8102786c9efacbbdf2555b590f77f34f49edd7b2d8fad3b6c6f4655835238`)

Evidence class: **Static and inspected Ran evidence.** This review is bounded to `lint-audited-orchestrator-source.json`; latest focused executions were still pending at inspection time. I inspected A002 prospective red (0/1 exact prior `archived publication day chronology`) and green (1/1 no/one/two-terminal and typed poison controls) receipts, the archive beginning control, current native source, and the quality-extraction patch. The red/green source identities precede the latest A002 fixture helper and Drop-audit change; they are historical evidence, not a current-source test result. Capped matched lint on the audited tree exited 0, but its command includes `--cap-lints warn` and is not a strict Clippy PASS. The only unmatched diagnostic in that comparison is on unchanged `v9_real_consumer_shadow_v10_accessors.rs:92`.

**High, newly detected default-feature test compilation scope.** `v9_real_consumer_shadow_publication_retention.rs:2091,2397–2400` adds a direct archive test inside a `#[cfg(test)]` module and calls both `restart_authority_restore_archived_publication_day_v1` (gated by either restart-authority or persisted feature) and `snow_stage3_v11_current_context_capture::member_backed_restore_tests::ProbeAudit` (gated by test **and** persisted feature). `openwepp-hillslope-orchestrator/Cargo.toml` declares `default = []`. Thus a default-feature orchestrator test build cannot resolve these names; the inspected lint build enables both features and cannot establish default-feature compilation. Gate the feature-specific direct test and its archive fixture helpers consistently, or provide an equally scoped test audit. This is a static finding pending a default-feature compile and same-reviewer fix verification.

**A-001/A-003 — Source checks provisionally addressed, executed outcome remains refusal.** The native reader's `restore_authenticated_archive_manifest`, `restore_native_context`, `validate_selected_support`, `validate_provisional`, and `replay_deferred_ordered_state_chain` extraction preserves the previously inspected predicate order and error exits. `pin_hydrology` now checks bit-exact lane geometry/topology plus phase-plan and day-input digests; the DTO test contains semantic pinned-digest and geometry controls. `validate_archive_first_positive_join` runs inside canonical archive authentication, and the day-0 test rejects a pre-event beginning substitution. `restore_native_result` returns Err for every constructor or independent position/GSI refusal combination and reaches the six-owner projection only on complete acceptance. The new negative composed selector asserts the exact immutable 214/0 position, GSI, and canonical soil-support refusal; the original success-asserting test is retained and remains a known FAIL. Final bound tests and same-reviewer source reconciliation are needed before closing A001–A005.

## Terminal same-reviewer disposition — tree `7c04f52e6cae6327202591b37f0c4e7a5fff3ae34a103255a35c49a3dbdf37ab`

Evidence class: **Static and inspected Ran evidence.** I inspected the terminal reconciliation, exact source, binary and run receipts, canonical admission predicate, and their stdout/stderr; I ran no Rust build, test, or model command. The terminal focused binary is SHA-256 `47fd72c2b2c9acb422021659d499be3ba0b29020d94468f084a9846eb89cb631`. Terminal reconciliation records exact-base reconstruction PASS and unchanged acquisition, 49 export members, prior frozen binary, and supplemental inputs. The final feature-enabled build and formatting checks passed. All eleven final focused tests ran in fresh processes against this tree and passed: direct archive, day-0 beginning, DTO/seed/surface, manifest, phase positive, phase poisons, provider poisons, three pure chronology controls, and composed native refusal. Earlier red, failed combined-policy, and success-asserting results remain historical FAILs; none is relabeled PASS.

**A-001 — Bounded diagnostic resolved; full native restoration/admission remains HOLD.** The actual member-backed path reconstructs hydrology inputs, joins immutable seed geometry/topology/phase/day-input digests and pinned provider/surface, authenticates current Stage3, parcel posture, clock owners, deferred 12-child transaction/credit-state chain, provider support and forcing, and invokes `DirectV10RealConsumerShadow::diagnostic_restore_native_consumer_v1` with the original soil/bootstrap/native fields. The terminal composed selector authenticated all four archives and the manifest, then returned the exact combined refusal: outer accepted position 214 versus captured 0, provider GSI `85ca5300…` versus captured `e1ca5d7e…`, and canonical soil proof `native soil proof accepted support or exact bootstrap`. Independent source/immutable-member inspection attributes the soil predicate to resident accepted support ending `385200000000000` while the clock ends `385920000000000`; the unpublished deferred trial ending at the clock cannot be installed as an accepted resident. The constructor was reached; the six-owner projection was **NOT REACHED**, and the retained original success-asserting test still fails. This is a defensible precisely attributed checkpoint refusal, not checkpoint restoration or admission.

**A-002 — Terminal-event archive endpoint correction verified for the bounded path.** The source now takes the canonically replayed full-history tail after `replace`, allowing zero-duration terminal events after the last positive support without changing event chronology admission. A prospective source-bound red test failed 0/1 on the old `archived publication day chronology`; its paired green passed 1/1. The final-tree direct-restorer control passed 1/1 in 124.29 seconds with no-terminal, one-terminal and two-same-tick-terminal positives; raw and independently resealed wrong tick, foreign parent and wrong beginning; reorder, omission, duplication, wrong final owner, schema/count and noncanonical-byte controls. Exact `Identity` messages and precedence were asserted, and physical/direct/publication counters remained zero. The initial red binary's missing pre-edit source snapshot is a documented earlier custody gap, superseded for this prospective pair rather than retroactively repaired.

**A-003 — Archive beginning and root-chain diagnostic joins verified.** The canonical archive authenticator now joins the unique first positive support and beginning digest from the parent receipt to the independently restored publication projection, retaining the pre-start-event archive entry beginning separately. The actual day-0 control passed and rejected substitution of the pre-event entry beginning for the positive support beginning. All four original archive frames authenticated in order through `Stage3CommittedDayArchiveManifestV1::append`; the composed reader joined resulting day count, ordered chain root, content root, accepted clock endpoint and ending owner to the prepared-day prefix. Manifest omission, duplicate, reorder and foreign run/topology controls passed. This validates the bounded archive prefix; it does not override native refusal.

**A-004 — Diagnostic zero-physics evidence verified, with success boundary explicit.** The final direct archive, component positive/poison and composed negative runs report/assert zero solver maps, solves, sweeps, probes, evaluations, direct compute/mutation, day-frame commits, publication capture, and native snow-free litter/surface/WB14 calls. The composed refusal constructs one run frame; that is reconstruction, not a process step. No full accepted native consumer or six-owner success path executed, so these counters cannot be promoted to scientific regression, conservation closure, or restart-success evidence.

**A-005 — Semantic negatives materially expanded; terminal native-owner negatives remain unreachable.** Final-tree controls exercised source-defined refusals for event order/tick/parent/beginning/final and canonical wire, archive first-positive beginning and manifest roots/namespaces, provider position/GSI, immutable seed digests/geometry/surface, current Stage3/terminal parcel, deferred transaction/predecessor/custody variant/seal/ordered credits, and prefix proof/event membership. The native six-owner projection guard remains unexercised because the canonical soil admission refuses first. The earlier same-process policy `already installed` 3/4 run remains FAIL; fresh-process DTO and other component controls passed. The private hydrology/trial digest mirrors have actual captured-seal parity and poisons but remain a medium maintenance risk requiring synchronization with their canonical sources.

**Gate and verdict.** The final default-feature `cargo check --tests --no-default-features` still exits 101 with 61 errors, but its error-message multiset exactly matches the acquisition source's inherited 61; the introduced feature-scope regression found in this review is fixed. The final capped, matched-target Clippy diagnostics completed but are **not** a strict lint PASS. Immediate Critical full-workspace correctness regression, model/restart campaign, conservation qualification, original success assertion, and native six-owner success remain **NOT RUN/BLOCKED or FAIL** under the owner's bounded allowance and current immutable refusal. I find no additional source correctness blocker for reporting this **file-only diagnostic refusal**. **Overall restoration/admission: HOLD.**
