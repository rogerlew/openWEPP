# Codex Execute Prompt - WA Positivity-Clamp Numerics Hold-Lift

Execute
`docs/work-packages/20260708-laned-router-wa-positivity-clamp-numerics-hold-lift-001/`
end to end.

Read the package and required governance first. The narrow hypothesis from the
prior WA investigation is that the active router treats material intermediate
negative-depth predictor/corrector excursions as positivity-clamp mass instead
of failing closed. Contract-first authority is required before changing clamp
semantics.

Do not promote target-`dx`, tune coefficients, alter source producers, revive
the abandoned hybrid subsystem, or touch the Tier-2 package except by reference.

Expected output protocol:

1. Write diagnostic reproduction and localization evidence.
2. If the contract supports a narrow fix, amend `SC-OFEROUTE-001` first, then
   implement the guard/fix and focused tests.
3. Re-run WA active fixed `10 cells/OFE` and at least one affected rung with
   release-binary provenance.
4. Run required gates, dispatch authorized review/verification as useful, and
   disposition every finding.
5. Close with `final-disposition.md` and a worker handoff for the next package
   if target-`dx` promotion or deeper solver correction remains blocked.
