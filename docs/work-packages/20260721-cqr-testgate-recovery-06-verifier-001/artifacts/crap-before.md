# CRAP Before

Static: Attempt-15 retained evidence reports
`verify_receipt_after_ready_audit` at CC 6, 0% coverage, and CRAP 42. One exact
pre-production matching-module baseline was therefore required after scaffold
and dual eligibility review.

Ran: the single baseline at exact clean HEAD `ce1da0a6` used one matching
`cargo llvm-cov -p openwepp-gate-planner --lib --ignore-run-fail` traversal and
the same LCOV for `cargo-crap`. It reports the target at CC 6, 34.7826% line
coverage, and CRAP 15.9860. No compiled `verifier.rs` function is above CRAP
30; `verify_dag` is the maximum at exactly 30.0. Evidence is retained at
`/tmp/cqr-verifier-baseline-NLrOph`.
