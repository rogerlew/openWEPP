# Exact Diff Reconciliation

Status: `PASS`

Evidence class: `Static`

The terminal diff is confined to the declared write set:

- this CAL-04B package subtree;
- `docs/work-packages/README.md`;
- `docs/planning/canopy-phenology-assurance-roadmap.md`.

The package changes correct package-local synthetic fixture semantics, external
authority-path resolution, pre-open aggregate validation, freeze identity
construction, retention command custody, bounded result publication, and
terminal evidence. Production crates, canonical science contracts, admitted
observations, protected fixtures, CAL-04, and CAL-04A are unchanged.

The accepted freeze binds the historical terminal validator byte-for-byte. Its
four-local-ULP-width aggregate comparison is wider than four representable
steps at binade boundaries, so it was not modified after Harvard opened.
`tools/validate_terminal_aggregate_ulps.py` is a post-freeze supplemental audit
that proves every actual finite aggregate is within three exact IEEE-754 steps.

The unrelated untracked
`docs/audits/20260727_gate_planner_demotion_readiness_audit.md` is user-owned
and excluded from this package.

The 253 MB typed failure ledger is a required result and is scoped to Git LFS
by the package `.gitattributes`. The 4.32 GB raw trace and 972 MB compressed
trace remain checksum-bound external objects and are not added to Git.
