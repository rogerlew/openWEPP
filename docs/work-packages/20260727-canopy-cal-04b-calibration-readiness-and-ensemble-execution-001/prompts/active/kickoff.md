# Kickoff: CANOPY-CAL-04B Calibration Execution

Scope: local repository calibration execution. Package-local tools, evidence,
copied-fixture production probes, and authorized population batches only; no
production source edits, deployment, or external contact.

Execution mode: `package-end-to-end`.

Execute the accepted rework strategy through final disposition. Implement and
review the executor before Hubbard population work. Keep Harvard sealed until
the immutable accepted ensemble and exact holdout identities receive two
independent verifier PASS receipts.

Required-reading budget: `197083` local bytes, `OK`; map:
`artifacts/required-reading-map.md`.

Do not widen CAL-04A domains, use Harvard or downstream responses for selection,
alter production physics, or collapse equifinal ranges.

Subagent requirement: REQUIRED. This prompt explicitly authorizes subagent
spawning/delegation to two prospective code/science reviewers, the
`comparator_suite_runner` for heavy population work, two freeze verifiers, two
terminal scientific reviewers, and two terminal verifiers. Review and
verification are read-only; comparator writes are package results/logs only.

Closure requires all package advancement criteria and truthful final
disposition.
