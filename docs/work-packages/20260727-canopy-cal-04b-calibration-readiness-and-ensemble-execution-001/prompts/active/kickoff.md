# Kickoff: CANOPY-CAL-04B Calibration Readiness and Ensemble Execution

Scope: local repository scientific calibration and evidence work. Flat-file
reads/edits, package-local tooling/results, production-runner execution, and
authorized heavy candidate batches only; no deployment or external contact.

Execution mode: `package-end-to-end`.

Execute every phase in `package.md` sequentially through disposition. Read the
package-local required-reading map before affected work. Preserve immutable
calibration/independent-validation roles, the Harvard seal, contract-first
science, typed failures, real production parameter consumption, and ADR-0042
claim discipline.

Required-reading budget: `197083` local bytes, `OK`; map:
`artifacts/required-reading-map.md`.

Do not widen CAL-04A domains, refine after scores, select from Harvard or
downstream outputs, alter production physics, or collapse equifinal ranges.

Subagent requirement: REQUIRED. This prompt explicitly authorizes subagent
spawning/delegation to two prospective reviewers, two terminal scientific
reviewers, two terminal verifiers, and `comparator_suite_runner` for heavy
9,261-candidate/population execution. Reviewer/verifier access is read-only;
comparator outputs are limited to package-local logs/results and compact
metrics/receipt paths.

Autonomy: execute end-to-end without additional user direction unless a proven
hard blocker remains after safe in-scope alternatives are exhausted.
