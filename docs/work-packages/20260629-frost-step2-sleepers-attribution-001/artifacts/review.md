# Review

Evidence mode: Static + Ran.

## Review A

Finding: accepted. The package correctly excludes Morris, Mandan, and Reynolds
Creek. Morris remains blocked by Step 1; the two isotherm sites lack paired snow
control.

Finding: accepted. Magnitude residuals are reported but not used as
verdict-bearing defects. W9's sign-incoherent magnitude is a signal for Step 3
context, not a standalone `OPENWEPP-DEFECTIVE` verdict.

Finding: accepted. South Field is not a full timing pass: late-thaw failures in
four water years are not explained by deeper modeled snow.

## Review B

Finding: accepted. The Step 3 pointers are limited to diagnostic pointers.
Residue-lifecycle handoff is the primary pointer; the legacy-envelope outlier
record is comparator context under ADR-0017. No Qwet or residue implementation
is authorized here.

Finding: accepted. The Step 1 `>0.25` systematic-timing-fraction cutoff remains
package-local and is explicitly flagged for Step 4 ratification review.
