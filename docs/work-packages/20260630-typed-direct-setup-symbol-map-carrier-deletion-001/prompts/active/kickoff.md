# Kickoff Prompt

Scaffold and execute TYPED DIRECT SETUP + SYMBOL-MAP CARRIER DELETION — make
the typed `DirectRunFrame` the SOLE production runtime representation, then
delete the symbol-map compatibility runtime. Identity-preserving,
no-compatibility-proven, staged, worktree.

Primary discipline: single-authority typed direct setup; no physics or output
schema changes; retain only the explicit deprecated `--compatibility-runtime`
replay/comparator seam.
