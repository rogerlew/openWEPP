# Independent science review

Disposition: `HOLD before production implementation`.

The reviewer confirmed the physical derivation and exact calculator vectors:
at exact zero absorbed PAR, `Ag=0`, `An=-Rd`, `gs=g0`, and the admitted
diffusion identities produce `cs>ca` and `ci>ca`.

The active Version-13 numerical clause still mandates Brent on
`[Gamma*,ca]`, while V9 is an oracle/serialization-only successor importing
V8 runtime behavior and supported domain. An analytic bypass is therefore not
admitted under the current identity. A prospective contract successor or
explicit reviewed versioned erratum is required, including an exact `+0.0`
versus `-0.0` rule. No production implementation was made.
