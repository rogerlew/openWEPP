# Coupled Nighttime Oracle Evidence

This directory is executing evidence. It is not yet the independent
high-precision oracle or released authority.

The first decision test evaluates the accepted potential coordinates through
the complete fixed-final evaluator under exact real-owner FullSupply caps.
Reproduce it from detached commit `aa8f55d93d58df6c62b5ae4eebb78245b5469fd6`
by applying `potential-as-final-diagnostic.diff` and running:

```text
nix develop --command cargo test -p openwepp-hillslope-orchestrator \
  v9_real_consumer_shadow::tests::sealed_repository_receipts_project_into_real_child4_forcing_types \
  --lib -- --exact --nocapture
```

The command intentionally exits 101 because the historical solver forms a
singular Jacobian after observing a tolerance-satisfying iteration-zero
fixed-final residual. The raw log, structured f64-bit ledger, cap identity,
and interpretation are preserved alongside this README.

Remaining work includes canonical typed input files, complete equation and
owner maps, independent >=100-digit H0 partial-supply solves, root/rank search,
and the three authority reviews. H1 remains unselected.

