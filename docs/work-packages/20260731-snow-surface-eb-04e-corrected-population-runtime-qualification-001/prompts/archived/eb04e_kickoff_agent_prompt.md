# SNOW-SURFACE-EB-04E Kickoff

Scope: local repository science/runtime qualification work; flat-file reads and
edits only; no external connectivity.

Execution mode: package-end-to-end (completed).

Execute every phase in `package.md` sequentially through disposition.

Required reading:

- Core: `AGENTS.md`, `docs/work-packages/AGENTS.md`,
  `docs/codex_exec_plans.md`, `package.md`, and
  `artifacts/prospective-qualification-protocol.md`.
- Conditional: testing strategy, kernel preparation, prompt wording, and
  science-contract AGENTS when their governed surfaces activate.
- On-demand: `SC-SNOWENERGY-001`, campaign roadmap, and EB-04/04A/04C/04D
  predecessor evidence for the mechanism being checked.

Required-reading budget: `251136 bytes`, `OK`; map:
`artifacts/required-reading-map.md`.

Task: execute the corrected 12-lane/48-cell runtime qualification end-to-end.

Constraints: freeze protocol before results; preserve canonical authority,
typed guards, fixtures, selectors, observations, and prior evidence; no
surrogate physics, silent defaults, retry of semantic failure, observation
scoring, factorial effects, calibration, interaction, or promotion.

Conservation acceptance: independently reconstruct mass, surface energy,
cold content, latent/mass, hourly totals, and layer SWE/depth from published
operands; reject aliases and producer-only evidence.

Subagent requirement: REQUIRED: spawn `comparator_suite_runner` for the heavy
population and Critical full runs. This prompt explicitly authorizes subagent
spawning/delegation to that runner, two read-only reviewers, and two read-only
terminal verifiers for the roles in `package.md`; expected outputs are compact
metrics and package evidence, with no production write access.

Autonomy: execute end-to-end without requesting user direction unless a hard
boundary is proven.
