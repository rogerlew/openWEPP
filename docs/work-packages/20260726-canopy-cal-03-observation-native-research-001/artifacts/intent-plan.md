# CANOPY-CAL-03 Intent Plan

Evidence class: `Admitted pre-implementation record`

Admitted base: `main` at package scaffold time; record the exact SHA in the run
manifest before execution.

Intent: install pre-calibration evidence, paired native fixtures, and an
opt-in campaign research trace without changing parameters, process physics, or
public schemas.

The terminal write set and gate selection are exactly those declared in
`../package.md`. Production edits are limited to carrying already-computed
native GSI/canopy values into an optional diagnostic record and writing exact
downstream consumer operands. The trace must be absent unless explicitly
selected, must fail closed on I/O/serialization errors, and must not alter
simulation state or chronology.

Any physics, default, management-schema, Cargo, public-output, or source-input
change invalidates this plan until prospectively amended.
