# Archived Execution Prompt

Execute `20260726-canopy-cal-03-observation-native-research-001` end to end.
Preserve the no-calibration/no-new-physics boundary. The executor is explicitly
authorized to spawn subagents for the package-required two independent reviews,
two independent terminal verifications, and bounded heavy fixture execution.

Subagent authorization: this package explicitly authorizes spawning/delegating
those review, verification, and heavy-execution tasks. Expected outputs are two
review verdicts, two verification verdicts, and a matrix execution receipt.
Write access is read-only except for a package artifact explicitly assigned by
the primary executor.
