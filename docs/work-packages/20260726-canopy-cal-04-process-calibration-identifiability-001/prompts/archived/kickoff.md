# Kickoff: CANOPY-CAL-04 Process Calibration and Identifiability

Execute
`docs/work-packages/20260726-canopy-cal-04-process-calibration-identifiability-001/package.md`
end to end from `/home/workdir/openWEPP`.

Read the applicable `AGENTS.md` chain and the complete required-reading set
before acting. Maintain the package as a living ExecPlan and preserve all
protected inputs and authority roles.

Hard constraints:

- select parameters only with admitted Hubbard Brook calibration evidence;
- keep Harvard Forest fully embargoed until the calibration ensemble, tools,
  domains, weights, stopping rules, and commands are checksum-frozen;
- use the frozen daily GSI21 0.5 crossing operator and equal-year interval
  objective;
- use a deterministic prior-free search and retain every candidate, failure,
  boundary hit, and equifinal range;
- fit in the declared process order and freeze accepted upstream ranges;
- never use Harvard, snow, interception, ET, runoff, frost, erosion, legacy
  agreement, or current defaults to choose parameters;
- make no production-code, physics, contract, protected-fixture, or CAL-05
  decomposition change.

Complete prospective review before result-bearing runs, then execute the
ordered stages, freeze the ensemble, open Harvard exactly once, perform
evaluation-only downstream runs, and complete terminal review and verification.

Subagent authorization: this package explicitly authorizes subagent
spawning/delegation to the prospective reviewers, bounded execution workers,
terminal reviewers, and terminal verifiers named in the package; expected
outputs and write access are exactly those declared there.

Close truthfully as `PASS_BOUNDED`, a negative scientific outcome, or `HOLD`.
Do not select a convenient point from an unidentified range.
