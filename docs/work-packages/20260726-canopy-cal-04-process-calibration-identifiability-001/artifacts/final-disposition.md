# Final Disposition

Evidence class: `Ran: authority/input intake, deterministic rebuild,
non-result-bearing gates, dual review, dual verification, and exact terminal
reconciliation; Static: scientific authority boundary`

Package disposition: `EXECUTED / HOLD`

CAL-04 stopped before the first result-bearing candidate. Typed process-input
contracts define admissibility and ordering, but do not define finite
evidence-supported calibration bounds for the complete six-operand GSI vector.
The prior-free design cannot lawfully invent those bounds or derive them from
current/default, Bill Elliot, or legacy values.

The frozen later-stage authority is preserved precisely:

- `CAL03-OBS-HB-001` constrains only the biomass partition sum and does not
  separate `Bf,max` from `Bs`;
- `CAL03-OBS-HB-005` supplies bounded mature leaf-on LAI authority but cannot
  identify `xmxlai` before upstream GSI and `fe` are frozen;
- no quantitative selection authority separates `fe`, `Cs`, or `bb`.

Terminal evidence:

- both retained checksum manifests pass;
- the 1,251-row admitted timing ledger rebuilt byte-identically, SHA-256
  `890a0ff09ca707b097a15cb5de7964698a9b4d5af797ed6b81d5fccf7c141b61`;
- roles remain 932 Hubbard calibration and 319 independent Harvard holdout
  rows, disjoint, with Harvard fall 1992 absent;
- zero candidates, zero model failures, zero accepted ensemble members, and
  zero Harvard result rows exist;
- all seven stages truthfully hold with no accepted range;
- Harvard remains `SEALED`; downstream evaluation was not run;
- prospective and terminal dual reviews passed after accepted corrections;
- dual independent verification passed the corrected state;
- CMD-014..018 post-verification reconciliation passed the hold, roles,
  inventory, write set, prompt state, Markdown, diff, and terminal identity
  checks.

Required scientific gates remain `BLOCKED`, so this package is not
`COMPLETE`, `PASS_BOUNDED`, `NONIDENTIFIABLE`, `CALIBRATION_FAIL`, or
`HOLDOUT_FAIL`. No fitted parameter range, empirical profile, model failure,
Harvard score, or downstream effect is established.

Hold lift requires a new prospective authority amendment that supplies finite
evidence-supported bounds and an exact deterministic grid/refinement/stopping
design for all six GSI thresholds. Later stages additionally require sufficient
separating observation authority and finite operand search bounds, while
retaining the existing partition-sum and LAI roles. Harvard must remain sealed
until a nonempty accepted calibration ensemble and exact analysis/command
identities are frozen.
