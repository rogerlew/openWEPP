# Identifiability and Equifinality

Status: `PROSPECTIVE HOLD / NO RESULT-BEARING EXECUTION`

Evidence class: `Static`

No parameter profile, correlation, boundary hit, accepted candidate, or
equifinal family was computed. Zero candidates and zero model failures were
attempted.

This absence is required by the prospective freeze gate. A complete finite GSI
search vector cannot be enumerated from the admitted authority because five
threshold operands lack finite two-sided search bounds. Using the protected
management values as bounds or centers would violate their admitted
comparison/initialization-only role.

The later stages have insufficient separating authority.
`CAL03-OBS-HB-001` constrains the total aboveground biomass partition sum, not
`Bf,max` and `Bs` separately. `CAL03-OBS-HB-005` supplies a bounded mature
leaf-on LAI range, but `xmxlai` cannot be evaluated in process order while GSI
and `fe` remain unfrozen. No quantitative selection authority identifies
`fe`, `Cs`, or `bb`; qualitative/proxy canopy screens cannot create a scalar
loss. Under `INV-PLANT-034`, these operands do not alter `/gsi/gsi21`, so the
timing objective cannot substitute for missing separation.

The terminal scientific outcome is not a fitted range. It is an authority
hold requiring, prospectively and before any affected execution:

1. finite evidence-supported search bounds and an initial grid for all six GSI
   thresholds, plus fixed refinement/stopping rules; and
2. sufficient separating calibration authority and finite search bounds for
   each requested magnitude/partition/LAI/canopy stage, while retaining
   `CAL03-OBS-HB-001` and `CAL03-OBS-HB-005` in their frozen roles.

Harvard timing and downstream hydrology/erosion cannot lift either boundary.
