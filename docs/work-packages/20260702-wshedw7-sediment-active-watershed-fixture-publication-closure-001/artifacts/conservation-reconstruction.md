# Conservation Reconstruction

Status: `blocked`

Evidence mode: `Ran:` negative fixture probes.

Independent sediment-publication reconstruction could not close because no
accepted fixture produced nonzero sediment.

Negative reconstruction on `/tmp/wshedw7_probe_carn/out`:

- `totalwatsed3.tdet = 0.0`
- `totalwatsed3.tdep = 0.0`
- `totalwatsed3.sed_del = -0.0`

Negative pass-level reconstruction on `/tmp/wshedw7_probe_insensible_h1`:

- `row_count = 18262`
- `max(tdet) = 0.0`
- `sum(tdet) = 0.0`
- `max(tdep) = 0.0`
- `sum(tdep) = 0.0`
- `max(sedcon_1 + ... + sedcon_5) = 0.0`

The same zero pass aggregate held for additional multi-OFE hillslopes
`21`, `172`, `297`, `333`, `390`, and `437`.

W7 therefore holds before conservation acceptance rather than closing on
self-consistency or zero-only fixtures.
