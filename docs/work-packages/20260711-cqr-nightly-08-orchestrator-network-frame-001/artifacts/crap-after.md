# CRAP After

Ran provisionally: `direct_terminal_event_duration_s` fell from `34.668` to
`17.126` through characterization, but other eligible rows remained above `30`:
`sum_contributing_area_m2` `72`, `build_typed_publication_frame` `42`, and
`build_routing_globals` `33`. Formatter `132` is dispositioned. Tests were
rolled back, so no final metric reduction is claimed. Raw CRAP SHA-256 is
`06e1ebaf859808390213fb179e4ccd9de40066cdc7da8e8fa98f6c734bd7279a`.
