# Scaffold Evidence

Evidence mode: Static.

- Package directory created with `package.md`, active prompt, archived prompt
  directory, and artifacts directory.
- Package objective is bounded to coupled WAT diagnostic evidence.
- Protected boundaries explicitly exclude production physics, defaults, fixtures,
  schemas, parser/runfile/user CLI selectors, and site calibration.
- Acceptance gates require the real direct-production WAT path and package-local
  report artifacts before closure.
