# Interpretation And Disposition

Status: `executed / operator mechanics characterized / predecessor bridge HOLD`.

Evidence mode: `Ran` at exact clean execution commit
`5ebfc5135b80d250cb6b38d1b6241a7d2a72d6c5`.

## Frozen Decision

The consumer emitted exactly two admitted classes:

- `PREDECESSOR_NOT_REPRODUCED`; and
- `INITIAL_CONTROL_VOLUME_PROJECTION_DIFFERENCE`.

The stronger `STATE_EVOLUTION_RECONCILES_SIGN_CONTRADICTION` class is not
admitted. Snowbird's reconstructed legacy sequential median is
`+188.8725288356066 MJ m^-2`, while the frozen predecessor is
`+170.2536089 MJ m^-2`: a `+18.6189199356066 MJ m^-2` or `10.935991346%`
difference, far outside the frozen `1e-7 MJ m^-2` tolerance.

## What The Result Establishes

On identical three-way support, every site has negative immutable same-state
`S`, negative frozen-active reference `F`, and positive evolving sequential
`Q`. Snowbird medians are `S=-1086.4748`, `F=-742.0217`, and
`Q=+179.5949 MJ m^-2`. The corresponding pattern also holds at Mica Creek,
Niwot, and Paradise. Sequential state evolution is therefore a strong and
repeatable operator-level correlate of the sign reversal, but the failed exact
predecessor bridge prohibits calling it the resolved cause of the historical
contradiction.

Initial projection differs at all four sites, but it does not itself cross the
sign boundary: both `S` and `F` remain negative. Omitted support is below one
percent at every site (`0.49%` to `0.94%`) and never changes sign. Shortwave is
invariant. Protected HBP, PASS, and WAT outputs are byte-exact.

Snowbird's median evolution deltas are concentrated in sensible and latent
exchange, with smaller longwave and negligible advected-heat contributions.
These are medians of separately reduced term distributions and are not
additive identities. They identify the next lineage surface; they do not
authorize coefficient, flux, or physics changes.

## Disposition

Close this characterization package as `executed / HOLD for predecessor
bridge reconciliation`. The next increment must reproduce the v5
`+170.2536089 MJ m^-2` estimator from the schema-v6 source by isolating daily
reset cadence, first-state projection, substep transition, and legacy
active-conduction lineage. Persistence, seasonal chronology, terminal receipt,
physics correction, Stage 3 promotion, and CoE retirement remain held. CoE is
unchanged as the sole authoritative production owner.
