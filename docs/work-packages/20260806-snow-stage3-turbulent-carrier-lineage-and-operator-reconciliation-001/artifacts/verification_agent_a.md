# Verification Agent A

Status: `PASS / scientific predecessor-bridge HOLD confirmed`.

Evidence class: `Static + Ran` at immutable clean commit
`2018433c75921527ddccd641b83adf6bbde3b291`.

No cohort or model rerun was performed.

## Custody And Reconstruction

- The frozen base, execution source, and terminal candidate form one ancestry
  chain. The base-to-head diff has exactly `113` declared paths and no outside
  write.
- Direct manifest-set and SHA-256 checks pass `143/143`. Raw result, receipt,
  manifest, and binary hashes match the frozen package identities.
- Independent reduction of all `154` annual rows reproduces Mica, Niwot,
  Paradise, and Snowbird `S/F/Q` medians exactly. Snowbird is
  `-1086.474818493409 / -742.021651464931 / +179.594922524461 MJ m^-2`.
- The Snowbird legacy median is `+188.8725288356066 MJ m^-2`, versus frozen
  predecessor `+170.2536089`; the `+18.618919935606613` difference is
  `10.935991346029326%`. The ordered classifier is therefore exactly
  `PREDECESSOR_NOT_REPRODUCED` then
  `INITIAL_CONTROL_VOLUME_PROJECTION_DIFFERENCE`. The state-evolution causal
  class remains correctly withheld.
- Common-support omission independently reproduces below one percent at all
  four sites and never changes the common-versus-all sign.
- A direct real-JSONL check reconstructed `2,468` tuples with zero primitive,
  vapor-duration, mass, cold, legacy-bridge, or endpoint failures. All `2,368`
  adjacent sequential transitions preserve the applicable exact state.
- Paired/sequential WAT and HBP rehash equal control at every site; runfile
  custody binds PASS exactly and every PASS flag is true.

## Authority And Disposition

Focused consumer `51/51`, contract/observability `13/13`, Binding Exposure
`11/11`, and unit compliance pass. Assurance remains generation `cee22d5f...`,
DRAFT, nonpublic, and without review, approval, realization, release, or
publication authority. CoE remains the sole authoritative melt owner. The
package correctly limits its claim to operator mechanics and routes the
unreproduced bridge to
`SNOW-STAGE3-LEGACY-PREDECESSOR-BRIDGE-RECONCILIATION`.

No finding remains. The scientific predecessor-bridge HOLD is the verified
outcome, not a terminal-verification failure.
