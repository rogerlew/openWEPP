# Kernel Profile Compliance

Status: `queued`.

Evidence class: `Static` pending implementation.

The final checklist must cover canonical authority, algorithm/schema semantics,
guard mapping, units, calibration N/A posture, test vectors, gap disposition,
and evidence paths.
