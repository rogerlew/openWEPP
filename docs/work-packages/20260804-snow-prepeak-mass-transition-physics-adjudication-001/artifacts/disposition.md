# Disposition

Status: `complete / review and verification pass`

Evidence mode: `Ran` plus `Static`

The package executes and adjudicates to `UPSTREAM_GENERATION_PRIORITY` as an
investigation priority only.

- Four exact CLI replays, 61,364 schema-v4 rows, and 154 primary site-years are
  complete.
- Pre-v4 operands, WAT, and HBP remain exact.
- Independent primitive reconstruction and all linked-ledger closures pass.
- Daily-local signed opportunity is systemic but materially large at only two
  sites; post-CoE cold-content and Stage-3 capture indices are small everywhere.
- Static consumer evidence excludes current Stage-3 disposition as the
  authoritative SWE/runoff loss path.
- Both independent reviews pass after all findings were accepted and resolved.
- Both fresh terminal verifiers independently reproduce the evidence and pass
  with no remaining findings.
- Direct terminal gates pass; full workspace profiles are not applicable to the
  empty production/kernel diff.

No production equation, contract, selector, default, test, fixture,
observation, or output schema changed. No signed-netting, cold-content,
capacity, forcing, phase, calibration, validation, or promotion claim is
authorized. The package is closed.
