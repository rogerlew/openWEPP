# Terminal Verification B

Status: `PASS / no findings`

Evidence mode: `Ran` plus `Static`

Fresh terminal verification independently confirmed:

- byte-exact scaffold-v1 custody, v2 hash/namespace custody, all four trace
  hashes, and absence of the rejected v1 result namespace;
- the observation, earliest-peak, exclusion, censoring, annual-first, and
  site-median operators over `154` primary plus four censored site-years;
- all `2,865` events and `1,587` hour-class rows over `632` site-year/dimension
  partitions;
- upstream localization at four sites, signed materiality at two sites,
  systemic signed direction in `154/154` years, and cohort-wide failure of the
  cold and downstream-throughput screens;
- all six visual/source/generator hash chains, calibration/readiness fields,
  reviewer remediations, package tests `6/6`, focused snow contracts `34/34`,
  JSON, Markdown, format, and diff gates; and
- truthful non-applicability of full-workspace regression to the empty
  production/kernel diff.

Verdict: `PASS`, with no findings. `UPSTREAM_GENERATION_PRIORITY` remains an
investigation priority only, not proof of excessive melt, correction authority,
forcing exoneration, simulated recovery, calibration, or validation.
