# Terminal Verification A

Status: `PASS / no remaining findings`

Evidence mode: `Ran` plus `Static`

Fresh terminal verification independently streamed all 61,364 candidate and
predecessor trace rows and confirmed:

- exact pre-v4 identity, exact WAT/HBP outputs, and all recorded fixture,
  binary, tool, trace, result, and sidecar identities;
- primitive hourly-snowfall storage closure within `9.9973e-13 m`, handoff
  closure near `1.56e-17 m`, and Stage-3 closure within `2.5045e-17 m`;
- all six primary anti-alias populations and the exact 56-path tracked write
  set with an empty production/kernel diff;
- reproduction of the frozen v2 screens: positive signed opportunity in
  `154/154` primary site-years, signed materiality at two sites, upstream
  localization at four sites, and `UPSTREAM_GENERATION_PRIORITY`; and
- real-consumer evidence that Stage-3 disposition controls neither
  authoritative runtime SWE nor hydrologic liquid publication.

The sole low finding was the stale queued status in `prompts/README.md`. It was
accepted and corrected during terminal lifecycle reconciliation. No high- or
medium-severity finding was reported, and no scientific, correction, forcing,
calibration, or validation claim was expanded.
