# Owned File Manifest

Status: `queued`

Evidence mode: `Not run`

Reconcile the exact terminal path set against the declared documentation,
package-local tool/evidence, catalog, and target-only write set.
