# Execute SNOW-PREPEAK-MASS-TRANSITION-PHYSICS-ADJUDICATION

Execute the work package at
`docs/work-packages/20260804-snow-prepeak-mass-transition-physics-adjudication-001/package.md`
end-to-end without asking for intermediate direction unless a declared hard
boundary is reached.

## Required Reading

Core:

- `/workdir/openWEPP/AGENTS.md`
- `/workdir/openWEPP/docs/codex_exec_plans.md`
- `/workdir/openWEPP/docs/work-packages/AGENTS.md`
- `/workdir/openWEPP/docs/work-packages/README.md`
- package-local `package.md`

Conditional:

- `docs/specifications/science-contracts/AGENTS.md` if canonical authority
  appears to require an edit; stop because that edit is outside this package.
- `docs/standards/testing-and-gate-strategy.md` for terminal gate selection.

On-demand:

- `SC-SNOWFREEZE-001` v124 and `SC-SNOWENERGY-001` v6.
- predecessor audit integrated evidence and mechanism matrix.
- Stage-3 trace closure and mass-transition persistence evidence.

The core local reading budget is approximately `450 kB`: `WARN`, not
`REQUIRES-JUSTIFICATION`. See `artifacts/required-reading-map.md`.

## Execution Directives

Autonomy: execute Phases A-E, update living artifacts, and reach truthful
completion or hold without pausing for ordinary choices.

No surrogate physics: do not implement or recommend a proxy, heuristic,
fitted limiter, empirical tax, or invented equation as production physics.

Conservation/output acceptance: use the frozen operand lineage, explicit
anti-alias checks, independent real-file reconstruction, real closure/magnitude
audits, and metadata/sidecar alignment. Producer self-consistency is not
acceptance.

Real consumer proof: run the exact release CLI and parse the four produced
schema-v4 files directly. Existing summaries, unit fixtures, or producer-only
state cannot close the result.

Result blindness: do not alter sites, windows, censoring, thresholds, classes,
opportunity formulas, or verdict screens after reading results. A genuine tool
defect requires rejected-result custody and a new frozen analysis version.

Claim discipline: CoE `A/B/C/D` are empirical melt-depth terms, opportunity
bounds are feedback-free `ASSUMED_FOR_EXECUTION` screens, and legacy routing is
a rollback bound. Do not call any of them physical energy shares, simulated
recovery, calibration, correction authority, or validation.

Protected paths: do not edit production Rust, canonical contracts, tests,
fixtures, observations, assurance sources, references, selectors, defaults, or
historical package evidence.

Subagent authorization: this prompt explicitly authorizes subagent
spawning/delegation to up to four read-only investigators, two read-only
reviewers, two read-only terminal verifiers, and one comparator runner with
target-only write access, exactly as bounded in `package.md`.

Gate legitimacy: every current-scope exit criterion needs current direct
evidence. Do not defer a required gate while marking the package complete.

Archive this prompt byte-identically at execution start. The orchestrator alone
edits tracked files and makes the local completion commit. Do not push.
