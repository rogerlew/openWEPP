# V9 libcrypto runtime-equivalence defect closure kickoff

Scope: local repository science-contract and authority-test engineering;
flat-file reads/edits only; no external connectivity or external-system changes.

Execution mode: package-end-to-end. Execute every phase in `package.md`
sequentially through disposition. Close defect `VEG-V9-CRYPTO-001` end-to-end.

Required reading:

- Core: `/workdir/openWEPP/AGENTS.md`, `docs/codex_exec_plans.md`,
  `docs/work-packages/AGENTS.md`, `docs/work-packages/README.md`, this package's
  `package.md`, `docs/standards/testing-and-gate-strategy.md`, and
  `tests/AGENTS.md`.
- Conditional: `docs/defect_closure_execplans.md`,
  `docs/specifications/science-contract-authoring-procedure.md`,
  `docs/specifications/science-contracts/kernel-process-contract-profile.md`,
  and `docs/specifications/science-contracts/index.md`.
- On-demand: `docs/specifications/science-contracts/contracts/SC-VEGETATION-001.md`
  and the historical V9 package's calculator, descriptor, definition, vectors,
  and repeatability evidence.

Required-reading budget: 787,520 local bytes before this package's small local
files, `WARN`; map: `artifacts/required-reading-map.md`. The large vegetation
contract is on-demand and only its V9 authority, Binding Exposure Index, front
matter, and change-log surfaces need active amendment.

Task: preserve frozen V9 bytes; amend canonical authority; add contract-derived
tests; record the failing pre-implementation gate; implement the bounded
provider-equivalence verifier; execute every selected gate; review, verify,
dispose, commit, and push.

Constraints: contract-first sequencing; no V9 byte rebinding; no production
physics/state/selector/output changes; no silent skip or unconditional checksum
bypass; retain all non-libcrypto descriptor checks and exact complete-vector
equality.

DC closure: the package must convert the reproduced in-envelope cause into the
authorized correction. Do not hold while contract, test, implementation, or
validation work remains possible.

HOLD legitimacy audit: any claimed hold must name the boundary, evidence, the
in-envelope route considered, and why it cannot close now.

Subagent requirement: REQUIRED for the heavy exact-workspace run and mandatory
independent closure roles. This prompt explicitly authorizes subagent
spawning/delegation to one read-only `comparator_suite_runner`, two read-only
reviewers, and two read-only verifiers. Outputs are compact metrics/log paths,
findings, and PASS/FAIL verdicts; no subagent has write access.

Autonomy: execute through final disposition without requesting further user
direction unless hard-blocked.
