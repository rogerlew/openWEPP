# Real Consumer Preconditions

Evidence class: `Static`

Child 2 supplies only the real-hydrology arbitration seam. Child 3 and Child 4
must additionally provide:

- V8 shared canopy-air/tile state and the admitted LSE ground requester;
- one joint potential and fixed-cap final solve from immutable beginning state;
- current-ingress partition after the capped solve;
- soil/frost energy receiving ownership;
- an actual scheduler consumer and complete atomic shadow owner set.

This package cannot claim ground evaporation, LSE runtime, a scheduler
consumer, production cutover or runtime activation.
