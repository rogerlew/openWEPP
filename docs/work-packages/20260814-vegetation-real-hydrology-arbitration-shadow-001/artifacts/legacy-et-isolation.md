# Legacy ET Isolation

Evidence class: `Static`

The unchanged production lane continues to run native R4N surface ET and root
uptake. The explicit shadow clone suppresses both R4N ET spans and substitutes
only the V7 finalized root-use debit in its V8-precursor owner candidate.

The campaign never subtracts legacy ET after it executes. It never redirects
denied canopy demand to surface evaporation, never donates unused root
authorization, and does not yet introduce a ground-evaporation requester.
Production selectors, scheduler dispatch, outputs and mutation remain
unchanged.
