# Request, Authorization, And Finalization Ledger

Status: `focused runtime PASS / independent hydrology and Rust reviews GO`

Evidence class: `Static`

The Child-2 join retains two linked surfaces:

1. the unchanged V7 constitutive request record used by the accepted
   potential/fixed-cap vegetation solve; and
2. a V8-precursor root/OFE envelope adding exact OFE, hydrology owner, source store,
   beginning-snapshot fingerprint and interval identity.

The mapping is one-to-one and amount-preserving. Requests are never aggregated
before identity validation. The real owner groups only by `(OFE, soil layer)`
against beginning `theta_m`; every authorization retains the complete requester
key. Final use must satisfy `0 <= F <= A <= D` for that exact identity.

Only finalized use enters the cloned production-layer debit. Authorization is
not a debit and unused authorization remains in the ending candidate.

The focused public case executes the real V7 potential request, one adapter
authorization, the V7 fixed-cap final solve, and the cloned production debit.
All records pass exact `F <= A <= D`; the original production owner remains
unchanged.
