# Terminal Diff Reconciliation

Status: `stable implementation bytes / terminal verification pending`

Evidence class: `Static + Ran`

The terminal Child-2 diff is limited to:

- the shared typed resource-allocation primitive and water-protocol validation;
- production-owned direct-runtime snapshot, authorization, aggregation and
  finalized-debit seams;
- the explicit default-off single-OFE V7 real-hydrology shadow bridge;
- its public integration target and tests;
- authority-impact bindings; and
- this child package's evidence.

No runner selector, default dispatch, production output, production scheduler
call site or production state publication path was added. The bridge accepts
production state immutably, stages an owned candidate, and leaves the supplied
production value structurally equal after success and failure.

The exact-terminal comparator rerun passed 507/507 orchestrator tests and all
declared focused authority, admission, strict-Clippy and hygiene gates. Routed
multi-OFE coordination, partial-frost execution, ground evaporation, LSE and a
real scheduler consumer remain outside this child and are not claimed.
