# Extraction Parity Evidence

Status: `focused runtime PASS / independent hydrology review GO`

Evidence class: `Static + Ran`

The projection is intentionally lossless at the owner boundary: the adapter
retains the complete cloned `DirectRunFrame` and ordered production layer
records. The dependency-neutral view is an indexed read surface, not a
replacement owner state.

Focused parity evidence must prove before closure:

- exact layer order and cardinality;
- every production layer field retained;
- aggregate water and transfer facts projected while frost/winter and all
  other production lineage remain protected by the full cloned frame;
- deterministic fingerprint sensitivity for each retained field;
- one-lane public execution and multi-lane source/candidate identity;
- candidate debit equivalence to a named finalized-use subtraction; and
- byte-identical original production state after success and every failure.

## Focused result

The implemented adapter retains a full cloned `DirectRunFrame`, seeds the exact
production `DirectDayFrame`, binds an exact configured source-to-vector-index
map, serializes the bounded arbitration projection, and independently
recomputes aggregate soil water after finalized debits. Thirteen crate-local
cases include reverse-lexical layer ordering, multi-lane source separation,
partial-frost rejection, reason differentiation and exact full depletion. The
three-case public contract invokes the complete V7 potential/final phase,
rejects cross-snapshot joins and proves the immutable production frame remains
equal. Independent hydrology/ownership review accepted this bounded single-OFE
parity surface; routed multi-OFE execution remains an explicit Child-4
obligation.
