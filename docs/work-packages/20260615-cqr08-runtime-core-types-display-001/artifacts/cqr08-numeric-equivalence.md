# Numeric Equivalence

Static: no numeric formula, process-physics math, runtime projection builder,
guard threshold, unit conversion, alias mapping, symbol publication, or
serialization path was edited.

Static: edited production code is limited to `HillslopeRuntimeInputError`
`code()` and `fmt::Display` helper decomposition.

Ran: focused runtime-input tests and full workspace tests passed after the
refactor.

Disposition: independent numeric reconstruction is not applicable to this
package. Numeric and runtime-seam behavior is unchanged by scope.
