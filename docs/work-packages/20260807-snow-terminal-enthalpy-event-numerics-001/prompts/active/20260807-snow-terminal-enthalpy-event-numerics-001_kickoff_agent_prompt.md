# Kickoff: Snow Terminal Enthalpy Event Numerics

Execute this package end to end in `/home/workdir/openWEPP` under the root,
work-package, standards, contract, crate, and test instructions.

Autonomy: complete every phase through disposition without asking for a next
step unless a proven boundary outside the declared write set blocks execution.
Missing receiving-surface authority limits the endpoint claim; it does not
block the isolated snow-domain enthalpy/event implementation.

Required reading:

- Core: `AGENTS.md`, `docs/codex_exec_plans.md`,
  `docs/work-packages/AGENTS.md`, `docs/work-packages/README.md`, and this
  package's `package.md`.
- Conditional: `docs/standards/kernel-work-package-preparation.md`,
  `docs/standards/testing-and-gate-strategy.md`,
  `docs/specifications/science-contract-authoring-procedure.md`,
  `docs/specifications/science-contracts/kernel-process-contract-profile.md`,
  `docs/specifications/science-contracts/index.md`, `crates/AGENTS.md`, and
  `tests/AGENTS.md`.
- On-demand: `SC-SNOWENERGY-001`, `SC-SNOWFREEZE-001`, ADR-0042, pinned CC0
  libsnobal terminal/shallow-pack source, and predecessor package evidence.

Contract-first: contracts, contract-derived tests, recorded pre-implementation
gate, then production code. No surrogate physics: implement only the
contract-backed shallow-pack enthalpy and solid-exhaustion event; do not invent
a post-snow recipient. Conservation/output acceptance: require operand lineage,
anti-tautology fixtures, rejected aliases, independent real-row reconstruction,
two-sided numerical error checks, and snow-side mass/energy/time closure. Real
consumer proof: the internal rejecting consumer must consume the event operands
and production consumers must be proven unable to read them.

Subagent authorization: this package explicitly authorizes subagent
spawning/delegation to a read-only numerical-science reviewer, primary Rust
reviewer, QA/consumer reviewer, comparator suite runner for heavy gates with
writes limited to ignored target logs, and two read-only terminal verifiers.
Expected outputs are compact findings, exact commands/counts,
convergence/closure evidence, and verification verdicts.

Required-reading byte budget and disposition are recorded in
`artifacts/required-reading-map.md`.

