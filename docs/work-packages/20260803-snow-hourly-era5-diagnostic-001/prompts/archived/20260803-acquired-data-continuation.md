# Acquired-Data Continuation

Continue the active package from authenticated local ERA5/ERA5-Land custody.
Apply the exact checksum-bound shortwave-negative disposition, complete direct
content validation and compatible grid-elevation custody, then execute the
frozen radiation-first protocol. Preserve precipitation, observations,
calibration outputs, production, tests, contracts, and predecessor evidence.

Subagent authorization: this prompt explicitly authorizes spawning/delegating
to two independent read-only reviewers and two read-only terminal verifiers for
package-local review and verification artifacts.

