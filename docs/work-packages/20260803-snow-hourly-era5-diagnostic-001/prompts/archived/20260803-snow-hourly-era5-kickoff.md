# SNOW-HOURLY-ERA5-DIAGNOSTIC Kickoff

Execute `package.md` end-to-end. Preserve precipitation, observations,
calibration outputs, production, tests, contracts, and predecessor evidence
read-only. Never print or commit CDS credentials. Stop at a truthful external
data HOLD rather than fabricating ERA5 or installing an unauthorized fallback.

Subagent authorization: this prompt explicitly authorizes spawning/delegating
to two independent read-only science/data-governance reviewers and two read-only
terminal verifiers for their named package-local artifacts.
