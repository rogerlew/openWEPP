# Review Finding Disposition

| Finding family | Disposition | Correction |
|---|---|---|
| Fail-open admission | `accepted / corrected` | The intake audit cannot emit `ADMITTED`; discovered files remain candidate-only until a separately authorized validator directly proves content and completeness. |
| Dataset/source mismatch | `accepted / corrected` | Each time-series dataset now owns its exact variables, format, grid rule, and radiation semantics; the official ERA5 link matches the selected time-series product. |
| Acquisition route | `accepted / corrected` | A fail-closed `cdsapi` site/year retrieval command is package-local and named in the handoff. |
| Comparator custody | `accepted / corrected` | Exact four-fixture hashes, row counts, hourly synthesis operator/hash, and output capacity are bound. |
| Elevation operator | `accepted / corrected` | Signed formula and dataset-specific grid-elevation prerequisite are explicit. |
| Credential wording | `accepted / corrected` | Environment credential presence uses membership checks without fetching values. |
| CSV reader mismatch | `accepted / corrected` | Frozen acquisition format is NetCDF; validated inventory, not filename/nominal reader presence, controls admission. |
| Generated bytecode | `accepted / corrected` | Package `__pycache__` was removed. |
| Live CDS product fields/semantics | `accepted / corrected` | Exact live catalogue form metadata governs: point-series shortwave and longwave are de-accumulated for both datasets, while geopotential is a separate ancillary. |
| ERA interval labels | `accepted / corrected` | De-accumulated preceding-hour radiation is labeled `valid_time - 1 h` before fixed-local alignment. |
| Shortwave plane mismatch | `accepted / corrected` | Horizontal daily comparison is primary; slope/aspect-transformed hourly SIMIMPL comparison is geometry-confounded chronology evidence only, and the former `+84%` Snowbird magnitude claim is withdrawn. |
| Peak-time semantics | `accepted / corrected` | The implemented integer operator is transparently named fixed-local-standard; no local-solar claim remains. |
| Figure determinism and populations | `accepted / corrected` | Fixed SVG date metadata produces byte-identical regeneration; exact plotted values and counts are present in sidecars and machine data. |
| Figure non-color encoding | `accepted / corrected` | Hatches, filled/open markers, and circle/X product markers supplement color. |
| Figure semantic validation | `accepted / corrected` | Validator reconstructs every plotted operand/count from the result receipt and enforces exact paths, bindings, accessibility metadata, sidecar sections, and lane limitations. |

Final disposition: both independent fresh re-reviews `PASS`; all accepted
findings are corrected and no finding is deferred or left open.

Acquired-data addendum: both fresh shortwave-disposition re-reviews `PASS`
after exact cohort binding, rule propagation, authority-inference labeling, and
custody/lifecycle reconciliation. Earlier no-data reviews remain historical.

Radiation/figure addendum: both fresh result reviews and both focused figure
re-reviews `PASS` after all accepted interval, plane, peak, determinism,
population, accessibility, and validation findings were corrected. No figure
finding is deferred or open.
