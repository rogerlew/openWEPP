# Terminal Verification Agent B

Ran: final exact-current verification `PASS` for the achieved
`EXECUTED / HOLD / EXTERNAL DATA` claim.

The live CDS catalogue forms support all requested fields, including
de-accumulated ERA5-Land downward longwave, and neither point product offers
geopotential. Manifest/receipt identity, 2-dataset/4-site/8-field/24-hour
inventory, comparator/runtime hashes and line counts, Python/JSON/Markdown/diff
checks, fail-closed receipt, protected paths, prompt archive, handoff, separate
ancillary/validator boundary, and artifact hygiene passed.

No substantive finding remains.

Acquired-data addendum: initial shortwave-disposition verification reproduced
all hashes and numerics but returned `HOLD` for stale lifecycle/source framing.
The no-data result above is historical; fresh exact-current verification is
pending after correction.

Ran: final exact-current acquired-data verification `PASS` for the bounded
shortwave-negative disposition. Exact cohort/numeric reconstruction,
unresolved-origin framing, manifest/audit identity, rule boundaries, lifecycle,
and mechanical gates pass. Full attribution remains HOLD.

Ran: final exact-current radiation-first verification `PASS`. All eight primary
horizontal daily and wet-winter lanes independently reproduce to less than
`1e-12`; interval-start labeling, fixed-local peak semantics, 8/9-hour boundary
exclusions, withdrawn plane-confounded `+84%` claim, accepted Snowbird
`+28.532%`/`+28.392%` results, longwave limitations, `DIVERGES` posture, and
no-provider/no-snow claims pass. No finding remains.

Ran: final exact-current radiation-figure verification `PASS`. Four SVGs, four
same-stem Markdown sidecars, and the exact plotted-data JSON reproduce
byte-identically and match every manifest hash. Each figure contains eight
result-derived product/site rows; populations, accessibility metadata,
non-color encodings, lane limitations, review disposition, and terminal hygiene
pass. No finding remains.
