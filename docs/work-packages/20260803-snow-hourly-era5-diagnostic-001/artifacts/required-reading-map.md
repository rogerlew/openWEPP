# Required Reading Map

Ran: `tools/agents/find-agents --for` resolved the following instruction chain
for every declared write path before edits:

1. repository-root `AGENTS.md`;
2. `docs/work-packages/AGENTS.md` for the package tree and catalog;
3. root `AGENTS.md` only for `docs/ROADMAP.md` and
   `docs/planning/snow-surface-energy-balance-roadmap.md`.

No production, Rust, test, or science-contract file is in the terminal diff.

