# Gate Results

Ran: the exact pass-receipt and fail/blocked-receipt regressions pass 2/2 in
158.090 seconds.

Static: only both fixture definition IDs and the dependent fixture's exact
prerequisite ID changed. Receipt counts, results, blocked reason, artifact-root,
and reconstruction-cleanup assertions remain unchanged.
