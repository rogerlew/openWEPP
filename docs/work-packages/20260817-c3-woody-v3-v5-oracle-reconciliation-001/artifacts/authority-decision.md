# Authority Decision

Evidence class: `Static + Ran`

Verdict: `PASS / prospective V9 authorized / package and Child 3 remain HOLD`

Independent science review selected a prospective `OPENWEPP_C3_WOODY_V9`
identity. V3--V8 calculators, vectors, definitions, digests, fixtures, and Rust
expectations remain immutable historical authority. Their release sources did
not drift; their exact historical numerical environment is unavailable; and
the numeric-only same-branch differences provide no scientific basis for
choosing current regenerated bytes over frozen bytes.

V9 must import exact V8 physics and supersede only the oracle numerical-runtime
and serialization identity needed for future reproducible release provenance.
It requires an explicit V8-to-V9 migration, no historical aliases, a
content-addressed environment descriptor, a non-Rust independent calculator,
new vectors and definition, two isolated byte-identical generations, and proof
that generator tests never mutate repository authority bytes.

After V9 authority and implementation pass, closure still requires focused
authority and anti-evasion gates, a clean full workspace, all seven corrected-
litter Child-3 benchmark surfaces, fresh exact-byte Rust and science reviews,
and both terminal verifiers. Close this package before Child 3.
