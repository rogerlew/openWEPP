# Heavy Runner Fallback

Evidence class: `Ran`

The package-required `comparator_suite_runner` was dispatched for the clean
full-workspace campaign and seven Child-3 benchmark surfaces. Its fixed model
returned `Selected model is at capacity` twice, including one explicit retry,
before executing commands or writing evidence. Under prompt-wording guidance,
the primary agent therefore executes the same direct commands locally and
preserves raw package logs. This is a tooling-capacity fallback, not a waived,
deferred, narrowed, or passed gate.
