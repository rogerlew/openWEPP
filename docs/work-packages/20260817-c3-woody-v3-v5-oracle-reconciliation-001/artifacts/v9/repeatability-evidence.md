# V9 isolated generation evidence

Evidence class: `Ran`

The prospective V9 generator was executed twice with the exact interpreter,
locale, epoch, calculator, definition, and runtime descriptor identified by
this directory. Each execution used a separately created `/tmp` working
directory and wrote only redirected stdout outside the repository.

```text
LC_ALL=C.UTF-8 SOURCE_DATE_EPOCH=0 /usr/bin/python3.14 -I -S -B reference_calculator_v9.py
generation A sha256: f86770cce11235ba282b47e81de2fa5dc9af19c29dc3bd91c62256957c590633
generation B sha256: f86770cce11235ba282b47e81de2fa5dc9af19c29dc3bd91c62256957c590633
byte length including LF: 1105
cmp result: byte-identical
```

The generated bytes equal `openwepp_c3_woody_v9_vectors.json`. The imported
exact V8 full execution is canonicalized before hashing: 186,709 bytes with
SHA-256
`e9a53d6d15cb04136683c24d5b7ebd4437eb26569cf984b5cecfb5e47d73b416`.
The concise V9 vector retains exact hashes for every top-level scenario family,
so any imported equation result, diagnostic branch, or serialization change
changes prospective authority without rewriting a historical artifact.

Before/after SHA-256 manifests over every file in the V3--V8 authority-package
trees were byte-identical. No CPython 3.14 bytecode was created in those trees.
The generator requires `-I -S -B`, validates the exact executed command and
serializer, and verifies its own source digest, definition/runtime cross-links,
the 1,311-file Python standard-library closure, all extension/transitive shared
objects, and all locale/OpenSSL runtime files declared by the descriptor.
It also fail-closes on the exact CPU vendor, family, model, stepping, complete
flag string, and Linux `AT_HWCAP`/`AT_HWCAP2` values that govern glibc IFUNC and
hardware-selected floating-point execution. This is exact-host reproducibility;
execution on a different CPU feature identity is intentionally unsupported.
