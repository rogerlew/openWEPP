# Kickoff: C3 Woody V3/V5 Oracle Authority Reconciliation

Scope: local repository science-contract and oracle-authority work; flat-file
reads/edits only; no external connectivity.

Execution mode: package-end-to-end.

Execute every phase in `package.md` sequentially through truthful disposition.
Begin contract-first: do not edit calculators, frozen fixtures, definitions,
or digest bindings until the authority decision and dependency graph are
written and independently reviewed. Never widen exact regeneration or generate
authority expectations from Rust. Preserve isolated nonmutating generator tests.

Required reading:

- Core: root `AGENTS.md`, `docs/work-packages/AGENTS.md`,
  `docs/codex_exec_plans.md`, this package, and
  `artifacts/required-reading-map.md`.
- Conditional: science-contract authoring procedure, kernel contract profile,
  testing strategy, and authority anti-evasion requirements before any
  contract or fixture edit.
- On-demand: C3 V3--V8 definitions, calculators, vectors, migration ledgers,
  and the Child-3 terminal HOLD evidence for the exact mechanism being traced.

Required-reading budget: `WARN / measure exact bytes during intake`; keep
large successor artifacts on-demand and justify any promotion to Core.

Subagent authorization: this prompt explicitly authorizes subagent
spawning/delegation to read-only science and Rust reviewers, bounded
package-artifact comparator runners, and read-only terminal verifiers for the
roles and outputs named in `package.md`.

Run direct gates selected by `docs/standards/testing-and-gate-strategy.md` and
preserve raw evidence. A clean full-workspace correctness gate is mandatory;
the package and Child 3 remain HOLD on any failure.
