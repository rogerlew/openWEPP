# Final disposition

Status: executed / HOLD

Evidence mode: Static + Ran

The contract-first increment landed locally as `83cf6eb8e`. It admits a fresh,
default-off snow-terminal receipt, half-open receiver support, exact-one liquid
custody, variable-duration shared WB14 execution, restart state, error
precedence, and whole-owner rollback. Focused contract tests passed 12/12 and
real shared-WB14 duration tests passed 2/2 under `nix develop`.

The package is HOLD before production implementation. Final consumer tracing
proved that V8/V9/V10 vegetation integrates carbon, water, hydraulics,
phenology, interception, and T10 using the configuration-owned `dt_s`; receipts
and state/configuration hashes require the same exact duration. The admitted
terminal transaction instead requires execution over `dt_remaining`.
`SC-VEGETATION-001` contains no separate transaction-support authority.

Independent snow/LSE science and hydrology/ownership reviewers both returned
HOLD on this exact contradiction. A mechanical override would either
over-advance vegetation, rekey the owner, or scale finalized nonlinear state.
All are prohibited. Authority must also decide the pre-event chronology:
freezing vegetation under snow leaves an explicit time gap, while evolving it
requires the unresolved snow/canopy turbulent carrier and forest-exposure
operator.

No production Rust path was edited. The actual scheduler does not consume the
terminal event; the external Stage 3 rollback gap remains; CoE remains the
sole production snow/melt owner. No selector, default, schema, WAT/HBP/PASS,
output, assurance, efficacy, ownership, or cutover claim is made.
