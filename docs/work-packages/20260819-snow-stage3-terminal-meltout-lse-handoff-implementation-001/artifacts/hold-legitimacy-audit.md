# HOLD legitimacy audit

Status: legitimate authority boundary

Evidence mode: Static

The observed blocker is not implementation effort. `SC-VEGETATION-001` makes
`dt_s` part of exact configuration identity and V8/V9/V10 inherit that
configuration and algorithm. Runtime carbon/final receipts reject any other
duration, while carbon, water, hydraulics, phenology, interception, and T10
materially consume the duration. The terminal contracts require those owners
to advance only on `dt_remaining`.

In-envelope correction routes considered:

1. Run nominal 1800 seconds: rejected because it over-advances state beyond the
   receiver support.
2. Scale full-step fluxes or amounts: rejected because the coupled owners are
   nonlinear and canonical contracts prohibit proxy physics.
3. Clone configuration with `dt_remaining`: rejected because it creates a new
   configuration/state owner and invalidates exact hashes without migration.
4. Leave vegetation frozen before `t*`: rejected as an unreviewed scientific
   choice and an unexplained vegetation time gap.
5. Advance vegetation before `t*`: blocked on the unresolved snow/canopy
   turbulent carrier and forest-exposure authority.

The same condition received independent science and ownership HOLD verdicts.
Meaningful progress cannot continue without a canonical scientific choice that
is already assigned to campaign Child 2. Production edits stopped before the
violation. The accepted contract checkpoint remains reusable evidence, not a
claim that the real scheduler handoff exists.
