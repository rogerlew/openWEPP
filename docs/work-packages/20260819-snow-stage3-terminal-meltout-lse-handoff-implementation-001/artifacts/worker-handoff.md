# Worker handoff

Status: HOLD handoff

Evidence mode: Static

First actionable item: in
`SNOW-STAGE3-TURBULENT-CARRIER-AUTHORITY-CLOSURE`, decide and canonically bind
the snow-covered vegetation chronology before `t*` and the partial-support
vegetation chronology after `t*`.

Required authority must amend `SC-VEGETATION-001` to distinguish immutable
nominal `configuration.dt_s`/digest from a separately fingerprinted
`transaction_support_s`, bind every duration-sensitive V10 operation and
receipt to the same support, preserve configuration/state identity, define
zero-remainder skip, restart/replay/rollback, and prohibit nominal-step reuse,
proportional scaling, or configuration rehashing. It must explicitly decide
whether pre-event vegetation is dormant/censored or evolves with an admitted
snow/canopy turbulent environment.

Trigger to resume this package: dual-reviewed contract authority and executable
unequal-duration, full-step-poison, hash, zero-remainder, restart, and
pre-event chronology tests. Resume from contract checkpoint `83cf6eb8e`.

Protected boundary: CoE remains production owner; no Stage 3 receiver runtime,
selector/default/output change, turbulent efficacy, forest applicability,
qualification, assurance approval, or cutover is authorized.
