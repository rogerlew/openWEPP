# Worker Handoff

Status: `READY`
Evidence mode: Static.

## Recommended Next Package

Scaffold a narrow target-`dx5` promotion hold-lift package only if the operator
wants to continue target-`dx` promotion.

First actionable tasks:

1. Close the `mn_corn_h4` fine-reference adequacy blocker by running a further
   `dx0p625` reference or by explicitly amending the adequacy rule under
   contract review.
2. Price and optimize the `dx5` cost burden on the real cohort, especially
   `n_idaho_forest_h1` and `wa_cascades_forest_h1`.
3. Re-adjudicate whether `dx5` fidelity improvement over fixed10 justifies a
   production default cost increase.

## Current Posture

- Production active mesh remains fixed `10 cells/OFE`.
- `dx5` is provisional evidence only.
- Fixed10 is operational default, not fidelity-ratified by this package.
- Do not alter shadow mesh policy unless a separate package scopes it.

