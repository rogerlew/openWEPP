# Codex Execute Prompt

Execute
`docs/work-packages/20260708-laned-router-tier2-dx-target-mesh-policy-readjudication-rev41-001/`.

Read `package.md`, package governance, `SC-OFEROUTE-001`, the prior Tier-2
hold package, and the rev-41 WA solver-correction package. Rerun the full
selected-cohort target-`dx` mesh ladder on current `main`, adjudicate fixed10
and target-`dx` rungs against the rev-39 surfaces on the rev-41 solver, and
close the package as complete or held with direct evidence.

Subagent authorization: this package explicitly authorizes
spawning/delegation to comparator/timing, review, verification, explorer, and
bounded worker subagents as described in `package.md`.

