# Operand Lineage

Status: `EXECUTED-PASS-BEFORE-PRODUCTION`

Evidence mode: `Static` from `SC-ROUTE-001` v53, W11A disposition, current
typed frame, and pinned baseline source.

| Field | Units/grid | Normalization and basis | Authority -> consumer | Status / rejected aliases |
|---|---|---|---|---|
| `V_h` | m3/hour slot | Hour-integrated water, uniform inside its hour | HBP minor-1 -> exact-overlap grid projector | authoritative; reject event peak/active-span reconstruction |
| `S_h` | kg/hour slot | Hour-integrated total sediment | HBP minor-1 -> exact-overlap class projector | authoritative total timing; reject daily mass smearing |
| `f_k` | fraction/day | Normalize the validated day-level class blend once | HBP class payload -> interval class ingress | authoritative limited blend; never claim enriched hourly composition |
| `dtchr`, `ntchr` | s, count | Positive; exact `ntchr*dtchr=86400` | chan/global water authority -> interval router | authoritative grid; reject hour-as-solve-quantum and non-covering grids |
| `qin(it)` / `qt(it)` | m3/s/interval | Top inflow from upstream same-grid egress | dependencies -> profile Eq. 13.5.6 | authoritative; reject event inlet peak |
| `q1(it)` / `qe(it)` | m3/s/interval | Routed outlet anchor | WS11 wave router -> segment profile | authoritative; reject `qin`-anchored profile |
| `qlat(it)` | m3/s/interval, whole reach | Total lateral water; used only in effective-length partition | projected local water/wave state -> `leff` | authoritative total; reject per-length solve use |
| local baseflow | m3/interval/day | Only groundwater generated by this channel step enters local `qlat`; cumulative upstream baseflow is already inside dependency `q1` | groundwater authority -> interval water router | authoritative external source; reject downstream re-addition of dependency baseflow |
| `leff(it)` | ft | `lc*(1+qt/qlat)` after unit bridge when lateral is positive; named no-lateral branch otherwise | Ch.13/pinned `chnrt` -> profile | authoritative |
| `qlat_eff(it)` | ft3/s/ft | `qe/leff`, derived after SI-to-English bridge | profile assembler -> WS20 per-length slot | authoritative; reject raw `qlat`, `qlat/lc`, event-peak fractions |
| interval inlet/lateral mass | kg/class/interval | Exact overlap of `S_h`, multiplied by `f_k` | projector/upstream egress -> segment solve | authoritative; reject scalar compatibility state |
| `t_exp(it)` | s/interval | `dtchr` above gate, zero below; contact splits `timpot/timex` | INV-018 -> every legacy `timsh` role | authoritative; reject triangular/event duration |
| `t_norm(it)` | s/interval | exactly `dtchr` | INV-018 -> every legacy `tb` denominator | authoritative; reject `2*dtchr` |
| `depa/depb,wida/widb,wera/werb` | ft/point | six distinct carriers in interval order; non-refill/non-narrow; explicit day tillage authority for the sole `ishape=3` reseed | run state/pinned `dcap` -> next interval/day | authoritative geometry; no event/day reset or hydraulic/eroded-width alias |
| class detachment | kg/class/interval | DCAP upper/lower class rates integrated over solved Case II/III/IV/ENDDET spatial spans and `dtchr` | segment process -> daily ledger | authoritative gross constructive operand; never derived as signed closure residual |
| class deposition | kg/class/interval | `inlet + lateral + constructive detachment - egress`, fail-closed if materially negative | continuity -> daily ledger | authoritative gross deposition; may coexist with gross detachment |
| water storage | m3/day/interval ledger | `qt+qlat-q1` totals integrated on water grid | water router -> water closure | authoritative water-only; never a suspended sediment pool |

Anti-alias fixtures must make event peak, raw total `qlat`, `qlat/lc`,
`qe/leff`, daily mass, active span, and scalar compatibility candidates all
distinct. Acceptance reconstructs closure from typed produced interval fields,
not by restating a producer total.
