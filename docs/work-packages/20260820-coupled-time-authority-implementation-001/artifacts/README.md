# Coupled-time package artifacts

All evidence is queued and not run at scaffold time. Replace placeholders with
truthfully labeled `Static:` and `Ran:` evidence as phases execute; never erase
historical failures or rejected attempts.
