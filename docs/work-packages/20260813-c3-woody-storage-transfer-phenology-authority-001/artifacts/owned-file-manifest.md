# Owned File Manifest

Owned authority writes are the V7 package tree, `SC-VEGETATION-001.md`, its
registry row, the V7 canonical model-stack definition copy, the work-package
catalog entry, and the vegetation authority integration test. Production Rust
and the executable model registry are excluded. The terminal gate exposed two
pre-existing warnings-denied `assigning_clones` findings in the adjacent C3
implementation integration test; the package owns only their mechanical
`clone_from` correction, with no semantic or assertion change.
