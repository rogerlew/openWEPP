# V7 Authority Evidence

This directory binds the narrow `OPENWEPP_C3_WOODY_V7` storage-transfer
phenology amendment. The JSON definition and vectors are immutable authority
inputs after release; the Python calculator is the independent executable
oracle. Review, verification, and gate artifacts record lifecycle evidence.

