# Problem Statement

Static: V6 E19 credits `(1-f_cur)` current growth to each tissue storage pool,
while E20 onset deployment consumes each tissue transfer pool. No admitted
equation connected those owners. `GAP-VEGETATION-027` therefore required
`VEG-E-060` instead of a copy, rename, or inferred annual rollover.

V7 closes only that authority gap. It does not implement persistent C/N
mutation, publish a candidate, activate a selector, or claim E01--E22
completion.

