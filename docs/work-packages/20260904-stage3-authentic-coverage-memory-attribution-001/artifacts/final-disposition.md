# Final disposition

Status: `EXECUTED — TERMINAL HOLD / V31 DISCRIMINATOR CLOSED; V61 HISTORICAL EVIDENCE LIMITATION`

Evidence mode: `Static + Ran`

This package completed its authorized diagnostic scope without changing the
predecessor's terminal HOLD or editing retained Rust source.

## Decision-ready answers

1. **Revision-31 coverage.** Static authority confirms the authentic release
   fixture is N=2 occupancies and S=6 soil/thermal layers, with the exact
   `58/14/16/28` completed-sweep target. The strongest limiting predicate found
   is the recovered candidate's centered signed-stencil guard over closed beta
   and temperature domains; the authenticated seed begins on beta=1.0 and
   sun/shade/wet temperature=273.15 K boundaries (dry-stem 273.15 K is
   interior to its 200–350 K domain). The exact historical binary was executed:
   its focused real-runner replay/parity test passed, while the authentic
   release profile reproduced the `component_replay_audit.rs:131` assertion.
   The exact binary was then observed in a debugger at the audit-return
   boundary, before `aggregate_component_replay_audit`. All 2,000 authentic
   N=2/S=6 sweeps were decoded: 200 rows were `48/14/10/24` and 1,800 were
   `54/14/16/24` (logical/identity/component/complete), with zero target rows.
   Every captured sweep was completed and not failed. The first captured
   stencil had ten inward one-sided coordinates and iteration 2 had four;
   each omits one signed probe while preserving the closed-domain guard. The
   runtime discriminator is therefore boundary-aware one-sided stencil
   selection producing the logical/complete probe shortfall, not collector or
   lifecycle loss. A second lifecycle capture confirms the authentic audit is
   distinct from the forced-complete oracle's later audit. No assertion bypass
   or binary mutation was used. Exact-HEAD, a28, and ordered session-patch
   recoveries did not produce an equivalent candidate executable.

2. **Authentic workload coverage.** The real retained path is terminal/provider
   -> strict V8 projection -> native V3 adoption -> canonical covered solver ->
   transaction publication. The admitted control exercises one lane, two
   vegetation strata, six soil layers, one day, 48 parent supports (44
   snow-free/4 covered), 20 direct trials, 32 split-child trials, 4 accepted
   microsteps, and 56 accepted publication supports. Retained source contains
   no component-replay implementation or published replay counter; therefore
   replay is not applicable to the current implementation. The historical
   focused diagnostic demonstrates positive replay coverage and bit-identical
   forced-complete output, but its exact sweep counts were not serialized. The
   lawful two-OFE bootstrap proves topology only and was not promoted as
   replay-positive.

3. **Memory semantics and attribution.** Historical `rss_kib` is the first
   numeric `VmRSS` field from `/proc/self/status`, sampled while JSON/report,
   telemetry, output, and allocator/mapping state remain live and before fixture
   cleanup. It is a point sample, not peak RSS, HWM, heap bytes, or retained
   allocation. The admitted same-binary R1 control has endpoint medians A
   70,850 KiB and B 70,556 KiB (paired median −298 KiB; 4/12 positive), while
   observer sampled HWM median is 75,664 KiB and all 12 paired wall-time deltas
   are positive (median +577,643.5 us). Historical baseline and revision-61
   triples overlap (`59,364` vs `69,768` KiB medians), are unpaired, and have no
   HWM/smaps/heap/cleanup custody. No causal revision-61 attribution is
   supported. A manually repaired detached v61 tree did complete the real
   one-OFE consumer with 200 feed-forward calls and exact closure (raw
   `v61_manual_reconciled_release_probe.log`, SHA-256
   `dbced82c…9955c1`; the 13-row ordered changed-Rust hash stream is
   `raw/v61_manual_candidate_changed_rust_hashes.txt`, SHA-256
   `229b17c644b1e54dbbb9e836752f5f499bb7b6477171d126339dbf879a392348`.
   Its documented changed-Rust manifest is
   `19a07121…23421` (an earlier undocumented canonical computation recorded
   `2fc5e8ca…e6eb7f`), but neither is comparable to the historical
   `650f6713…57d41` digest because the historical Git-index/dirty-worktree
   state, per-file manifest, and candidate binary are not custodied. It is
   diagnostic-only, not a matched treatment.

4. **Salvage value and gates.** Neither rejected mechanism merits a successor
   increment now. A future successor would need a test-only audit at the real
   solver consumer that records exact signed stencils, eligibility, lifecycle,
   completion/error, and dropped-event reconciliation; a lawful positive
   fixture if the original workload is negative; an equivalent source/binary
   candidate with matched fresh-process A/B treatment; endpoint VmRSS plus
   sampled-HWM/smaps/private/cleanup observations; exact science/output parity;
   three fresh release repeats; and the applicable focused/full correctness
   gates. No production replay activation, solver change, tolerance change, or
   acceptance-rule change is authorized by this package.

## Evidence and truthful gate posture

The comparator role failed before execution because its fixed-role service hit
the GPT-5.3-Codex-Spark usage limit. Parent fallback ran the bounded gates
applicable to the final documentation/raw-only retained diff: `git diff --check`,
JSONL/matrix parsing, shell syntax, format check, scoped retained-tree Cargo
check, the authentic release probe, the admitted R1 paired control, and the
isolated v61 candidate `cargo check --tests`. The exact historical v31 binary
also passed the focused diagnostic, reproduced the release assertion, and
yielded the debugger audit-return/lifecycle captures described above. The
manually repaired v61 detached tree compiled and completed the real consumer,
but the reverse-cleanup reconstruction attempt also failed to yield a complete
reversible source snapshot and the detached source/index custody is not the
historical measurement state. It was therefore not treated as a candidate
treatment. The reverse-cleanup transcript is retained under
`raw/v61_reverse_cleanup_reconstruction_verification_b.log` (SHA-256
`f79dcd05571582bec7695020d6f845de6b687c257bfb58b3dd20437a13311a3c`). Authority
anti-evasion/AUTH11 were not applicable because no authority or fixture binding
changed. The predecessor's expected-red heavy results remain unchanged.

## Disposition

**TERMINAL HOLD — no qualifying salvage and no successor authorization.** The v31
runtime discriminator is closed by direct pre-assertion debugger evidence. The
revision-61 memory cause remains an evidence limitation: its historical triples
are unpaired endpoint samples, the admitted R1 control measures observer
perturbation only, and no authenticated equivalent candidate source/binary or
matched treatment was recovered. Fresh independent reviews and terminal
verifications approve this evidence boundary and keep the package in terminal
HOLD rather than GO. The predecessor package remains terminal HOLD/nonqualifying.
