# Independent terminal review A

Status: **PASS / TERMINAL HOLD LEGITIMATE**

Evidence mode: `Static + Ran`

Frozen evidence cut: 171 package files, excluding the two review and two
terminal-verification artifacts. SHA-256 over the lexically ordered
`sha256sum` stream is
`7fe4c52a8f73d8264e0494be449d846f6a5502915025582446b586593c375efa`.

## Verdict

**PASS. The package's terminal disposition is HOLD, not GO.** The corrected
evidence closes the exact revision-31 runtime discriminator and exhausts the
available revision-61 historical-source recovery routes to a technically
specific identity boundary. Neither rejected mechanism is qualified, the
historical v61 RSS contrast has no supportable causal attribution, and no
successor should be authorized.

No evidence-artifact blocker remains in this review. The prior v31 iteration
misstatement, ambiguous sweep-versus-target completion field, undocumented
v61 digest rationale, and missing 13-row diagnostic hash stream are corrected.
The reverse-cleanup experiment also closes the remaining claim that the
successful cleanup patches could mechanically recover the historical v61
source cut. The refreshed memory-attribution artifact now cites that 13-row
stream explicitly and preserves the noncomparability rationale: neither
detached-tree digest is promoted as a test against the opaque historical
dirty-worktree aggregate.

The other current assurance artifacts already return PASS on the same
substantive evidence, but their recorded evidence identity predates the final
terminal-status wording edits. They must refresh that identity to this exact
cut for fully synchronized closure. That is a bookkeeping step, not a defect
in the terminal-HOLD rationale reviewed here.

## Findings

No blocking or nonblocking evidence findings.

## Revision-31 discriminator

**Closed by direct runtime evidence.** The exact historical executable hashes
to `f9386eec584664f9639da281c15796730240239cd43ad2f158f4fa6d27fbeeaf`.
The debugger stops at the return from
`take_covered_component_dependency_replay_audit`, before aggregation and the
release assertion, without modifying the binary or bypassing the assertion.

Independent parsing of the returned audit found exactly 2,000 contiguous
N=2/S=6 rows across 400 maps and five iterations per map. Every row satisfies
`logical = identity + component + complete`, `collecting=1`, `completed=1`,
and `failed=0`. The exact distribution is:

- 100 iteration-0 and 100 iteration-1 rows for map IDs 0..99 are
  `48/14/10/24`;
- 300 iteration-0 and 300 iteration-1 rows for the remaining maps are
  `54/14/16/24`;
- all 1,200 iteration-2/3/4 rows are `54/14/16/24`;
- no row has logical count 58, complete count 28, or tuple `58/14/16/28`.

The corrected summary now states those memberships exactly and labels its two
29-coordinate stencil vectors as representative captures. Recovered source
maps the displayed nonzero values to inward one-sided finite differences. The
48-count vector has ten one-sided coordinates: four beta-upper and six
sun/shade/wet-temperature lower-bound coordinates. The 54-count vector has the
four beta-upper coordinates. The bucket accounting consequently matches
`14+10+24=48` and `14+16+24=54`.

Because occupancy and soil counts are already 2 and 6, the first false
conjunct in the release assertion is `logical_probe_count == 58`. The complete
and failed flags exclude lifecycle failure as the cause. The separate
lifecycle capture records authentic begin/take 1, forced-complete-oracle
begin/take 2, then aggregate after the oracle returns, excluding collector
loss and audit/oracle conflation. The observed boundary-aware one-sided
stencils therefore provide the source-real runtime explanation for the signed
probe shortfall.

## Revision-61 historical-attribution boundary

**Terminal HOLD is legitimate.** The original broad replay and corrected
pre-cleanup cut replay failed mechanically, but manual reconciliation then
produced a compiling detached implementation. Its real one-OFE consumer passes
with 200 feed-forward calls, protected `48/56/20/32/4` counters, and exact
source/outlet/storage/clamp closure. This proves that implementation effort or
patch context is not the asserted terminal boundary.

The package now preserves the exact 13-row changed/untracked-Rust hash stream
for that diagnostic tree. Independent recomputation gives
`19a07121e6ecdfd8e1b0bd559404bcc69554943ef5c1d68b5d53d91534623421`,
matching the artifact. The earlier `2fc5e8ca…e6eb7f` value is retained only as
an explicitly undocumented computation; it is not used as an equivalence
test. The diagnostic binary independently hashes to
`d71fcef567e02ad1a06d5268bfee20dd84c4e751785f5ec4d4798dbe463ce3bb`.

The historical evidence preserves aggregate source digest
`650f67132aec95818228c1f3ed85db7310a7e66671dcb5f94cb18948ae257d41`
and binary digest
`e6b57efad9121915e95d45fa85323dfd29e9b5b8c8201f6356a2c6a10ea2df65`,
but not the measured dirty-worktree path/hash stream, its per-file source/Git
index bytes, or the executable bytes. The diagnostic and historical aggregate
digests cover unprovably different domains, so their inequality is correctly
not presented as an equivalence discriminator.

The reverse-cleanup route supplies the remaining negative feasibility proof.
Independent session parsing confirms cleanup calls 41,42,43,45,46,47,48,49,
50,52,54,55,56 succeeded originally while 44,51,53 failed. Reversing the 13
successful calls from exact retained HEAD yields 12 applied inversions and one
formatting-context failure. The resulting 11-path tree hashes to
`475580610f492e4f92781cacae3660a9d9dad08dc9cd7c3ccc98307812516c42`
and fails the scoped Cargo check with the recorded 10 errors: two misplaced
module-scope field/initializer errors, two missing binding/export errors, and
six absent request-field errors. The cleanup deltas omit placement context and
candidate-side edits; they are not a reversible full-source snapshot.

Manual choices can create a coherent new diagnostic candidate but cannot
authenticate the missing historical bytes, build identity, allocation
behavior, or memory lifetime. Exact historical attribution therefore requires
newly supplied historical source/binary bytes. A fresh nonhistorical mechanism
study would be a separately frozen matched experiment, not completion of the
historical treatment. This is a valid terminal evidence boundary under the
package acceptance rule.

## Memory and science controls

The admitted R1 same-binary control remains internally complete:

- 26 result rows comprise two warmups and 12 balanced A/B blocks; all 24
  admitted arms exit zero and retain exact science, closure, and
  `48/4/44/20/32/4/56` counters;
- the 12 endpoint-RSS B−A deltas have exactly 4/12 positive values and paired
  median `-298 KiB`;
- all 12 wall-time B−A deltas are positive, with minimum `+48,930 us` and
  paired median `+577,643.5 us`;
- historical `rss_kib` is a late `/proc/self/status` `VmRSS` point sample
  before cleanup, not peak RSS, heap bytes, allocator return, or HWM;
- median `75,664 KiB` is the maximum successfully sampled kernel `VmHWM` and
  only a lower bound on any later process-lifetime peak.

The observer is therefore a measurable timing perturbation, not a v61
treatment. Historical baseline/candidate endpoint triples remain unpaired,
overlapping, and without HWM/smaps/heap/cleanup custody. No causal memory claim
is warranted.

## Exact evidence identities

| Evidence | SHA-256 |
|---|---|
| `historical_v31_gdb_take_return.gdb` | `b0cf2e8b157536d894e475930428ca31dbc6ca80fc5c16e544c5806900235eb8` |
| `historical_v31_gdb_take_return.log` | `1aa29a99c6dc993906fea9fcc2883ef939ba99f05a4fcd67d94297e346425877` |
| `historical_v31_gdb_take_return_summary.txt` | `73c0d88bfa476592ed53826a6a133f5961eb28508eecb7144edb5557a95cf366` |
| `historical_v31_gdb_lifecycle_counts.log` | `07bf092e930de5c083b11ceb4178e3e480505a81b24a4f43e4b963db84710c76` |
| `v61_manual_candidate_changed_rust_hashes.txt` | `229b17c644b1e54dbbb9e836752f5f499bb7b6477171d126339dbf879a392348` |
| `v61_manual_reconciliation_feasibility.log` | `b1d62f645de1e7268263a0d316b283c74b29d94fa3733f13d295b941b6b1ea55` |
| `v61_manual_reconciled_release_probe.log` | `dbced82c9d15ad63faa652e2bdf609b90a9a7d241b02fea845e5b634fb9955c1` |
| `v61_reverse_cleanup_reconstruction_verification_b.log` | `f79dcd05571582bec7695020d6f845de6b687c257bfb58b3dd20437a13311a3c` |
| `current_memory_results.jsonl` | `08aa5e5c1282a22e0eceffb40c6fa24ad880bed8b8dcc52d9aef3ab9720b08d0` |
| `current_memory_samples.csv` | `0a7aa5cf742333de2b48c03b7ad5c8183b5e02621142ae3dd7dbe64cd3f7be18` |
| `current_memory_manifest.jsonl` | `7ef7ba6c68330485bcd5b946ec4242f886285dfe4594a611d8145b97d766b320` |
| `memory_reconstruction_summary.json` | `7aa1e6b5a1d6d4bffe41e55e72c57ce242070782d3c942de403c59a05d632f46` |

## Reviewer-run checks

- **Ran:** parsed all 2,000 GDB sweep rows; recomputed topology, map/iteration
  frequency, lifecycle flags, target absence, and bucket identities.
- **Ran:** rebuilt the 13-row detached diagnostic hash stream and independently
  obtained `19a07121…23421`; rehashed all exact evidence listed above.
- **Ran:** independently reconstructed original cleanup call outcomes from the
  source session and confirmed the reverse experiment's 13/12/1 accounting.
- **Ran:** recomputed the reverse tree's 11-path digest as `47558061…6c42` and
  reran `cargo check -p openwepp-runner --tests`; it reproduced exactly 10
  source errors and exit 101.
- **Ran:** parsed the coverage matrix and memory JSONL, and ran a whitespace/
  diff check on this review artifact. No retained Rust, authority, or package
  artifact other than this review was changed.

Final recommendation: **APPROVE TERMINAL HOLD / NO GO / NO SUCCESSOR**. After
the remaining assurance artifacts refresh their evidence identity to this
frozen cut, close the package without restoring either rejected mechanism.
