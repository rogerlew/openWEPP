# CAL-07D Finding Disposition

Evidence class: `Static + Ran`

## Prospective review

Both reviewers initially held execution. All findings were accepted and
resolved before result-bearing analysis:

- source date-10/date-25/date-50 levels and raw/smoothed support were retained;
- falling source levels were mapped as remaining normalized state (`q=p`),
  consistent with their date order;
- one event-year threshold was held constant over each austral event window;
- crossing inequalities, interpolation, selection, unmatched rows, and
  inventories were frozen;
- CP-GSI01 equations, signed photoperiod, leap-day denominator, FIFO cold
  start, chronology, and scenario isolation were fully specified;
- causal/dominance labels were replaced by machine-readable contributor and
  plausibility predicates;
- exact source hashes, all-crossing keys, figure requirements, independent
  validation, and evidence routes were frozen; and
- execution-dependent readiness rows remained pending until validation.

Final prospective verdicts A and B: `GO FOR BOUNDED DIAGNOSTIC EXECUTION`.

## Execution findings

No implementation or validation defect remains open. Scientific divergence is
retained rather than treated as execution failure:

- source/model scale changes match availability but does not resolve timing;
- photoperiod and VPD are mathematical contributors;
- temperature has little chronology leverage in this lane;
- counterfactual improvement is nonlinear and operator-dependent; and
- forcing, ecotype thresholds, and missing process remain non-identifiable.

## Terminal findings

Terminal Review A and Verification A passed the bounded diagnostic evidence,
source custody, claim calibration, scenario reconstruction, crossing
inventories, decision predicates, and figure/sidecar integrity.

Terminal Review B initially held closure on three findings:

- `CAL07D-TRB-001`: the validator did not yet independently reconstruct all
  scenarios, crossings, event tables, and decision predicates;
- `CAL07D-TRB-002`: figure values and sidecar table semantics were not bound
  with sufficient independent evidence; and
- `CAL07D-TRB-003`: package completion conflicted with pending current-scope
  terminal records.

All three findings are resolved. The validator now independently rebuilds five
37-member scenarios and the single canonical default, 9,996 daily summaries,
34,480 global crossings, every event table, source support/attribution, and all
seven predicates. Every SVG carries a verified plotted-data fingerprint and
every sidecar states exact keys, counts, selection rules, and unmatched
semantics. Markdown, syntax, XML/render, exact-diff, and dual terminal gates
pass.

Final terminal verdicts A and B:
`PASS / ORDER 7 HOLD RETAINED`.
