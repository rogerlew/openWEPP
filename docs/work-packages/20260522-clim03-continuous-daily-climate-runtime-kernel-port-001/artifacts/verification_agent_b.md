# Verification Agent B

Evidence mode: `Ran`

## Verification
- Confirmed CLIM03-specific behavior checks:
  - `ip *= 0.70` for `datver>=4.0`
  - explicit `datver=0.0` override behavior
  - duration cap and event-shape symbol projection
- Integration assertions passed.
