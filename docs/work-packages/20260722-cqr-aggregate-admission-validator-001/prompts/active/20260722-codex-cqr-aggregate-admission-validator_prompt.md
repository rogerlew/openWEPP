# Kickoff: CQR Aggregate Admission Validator

Execute this package end-to-end. Implement a fail-closed pre-implementation
validator and focused Git-fixture tests; preserve immutable package authority;
obtain dual review; close RTR-031 only after the exact correction commit; then
return to the single delegated changed-head recovery qualification attempt.

Subagent authorization: this prompt explicitly authorizes spawning/delegating
to the review, comparator, and verification roles defined in `package.md`.
Do not push, deploy, switch branches, manually dispatch TESTGATE, run HEAVY on
the parent, or repeat an unchanged expensive gate.
