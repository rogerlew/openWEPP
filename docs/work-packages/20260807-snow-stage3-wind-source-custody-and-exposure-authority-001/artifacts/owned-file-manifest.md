# Owned File Manifest

Status: `terminal inventory reconciled / PASS`.

Evidence mode: `Static`.

Selected and changed: this package tree; root/snow roadmaps and work-package
catalog; both snow contracts and index; `Cargo.toml` test-target registration;
new `snow_stage3_wind_source_custody_contract.rs`; and version/registry
expectations in `snow_stage3_legacy_predecessor_bridge_contract.rs`,
`snow_stage3_turbulent_operator_reconciliation_contract.rs`, and
`snow_surface_eb03_contract.rs`. DRAFT assurance adoption is selected because
both contracts are identified report dependencies.

Not selected: production Rust, climate fixtures, observations, schemas,
manifests/dependencies, public outputs, defaults, WAT/HBP/PASS, CoE ownership,
persistence, canopy implementation, or release lifecycle.
