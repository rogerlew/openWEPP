# Worker Handoff

Status: `authority-shaped follow-up`.

Evidence mode: `Static + Ran`.

First actionable item: obtain immutable climate-generation receipts for each
canonical CLI (asset/version/status, request/CRS, pixel/sampling, wind
derivation, timezone/day boundary, missing policy, transformation/rounding,
and hashes), then authoritative forcing exposure, modeled forest-snow target
classification, and linkage. Do not inspect values, fit attenuation, infer
exposure, add canopy physics, or correct production wind. Separately close
stability-geometry and physical magnitude-envelope gaps before persistence.
