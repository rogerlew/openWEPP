# Worker Handoff

Status: `authority-shaped follow-up`.

Evidence mode: `Static + Ran`.

Provider recovery directly closed hashes, retained centroids/GRIDMET flags,
daily parquet wind, and parquet-to-CLI equality. Nearby-source request/product/
sharing/serialization semantics remain static reconstruction. First actionable
authority item: recover the deployed identity/request response and server-side asset
version, pixel/sampling, timezone/day boundary, and missing policy if those
records exist; then obtain authoritative forcing exposure, physical forest-snow
target classification, and linkage. Do not fit attenuation, infer
exposure, add canopy physics, or correct production wind. Separately close
stability-geometry and physical magnitude-envelope gaps before persistence.
