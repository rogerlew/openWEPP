# Calibration Readiness Matrix

Status: `queued`.

Evidence mode: `not-run`.

Intent: authority reconciliation, not empirical calibration. Record
`science_implementation_status`, `calibration_evidence_status`, and
`identifiability_status`; fitted attenuation is out of scope.
