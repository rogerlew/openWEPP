# Gate Results

Status: `queued`.

Evidence mode: `not-run`.

Freeze exact direct commands after pre-implementation intent and reconcile them
against the terminal diff. Heavy selected gates require comparator delegation.
