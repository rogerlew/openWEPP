# Disposition

Status: `package complete / persistence HOLD`.

Evidence mode: `Ran`.

All four rows remain `AUTHORITY_MISSING`. Follow-on provider recovery directly
proves byte-identical source-run CLIs, stored watershed centroids and GRIDMET
flags, complete daily parquet wind, and exact parquet-to-CLI equality. Nearby
pre-build source only statically reconstructs the GRIDMET product/request,
run-level sharing, and one-decimal transform. Exact deployed identity/request,
server-side product/pixel/version/timezone/fill policy, and authoritative
forcing-to-forest exposure linkage remain absent. This is neither `APPLICABLE`
nor `INAPPLICABLE`.

Raw CLI wind reaches Stage 3 unchanged; PMET's 2 m adjustment remains local.
Nominal GRIDMET 10 m source height is distinct from virtual Stage 3 5 m
geometry. Independent neutral sensitivity is 10.0771%/21.1697%, not correction
authority. All selected gates pass. No fitting, inferred exposure, canopy
operator, production correction, persistence, promotion, CoE retirement, or
cutover occurred. The package objective is complete; scientific persistence
remains `HOLD` pending the narrowed custody/exposure and other physical-envelope gaps.
