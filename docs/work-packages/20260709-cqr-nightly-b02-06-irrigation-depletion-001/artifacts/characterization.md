# Characterization Plan

Before decomposition, bind all error display variants, public parse success and
exact failure behavior across sprinkler/furrow/datetime paths, strict and
compatibility policy, and topology/continuation constraints to the real parser
consumer. Use exact error ID/line/field/message assertions, not helper-only
values.
