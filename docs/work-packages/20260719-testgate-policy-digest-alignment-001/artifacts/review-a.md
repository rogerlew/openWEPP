# Independent Review A

Evidence class: `Static` and `Ran`

Verdict: `FAIL` for closure; correction bytes pass.

## Findings

- `TGDA-A-01` (`HIGH`, closure-blocking): the focused-only plan improperly
  downgraded a mechanically `CRITICAL` impact-map change. Canonical governance
  requires immediate campaign-strength workspace regression, global CRAP, and
  the specialized gates selected for critical risk. Evidence remains
  producible, so the package must continue rather than HOLD.
- `TGDA-A-02` (`MEDIUM`): combined 57-plus-5 planner evidence omitted the exact
  five-test selector and durable identities for the four fail-fast
  cancellations.

The reviewer independently confirmed direct digest equality, JSON validity,
an exact one-field commit, semantic pre/post equality after excluding
`policy_sha256`, unchanged blocking/escalation posture, current 15/15 focused
integration JUnit evidence, clean Markdown lint, and clean diff hygiene. No
broad test was run by the reviewer.
