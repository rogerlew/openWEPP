# CRAP After

Evidence label: Static.

Status: `PENDING`

Run after implementation. Required target: every eligible production function in
`crates/openwepp-runner/src/hillslope/laned_shadow.rs` has CRAP `<= 30` or an
explicit disposition.
