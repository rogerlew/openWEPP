# Execute TESTGATE Adversarial Clippy Cleanup

Scope: behavior-preserving test-helper extraction only.

Read `AGENTS.md`, `docs/work-packages/AGENTS.md`,
`docs/defect_closure_execplans.md`, `tests/AGENTS.md`, the package, and its
required-reading map. Split the one oversized helper, preserve every assertion
and source string, run only the focused repair loop before the mechanical
terminal plan, complete dual review/verification, and archive this prompt.

Do not add lint suppression, change workflows/policy/production code, weaken a
test, run an ad hoc broad suite, dispatch GitHub, or use forest1.
