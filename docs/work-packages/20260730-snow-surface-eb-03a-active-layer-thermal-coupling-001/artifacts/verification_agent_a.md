# Terminal Verification A

Status: `complete / HOLD`

Evidence mode: `Static + Ran`

Verifier A found the EB-03A technical/runtime scope `PASS` and the terminal
package `HOLD`.

Ran:

- focused EB-03/03A plus affected Stage 0: 18/18;
- meteorology: 22/22;
- formatting and workspace Clippy with warnings denied;
- strict Binding Exposure and unit compliance;
- assurance validate: three DRAFT, zero public;
- tracked assurance rendering: 92 files current; and
- independent CQR reproduction: FAIL after `136.800 s`, because the generated
  valid exact-head fixture was not CURRENT.

Static/retained evidence confirmed all six consumer cells, nonzero active/lower
conduction, exact closure/reconstruction, rollback identity, and matching
snow/freeze assurance source bytes.

The verifier identified the ADR-0043 Critical full-profile requirement. That
profile was subsequently attempted and reached two `720 s` assurance
publication-matrix timeouts. Because amended assurance inputs may be consumed
by those tests, no independence claim is made. The verifier's required
disposition is therefore retained: technical/science `PASS`, overall `HOLD`.
