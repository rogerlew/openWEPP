# Execute SNOW-SURFACE-EB-04W

Lifecycle: archived after successful execution and dual verification on
2026-08-02.

Execute `package.md` end to end within its declared write set. Use contract-first
sequencing, typed units and failures, direct real-consumer evidence, independent
phase/mass/component closure, and exact retained-output comparison. Run four
unique lanes under B/L/S/LS and evaluate all five frozen EB-04U operators.

No surrogate physics, fitting, forcing compensation, process/default change,
or promotion is authorized. Treat modeled-zero wind redistribution separately
from unknown physical redistribution, and do not relabel mixed empirical CoE
terms as pure energy fluxes.

Subagent requirement: REQUIRED. This prompt explicitly authorizes subagent
spawning/delegation to two independent science/code reviewers, two terminal
verifiers, and `comparator_suite_runner` for the 16-cell population and full
critical suite. Outputs are compact package artifacts/log paths; writes are
limited exactly as stated in `package.md`.

Every current-scope gate needs direct current evidence. Otherwise close `HOLD`
with the named blocker.
