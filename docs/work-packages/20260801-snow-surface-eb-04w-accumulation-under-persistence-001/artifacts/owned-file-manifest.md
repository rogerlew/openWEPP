# Owned File Manifest

Evidence mode: **Static**.

EB-04W owns:

- this package tree;
- snow-campaign roadmap, root roadmap, and work-package catalog rows;
- `SC-SNOWFREEZE-001` version 121 and mechanically version-bound contract tests;
- the minimum orchestrator snow forcing, melt-ledger, partition, and exports;
- the minimum runner projection, trace suffix helper, formatter call, and
  snowbench diagnostic forcing adapter;
- the focused EB-04W integration test and `Cargo.toml` registration; and
- governed assurance source-adoption locks, two sequential transactions, and
  the rendered review draft caused by the contract amendments.

Protected observation fixtures, authority-suite membership, WAT/PASS/HBP
schemas, dependencies, runtime defaults, and historical package evidence are
not modified. Terminal exact-diff reconciliation enumerates the actual paths.
