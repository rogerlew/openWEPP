# PERFDEEP07 Publication Shadow Ledger

Status: blocked by HOLD.
Evidence mode: Static.

No direct-frame publication shadow run was executed because the ordered
default-disabled P0 timing gate failed.

## Required Evidence

Use the PERFDEEP06 publication operand ledger. Record whether direct-frame
shadow publication preserves:

- HBP byte identity;
- WAT byte/Arrow identity;
- PASS Arrow equivalence;
- WB13 manifest/provenance equivalence;
- identity/calendar/schema/producer metadata;
- anti-alias fixtures for every touched operand.

No output schema change is allowed in this package without amendment.
