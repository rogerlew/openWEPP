# Operand Lineage

Status: **QUEUED**.

Evidence mode: Not run.

Before active-owner production edits, document each conservation-sensitive
operand: field name, units, normalization/denominator, area or volume basis,
producer, in-memory state/frame object, runner handoff, downstream consumer,
authority source, and old path that active mode bypasses.
