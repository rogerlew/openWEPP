# Watershed Consumer Proof

Status: `QUEUED`

Record the real producer -> HBP -> supervisor -> `WatershedNetworkFrame` ->
watershed/channel kernel path. Producer-only, parser-only, inventory-only, and
shadow-only evidence cannot close this artifact.
