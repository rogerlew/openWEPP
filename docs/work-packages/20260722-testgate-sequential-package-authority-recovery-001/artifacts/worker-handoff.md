# Worker Handoff

Static: first actionable item is rebuild and hash the release planner, then
delegate exactly one changed-head `INCREMENT` qualification attempt for
TESTGATE-RECOVERY-TRUST-01. The comparator owns HEAVY; the parent must not run
HEAVY or manually dispatch TESTGATE. RTR-044 is durably closed at correction
commit `51c7e06d` and dual terminal verification passed.
