# RTR-044 Sequential Package Authority Recovery Prompt

Scope: local repository engineering; no external connectivity or system action.

Execution mode: package-end-to-end.

Required reading:

Core: root/crate/test/work-package/standards instructions, ExecPlan and
DC-ExecPlan authority, testing/gate strategy, recovery package, and this
package.

Conditional: no science-contract authority; this is gate tooling only.

On-demand: in-scope planner, Python intent, schema, and focused test surfaces.

Required-reading budget: WARN; the cross-language trust boundary requires the
listed governance and focused sources. Map:
`artifacts/required-reading-map.md`.

Task: close RTR-044 end-to-end inside the declared envelope. Preserve
prospective timing by reconstructing commit-ordered package authority, bind
intent and pre-HEAVY admission to one canonical chain artifact, and reject
zero, ambiguous, retroactive, malformed scaffold, and unmet prerequisite cases.

Subagent requirement: this prompt explicitly authorizes subagent
spawning/delegation to two independent read-only implementation reviewers, two
read-only terminal verifiers, and one comparator runner. Outputs are package
review/verification artifacts and retained ignored comparator evidence.

Autonomy: continue through tests, correction, focused validation, review,
durable closure, terminal verification, release rebuild, and exactly one
changed-head `INCREMENT` qualification unless a declared boundary blocks.

Do not push, deploy, switch branches, manually dispatch TESTGATE, run HEAVY on
the parent, or rerun unchanged expensive gates.
