# Workspace gate hold-lift

Status: `SUSPENDED — REPLACEMENT NUMERICAL/RUNTIME PACKAGE REQUIRED`

Execution mode: `suspended-after-r151`

This is a living ExecPlan maintained under `docs/codex_exec_plans.md`.

## Objective

Lift the mandatory workspace-gate HOLD recorded by
`20260830-snow-stage3-cold-content-fixed-point-optimization-001` by obtaining a
passing warnings-denied workspace Clippy command and a passing complete
full-workspace correctness profile on one exact clean terminal source identity.

## Progress

- [x] 2026-08-30: authorize and scaffold the hold-lift package.
- [x] 2026-08-30: freeze current source identity and classify retained current
  failures; delegated isolated-baseline profile remains in flight.
- [ ] 2026-08-30: correct warnings-denied and attributable correctness defects.
- [ ] 2026-08-30: execute exact-clean terminal mandatory gates.
- [ ] 2026-08-30: complete dual review, dual verification, disposition, and delivery.
- [x] 2026-09-01: disposition canonical r151 and suspend this package by owner
  direction without a completion claim.

## Owner stop and r151 disposition

Execution stopped after canonical r151 by direct owner instruction. V56 and
V57 remain implemented but unresolved. R151 proves that the bounded-liquid and
terminal-one-volume eligibility corrections do dispatch the frozen
temperature-primary specialization, but that specialization then fails inside
its safeguarded physical solve on exact support `1800..1860 s`. This is earlier
than the former `2100..2160 s` V55 no-witness blocker and is not passing
end-to-end evidence.

No V58 or further exact-witness, receipt-lattice, carry, or eligibility
successor is authorized from this package. The current worktree is preserved;
the mandatory full-workspace gates, canonical one-day accepted/rejected counts,
step-width distribution, runtime qualification, ledger-closure report, dual
terminal review/verification, package closure, commit, and push remain
unperformed or incomplete. This package therefore makes no hold-lift or
completion claim.

Further numerical/solver work is owned by the separately authorized replacement
[`20260901-stage3-native-vegetation-laned-watershed-throughput-recovery-001`](../20260901-stage3-native-vegetation-laned-watershed-throughput-recovery-001/package.md).
Before authorizing solver changes, that package must establish
representative workload/runtime budgets and physically justified convergence
tolerances. It must retain Lane D MOFE behavior, RHESSys-derived native-
vegetation evapotranspiration, and the new non-CoE Stage 3 snow authority and
runtime path. The active frozen forest-litter work and all current conservation,
custody, phase, topology, receipt, rollback, event, and fail-closed obligations
remain preserved rather than waived by this suspension.

## Observed defects

- `WGHL-CLIPPY-001`: `cargo clippy --workspace --all-targets --all-features -- -D warnings`
  exits 101 after `filter_map_bool_then` in `openwepp-coupled-time` and
  `similar_names` in `openwepp-biogeochemistry`; Cargo may expose further
  diagnostics after those are corrected.
- `WGHL-FULL-001`: `cargo nextest run --workspace --profile full` completes
  3,628 attempted tests with 3,503 pass, 96 fail, and 29 timeout. Three stale
  accepted-endpoint source scans are already corrected; 122 adverse outcomes
  remain unclassified against the fixed-point package baseline.
- `WGHL-FULL-001D`: the authentic exact-floor open-snow Picard map oscillates
  across the terminal one-volume snow enthalpy kink: a mixed `0 C` endpoint
  maps to a dry frozen `196.469 K` endpoint below the already-required LSE
  domain. Componentwise contraction correctly refuses the phase/posture
  crossing; the real `dff_ws2` fixture fails at `60 s`. The lawful v31 W/H
  projection then exposes a pure deposition-to-sublimation active-set reversal
  on `1860..1920 s`, which its required vapor-disposition guard correctly
  refuses.
- `WGHL-FULL-001E`: committed Stage-3 publication omits the requested accepted
  WAT5 producer projection and reconstructs accepted upstream runon from the
  LSE forcing field that correctly rejects routed parcels, masking transaction
  errors and collapsing `UpStrmQ` into local liquid.
- `WGHL-FULL-001F`: at an otherwise converged covered-column state, the full
  Newton trial may cross a closed phase bound while the first domain-valid
  halved trial has sub-tolerance prospective steps. The solver nevertheless
  requires strict residual decrease at floating-point roundoff and exhausts
  all 20 backtracking halvings in two interior terminal-event tests.
- `WGHL-FULL-001G`: valid `p61` and native-forest fixtures enter frozen forest
  litter, which current LSE and surface-liquid contracts explicitly reject.
  The distinct-authority successor
  `20260830-frozen-forest-litter-phase-authority-001` is scaffolded and active;
  its passing real-consumer evidence is a dependency of this hold-lift.
- `WGHL-FULL-001H`: after the corrected covered LSE solve, a terminal snow-free
  successor beginning at `600 s` is bounded only by adaptive cadence/parent end
  and crosses the accepted terminal child ending at `900 s`; qualification
  correctly rejects the successor chronology.
- `WGHL-FULL-001I`: a valid accepted infiltration enthalpy credit on the
  canonical WAT5 path is nonzero but only `1.10875e-7` ULP of the persistent
  soil-layer binary64 enthalpy. Scalar installation cannot change the owner
  bits, so strict independent closure correctly fails `SURFACELIQUID-E-003`.

## Rationale

Focused solver, contract, authority, conservation, and canonical performance
evidence passes, but package governance prohibits completion while mandatory
campaign-strength workspace gates fail. This package owns the cross-workspace
classification and repair needed to replace that HOLD with direct passing
evidence. It does not treat pre-existing failures, retry, or location outside a
prior write set as an implicit waiver.

## Included scope

- exact-source failure inventory and baseline/current comparison;
- narrow warnings-denied source-quality corrections;
- fixture, orchestration, runner, CLI, assurance, and test-support corrections
  proven necessary by the complete profile;
- a contract-first, exact-floor terminal-one-volume phase-aware unpublished
  contraction for the authentic open-snow fixed-point map, reconstructed in
  canonical total-water/enthalpy coordinates and preserving authentic-only
  acceptance;
- conservation-sensitive accepted Stage-3 WAT5 and upstream-runon publication
  reconstruction from sealed accepted producer/custody operands;
- a contract-first covered-column no-update termination witness evaluated on
  the first domain-valid halved Newton candidate when current residuals and
  prospective steps already satisfy every unchanged convergence threshold;
- producer-side terminal successor partitioning at the sealed accepted-child
  end, preserving the unchanged qualification validator and physical supports;
- contract-first exact soil-thermal enthalpy carry custody for accepted energy
  below scalar-state representability, with versioned owner/receipt/restart
  schemas, exact rollback, and unchanged constitutive physics;
- delegated execution and handback of the distinct contract-first frozen
  forest-litter phase successor before terminal workspace reruns;
- timeout diagnosis and deterministic resource/scheduling correction when the
  test's semantic obligation is preserved;
- direct focused tests followed by exact-clean full Clippy and full Nextest;
- review, verification, line-count, terminal-diff, and truthful disposition.

## Excluded scope

- numerical tolerance, conservation threshold, physical constitutive equation,
  event chronology, custody, topology, receipt, rollback, or adaptive temporal-
  policy changes beyond the exact phase-aware unpublished numerical iterate
  safeguard explicitly amended below;
- deleting, ignoring, weakening, filtering, reclassifying, or relaxing a test
  merely to make the workspace profile pass;
- TESTGATE/planner repair, coverage/CRAP campaigns, dependency changes, release,
  publication, or unrelated performance optimization;
- treating a baseline failure as waived without an explicit authoritative
  correction or accepted lifecycle disposition.

## Correction authority envelope

The package may make behavior-preserving source-quality edits and correct
production/test infrastructure when a retained failure plus focused evidence
demonstrates the defect. Every Rust implementation path must be entered in
`artifacts/failure-inventory.md` with failure ID, owner, exact source/test path,
classification, intended correction, and focused command before editing.

This package is amended to own the following classified exceptions. First,
`WGHL-FULL-001D` must follow contract-first sequencing and may admit only a
deterministic exact-floor terminal-one-volume phase-aware contraction. It must
derive the unpublished intermediate from immutable physical beginning state,
complete support water/energy operands, and the canonical SC-SNOWENERGY total-
water/enthalpy phase projection; it may not independently interpolate liquid,
cold content, melt, refreeze, or cumulative ledger fields. It must retain raw
authentic history and require a later fresh authentic image for convergence,
final replay, acceptance, and publication. It may not change the `60 s` floor,
convergence tolerances/cap, physical constitutive equations, event topology,
custody, receipts, rollback, or authentic-only publication. Second,
`WGHL-FULL-001E` may repair
public-output projection only from sealed accepted operands, with prospective
operand lineage, anti-tautology tests, explicit wrong-formula rejection,
independent reconstruction, and real closure evidence. Any further science,
numerical-policy, public schema, or serialization change remains outside the
envelope and requires another prospective amendment.

Third, `WGHL-FULL-001F` may extend only the existing no-update convergence
witness to the first domain-valid halved Newton candidate after the full trial
fails the existing no-update witness because it is domain-invalid or any
governed prospective step exceeds its unchanged threshold. It accepts the
current iterate without applying a step only when the current complete residual
vector and that first eligible halved candidate's exact component step norms
satisfy all existing thresholds. It changes no phase bound, residual/step tolerance,
strict-decrease rule for actual updates, iteration/backtracking limit, ledger,
receipt, event, custody, rollback, or 60-second floor.

Fourth, `WGHL-FULL-001I` must follow contract-first sequencing and may admit
only a receiver-owned exact dyadic carry for soil-layer enthalpy that cannot be
represented in the existing binary64 high term. The canonical total is
`E = exact(H_hi) + R`; `R` has one normalized signed-dyadic wire form, is
retained exactly, and is rejoined on every later solve and credit. Each accepted
candidate must aggregate the exact beginning total plus the canonical ordered
accepted soil, top-boundary, and infiltration energy operands, round the total
once to nearest-even finite `H_hi`, and retain exactly
`R = E - exact(H_hi)`. The amendment must freeze every V1 byte/digest, provide
explicit V1-to-V2 zero-carry migration with no production downgrade, bind the
carry through typed credit receipts and successor restart/checkpoint schemas,
and prove byte-exact rollback. It may not zero a nonzero carry, use tolerance,
`nextafter`, forced-ULP installation, a producer residual, a persisted
diagnostic, or change any constitutive equation, heat capacity, chronology,
event, custody, receipt, rollback, or temporal floor.

Fifth, `SC-SNOWENERGY-001` v32 supersedes the in-review v31 controller while
retaining its same-disposition W/H projection and vapor-disposition refusal.
When two independently closed, pure one-sided actual-vapor support images have
identical immutable beginning/support/identity/custody/receipt structure and
strictly opposite signed actual vapor, the covered solver may localize one
unpublished vapor-active-set interface. It derives the unique convex vapor-root
fraction from the signed endpoint masses, sets actual vapor, deposition,
sublimation, and their already-once latent-energy component to exact positive
zero, contracts external liquid and every nonlatent ordered energy component at
that fraction, recomputes complete energy, and applies the unchanged canonical
total-water/enthalpy phase projection. A subsequent zero-to-one-sided numerical
branch entry may preserve the fresh authentic endpoint's positive finite
specific latent heat under the existing bounded support-scaled Picard weight.
These numerical images are never authentic, converged, finalizable, replayable,
acceptable, or publishable; only a later fresh authentic map image may satisfy
those branches. Mixed deposition/sublimation, failed mass/latent/component
closure, or any identity/event/topology/custody/receipt change remains typed
failure. This private transition may apply on any exact covered support at or
above the unchanged 60-second floor solely when ordinary guarded contraction is
blocked by the terminal-one-volume phase/vapor active set; it changes no
adaptive temporal policy, tolerance, iteration cap, constitutive equation,
event, custody, receipt, rollback, public schema, persistence, or publication.

Sixth, `SC-SNOWENERGY-001` v33 supersedes the nonprogressing v32 controller on
the exact affected terminal-one-volume covered support. Versions 31 and 32 are
retained as diagnostic reconstruction/refusal oracles but must not remain in
production control flow: retained evidence proves the v32 root/branch map has
an exact active-set transition reset and no fresh-authentic fixed point. The
v33 trigger is not bitwise equality of raw authentic owners. It requires exact
unchanged support/source/event/topology/custody/receipt joins and the sequence
`root/interface -> one-sided branch-entry -> opposite pure-vapor raw-authentic
-> same root/interface/reset coordinates and branch predicates`, with opposite
pure vapor disposition; asymptotically changing raw-authentic continuous owner
fields need not compare equal.

Only then may v33 invoke a private reduced semismooth coupled solve. Its
unknowns are ending snow water/enthalpy and only the coupled soil endpoint
coordinates required by the unchanged CN snow--soil receipt. A sealed physical
evaluator must expose concrete `R_W/R_H/R_E/R_T`, canonical phase, existing LSE
and CN equations, soil enthalpy--temperature closure, exact water/complete
energy including linked vapor latent energy, equal/opposite snow--soil custody,
and reconstructable receipt side constraints. Coordinate-map difference
`F(x)-x` is not an admissible residual. One shared physical-evaluation budget,
bounded by the unchanged 96, includes trigger confirmation, baseline/Jacobian,
trust and rejected trials, fresh evaluation, and final replay without reset.
Only an exact fresh coupled-root replay that independently reseals every receipt
may enter private `CoveredConvergenceAdmissionV1::CoupledAuthentic`; that branch
bypasses only Picard current/candidate equality and convergence and retains all
residual, finalization, receipt, rollback, and publication checks. This is
neither a convex event-time mixture nor synthetic Picard acceptance. Existing
discrete event/forcing/receipt boundaries still force exact partition/refusal.
No floor, tolerance, 96-evaluation cap, constitutive equation, event, custody,
receipt, rollback, public schema, persistence, or diagnostic policy changes.

Seventh, `SC-SNOWENERGY-001` v34 extends only eligibility for the existing v33
equation-level reduced solver. On one event-free terminal-one-volume covered
support at or above the exact 60-second floor, exactly eight consecutive raw
authentic Picard maps may trigger the unchanged `R_W/R_H/R_E/R_T` solver only
when support/source/event/topology/custody/static-receipt/phase-branch and exact
V2 soil-enthalpy-carry authority and representation remain unchanged, all
physical residuals are finite, governed residual merit decreases strictly at
each map, and no
`A/B/A`, active-set transition, or finalization restart occurs. All eight raw
maps and every later residual, Jacobian, trust, rejected, fresh, and final
replay evaluation charge the same unchanged 96-physical-evaluation budget.

Static receipt joins are schema, support, topology, ordered operand lineage,
and beginning-owner/authority identities. They remain exact while physical
applied energy, candidate-ending identity, and the resealed receipt digest
evolve under the unchanged residual and receipt-reconstruction rules. Exact
carry retains the signed-dyadic schema/version, normalization, units,
layer/owner/order authority, and exact `E=exact(H_hi)+R` reconstruction on
every map; the physical `H_hi` and `R` coordinate values may evolve and are not
required to compare byte-equal. This distinction does not relax receipt
custody, exact carry, closure, replay, or reseal.

The v34 trigger changes no tolerance, physical residual, constitutive equation,
or adaptive temporal policy. If eligibility or the private solver refuses
before a root, every private trial is discarded and ordinary raw authentic
Picard resumes using only the budget that remains. No private trial may enter
acceptance. A root may advance only through the existing fresh
`CoveredConvergenceAdmissionV1::CoupledAuthentic` evaluation, replay, receipt
reseal, finalization, and publication path. The exact 60-second floor, exact V2
carry, conservation closure, phase/capacity, events, topology, custody,
receipts, rollback, public schema, persistence, diagnostics, and fail-closed
posture remain unchanged.

Eighth, `SC-SNOWENERGY-001` v35 adds exact authentic receipt stabilization
after the v33/v34 coupled physical root. For complete immutable receipt input
`R_n`, one authentic physical probe reconstructs/reseals `R_(n+1)` from
immutable beginning owners and sealed sources. That reconstructed receipt set
becomes the next immutable authentic physical input without tolerance, digest
repair, or in-place mutation. Every probe charges the same unchanged
`CoveredPhysicalEvaluationBudgetV1` already charged by prior Picard and solver
work. The first private-root-to-authentic reseal is a probe under a different
receipt input and must not be compared exact against root/prior artifacts.

Only exact canonical input/output receipt equality stabilizes the receipt set.
The implementation must then perform one independent physical replay using
the same stabilized receipt input and require exact equality of finite physical
residuals, complete candidate artifacts, and reconstructed receipts with the
stabilizing probe before `CoupledAuthentic` or finalization. Exact receipt
oscillation, nonfinite state, side-constraint/reconstruction failure, budget
exhaustion, replay disagreement, digest/tolerance repair, in-place mutation,
or retention of any private/probe artifact fails closed and discards all such
artifacts. Physics, tolerances, exact 60-second floor, events, topology,
custody, receipt meaning, exact carry, rollback, finalization, publication,
persistence, diagnostics, and the unchanged shared 96-evaluation cap remain
unchanged.

Ninth, `SC-SNOWENERGY-001` v36 makes the v33--v35 reduced solver
geometry-complete by adding exactly one terminal density coordinate per
affected lane. Canonical phase projection remains sole owner of ice mass, and
thickness is reconstructed as `z=I/rho` on the existing mass-depth basis.
Physical `R_rho` comes only from the unchanged Stage 3 density/settling
constitutive map evaluated from immutable beginning, sealed forcing, and
current physical operands. The generalized solver uses
`R_W/R_H/R_rho/R_E/R_T`, the same shared 96-evaluation budget, v35 receipt
stabilization, and unchanged authentic finalization.

Stable eligibility keeps exact layer identity/order, settling authority, and
density-model branch while finite positive rho/z may evolve. Generic
`F(x)-x`, density/thickness interpolation or repair, tolerance/cap/floor
change, uncharged physics, private acceptance, receipt-stabilization bypass,
or finalization bypass is prohibited. All physics, events, topology, custody,
receipts, exact carry, rollback, publication, persistence, and diagnostics
remain unchanged.

Tenth, `SC-SNOWENERGY-001` v37 closes the retained r93 derived-thickness
admission gap. For each affected terminal-one-volume lane, the unchanged
physical evaluation derives proposed and physical thickness from canonical
`I/rho` and exposes `R_z=z_1-z_phys`. `R_z` participates in scaled merit and
root admission under the unchanged `depth_abs_m`; it is not a new unknown and
does not replace `R_W`. The solver still solves only
`R_W/R_H/R_rho/R_E/R_T`, while the water equation must contract far enough to
close its low-density depth image.

Every private, Jacobian, trust, rejected, fresh, receipt-probe, and replay
evaluation derives `R_z` from the same already-charged Stage 3 map. V35 exact
receipt stabilization, same-input replay, `CoupledAuthentic`, and unchanged
authentic finalization remain mandatory. Independent-z solving, omission of
derived depth from merit/admission, interpolation/copy/repair, added or
uncharged physics, tolerance/cap/floor change, or receipt/replay/finalization
bypass is prohibited. All constitutive physics, events, topology, custody,
receipts, exact carry, rollback, publication, persistence, and diagnostics
remain unchanged.

Eleventh, `SC-SNOWENERGY-001` v38 supersedes v37 as the active closure route
after retained r95 proved that the coupled root and stabilized same-input
replay each close derived `R_z` exactly while the later authentic finalization
still evaluates a different carrier map and changes lane-1 thickness. Closure
of the provisional map is not authority for the finalization map.

Every private, Jacobian, trust, rejected, receipt-probe, and same-input replay
evaluation must use one shared-budget-charged finalization-equivalent endpoint
map. It reconstructs proposed terminal snow and exact-carry soil operands from
immutable beginnings and `W/H/rho/E/T`; builds the same non-provisional
carrier, corrected LSE exchange, covered/open endpoint boundaries,
precipitation custody, and immutable CN receipt input as finalization; and
calls Stage 3 exactly once for that charge. Residuals, `R_z`, artifacts, and
reconstructed receipts derive from that one image. Provisional-map closure,
outer-iterate substitution, a second or uncharged Stage 3 call, repair/copy,
private publication, or replay/finalization bypass is forbidden. V35 exact
receipt stabilization and same-input replay remain mandatory, and independent
authentic finalization must reproduce exact state/LSE/boundary/receipt/ledger
images. Physics, equations, tolerances, the shared 96 cap, exact 60-second
floor, events, topology, custody, receipt meaning, exact carry, rollback,
publication, persistence, and diagnostics remain unchanged.

Twelfth, `SC-SNOWENERGY-001` v39 supersedes v38 only for V2 soil-energy
operand transaction custody after retained r102 proved that the composed
second child legitimately retains outer surface-ingress/source transaction 42
while its authenticated soil target owns transaction 43. The typed
`PhysicalSoilEnergyTransactionAuthorityV2` must carry both exact nonzero
identities. Ingress and its receipts validate against the source transaction;
the prepared/unpublished soil beginning validates against the soil-target
transaction; and every soil-internal/infiltration debit-credit digest binds
both identities plus exact support and source lineage. No caller may infer
adjacency, copy one authority into the other, rewrite a source receipt, or
weaken target acceptance. All physics, energy values, topology, receipts,
exact carry, conservation, rollback, publication, adaptive timing, budget,
tolerances, persistence, and diagnostics remain unchanged.

Thirteenth, `SC-SNOWENERGY-001` v40 adds only an earlier numerical trigger
after retained r107 proved the corrected rolling exact reset first dispatches
at the already exhausted shared budget 96. Exactly four consecutive nonexact
rolling reset windows may invoke the unchanged coupled solver only with exact
static joins, exact promoted-root chaining, fixed phase/opposite-vapor sides,
finite positive strictly decreasing tolerance-independent root drift,
nonstagnant parity coordinates, exact two-map cadence, and sufficient unused
capacity in the same shared budget for the minimum solve/probe/replay path.
No drift threshold admits a root. Physical residuals, solver equations,
tolerances, the 96 cap, 60-second floor, receipt stabilization, authentic
replay/finalization, custody, rollback, and publication remain unchanged.

Fourteenth, `SC-SNOWENERGY-001` v41 adds a distinct early numerical trigger
after retained r109 proved the real four-window sequence is not V40
root-drift-monotone or fixed-phase. V41 requires exact static joins, promoted
root chaining, opposite vapor sides and two-map cadence; bit-identical finite
nonnegative water; a strictly one-way finite enthalpy chain; independently
reconstructed canonical predicates; and exactly one adjacent phase-boundary
crossing. The bracket is not a tolerance, interpolation, projection, or root
admission. It may dispatch only the unchanged solver with the same shared
reserve and every authentic receipt/replay/finalization/rollback/publication
guard unchanged.

Fifteenth, `SC-SNOWENERGY-001` v42 corrects the canonical support-coordinate
image after the persisted-restart gate reached 69/71 and both snow-reappearance
fixtures exposed the same exact `+2.3211191058 J m^-2` omitted term. The
authentic Stage 3 result already reports cold-content magnitude exported with
removed ice as `complete_arm_cold_content_export_j_m2`; V42 carries that exact
finite nonnegative `X_c` into `H=-C_0+L_f(L_0+L_in)+Q+X_c` and contracts it
with the same ordered private weight as external liquid and nonlatent energy.
Zero-export behavior remains byte-identical. No ledger math, physics,
tolerance, budget, floor, custody, receipt, rollback, or publication rule
changes.

Sixteenth, `SC-SNOWENERGY-001` v44 corrects the evaluation-posture ordering
proven by retained r116. A private nonlinear coupled coordinate reaches the
aggregate weighted-OFE guard before the reciprocal-longwave, shortwave,
sensible, and vapor exchange can be rebuilt; its residual changes sign and
magnitude across trials, so this is not a fixed missing ledger term and no
tolerance change is authorized. The charged `PrivateTrial` uses only the
existing uncommitted provisional LSE posture to obtain corrected exchange and
one Stage 3 image. Authentic receipt probes, independent same-input replay,
and finalization remain strict non-provisional evaluations and must pass the
unchanged weighted-OFE closure before admission. The V38 top-soil coordinate
continues to enter exactly once through snow--soil CN receipt/credit custody;
Stage3-covered V8 retains zero ground heat/storage and must not double-apply
that coordinate. No private trial can publish.

Seventeenth, `SC-SNOWENERGY-001` v45 corrects the exact receipt-stabilization
precision and budget-allocation defect proven by retained r117/r118. The
canonical `1860..1920 s` support reaches unchanged tolerance closure but its
binary64 Crank--Nicolson receipt map remains nonexact and consumes the complete
shared budget. The existing physical generalized-Jacobian/Newton/trust solve
may therefore continue privately after tolerance closure only while the same
canonical residual merit decreases strictly. Exact-zero or finite side-valid
sub-tolerance stagnation/non-descent may hand only the carried best matching
physical bundle to unchanged V35 receipt stabilization; it cannot admit or
publish. Every private solver evaluation preserves three later charges, every
polishing evaluation preserves two, and every nonstable authentic probe
preserves the final independent-replay charge, all inside the unchanged shared
maximum 96. Exact receipt input/output equality and exact same-input replay
remain mandatory. No tolerance, equation, receipt, custody, floor, cap,
rollback, finalization, publication, persistence, or diagnostic rule changes.

Eighteenth, `SC-SNOWENERGY-001` v46 corrects the incomplete safeguarded-step
budget waste proven by retained r119/r120. Before a finite-difference step
charges its first column, it must preflight the exact residual dimension, one
mandatory trust trial, and the unchanged role reserve. A sub-tolerance private
polish that cannot fund the whole minimum step carries its matching best bundle
to V35 stabilization without a new charge; an above-tolerance solve fails typed.
Per-map reserve checks remain binding for reverse columns and rejected trust
trials. The generalized Jacobian/Newton/trust method, physical residuals,
tolerances, one shared maximum 96, exact immutable receipt iteration,
independent replay, rollback, finalization, publication, persistence, and
diagnostic policy do not change. Canonical sufficiency remains an explicit r121
qualification question rather than an assumed closure claim.

Nineteenth, `SC-SNOWENERGY-001` v47 corrects the stale single-transaction
atomic complete-owner guard exposed after r121 cleared the prior `60 s`
receipt-stabilization blocker. Native-V2 accepted soil installation must first
prove vegetation, LSE, and biogeochemistry source transactions mutually equal,
and prove the accepted soil state sealed to its authenticated target. It may
then admit only exact source-equals-target first-child custody or exact
authenticated soil-predecessor-equals-source composed second-child custody.
Numeric adjacency, inferred succession, source/target copying, foreign,
swapped, missing, stale, or disagreeing owner identities remain typed refusal.
The install retains clone-then-replace atomicity, exact no-op, rollback,
support/receipt/carry custody, and no private publication. Physics, energy
values, equations, tolerances, the shared maximum 96, the exact 60-second
floor, events, topology, persistence, diagnostics, and adaptive timing do not
change.

Twentieth, `SC-SNOWENERGY-001` v48 corrects the remaining real ordinary
fixed-point finalizer exposed by r122. That route already reconstructs the
exact prepared native-V2 soil target and accepted result, but calls the strict
generic installer after discarding the prepared-beginning transaction posture.
V48 adds one distinct authenticated-prepared-beginning final install that
validates the authoritative resident, complete prepared target/predecessor/
support/receipt custody, accepted result, state/layer target seals, and
orchestrator seals before carrying an explicit V39 source/soil-target authority
through unchanged V47 atomic validation. The generic/public installer remains
strict same-ID. No adjacency inference, source/target copying, guard bypass,
partial install, rollback mutation, or private publication is allowed. Physics,
ledgers, tolerances, the shared maximum 96, exact receipt replay, the exact
60-second floor, events, topology, persistence, diagnostics, and adaptive
timing do not change.

Twenty-first, `SC-SNOWENERGY-001` v49 corrects the later repeated-child
transaction posture exposed by r123/r124. At exact direct support
`1920..2040 s`, the fixed V11 parent retains mutually equal vegetation/LSE/BGC
source transaction 42 while the independently advancing authenticated soil
resident is transaction 43 and its prepared successor is target 44 with exact
predecessor 43. V49 adds one opaque authenticated prepared-install authority
binding all three roles and the complete resident/prepared custody. The
authority is independently reconstructed at install before unchanged atomic
clone-and-replace mutation. It contains no adjacency inference or transaction
repair and does not rebase outer owners. Generic installation and V48's exact
predecessor-equals-source path remain strict. Resident, prepared, accepted,
seal, authority, or source-owner substitution fails before mutation with
byte-exact rollback and no publication. Physics, ledgers, tolerances, shared
maximum 96, exact receipt replay, the exact 60-second floor, events, topology,
persistence, diagnostics, and adaptive timing do not change.

Twenty-second, `SC-SNOWENERGY-001` v50 corrects the source anchor exposed by
r125-r129. The exact authenticated constitutive beginning may lawfully retain
different per-owner transaction postures (captured vegetation 41, LSE 40,
BGC 41, soil 41), so it is not eligible for the mutually-equal ending-source
validator. The already validated covered V8 envelope owns the exact ending
source transaction. V50 passes `envelope.transaction_id()` explicitly through
the real fixed-point helper and V49 authority construction/install, requires
the candidate vegetation/LSE/BGC owners mutually equal that anchor, and
retains the heterogeneous beginning solely for exact soil resident/prepared
custody. It admits no numeric adjacency, source copying, owner rebasing,
receipt repair, or generic-path broadening. Physics, ledgers, tolerances,
shared maximum 96, exact receipt replay, the 60-second floor, events,
topology, persistence, diagnostics, and adaptive timing do not change.

Twenty-third, `SC-SNOWENERGY-001` v51 corrects the exact-floor V41 trigger
exposed by r130/r132. At `1920..1980 s`, one exact adjacent canonical phase
crossing is followed only by finite nonstagnant corrections inside the same
entered phase; their exact absolute enthalpy steps strictly decrease and the
within-phase correction directions alternate. V51 permits that bounded
post-crossing contraction while retaining V41's exact static joins,
constant-water bits, promoted-root chain, two-map cadence, opposite vapor
sides, raw-owner/no-A-B-A guards, one adjacent crossing, and shared-budget
reserve. Pre-crossing reversal, predicate reversal/skip/recross, multiple or
zero crossing, nonalternating/noncontracting/equal/stagnant/nonfinite
correction, water/static/cadence/side/owner/budget poison, or publication
posture remains a typed refusal. Only the unchanged authentic coupled solver
may be dispatched. No tolerance, equation, physics, shared maximum 96,
receipt, closure, event, topology, custody, rollback, publication, persistence,
diagnostic, adaptive-floor, or 60-second-floor rule changes.

Twenty-fourth, `SC-SNOWENERGY-001` v52 corrects the authentic CN receipt
two-cycle exposed by r133/r134. The private coupled map previously eliminated
snow--soil CN heat algebraically from proposed temperatures, while authentic
receipt probes consumed a sealed heat value and reconstructed its successor
from the physical endpoint. V52 adds exactly one ordered snow-candidate CN
heat coordinate `Q_cn` per covered lane in `J m^-2 OFE-ground`, positive into
snow, and solves physical `R_Q=Q_cn-Q_cn,physical` with the unchanged lane
energy tolerance. The typed unpublished CN operand consumes the coordinate
exactly once; it is not a fabricated receipt. Shared maximum 96, complete-step
preflight, V35 exact receipt iteration and same-input replay, authentic
finalization, ledgers, events, topology, custody, rollback, no-publication,
and the exact 60-second floor remain unchanged. Receipt repair, averaging,
digest distance, `F(x)-x`, uncharged physics, and tolerance/cap/floor changes
remain forbidden.

Twenty-fifth, `SC-SNOWENERGY-001` v53 corrects the V52 initial-coordinate
posture exposed by r135/r136. The Q seed was taken from an accepted retained
receipt while W/H/rho/E/T described an already-produced later endpoint. V53
requires fresh active-set and retained-legacy seeds alike to reconstruct Q
from the exact endpoint Stage 3/soil candidate pair associated with solver
dispatch through the unchanged
canonical receipt formula. This representational assembly performs no
physical map, consumes no shared-budget charge, and changes no non-Q seed. The V52 equation,
residual, tolerance, complete-step preflight, shared maximum 96, exact receipt
stabilization/replay/finalization, exact 60-second floor, ledgers, events,
topology, custody, rollback, and no-publication remain unchanged. Cross-map
receipt seeding, repair, uncharged physics, and tolerance/cap/floor changes
remain forbidden.

Twenty-sixth, `SC-SNOWENERGY-001` v54 adds one bounded opportunity after exact
receipt iteration proves a cycle of at most three members. Each member's own
already-produced Stage 3/soil artifact and output receipt reconstruct a
complete representable W/H/rho/Q/E/T endpoint. The entire observed cycle plus
one replay is preflighted inside the existing shared maximum 96; each witness
attempt is one charged authentic full map under unchanged residual/z/side/
branch/custody closure. Only exact input==output receipt followed by exact
same-coordinate/same-input replay may proceed. A failed search makes no
absence claim and fails closed with rollback. Q-only enumeration cannot alter
the authentic physical map because sealed receipt Q remains the consumed
operand. Averaging, interpolation, nextafter or arbitrary ULP search, receipt
repair, uncharged physics, tolerance/cap/floor changes, and private publication
remain forbidden.

Twenty-seventh, `SC-SNOWENERGY-001` v56 rejects the transient-witness posture
disproved by r144 and adds a distinct strictly frozen, noncrossing
temperature-primary material posture before V55. Its private coordinates are
ordered `W/T_s/rho` plus top-soil `E/T`; exact snow enthalpy is reconstructed
from exact `W`, exact `c_ice`, exact `273.15 K`, and exact `T_s`, then split by
round-nearest-even into one binary64 high word plus an exact dyadic additive
carry. Unchanged CN heat is derived and consumed exactly once. Only a fresh
authentic Stage 3 result can create the compound snow material owner and
additive carry receipt, and the complete CN-plus-carry receipt set must
stabilize exactly, replay independently, and pass strict finalization before
admission. Restart V5 preserves committed, pending, and in-progress compound
owner/carry chronology; V4 migration assigns only canonical zero carry and a
nonzero carry blocks downgrade. Every physical map remains in the same shared
maximum-96 account, the exact 60-second floor and all physics/ledger/event/
topology/custody/rollback guards remain unchanged, and no V54/V55 transient
witness may be promoted into material authority.

Twenty-eighth, `SC-SNOWENERGY-001` v57 corrects the exact r147 dispatch
predicate without normalizing physics. A finite nonnegative external-liquid
operand through the existing terminal physical snow-closure floor of
`1.0e-12 kg m^-2 OFE-ground` is neutral only to frozen-specialization
eligibility; its exact bits, mass, refreeze latent energy, receipts, replay,
finalization, and independent closure remain mandatory. Any corresponding
refreeze must exactly consume that bounded operand into frozen material with
zero ending liquid and no bulk phase/event transition. The same zero-charge
coordinate transition may occur from the first tolerance-closed legacy root
immediately before V55 while retaining the already-used shared budget. Above-
bound, negative, nonfinite, unmatched-refreeze, ending-liquid, melt, or event
cases remain ineligible; any error after the first charged V57/V56 map remains
fail-closed without fallback. No equation, physical tolerance, 96 cap,
60-second floor, topology, custody, receipt, rollback, persistence, diagnostic,
or publication rule changes.

## Intended write set

- `docs/work-packages/README.md`;
- this package tree;
- `crates/openwepp-coupled-time/src/event.rs`;
- `crates/openwepp-coupled-time/tests/authority.rs`;
- `crates/openwepp-biogeochemistry/src/lib.rs`;
- `crates/openwepp-runner/tests/watershed_cli_behavior_contract.rs`;
- `crates/openwepp-runner/tests/test_fixture_authority_contract.rs`;
- `tests/fixtures/watershed/p102-sediment-active/README.md`;
- `tests/fixtures/watershed/p102-sediment-active/input-manifest.sha256`;
- `tests/fixtures/watershed/p102-sediment-active/runs/H1.source.run.snow_stage3_v11_owner_seed.json`;
- `tests/integration/vegetation_boundary_authority_contract.rs`;
- `crates/openwepp-vegetation/src/v11.rs`;
- `crates/openwepp-vegetation/src/v11/tests/v11_bgc_tests.rs`;
- `assurance/v2/identity.lock.json`;
- `assurance/v2/reports/linear-groundwater-reservoir-recurrence/review.lock.json`;
- `assurance/v2/reports/native-forest-canopy-phenology-evaluation/review.lock.json`;
- `assurance/v2/reports/snow-and-frozen-soil-process-evaluation/report.yaml`;
- `assurance/v2/reports/snow-and-frozen-soil-process-evaluation/review.lock.json`;
- the single typed `adopt-report-source` receipt generated under
  `assurance/v2/transactions/`;
- `tools/release/authority-policy/impact-map.json` for exact admission of newly
  touched science-bound test surfaces;
- `docs/specifications/science-contracts/contracts/SC-SNOWENERGY-001.md`;
- `docs/specifications/science-contracts/index.md`;
- `tests/integration/snow_terminal_enthalpy_event_numerics_contract.rs`;
- `crates/openwepp-hillslope-orchestrator/src/snow_stage3_v11_adaptive_execution_tests.rs`;
- `crates/openwepp-hillslope-orchestrator/src/v11_covered/fixed_point.rs`;
- `crates/openwepp-hillslope-orchestrator/src/v11_covered/open_snow.rs`;
- `crates/openwepp-hillslope-orchestrator/src/v11_covered/open_snow_physical_support.rs`
  only for the V38 initial private soil candidate's explicit
  carrier-transaction/support binding and its exact regression tests;
- `crates/openwepp-land-surface-energy/src/transaction.rs`,
  `crates/openwepp-land-surface-energy/src/transaction_tests.rs`,
  `crates/openwepp-land-surface-energy/src/transaction_v3_tests.rs`,
  `crates/openwepp-land-surface-energy/src/transaction_native_v2_finalization_tests.rs`,
  and `tests/integration/land_surface_energy_real_hydrology_shadow_contract.rs`
  only for the post-r101 typed separation of the outer LSE transaction from
  the authenticated native-V2 soil-child transaction in
  `RuntimeTileIdentity`, including exact finalization joins and transaction
  poisons;
- `crates/openwepp-hillslope-orchestrator/src/land_surface_energy_shadow/v8_input_projection.rs`,
  `crates/openwepp-hillslope-orchestrator/src/land_surface_energy_shadow/strict_v8_endpoint.rs`,
  `crates/openwepp-hillslope-orchestrator/src/land_surface_energy_shadow/strict_v8_endpoint_tests.rs`,
  `crates/openwepp-hillslope-orchestrator/src/land_surface_energy_shadow/v3_multitile_adoption.rs`,
  and
  `crates/openwepp-hillslope-orchestrator/src/v9_real_consumer_shadow/direct_v9_real_consumer_shadow_impl.rs`
  only for carrying that exact authenticated soil-child transaction while
  retaining the unchanged outer LSE/forcing/canopy/hydrology transaction,
  plus first-child, second-child, foreign, missing, and out-of-order vectors;
- `crates/openwepp-hillslope-orchestrator/src/land_surface_energy_shadow/real_hydrology_execution.rs`,
  `crates/openwepp-hillslope-orchestrator/src/land_surface_energy_shadow/receiver_validation.rs`,
  `crates/openwepp-hillslope-orchestrator/src/v9_real_consumer_shadow_soil_thermal.rs`,
  and the existing `physical_soil_energy_operands_v2` call sites under
  `v11_covered/` and `v9_real_consumer_shadow/` only for the v39 typed
  source/soil-target transaction authority, exact two-identity operand digest,
  independent surface-cancellation replay, and required transaction poisons;
- `crates/openwepp-hillslope-orchestrator/src/v11_covered/open_snow_convergence_tests.rs`;
- new `crates/openwepp-hillslope-orchestrator/src/v11_covered/open_snow_convergence_v45_tests.rs`
  for the exact V45 move-out behavior vectors;
- new `crates/openwepp-hillslope-orchestrator/src/v11_covered/open_snow_convergence_v46_tests.rs`
  for the exact V46 complete-step budget and receipt-chain vectors;
- new `crates/openwepp-hillslope-orchestrator/src/v11_covered/phase_consistent_coupled_solve.rs`;
- `crates/openwepp-land-surface-energy/src/solver_covered_evaluation.rs` and
  `crates/openwepp-land-surface-energy/src/transaction.rs` only if the v33
  nested trial requires a read-only private evaluation seam, with no equation,
  tolerance, or public-schema change;
- `crates/openwepp-land-surface-energy/src/owner_envelope.rs`,
  `crates/openwepp-land-surface-energy/src/soil_thermal_v2_cutover_tests.rs`, and
  `crates/openwepp-hillslope-orchestrator/src/v9_real_consumer_shadow/v10_soil_thermal_v2.rs`
  only for the V38 authenticated unpublished/non-publishable numerical
  soil-coordinate projection, including one ordered top-layer coordinate per
  OFE with bit-exact retention of all lower layers; no
  accepted-owner/receipt/schema, physics, tolerance, or publication change;
- `crates/openwepp-hillslope-orchestrator/src/v9_real_consumer_shadow/v10_soil_thermal_v2.rs`
  and its included `v10_soil_thermal_v2_tests.rs` only for the V47 typed atomic
  complete-owner source/soil-target transaction posture, exact first/second-
  child vectors, identity poisons, rollback, and no-publication evidence;
- `crates/openwepp-hillslope-orchestrator/src/v9_real_consumer_shadow.rs`,
  `crates/openwepp-hillslope-orchestrator/src/v11_covered/owner_finalization.rs`,
  and
  `crates/openwepp-hillslope-orchestrator/src/snow_stage3_v11_adaptive_execution_stack_helpers.rs`
  only for passing the explicit V39 source/soil-target transaction authority
  through authenticated continuation installation; ordinary/public V2 install
  remains strict same-ID;
- `crates/openwepp-hillslope-orchestrator/src/v9_real_consumer_shadow/v10_soil_thermal_v2.rs`,
  its included `v10_soil_thermal_v2_tests.rs`, and
  `crates/openwepp-hillslope-orchestrator/src/v11_covered/owner_finalization.rs`
  only for the V48 authenticated-prepared-beginning fixed-point final install,
  explicit V39 source/soil-target authority propagation, exact real-callsite
  coverage, substitution poisons, rollback, and no-publication evidence;
- `tests/integration/dff_ws2_ksatadj_direct_runtime.rs`;
- `crates/openwepp-hillslope-orchestrator/src/direct_runtime/stage3_committed_publication.rs`;
- `crates/openwepp-hillslope-orchestrator/src/direct_runtime/01_publication.rs`;
- `crates/openwepp-hillslope-orchestrator/src/direct_runtime/stage3_committed_publication_wat5.rs`;
- `crates/openwepp-hillslope-orchestrator/src/direct_runtime/stage3_committed_publication_tests.rs`;
- `crates/openwepp-hillslope-orchestrator/src/direct_runtime/stage3_committed_publication_tests_tail.rs`;
- `crates/openwepp-hillslope-orchestrator/src/v9_real_consumer_shadow_publication_retention.rs`;
- `crates/openwepp-hillslope-orchestrator/src/v9_real_consumer_shadow_wb14_routing_tests.rs`;
- `crates/openwepp-runner/src/hillslope/tests03/wat5_output_transaction.rs`;
- `tests/integration/cli03_runner_contract_derived_tests.rs`;
- `docs/specifications/science-contracts/contracts/SC-LANDSURFACEENERGY-001.md`;
- `crates/openwepp-land-surface-energy/src/solver_covered_solve.rs`;
- exact existing/new covered-solver test modules under
  `crates/openwepp-land-surface-energy/src/` entered in the failure inventory;
- `crates/openwepp-hillslope-orchestrator/src/v9_real_consumer_shadow_wb14_tests.rs`
  as unchanged real-consumer evidence only;
- `crates/openwepp-hillslope-orchestrator/src/snow_stage3_v11_adaptive_execution.rs`;
- `crates/openwepp-hillslope-orchestrator/src/snow_stage3_v11_adaptive_execution_tests.rs`;
- V56 production dispatch may add
  `crates/openwepp-hillslope-orchestrator/src/v11_covered/phase_consistent_temperature_primary.rs`
  and
  `crates/openwepp-hillslope-orchestrator/src/v11_covered/open_snow_convergence_v56_tests.rs`,
  and may edit only the V56 seams in
  `v11_covered/{phase_consistent_coupled_solve.rs,open_snow.rs,fixed_point.rs}`;
- V56 compound material/carry custody may add
  `crates/openwepp-hillslope-orchestrator/src/snow_stage3_v11_snow_enthalpy_carry.rs`
  and edit only the necessary attachment/finalization/canonical-owner wiring;
- V56 restart may edit
  `crates/openwepp-persisted-restart-v1/src/{lib.rs,hydrology_restart.rs,snow_stage3_v11.rs}`
  and add
  `crates/openwepp-persisted-restart-v1/src/snow_stage3_v11_v5_snow_enthalpy.rs`
  plus its focused tests, solely for committed/pending/in-progress compound
  owner/carry preservation, canonical-zero V4 migration, and nonzero-carry
  downgrade refusal;
- `crates/openwepp-hillslope-orchestrator/src/snow_stage3_v11_attachment.rs`
  and new sibling
  `crates/openwepp-hillslope-orchestrator/src/snow_stage3_v11_prepared_support_identity.rs`
  only for the exact-move prepared-support identity line-count split;
- `crates/openwepp-hillslope-orchestrator/src/v11_covered/execution.rs`
  and new sibling
  `crates/openwepp-hillslope-orchestrator/src/v11_covered/execution_carrier_humidity.rs`
  only for the exact-move carrier-humidity/audit line-count split;
- `crates/openwepp-hillslope-orchestrator/src/snow_stage3_v11_qualification_crossjoin_tests.rs`;
- `crates/openwepp-hillslope-orchestrator/src/snow_stage3_v11_qualification_crossjoin_child_tests.rs`;
- `crates/openwepp-hillslope-orchestrator/src/snow_stage3_v11_attachment_receipts.rs`
  as unchanged poison/validator evidence only;
- `tests/integration/land_surface_energy_balance_authority_contract.rs` for the
  exact v13/`INV-LANDSURFACEENERGY-139` source binding exposed by 001F;
- `docs/specifications/science-contracts/contracts/SC-SURFACELIQUID-001.md`;
- `tests/integration/surface_liquid_hydrology_custody_authority_contract.rs`;
- `crates/openwepp-land-surface-energy/src/lib.rs`;
- `crates/openwepp-land-surface-energy/src/owner_envelope.rs`;
- `crates/openwepp-land-surface-energy/src/transaction.rs`;
- new `crates/openwepp-land-surface-energy/src/exact_dyadic_enthalpy.rs` and
  included focused tests;
- `crates/openwepp-hillslope-orchestrator/src/v9_real_consumer_shadow.rs`;
- `crates/openwepp-hillslope-orchestrator/src/v9_real_consumer_shadow_soil_thermal.rs`;
- `crates/openwepp-hillslope-orchestrator/src/v9_real_consumer_shadow_serialization.rs`;
- `crates/openwepp-hillslope-orchestrator/src/canonical_owner_bytes.rs`;
- `crates/openwepp-hillslope-orchestrator/src/land_surface_energy_shadow/mod.rs`;
- `crates/openwepp-hillslope-orchestrator/src/land_surface_energy_shadow/real_hydrology_execution.rs`;
- `crates/openwepp-hillslope-orchestrator/src/land_surface_energy_shadow/receiver_validation.rs`;
- `crates/openwepp-hillslope-orchestrator/src/land_surface_energy_shadow/finalization_sealing.rs`;
- exact focused soil-thermal receiver/closure test modules entered under
  `WGHL-FULL-001I` in the failure inventory before edits;
- `crates/openwepp-persisted-restart-v1/src/lib.rs`;
- `crates/openwepp-persisted-restart-v1/src/scientific_owners.rs`;
- `crates/openwepp-persisted-restart-v1/src/checkpoint.rs`;
- `crates/openwepp-persisted-restart-v1/src/projection.rs`;
- `crates/openwepp-persisted-restart-v1/src/host.rs`;
- `crates/openwepp-persisted-restart-v1/src/transaction.rs`;
- new `crates/openwepp-persisted-restart-v1/src/soil_thermal_v2.rs`;
- new `crates/openwepp-persisted-restart-v1/src/scientific_owners_v2.rs`;
- new `crates/openwepp-persisted-restart-v1/src/checkpoint_v2.rs`;
- new `crates/openwepp-persisted-restart-v1/src/projection_v2.rs`;
- new `crates/openwepp-persisted-restart-v1/src/host_v2.rs`;
- new `crates/openwepp-persisted-restart-v1/src/transaction_v2.rs`;
- new `crates/openwepp-persisted-restart-v1/src/v2_tests.rs`;
- `crates/openwepp-runner/src/hillslope/snow_stage3_v11_production_seed.rs` and
  exact successor seed tests only if compile or real-consumer adoption proves
  the versioned seed handoff is required;
- exact production/test paths under `crates/openwepp-assurance/`,
  `crates/openwepp-runner/`, `crates/openwepp-hillslope-orchestrator/`, and
  `tests/integration/` entered prospectively in the failure inventory after
  baseline/current classification;
- `.config/nextest.toml` only if measured timeout evidence proves a scheduler
  defect and semantic test duration/coverage remains unchanged.

## Required evidence

- source identities and retained-log hashes;
- complete deduplicated baseline/current failure inventories;
- causality classification for every current adverse outcome;
- focused before/after evidence for each correction family;
- exact-clean warnings-denied Clippy and full-workspace Nextest results;
- no-test-weakening audit, line-count reconciliation, terminal diff, dual
  independent review, and dual independent verification.

## Gate selection

Risk is `CRITICAL`: this package owns workspace-wide correctness execution and
may touch cross-domain orchestration/test infrastructure. Mandatory terminal
commands are:

- `nix develop -c cargo fmt --all -- --check`;
- `nix develop -c cargo clippy --workspace --all-targets --all-features -- -D warnings`;
- `env RUST_MIN_STACK=67108864 nix develop -c cargo nextest run --workspace --profile full`;
- every focused command named by the prospective failure inventory;
- `git diff --check` and exact terminal write-set reconciliation.

Additional A0/A1/A3, anti-evasion, contract, conservation, consumer, restart,
or publication commands become mandatory if the exact diff touches their
owning surfaces. No faster profile substitutes for the selected full profile.

Contract-first sequence for `WGHL-FULL-001D` is mandatory: amend canonical
authority; author/adjust contract-derived tests; record a failing
pre-implementation gate on the unchanged production implementation; only then
edit production code. Kernel-profile compliance and dual contract review are
blocking.

The v32 successor repeats that contract-first sequence. Its retained pre-red
must include the exact `1860..1920 s` vapor/latent operands, the unique
`alpha=0.04393657257739406` root, proof that affine latent interpolation leaves
an inadmissible nonzero `45.77845449909091 J m^-2` at zero vapor, and direct
support above 60 seconds. Mandatory green evidence includes opposite-sign/root/
branch-entry and same-sign-v31 vectors; mixed-disposition, capacity, nonfinite,
component/identity/event, cap, rollback, and no-intermediate-publication
refusals; real DFF accepted/rejected counts, widths, runtime, limiting reasons,
and independent mass/energy/vapor closure.

The v33 successor repeats that contract-first sequence independently. Its
retained pre-red must reproduce the exact 60-second and 120-second v32
transition-reset/96-cap refusal, including non-bit-equal asymptotically changing
raw owners, and fail on the current coordinate-map `F(x)-x` draft for absent
equation-level residual evaluator, shared physical-evaluation budget, and
`CoupledAuthentic` admission seams while retained contract oracles pass.
Mandatory green evidence binds exact trigger order/root coordinates/branch
predicates/joins, concrete `R_W/R_H/R_E/R_T`, algebraic CN/LSE/receipt side
constraints, independent cold/phase/fusion roots, and exact fresh replay. It
also includes roots distinct from every affine v31/v32 image;
nonfinite/identity/event/active-set/singularity/stagnation/domain/capacity/
component/soil/LSE/receipt/budget refusals; exact rollback; and real DFF
accepted/rejected counts, width histogram, evaluator/budget counts, wall time,
limiting reasons, and maximum mass/energy/vapor/soil/receipt residuals. Closure
requires zero repeated-96 failures and a material reduction from retained ~1435
accepted/~1500 rejected; the provisional 4x gate for both counts, accepted-time
fraction at `>=900 s`, and stable-support maximum width remain performance
qualification without weakening any event tick or ledger tolerance.

The v34 successor repeats the contract-first sequence before any production
edit. Its expected red must pass the canonical authority assertions while
reporting absent `CoveredStableMonotoneSolveEligibilityV1`,
`covered_stable_monotone_solve_eligibility_v1`, and the exact eight-map
production behavior obligations. Mandatory green evidence covers exact-side
eight-map admission with evolving physical receipt digests and `H_hi/R`
coordinates; changed support/source/event/topology/custody/static receipt/
phase/carry authority or representation, failed exact high-plus-carry
reconstruction, nonfinite/nondecreasing merit, `A/B/A`, active-set, restart,
budget, private-admission, fallback, replay, reseal, rollback, and publication
refusals; and the canonical one-day performance/ledger report required by
`OBL-SNOWENERGY-C-026`. The report must include accepted/rejected counts,
step-width distribution, runtime, limiting rejection reasons, maximum ledger
closure, physical-evaluator/budget counts, repeated-96 outcomes, and absence of
production-persisted microstepping diagnostics.

The v35 successor repeats the contract-first sequence before production edits.
Its retained r83 evidence is `/tmp/wghl_001d_v34_64m_r83.log`, SHA-256
`bd091a4154eafff60309677e38bbbf598da6199cb15b3b84da93e7e23977b909`:
the exact `1800..1860 s` support refuses `phase-consistent authentic
replay/reseal` after `298.91 s`. The expected red must pass v35 authority while
reporting absent `CoveredAuthenticReceiptStabilizationV1`,
`covered_authentic_receipt_stabilization_probe_v1`,
`covered_authentic_receipt_stabilization_replay_v1`, and their behavior
obligations. Mandatory green evidence covers immutable `R_n -> R_(n+1)` probe
chaining, shared-budget charging, exact stabilization, first-reseal cross-input
probe posture, one independent same-stabilized-input exact residual/artifact/
receipt replay, exact receipt oscillation, nonfinite/side-constraint/budget/
replay refusals, private/probe artifact disposal, exact rollback, unchanged
custody/carry, and no intermediate acceptance/publication.

The v36 successor repeats contract-first sequencing. Retained r88 evidence is
`/tmp/wghl_001d_v35_64m_r88_field_audit.log`, SHA-256
`55a904fbbb35126a00f50af60ba3c7d296e3298c575a784cbd3eedaa7f24ec65`:
lane-1 thickness bits change `4569208177783694401 -> 4569208162027237604`
(`3.06254815664577839e-3 -> 3.06254132337190239e-3 m`, delta
`6.833273876e-9 m`) while finalization LSE/boundary close and Stage 3 rejects.
Expected red must report absent density-coordinate/residual seams and behavior
vectors. Green evidence requires exact `I/rho`, physical `R_rho`, generalized
residuals, exact branch authority with evolving rho/z, shared budget, v35
receipt/finalization, and all domain/map-difference/interpolation/repair/
uncharged/bypass poisons.

The v37 successor repeats contract-first sequencing. Retained r93 evidence is
`/tmp/wghl_001d_v36_64m_r93_finalization_audit.log`, SHA-256
`c4ddfef9dc52bdd085ff43d97f1179406486795696425c9aa949097fd756b0a5`:
the coupled root closes density exactly but lane-1 derived thickness changes
`4569208177783694401 -> 4569208162027237604`, delta
`6.833273876e-9 m`, across authentic finalization. Expected red must report
absent derived-thickness closure and merit/admission seams plus behavior
vectors. Green evidence requires exact proposed/physical `I/rho`, a
low-density case where `R_W` alone passes but `R_z` blocks, a captured
root-to-finalization regression that closes under unchanged `depth_abs_m`, no
additional coordinate or physical call, shared-budget accounting, v35 exact
receipt replay, unchanged authentic finalization, and all omission/
independent-z/interpolation/repair/uncharged/bypass poisons.

The v45 successor repeats contract-first sequencing. Retained r117 is
`/tmp/wghl_001d_v44_64m_r117.log`, SHA-256
`33ac890a9dbe05962363a0a5838b992d7ca2ad3c13e9fe2912f5555968748c5e`;
retained r118 is `/tmp/wghl_001d_v44_64m_r118.log`, SHA-256
`fb65dfbfd53d4f587a416ffc97d6e1aca9a4d4a8cfd0ac2b49e925c265edc858`.
The expected red must bind absent `CoveredCoupledPolishedRootV1`,
`phase_consistent_coupled_root_polish_v1`, purpose-specific shared-budget
reservation, matching carried artifacts, and the exact named V45 behavior
vectors. Green evidence requires private solver/polish/probe reserve roles;
strict progress using only existing physical residuals; exact-zero and finite
sub-tolerance stationary stops; above-tolerance, singular, nonfinite,
side/branch, stale-rejected-artifact, separate/reset-budget, reserved-slot,
receipt-repair, oscillation, replay, rollback, and publication poisons; and the
canonical one-day performance/ledger report with no persistent diagnostics.

### WGHL-FULL-001D v33 corrective failure inventory

| ID | Current adverse condition | Required corrective seam | Expected-red proof |
|---|---|---|---|
| `V33-TRIGGER-001` | Full bitwise raw-authentic `A==A` is unreachable because continuous authentic owner fields change asymptotically across the retained reset. | Detect the exact root/interface, branch-entry, opposite-authentic, same-root/reset transition record under unchanged joins; compare raw authentic images only for required pure opposite vapor disposition and branch predicates. | Contract oracle accepts differing raw continuous coordinates and refuses changed support/source/event/topology/custody/receipt, reset coordinate, branch predicate, order, or vapor side. |
| `V33-RESIDUAL-001` | The current draft constructs `residuals = mapped_coordinates - coordinates`, which is `F(x)-x`, rather than evaluating physical ledger equations. | Add sealed `CoveredPhaseConsistentResidualInputsV1`, `CoveredPhaseConsistentResidualEvaluationV1`, and `covered_phase_consistent_residual_evaluate_v1` carrying concrete `R_W/R_H/R_E/R_T` plus CN/LSE/receipt side constraints. | Source-bound pre-red reports the forbidden map-difference draft and missing true evaluator carriers/seam. |
| `V33-BUDGET-001` | A local remaining counter and per-call `evaluations_used` do not prove one budget across Jacobian, rejections, fresh evaluation, and final replay. | Add one `CoveredPhysicalEvaluationBudgetV1` owned by the complete covered solve and charged before every physical evaluation without nested reset. | Source-bound pre-red reports the missing shared budget owner/charge seam. |
| `V33-ADMISSION-001` | The current draft has no typed route for a freshly replayed physical coupled root to avoid re-entering the same Picard equality failure. | Add private `CoveredConvergenceAdmissionV1::CoupledAuthentic`, bypassing only Picard equality/convergence and retaining all residual, finalization, reseal, rollback, and publication guards. | Source-bound pre-red reports the missing admission enum/variant and finalization dispatch seam. |

### WGHL-FULL-001D v34 stable-monotone failure inventory

| ID | Current adverse condition | Required corrective seam | Expected-red proof |
|---|---|---|---|
| `V34-TRIGGER-001` | Stable terminal-one-volume supports contract monotonically for about 93 raw Picard evaluations and therefore reach the unchanged 96-evaluation ceiling before ordinary stable widths can advance. Byte-equal full physical receipt digests and byte-equal `H_hi/R` coordinates cannot identify genuine contraction because those values necessarily evolve. | Admit the unchanged physical solver after exactly eight raw authentic maps satisfy exact static joins/phase/carry authority and representation, exact evolving `E=exact(H_hi)+R` reconstruction, finite residuals, strict merit decrease, and no cycle/active-set/restart condition. Physical receipt digests and carry coordinates may evolve only under unchanged custody/closure/reseal rules. | Contract assertions pass while the source-bound test reports missing `CoveredStableMonotoneSolveEligibilityV1`, `covered_stable_monotone_solve_eligibility_v1`, evolving-coordinate behavior, and eight-map behavior vectors. |
| `V34-BUDGET-001` | A new trigger could incorrectly reset or double-count the 96-evaluation account. | Charge all eight raw maps and every solver/fresh/replay evaluation to the existing `CoveredPhysicalEvaluationBudgetV1` without reset. | Contract/source binding requires an already-charged shared budget and exact remaining-budget vectors. |
| `V34-FALLBACK-001` | Private solver refusal could leak a trial, consume a new budget, or force immediate support rejection. | Before a root, destroy all private trials and resume raw authentic Picard using only the remaining shared budget; no private trial may accept. | Contract/source binding requires private-trial disposal, remaining-budget fallback, and acceptance/publication poison vectors. |

### WGHL-FULL-001D v35 authentic-receipt failure inventory

| ID | Current adverse condition | Required corrective seam | Expected-red proof |
|---|---|---|---|
| `V35-RECEIPT-STABILIZATION-001` | R83 reaches a coupled root, then compares exact replay/reseal artifacts across different physical receipt inputs and refuses a lawful evolving authentic receipt. | Feed reconstructed `R_(n+1)` as the next immutable physical input until canonical input/output receipts are exactly equal; classify the first root reseal as a charged probe, never replay evidence. | V35 authority assertions pass while source binding reports absent stabilization carrier/probe/replay seams and behavior vectors. |
| `V35-REPLAY-001` | Receipt equality alone does not prove the stabilized physical result is independently reproducible. | After exact receipt stabilization, run one independent physical replay under the same receipt input and require exact finite residual, complete artifact, and reconstructed-receipt equality before `CoupledAuthentic`. | Source-bound expected red requires same-input exact replay behavior and refuses cross-input comparison or replay disagreement. |
| `V35-BUDGET-ROLLBACK-001` | Repeated probes could reset the budget or leak private artifacts; exact receipt maps can oscillate. | Charge every probe/replay to the existing shared 96-evaluation budget; refuse oscillation, nonfinite/constraint/budget/replay failure; discard every private/probe artifact and preserve exact rollback. | Source-bound expected red requires shared-budget, oscillation/refusal, disposal, rollback, and no-publication behavior. |

### WGHL-FULL-001D v36 geometry-complete failure inventory

| ID | Current adverse condition | Required corrective seam | Expected-red proof |
|---|---|---|---|
| `V36-GEOMETRY-001` | R88 closes LSE/boundary but rejects Stage 3 on a material terminal thickness coordinate absent from the reduced solve. | Add one `rho` coordinate, derive `z=I/rho`, and evaluate unchanged constitutive `R_rho`. | Authority passes while source test reports absent coordinate/evaluator/generalized residual seams. |
| `V36-BRANCH-001` | Requiring byte-equal rho/z would prevent genuine contraction; allowing branch drift would change physics. | Preserve exact layer/order, settling authority, and density-model branch while rho/z evolve under physical residuals. | Positive evolving-coordinate vector plus branch/layer/settling poisons. |
| `V36-BUDGET-FINALIZATION-001` | Geometry physics could be uncharged or bypass v35 receipt/authentic finalization. | Charge all geometry evaluations to the same 96 budget and require v35 stabilization/finalization unchanged. | Source red requires shared-budget, no-bypass, rollback, and no-publication vectors. |

### WGHL-FULL-001D v37 derived-thickness failure inventory

| ID | Current adverse condition | Required corrective seam | Expected-red proof |
|---|---|---|---|
| `V37-DERIVED-DEPTH-001` | R93 closes the existing water and density residuals, but at low density the admitted water-mass error yields a `6.833273876e-9 m` derived thickness change, above unchanged `depth_abs_m`. | Derive physical `R_z` from canonical proposed/physical `I/rho` and require it in scaled merit and root admission without adding a coordinate or equation. | Authority passes while source binding reports absent `CoveredDerivedThicknessClosureV1`, evaluator, result carrier, and admission seam. |
| `V37-BUDGET-001` | A depth check implemented as a second constitutive evaluation would silently consume uncharged physics or reduce the shared cap. | Derive `R_z` from the same physical image already used for `R_W/R_H/R_rho`; preserve one shared 96-evaluation budget. | Behavior vector proves one budget charge produces both residual vector and derived depth closure; duplicate/uncharged map poison refuses. |
| `V37-REPLAY-FINALIZATION-001` | A private root could pass derived depth but bypass v35 receipt stabilization or the authentic finalization comparison. | Require derived-depth closure on fresh/probe/replay evaluations and retain v35 exact receipt replay plus unchanged authentic finalization. | Captured root-to-finalization vector, same-input replay, omission/bypass poisons, rollback, and no-publication tests. |

### WGHL-FULL-001D v38 finalization-equivalent-map failure inventory

| ID | Current adverse condition | Required corrective seam | Expected-red proof |
|---|---|---|---|
| `V38-MAP-IDENTITY-001` | R95 closes solver and stabilized replay `R_z=0`, but authentic finalization rebuilds different carrier operands and reproduces the same thickness change. | Make the authentic finalization-equivalent endpoint map the sole charged physical residual/replay map. | Authority passes while source binding reports absent `covered_phase_consistent_finalization_equivalent_map_v1` and endpoint-map carrier. |
| `V38-ONE-MAP-BUDGET-001` | A provisional pre-map followed by a finalization map would double physical work or hide an uncharged Stage 3 call. | Reconstruct proposed endpoint operands before physics and perform exactly one Stage 3 map per shared-budget charge. | One-charge/one-map vector plus duplicate/uncharged/provisional-fallback poisons. |
| `V38-REPLAY-PUBLICATION-001` | Solver-map equality could again be accepted without exact finalization equivalence. | Retain v35 receipt stabilization and same-input replay, then independently reevaluate finalization and require exact complete-image equality before publication. | Real endpoint replay vector plus receipt, map-image, rollback, finalization-bypass, and private-publication poisons. |
| `V38-MULTILAYER-SOIL-001` | The first V38 implementation required exactly one soil layer per OFE even though the canonical owner has multiple ordered layers; supplying every layer through the total-enthalpy coordinate API would reround and zero deeper exact carries that are not solver coordinates. | Project only the first ordered snow-coupled `E/T` coordinate per OFE and clone every deeper layer bit-exact, including high, carry, temperature, identity, order, and custody. | Multi-layer exact-carry preservation vector plus missing/reordered/topology/carry-substitution poisons. |

### WGHL-FULL-001D v39 soil-energy transaction-custody failure inventory

| ID | Current adverse condition | Required corrective seam | Expected-red proof |
|---|---|---|---|
| `V39-SOURCE-TARGET-TX-001` | R102 reaches a valid second composed child whose surface ingress remains outer transaction 42 and whose authenticated soil target is transaction 43, but the operand builder requires one value to identify both. | Add `PhysicalSoilEnergyTransactionAuthorityV2` with exact source and soil-target identities; validate each owner against its own field. | Contract assertions pass while source binding reports the absent two-authority seam and retained one-transaction ingress comparison. |
| `V39-DIGEST-REPLAY-001` | Replacing the ingress check alone could detach the debit-credit digest or independent cancellation replay from one transaction authority. | Bind both identities, exact support, source owner, candidate/receipt lineage, layer, ordinal, basis, and energy in every internal/infiltration digest and rebuild the same digest independently. | First/second-child digest vectors and source/target/receipt/support substitution poisons. |
| `V39-ROLLBACK-001` | Adjacent-transaction inference or copied authority could admit a private operand under the wrong target. | Derive neither value; use already-authenticated owners, refuse zero/stale/foreign/swapped/out-of-order identities, and preserve exact rollback/no-publication. | Typed refusal and before/after owner-byte vectors. |
| `V39-CONTINUATION-TX-001` | R103 passed V39 operand construction, then compared authenticated second-child soil successor 43 to outer source 42 in unpublished physical-beginning custody. | Remove the outer transaction from soil-only physical-beginning/fixed-point joins; bind exact target and predecessor from independently validated prepared soil owner custody. | Second-child successor vector plus foreign transaction/owner/support/prior poisons and unchanged resident bytes. |
| `V39-PRIVATE-SUPPORT-TX-001` | R104 passed V8, operand, and continuation joins, then covered initial/finalization-equivalent private support preparation still selected outer transaction 42 instead of authenticated soil successor 43. | Prepare exact initial and coordinate-projection child supports through the resident's validated next-support custody chain; retain outer transaction 42 only for source/LSE forcing and require projected endpoint seed transaction/support to equal the prepared owner. | Source-bound negative guard against raw outer preparation, exact first/second-child successor vectors, seed transaction/support joins, foreign transaction/support/owner poisons, unchanged private/no-publication posture, and canonical composed consumer. |
| `V33-ROLLING-RESET-001` | R105 cleared transaction custody, then the exact `1860..1920 s` active-set cycle exhausted 96 Picard maps because the first nonexact interface reset left the stale first root latched forever. | Retain the existing exact reset predicate, but make its three-transition observation window rolling: a failed reset promotes the current validated interface and clears branch-entry state; only a later exact complete window dispatches the unchanged coupled solver. | Stale-first/nonexact-reset then exact-reset activation; join/coordinate/phase/opposite-side nonexact windows rearm without dispatch; unchanged shared budget, authentic replay, rollback, and no-publication. |
| `V40-EARLY-PARITY-TRIGGER-001` | R107 proved the rolling exact detector eventually dispatches correctly but only at shared budget 96, leaving no charged solver evaluation. | Admit an earlier trigger after four exact-static consecutive nonexact reset windows with exact promoted-root chaining and strictly decreasing finite positive root drift; require the unchanged shared-budget minimum path reserve and dispatch only the unchanged solver. | Retained r107, four-window descent/chain/cadence, nonfinite/stagnation/reversal/A-B-A/static-join/phase/side/budget poisons, authentic-only replay/finalization, rollback/no-publication, and canonical one-day qualification. |
| `V41-ONE-WAY-PHASE-BOUNDARY-001` | R109 proved the real four-window sequence has exact constant water and a strictly one-way enthalpy sweep across zero, while V40 drift descent/fixed-phase assumptions refuse. | Admit only one exact adjacent canonical enthalpy-boundary bracket under static joins, promoted-root chain, opposite sides, two-map cadence, and the unchanged shared-budget reserve; dispatch only the unchanged solver. | Retained r109 exact chain; reversal/stagnation/no-or-multiple-crossing/direct-jump/water-drift/join/side/chain/cadence/budget/publication poisons; authentic replay/finalization and one-day qualification. |
| `V42-COLD-CONTENT-EXPORT-001` | Authentic snow-reappearance endpoints close water and cumulative ledgers but differ from the V31/V32 support enthalpy by the exact positive cold-content magnitude exported with sublimated ice. | Carry authentic `X_c` as a distinct ordered support operand and reconstruct `H=-C_0+L_f(L_0+L_in)+Q+X_c` for endpoints and private contractions without changing the physical ledger. | Captured `+2.3211191058 J m^-2` vector; zero-export byte lock; omission/nonfinite/negative/order/weight/substitution/closure poisons; persisted-restart 71/71 and canonical one-day qualification. |
| `V43-PROJECTED-BASE-CUSTODY-001` | R113 proves the real V38 numerical-coordinate sibling reaches the endpoint carrier but the generic fixed-point validator reconstructs it as ordinary base unpublished physics and rejects its correct `NumericalCoordinateProjection` custody. | Preserve a distinct typed numerical-coordinate same-support fixed-point posture with exact prepared-owner, receipt, authority/set, topology/order, carry, lower-layer, and empty-credit validation; retain ordinary base reconstruction and all sequential/acceptance/publication refusals. | Retained r113 SHA-256 `9c1d35d0f34991bec6386cbef9b6ca1295f6ca2e281e9774ba4b6bade5df3188`; exact custody first difference; positive endpoint-carrier vector plus erased/mixed/foreign/support/receipt/authority/set/order/carry/lower-layer/credit/sequential/acceptance/publication poisons. |
| `V44-UNCOMMITTED-LSE-CLOSURE-001` | R116 proves arbitrary coupled coordinates fail strict weighted-OFE closure before corrected boundary exchange is available; exact local residuals change from `+423.500683` to `-15011.962665` and `-8349.393300 J m^-2 tile`. | Use the existing provisional LSE posture only for charged `PrivateTrial` exchange reconstruction; require strict unchanged weighted closure for authentic probes, independent replay, and finalization. | Retained r116 SHA-256 `b4046720e3719f4736408833bfa1fc32a23e9bfc95aca8219c760bef9c59aadb`; posture selector, corrected-boundary, strict probe/replay/finalization, closure poison, rollback, and no-publication vectors. |
| `V44-SOIL-CN-EXACT-ONCE-001` | A posture correction could omit the projected top-soil E/T coordinate or incorrectly apply it both to the snow--soil CN calculation and the Stage3-covered V8 soil control volume. | Retain exact projected-coordinate use in CN receipt/credit reconstruction once; retain admitted V8 soil beginning with zero Stage3-covered ground heat/storage. | Exact coordinate-to-CN vector, changed-coordinate sensitivity, omission and V8-double-use poisons, unchanged lower-layer/carry/custody, and no-publication evidence. |

### WGHL-FULL-001D v45 authentic-receipt root-polishing failure inventory

| ID | Current adverse condition | Required corrective seam | Expected-red proof |
|---|---|---|---|
| `V45-ROOT-POLISH-001` | R117 reaches unchanged tolerance closure, but the private root remains above representational receipt fixed-point precision and produces a long nonrepeating exact CN receipt tail. | Carry one matching complete root bundle and continue the existing physical generalized-Jacobian/Newton/trust map through strict canonical merit descent; sub-tolerance stationarity may only seed exact V35 stabilization. | Contract/source authority passes while production lacks the typed polished-root carrier/function and exact-zero, sub-tolerance, above-tolerance, stale-artifact, and no-`F(x)-x` vectors. |
| `V45-BUDGET-RESERVE-001` | R118 proves the solver and authentic probes may consume every shared charge, leaving no independent replay capacity. | Refuse private solver maps that would leave fewer than three charges, polishing maps that would leave fewer than two, and nonstable probes that would consume the final replay charge; keep one owner, one charge site, and maximum 96. | Start-near-cap vectors prove solver/polish reservation, stable probe+replay ending exactly at 96, evolving probe refusing with replay slot unused, and reset/separate/enlarged-budget poisons. |
| `V45-EXACT-ADMISSION-001` | A stricter private root could be mistaken for authentic convergence or used to tolerance-compare/repair receipts. | Retain immutable exact `R_n -> R_(n+1)`, exact canonical input/output equality, one independent same-input exact residual/artifact/receipt replay, unchanged finalization, exact rollback, and no private/probe publication. | Receipt one-bit evolution/oscillation, digest repair/interpolation, replay-input substitution, private admission/publication, rollback, and exact rerun vectors. |

### WGHL-FULL-001D v46 complete-step budget-preflight failure inventory

| ID | Current adverse condition | Required corrective seam | Expected-red proof |
|---|---|---|---|
| `V46-PARTIAL-STEP-WASTE-001` | R120 enters receipt stabilization at used 94 after 17 polish charges; the final safeguarded step charged a partial or complete Jacobian tail but could not fund the trust trial required to update the carried root. | Before the first column, preflight exact `dimension + one trust trial + downstream reserve`; a failing sub-tolerance preflight returns the carried bundle without a charge, while above tolerance remains typed budget failure. | Retained r119/r120 hashes, exact source/accounting trace, dimensions 1/5/multiple, boundary/one-short vectors, and zero-partial-charge assertions. |
| `V46-PER-MAP-RESERVE-001` | Whole-step preflight alone could be misread as authority to let reverse columns or rejected trust trials consume receipt/replay capacity. | Retain every existing per-map reserve check after preflight and protect the final replay slot through unchanged V35 stabilization. | Reverse-column, twelve-backtrack, evolving multi-probe, stable-probe-plus-replay-at-96, and reserve-theft poisons. |
| `V46-NUMERICAL-SCOPE-001` | Budget recovery could silently introduce stale-J/quasi-Newton or a receipt/digest/map-difference residual without evidence. | Preserve the exact existing finite-difference generalized Jacobian/Newton/trust map and residual vector; explicitly forbid chord/secant/Broyden state and any new receipt equation in V46. | Source obligations reject new numerical state/residual seams, enlarged/reset/separate budgets, partial artifact use, rollback mutation, and private publication. |

### WGHL-FULL-001D v47 atomic complete-owner transaction failure inventory

| ID | Current adverse condition | Required corrective seam | Expected-red proof |
|---|---|---|---|
| `V47-ATOMIC-TX-POSTURE-001` | R121 clears the prior `60 s` numerical blocker, then a lawful composed `1800..1980 s` install carries outer vegetation/LSE/BGC source transaction 42 and authenticated soil target 43 with exact predecessor 42; the legacy atomic guard requires all owners equal to 42 and rejects the target. | Add one typed posture that admits only mutually equal source owners plus soil state sealed to target, with exact source==target or exact authenticated soil predecessor==source. | Retained r121 hash, exact same-ID first-child and source42/target43/predecessor42 second-child vectors; unchanged V39/V46 gates. |
| `V47-ATOMIC-TX-POISON-001` | A broad successor guard could infer adjacency, copy the soil target into outer owners, or accept a foreign/swapped/missing predecessor. | Read source, target, and predecessor only from their authenticated owners and refuse every disagreement or substitution before mutation. | Foreign, swapped, missing, stale, out-of-order, source-owner disagreement, target/state, and inferred-adjacency poisons. |
| `V47-ATOMIC-ROLLBACK-001` | A failed late owner join could partially install soil state or expose an unpublished continuation. | Validate the complete posture before clone/install and retain exact accepted no-op, clone-then-replace atomicity, owner-byte rollback, and no private publication. | Before/after canonical owner bytes for every refusal plus exact no-op and successful first/second-child installs. |

### WGHL-FULL-001D v48 fixed-point final-install authority failure inventory

| ID | Current adverse condition | Required corrective seam | Expected-red proof |
|---|---|---|---|
| `V48-REAL-FINALIZER-AUTHORITY-001` | R122 reaches the real ordinary fixed-point finalizer with exact source 42, prepared target 43, and predecessor 42, but the wrapper supplies no continuation or precomputed candidate and the finalizer calls the strict generic installer with no split authority. | Add one typed authenticated-prepared-beginning final install and route the real fixed-point finalizer through it; construct explicit V39 authority only after exact authoritative-resident/prepared/accepted/seal validation. | Retained r122 hash, real source-bound callsite, exact source42/target43/predecessor42 behavior vector, and expected pre-implementation source/behavior red. |
| `V48-GENERIC-STRICTNESS-001` | Reusing V47's internal split option through the generic/public method would silently broaden all callers. | Keep `install_soil_thermal_accepted_v2` strict same-ID and expose a distinct typed finalization-only method requiring explicit authenticated prepared-beginning authority. | Generic split refusal, missing/foreign/swapped authority, and source-owner disagreement poisons. |
| `V48-PREPARED-CUSTODY-001` | A source/target pair alone could be replayed against a foreign prepared owner, support, receipt chain, accepted result, or seal set. | Validate the authoritative resident's exact prepared-beginning join and the accepted result plus seals against that same prepared beginning before atomic clone/install. | Target/predecessor/support/receipt/state/seal substitution, rollback, no-op, and no-publication vectors. |

### WGHL-FULL-001D v49 multi-child prepared-install authority failure inventory

| ID | Current adverse condition | Required corrective seam | Expected-red proof |
|---|---|---|---|
| `V49-THREE-DOMAIN-AUTHORITY-001` | R123/r124 reaches direct `1920..2040 s` with outer source 42, authenticated resident/predecessor 43, and prepared target 44; V48 incorrectly aliases the soil predecessor to the fixed outer source. | Add one opaque authenticated prepared-install authority binding mutually equal outer source owners, the exact resident/predecessor bundle, the exact prepared target/support/receipt/state, and unchanged explicit source/target operand authority; reconstruct it at install. | Retained r123/r124 hashes, exact source42/resident43/predecessor43/target44 runtime vector, and expected pre-implementation behavior/source red. |
| `V49-REPEATED-CHILD-001` | Native-V2 soil advances once per accepted child while V11 outer source remains fixed for the parent, so the source/predecessor gap can grow and is not numeric authority. | Admit repeated children only from the exact authenticated resident and contiguous prepared successor; never infer adjacency to the outer source or rebase outer owners. | Exact third-child and further same-parent successor positives; noncontiguous, stale-resident, source-owner, predecessor, and target poisons. |
| `V49-ATOMIC-ROLLBACK-001` | A broad nonadjacent split flag could bypass V47/V48 or partially install a foreign resident/prepared/accepted bundle. | Keep generic/V48 paths strict and require exact opaque-authority equality before the existing clone-then-replace install. | Missing/foreign/swapped authority; resident/prepared/accepted/state/layer/receipt/support/seal substitutions; exact no-op, byte-exact soil/source rollback, and no-publication evidence. |

### WGHL-FULL-001D v50 envelope-source transition authority failure inventory

| ID | Current adverse condition | Required corrective seam | Expected-red proof |
|---|---|---|---|
| `V50-HETEROGENEOUS-BEGINNING-001` | R125 fails composed `1800..1980 s`; r129 proves the exact authenticated beginning is vegetation41/LSE40/BGC41/soil41, so V49's post-review mutual-source validation of that beginning is invalid. | Retain exact resident/prepared custody but derive no ending source from the heterogeneous beginning. | Retained r125/r129 hash/tuple, exact mixed-beginning real-helper vector, and source red rejecting `direct_complete_source(authoritative_beginning)`. |
| `V50-ENVELOPE-SOURCE-001` | Removing the invalid beginning join without a replacement would let a jointly rebased candidate mint authority. | Pass the already validated `UncommittedCoveredV8OwnerEnvelope.transaction_id()` into the helper and require the candidate's mutually equal vegetation/LSE/BGC source to equal it at construction and install. | Exact envelope source positive plus altered envelope source, candidate joint/individual rebase, vegetation/physical/BGC/material-receipt envelope poisons. |
| `V50-ROLLBACK-001` | A late source transition failure could mutate soil or publish private state. | Reconstruct exact envelope-source/V49 custody before the unchanged atomic install and preserve full-shadow rollback/no publication. | Missing/foreign/swapped source authority, resident/prepared/accepted/seal poisons, exact no-op, full-shadow rollback, and publication-history equality. |

### WGHL-FULL-001D v51 post-crossing contraction failure inventory

| ID | Current adverse condition | Required corrective seam | Expected-red proof |
|---|---|---|---|
| `V51-WITHIN-PHASE-CONTRACTION-001` | R130/r132 reaches one exact `0 -> 1` canonical crossing at the 60-second floor, then V41 rejects a finite alternating, strictly shrinking enthalpy correction entirely inside unchanged predicate 1. | Retain the one adjacent direction-consistent crossing and admit only post-crossing within-phase corrections whose exact positive finite `abs(delta H)` strictly decreases and whose correction directions alternate after the first. | Retained r130/r132 hashes and exact W/H/predicate/delta chain; expected-red source/behavior vector. |
| `V51-PHASE-GUARD-001` | Broadly allowing enthalpy reversal could admit a real phase recross, skipped boundary, pre-cross reversal, or divergent oscillation. | Keep every V41 guard and reject predicate reversal/skip/recross, multiple/no crossing, pre-cross reversal, nonalternating/noncontracting/equal/stagnant/nonfinite correction, water/static/cadence/side/raw-owner/A-B-A/budget poison. | Independent poison matrix with exact rollback and no solver dispatch/publication. |
| `V51-AUTHENTIC-ADMISSION-001` | Trigger eligibility could be mistaken for a numerical root or a reason to refine below the exact floor. | Dispatch only the unchanged charged coupled solver under the existing shared 96 budget; retain authentic residual, receipt stabilization, replay, finalization, closure, event, rollback, and no-publication requirements. | Dispatch seam/source guard plus retained V41/V45/V46/floor/receipt/finalization tests. |

### WGHL-FULL-001D v55 private Q-lattice witness failure inventory

| ID | Current adverse condition | Required corrective seam | Expected-red proof |
|---|---|---|---|
| `V55-SPARSE-CYCLE-MISS-001` | R139/r140 proves both exact V54 cycle artifacts map to different receipts even though the first tolerance-closed private root's own Q output lies only 21 positive binary64 values away. | At that first root and before V45 polish, traverse the complete root-exclusive, own-output-inclusive Q interval in deterministic total-order bits while every non-Q coordinate remains bit-identical. | Retained r139/r140 identities, exact two-member map rows, 21-member interval, expected-red exact Q witness. |
| `V55-ATOMIC-BUDGET-001` | Starting the finite interval without capacity for every candidate and protected authentic proof could consume the shared 96 budget without a lawful admission path. | Preflight the checked interval cardinality plus protected authentic probe and replay before the first candidate charge; every candidate is one full charged map and no double charge is permitted. | Exact-fit and one-short budgets, interval overflow/cardinality poisons, unchanged used count on preflight refusal. |
| `V55-PRIVATE-ONLY-001` | Applying Q enumeration to authentic receipt maps would overwrite sealed receipt heat and repair custody rather than solve the private physical equation. | Limit enumeration to `PrivateTrial`; admit only exact positive-zero physical `R_Q`, coordinate/own-output Q bit equality, all unchanged physical guards, then require unchanged whole-receipt stabilization, replay, and strict finalization. | Authentic substitution/refusal, signed-zero/nonfinite/no-witness/residual/z/branch/side/custody poisons, rollback and no-publication. |
| `V55-ELIGIBILITY-FALLTHROUGH-001` | R142 reaches a valid tolerance-closed root at budget 30 whose 1394-member interval cannot fit the unchanged 96 cap; treating capacity as a hard error suppresses lawful V45 polishing. | Keep Q shape/finiteness/lineage hard, but return zero-charge `NotApplicable` for unresolved-count, positive-domain, checked-cardinality, or interval-plus-two capacity misses. Commit immediately before the first candidate charge; no later error may fall back. | Exact r142 overcapacity with callback/budget unchanged followed by behavior-identical V45 polish; multi-Q/nonpositive misses; hard lineage/shape poisons; exact-fit commit and post-charge no-fallback. |

### WGHL-FULL-001D v56 frozen temperature-primary exact-carry failure inventory

| ID | Current adverse condition | Required corrective seam | Expected-red proof |
|---|---|---|---|
| `V56-TRANSIENT-WITNESS-001` | R144 exhaustively charges the entire 21-member V55 Q interval and finds no exact witness, so a transient cycle/lattice artifact cannot lawfully become snow material authority. | Dispatch a distinct strictly frozen/noncrossing temperature-primary solve before V55; never promote V54/V55 artifacts into material or carry custody. | Retained r144 SHA-256 `161712621295b503da41b065846304ce0e0198a26a9d9b97efa6d4012fa36c65`, exact last-candidate Q/RQ/Qout bits, source seam ordering, and transient-witness poison. |
| `V56-EXACT-ENTHALPY-001` | Binary64 `H` as a primary coordinate discards a representable frozen material remainder and leaves exact receipt fixed points dependent on transient Q choices. | Solve ordered `(W,T_s,rho,Esoil,Tsoil)`, reconstruct `H_exact=-exact(W)*exact(c_ice)*(exact(273.15)-exact(T_s))`, store RN-even high plus exact dyadic carry, and evaluate the unchanged ledger tolerance from their exact sum. | Frozen/noncrossing positive; formula, tie-even, cancellation, subnormal, overflow, one-bit high/carry/order, and exact-ledger poisons. |
| `V56-COMPOUND-OWNER-001` | Adding a carry field without material custody would permit substitution, omission, independent publication, or repair outside the Stage 3 owner/receipt chronology. | Seal unchanged Stage 3 material plus ordered high/carry as one authenticated compound owner and additive carry receipt; stabilize the whole CN/carry receipt set, replay independently, and finalization-compare exactly. | Owner/receipt schema/support/transaction/predecessor/lane/order/base-digest/operand/branch/topology/custody substitutions; Q double-use; replay/finalization mismatch; rollback/no-publication. |
| `V56-RESTART-001` | Current V4 restart has no snow enthalpy carry and cannot truthfully restore compound committed, pending, or in-progress state. | Add versioned V5 compound-owner/carry persistence for committed, pending, in-progress day and current support; V4 migration uses only canonical exact zero carry and V5 downgrade refuses any nonzero carry. | Exact all-posture round trips, zero-carry migration parity, nonzero downgrade, missing/foreign/reordered receipt, partial restore, rollback, and resume-replay poisons. |
| `V56-BUDGET-BRANCH-001` | A broad temperature-primary path could cross phase, add an uncharged map, bypass V55 ordering, or relax the exact floor/ledger. | Require authentic strictly frozen/noncrossing eligibility and exact static joins; dispatch before V55; charge every physical/Jacobian/trust/rejected/probe/replay map to the same maximum 96; derive H and Q without extra physics. | Exact 60-second and larger supports; phase/liquid/melt/refreeze/event/branch/density/order/static-join poisons; 96-cap accounting; unchanged physics/ledger/event/finalization evidence. |

### WGHL-FULL-001D v57 bounded-liquid eligibility failure inventory

| ID | Current adverse condition | Required corrective seam | Expected-red proof |
|---|---|---|---|
| `V57-ELIGIBILITY-RESIDUE-001` | R147 proves V56 remains zero-charge ineligible solely on an immutable `2.7404676319867775e-15..5.480935263973555e-15 kg m^-2` external-liquid operand, far below the existing `1.0e-12 kg m^-2` terminal physical snow-closure floor; the unchanged legacy/V55 path then fails at the known no-witness limit. | Make finite nonnegative external liquid through `1.0e-12 kg m^-2` eligibility-neutral only, retaining every exact mass/latent/receipt/closure operand and allowing only its exactly matched complete refreeze into unchanged frozen material. | Retained r147 SHA-256 `1b95e317d65cf831933ef7778f20c7295ef2e590a199b986fcfee8dc97b759fc`; exact zero/equality/one-bit-above/negative/nonfinite and matched-refreeze vectors; bit-exact independent ledger reconstruction. |
| `V57-POST-ROOT-TRANSITION-001` | The pre-seed V56 check can miss a lawful temperature-primary posture that is established only by the first tolerance-closed legacy root at shared budget 63, after which V55 consumes the remaining Q interval without a witness. | Permit a zero-charge coordinate conversion from that root immediately before V55, retain `used=63` and the maximum 96, commit before the first V57/V56 physical map, and prohibit fallback once charged. | Exact posture/budget transition, zero extra callback/charge, V55 callback zero after commit, zero-charge poison continuation, post-charge no-fallback, canonical DFF rerun. |

The same contract-first sequence applies to `WGHL-FULL-001F` under
`SC-LANDSURFACEENERGY-001`: amend authority, author the no-update witness and
refusal tests, record the expected pre-implementation failure, then edit the
covered solver. No convergence threshold or phase-domain amendment is allowed.

`WGHL-FULL-001I` independently requires `SC-SURFACELIQUID-001` v15
`INV-SURFACELIQUID-022` and `SC-LANDSURFACEENERGY-001` v15
`INV-LANDSURFACEENERGY-150`, contract-derived tests, and a retained isolated
expected-red on unchanged production before any carry/schema implementation.
Blocking evidence includes the exact canonical WAT5 sub-ULP operand, signed and
tie-even/crossing/cancellation/order/subnormal/overflow vectors, V1 byte locks,
V1-to-V2 migration and downgrade poisons, typed receipt identity/cardinality/
order/substitution poisons, restart split equivalence, exact rollback, and real
WAT5 plus unchanged p61/native-forest successor consumers.

`WGHL-FULL-001G` remains outside this package's direct write set because it
crosses a distinct canonical phase/state/restart authority. This package must
not waive it as historical: it owns executing the named successor and consuming
its stable passing handback before workspace closure.

Conservation/output acceptance for `WGHL-FULL-001E` requires the prospective
operand-lineage table, anti-tautology fixtures, explicit rejection of adjacent
aliases and known wrong formulas, independent output reconstruction, real
per-OFE/adjacent-transfer/hillslope cancellation and WAT5 magnitude/closure
evidence, and metadata/schema alignment. Producer-only or self-consistency
evidence cannot close the real consumer claim.

The mechanical vegetation Clippy correction retains
`SC-VEGETATION-001#INV-VEGETATION-123/124/127` and
`SC-VEGETATIONTRANSACTION-001#INV-VEGTRANSACTION-009/010/013`; affected V11
restart/custody authority tests and exact A0 admission are mandatory.

## Exit criteria

- warnings-denied workspace Clippy exits zero on the exact clean terminal source;
- every test started by the full profile passes on that same source, with
  skipped inventory separately reconciled against profile configuration;
- no test assertion, timeout, fixture, authority binding, or failure policy is
  weakened to obtain the pass;
- all attributable failures have focused regression evidence;
- the terminal diff stays inside the amended prospective write set and every
  applicable instruction/line-count requirement is dispositioned;
- dual review and dual verification accept the direct gate evidence and final
  package status.

## Review, verification, and security

Subagent authorization: this package explicitly authorizes subagent
spawning/delegation to `comparator_suite_runner` subagents for baseline/current
full-workspace and Clippy execution, triage/worker subagents for bounded failure
families, two independent review subagents, and two independent verification
subagents. Comparator outputs are compact metrics, exact identities, failure
inventories, and retained log paths with read-only source access. Triage outputs
are bounded diagnoses or prospective inventory entries; worker write access is
limited to explicitly assigned files. Review and verification write access is
limited to the assigned package artifact. Standing user/session authorization
was supplied on 2026-08-30.

Security impact is `NONE` unless exact triage discovers a security/protected-data
surface; such a discovery requires explicit package amendment before edits.

## Gate non-deferral

The two mandatory failing commands are the package objective and cannot be
deferred, waived, or converted to a narrative handoff. Continue through safe
in-envelope repair while any classified correction remains available.
