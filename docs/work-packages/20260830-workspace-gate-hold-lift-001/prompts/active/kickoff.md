# Execute the workspace gate hold-lift

Status: `SUSPENDED — R151 DISPOSITIONED — DO NOT RESUME`

Owner stop: stop after r151. Do not authorize or implement V58 or any further
exact-witness, receipt-lattice, carry, or eligibility successor. Preserve the
current worktree and record V56/V57 as unresolved; this package makes no
completion or hold-lift claim. Further solver work requires a replacement
numerical/runtime package that first establishes representative runtime budgets
and physically justified convergence tolerances while retaining Lane D MOFE,
RHESSys-derived native-vegetation ET, and the new non-CoE Stage 3 snow.
That separately authorized successor is
`20260901-stage3-native-vegetation-laned-watershed-throughput-recovery-001`;
execute it from its own kickoff and never resume work from this prompt.

Scope: local repository engineering work using flat-file reads/edits and local
commands only; no external systems or network actions are required.

Execution mode: suspended after r151; retained only as historical execution
context for the replacement-package intake.

Phase plan: stopped after truthful r151 disposition. Do not continue any prior
phase from this prompt.

Required reading:

- Core: `AGENTS.md`, `docs/codex_exec_plans.md`,
  `docs/defect_closure_execplans.md`, `docs/work-packages/AGENTS.md`, this
  package, and `artifacts/required-reading-map.md`.
- Conditional: `crates/AGENTS.md` and `tests/AGENTS.md` before Rust/test edits;
  nearest nested `AGENTS.md` for every classified path;
  `docs/standards/kernel-work-package-preparation.md`,
  `docs/specifications/science-contract-authoring-procedure.md`,
  `docs/specifications/science-contracts/kernel-process-contract-profile.md`,
  and `docs/specifications/science-contracts/index.md` before the amended
  kernel/contract correction.
- On demand: owning module README/contracts and the predecessor fixed-point
  package's review/verification/gate artifacts; `SC-SNOWENERGY-001`,
  `SC-OUTPUT-WAT5-001`, `SC-RUNOFFPART-001`, `SC-WATBAL-001`, and
  `SC-SYSTEM-001` for their amended correction phases; and
  `SC-LANDSURFACEENERGY-001` for the covered-solver witness correction.

Required-reading budget: 137,872 bytes for the initial governance map,
`REQUIRES-JUSTIFICATION`; the full testing/work-package standards are required
because this package owns a critical cross-workspace hold-lift. Map:
`artifacts/required-reading-map.md`.

Historical task, now suspended: close `WGHL-CLIPPY-001` and
`WGHL-FULL-001` end-to-end. The replacement numerical/runtime package, if
separately authorized, must define its own task and authority before edits.

Constraints: no test weakening, silent waiver, retry-to-pass, surrogate
physics, tolerance relaxation, authority bypass, or TESTGATE/planner work.
For `WGHL-FULL-001D`, contract-first sequencing is mandatory and the only
admitted numerical change is the package's candidate-local unpublished-iterate
domain safeguard; the 60-second floor, equations, tolerances/cap, conservation,
phase/event chronology, discrete state, custody, topology, receipt, rollback,
and authentic-only acceptance remain exact.

For `WGHL-FULL-001F`, contract-first sequencing is also mandatory. The only
admitted change extends the existing no-update convergence witness to the first
domain-valid halved candidate after the full trial is domain-invalid or fails
any unchanged prospective-step threshold; current residuals and that candidate's
component steps must independently pass unchanged thresholds. No trial is
installed, and all actual updates retain strict residual decrease.

No surrogate physics: use only the amended canonical fixed-point transition
authority and existing authentic constitutive map; no proxy, clamp, replacement
temperature, heuristic heat term, or provisional process equation is allowed.

Conservation/output acceptance: for `WGHL-FULL-001E`, complete prospective
operand lineage, anti-tautology tests, explicit wrong-formula rejection,
independent reconstruction, real transfer/WAT5 closure and magnitude evidence,
and metadata/schema alignment. Real consumer proof must show the public runner
reads the corrected accepted path and no wrapper/shadow/compatibility path is
carrying the claim.

Subagent requirement: suspended with the package. The prior authorization is
retained as historical execution context only and does not authorize any
post-r151 dispatch from this package.

Outputs: preserve the current worktree and active prompt in suspended state.
Do not archive, commit, or push as a package-completion action.
