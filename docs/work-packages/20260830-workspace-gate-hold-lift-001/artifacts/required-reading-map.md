# Required reading map

Status: `COMPLETE`

Evidence mode: `Static`

Initial instruction discovery used:

`tools/agents/find-agents --for docs/work-packages/20260830-workspace-gate-hold-lift-001/package.md crates/openwepp-coupled-time/src/event.rs crates/openwepp-biogeochemistry/src/lib.rs tests/integration`

Applicable chains are root plus `docs/work-packages/AGENTS.md` for package
artifacts, root plus `crates/AGENTS.md` for the two initial Rust sources, and
root plus `tests/AGENTS.md` for integration tests. Nearest instructions must be
rediscovered and recorded before any newly classified path is edited.

The newly exposed coupled-time authority test path resolves to root plus
`crates/AGENTS.md`; that chain was rediscovered before authorizing
`WGHL-CLIPPY-001C`.

The runner relative-path test resolves to root plus `crates/AGENTS.md`; that
chain was rediscovered before authorizing `WGHL-FULL-001B`.

The generation-chain integration test resolves to root plus `tests/AGENTS.md`;
the impact map resolves to root. Those chains were rediscovered before
authorizing `WGHL-FULL-001A-CHAIN` and `WGHL-A0-001`.

The P102 fixture resolves through root, `tests/AGENTS.md`, and
`tests/fixtures/AGENTS.md`; the runner authority test resolves through root and
`crates/AGENTS.md`. Those chains were read before `WGHL-FULL-001C` was
authorized.

The vegetation Clippy continuation resolves to root plus `crates/AGENTS.md`.
Before authorization, the science-contract `AGENTS.md`, mechanical-refactor
guide, and the applicable restart/custody bindings in `SC-VEGETATION-001` and
`SC-VEGETATIONTRANSACTION-001` were read. The edit is mechanical-only and does
not amend canonical authority.

The `WGHL-FULL-001D` contract/kernel amendment resolves through root,
`docs/specifications/science-contracts/AGENTS.md`, `crates/AGENTS.md`, and
`tests/AGENTS.md`. The kernel work-package preparation procedure, science
contract authoring procedure, kernel-process profile, contract index, and the
exact fixed-point/lower-boundary portions of `SC-SNOWENERGY-001` are mandatory
before its contract edit. The complete contract is on-demand authority for the
worker and both independent reviewers.

The `WGHL-FULL-001E` publication correction resolves through root,
`crates/AGENTS.md`, and `tests/AGENTS.md`. `SC-OUTPUT-WAT5-001`,
`SC-RUNOFFPART-001`, `SC-WATBAL-001`, and `SC-SYSTEM-001` are mandatory
on-demand authority for its assigned worker and reviewers. The prospective
operand-lineage artifact is a precondition to production edits.

`WGHL-FULL-001F` resolves through root,
`docs/specifications/science-contracts/AGENTS.md`, and `crates/AGENTS.md`.
`SC-LANDSURFACEENERGY-001` version-12 closed-bound and `INV-108` numerical
authority, the kernel-process profile, and contract authoring procedure are
mandatory before its contract/test/solver sequence.

Ran: the 001F worker read the complete applicable root, science-contract,
crate, and work-package instruction chains; the complete
`SC-LANDSURFACEENERGY-001` numerical authority; the science-contract authoring
procedure; and the kernel-process contract profile before the v13 contract,
test, and production sequence. No additional instruction surface was needed
for its private inline tests or unchanged real-consumer execution.

The initial governance reading totals 137,872 bytes and is
`REQUIRES-JUSTIFICATION`: this package owns a critical cross-workspace
correctness hold-lift, so the full work-package, defect-closure, validation,
prompt, crate, and test rules cannot be moved to on-demand reading.

The amended conditional/on-demand budget will be recomputed after terminal
write-set reconciliation; the initial threshold disposition remains
`REQUIRES-JUSTIFICATION` because the contract and publication corrections are
critical and conservation-sensitive.

## V56 frozen temperature-primary amendment

Ran: before the V56 contract/docs-only phase,
`tools/agents/find-agents --for` was run for the canonical snow contract, this
package and manifest, and the snow source-bound integration contract. The
applicable chains were root plus
`docs/specifications/science-contracts/AGENTS.md`, root plus
`docs/work-packages/AGENTS.md`, and root plus `tests/AGENTS.md`. The worker read
those instructions, the complete active package, the science-contract
authoring procedure, kernel-process profile, canonical testing/gate strategy,
and this required-reading map before editing. Production crate instructions
remain mandatory for the separately dispatched implementation phase; no
production Rust file is edited by this contract-first checkpoint.

## V57 bounded-liquid eligibility amendment

Ran: before the V57 contract/docs-only phase,
`tools/agents/find-agents --for` resolved the canonical snow contract, active
package artifacts, source-bound integration test, and production solver seam
through the root plus their nearest science-contract, work-package, test, and
crate instructions. Those previously read instructions remain active. A
read-only delegated tolerance audit additionally traced the existing terminal
physical snow-closure floor and every exact external-liquid ledger seam before
the amendment. Production remains frozen until the isolated V57 expected-red
is recorded.
