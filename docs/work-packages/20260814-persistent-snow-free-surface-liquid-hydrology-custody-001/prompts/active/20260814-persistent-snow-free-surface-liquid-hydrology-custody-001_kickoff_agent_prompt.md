# Execute Persistent Snow-Free Surface-Liquid Hydrology Custody

Start at exact clean `main` commit
`af9a989063aa8751dfadb14c442e1b360653658c`. Preserve the existing campaign
and Child-3 HOLD artifacts as immutable history. Admit and implement the actual
hydrology-owned per-OFE/tile/surface-class liquid state, strict restart lineage,
immutable snapshot, D/A/F debit, signed condensation credit, capacity and
post-solve ingress routing, validation and rollback. Then resume the existing
held Child 3 and campaign; do not replace them.

Production selection, defaults, legacy ET, publication, snow ownership,
activation, calibration, deployment, branch/PR creation and push are prohibited.

Subagent authorization: this prompt explicitly authorizes and requires an
independent hydrology/ownership reviewer, Rust correctness reviewer, comparator
runner and two terminal verifiers with the read/write limits in `package.md`.
