# Terminal Diff Reconciliation

Evidence class: `Static`

Status: `in progress / test-only heavy-gate correction not yet frozen`

The current diff reconciles to the declared dependency-lift objective:

- v5 authority clarification binds actual production receivers, canonical
  failure payloads, strict restart combinations, and independent closure;
- owner hardening seals candidate construction and canonical persistence;
- ingress and closure retain typed mass/enthalpy recipients and reconstruct all
  joins independently;
- production and continuation WB14 use one complete transition;
- the unified bridge hashes canonical production bytes and returns the complete
  LSE, hydrology, and soil-thermal receiver set from cloned state; and
- tests and impact-map entries bind every changed science surface.
- the second correction increment independently reconstructs all actual
  receiver endings, seals all candidate/arbitration envelopes, re-derives
  proportional authorization, validates canonical state emission, and makes
  every applicable typed failure branch executable.

The two new Rust modules and every modified path are inside the amended intended
write set. No unrelated source change is present. The only post-review source
delta is a semantic-neutral strict-Clippy correction in five package-owned
integration-test files; the failed lint output and successful focused retry are
both retained. Final hashes and commit identity will be recorded after the
correction is frozen and the invalidated exact-head gates pass.
