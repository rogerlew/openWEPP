# Production Selector Exclusion

Evidence class: `Static`

The implementation diff contains no file under `crates/openwepp-runner/**` and
introduces no selector, default, production output, or production scheduler
reference.

The new owner is absent by default (`surface_liquid_shadow = None`) and is
reachable only after an explicit shadow caller supplies and validates complete
configuration and state. Legacy depression, PMET/ET, snow ownership, and
production hydrology execution are unchanged.

Repository search found the new public unified bridge and surface-liquid state
only in the orchestrator/LSE implementation and focused integration tests; no
runner path references them.
