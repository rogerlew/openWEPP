# Worker Handoff

Status: EXECUTED-HOLD-COHORT-AUTHORITY. Evidence mode: Static + Ran.

## First Actionable Follow-On

Scaffold `D16-HYB-COHORT-AUTHORITY`.

Objective: construct an executable active plain-vs-hybrid promotion cohort
before any default selector flip.

Required first implementation action:

- Select cohort members from the existing owcmp inventories plus H2637.
- Produce source-authorized openWEPP run inputs for active Lane-D, including
  `routing_coefficients` from native management authority.
- Add an executable comparator/timing suite manifest for active plain and
  active explicit hybrid.

Required gates:

- `tools/owcmp/owcmp env --manifest` and executable suite run.
- Active plain and active explicit hybrid for every cohort member.
- HBP/pass/WAT publication deltas, routed outlet/tail/storage/clamp surfaces,
  active closure residuals, and timing/counters.
- Anti-evasion guards if required-case bindings, cohort fixtures, or
  external-authority suite posture are touched.

Do not return to D16 default-promotion selector changes until this cohort
exists and passes.
