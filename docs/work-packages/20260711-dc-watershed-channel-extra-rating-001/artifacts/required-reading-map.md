# Required-reading map

Status: active
Evidence mode: Static

Applicable chains were resolved with `tools/agents/find-agents --for` for every
declared write path. Core reading is root/work-package governance, the queue
ExecPlan, DC procedure, contract procedure/profile, prompt guidance, ADR-0012,
ADR-0021, target contract/spec/parser/tests/fixtures, and pinned legacy files.
Network-frame and WSHED-W5 sources activate on demand for consumer
non-regression proof.
