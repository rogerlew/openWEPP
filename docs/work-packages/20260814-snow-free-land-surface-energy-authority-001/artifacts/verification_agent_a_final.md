# Terminal Verification A: Final Science And Numerics Verification

Evidence class: `Static + prior targeted Ran` against the exact current
worktree.

Verdict: **PASS / terminal authority release eligible**.

No material science, numerical, digest, fixture, reference-custody,
owner-closure, rollback, evidence-lifecycle, or protected-runtime finding
remains. `TVA-HIGH-001`, `TVB-HIGH-001`, and `TVA2-HIGH-001` are fully
corrected without changing any constitutive or machine-readable authority
byte or rewriting any historical failed review/checkpoint evidence.

## Exact immutable authority

The final exact hashes remain:

| Surface | SHA-256 |
| --- | --- |
| LSE V1 definition | `e1736b8c77d13d6fb12fb97a6f747e54eea877edf237817b6c6e8954cff8332f` |
| C3 woody V8 definition | `622bc900a08bd4c70e67c09e1fa113a9de24c48afce3b145a494bb76f6dcbe9b` |
| independent calculator | `1156fa88a6d7e4dd98f6dd70fe5b891f69e0b6825694179ac4d687a38907c859` |
| joint canopy-ground core | `c9555b2dd02a5d6f11d71eb923fb60bc882e9638ec20eb79accc96cec9018be5` |
| independent vectors | `7b6a303ae434ca6ad59c7082ebf486300214427d6abe20c36bfaa9b8cbdab91c` |

The current contract file hashes remain LSE `67b51fde...`, vegetation
`1d7ec369...`, and vegetation transaction `44b77e5d...`. Their named canonical
sections remain LSE `9302913b...` and V8 `50e09597...`, exactly equal to the
definition bindings. The six schemas remain configuration `6499b98c...`,
coupled transaction `02dfa522...`, diagnostics `41fb7909...`, forcing
`f1fb785e...`, state `91243e40...`, and water protocol `2e5ade75...`.

V8 remains an immutable successor to V7 definition `a78264d8...`. The terminal
vegetation authority gate previously reran 26/26 on the current protected
model chain, including V1--V7 preservation and the V8 successor digest. No
historical model definition was rewritten.

## Scientific and numerical completeness

The admitted `OPENWEPP_SNOW_FREE_LSE_V1` authority remains complete for its
explicit supported domain:

- bare-mineral and forest-litter surface classes;
- source-resolved two-stream shortwave with reciprocal current-temperature
  V8 longwave;
- neutral open and canopy-covered heat/vapor networks with no wind floor;
- hydrology-exclusive water mass, one LSE surface enthalpy state, and
  soil-thermal layer ownership;
- signed evaporation/condensation with one liquid-plus-latent enthalpy;
- finite-capacity and exact equilibrium-zero surface branches;
- arbitrary-layer Crank--Nicolson soil heat;
- source-carried precipitation, canopy release, runon, infiltration, and
  runoff enthalpy;
- immutable-beginning potential requests, one authorization, and fixed-cap
  rebuild from original beginning state; and
- exact ordered unknowns/residuals, finite differences, pivots, backtracking,
  tolerances, error precedence, diagnostics, and atomic rollback.

The independent evidence retains 22 mandatory exact cases, 76/76 rejecting
poisons, and 11 typed null-candidate failures. Every failure retains identical
before/after hashes for vegetation, hydrology, LSE, soil thermal, BGC, and the
transaction envelope. The physical transaction has five nonempty owner
candidate bodies and five independently reconstructed receipts, including a
nonempty material path.

The decisive soil-thermal correction remains exact:

```text
291.5 + (4766.427866774984 + 53529.68923674751) / (120000 * 0.62)
= 292.28354996106884 K.
```

The first soil node consumes both ground heat and infiltration enthalpy;
omission, duplication, wrong-node, and wrong-area-basis alternatives reject.
All accepted `A`, `A2`--`A6`, `OWN`, and `OWN2`--`OWN6` review findings remain
closed by the exact-byte dual terminal reviews.

## Verification-finding closure and historical preservation

The initial verifier reports remain immutable FAIL evidence. The first
remediation report likewise remains immutable FAIL evidence for the one
residual sentence it found. The review-finding disposition now records all
three findings as accepted and corrected, while historical review tables keep
their checkpoint-specific `in remediation` or `confirmation pending` states.
Those cells are explicitly superseded only by the final closure matrices;
they were not rewritten into retrospective passes.

Current, nonhistorical lifecycle prose is now internally consistent:

- amendment and contract-test evidence report exact-hash PASS;
- the oracle manifest calls the terminal hashes independently confirmed;
- owner, custody, advection, equation, and schema summaries report confirmed
  terminal authority;
- the schema index now says its digests are frozen and bound rather than
  awaiting stabilization;
- review disposition reports the heavy-gate PASS while preserving historical
  rows; and
- final disposition and handoff remain correctly in progress only until the
  final verifier pair returns PASS and the prompt is archived.

The bounded lifecycle corrections changed human-readable current-status prose
only. `gate-results.md` preserves both comparator service-capacity failures,
the initial terminal verifier failures, the residual verifier failure, and all
subsequent reconciliation gates. Package Markdown passed 48/48, and formatting
and diff hygiene passed after the correction.

## Heavy evidence, references, and protected boundary

The heavy evidence remains truthful: strict workspace Clippy PASS after a
preserved test-only failure, full workspace nextest 2,674/2,674 PASS under an
external short `TMPDIR`, doctest invocation PASS, dependency policy PASS, and
baseline-relative documentation PASS with exactly the frozen 15 unrelated
broken links and zero warnings. Neither failed comparator attempt is
misrepresented as a comparator PASS.

The four committed Copernicus references retain matching acquisition hashes
and CC-BY-3.0/4.0 custody. Restricted CLM5 bytes remain gitignored under
`references/copyrighted/` and match their recorded checksum.

No file below `crates/`, Cargo manifest/lockfile, runner selector, production
dispatch, default, mutable production state, or output-publication path is
part of this authority release. The Python hydrology arbiter is correctly
classified as Child-1 authority evidence, not the actual production owner.
Child 2 retains real-hydrology proof; Child 3 retains Rust constitutive runtime
implementation. No activation, cutover, calibration, empirical validation, or
transferability claim is made.

## Conclusion

The exact current Child-1 bytes satisfy the declared authority exit boundary.
All material reviews and verification findings are dispositioned and closed;
the immutable definitions, oracle, schemas, fixtures, and scientific
contracts remain unchanged through lifecycle remediation.

**Result: PASS / `COMPLETE / snow-free land-surface-energy implementation
authority released` is a truthful terminal disposition after the independent
verifier pair completes and the prompt is archived byte-for-byte.**
