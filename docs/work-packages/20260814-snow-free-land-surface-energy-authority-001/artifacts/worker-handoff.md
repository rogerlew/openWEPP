# Worker Handoff

Authority release is complete. Child 2 must preserve the exact shared-water
transaction and V8 successor identity while extracting the real production
hydrology owner into a behavior-neutral default-off shadow. Child 3 owns the
constitutive LSE runtime and must implement the exact LSE V1 equations,
schemas, numerics, fixtures, and failure posture without substituting existing
meteorology helpers as authority.

The handoff does not authorize production mutation, selector/default changes,
runtime activation, output publication, deployment, calibration, or cutover.
