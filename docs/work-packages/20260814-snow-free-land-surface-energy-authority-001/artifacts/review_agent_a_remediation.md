# Fresh Land-Surface Science Re-Review

Evidence class: `Static + Ran` exact-worktree review.

Reviewed contract/oracle candidate identities:

- `SC-LANDSURFACEENERGY-001.md`: `db5843c1...`;
- `SC-VEGETATION-001.md`: `7e6b5165...`;
- `SC-VEGETATIONTRANSACTION-001.md`: `44b77e5d...`;
- calculator: `c8467bb0...`;
- joint core: `095d5fcb...`;
- vectors: `94481bee...`.

Ran: the committed vectors regenerated byte-identically.

Verdict: **NO-GO / FAIL**. Canonical text is substantially improved, but the
independent authority evidence does not yet execute the complete admitted
model.

## Findings

### `A2-CRITICAL-001` — Mandatory vectors use a noncanonical surrogate

`reference_calculator.py:362-507` solves only surface temperature, canopy-air
temperature and canopy-air humidity. It uses hard-coded resistances and canopy
states, one fixed soil temperature, different numerical rules, and no FvCB,
Medlyn, plant hydraulics, root-request generation or cap-active canopy
re-solve. Most mandatory open, bare, litter, multirank, multitile, scarcity,
advection and warm-start vectors use that path. The exact joint core covers
only one covered litter tile with one occupancy. Those vectors also send raw
top-of-column shortwave directly to ground instead of using V7/V8 terminal
canopy radiation.

### `A2-CRITICAL-002` — Post-ingress thermal owner transaction is incomplete

`reference_calculator.py:545-596` computes mixed liquid temperature but does
not update LSE ending enthalpy, construct the soil-thermal infiltration
candidate, or close the complete receiver joins. Infiltration receipts name
hydrology, and hydrology/soil-thermal candidates remain null.

### `A2-HIGH-003` — Failure, poison and transaction evidence is asserted

Most typed failures construct labels instead of executing validation. Poisons
are scalar inequality records rather than rejected counterfactual executions.
`final_rebuilt_from_beginning` and `single_immutable_authorization` are literal
booleans. A real singular system and joint backtracking-limit vector are absent.

### `A2-HIGH-004` — Equilibrium-zero storage vector contradicts authority

The emitted literal `U0=U1=125000` branch does not execute the canonical
`C_dry=W=U_s=0` algebraic-temperature case.

### `A2-HIGH-005` — Strict schemas are not bound to positive vectors

Oracle forcing/state/candidate records do not instantiate the six normative
schemas. Required identities, digests and owner receipts are missing, and no
complete positive configuration/state/forcing/water/diagnostic/envelope
fixture validates against the schemas.

## Initial-Finding Reassessment

| Initial finding | Reassessment |
|---|---|
| `A-CRITICAL-001` | not corrected |
| `A-CRITICAL-002` | canonical text corrected; evidence incomplete |
| `A-CRITICAL-003` | not corrected |
| `A-CRITICAL-004` | canonical equations corrected; closure evidence insufficient |
| `A-CRITICAL-005` | not corrected |
| `A-CRITICAL-006` | canonical ordering corrected; evidence insufficient |
| `A-CRITICAL-007` | not corrected |
| `A-HIGH-008` | corrected |
| `A-HIGH-009` | not corrected in evidence |
| `A-HIGH-010` | not corrected |
| `A-HIGH-011` | corrected |
| `A-MEDIUM-012` | corrected |

Release remains blocked until exact mandatory joint vectors, complete
post-ingress owner candidates, executed typed failures/poisons, and
schema-conforming positive fixtures pass fresh review.
