# Verify Frozen LSE Canonical JSON Portability

Preserve all physics, model-definition and schema bytes. Bind the two-digit
exponent lexical form already used by the authority and correct the stale
strict-positive-state digest only after its final projection. Prove Rust
reproduces the positive configuration/state digests without rewriting string
content; prove the fixture changes only in the state digest copies and their
dependent instance hashes; then repeat focused independent review and terminal
verification. No runtime activation, selector change or push.
