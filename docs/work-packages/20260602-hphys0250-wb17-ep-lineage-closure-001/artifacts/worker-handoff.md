# Worker Handoff

Status: HOLD

Evidence mode: static + ran

Completed in HPHYS0250:

- Contract-first amendments for final `Ep` lineage, established-perennial
  initial live-canopy assimilation, and WB15 near-zero publication.
- Production fixes for PL activation preservation, zero-date perennial dispatch,
  initial canopy PL state, growth/decomposition transition writeback, final
  WB13 `Ep` publication, and WB15 roundoff normalization.
- Targeted tests, workspace gates, anti-evasion guards, and full 39 suite ran.

Key run roots:

- Full 39 suite: `/tmp/hphys0250_20260602T175731Z`.
- H1 probe: `/tmp/hphys0250_h1_probe_20260602T175631Z`.
- H1 trace: `/tmp/hphys0250_trace_probe_20260602T180043Z`.

Continuation recommendation:

- Scaffold next work-package for baseline-authoritative `swu.for` actual root
  uptake and water-stress magnitude closure, using H1/H13/H39 as diagnostic
  anchors and the HPHYS0245 trace fields for `Etp`, `UPi`, `Ui`, `Ep`,
  `Ws`, `rtd`, and layer storage.
- Keep disposition `HOLD` until `Ep` magnitude and aggregate storage
  residuals are materially reduced without heuristic tuning.

Review-disposition carry-forward:

- Consider centralizing or cross-testing runner PL activation and scheduler
  active-slot selection to prevent future divergence.
- Add explicit established-perennial `rdmax > solthk` evidence when initial
  state migration expands beyond the management-only projection seam; current
  growth transition caps by `min(rdmax, solthk)` before ET/root uptake.

Follow-up completed 2026-06-02:

- Installed `pandas==3.0.3` into repo-local `.venv` after bootstrapping pip with `ensurepip`.
- Added `AGENTS.md` guidance so agents discover `.venv/bin/python` and know `.venv` is local/untracked.
- Added a WEPPpy corn/no-till growth coefficient projection regression anchored to `tests/fixtures/infile/management/canonical_cropland_nonzero_98_4.man`, whose provenance is `Agriculture/corn-no till.man` in `/workdir/wepppy/wepppy/wepp/management/data`.
