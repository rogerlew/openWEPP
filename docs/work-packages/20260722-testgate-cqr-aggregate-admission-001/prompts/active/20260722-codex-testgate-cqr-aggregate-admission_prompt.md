# Kickoff: TESTGATE CQR Aggregate Admission Authority

Execute `20260722-testgate-cqr-aggregate-admission-001` end-to-end in
`/home/workdir/openWEPP`. Preserve immutable package authority; do not weaken
package validation or claim retroactive authorization. Correct the CQR
workflow canonically, run narrow validation, obtain dual independent review,
close RTR-031 only after the exact correction commit exists, then return to the
recovery campaign for one delegated changed-head qualification attempt.

Subagent authorization: this prompt explicitly authorizes subagent
spawning/delegation to two read-only implementation reviewers, one comparator
runner, and two read-only terminal verifiers for the scopes and artifacts in
`package.md`. Do not push, deploy, switch branches, manually dispatch TESTGATE,
run HEAVY locally, or rerun an unchanged expensive gate.
