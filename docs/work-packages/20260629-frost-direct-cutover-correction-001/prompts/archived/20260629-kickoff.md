# Kickoff Prompt

Scaffold and execute FROST DIRECT CUTOVER CORRECTION.

Operator correction: preserving compatibility fallback for multi-OFE/Wave-2 and
legacy sidecar-discovery runs was an oversight. The frost validation arc was
intended to retire the compatibility runtime boundary. Correct the contract and
runtime so default candidate selection does not fall back to compatibility for
those surfaces. Fix exposed direct-runtime blockers or close HOLD with a named
blocker; do not silently preserve compatibility fallback.
