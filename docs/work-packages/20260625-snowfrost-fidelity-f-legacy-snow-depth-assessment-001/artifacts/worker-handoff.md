# Worker Handoff

Evidence mode: Static + Ran.

SNOWFROST-FIDELITY-F is complete.

Next package:

- Scaffold and execute SNOWFROST-FIDELITY-G as the snow-depth
  producer/carry/input/settlement DC.

Use this package's evidence as follows:

- Treat pinned legacy WEPP as source-line guide and flag evidence only.
- Do not pursue legacy snow-depth bit parity as a target.
- Start with depth-producing mechanisms rather than WAT SWE publication: current
  openWEPP SWE is close to legacy SWE, while physical depth still fails
  observed snow-depth control.
- Preserve the capture distinction:
  - legacy WAT `Snow-Water` = SWE;
  - legacy daily-winter hour-24 rows = dated physical snow depth and density;
  - legacy large graphics `treal(73)`/`treal(75)` = sparse operand provenance.

Required first action for SNOWFROST-FIDELITY-G:

- Author contract-first source-line lineage for snowpack state/carry,
  snowfall-depth input, density/settlement, rain-on-snow storage, melt
  depletion, and publication before any production code edit.
