{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "openwepp:assurance:v2:principals:2",
  "title": "openWEPP assurance principal registry",
  "type": "object",
  "additionalProperties": false,
  "required": ["schema_version", "trust_domain", "principals"],
  "properties": {
    "schema_version": { "const": 2 },
    "trust_domain": { "enum": ["production", "test_only"] },
    "principals": {
      "type": "array",
      "minItems": 1,
      "items": { "$ref": "#/$defs/principal" }
    }
  },
  "$defs": {
    "id": { "type": "string", "pattern": "^[A-Za-z0-9][A-Za-z0-9_.:-]*$" },
    "text": { "type": "string", "pattern": "\\S" },
    "principal": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "id", "record_version", "supersedes", "display_name",
        "affiliations", "kind", "identity_authority",
        "identity_reference", "roles"
      ],
      "properties": {
        "id": { "$ref": "#/$defs/id" },
        "record_version": { "type": "integer", "minimum": 1 },
        "supersedes": {
          "oneOf": [
            { "$ref": "#/$defs/id" },
            { "type": "null" }
          ]
        },
        "display_name": { "$ref": "#/$defs/text" },
        "affiliations": {
          "type": "array",
          "uniqueItems": true,
          "items": { "$ref": "#/$defs/text" }
        },
        "kind": { "enum": ["human", "organization", "agent", "software"] },
        "identity_authority": { "$ref": "#/$defs/text" },
        "identity_reference": { "$ref": "#/$defs/text" },
        "roles": {
          "type": "array",
          "minItems": 1,
          "uniqueItems": true,
          "items": { "$ref": "#/$defs/id" }
        }
      }
    }
  }
}
